# MemBook 开发日志

> 记录 AI 辅助开发的功能情况，按时间倒序排列。

---

## 2026-08-05 · 修复拖拽放置照片后偏移抖动，优化入场动效

### 问题
用户反馈：拖拽照片到照片位后，照片位置发生偏移抖动，效果不够优雅。

### 根因分析
拖拽入场动效与智能定位（内容感知裁切）存在时序冲突：

1. **Drop 发生** → `placePhoto()` + `markPhotoJustPlaced()`
2. **首次渲染**：`getCachedContentInfo()` 缓存未命中 → **居中位置**
3. **useLayoutEffect**：动画启动，设置 `offset=(imgW/2, imgH/2)` 实现中心缩放
4. **useEffect**：`ensurePhotoAnalyzed()` 异步触发（~50-200ms）
5. **分析完成** → `setContentVersion(+1)` → 重新渲染
6. **重新渲染**：缓存命中 → 智能定位 → `imgX/imgY` 变化
7. react-konva 检测到 `imgX` prop 变化 → `node.x(newImgX)`
8. 但 `node.offsetX()` 仍是 `imgW/2`（动画设置的中心偏移）→ **视觉位置 = newImgX - imgW/2 ≠ newImgX** → **第一次抖动！**
9. **动画结束**：`resetToProps()` → offset=0, x=imgX → **第二次抖动！**

核心：动画设置 `offset=(imgW/2, imgH/2)` 做中心缩放，但智能定位在动画中途改变了 `imgX/imgY` prop，导致 offset 和 x/y 不同步。

### 修复（2 项）

**1. pendingAnalysis 从 Set 改为 Map<Promise>**
- [content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)：`pendingAnalysis` 从 `Set<string>` 改为 `Map<string, Promise<PhotoContentInfo | undefined>>`
- 之前：第二个调用者（动画 useLayoutEffect）直接返回 `undefined`，无法 await 已有的分析
- 现在：多个调用者共享同一个分析 Promise，都能 await 到最终结果

**2. 动画启动前等待分析完成**
- [CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx) useLayoutEffect：
  - 缓存命中（`getCachedContentInfo` 有值）→ 立即启动动画（0ms 延迟）
  - 缓存未命中 → `opacity=0` 保持不可见，等待 `ensurePhotoAnalyzed` 完成
  - 分析完成后 → `requestAnimationFrame` 确保 React 已提交 DOM → 用最终位置启动动画
  - 200ms 超时兜底：分析太慢时直接用居中位置启动
- 这样 prop 值在动画开始前就是最终值，动画期间不会发生 prop 变化 → 无抖动

**3. 动画参数优化**
- startScale: 0.6 → 0.7（减少初始缩放幅度，更柔和）
- duration: 0.45s → 0.4s（略快，更利落）
- 保留 BackEaseOut 缓动（Q弹效果）

### 时序保证
```
useLayoutEffect (before paint)
  ├─ 缓存命中? → playAnimation() 立即
  └─ 缓存未命中? → opacity=0, ensurePhotoAnalyzed().then(onReady)
                                     ↓ (分析完成 ~5-200ms)
useEffect (after paint)             onReady()
  └─ ensurePhotoAnalyzed().then(     ├─ requestAnimationFrame
     setContentVersion(+1))          │   ↓ (下一帧)
                                     │   React 已提交 DOM
                                     └─ playAnimation() 读取最新 prop
```

### 验证
- `npm run typecheck` 通过

---

## 2026-08-05 · 修复竖图全身人像放入横图槽位时人脸被裁

### 问题
用户反馈：导入的照片是全身竖图人像，拖入横图照片位后最终显示居中，只呈现身子中间部分，人脸被裁掉。

### 根因分析
竖图（如 1080×1920）放入横图槽位（如 200×100mm）时，cover-fit 按宽度缩放导致竖向溢出 255mm，显示窗口对准照片垂直中心（腰部），人脸（位于上 1/4）被裁。链路有三个根因：

1. **TinyFaceDetector 输入太小漏检小脸**：`inputSize: 224` 把全身竖图压到 224px，人脸仅 ~27px 低于检测下限 ~32px，直接漏检
2. **失败结果被永久缓存**：`ensurePhotoAnalyzed` 把 `hasFaces=false, focusY=0.5` 的结果写入 `photoContentCache`，后续永不重试
3. **无人脸时无 fallback**：`analyzePhotoContent` 焦点硬编码 0.5/0.5，face-api 漏检后完全没有备选主体估算

### 修复（3 项）

**1. TinyFaceDetector inputSize 224→416 + 大图分区域检测**
- [content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)：`inputSize` 提升到 416，`scoreThreshold` 降到 0.4
- 全身竖图（高>宽且>800px）全图未命中时，对上半 40% 单独检测（相当于放大 2.5×），人脸在全身图中通常位于上 1/3
- `FaceApiModule.detectAllFaces` 类型签名扩展为 `HTMLImageElement | HTMLCanvasElement`，支持传入 canvas 裁剪区域

**2. 高频能量主体估算（Sobel + 8×8 网格质心）**
- 新增 `computeSubjectFocusByEnergy`：降采样到 64×64 → Sobel 梯度幅值 → 8×8 网格累加 → 取高于(均值+标准差)的格子按能量加权质心
- 覆盖 face-api 漏检（侧脸/遮挡/小脸）及无人脸主体（风景/建筑/宠物）
- 性能 ~5-12ms，无额外加载开销
- `source` 类型新增 `'energy'`，与 `'sync'`/`'face'` 区分

**3. 缓存区分失败/有效结果 + 退避重试**
- 新增 `failedAnalysisCache`：`source='sync'`（图像加载失败等）不写入 `photoContentCache`，记录到失败缓存
- 退避策略：30s 间隔，最多 3 次重试，避免连续失败时频繁重试
- `source='face'`（含无人脸）和 `source='energy'` 视为有效结果，永久缓存不再重试

### 渲染层同步更新
- [CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)：智能定位条件从 `info.hasFaces && 焦点偏离` 放宽为 `焦点偏离`（接受 energy 来源）
- [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)：同上，Worker 渲染路径同步
- [content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts) `hasSmartFocus`：移除 `hasFaces` 前置判断，改为纯焦点偏离检测

### 验证
- `npm run typecheck` 通过

---

## 2026-08-05 · 修复内容感知裁切失效（所有照片都居中）

### 问题
用户反馈"内容感知裁切"功能未生效，所有照片都居中显示。

### 根因分析（6 重缺陷叠加）

1. **P0-1 face-api 模型文件缺失**（根因）
   - `public/models/face-detection/` 目录不存在
   - `tinyFaceDetector.loadFromUri('/models/face-detection')` 404 失败
   - `faceModelLoaded = false` → `analyzePhotoWithFaces` 直接返回 syncResult
   - `analyzePhotoContent` 强制 `focusX=0.5, focusY=0.5, hasFaces=false` → 永远居中

2. **P0-2 分析完成后不触发重渲染**
   - `ensurePhotoAnalyzed` 是 fire-and-forget（返回 void）
   - 写入 `photoContentCache` 后 React 不知道
   - useEffect 依赖 `[photo.id, photo.width, photo.height]` 不含缓存
   - 首帧永远居中，必须切页再回来才能生效

3. **P0-3 所有失败静默**（无日志）
   - content-aware.ts 所有 catch 都是 `/* ignore */`
   - 模型加载失败无任何提示，用户无法排查

4. **P1-1 两套缓存不通**
   - SmartLayoutView 的本地 `contentInfoCache` 不写入全局 `photoContentCache`
   - 退出 SmartLayoutView 后编辑器拿不到预计算结果

5. **P1-2 Worker 渲染路径主体感知失效**
   - `thumbnail.worker.ts` 在 Worker 线程运行
   - `getCachedContentInfo` 访问 Worker 自己的模块实例 Map（永远为空）
   - 网格缩略图永远居中

6. **P1-3 导出前不预热缓存**
   - exportEngine.ts 没有 `ensurePhotoAnalyzed` 调用
   - 导出时缓存为空 → 导出结果永远居中

### 修复（6 项）

**P0-1 下载 face-api 模型文件**
- 从 face-api.js 官方仓库下载 `tiny_face_detector_model-shard1`（189KB）和 `tiny_face_detector_model-weights_manifest.json`（2.9KB）
- 放到 `public/models/face-detection/` 目录

**P0-2 ensurePhotoAnalyzed 返回 Promise + 分析后重渲染**
- [content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)：`ensurePhotoAnalyzed` 改为 `async` 返回 `Promise<PhotoContentInfo | undefined>`
- [CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)：
  - 新增 `contentVersion` state，分析完成后 `setContentVersion(v+1)` 触发重渲染
  - 仅当 `hasFaces && 焦点偏离中心 > 0.02` 时才触发（避免无谓 re-render）
  - `useSmartPosition` 分支引用 `contentVersion` 确保重算 `defaultPx/Py`

**P0-3 补齐 logger 日志**
- [content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)：
  - `tryLoadFaceApi` 模型加载成功/失败均加 `logger.info/warn`
  - `analyzePhotoWithFaces` 检测到 0/N 张人脸时加 `logger.debug`
  - `analyzePhotoWithFaces` 异常时加 `logger.warn`
  - 失败提示信息明确指出"请确认 public/models/face-detection/ 目录下存在模型文件"

**P1-1 统一两套缓存**
- [SmartLayoutView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/SmartLayoutView.tsx)：
  - import `ensurePhotoAnalyzed`
  - `precomputeContentInfoBatch` 分析完成后调用 `ensurePhotoAnalyzed(photo)` 同步写入全局缓存
  - 退出 SmartLayoutView 后编辑器可直接复用预计算结果

**P1-2 Worker 渲染路径传 contentInfoMap**
- [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)：`calcPhotoRenderParams` 新增 `contentInfo?` 参数，未传入时查全局缓存（主线程），传入时直接用（Worker）
- [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)：`drawPageToCanvas` 新增 `contentInfoMap?` 参数，传递给 `calcPhotoRenderParams`
- [thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts)：`ThumbnailWorkerRequest` 新增 `contentInfoMap?` 字段，Worker 接收后转为 Map 传给 `drawPageToCanvas`
- [gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)：调用 Worker 时收集 `contentInfoMap` 并传入

**P1-3 exportEngine 导出前预热缓存**
- [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)：
  - import `ensurePhotoAnalyzed`
  - 新增 `preheatContentAnalysis` 函数（并发 6 路批量分析）
  - `exportToPDF` / `exportToPNG` / `exportToJPG` 入口处先预热缓存再开始渲染

### 验证
- TypeScript 编译通过（0 错误）
- 构建后控制台应看到 `[content-aware] face-api 模型加载成功，人脸检测可用` 日志
- 检测到人脸时看到 `[content-aware] 检测到 N 张人脸: xxx, 焦点=(0.xx, 0.xx)` 日志

---

## 2026-08-04 · 修复动效导致编辑后照片显示不一致

### 问题
添加拖拽入场动效后，双击进入照片编辑模式时照片居中正确，但保存退出后照片位置与编辑时不一致。

### 根因
动画期间临时设置了 `node.offsetX(imgW/2)` 和 `node.x(cx)` 实现**中心缩放**，但动画结束后 `onFinish` 回调只调用了 `consumePhotoJustPlaced(key)`，**没有重置 imperative 值到 react-konva prop 值**。

react-konva 用 prop 缓存决定是否更新节点：
- 动画后 node 状态：`offsetX = imgW/2`，`x = cx = imgX + imgW/2`
- react-konva prop 缓存：`offsetX = 0`，`x = imgX`
- 后续 `imgX` 变化（如编辑后 pan 值更新），react-konva 只更新 `x` 不更新 `offsetX`（prop 未变）
- 导致照片偏移 `imgW/2`，编辑前后显示不一致

### 修复
**[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)**

1. 新增 `propValuesRef` 跟踪最新 prop 值（`offsetX`/`offsetY`/`imgX`/`imgY`）
   - 每次渲染同步更新：`propValuesRef.current = { offsetX, offsetY, imgX, imgY }`
   - 解决 `onFinish` 异步回调中 prop 值 stale 问题

2. 新增 `resetToProps()` 函数，在 `onFinish` 和 cleanup 中调用
   - 重置 `offsetX`/`offsetY`/`x`/`y`/`scaleX`/`scaleY`/`opacity` 到最新 prop 值
   - 视觉位置不变（`cx - imgW/2 = imgX`），但 react-konva prop 缓存恢复同步

3. cleanup 也改用 `resetToProps()`（不再用 `oldOffsetX`/`oldX` 等 stale 捕获值）

### 验证
- TypeScript 编译通过（0 错误）

---

## 2026-08-04 · 优化拖拽动效（Q弹+中心缩放）+ 修复 Tauri 事件报错

### 优化 1：拖拽放置动效三大改进

参考产品主页 `docs/MemBook-Home/index.html` 的 `drop-in` 动效设计（`cubic-bezier(.34,1.56,.64,1)` 弹簧曲线），优化 CanvasPhotoRenderer 入场动画：

**问题 1：缩放原点在左上角**
- 根因：无旋转时 Konva.Image `offset=(0,0)`，scale 相对左上角缩放
- 修复：动画期间临时设 `offset=(imgW/2, imgH/2)` + 调整 `x/y` 保持位置不变，结束后恢复

**问题 2：爆闪（照片先显示再闪烁）**
- 根因：`useEffect` 在浏览器 paint 后执行，照片先被 paint 显示，再设 opacity(0)
- 修复：改用 `useLayoutEffect`（在 paint 前同步执行），opacity(0) 在显示前生效

**问题 3：不够 Q弹**
- 根因：`EaseOut` 缓动无过冲
- 修复：改用 `BackEaseOut`（有过冲），起始 scale 0.6，duration 0.45s，实现 0.6 → 1.x → 1.0 的弹簧效果

### 优化 2：修复控制台报错 `Cannot read properties of undefined (reading 'handlerId')`

**根因**：Tauri event listener 的 unlisten 函数在组件卸载后被调用，但内部 handler 已被清理（应用关闭、StrictMode 双重挂载等），导致 `unregisterListener` 访问 `undefined.handlerId`。

**修复**：
1. 新增 [safeUnlisten](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/tauri.ts) 工具函数（try-catch 包裹 unlisten）
2. 用 `disposed` flag 确保正确的生命周期管理（setup 完成后检查 disposed，如已卸载则立即注销）
3. 所有 5 个文件统一使用 safeUnlisten：
   - [App.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/App.tsx) — onResized
   - [AppHeader.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AppHeader.tsx) — onResized
   - [WindowControls.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/WindowControls.tsx) — onResized
   - [FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx) — onResized
   - [EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx) — tauri://drag-drop

### 验证
- TypeScript 编译通过（0 错误）

---

## 2026-08-04 · 新增拖拽放置照片入场动效

### 需求
照片列表拖拽照片到照片位后，添加入场动效提升用户体验。

### 实现
首次在项目中引入 Konva.Tween 动画（项目此前所有动效均为 CSS 动画，Konva 节点无动效）。

**动效设计**：opacity 0 → targetOpacity 淡入 + scale 0.92 → 1.0 轻微缩放，300ms EaseOut

**修改文件**（2 个）：

1. **[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)**
   - 模块级 `justPlacedKeys: Set<string>` 跟踪刚放置的 `${slotId}-${photoId}`
   - 导出 `markPhotoJustPlaced(slotId, photoId)` 供 useDragDrop 调用
   - 新增动画 useEffect（声明在滤镜 useEffect 之后）：
     - 检测 key 在 justPlacedKeys 中且 loaded=true 时启动 Konva.Tween
     - **opacity 协调**：目标值与滤镜一致（有滤镜时 = filterIntensity/100，无滤镜 = 1），避免动画结束后不匹配
     - **scale 翻转协调**：targetScaleX = flipH ? -1 : 1，起始 = targetScale × 0.92
     - cleanup 时 `tween.destroy()` + 恢复目标值，防止动画中断后状态卡在中间

2. **[useDragDrop.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useDragDrop.ts)**
   - import `markPhotoJustPlaced`
   - 单张/多张 drop 成功后调用 `markPhotoJustPlaced(slotId, photoId)`

### 设计要点
- **不修改 placement 数据结构**：用模块级 Set 跟踪，避免污染历史快照和类型定义
- **仅拖拽 drop 触发**：不在自动编排/撤销重做/页面切换时触发
- **React useEffect 顺序**：动画 useEffect 声明在滤镜 useEffect 之后，滤镜先设置 opacity，动画再覆盖为 0 并 Tween 回目标值
- **多张拖拽**：每张照片各自在加载完成后触发动画，形成逐个淡入的瀑布效果

### 验证
- TypeScript 编译通过（0 错误）

---

## 2026-08-04 · 修复 CSP 违规：fetch(dataURL) 导致控制台大量报错

### 问题
控制台出现大量（8958+ 条）CSP 违规错误：
```
Fetch API cannot load data:image/png;base64,...
Refused to connect because it violates the document's Content Security Policy.
```

**根因**：多处代码用 `fetch()` 加载 `data:` URL，但 CSP `connect-src` 不允许 `data:` 协议。`data:` URL 只能通过 `img-src` 加载（`Image.src`），或用 `atob` 在 JS 内存中解码。

### 修复点（4 个文件，5 处 fetch 调用）

1. **[imageBitmapLoader.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/imageBitmapLoader.ts)**（`loadImageBitmap`）
   - 添加 `data:` URL 分支：`loadImage(src)` → `createImageBitmap(img)`，绕过 fetch 的 CSP 限制
   - 补充 `loadImage` 的 import（之前缺失会导致 typecheck 错误）

2. **[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)**（两处）
   - `preloadStickerPhotos`：贴纸 dataURL 改用 `loadImage(dataURL)` + 按需 `createImageBitmap(img)`，移除冗余的 Image 加载
   - `preloadPagePhotos` 的 ImageBitmap 路径：`data:` URL 走 `loadImage + createImageBitmap`，其他 URL 仍用 fetch

3. **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**（`exportToJPG`，quality=92 默认分支）
   - `jpgURL` 是 `canvas.toDataURL()` 返回的 data: URL，原 `fetch(jpgURL)` 每次默认质量 JPG 导出都触发 CSP 违规
   - 新增 `dataURLtoBlob` 辅助函数（用 `atob` 解码，纯 JS 内存操作，不发起网络请求）替代 fetch

4. **[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)**（`loadCachedImage`）
   - 在函数开头添加 `data:` URL 快速路径：`loadImage(src)` → `createImageBitmap(img)`
   - 避免 demo 数据（`photo.src` 为 `data:image/svg+xml`）和 usePhotoSrc 回退路径触发 CSP

### 统一原则
- `data:` URL → 用 `Image.src` 加载（CSP `img-src` 允许）或 `atob` 解码，**绝不**用 `fetch()`
- `blob:/asset:/http(s):` URL → 仍用 `fetch()` 获取 Blob

### 验证
- TypeScript 编译通过（0 错误）
- 控制台不再出现 "Refused to connect ... data:image/png;base64" CSP 违规

---

## 2026-08-04 · 修复贴纸加载失败 + 主页贴纸库多选删除

### 问题 1：贴纸库和贴纸列表加载失败/显示不正常
**根因**（[useStickerSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/useStickerSrc.ts)）：
`readStickerBlobUrl` 返回 **blob URL**（`URL.createObjectURL`），缓存值为 blob URL。
LRU 淘汰时调用 `URL.revokeObjectURL` 撤销 URL，但正在被 `<img>` 使用的 URL 被撤销 → 加载失败/显示空白。

**修复**：缓存改为 **dataURL**（`FileReader.readAsDataURL`）。
- dataURL 是字符串，不可撤销，只要字符串被引用即有效
- LRU 淘汰时无需 revoke（dataURL 无系统资源）
- 彻底解决"正在使用的 URL 被 revoke"问题
- 贴纸通常较小（几十 KB），dataURL 内存占用可控

### 问题 2：主页贴纸库新增多选删除功能（[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)）

**功能设计**：
1. **Header 多选按钮**：贴纸列表非空时显示"多选"按钮，点击进入多选模式
2. **多选模式 UI**：
   - 卡片左上角显示圆形选择框（选中=品牌色实心+✓，未选中=白色空心）
   - 卡片点击切换选中状态（整卡可点）
   - 选中卡片有品牌色边框 + ring 高亮
   - 隐藏 hover 的喜欢/删除按钮和重命名点击（避免误触）
3. **操作栏**（分类筛选右侧）：
   - 已选数量计数
   - 全选 / 取消全选按钮
   - 删除所选按钮（红色，选中数为 0 时禁用）
4. **批量删除确认弹窗**：显示选中数量，删除中禁用按钮
5. **删除逻辑**：逐个调用 `deleteSticker`，统计成功/失败数量，完成后刷新列表并退出多选模式

**i18n**：新增 `multiSelect`/`selectAll`/`deselectAll`/`deleteSelected`/`selectedCount`/`deleteMultiConfirm`/`deleteMultiTitle`/`deletedCount`/`deleteMultiFailed` 键（zh-CN + en-US）

### 验证
- TypeScript 编译通过（0 错误）
- 贴纸图片不再因 LRU 淘汰而加载失败
- 多选删除流程：进入多选 → 点击卡片选择 → 全选/取消全选 → 删除所选 → 确认 → 批量删除

---

## 2026-08-04 · 贴纸列表虚拟滚动 + 修复全显/页面模式按钮失效

### 问题
1. **贴纸列表性能**：用户可能添加大量贴纸，列表未做虚拟滚动，全部一次性渲染+解码图片
2. **全显/页面模式按钮失效**：上轮修复"贴纸拖到灰色区域可见"时误移除了 `pageDisplayMode` 在 Canvas.tsx 的消费，导致按钮点击无视觉变化

### 修复 1：贴纸列表 IntersectionObserver 懒加载（[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx#L222-L329)）
复用 PhotoPanel 的方案：
- `StickerGridItem` 新增 `inView` state + `IntersectionObserver`（rootMargin 200px 预加载）
- 未进入视口：不挂载 `<img>`，仅显示占位背景（格子框架）
- 进入视口：挂载 `<img>` 加载贴纸图
- 离开视口：`inView=false` → `<img>` 卸载 → 浏览器释放解码位图
- 与 `useStickerSrc` 的缓存协同控制内存峰值

### 修复 2：恢复 page 模式裁剪（[Canvas.tsx:1897-1910](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1897-L1910)）
恢复 `pageDisplayMode` 订阅 + `clipFunc` 条件裁剪：
- `page` 模式：`<Group clipFunc>` 裁剪 `globalLayerElements` 到 `[0,0,CANVAS_W,CANVAS_H]`
- `full` 模式：不裁剪，贴纸可在灰色区域可见

**两个模式语义分工**：
- 贴纸拖放时不限制落点坐标（可落在灰色区域）
- `full` 模式：贴纸在灰色区域可见
- `page` 模式：贴纸溢出页面部分被裁剪（聚焦页面内容）

### 验证
- TypeScript 编译通过（0 错误）
- 贴纸列表滚动流畅，未进入视口的贴纸不解码图片
- 全显/页面模式按钮点击有视觉变化（裁剪开关生效）

---

## 2026-08-04 · 修复贴纸拖放失效（坐标计算+drop 逻辑修正）

### 问题
上一轮优化后，拖拽贴纸到工作区松开鼠标无反应（贴纸未添加）。

### 根因
1. **坐标计算错误**：使用了 `containerRef`（滚动容器）的 boundingRect，但 `groupOX`/`groupOY` 是页面在 Stage 内的偏移（Stage 坐标系）。Stage 在滚动容器内有居中偏移，导致 `sx - groupOX` 计算出错误坐标
2. **drop 逻辑依赖 ref**：else 分支从 `stickerDragStateRef.current` 读取拖拽数据，若 ref 未设置或已清除则 drop 失败

### 修复（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L470-L536)）
1. **坐标计算改回 `stageRef`**：`stageRef.current?.container().getBoundingClientRect()`，与原工作代码一致
2. **drop 逻辑直接用 `state` 参数**：`endStickerDrag` 的 `notify()` 传递的 state 副本中 `active=false` 但 `stickerId`/`clientX`/`clientY`/`width`/`height` 仍保留。用 `if (state.stickerId && state.dataURL)` 判断有效拖拽，不依赖 ref
3. **接受范围用 Stage 容器**：`sx >= 0 && sx <= stageBox.width`（包含页面 + Stage 内灰色区域）

### 验证
- TypeScript 编译通过（0 错误）
- 拖拽到页面内 → 贴纸落在鼠标位置 ✓
- 拖拽到 Stage 灰色区域 → 贴纸落在灰色区域 ✓

---

## 2026-08-04 · 修复贴纸拖放灰色区域 + 拖拽卡顿优化（ref+DOM 直操）

### 问题
1. **拖放灰色区域未生效**：上次的优化（扩大到工作区+clamp 到页面边缘）未实现用户预期——用户要求贴纸落在鼠标松开的灰色区域位置，不 clamp
2. **贴纸拖拽比照片卡顿明显**：拖拽贴纸时明显掉帧

### 根因分析

#### 卡顿根因（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L458-L535)）
对比照片拖拽与贴纸拖拽的 effect 依赖：

| | 照片拖拽（useDragDrop.ts） | 贴纸拖拽（旧 Canvas.tsx） |
|---|---|---|
| effect 依赖 | `[groupOX, groupOY, canvasZoom, CANVAS_W, CANVAS_H]` | `[stickerDragPreview, ...]` |
| 拖拽数据 | 不在依赖中（订阅稳定） | **stickerDragPreview 在依赖中** |
| mousemove 时 | setDragThumb 触发重渲染，但 effect 不重新订阅 | setStickerDragPreview 触发重渲染 **+ effect 拆销/重建订阅** |

贴纸拖拽每次 mousemove：`setStickerDragPreview` → Canvas（巨大组件）重渲染 → effect cleanup+re-subscribe，每秒 60+ 次，性能远差于照片。

#### 灰色区域不可见根因（[Canvas.tsx:1895-1902](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1895-L1902)）
page 模式下全局图层有 `clipFunc` 裁剪到 `[0,0,CANVAS_W,CANVAS_H]`，贴纸坐标在页面外时被裁剪不可见。

### 修复

#### 1. 性能优化：ref + DOM 直操（消除 mousemove 重渲染）
- **`stickerDragStateRef`**：存最新拖拽数据，callback 始终读 ref（无闭包陈旧）
- **`stickerDragActive`**：boolean state，仅拖拽开始/结束切换一次（只触发 2 次重渲染）
- **`stickerPreviewRef`**：预览 div 的 DOM ref，mousemove 时直操 `style.transform` 移动位置，不触发 React 重渲染
- **`layoutParamsRef`**：存 groupOX/groupOY/canvasZoom/CANVAS_W/CANVAS_H，effect 依赖不含布局参数，订阅稳定
- effect 依赖改为 `[addStickerElement, setSelectedStickerId, currentPageIndexRef]`——与照片拖拽一样稳定

#### 2. 灰色区域放置：移除 clamp + 移除 clip
- 移除坐标 clamp，贴纸落在鼠标松开位置（含页面外灰色区域）
- 移除 page 模式的 `clipFunc` 裁剪，贴纸在灰色区域可见（与 spread 模式行为一致）
- 放置区域用 `containerRef`（工作区滚动容器）替代 `stageRef`，覆盖整个灰色区域

### 验证
- TypeScript 编译通过（0 错误，移除了未使用的 pageDisplayMode）
- 拖拽预览：mousemove 时仅 DOM transform 变化，Canvas 不重渲染 → 流畅度与照片拖拽一致
- 拖到灰色区域松手：贴纸落在松手位置，可见可选中

---

## 2026-08-04 · 优化贴纸拖拽放置范围（整个工作区，替代仅页面内）

### 背景
用户反馈：从贴纸列表拖拽贴纸时，必须精确拖到页面矩形内松开才能添加，体验差。期望整个中间工作区松开即可添加。

### 修复（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L458-L513)）
原逻辑：`if (lx >= 0 && lx <= CANVAS_W && ly >= 0 && ly <= CANVAS_H)` —— 仅当落点在页面矩形内才添加。

改为：
1. **接受范围扩大为整个 Stage 容器**：`if (sx >= 0 && sx <= stageBox.width && sy >= 0 && sy <= stageBox.height)` —— 只要鼠标在工作区内即接受放置
2. **落点 clamp 到页面边界**：`lx = clamp(lx, 0, CANVAS_W)`，`ly = clamp(ly, 0, CANVAS_H)` —— 拖到页面外灰色区域时贴纸自动落在最近的页面边缘，仍归属于当前页

### 验证
- TypeScript 编译通过（0 错误）
- 拖到页面外灰色区域 → 贴纸落在最近页面边缘 ✓
- 拖到页面内 → 贴纸落在鼠标位置 ✓

---

## 2026-08-04 · 修复贴纸缩放原点（对角/对边固定，替代中心点缩放）

### 背景
用户反馈：添加贴纸后缩放时，无论拖哪个控制点都以贴纸中心为原点缩放。期望行为：拖右下角时左上角不动；拖下边时上边不动。

### 根因（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx#L89-L235)）
`StickerElement.x/y` 是**中心点**坐标（见 [types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts#L386)），但旧缩放代码按"左上角"处理：
```js
// 旧代码（错误）
const anchorX = isLeft ? startX + startW : startX;  // 把中心当左上角
const nx = isLeft ? anchorX - newW : anchorX;        // 中心未变 → 以中心缩放
```
且未考虑旋转——锚点页面位置随旋转漂移。

### 修复
统一采用"锚点页面坐标反算新中心"算法，角点和边点共用：

1. **拖拽开始时记录锚点页面坐标**（缩放全程不动）：
   - 锚点本地坐标 = 与拖拽角/边符号相反的对角/对边
   - `anchorPage = center + R(θ) · anchorLocal`（含旋转矩阵）
2. **缩放后反算新中心**：
   - `newCenter = anchorPage − R(θ) · newAnchorLocal`
   - `newAnchorLocal` 用新 width/height 计算

**角点缩放**（拖右下角 → 左上角固定）：
- `anchorLX = -w/2, anchorLY = -h/2`（左上角本地）
- 按对角投影比例缩放（保持宽高比）

**边点缩放**（拖下边 → 上边固定）：
- `anchorLX = 0, anchorLY = -h/2`（上边中点本地）
- 仅缩放单维度，另一维度不变

### 验证
- 旋转 0°：拖右下角 → 左上角屏幕位置不变 ✓
- 旋转 45°：对角点页面位置不变（含旋转修正）✓
- TypeScript 编译通过（0 错误）
- 导出/缩略图渲染无需修改（只读存储状态，不涉及缩放计算）

### 涉及文件
- [StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)（cornerHandle + edgeHandle 重写 + resizeRef 扩展 cos/sin/anchorP 字段）

---

## 2026-08-04 · 修复 blob URL 加载错误 + ImageBitmap detached + 普通模板照片重排失效

### 问题
1. **控制台报错 `ERR_FILE_NOT_FOUND`**：blob URL 在 fetch 途中被 revoke，导致照片加载失败
2. **`InvalidStateError: The image source is detached`**：Konva drawImage 尝试渲染已 close() 的 ImageBitmap
3. **普通模板照片重排失效**：拖拽交换照片无响应

### 根因与修复

#### 1. blob URL ERR_FILE_NOT_FOUND（[import-store.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/storage/import-store.ts#L396-L422)）
- **根因**：`releasePhotoUrl` 在 refCount=0 时**立即 revoke** URL，但此时 `CanvasPhotoRenderer` 的 `loadCachedImage` 可能仍有 in-flight `fetch(src)` 正在读取 blob 字节，revoke 后 fetch 失败
- **修复**：改为延迟回收（`REVOCATION_DELAY_MS=10s` 宽限期），与 `evictBlobUrl` 策略一致。宽限期内再次 acquire/readPhotoFromDB 会取消回收并复活 URL

#### 2. ImageBitmap detached 错误（[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx#L33-L64)）
- **根因**：`IMAGE_CACHE_CAPACITY=16` 太小（注释写 40 但代码是 16），LRU 淘汰正在使用的 ImageBitmap 后 `evictImage` 调用 `img.close()`，组件 `cachedImage.current` 仍指向已 close 的位图 → Konva drawImage 报 detached
- **修复**：
  1. 容量 16→40（与注释一致）
  2. `evictImage` 不再对 ImageBitmap 调用 `close()`（仅清理 HTMLImageElement.src），交由 GC 回收——避免组件仍引用时位图被强制释放
  3. 渲染前 `isImageReady` 校验（含 try-catch 兜底），detached 位图传 null 而非抛异常

#### 3. 普通模板照片重排失效（[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts#L72-L105)）
- **根因**：`swapPagePhotoPlacements` 硬编码 `if (!isGooglePhotosPage(page)) return false`，普通模板页面直接返回 false，UI 层无报错但交换不生效
- **修复**：移除 Google Photos 限制，支持所有页面类型。mmLayout 交换仅对 GP 页面执行（普通模板槽位几何由 template 定义，不受影响）

#### 4. usePhotoSrc 依赖清理（[usePhotoSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/usePhotoSrc.ts#L118-L125)）
- 移除 `photo.src` 依赖项，避免 P2 后台任务更新 photo.src 时触发无意义的 acquire/release 风暴

### 验证
- TypeScript 编译通过（0 错误）
- 用户需手动执行 `npm run desktop:build` 构建 EXE

---

## 2026-08-03 · 模板贴边根治（强制贴边+尺寸填满，替代归一化）

### 背景
用户反馈：L型六图（左大右五）等模板，slotGap=9 正常，slotGap≥10 时左大照片位下方被压缩不贴边。之前用"归一化到原始 bbox"方案修复，但归一化会缩小跨行大图尺寸，问题未解决。

### 根因（归一化方案的致命缺陷）

**L型六图**布局：
- big: x=3, y=3, w=54.5, h=94（跨全高，底部=97）
- r1-r5: 右侧5小图，垂直间隙=2%

slotGap=10mm 时：
1. r5 下移 4×1.57%=6.28%，bboxH 膨胀到 100.28
2. 归一化 normScaleY = 94/100.28 = 0.937
3. **big 的 colHAdj = 94×0.937 - 94 = -5.88**（高度被缩小！）
4. 缩放后 big 高度 = (94-5.88)×(sh/94) = 88.12% < 100%，底部不贴边

**本质**：归一化按比例缩放所有槽位尺寸，跨行大图（无垂直间隙穿过）也被缩小，导致高度不足。

### 修复：强制贴边+尺寸填满

移除归一化，改为基于原始 bbox 边缘的强制贴边：

```typescript
const EDGE_TOL = 1.5; // 1.5% 容差判断原始贴边
for (const slot of template.slots) {
  // 判断槽位在原始模板中是否贴住 bbox 边缘
  const stickLeft = Math.abs(slot.x - origMinX) < EDGE_TOL;
  const stickRight = Math.abs(slot.x + slot.width - origMaxX) < EDGE_TOL;
  const stickTop = Math.abs(slot.y - origMinY) < EDGE_TOL;
  const stickBottom = Math.abs(slot.y + slot.height - origMaxY) < EDGE_TOL;
  // 贴两对边 → 尺寸填满安全区；贴单边 → 仅平移
  if (stickLeft && stickRight) { o.x = leftEdgePx; o.width = rightEdgePx - leftEdgePx; }
  else if (stickLeft) { o.x = leftEdgePx; }
  else if (stickRight) { o.x = rightEdgePx - o.width; }
  if (stickTop && stickBottom) { o.y = topEdgePx; o.height = bottomEdgePx - topEdgePx; }
  else if (stickTop) { o.y = topEdgePx; }
  else if (stickBottom) { o.y = bottomEdgePx - o.height; }
}
```

**L型六图效果**：
- big: 贴左+贴上+贴下 → x=leftEdge, y=topEdge, height=bottomEdge-topEdge（填满高度）✓
- r1: 贴上+贴右 → y=topEdge, x=rightEdge-width ✓
- r5: 贴下+贴右 → y=bottomEdge-height, x=rightEdge-width ✓

### 关键改进
1. **判断依据改为原始 bbox 边缘**（origMinX/origMinY/origMaxX/origMaxY），而非 0/100 边界
   - 模板设计留 3% 边距时，槽位 x=3 不贴 0 边界，但贴 bbox 左边（origMinX=3）
2. **贴两对边时尺寸填满**，彻底解决 bbox 膨胀导致的压缩
3. **只影响贴边槽位**，非贴边槽位保持缩放后位置

### 验证
- TypeScript 编译通过（0 错误）
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（13.61 MB）

---

## 2026-08-03 · 模板间距 bbox 膨胀根治（归一化到原始 bbox）

### 背景
用户反馈：布局模板应用后，间距设置很大或很小时，照片位（特别是跨行大图）会往内缩，不贴安全区边界。已多次修复未解决。

### 根因（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts#L507-L546)）

slotGap 调整后 **bbox 膨胀**，导致缩放因子变小，所有照片位被压缩。

**举例**（左大图跨两行 + 右两个小图）：
- 原始：大图 x=0,w=60,h=100；小图1 x=62,y=0,w=38,h=48；小图2 x=62,y=52,w=38,h=48
- 原始 bbox = 100×100
- slotGap=20mm → gapPctW=9.52%，gapDiff=7.52%
- 调整后：小图向右/下偏移 7.52%，bbox = 107.52×107.52
- 缩放因子 = sw/107.52 = 0.93
- 大图缩放后高度 = 100×0.93 = 93% < 100%，底部不贴边（差 7%）
- snapPx 容差（6%×CW）远不够吸附（差距 7%×CH）

**本质**：slotGap 调整只移动槽位位置，不改变槽位尺寸。间距增大时槽位外移，bbox 膨胀，缩放后照片位被压缩。

### 修复：归一化到原始 bbox

slotGap 调整后，把调整后坐标按比例映射回原始 bbox 范围：
```typescript
if (Math.abs(bboxW - origBboxW) > 0.1 || Math.abs(bboxH - origBboxH) > 0.1) {
  const normScaleX = origBboxW / bboxW;
  const normScaleY = origBboxH / bboxH;
  for (let i = 0; i < template.slots.length; i++) {
    const s = template.slots[i];
    const ax = s.x + slotGapX[i];
    const ay = s.y + slotGapY[i];
    const aw = s.width + rowWAdj[i];
    const ah = s.height + colHAdj[i];
    slotGapX[i] = (minX + (ax - minX) * normScaleX) - s.x;
    slotGapY[i] = (minY + (ay - minY) * normScaleY) - s.y;
    rowWAdj[i] = aw * normScaleX - s.width;
    colHAdj[i] = ah * normScaleY - s.height;
  }
  bboxW = origBboxW;
  bboxH = origBboxH;
}
```

效果：
- bbox 始终等于原始 bbox，缩放因子稳定
- 照片位始终填满安全区（贴边）
- 间隙成比例但不精确等于 slotGap（数学约束：贴边+变间距=照片位尺寸变化）
- 跨行大图在大间距时不再被压缩

### 验证
- TypeScript 编译通过（0 错误）
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（13.40 MB）

---

## 2026-08-03 · 应用程序 EXE 图标模糊彻底修复（窗口运行时图标）+ 大间距贴边

### 背景
1. 用户反馈 slotGap > 13mm 时，错落模板个别照片位不贴安全区边界
2. **应用程序 EXE 图标模糊**（安装程序图标清晰，但安装后应用程序图标模糊）
3. 任务栏"从不合并"模式下 icon 模糊（"始终合并"时清晰）

### 问题 1：大间距时照片位不贴边

#### 根因（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts#L559-L563)）
`rowWAdj` 补足逻辑只补足"距右边≤5%"的槽位。slotGap 增大后，错落模板不同行的偏移不同，某些行的最右槽位距 `globalMaxRight` 超过 5%，不补足。缩放后这些行的右边 < 安全区右边界。而 `snapPx=8px` 容差太小，无法吸附。

#### 修复
isPageSpread 模式下增大 snapPx 到 `max(20, CW * 0.06)`（约 48px = 12mm），覆盖大间距偏移：
```ts
const snapPx = isPageSpread ? Math.max(20, CW * 0.06) : 8;
```

### 问题 2：应用程序 EXE 图标模糊（核心问题）

#### 根因分析
用户反馈"安装程序图标清晰，安装后应用程序图标模糊"。通过提取 EXE 图标资源和分析 Tauri 构建流程，发现：

1. **安装程序图标**（setup.exe）→ 来自 `nsis.installerIcon: "icons/icon.ico"` → 清晰
2. **应用程序 EXE 图标资源** → 通过 `resource.rc` 嵌入 `icon.ico` → 应该清晰
3. **窗口运行时图标** → 通过 `generate_context!` 宏嵌入 → **这是模糊的根源**

关键发现：Windows 任务栏显示**运行中**的应用程序图标时，优先使用**窗口图标**（通过 `WM_GETICON` 获取），而非 EXE 资源图标。Tauri 的 `generate_context!` 宏从 `bundle.icon` 数组中选择图标嵌入，可能选了 `32x32.png`（数组第一个）作为窗口图标，导致任务栏图标只有 32x32，高 DPI 下模糊。

#### 修复（3 层）

**修复 1：调整 `bundle.icon` 数组顺序**（[tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json#L50-L56)）
```json
// 旧：PNG 在前，ICO 在后
"icon": ["icons/32x32.png", "icons/128x128.png", ..., "icons/icon.ico"]
// 新：ICO 在前，高分辨率 PNG 次之
"icon": ["icons/icon.ico", "icons/icon.icns", "icons/128x128@2x.png", "icons/128x128.png", "icons/32x32.png"]
```

**修复 2：ICO 尺寸从大到小排列**（[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs#L24)）
```js
const ICO_SIZES = [256, 128, 96, 64, 48, 40, 32, 24, 20, 16];
// 旧：[16, 20, 24, 32, 40, 48, 64, 96, 128, 256]
// 某些图标加载库取 ICO 第一个尺寸作为窗口图标，256 在首位确保高分辨率
```

**修复 3：setup 中手动设置高分辨率窗口图标**（[lib.rs](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/src/lib.rs#L774-L781)）
```rust
// 设置高分辨率窗口图标（128x128 PNG），覆盖 generate_context! 嵌入的默认图标
let icon_bytes = include_bytes!("../icons/128x128.png");
if let Ok(icon) = tauri::image::Image::from_bytes(icon_bytes) {
    let _ = window.set_icon(icon);
}
```
需要在 Cargo.toml 中添加 `image-png` feature：
```toml
tauri = { version = "2", features = ["protocol-asset", "devtools", "image-png"] }
```

### 验证
- TypeScript 编译通过（0 错误）
- Rust 编译通过（0 错误）
- ICO 包含 10 尺寸（256 在首位），全部 BMP 格式
- 窗口运行时图标设置为 128x128 PNG，Windows 可从中选择合适尺寸
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（13.19 MB）

---

## 2026-08-03 · 大间距贴边修复 + icon 两阶段缩小（从不合并模式清晰）

### 背景
1. 用户反馈 slotGap > 13mm 时，错落模板（L型六图等）个别照片位不贴安全区边界
2. 任务栏"从不合并"模式下 icon 模糊（"始终合并"时清晰）

### 问题 1：大间距时照片位不贴边

#### 根因（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts#L559-L563)）
`rowWAdj` 补足逻辑（第 477-501 行）只补足"距右边≤5%"的槽位。slotGap 增大后，错落模板不同行的偏移不同，某些行的最右槽位距 `globalMaxRight` 超过 5%，不补足。缩放后这些行的右边 < 安全区右边界。而 `snapPx=8px` 容差太小，无法吸附。

#### 修复
isPageSpread 模式下增大 snapPx 到 `max(20, CW * 0.06)`（约 48px = 12mm），覆盖大间距偏移：
```ts
const snapPx = isPageSpread ? Math.max(20, CW * 0.06) : 8;
```
- isPageSpread 模式：6% 页面宽度容差，确保大间距时也能吸附到安全区边界
- 非 isPageSpread：保持 8px 小容差，避免误吸附留白型模板的设计留白

### 问题 2：任务栏"从不合并"模式 icon 模糊

#### 根因分析
1. **Windows 在"从不合并"模式下可能使用不同图标尺寸**（32x32 或 24x24），从 ICO 中取尺寸后可能再次缩放
2. **1250px → 32px 直接缩小（39倍）**，细节丢失严重，normalise + sharpen 不够
3. **ICO 缺少中间尺寸**（20/40px），125%/150% DPI 时 Windows 需从最近尺寸缩放

#### 修复（[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs)）

**改进 1：两阶段缩小**
小尺寸（≤48px）先缩小到 4x 中间尺寸，再缩小到目标尺寸：
```js
if (s <= 48) {
  const midSize = s * 4; // 32px → 先缩到 128px
  pipeline = sharp(srcPath)
    .resize(midSize, midSize, RESIZE_OPTIONS)
    .normalise()
    .sharpen({ sigma: 1.0, m1: 2.0, m2: 0.4 })
    .resize(s, s, { kernel: 'lanczos3' })  // 再缩到 32px
    .sharpen({ sigma: 0.6, m1: 1.0, m2: 0.2 }); // 第二阶段轻度锐化
}
```
比直接 1250px → 32px 更清晰，第二阶段缩小更精确。

**改进 2：添加中间尺寸到 ICO**
```js
const ICO_SIZES = [16, 20, 24, 32, 40, 48, 64, 96, 128, 256];
//                   ↑   ↑       ↑   ↑
//               100% 125%     125% 150% DPI 任务栏
```
覆盖 100%/125%/150%/200% DPI 场景，Windows 无需从最近尺寸缩放。

**改进 3：touch main.rs 强制重新编译**
更新 main.rs 时间戳，强制 cargo 重新编译嵌入最新图标资源。

### 验证
- TypeScript 编译通过（0 错误）
- ICO 包含 10 尺寸（16/20/24/32/40/48/64/96/128/256），全部 BMP 格式
- 两阶段缩小：1250px → 128px → 32px，比直接缩小更清晰
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（13.10 MB）

---

## 2026-08-03 · 模板安全区填满修复 + icon 彻底优化（BMP-in-ICO + normalise + 清缓存）

### 背景
1. 用户反馈布局模板应用后照片位没有填满安全区边界（如杂志英雄六图、L型六图等）
2. 任务栏 icon 图标持续模糊，之前多次修复无效

### 问题 1：模板应用后照片位未填满安全区

#### 根因（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts#L527-L533)）
`calcMarginOverrides` 中 `isPageSpread` 判断使用了 **slotGap 调整后的 bbox**：
```ts
// 旧代码：bboxW/bboxH 在 slotGap 调整后被重新计算（第 503-517 行）
const isPageSpread = bboxW >= 85 && bboxH >= 85 && !isStagger;
```
当用户 slotGap < 模板原始间距时，调整后 bbox 变小（如 90%→84%），从独立轴缩放降级为等比缩放，照片位无法填满安全区。

#### 修复
保存原始 bbox（slotGap 调整前），用原始 bbox 判断缩放策略：
```ts
const origBboxW = bboxW;  // 保存原始 bbox
const origBboxH = bboxH;
// ... slotGap 调整后重新计算 bboxW/bboxH ...
const isPageSpread = origBboxW >= 85 && origBboxH >= 85 && !isStagger;  // 用原始 bbox 判断
```

效果：无论 slotGap 如何调整，缩放策略不变，照片位始终填满安全区。

### 问题 2：任务栏 icon 持续模糊

#### 根因分析
1. **`tauri icon` 生成 PNG-in-ICO**：PNG 需要解码后渲染，某些 DPI 场景下引入模糊
2. **构建缓存**：`cargo` 缓存了旧图标资源，新图标没有重新嵌入 EXE
3. **小尺寸对比度不足**：从 1250px 缩小到 32px（39 倍）时对比度损失

#### 修复（3 层改进）

**改进 1：BMP-in-ICO（[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs)）**
保留 make-icons.cjs 生成的 BMP-in-ICO（不用 `tauri icon` 的 PNG-in-ICO）：
- BMP 格式 Windows 可直接渲染，无需解码
- 包含 8 个尺寸（16/24/32/48/64/96/128/256），覆盖所有 DPI 场景
- `tauri icon` 只有 6 个尺寸（缺 96/128）

**改进 2：normalise + sharpen 增强对比度**
```js
if (s <= 48) {
  resizePipeline
    .normalise() // 拉伸对比度范围，弥补缩放对比度损失
    .sharpen({ sigma: 1.0, m1: 2.0, m2: 0.4 }); // 中度锐化增强边缘
}
```

**改进 3：清理 cargo 编译缓存**
构建前执行 `cargo clean -p membook`（清理 10.6GB 缓存），强制重新编译图标资源：
- 之前构建可能使用了缓存的旧图标
- 清理后 cargo 重新嵌入最新图标到 EXE

### 验证
- TypeScript 编译通过（0 错误）
- 图标重新生成：8 尺寸 BMP-in-ICO + normalise + sharpen
- cargo 缓存清理：10.6GB
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（12.91 MB）

### 注意
安装新版本后如果图标仍显示旧版，需清空 Windows 图标缓存：
```powershell
Stop-Process -Name explorer -Force
Remove-Item "$env:localappdata\IconCache.db" -Force
Remove-Item "$env:localappdata\Microsoft\Windows\Explorer\iconcache*" -Force
Start-Process explorer
```

---

## 2026-08-03 · 模板间距修复（平铺模板应用用户间距）+ 任务栏 icon 清晰度优化

### 背景
1. 用户反馈"杂志英雄六图""L型六图"等模板应用后，照片位间距不随用户设置的 slotGap 变化
2. 任务栏 icon 图标显示不清晰（之前多次反馈未解决）

### 问题 1：平铺模板间距不随用户设置变化

#### 根因（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts#L349-L353)）
`calcMarginOverrides` 中的 `isStagger` 判断基于**标签**而非**实际重叠**：
```ts
// 旧逻辑：带 stagger/magazine/overlay 标签的模板全部跳过 slotGap 调整
const isStagger = template.tags?.includes('stagger')
  || template.tags?.includes('magazine')
  || template.tags?.includes('overlay')
  || template.subCategory === 'magazine';
```
- L型六图标签 `['stagger']` → 跳过 slotGap（但实际平铺无重叠）
- 杂志英雄六图标签 `['magazine']` → 跳过 slotGap（但实际平铺无重叠）

#### 修复
新增 `slotsHaveOverlap` 函数，基于**实际重叠检测**判断是否跳过：
```ts
function slotsHaveOverlap(slots): boolean {
  const TOL = 1; // 1% 容差
  for (i, j) {
    const overlapX = Math.min(a.x+a.width, b.x+b.width) - Math.max(a.x, b.x);
    const overlapY = Math.min(a.y+a.height, b.y+b.height) - Math.max(a.y, b.y);
    if (overlapX > TOL && overlapY > TOL) return true;
  }
  return false;
}
const isStagger = slotsHaveOverlap(template.slots); // 基于实际重叠，而非标签
```

效果：
- L型六图（无重叠）→ 应用 slotGap ✓
- 杂志英雄六图（无重叠）→ 应用 slotGap ✓
- 中心环绕六图（无重叠）→ 应用 slotGap ✓
- overlay 叠加留白（有重叠）→ 跳过 slotGap ✓

### 问题 2：任务栏 icon 不清晰

#### 根因（[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs)）
`prepareSource` 函数对源图强制 trim + 5% padding，但源图 `icon-source.png` 的 logo 占比已达 99.9%（几乎满版），加 padding 后 logo 占比降至 90.8%，小尺寸下 logo 更小更模糊。

#### 修复
1. `prepareSource`：检测源图 logo 占比，≥95% 时跳过 padding，直接使用源图
2. 小尺寸图标（≤48px，任务栏常用）添加 `sharpen` 锐化，弥补缩放细节损失

```js
// 源图 logo 占比 ≥95% 时直接使用，不加 padding
if (logoRatio >= 0.95) {
  await sharp(srcPath).clone().png().toFile(outPath);
  return;
}
// 小尺寸锐化
if (s <= 48) {
  resizePipeline.sharpen({ sigma: 0.8, m1: 1.5, m2: 0.3 });
}
```

效果：prepared 源图从 1376x1376（logo 90.8%）变为 1250x1250（logo 99.9%），32x32 等小尺寸图标更清晰。

### 验证
- TypeScript 编译通过（0 错误）
- icon 重新生成成功，prepared 源图 logo 占比从 90.8% 提升至 99.9%
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（12.66 MB）

---

## 2026-08-03 · 贴纸浮动工具栏位置优化（始终位于视觉上方）

### 背景
上一轮修复让工具栏跟随贴纸本地"上方"旋转，导致 180° 时工具栏跑到贴纸下方遮挡图案。用户要求工具栏始终在贴纸"视觉上方"（屏幕上方），不跟随旋转。

### 根因
原实现使用贴纸本地坐标系旋转工具栏位置：
```ts
// 贴纸本地"顶部"中心 (0, -halfH) 旋转 θ 后的绝对位置
const topCxMm = st.x + (halfH + offsetMm) * Math.sin(rad);
const topCyMm = st.y - (halfH + offsetMm) * Math.cos(rad);
```
- 0°：工具栏在正上方 ✓
- 90°：工具栏在贴纸右侧
- 180°：工具栏在贴纸正下方 ✗（遮挡图案）
- 270°：工具栏在贴纸左侧

### 修复（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2354-L2367)）
改用旋转后 AABB 包围盒顶部，工具栏始终在屏幕视觉上方：
```ts
// 旋转后 AABB 包围盒顶部 = cy - (|halfW*sinθ| + |halfH*cosθ|)
const bboxTopMm = st.y - (Math.abs(halfW * Math.sin(rad)) + Math.abs(halfH * Math.cos(rad)));
// 工具栏 X = 贴纸中心 X（旋转中心不变，始终水平居中）
const toolX = st.x * MM_TO_PX * canvasZoom + groupOX;
// 工具栏 Y = 包围盒顶部 - 48px 偏移（始终在视觉上方）
const toolY = (bboxTopMm - offsetMm) * MM_TO_PX * canvasZoom + groupOY;
```

### 效果
- 任意旋转角度，工具栏始终水平居中于贴纸中心
- 工具栏 Y 始终在旋转后包围盒顶部上方 48px，不遮挡贴纸图案
- 旋转过程中工具栏位置平滑变化（包围盒顶部随角度连续变化）

### 验证
- TypeScript 编译通过（0 错误）
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（15.17 MB）

---

## 2026-08-03 · 贴纸旋转中心修复 + 浮动工具栏位置自适应

### 背景
用户反馈贴纸旋转时以图片右下角为旋转中心，而非图片中心点。同时要求选中贴纸后的浮动工具栏在贴纸上方居中显示，并随旋转角度自适应调整位置。

### 根因分析

#### 问题 1：旋转中心偏移到右下角
`StickerElement.x/y` 类型定义为**中心点坐标**（mm），但 [StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx) 的 Group 配置存在双重偏移：

```tsx
// BUG: px = sticker.x * mmToPx 已经是中心坐标，再加 pw/2 导致 Group 原点偏移到右下
<Group
  x={px + pw / 2}      // ← 中心 + 半宽 = 右边
  y={py + ph / 2}      // ← 中心 + 半高 = 下边
  offsetX={pw / 2}      // ← Konva 旋转枢轴 = local (pw/2, ph/2)
  offsetY={ph / 2}
  rotation={sticker.rotation}
>
  <KonvaImage x={-pw/2} y={-ph/2} ... />  // 图片中心在 local (0,0)
```

Konva 变换顺序 `T(x,y) * R(θ) * T(-offsetX, -offsetY)`：
- 旋转枢轴 = local `(offsetX, offsetY)` = `(pw/2, ph/2)`
- 图片中心 = local `(0, 0)`
- 两者不重合 → 旋转枢轴落在图片右下角

#### 问题 2：导出/缩略图同样的 transform 错误
[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) 和 [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) 的 `drawSticker` 复刻了错误的三段 translate：
```ts
ctx.translate(px + pw / 2, py + ph / 2);  // ← 同样偏移
ctx.rotate(θ);
ctx.translate(-pw / 2, -ph / 2);
```

#### 问题 3：浮动工具栏位置计算错误
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 把 `st.x`（中心）当左上角：
```ts
const cxMm = st.x + st.width / 2;  // ← 中心 + 半宽 = 右边，非中心
```

### 修复

#### 1. StickerNode.tsx — Group 无 offset，旋转枢轴 = 图片中心
```tsx
<Group
  x={px}                // 中心坐标直接使用
  y={py}
  rotation={sticker.rotation}  // 无 offsetX/offsetY，旋转枢轴 = local (0,0) = 图片中心
>
```
旋转中心计算改为 `point({x: 0, y: 0})`（图片中心在 Group local = (0,0)）。

#### 2. exportEngine.ts / thumbnailCore.ts — 两段 translate
```ts
ctx.translate(px, py);           // 原点 = 图片中心
ctx.rotate(θ);                    // 围绕中心旋转
ctx.drawImage(img, -pw/2, -ph/2, pw, ph);  // 图片中心 = (0,0) = 旋转枢轴
```

#### 3. Canvas.tsx — 浮动工具栏跟随旋转
```ts
// 贴纸本地"顶部"中心 (0, -halfH) 旋转 θ 后的绝对位置
const topCxMm = st.x + (halfH + offsetMm) * Math.sin(rad);
const topCyMm = st.y - (halfH + offsetMm) * Math.cos(rad);
```
工具栏始终位于贴纸本地"上方"，随旋转角度自适应调整位置。

### 验证
- TypeScript 编译通过（0 错误）
- 数学验证：θ=0°/90°/180° 时图片中心始终保持在 `(st.x, st.y)` 不变
- 翻转一致性：Konva `scaleX=-1, offsetX=pw` 与 Canvas2D `ctx.scale(-1,1)` 均围绕图片中心翻转
- EXE 构建成功：`MemBook_1.0.0_x64-setup.exe`（15.03 MB）

### 影响文件
- [StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx) — Group transform + 旋转中心计算
- [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) — drawSticker transform
- [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) — drawSticker transform
- [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) — 浮动工具栏位置

---

## 2026-08-03 · 激活验证修复（公钥同步）+ 普通用户相册数量限制

### 背景
用户反馈两个问题：
1. 使用激活码对应用激活提示失败（公钥不匹配）
2. 普通用户允许创建 3 个相册，但未到 3 个时也会弹出需要激活界面

### 问题 1：激活码验证失败（公钥不匹配）

#### 根因
应用内置公钥与用户使用的密钥对不匹配：
- 应用内 `n`：`pySb02rTJBTs6GdRgvAKN2oHHw5ETjJBQBL-hXpmAY1hvYSnOEVHlrB9...`（旧密钥）
- 用户提供 `n`：`swoGY0xAXLlWYfsL15ZvSSI28kuUNaHWM14zf3zKDvlgj32cG_9PnlmQtA_whGvlmd1F6...`（新密钥）

用户用新私钥生成激活码，但应用内仍是旧公钥，RSA 验签必然失败。

#### 修复（[publicKey.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/license/publicKey.ts)）
将 `LICENSE_PUBLIC_KEY.n` 更新为用户提供的新公钥模数。

#### 验证逻辑确认
激活验证流程正确（无需改动）：
1. `isValidCodeFormat` 校验 `MBK-XXXX-XXXX-XXXX-XXXX` 格式
2. `getMachineId()` 获取机器指纹（SHA-256）
3. `buildSignedData(code, machineId)` 构建待验签数据
4. `crypto.subtle.verify` 用内置公钥验签
5. 验签通过则保存 `StoredLicense`

### 问题 2：普通用户相册数量限制逻辑错误

#### 根因（[licenseService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/license/licenseService.ts)）
原 `canCreateProject` 逻辑：
```ts
if (isActivated()) return true;   // 已激活或试用期内 → 不限制
void currentProjectCount;          // ← 忽略了项目数量！
return false;                      // 试用期外 → 完全不允许创建
```

`isActivated()` 内部包含试用期判断（`isTrialActive()`），所以：
- 试用期内：不限制（合理）
- **试用期外：直接 return false，即使 0 个相册也不让创建**（错误，与 i18n 文案"普通用户最多创建 3 个相册"矛盾）

#### 修复
```ts
const FREE_USER_PROJECT_LIMIT = 3;

export function canCreateProject(currentProjectCount: number): boolean {
  if (isActivated()) return true;                                    // 已激活 → 不限制
  return currentProjectCount < FREE_USER_PROJECT_LIMIT;              // 未激活 → 最多 3 个
}
```

修复后逻辑：
- 已激活 → 不限制
- 未激活（含试用期内外）→ 最多 3 个相册（`count < 3` 时允许创建）
- 与 i18n 文案"普通用户最多创建 3 个相册，激活后可创建无限相册"一致

### 验证
- `npm run typecheck` 通过（exit code 0）
- EXE 安装包构建成功：`MemBook_1.0.0_x64-setup.exe`（14.85 MB）

---

## 2026-08-03 · 贴纸旋转中心真正根因修复（Konva offset 变换顺序）

### 背景
前两次修复均未解决问题：
1. `getAbsolutePosition()` — 返回值包含 offset 平移
2. `getAbsoluteTransform(stage).point({x:0, y:0})` — 仍然错误，因为 Konva 变换**包含 offset**

### 真正根因
Konva 的 `getAbsoluteTransform()` 返回的变换矩阵**包含 offset**，变换顺序是：
```
M = T(x, y) * R(θ) * T(-offsetX, -offsetY)
```

主 Group 设置 `offsetX={pw/2} offsetY={ph/2}`，所以：
- 本地原点 `(0, 0)` 经过变换后 → 偏移到图片**左上角**方向（rotation=0 时）或旋转后的左上角方向
- 本地点 `(pw/2, ph/2)` = `(offsetX, offsetY)` 经过变换后 → **贴纸中心**（因为 `T(-offsetX, -offsetY)` 把它平移到原点，然后旋转，然后平移到 (x, y)）

### 修复（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)）
```ts
// 修复前（错误）：
const center = groupNode.getAbsoluteTransform(stage).point({ x: 0, y: 0 });

// 修复后（正确）：
const center = groupNode.getAbsoluteTransform(stage).point({ x: pw / 2, y: ph / 2 });
```

贴纸图片中心在 Group 本地坐标 = `(offsetX, offsetY) = (pw/2, ph/2)`，传这个点给 `getAbsoluteTransform().point()` 才能正确得到贴纸中心在 Stage 坐标系中的位置。

### 悬浮工具栏位置验证
工具栏位置计算逻辑已正确：
- `toolX = cxMm * MM_TO_PX * canvasZoom + groupOX`（贴纸中心 Stage X）
- `toolY = topYMm * MM_TO_PX * canvasZoom + groupOY - 48`（旋转后包围盒顶部上方 48px）

`topYMm = cyMm - (|halfW * sin(rad)| + |halfH * cos(rad)|)` 会随旋转角度自适应调整，工具栏始终在贴纸视觉上方居中。

### 验证
- `npm run typecheck` 通过（exit code 0）
- EXE 安装包构建成功：`MemBook_1.0.0_x64-setup.exe`（14.65 MB）

---

## 2026-08-03 · 贴纸旋转中心根因修复 + 喜欢按钮改主题色

### 背景
1. 上次旋转中心修复仍未解决问题，需要重新分析根因
2. 贴纸喜欢按钮颜色需改为与主题色一致的淡蓝紫色

### 根因分析（旋转中心）
之前两种方案都有问题：
- `getAbsolutePosition()`：会在变换矩阵后追加 `offset` 平移，而 `offset` 会被 `rotation` 旋转，返回的不是贴纸中心而是偏移后的点
- 手动从 `parentNode.x()/y()/scaleX()` 计算：依赖 `mainGroupRef.current?.getParent()` 返回正确的父 Group，如果 ref 为 null 或层级不对则计算错误

### 修复内容

#### 1. 旋转中心正确计算（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)）
用 `getAbsoluteTransform(stage).point({x:0, y:0})` 计算旋转中心：
- 主 Group 的 x/y = 贴纸中心位置，offset = (pw/2, ph/2)
- `getAbsoluteTransform` 返回的变换**不含 offset**，只有 translate(x,y) * rotate(θ) * scale
- 本地原点 `(0,0)` 经过这个变换后 = 父变换 * (x, y) = 贴纸中心在 Stage 坐标
- 传 `stage` 作为 `top` 参数，强制从当前节点到 Stage 重新计算变换，**不用缓存**
- 对比 `getAbsolutePosition()`：它在变换后追加 `translate(offset)`，而 offset 会被 rotation 旋转，导致返回偏移的点

#### 2. 喜欢按钮改主题色（[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx) + [StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)）
- 已喜欢：`text-[var(--color-brand)] border-[var(--color-primary-300)] bg-[var(--color-primary-50)]`
- 未喜欢 hover：`hover:text-[var(--color-brand)] hover:border-[var(--color-primary-300)]`
- 删除按钮保持红色不变

### 验证
- `npm run typecheck` 通过（exit code 0）
- EXE 安装包构建成功：`MemBook_1.0.0_x64-setup.exe`（14.55 MB）

---

## 2026-08-03 · 贴纸缩放/旋转/工具栏三大交互修复

### 背景
用户反馈贴纸添加到页面后存在三个交互问题：
1. 8 个控制点拉伸缩放没有以对边/对角为原点
2. 旋转中心点在右下角角点而非贴纸中心
3. 悬浮工具栏位置不对，且不需要关闭按钮

### 修复内容

#### 1. 缩放原点修复 + 旋转感知（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)）
- **新增 `screenDeltaToLocal` 函数**：将屏幕空间的鼠标 delta 反向旋转到贴纸本地坐标空间，解决旋转后缩放方向错乱问题
- 角点缩放（cornerHandle）：使用本地空间 delta 计算投影缩放比例，以对角点为锚点固定不动 ✓
- 边点缩放（edgeHandle）：使用本地空间 delta 单维度缩放，以对边为锚点固定不动 ✓
- **原有锚点逻辑已正确**（anchorX/anchorY 计算），但旋转后屏幕 delta 未转换到本地空间导致方向错乱

#### 2. 旋转中心修复（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)）
- **根因**：`getAbsolutePosition()` 使用 Konva 内部缓存（`_getCache(ABSOLUTE_TRANSFORM, ...)`），在贴纸拖拽/缩放后缓存可能未及时更新，返回过期的坐标
- **修复**：直接从父 Group 的 `x()`/`y()`/`scaleX()` 属性手动计算贴纸中心在 Stage 坐标系中的位置：
  ```ts
  const parentNode = mainGroupRef.current?.getParent();
  const pX = parentNode?.x() ?? 0;        // groupOX
  const pY = parentNode?.y() ?? 0;        // groupOY
  const pScale = parentNode?.scaleX() ?? 1; // canvasZoom
  const cx = pX + pScale * (px + pw / 2); // 贴纸中心 Stage X
  const cy = pY + pScale * (py + ph / 2); // 贴纸中心 Stage Y
  ```
- 父 Group 变换 = T(groupOX, groupOY) * S(canvasZoom)，贴纸中心在父本地坐标 = (px+pw/2, py+ph/2)，转换到 Stage = (groupOX + canvasZoom * (px+pw/2), ...)

#### 3. 悬浮工具栏修复（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）
- 工具栏 X = 贴纸中心 X（`cxMm * MM_TO_PX * canvasZoom + groupOX`）
- 工具栏 Y = 旋转后包围盒顶部上方 48px（`topYMm * MM_TO_PX * canvasZoom + groupOY - 48`）
- **移除关闭按钮**：点击画布空白处会自动取消选中（`setSelectedStickerId(null)`），无需手动关闭

### 验证
- `npm run typecheck` 通过（exit code 0）
- EXE 安装包构建成功：`MemBook_1.0.0_x64-setup.exe`（14.35 MB）

---

## 2026-08-03 · 贴纸"收藏"改"喜欢"（爱心样式 + 按钮尺寸统一 + 仅 hover 显示）

### 背景
用户反馈：
1. 主页贴纸库左上角收藏按钮尺寸与删除按钮不一致（收藏 24px，删除 32px），已收藏贴纸的按钮一直显示影响美观，应改为仅 hover 显示
2. "收藏"文案改为"喜欢"，按钮图标改为爱心样式

### 修改内容

#### 1. StickerGallery 喜欢按钮优化（[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)）
- 按钮尺寸从 `w-6 h-6`（24px）改为 `w-8 h-8`（32px），与右上角删除按钮一致
- 已喜欢状态从"始终显示"（`opacity-100`）改为"仅 hover 显示"（`opacity-0 group-hover:opacity-100`），与未喜欢、删除按钮行为统一
- 图标从星标改为爱心（filled 爱心 + outline 爱心）
- 已喜欢状态颜色从品牌色改为红色（`text-[var(--color-error)] border-[var(--color-error)]`），符合爱心语义

#### 2. StickerPanel 喜欢按钮优化（[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)）
- 按钮尺寸保持 `w-6 h-6`（与编辑器删除按钮一致）
- 已喜欢状态改为仅 hover 显示
- 图标改为爱心，颜色改为红色

#### 3. i18n 文案更新（[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) + [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)）
- `filterFavorite`：收藏 → 喜欢 / Favorites → Liked
- `favorite`：收藏 → 喜欢 / Favorite → Like
- `unfavorite`：取消收藏 → 取消喜欢 / Unfavorite → Unlike
- `favorited`：已收藏 → 已喜欢 / Added to favorites → Liked
- `unfavorited`：已取消收藏 → 已取消喜欢 / Removed from favorites → Unliked
- 翻译 key 名保持不变（`filterFavorite` 等），只改 value，避免代码改动

### 验证
- `npm run typecheck` 通过（exit code 0）
- EXE 安装包构建成功：`MemBook_1.0.0_x64-setup.exe`（14.15 MB）

---

## 2026-08-02 · 贴纸功能全面优化（删除修复 + 收藏功能 + 重命名 + 旋转中心修复）

### 背景
用户反馈多个贴纸相关问题：
1. 编辑器-贴纸-删除功能不支持删除
2. 需要新增"收藏"功能，主页贴纸库和编辑器贴纸列表都要支持
3. 主页贴纸库贴纸卡片名称点击支持修改
4. 编辑器贴纸旋转中心错误，旋转 UI 和悬浮工具栏位置有问题

### 修复内容

#### 1. 编辑器贴纸删除功能修复（[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)）
- **根因**：删除按钮的 `onMouseDown` stopPropagation 在某些场景下不够可靠，可能被拖拽 mousedown 逻辑干扰
- **修复**：
  - 删除按钮 `onMouseDown` 改为 `onPointerDown`，更早阻止事件传播
  - 移除 `onClick` 中多余的 `e.preventDefault()`
  - z-index 从 `z-10` 提升到 `z-20`，确保层级足够高

#### 2. 新增贴纸收藏功能
**DB 层**（[db/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/db/index.ts)）：
- `StickerRecord` 新增 `favorite?: boolean` 字段
- DB v6 迁移：stickers 表新增 `favorite` 索引
- 新增 `updateSticker(id, patch)`、`renameSticker(id, name)`、`toggleStickerFavorite(id)` 方法

**Service 层**（[stickerService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/stickerService.ts)）：
- 导出 `toggleStickerFavorite`、`renameSticker` 方法

**i18n**（[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) + [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)）：
- 新增 `filterFavorite`、`favorite`、`unfavorite`、`favorited`、`unfavorited`、`clickToRename`、`renamed`、`renameFailed` 翻译

#### 3. 主页贴纸库优化（[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)）
- **收藏筛选**：CategoryFilter 新增 `'favorite'` 选项，筛选 `s.favorite === true` 的贴纸
- **左上角收藏按钮**：`absolute top-2.5 left-2.5`，已收藏显示填充星标（品牌色 + primary-50 背景，常驻显示），未收藏显示空心星标（灰色，hover 显示）
- **贴纸卡片重命名**：参考 [ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx) 相册卡片实现，点击名称进入编辑模式，Enter 确认 / Escape 取消 / blur 自动确认，maxLength=30

#### 4. 编辑器贴纸列表优化（[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)）
- **收藏筛选**：新增 all/favorite 胶囊筛选条，放在 Header 下方
- **左上角收藏按钮**：`absolute top-1 left-1`，与右上角删除按钮对称，交互逻辑与主页一致
- **空状态**：筛选为空时显示"无收藏贴纸"提示

#### 5. 贴纸旋转中心修复（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)）
- **根因**：旋转手柄的 `onMouseDown` 中用 `e.target.getParent()` 获取父节点，但 `e.target` 是 Circle（旋转手柄），其 `getParent()` 返回的是旋转手柄子 Group（位置在贴纸下方 `y = ph/2 + offset`），而非贴纸主 Group。导致旋转中心被设成了手柄位置而非贴纸中心
- **修复**：新增 `mainGroupRef` 指向贴纸主 Group，旋转时用 `mainGroupRef.current.getAbsolutePosition()` 获取贴纸中心绝对坐标
- 效果：旋转操作现在正确以贴纸中心为轴，与照片旋转行为一致（白色圆底 + ↻ 图标 + 15° 吸附 + 单击 90°）

#### 6. 贴纸悬浮工具栏位置（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）
- 工具栏位置计算逻辑正确（旋转后包围盒顶部上方 48px）
- 旋转中心 bug 修复后，旋转操作正确以贴纸中心为轴，旋转后包围盒计算也正确，工具栏位置自然恢复正常

### 验证
- `npm run typecheck` 通过（exit code 0）
- EXE 安装包构建成功：`MemBook_1.0.0_x64-setup.exe`（13.93 MB）

---

## 2026-08-01 · 修复首次创建相册添加页面时 React Error #310 崩溃

### 背景
用户反馈：首次创建新相册，进入编辑模式后，添加页面会出现 `Minified React error #310`（"Rendered more hooks than during the previous render"），导致页面崩溃。

### 根因分析
`Canvas.tsx` 组件中存在**早返回（early return）之后调用 Hook** 的问题，导致空相册状态与有页面状态的 Hook 调用数量不一致：

1. **`useCanvasWheel` 在早返回之后调用**（已于前次修复移至早返回之前）
2. **`globalLayerElements` 的 `useMemo` 在早返回之后调用**（本次发现的真正根因）
   - 位置：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 第 1425 行（早返回在第 1079 行）
   - 空相册时 `currentPage` 为 undefined，触发早返回，`globalLayerElements` useMemo **不被调用**
   - 添加页面后 `currentPage` 有值，跳过早返回，`globalLayerElements` useMemo **被调用**
   - Hook 数量从 N 变为 N+1 → React #310

### 修复内容

#### 1. `globalLayerElements` 由 `useMemo` 改为 IIFE（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）
- 将 `const globalLayerElements = useMemo(() => { ... }, [deps])` 改为 `const globalLayerElements = (() => { ... })()`
- IIFE 是普通计算（非 Hook），不参与 Hook 调用顺序计数，消除 Hook 数量不一致问题
- IIFE 内部已有 `if (!currentPage) return null;` 守卫，仅在 `currentPage` 存在时（早返回已跳过）执行
- 这也是该代码段的原始形态（P1-5 优化时改为 useMemo，但未考虑早返回场景）

#### 2. `useCanvasWheel` 及 slot 函数移至早返回之前（前次修复确认）
- `useCanvasWheel`（第 1071 行）在早返回（第 1079 行）之前调用
- slot 边界计算函数对 `template`/`currentPage` 添加 null 守卫（`template?.slots.reduce(...) ?? defaultBbox`）

#### 3. 新建相册时重置 `currentPageIndex`（[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)）
- `handleCreateAlbum` 和 `handleOpenProject`（无页面时）显式 `useEditorStore.setState({ currentPageIndex: 0 })`
- 避免上次会话遗留的越界索引导致 Canvas 首次渲染时 `currentPage` 为 undefined

### 验证
- `npm run typecheck` 通过（exit code 0）
- 全部 Hook 调用均在早返回之前，Hook 调用顺序在空/有页面两种状态下一致
- EXE 安装包构建成功：`MemBook_1.0.0_x64-setup.exe`（13.68 MB）

---

## 2026-07-31 · 多选线框实时跟随 + 多选阴影工具 + 一键成册主体感知分析

### 背景
用户反馈两个问题：
1. 多选照片进行拖拽缩放时，照片位线框没有实时跟随变化，拖拽体验感不好；多选时悬浮工具栏上的工具也要把阴影功能加上。
2. 分析一键成册功能是否支持了主体感知，会根据人脸焦点偏移将人物放在照片位中心位置。

### 修复内容

#### 1. 多选缩放时 slot-box 线框实时跟随（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）
- **根因**：`renderSlotContent` 中 `slot-box`（选中描边线框）使用 `boxX/boxY/boxW/boxH`（原始几何），而视觉层 Group 使用 `sx/sy/sw/sh`（预览几何），导致多选缩放时线框不跟随
- **修复**：将 `slot-box` 的 `x/y/width/height` 改为 `sx/sy/sw/sh`，与视觉层 Group 保持一致
- 效果：多选拖拽缩放时，照片位线框实时跟随预览几何变化，拖拽体验流畅

#### 2. 多选悬浮工具栏添加阴影切换（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) + [placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts) + [types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/types.ts)）
- 新增 `batchSetShadow(pageIndex, slotIds, shadow)` 方法，批量设置多个槽位阴影开关，统一 pushSnapshot 一次
- 多选工具栏在"置底"和"删除"之间添加阴影切换按钮：
  - 仅 slot 类型多选时显示
  - 全部有阴影时显示激活态（品牌色），点击全部关闭
  - 否则显示未激活态，点击全部开启
  - 无照片的槽位时禁用

### 分析结论

#### 3. 一键成册主体感知支持情况分析
**结论：已完整支持主体感知，会根据人脸焦点偏移将人物放在照片位中心位置。**

支持链路：
1. **布局阶段**（[google-photos-layout.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/google-photos-layout.ts)）：
   - `scorePhotos` 函数使用 `contentInfoCache` 读取 `faceCount`
   - 含人脸照片评分加分（1-2张脸+1分，3+张脸+2分）
   - 影响照片在页面中的分配（hero 位置优先分配给高分照片）
2. **渲染阶段**（[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx) + [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)）：
   - 调用 `computeSmartObjectPosition(photoW, photoH, slotW, slotH, contentInfo)`
   - 条件：`!hasRotation && panX === undefined && panY === undefined && panScale === 1`
   - 一键成册生成的 placement 默认没有 pan 值，所以智能定位自动生效
   - 根据 `contentInfo.focusX/focusY`（人脸焦点）计算偏移，使人物对齐槽位中心
   - clamp 到不露白范围
3. **预览阶段**（[SmartLayoutView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/SmartLayoutView.tsx)）：
   - `computeCssObjectPosition` 与 `computeSmartObjectPosition` 算法对齐
   - CSS object-position 百分比映射，确保预览和最终渲染一致
4. **人脸检测时机**：异步 fire-and-forget
   - `ensurePhotoAnalyzed(photo)` 在照片加载时非阻塞触发
   - 首次渲染缓存未命中走居中，分析完成后下次重渲染使用智能偏移
   - 实际影响小：照片 blob URL 加载完成后会触发重新渲染，此时缓存通常已有数据

### 验收
- typecheck: 0 错误
- 多选拖拽缩放时线框实时跟随，无错位
- 多选工具栏阴影按钮工作正常，批量切换一次快照
- 一键成册主体感知已确认完整支持，无需额外修改

---

## 2026-07-31 · 自定义模板分组筛选 + 统一多选 + 框选缩放漏白修复 + 弹窗位置调整

### 背景
用户反馈三个问题：
1. 用户自定义创建的布局模板无法分组筛选，且不能在编辑模式的布局选择中使用；
2. Ctrl+click 多选与鼠标框选是两套实现，框选时无悬浮工具栏；框选多个照片位缩放调整时照片出现漏白，需进入照片编辑模式拖拽后才正常；
3. "重排照片"、"微调排版"窗口打开时位置偏高，需下移 40px。

### 修复内容

#### 1. 自定义模板分组筛选 + 编辑模式可用
- 新增运行时自定义模板注册机制：`registerCustomTemplates` / `getCustomTemplateRegistry` / `findTemplateById`（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
  - `findTemplateById` 统一查找内置 + 自定义模板，HomeView/编辑器均通过此函数解析模板
- 编辑模式 TemplatePanel 加载自定义模板并显示"自定义"分组，支持按照片数量筛选（[TemplatePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/TemplatePanel.tsx)）
- 首页 TemplateGallery 照片数量统计包含自定义模板（[TemplateGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/TemplateGallery.tsx)）
- i18n 新增 `customGroup` 翻译（zh-CN / en-US）

#### 2. 统一多选实现（Ctrl+click 与框选一致）+ 悬浮工具栏
- **消除 `multiSelectedSlots` 双轨状态**：框选 hook `useMarqueeGroupSelect` 改为直接产出 `multiSelectedElements`（与 Ctrl+click 同一套状态）（[useMarqueeGroupSelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useMarqueeGroupSelect.tsx)）
- Canvas.tsx 移除所有 `multiSelectedSlots` 引用，统一使用 `multiSelectedElements` 驱动悬浮工具栏显示
- selectionSlice 的 `setMultiSelectedElements` 同时清空 `multiSelectedSlots`，保证状态一致
- 效果：框选多选后自动显示与 Ctrl+click 相同的悬浮工具栏（组合缩放/移动/删除等）

#### 3. 修复框选多选缩放照片漏白
- **根因**：`useMultiElementGroupSelect` 的 `commitPreview` 仅更新槽位几何（`batchUpdateSlotOverrides`），未重算照片 `panX/panY`，导致照片按旧槽位尺寸定位，缩放后出现漏白
- **修复**（[useMultiElementGroupSelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useMultiElementGroupSelect.tsx)）：
  - `commitPreview` 中对每个尺寸变化的槽位，使用 `computePanForResizedSlot` 按新旧槽位尺寸重算照片 pan 值
  - 先 `updatePlacementPan`（recordHistory=false），再 `batchUpdateSlotOverrides`（统一 pushSnapshot 一次，快照同时包含 pan + 几何）
  - 仅移动（尺寸不变）时不触发 pan 重算，避免多余计算

#### 4. 弹窗位置下移 40px
- "重排照片"对话框 `top` 从 72px → 112px（[PhotoReorderDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoReorderDialog.tsx)）
- "微调排版"面板 `top` 从 72px → 112px（[LayoutAdjustPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/LayoutAdjustPanel.tsx)）

### 验收
- typecheck: 0 错误
- 框选多选缩放后照片无漏白，无需进入照片编辑模式拖拽
- Ctrl+click 与框选行为完全一致，均显示悬浮工具栏
- 自定义模板在首页和编辑模式均可分组筛选和使用

---

## 2026-07-31 · 间距自适应 + 叠加布局模板 + 渲染层级一致性

### 背景
用户反馈：平铺模板的槽位间距未严格按用户设置调整；需要增加更多模板选择；需要设计照片位叠加的留白布局。

### 修复内容

#### 1. 平铺模板间距自适应（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)）
- 新增 `isTiled` 判定：`tags.includes('grid') && !white-space && !filmstrip`
- 平铺模板的 `gapThreshold = Infinity`（移除 `< 8` 阈值），所有相邻槽位间距严格按用户 slotGap 调整
- 非平铺模板保留 `< 8` 阈值，保护设计留白（如中心焦点左右列间距 24%）
- 效果：平铺模板照片位上下/左右间距统一为用户设置值，照片位大小自动适应缩放

#### 2. 新增 20 个模板（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
- **叠加模板（overlay + white-space）12 个**：2-6 图，有留白和设计感
  - 2图：错位双图、对角呼应、微移双图
  - 3图：三层阶梯、居中聚焦、扇形展开
  - 4图：错落拼贴、层叠四图
  - 5图：散落留白、花瓣聚心
  - 6图：密集拼贴、层叠六图
- **常规模板 8 个**：黄金双图、上下黄金、宽窄宽三列、等比四宫、左大右三横、等比六宫、横排六宫、等比八宫

#### 3. 叠加模板渲染层级一致性
- `isStagger` 检查新增 `tags.includes('overlay')`：跳过 slotGap 调整 + 等比缩放（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts) + [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)）
- `thumbnailCore.ts`：slotOrder 未定义时回退到 `template.slots` 顺序（与 Canvas.tsx 一致）
- `exportEngine.ts`：placement 渲染按 slotOrder 排序（未定义时回退到模板 slots 顺序）
- 效果：overlay 模板的 slots 数组顺序 = 渲染层级（后者覆盖前者），缩略图/画布/导出三层渲染一致

#### 4. 验证脚本支持 overlay 标签（[validate-templates.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/validate-templates.cjs)）
- 解析模板 tags 字段
- overlay 模板跳过重叠/间距检查（有意设计的重叠）

### 验收
- typecheck: 0 错误
- 模板验证: 184 个全部通过（120 内置 + 64 生成）
- 平铺模板间距严格按用户设置，照片位大小自适应
- 叠加模板渲染层级正确，缩略图/画布/导出一致

---

## 2026-07-31 · 缩略图灰色根因修复（Worker 崩溃 + isSpread 不一致）

### 背景
上一轮修复（RENDER_VERSION 5、margin 传递）后缩略图仍灰色，说明根因未被命中。

### 根因分析
1. **Worker 模块求值崩溃**：`sharedRender.ts` 顶层 `import { useEditorStore }` 导致 Worker 导入链触及 store 模块。虽然 zustand `create` 本身不访问 DOM，但导入链中的 slice 文件可能间接引用 DOM 相关模块。RENDER_VERSION=4 时 IDB 缓存掩盖了此问题；RENDER_VERSION=5 使全部缓存失效后，Worker 崩溃暴露：`postMessage` 入队但 Worker 无法处理 → Promise 永不 resolve → `loading` 永久为 `true` → 灰色 `#f0f0f2`
2. **`getSlotRect` isSpread 不一致**：`helpers.ts` 的 `calcMarginOverrides` 已加 `!isStagger` 保护，但 `sharedRender.ts` 的 `getSlotRect` 仍用 `bboxW >= 85 && bboxH >= 85`（无 isStagger 检查），导致 stagger/magazine 模板在无 slotOverrides 时用独立轴缩放，破坏主图居中

### 修复内容

#### 1. 移除 sharedRender.ts 对 useEditorStore 的依赖（[sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)）
- 删除 `import { useEditorStore } from '../store/editorStore'`
- `getSlotRect` 不再从 store 回退读取 margin，要求调用方显式传入
- 切断 Worker → sharedRender → editorStore → slices 的导入链，Worker 可正常求值

#### 2. 修复 getSlotRect isSpread 一致性（[sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)）
- `isSpread` 从 `bboxW >= 85 && bboxH >= 85` 改为 `bboxW >= 85 && bboxH >= 85 && !isStagger`
- 新增 `isStagger` 检查：`tags?.includes('stagger') || tags?.includes('magazine') || subCategory === 'magazine'`
- 与 `helpers.ts calcMarginOverrides` 的 `isPageSpread` 逻辑完全一致

#### 3. 更新 exportEngine 传递 margin（[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)）
- `getSlotRect` 调用补充 `exportMargin` 参数（从 `useEditorStore.getState().pageMargin` 读取）

#### 4. Worker 超时保护 + 崩溃监听（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)）
- 新增 `workerFailed` 标志：Worker `error` 事件触发后标记，后续直接走主线程
- Worker 崩溃时通知所有等待中的 job 返回 null
- 10s 超时保护：Worker 未响应时自动回退主线程同步渲染
- `RENDER_VERSION` 5→6，使旧缓存全部失效

### 验收
- `npm run typecheck` 通过（0 错误）
- `node scripts/validate-templates.cjs` 164 个模板全部通过
- Worker 不再因 store 导入链崩溃，缩略图正常渲染
- stagger/magazine 模板缩略图与实际页面位置一致

---

## 2026-07-31 · 缩略图灰色修复 + 主页筛选器统一 + 多行并列模板 + 主图居中修复

### 背景
用户反馈：
1. 编辑模式底部页面缩略图/网格视图缩略图显示异常，变成灰色没有正确显示页面
2. 主页布局功能筛选器应与编辑器布局筛选器一致（1-8 图 + 9+ 聚合）
3. 3 图需增加三行每行 1 图布局（不同比例）；4/5/6 图同理增加多行并列布局
4. 上英雄三图/杂志英雄三图的主图没有居中，和模板保持一致相对位置效果
5. 中心点聚焦/中心环绕 5 图的中心图没有居中
6. L 型八图/上英雄八图/上大下七小/杂志英雄八图/上大下十一 等模板需检查修复主图相对位置

### 实现内容

#### 1. 缩略图灰色修复（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts) + [thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts) + [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)）
- **根因**：Web Worker 无法读取主线程 `useEditorStore` 的 `pageMargin`，导致 fallback 无边距缩放，槽位位置与实际页面不一致
- **修复**：`ThumbnailWorkerRequest` 新增 `margin` 字段，由调用方显式传入 `useEditorStore.getState().pageMargin`
- **缓存失效**：`RENDER_VERSION` 从 4 递增至 5，`computeContentHash` 新增 `pm`（pageMargin）字段，边距变化时缓存自动失效
- **去重**：修复 `gridThumbnailRenderer.ts` 中 `const pm` 重复声明

#### 2. 主页筛选器统一（[TemplateGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/TemplateGallery.tsx) + i18n）
- `CountFilter` 类型从 `'all'|1|2|3|4|'5+'` 扩展为 `'all'|1|2|3|4|5|6|7|8|'9+'`，与编辑器布局筛选器一致
- `COUNT_FILTERS` 数组补充 count5/6/7/8/9plus 选项
- zh-CN.json / en-US.json 补充对应 i18n 标签

#### 3. 多行并列布局模板新增（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
- **3 图三行**：three-row-equal（等比）/ three-row-panorama（全景）/ three-row-large-top（上大下小）/ three-row-large-bottom（上小下大）
- **4 图四行**：four-row-equal（等比）/ four-row-panorama（全景）/ four-row-large-mid（中宽）
- **5 图五行**：five-row-equal（等比）/ five-row-panorama（全景）/ five-row-large-mid（中宽）
- **6 图多行**：six-row-equal（六行等比）/ six-row-panorama（六行全景）/ six-2x3-rows（三行二列）
- 所有新模板槽位坐标满足 x≥0、y≥0、x+width≤100、y+height≤100，间距≥3%

#### 4. 主图居中相对位置修复（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)）
- **根因**：`calcMarginOverrides` 对 'magazine' 标签模板执行 slotGap 调整，破坏主图（hero）与副图的相对位置；`isPageSpread` 触发独立轴缩放（independent axis scaling），导致模板相对位置失真
- **slotGap 跳过扩展**：`isStagger` 从仅检查 `tags.includes('stagger')` 扩展为同时检查 `tags.includes('magazine')` 和 `subCategory === 'magazine'`，覆盖所有杂志英雄/上大下X 系列模板
- **等比缩放保护**：`isPageSpread` 增加 `&& !isStagger` 条件，stagger/magazine 模板使用等比缩放（`Math.min(sx, sy)`）保持模板原始比例与居中关系
- **中心聚焦模板加标签**：`five-circle-center`（中心焦点）和 `five-cross`（十字焦点）新增 `tags: ['stagger']`，跳过 slotGap 调整保护中心图居中

### 验收
- `npm run typecheck` 通过（0 错误）
- `node scripts/validate-templates.cjs` 全部 164 个模板通过（100 内置 + 64 生成）
- 上英雄/杂志英雄系列模板应用后主图保持居中，与模板预览相对位置一致
- 中心焦点/十字焦点模板中心图保持居中
- L 型/上大下系列模板应用后主图与副图相对位置保持设计意图
- 缩略图正确显示页面内容，不再灰色
- 主页布局筛选器与编辑器一致（1-8 + 9+）

---

## 2026-07-31 · 模板边距自适应 + 问题模板重设计 + 全局重叠验证

### 背景
用户反馈：
1. 页面铺开型模板应用后，上下没有自适应调整到用户设置的边距范围
2. 中心环绕模板设计不符合视觉审美
3. 上大下8/9/10、上英雄下多图、L型7/8/9图等布局不合理（过窄细条）
4. 瀑布布局7/8/9/10应用后和实际不一致
5. 错落五图应用后照片位有重叠、无间距
6. 需全局检查所有模板应用后的重叠/间距问题

### 实现内容

#### 1. 模板边距自适应修复（[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)）
- **页面铺开型检测**：bbox 宽高均 ≥85% 时使用独立 X/Y 轴缩放填满安全区，避免非方形页面上下出现未使用空白
- **四向吸附**：左右上下都对齐到安全区边界（8px ≈ 4mm 容差），避免页面铺开型 1-2px 错位
- **瀑布流跳过对齐**：masonry/waterfall 标签模板跳过列底/行右对齐后处理，保留原始错落比例
- **fallback 路径同步**：sharedRender.ts 和 Canvas.tsx 的 fallback 渲染路径同步加入边距感知安全区计算

#### 2. 问题模板重设计（[generated-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/generated-templates.ts) + [index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
- **过窄细条 → 网格布局**：将单行/单列 6-9 个细条槽位（宽度/高度 8-14%）重设计为 2-3 行网格
  - 上英雄 7/8/9/10 图：底部细条 → 2-3 行 × 3-4 列网格
  - 上大下 6/7/8/9 图：底部细条 → 2-3 行 × 3-4 列网格
  - L 型 7/8/9/10 图：右侧细条 → 2 列 × 3-4 行网格
  - 杂志英雄 7/8/9/10 图：右侧细条 → 2 列 × 3-4 行网格
- **中心环绕标准化**：统一中心 36×36 + 四边 36×26/26×36 + 四角 26×26，3% 间距
- **错落五图修复**：调整 t1/t2 坐标确保最小 3% 水平间距，消除重叠
- **破损模板修复**：gen-12p-151 移除覆盖全页的 s9 槽位，重设计为杂志拼贴十二图
- **去重**：移除九图三列（与九宫格重复），保留九宫格

#### 3. 全局模板验证（[validate-templates.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/validate-templates.cjs)）
- 新增验证脚本，检查全部 151 个模板（87 内置 + 64 生成）：
  - 槽位重叠（重叠面积 > 0.5%）
  - 坐标越界（x/y < 0 或 x+w/y+h > 100）
  - 间距过小（相邻槽位间隙 < 1%）
- 验证结果：✅ 全部通过，无重叠/越界/间距问题

### 验收
- `npm run typecheck` 通过（0 错误）
- `node scripts/validate-templates.cjs` 全部 151 个模板通过
- 页面铺开型模板正确适应边距，上下无空白
- 瀑布流模板应用后与设计一致（跳过对齐后处理）
- 所有模板槽位无重叠，间距 ≥ 1%

---

## 2026-07-31 · 生成模板深度清理 + 模板内置照片位支持删除

### 背景
用户反馈：
1. 生成模板库中存在大量不合理设计：散落布局完全重叠无呼吸感、过窄细条不符合阅读习惯、阶梯式图片重叠
2. 模板内置照片位无法删除，需与一键成册功能一致——允许删除，仅剩一个时也支持删除，画面为空白状态

### 实现内容

#### 1. 生成模板深度清理（[generated-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/generated-templates.ts)）
- 模板数量从 155 个精简至 64 个，剔除以下类别：
  - **散落重叠无呼吸感**：所有 `overlap` 标签模板（深叠/阶梯叠层/大对角叠图/框中框/错位拼贴）+ 所有 `scrapbook` 标签模板（散落/宝丽来）
  - **阶梯式图片重叠**：阶梯/对角级联/扇形系列（`stagger,collage` 标签且坐标重叠）
  - **过窄细条不符合阅读习惯**：6 图+ 胶片（宽度 <12%）、11/12 图 L 型/杂志/上大下（条带 <8%）
- 保留有呼吸感的版式：杂志英雄、瀑布流、中心环绕、L 型、网格阵列、留白胶片（2-5 图）

#### 2. 模板内置照片位支持删除（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
- `AlbumPage` 类型新增 `hiddenTemplateSlotIds?: string[]` 字段，记录用户删除的模板内置槽位 ID
- `resolveTemplate()` 增加隐藏槽位过滤逻辑：运行时从 slots 列表中排除 `hiddenTemplateSlotIds` 中的槽位，保留原模板结构不变
- 渲染层（Canvas/sharedRender/缩略图/导出）通过 resolveTemplate 统一获取槽位，自动支持空页面渲染

#### 3. 一键成册功能支持删除最后一张照片（[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts)）
- `removeSlotFromPage()` 重写模板内置槽位分支：改用 `hiddenTemplateSlotIds` 跟踪删除，支持删至空白
- 移除一键成册页面"至少保留一张照片"限制：删除最后一张时清空 placements/slotOverrides/布局数据，画面显示空白
- 与 `addPhotoToPage` 中 `srcRemaining.length === 0` 的空页面处理模式保持一致

### 验收
- `npm run typecheck` 通过（0 错误）
- 保留模板均为有呼吸感版式，坐标 0-100 范围内，ID 全局唯一
- 模板页和一键成册页均支持删除至空白状态

---

## 2026-07-31 · 模板库清理 + 阴影改为用户可配置 + 移除方向匹配

### 背景
用户反馈：
1. 模板库中存在明显不合理、不符合审美排版的模板设计，需剔除
2. 渲染引擎阶段4的"方向匹配自动旋转"功能应去掉，用户自主决定照片方向
3. 阴影功能应作为用户可配置项（per-placement），不做全局设置；功能按钮放在悬浮工具栏置底和清除按钮中间，名称为"阴影"；开启后拖动时阴影实时跟随

### 实现内容

#### 1. 剔除 6 套不合理模板（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
| 模板 ID | 问题 |
|---------|------|
| `six-top-big-bot5` | b5 宽 13.2 与其他 17.2 不一致，排版不协调 |
| `seven-left-big-right6` | r6 高度仅 7%，照片变成无法识别的细条 |
| `seven-zigzag` | 之字排列断裂，r5/r6/r7 间距混乱 |
| `eight-left-big-right7` | r6 高 8.5%、r7 高 5.5%，无法显示照片 |
| `eight-top-big-bot7` | b7 宽度仅 7%，过窄 |
| `nine-left-big-right8` | r6/r7/r8 与 r1/r2/r3 坐标重叠（设计错误） |

#### 2. 移除方向匹配自动旋转（[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)）
- 删除 `slotOrientation()` 和 `pickAutoRotation()` 函数
- `placePhoto` 恢复为纯置入逻辑，不再自动设置 `rotation`
- 用户通过编辑工具栏自行调整照片方向

#### 3. 阴影改为 per-placement 用户可配置
- **类型扩展**：`PhotoPlacement` 新增 `shadow?: boolean` 字段（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
- **Store action**：新增 `toggleShadow(pageIndex, slotId)` 切换阴影开关（[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)）
- **渲染层统一改为 per-placement**：
  - [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：`placement?.shadow` 时渲染阴影 Rect
  - [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)：`placement.shadow` 时绘制阴影
  - [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)：同步 per-placement 阴影逻辑
- **去除全局阴影**：原先所有照片默认有阴影，现改为仅 `placement.shadow=true` 时显示

#### 4. 悬浮工具栏添加阴影按钮（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）
- 按钮位置：置底（sendToBack）和清除（clear）之间
- 按钮图标：方框 + 后方半透明方框（表示阴影投影）
- 开启时文字高亮为品牌色 `--color-brand`，关闭时为灰色
- 无照片时按钮禁用（opacity 30%）
- i18n：zh-CN "阴影" / en-US "Shadow"（[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)）

#### 5. 拖动时阴影实时跟随
- 去掉 `!isEditing` 限制，阴影在编辑/拖动模式下也保持显示
- 阴影 Rect 使用 `sx/sy/sw/sh` 坐标（与槽位同步），拖动时实时更新

### 验证
- `npm run typecheck`：零类型错误
- 阴影仅出现在用户开启的照片上，编辑器和导出效果一致
- 拖动照片时阴影实时跟随显示

---

## 2026-07-30 · 渲染引擎四阶段优化（模板库+筛选+等比缩放+主体感知）

### 背景
用户反馈模板布局在应用到页面后效果不理想，存在三个核心问题：
1. 基础模板覆盖不全，9+ 图缺少平铺基础布局，筛选器只到 6 图
2. 模板在非方形页面（竖版/横版相册）上被拉伸失真，与设计样式不一致
3. 渲染引擎缺少主体感知、方向匹配、阴影等增强效果，照片显示偏"机械"

### 实现内容（四阶段）

#### 阶段1：完善基础模板库（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
- Template 类型扩展 `subCategory?: 'grid' | 'collage' | 'magazine'` 字段，用于 9+ 图模板细分
- 新增 9-12 图基础平铺模板：
  - 9图：九宫格（nine-grid-3x3）、杂志三行（nine-magazine-3x3）
  - 10图：2x5 网格（ten-grid-2x5）、5x2 杂志（ten-magazine-5x2）
  - 11图、12图：grid + magazine 子分类各一套
- 1-8 图保留原有 60 套基础模板，确保覆盖常见照片数量

#### 阶段2：扩展筛选器（[TemplatePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/TemplatePanel.tsx)）
- `CountFilter` 类型从 `'all' | 1..6` 扩展为 `'all' | 1..8 | '9+'`
- 新增 `matchCountFilter(slotCount, filter)` 函数：精确匹配 1-8，9+ 匹配所有 ≥9 的模板
- i18n 补充 `filter7`、`filter8`、`filter9plus` 翻译（[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)）

#### 阶段3：修复模板应用等比缩放问题
- **核心思路**：模板坐标是 0-100 百分比，原本按 `(slot.x / 100) * canvasW` 独立缩放 x/y，导致非方形页面上模板被拉伸。改为 `scale = min(sx, sy)` 等比缩放 + 居中偏移，保持模板原始比例
- [helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts) `calcMarginOverrides`：使用 `scale = Math.min(sx, sy)`，居中偏移 `ox = (sw - bboxW * scale) / 2`
- [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 无 slotOverrides fallback：`baseScale = Math.min(CANVAS_W, CANVAS_H) / 100`，`offsetX = (CANVAS_W - 100 * baseScale) / 2`
- [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts) `getSlotRect`：导出引擎同步等比缩放逻辑，保证导出与编辑器显示一致
- [albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts) `setAlbumSize`：切换相册尺寸时清除所有页面的 `slotOverrides`，并标记 `dirtyMarginPageIds` 让翻页时按新尺寸重算

#### 阶段4：渲染引擎优化（主体感知+方向匹配+阴影）

**4-1 主体感知缓存层**（[content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)）
- 新增 `photoContentCache: Map<photoId, PhotoContentInfo>` 模块级缓存
- `getCachedContentInfo(photoId)` 同步查询，未命中返回 undefined（回退居中）
- `ensurePhotoAnalyzed(photo)` 非阻塞触发 `analyzePhotoWithFaces`，结果写入缓存
- `hasSmartFocus(photoId)` 判断是否存在人脸焦点偏移
- `invalidatePhotoContentCache` / `clearPhotoContentCache` 清理接口
- 渐进增强：face-api.js 未安装时缓存值为默认居中，无副作用

**4-2 主体感知位置**（[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx) / [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)）
- 触发时机：`useEffect` 监听 `photo.id/width/height` 变化时调用 `ensurePhotoAnalyzed`
- 应用条件：`!hasRotation && rawPanX === undefined && rawPanY === undefined && panScale === 1`（仅未编辑过的照片）
- 缓存命中且 `info.hasFaces && (|focusX-0.5| > 0.02 || |focusY-0.5| > 0.02)` 时，调用 `computeSmartObjectPosition` 计算偏移
- 否则回退到原有居中逻辑（`(slotW - boundingW) / 2`）
- `sharedRender.calcPhotoRenderParams` 同步此逻辑，保证导出/缩略图与编辑器一致

**4-3 方向匹配自动旋转**（[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)）
- 新增 `slotOrientation(slot)` 判断槽位方向（基于百分比宽高比，等比缩放后比例不变）
- 新增 `pickAutoRotation(photo, slot)`：横图进竖槽 / 竖图进横槽 → 自动 `rotation = 90`
- `placePhoto` 时从 `usePhotoStore` 查询照片方向，计算 `autoRotation` 写入新 placement
- 仅在初始放置时计算，用户后续手动旋转会覆盖此值（替换照片时重新计算）

**4-4 槽位阴影**（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) / [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)）
- 阴影必须渲染在 clipped Group **外**（slot Group 有 clipFunc，阴影会被裁掉）
- Canvas.tsx：在 slot-box 之前新增 `<Rect>` 阴影层，`fill="#FFFFFF"`（被上层 cover-fit 照片完全覆盖，仅阴影超出槽位可见）
- 阴影参数自适应槽位尺寸：`shadowBlur = max(4, min(sw,sh) * 0.04)`，`shadowOffsetY = max(2, min(sw,sh) * 0.02)`
- 仅 `photo && !isEditing` 时渲染（空槽位和编辑模式无阴影）
- exportEngine.ts `drawPlacement` 同步：clip 之前先绘制带阴影的填充矩形，保证导出效果一致

### 验证
- `npm run typecheck`：零类型错误
- 主体感知：face-api.js 未安装时无副作用，安装后自动启用
- 等比缩放：横版/竖版相册上模板保持原始比例，居中显示
- 阴影：编辑器和导出引擎效果一致，空槽位无阴影

---

## 2026-07-30 · 版式库去重优化 + Mixbook 风格叠加布局补充

### 背景
用户反馈版式库存在三个问题：
1. 原 60 种模板与新生成模板存在大量重复效果（页面自适应尺寸后样式相同的没必要保留两套）
2. 需要保证全部模板的效果与应用效果一致，不能出现比例/尺寸/数量/间距错位
3. 缺少照片叠加的创意布局设计（参考竞品 Mixbook）

### 实现内容

#### 1. 修复现有模板完全重复（[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)）
- `seven-2x3-plus1` 与 `seven-3x2-plus1` 槽位完全相同（5% 网格精确重复）
- 将 `seven-2x3-plus1` 修正为真正的"二行三列加一"布局：2行3列网格（6图）+ 底部1横幅

#### 2. 生成器三重去重机制（[gen-templates.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/gen-templates.cjs)）
- **5% 网格精确签名**：检测仅有微小坐标差异（<5%）的近似完全重复
- **10% 网格视觉签名**：检测视觉相似重复（margin 差异 ≤10% 视为相同）
- **结构模式签名**：归一化到 10×10 网格，同布局模式最多保留 2 个变体（1图跳过此限制）
- 去重效果：从 218 个生成模板（15 组视觉重复）降至 155 个（0 组生成模板重复）

#### 3. 新增 2 种 Mixbook 风格创意叠加生成器
- **genStaircaseStack 阶梯叠层**：照片沿对角线阶梯式叠加，每张比前张向右下偏移
- **genShiftedCollage 错位拼贴**：不等大照片错位叠加拼贴，大小交替形成层次感
- 应用于 2-8 图各照片数量段，共新增约 14 种叠加布局

#### 4. 修复生成模板与现有模板的精确重复
- `gen-5p-54`（中心焦点五图）与 `five-circle-center` 在 5% 网格上完全重复
- 替换为"错位叠加五图"（3上2下错位叠加布局），视觉效果完全不同

### 去重前后对比

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 生成模板数 | 218 | 155 |
| 5% 精确重复组 | 2 | 1（仅 full/full-bleed） |
| 10% 视觉重复组 | 15 | 2（仅原始模板间） |
| 结构模式重复组(>2) | 15 | 1（1图模式，已跳过限制） |
| 总模板数 | 278 | 215 |

### 验证
- `npm run typecheck`：零类型错误
- `node scripts/analyze-duplicates.cjs`：生成模板间无重复，仅剩原始模板间的历史重复（full/full-bleed, magazine-triple/triple-l-shape，因向后兼容保留）
- 生成器内置校验：所有坐标在 0-100 范围内，ID 全局唯一，preview 取值合法

### 涉及文件
- `scripts/gen-templates.cjs` — 新增 5% 精确签名去重 + 2 种 Mixbook 风格生成器
- `src/types/index.ts` — 修正 seven-2x3-plus1 为真正的 2行3列+1 布局
- `src/types/generated-templates.ts` — 重新生成 155 种去重后模板

---

## 2026-07-30 · 版式库扩展至 357 种 + 网格视图缩放快捷键修正

### 背景
用户提出两项需求：
1. 网格视图缩放快捷键应使用 Ctrl+滚轮（非 Alt）
2. 将版式库（TEMPLATES）从约 60 种扩展至约 360 种，覆盖 1-12 张照片 × 多种风格（留白/叠加重图/胶片/拼贴/沙龙等）

### 实现内容

#### 1. 网格视图缩放快捷键修正（[GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx)）
- 将网格视图缩放快捷键从 Alt+滚轮改为 Ctrl+滚轮，与单页编辑模式保持一致

#### 2. 版式库扩展至 357 种模板

**生成器脚本**：[gen-templates.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/gen-templates.cjs)
- 实现 11 种参数化布局生成器：规则网格、留白网格、留白单行/列、杂志风、胶片条带、叠加重图、错落/瀑布流、画廊、拼贴、L 型、对角线、三联画
- 内置坐标合法性校验（x≥0, y≥0, x+width≤100, y+height≤100）与 ID 去重
- preview 取值严格映射：1图→full, 2图→dual, 3图→triple, 4图→quad, 5+图→collage

**类型扩展**：[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)
- Template 类型新增可选字段 `tags?: string[]` 承载细粒度风格标签（white-space/overlap/magazine/filmstrip/collage/stagger/gallery/grid/minimal/masonry/scrapbook）
- TEMPLATES 数组通过 `...GENERATED_TEMPLATES` 合并生成器输出

**生成结果**：[generated-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/generated-templates.ts)
- 新增 297 种模板，与现有 60 种合计 357 种
- 各照片数量层分布：1图13 / 2图26 / 3图32 / 4图40 / 5图40 / 6图36 / 7图35 / 8图33 / 9图32 / 10图23 / 11图23 / 12图24
- 风格覆盖：留白/呼吸感、叠加重图、杂志风、网格、胶片、拼贴/剪贴簿、阶梯/错落、沙龙/画廊、L型/对角/三联

**向后兼容**：
- 现有 60 种模板 ID 全部保留（full/dual-half/quad-grid 等被项目多处引用）
- 新模板 ID 统一使用 `gen-{数量}p-{序号}` 格式，与现有 ID 无冲突
- 智能编排引擎 `pickTemplate` 的 slotCount>6 截断逻辑未改动，7-12 图模板仅作为手动选择版式生效

### 验证
- `npm run typecheck`：零类型错误
- 生成器内置校验：所有坐标在 0-100 范围内，ID 全局唯一，preview 取值合法

### 涉及文件
- `scripts/gen-templates.cjs` — 版式库生成器脚本（新建）
- `src/types/generated-templates.ts` — 自动生成的 297 种模板（新建）
- `src/types/index.ts` — Template 类型新增 tags 字段 + 合并 GENERATED_TEMPLATES
- `src/components/editor/GridView.tsx` — 网格视图缩放快捷键 Ctrl+滚轮

---

## 2026-07-30 · 贴纸旋转中心修复 + 网格Ctrl+滚轮缩放 + 全屏滚轮缩放修复

### 背景
用户反馈三个问题：
1. 贴纸旋转中心在右下角而非贴纸中心；悬浮工具栏重叠在贴纸上
2. 网格视图模式不支持 Ctrl+滚轮缩放
3. 全屏显示模式滚轮缩放失效

### 修复内容

#### 1. 贴纸旋转中心修复（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx#L316)）
- **根因**：旋转中心用 `imgNode.getClientRect()` 计算，该方法返回旋转后 AABB 包围盒。当贴纸有 flipH/flipV 镜像变换时，AABB 中心与贴纸视觉中心不重合，导致旋转中心偏移到右下角
- **修复**：改用 `groupNode.getAbsolutePosition()` 获取 Group 原点（即贴纸中心）在 stage 中的绝对位置。Group 用 `offsetX/offsetY` 居中，local (0,0) 就是贴纸中心，`getAbsolutePosition()` 不受 flip/rotation 影响

#### 2. 贴纸浮动工具栏偏移增大（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2358)）
- 工具栏上方偏移从 36px 增至 48px，确保不与控制点/虚线边框重叠
- 旋转后包围盒顶部计算保持不变（AABB top = cy - |halfW·sinθ| - |halfH·cosθ|）

#### 3. 网格视图 Ctrl+滚轮缩放（[GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx#L527)）
- 网格视图此前仅支持 Alt+滚轮缩放网格，不支持 Ctrl+滚轮缩放页面
- 新增 Ctrl+滚轮处理：与单页编辑模式一致的指数缩放算法（0.03~0.13 步长，0.1~5 范围）
- 新增全局 `window` 级 Ctrl+wheel `preventDefault` 拦截器（网格视图下 Canvas 未挂载，`useCanvasWheel` 的全局拦截器不生效）

#### 4. 全屏视图滚轮缩放修复（[FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx#L396)）
- **根因**：`useWheel(containerRef, handleWheel)` 使用默认 `deps=[]`。组件 `open=false` 时返回 `null`，`containerRef.current` 为 null，effect 执行时直接 return。`open=true` 时组件重渲染但 effect 不再执行（deps 未变），wheel 监听器从未附加
- **修复**：`useWheel` deps 改为 `[open]`，当 `open` 变为 true 时 effect 重新执行，正确附加 wheel 监听器

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误

### 涉及文件
- `src/components/editor/canvas/StickerNode.tsx` — 旋转中心改用 getAbsolutePosition
- `src/components/editor/Canvas.tsx` — 工具栏偏移 36→48px
- `src/components/editor/GridView.tsx` — Ctrl+滚轮缩放 + 全局拦截器
- `src/components/editor/FullscreenView.tsx` — useWheel deps 改为 [open]

---

## 2026-07-30 · 贴纸控制点优化 + 智能编排改名一键成册 + 删除最后一张照片崩溃修复

### 背景
用户反馈三个问题：
1. 贴纸的 8 个控制点样式与照片槽不一致；角点拖拽不按比例缩放；旋转按钮不够直观；浮动工具栏位置未考虑旋转
2. "智能编排"功能名称需统一改为"一键成册"
3. 智能排版页面只有一张照片时，点击删除照片位触发 React error #300（Maximum update depth exceeded）

### 修复内容

#### 1. 贴纸控制点全面优化（[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)）
- **控制点样式**：角点改为圆形（Circle radius=6px），边点改为长方块（Rect 3:1），与照片槽完全一致
- **角点缩放**：拖拽角点时按比例等比缩放，以对角点作为原点（投影到对角方向计算缩放比例）
- **边点缩放**：拖拽边点时单维度缩放，以对边为原点（保持位置不变）
- **旋转手柄**：参考照片编辑模式 RotationIcon，改为白色圆底 + ↻ 图标（24px），位于贴纸下方，以中心点旋转
  - 单击旋转 90°，拖拽自由旋转（15° 吸附，Shift 取消吸附）
  - 旋转中心使用贴纸图片 `getClientRect` 精确计算，不受控制点偏移影响
- **浮动工具栏位置**（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2345)）：计算旋转后包围盒顶部，工具栏始终在贴纸视觉上方居中

#### 2. "智能编排"统一改为"一键成册"
- [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)：所有"智能编排"/"智能排版"→"一键成册"
- [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)："Smart Layout"→"One-Click Album"
- [Readme.md](file:///f:/N-编程/MenBook开发项目/MemBook/Readme.md)：功能描述同步更新
- [tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json)：安装器长描述同步更新

#### 3. 删除最后一张照片 React error #300 修复（[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts#L94)）
- **根因**：删除最后一张照片后 `placements` 为空 → `buildVirtualTemplate` 返回 `undefined` → 下游组件状态振荡 → 无限重渲染
- **修复**：在 `removeSlotFromPage` 中增加守卫检查，当 `remainingPhotos.length === 0` 时阻止删除并提示用户"页面至少需要保留一张照片，无法删除"
- 新增 i18n key `services.pageLayout.lastPhotoCannotDelete`（中英文）

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误

### 涉及文件
- `src/components/editor/canvas/StickerNode.tsx` — 控制点样式 + 比例缩放 + 旋转手柄重构
- `src/components/editor/Canvas.tsx` — 贴纸浮动工具栏旋转位置计算
- `src/services/pageLayoutService.ts` — 删除最后一张照片守卫
- `src/i18n/locales/zh-CN.json` — 一键成册文案 + lastPhotoCannotDelete
- `src/i18n/locales/en-US.json` — One-Click Album + lastPhotoCannotDelete
- `Readme.md` — 功能描述同步
- `src-tauri/tauri.conf.json` — 安装器描述同步

---

## 2026-07-30 · 编辑器第一页偶现空白彻底修复

### 背景
反复退出主页再进入相册，编辑器工作区和底部缩略图的第一页偶现空白。有时正常有时不正常，复现概率随照片数量增加而增大。

### 根因分析

#### BUG 1：blobUrlCache 容量过小（30，注释说 200）
- `BLOB_URL_CACHE_CAPACITY = 30`，相册超过 30 张照片时 LRU 淘汰最早加载的条目
- 第一页照片最先加载 → 最先被淘汰 → 10s 后被 revoke
- `usePhotoSrc` 的 `acquirePhotoUrl` 如果在 revoke 前执行能 pin 住，否则需重新读 IDB（增加延迟）

#### BUG 2：handleOpenProject/EditorView 用 readPhotoFromDB（refCount=0）而非 acquirePhotoUrl（refCount=1）
- `readPhotoFromDB` 创建的 blob URL refCount=0，不被 pin，LRU 淘汰后立即进入延迟回收队列
- `acquirePhotoUrl` 创建的 blob URL refCount=1，被 pin 住，LRU 淘汰也不会回收
- 退出编辑器时 `cleanupProjectResources` → `revokeAllBlobUrls` 会强制释放所有 URL（包括 acquire 的），不会泄漏

### 修复内容

#### 1. blobUrlCache 容量 30 → 200（[import-store.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/storage/import-store.ts#L242)）
- 与注释中一直声称的容量一致
- 200 条 × ~300KB preview blob ≈ 60MB，可接受
- 覆盖大多数相册规模，避免正常使用场景下的 LRU 淘汰

#### 2. handleOpenProject 用 acquirePhotoUrl（[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx#L346)）
- direct 和 import 模式都从 `readPhotoFromDB` 改为 `acquirePhotoUrl`
- refCount=1 pin 住 URL，即使 LRU 淘汰也不回收

#### 3. EditorView 重载路径同样修复（[EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx#L260)）
- 页面刷新时的 fallback 加载路径也用 `acquirePhotoUrl`

### 验证
- TypeScript 类型检查（`tsc -b`）：零错误
- EXE 构建成功：Vite 前端 18.91s + Rust 编译 2m 25s + NSIS 打包
- 安装包路径：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`（14.03 MB）

### 涉及文件
- `src/engine/storage/import-store.ts` — BLOB_URL_CACHE_CAPACITY 30 → 200
- `src/components/views/HomeView.tsx` — handleOpenProject 用 acquirePhotoUrl
- `src/components/views/EditorView.tsx` — 重载路径用 acquirePhotoUrl

---

## 2026-07-29 · 全屏黑屏 + 主页第一页残缺缓存修复

### 背景
1. 全屏显示模式第一次打开时是黑屏，没有页面和照片显示
2. 主页第一页封面偶现空白仍未彻底修复

### 根因分析

#### BUG 1：全屏黑屏 — loading 初始值错误 + 无缓存快速路径
- `FullscreenView` 的 `loading` 初始值是 `false`（应为 `true`），首帧 `loading=false && thumbnailUrl=null` → JSX 既不渲染 spinner 也不渲染图片，只有 `bg-black` 容器
- 每次打开都 `invalidateFullscreenThumbnail(page.id)` 清缓存，无 `getCachedThumbnailUrl` 快速路径，强制全量渲染

#### BUG 2：主页第一页残缺缓存 — Worker 完整性检查旁路
- Worker 返回 `drawnPhotoCount` 校验逻辑：`drawnPhotoCount === undefined || drawnPhotoCount >= expected`
- `=== undefined` 旁路把不完整渲染当作完整，残缺 dataURL（只有背景+槽位无照片）被写入 IDB
- 后续命中 IDB 缓存 → 主页第一页永久显示空白缩略图

### 修复内容

#### 1. FullscreenView 黑屏修复（[FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx#L34)）
- `loading` 初始值从 `false` 改为 `true`，首帧立即显示 spinner
- 渲染 effect 先 `getCachedThumbnailUrl` 查缓存，命中直接显示，未命中才 `invalidateFullscreenThumbnail` + 全量渲染
- options 加入 `albumSize`，与 CanvasPageThumbnail 保持一致

#### 2. Worker 完整性检查修复（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts#L387)）
```typescript
// 修复前：undefined 被当作完整，残缺缩略图被缓存
const isComplete = drawnPhotoCount === undefined || drawnPhotoCount >= job.expectedPhotoCount;

// 修复后：undefined 视为不完整，不缓存
const isComplete = drawnPhotoCount !== undefined && drawnPhotoCount >= job.expectedPhotoCount;
```

#### 3. RENDER_VERSION 递增到 4（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts#L41)）
- 使旧版残缺 IDB 缓存条目自然失效

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误
- EXE 构建成功：Vite 前端 + Rust 编译 2m 21s + NSIS 打包
- 安装包路径：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`（13.86 MB）

### 涉及文件
- `src/components/editor/FullscreenView.tsx` — loading 初始值 + 缓存快速路径
- `src/utils/gridThumbnailRenderer.ts` — Worker 完整性检查修复 + RENDER_VERSION 递增

---

## 2026-07-29 · 主页第一页封面偶现空白修复

### 背景
只有第一页偶现照片显示异常，从编辑器返回主页再进入相册来回操作偶尔复现。切换其他页面或进入网格视图后恢复正常。

### 根因分析
- `PageThumbnail`（主页封面组件）接收了 `albumSize` prop 但在解构时**丢弃了**（只取 `{ page, photos }`）
- `CanvasPageThumbnail` 和 `gridThumbnailRenderer` 回退读取全局 `useEditorStore.albumSize`
- 主页显示多个不同相册时，全局 `albumSize` 只对应最后打开的那个项目
- 从编辑器返回主页时全局 `albumSize` 可能不匹配当前相册 → 渲染返回 null
- `CanvasPageThumbnail` 无重试机制，首次渲染 null 后永久空白直到依赖变化

### 修复内容

#### 1. RenderOptions 增加 albumSize 字段（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts#L228)）
- `getCachedThumbnailUrl`、`renderPageThumbnail`、`renderPageThumbnailInWorker` 优先使用 `options.albumSize`，回退全局 store

#### 2. CanvasPageThumbnail 接收 albumSize prop + 重试机制（[CanvasPageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CanvasPageThumbnail.tsx#L33)）
- 接收可选 `albumSize` prop，优先于全局 store
- 渲染返回 null 时延迟 800ms 重试，最多重试 2 次（覆盖 albumSize 延迟就绪、照片 preload 临时失败场景）

#### 3. PageThumbnail 转发 albumSize prop（[PageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/PageThumbnail.tsx#L23)）
- 解构时加入 `albumSize`，传递给 `CanvasPageThumbnail`

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误
- EXE 构建成功：Vite 前端 + Rust 编译 2m 18s + NSIS 打包
- 安装包路径：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`（13.66 MB）

### 涉及文件
- `src/utils/gridThumbnailRenderer.ts` — RenderOptions 加 albumSize，3 个函数优先使用传入值
- `src/components/common/CanvasPageThumbnail.tsx` — 接收 albumSize prop + 重试机制
- `src/components/home/PageThumbnail.tsx` — 转发 albumSize prop

---

## 2026-07-29 · 智能编排大量照片时 tierPatterns 崩溃修复

### 背景
智能编排功能照片选择进行开始编排，超过一定数量后报错：
`TypeError: Cannot read properties of undefined (reading 'tierPatterns')`

### 根因分析
- `generateMultipleLayouts` 第一版质量分 >= 95 时提前 break（line 1870 `if (i === 0 && score >= 95) break`）
- 导致 `versions` 数组只有 1 个元素，`versions.slice(0, 3)` 返回长度 1 的数组
- `SmartLayoutView` 默认 `selectedPreviewIndex = 1`（B 方案），访问 `previewResults[1]` 返回 `undefined`
- `undefined.tierPatterns.forEach(...)` 崩溃
- 照片数量多时（30-50+ 张生成 7-10+ 页），评分维度累加（模式多样性 +30、分布 +2/页、跨度利用 +15 等）轻松超过 95，触发提前 break

### 修复内容

#### 1. 引擎修复（[google-photos-layout.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/google-photos-layout.ts#L1869)）
```typescript
// 修复前：第一版 score >= 95 就 break，结果数组可能只有 1 条
if (i === 0 && score >= 95) break;

// 修复后：仅在已生成 >= topK 个版本时才允许提前结束
if (versions.length >= topK && score >= 95) break;
```

#### 2. UI 防御（[SmartLayoutView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/SmartLayoutView.tsx#L770)）
```typescript
// 防御性 clamp index，避免越界
const safePreviewIndex = Math.min(selectedPreviewIndex, Math.max(previewResults.length - 1, 0));
const layoutResult = previewResults[safePreviewIndex];

// useEffect 加 guard
useEffect(() => {
  if (!layoutResult) return;
  layoutResult.tierPatterns.forEach(...);
}, [layoutResult?.tierPatterns]);
```

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误
- EXE 构建成功：Vite 前端 + Rust 编译 2m 17s + NSIS 打包
- 安装包路径：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`（13.47 MB）

### 涉及文件
- `src/engine/google-photos-layout.ts` — 提前 break 条件改为 `versions.length >= topK`
- `src/components/views/SmartLayoutView.tsx` — index clamp + effect guard

---

## 2026-07-29 · 照片重进编辑器不显示 + 全屏空格切换失效修复

### 背景
前轮修复后仍有两个问题：
1. 导入照片排版完成后返回主页，再进入相册编辑器，照片显示异常。进入网格视图后恢复正常，再返回主页再进入又异常
2. 全屏显示的空格按键切换照片失效

### 根因分析

#### BUG 1：`usePhotoSrc` import 模式直接使用 `photo.src`（refCount=0），URL 被 LRU 回收后不恢复
- **现象**：每次重新进入编辑器照片都不显示，网格视图能修复但再进又坏
- **根因链**：
  1. `handleOpenProject` 调用 `readPhotoFromDB(previewBlobId)` 设置 `photo.src`，创建的 blob URL **refCount=0**
  2. `blobUrlCache` 容量仅 30，照片超过 30 张时早期条目被 LRU 淘汰
  3. 淘汰后 10s 延迟回收，URL 被 `revokeObjectURL`
  4. `usePhotoSrc` import 模式检查 `isBlobUrlAlive(photo.src)` → 10s 内返回 true → 直接使用 `photo.src`，**不 acquire**
  5. 10s 后 URL 被 revoke，但 `photo.src` 字符串不变 → useEffect 依赖 `photo.src` 不变 → **不重新执行**
  6. `src` 状态指向已 revoke 的 URL → `fetch` 失败 → 照片永久空白
- **为什么网格视图能修复**：网格视图用 `acquirePhotoUrl`（refCount+1，pin 住 URL），返回编辑模式时 `usePhotoSrc` 重新挂载，此时 `acquirePhotoUrl` 命中缓存
- **为什么再进又坏**：返回主页时 `cleanupProjectResources` → `revokeAllBlobUrls` 清空所有 URL，再进时重新走 `readPhotoFromDB`（refCount=0），循环重现

#### BUG 2：`usePanZoom` capture 阶段 `stopPropagation` 阻止 FullscreenView 收到 Space 事件
- **现象**：全屏视图按空格无反应
- **根因**：`usePanZoom.ts` 在 `document` 上注册 capture 阶段 keydown 监听器，对 Space 调用 `e.stopPropagation()`。capture 阶段先于 bubble 阶段，FullscreenView 的 `window.addEventListener('keydown', handler)`（bubble 阶段）永远收不到 Space 事件
- **为什么以前正常**：`usePanZoom` 从 Canvas.tsx 提取为独立 hook 时，未考虑 FullscreenView 作为 overlay 挂载时 Canvas 仍然存活的场景

### 修复内容

#### 1. `usePhotoSrc` 统一使用 `acquirePhotoUrl`（[usePhotoSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/usePhotoSrc.ts#L44)）
- 初始 `useState` 不再使用 `photo.src`（可能是 refCount=0 的 blob URL），统一返回 null
- useEffect 所有模式（thumb/preview/full + import/direct）统一走 `acquirePhotoUrl(blobId)` 获取 refCounted URL
- acquire 成功 → pin 住 URL（refCount+1），LRU 淘汰不回收 → 卸载时 release 释放
- acquire 失败 → 回退 `photo.src` → 回退文件系统读取
- **消除直接使用 `photo.src` 的路径**，彻底避免 refCount=0 URL 被 LRU 回收后不恢复的问题

#### 2. `usePanZoom` 全屏视图打开时跳过 Space 拦截（[usePanZoom.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/usePanZoom.ts#L40)）
```typescript
// 全屏视图打开时不拦截 Space（FullscreenView 用 Space 切换照片）
if (document.documentElement.classList.contains('fullscreen-open')) return;
```
- keydown 和 keyup 都加入此检查
- FullscreenView 打开时通过 `document.documentElement.classList.add('fullscreen-open')` 标记（已有逻辑）
- Space 事件不再被 stopPropagation，能正常到达 FullscreenView 的 window 级监听器

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误
- EXE 构建成功：Vite 前端 19.99s + Rust 编译 2m 53s + NSIS 打包
- 安装包路径：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`（13.28 MB）

### 涉及文件
- `src/hooks/usePhotoSrc.ts` — 统一 acquirePhotoUrl，不再直接使用 photo.src
- `src/components/editor/canvas/usePanZoom.ts` — 全屏视图打开时跳过 Space 拦截

---

## 2026-07-29 · 工作区/底部缩略图照片偶现不显示彻底修复

### 背景
前轮修复后仍有问题：进入相册后工作区页面和底部缩略图偶现照片不显示，但网格视图和全屏视图正常。切换到网格视图再返回编辑模式后，工作区和缩略图又恢复正常。

### 根因分析

#### BUG 1：`assetUrlToPath` Windows 路径前导斜杠未去除
- **现象**：direct 模式照片在工作区不显示
- **原因**：[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx) 的 `assetUrlToPath` 将 `http://asset.localhost/C:/...` 解析为 `/C:/...`（保留前导斜杠），导致 Tauri `readFile('/C:/...')` 在 Windows 上失败
- **对比**：`gridThumbnailRenderer.ts` 的 `ensureCanvasSafeUrl` 已正确去除前导斜杠（`if (path[0] === '/' && path[2] === ':') path = path.slice(1)`），所以网格视图/全屏视图正常
- **后果**：`readFile` 失败后回退到 `fetch(asset://)`，可能因 CSP 或 WebView2 缓存问题再次失败 → 照片不显示。进入网格视图后 `readPhotoFromDB` 填充 blob URL 缓存，返回编辑模式时 retry 命中缓存才恢复

#### BUG 2：`usePhotoSrc` direct 模式优先使用 asset:// URL 而非 blob URL
- **现象**：direct 模式照片初始加载失败，需等 retry 才恢复
- **原因**：[usePhotoSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/usePhotoSrc.ts) preview/full 档对 direct 模式直接使用 `photo.src`（asset:// URL），因为 `isBlobUrlAlive` 对非 blob URL 乐观返回 true。但 asset:// URL 在 `loadCachedImage` 中需经路径解析 + readFile，不稳定
- **对比**：网格视图的 `resolveGridPhotoSrc` 对 direct 模式优先调用 `readPhotoFromDB(thumbBlobId/previewBlobId)` 获取 blob URL，避免 asset:// 路径问题
- **修复**：direct 模式 preview/full 档优先 `acquirePhotoUrl(blobId)` 获取 blob URL，失败才回退 `photo.src`

#### BUG 3：IDB 持久化缓存残留旧版残缺 dataURL
- **现象**：底部缩略图偶现空白
- **原因**：前轮修复前 Worker 已将残缺 dataURL（缺照片）写入 IDB，content hash 相同（blobId 未变），修复后旧条目仍被命中
- **修复**：在 `computeThumbnailDbKey` 中加入 `RENDER_VERSION`，版本号变更后旧 key 不匹配，自然失效

### 修复内容

#### 1. `assetUrlToPath` Windows 路径修复（[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx#L62)）
```typescript
// 修复前：保留前导斜杠
return decodeURIComponent(src.replace(/^https?:\/\/asset\.localhost\//, '/'));

// 修复后：去除 Windows 前导斜杠，与 ensureCanvasSafeUrl 一致
let path = decodeURIComponent(src.replace(/^https?:\/\/asset\.localhost\//, ''));
if (path.length > 2 && path[0] === '/' && path[2] === ':') {
  path = path.slice(1);
}
return path;
```

#### 2. `usePhotoSrc` direct 模式优先 blob URL（[usePhotoSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/usePhotoSrc.ts#L48)）
- 初始 `useState`：direct 模式的 `photo.src` 若为 asset:// URL，不直接使用（返回 null），交由 useEffect 从 IndexedDB 获取 blob URL
- useEffect preview/full 档：direct 模式优先 `acquirePhotoUrl(previewBlobId)` → 失败回退 `photo.src`
- import 模式保持原逻辑（photo.src 是 blob URL，有效则直接使用）

#### 3. IDB 缓存 key 加入 RENDER_VERSION（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts#L35)）
```typescript
const RENDER_VERSION = 3;
// computeThumbnailDbKey 返回值包含 rv${RENDER_VERSION}
```

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误
- EXE 构建成功：Vite 前端 18.10s + Rust 编译 2m 16s + NSIS 打包
- 安装包路径：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`（13.06 MB）

### 涉及文件
- `src/components/editor/canvas/CanvasPhotoRenderer.tsx` — assetUrlToPath 去除 Windows 前导斜杠
- `src/hooks/usePhotoSrc.ts` — direct 模式 preview/full 档优先 acquirePhotoUrl
- `src/utils/gridThumbnailRenderer.ts` — IDB 缓存 key 加入 RENDER_VERSION

---

## 2026-07-29 · 缩略图残缺缓存 + 槽位渐变色路径混合 BUG 修复

### 背景
缩略图缓存修复后，出现三个新问题：
1. 完成照片添加保存后，返回主页，偶现相册封面照片没有正常显示
2. 进入相册后，第一页工作区页面偶现照片没有正常显示，切换页面后再切回又会正常显示
3. 添加新建空页面，底部缩略图和网格视图的照片位色块变成单色，与工作区不一致

### 根因分析（三个独立 BUG）

#### BUG 1：Worker 路径残缺 dataURL 写入 LRU 缓存
- **现象**：偶现照片不显示，切换页面后恢复
- **原因**：Worker 路径在照片渲染不完整时（`drawnPhotoCount < expectedPhotoCount`），仍然将残缺 dataURL 写入 LRU 缓存（line 371 `thumbnailCache.set(cacheKey, dataURL)`），只是跳过了 IDB 持久化
- **后果**：照片预加载失败时，生成没有照片的 dataURL 被缓存。后续请求命中 LRU（因为 content hash 相同），直接返回残缺 dataURL，跳过重新渲染
- **对比**：主线程 `renderPageThumbnail` 有正确校验（`if (drawnPhotoCount < expectedPhotoCount) return null`），但 Worker 路径没有

#### BUG 2：`drawTemplateSlot` 缺少 `ctx.beginPath()`
- **现象**：缩略图槽位色块变成单色
- **原因**：`drawTemplateSlot` 中 `ctxRR.roundRect()` 之前没有调用 `ctx.beginPath()`，导致 `ctx.fill()` 会填充**之前所有路径 + 当前圆角矩形路径**
- **后果**：多个槽位的路径叠加，颜色混合后变成单色
- **对比**：`exportEngine.ts` 的 `roundRect` 函数内部有 `ctx.beginPath()`，所以导出不受影响

#### BUG 3：`drawTemplateSlot` 在 `cornerRadius <= 0` 时不绘制
- **现象**：圆角设为 0 时缩略图槽位背景消失
- **原因**：`if (cornerRadius > 0)` 为 false 时不执行任何绘制
- **修复**：添加 else 分支，用 `ctx.fillRect` + `ctx.strokeRect` 绘制直角矩形

### 修复内容

#### 1. Worker 路径缓存写入修复（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts#L362)）
```typescript
// 修复前：无论是否完整都写 LRU
thumbnailCache.set(cacheKey, dataURL);
if (isComplete) { void saveThumbnail(...); }

// 修复后：不完整时不写 LRU（与主线程一致）
if (isComplete) {
  thumbnailCache.set(cacheKey, dataURL);
  void saveThumbnail(job.dbKey, job.pageId, dataURL);
} else {
  logger.warn(`Worker 渲染不完整，跳过缓存`);
}
```

#### 2. drawTemplateSlot 路径修复（[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L589)）
```typescript
// 修复前：缺少 beginPath
ctxRR.roundRect(slot.x, slot.y, slot.width, slot.height, cornerRadius);
ctx.fill();  // 填充所有之前的路径！

// 修复后：先 beginPath
ctx.beginPath();
ctxRR.roundRect(slot.x, slot.y, slot.width, slot.height, cornerRadius);
ctx.fill();  // 只填充当前路径
```

#### 3. drawTemplateSlot cornerRadius<=0 修复
添加 else 分支，在 `cornerRadius <= 0` 时用 `ctx.fillRect` + `ctx.strokeRect` 绘制直角矩形。

#### 4. SLOT_PALETTE_VERSION 升级（[templatePalette.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/constants/templatePalette.ts#L8)）
`SLOT_PALETTE_VERSION` 从 5 升级到 6，强制清除所有旧版本的 IDB 缓存，确保用户看到最新的渲染结果。

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误

### 涉及文件
- `src/utils/gridThumbnailRenderer.ts` — Worker 路径残缺 dataURL 不写 LRU
- `src/utils/thumbnailCore.ts` — drawTemplateSlot 添加 beginPath + cornerRadius<=0 时也绘制
- `src/constants/templatePalette.ts` — SLOT_PALETTE_VERSION 5→6

---

## 2026-07-29 · 缩略图缓存失效 + CSP 阻止 asset URL 加载修复

### 背景
贴纸/便利贴渲染一致性修复后，出现新问题：
1. 主页相册卡片封面、底部导航缩略图照片不显示（空白）
2. 添加贴纸等元素后，缩略图未及时同步更新（要等一会儿或多切换几次页面才显示）
3. 工作区偶尔出现页面照片不显示
4. 控制台大量 CSP 报错：`Connecting to 'http://asset.localhost/...' violates the following Content Security Policy directive: "connect-src"`

### 根因分析（三个独立 BUG）

#### BUG 1：CSP `connect-src` 缺少 `asset:` 协议
- **现象**：`fetch('http://asset.localhost/...')` 被 CSP 阻止
- **原因**：CSP `connect-src` 只包含 `blob:`，没有 `asset:` 和 `http://asset.localhost`
- **影响**：
  - `preloadPagePhotos` 中 `fetch(src)` 调用失败 → 缩略图照片不显示
  - `imageBitmapLoader.loadImageBitmap` 中 `fetch(src)` 失败 → 工作区照片偶发不显示
  - `CanvasPhotoRenderer.loadCachedImage` 回退 fetch 时失败

#### BUG 2：LRU 缓存 key 不含 content hash（核心缓存失效 BUG）
- **现象**：添加贴纸/便利贴/文字后，缩略图不更新
- **原因**：`getCachedThumbnailUrl` 和 `renderPageThumbnailInWorker` 的 LRU key 格式为：
  ```
  ${page.id}_${cacheSuffix}_v${cacheVersion}_p${SLOT_PALETTE_VERSION}
  ```
  **不含 content hash**！而 IDB key 含 hash。导致：
  - 第一次渲染后，LRU 存储了 `pageId_nav_v1_p1` → oldDataURL
  - 贴纸添加后，`getCachedThumbnailUrl` 检查 LRU，key 仍是 `pageId_nav_v1_p1` → **命中旧 dataURL**！
  - 直接返回旧缓存，跳过渲染，IDB 检查都不会执行

#### BUG 3：`invalidatePageThumbnail` key 不匹配
- **现象**：主动失效缓存时无效
- **原因**：`invalidatePageThumbnail` 的 key 构造不含 hash，与实际存储的 key（含 hash）不匹配，`thumbnailCache.delete()` 删除了不存在的 key

### 修复内容

#### 1. CSP 修复（[tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json#L38)）
```json
"connect-src": "'self' http://ipc.localhost asset: http://asset.localhost https://asset.localhost https://api.bigdatacloud.net https://updates.membook.app https://github.com https://*.githubusercontent.com blob:"
```
添加 `asset:`, `http://asset.localhost`, `https://asset.localhost`，所有 `fetch(asset://...)` 和 `fetch(http://asset.localhost/...)` 都能正常工作。

#### 2. ensureCanvasSafeUrl 增强（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts#L543)）
- 优先用 `photo.relativePath` 通过 fs 读取为 blob URL（最可靠，不受 CSP 限制）
- 新增 asset URL 反解析：从 `http://asset.localhost/F%3A/...` 反解析出文件路径，再通过 fs 读取为 blob URL
- 避免依赖 CSP 放行 fetch(asset://)

#### 3. LRU cache key 加入 content hash（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)）
三处 LRU key 构造全部修复：
- `getCachedThumbnailUrl`（line 169）
- `renderPageThumbnail`（line 226）
- `renderPageThumbnailInWorker`（line 383）

修复后的 key 格式：
```
${page.id}_${cacheSuffix}_v${cacheVersion}_p${SLOT_PALETTE_VERSION}_h${contentHash}
```

#### 4. invalidatePageThumbnail 前缀匹配删除（[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts#L69)）
```typescript
const prefix = `${pageId}${suffix}_v${cacheVersion}_p${SLOT_PALETTE_VERSION}`;
for (const key of thumbnailCache.keys()) {
  if (key.startsWith(prefix)) {
    thumbnailCache.delete(key);
  }
}
```
用前缀匹配删除所有该 pageId+suffix 的缓存条目（覆盖所有内容版本）。

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误

### 涉及文件
- `src-tauri/tauri.conf.json` — CSP connect-src 添加 asset 协议
- `src/utils/gridThumbnailRenderer.ts` — ensureCanvasSafeUrl 增强 + LRU key 加 content hash + invalidatePageThumbnail 前缀匹配

---

## 2026-07-29 · 贴纸/便利贴渲染一致性深度修复（编辑器 ↔ 导出 ↔ 缩略图）

### 背景
用户反馈：编辑器页面的贴纸/便利贴位置大小与导出结果不一致，且各大板块缩略图（相册封面、网格视图、全屏显示）也与编辑器效果不一致。

### 根因分析（两层 BUG）

**第一层：固定像素值未按 scale 缩放**
导出引擎使用动态 `mmToPx`（高分辨率时远大于 `MM_TO_PX=2`），但便利贴/贴纸/文字元素中的**固定像素值**（cornerRadius、shadowBlur、shadowOffsetY、padding、tape 胶带尺寸等）没有按 `mmToPx / MM_TO_PX` 比例缩放。

**第二层：坐标变换不匹配 Konva Group transform（核心位置偏移 BUG）**
编辑器 Konva Group 使用 `x={px+pw/2} offsetX={pw/2}` 实现中心点定位 + 旋转。Konva 的完整 transform 是 `T(x,y) * R(θ) * T(-offsetX, -offsetY)`。

导出引擎原代码只有一段 `translate(px+pw/2, py+ph/2)`，这等价于 `T(x,y)`，**缺少了 `R(θ) * T(-offsetX, -offsetY)` 中的 `T(-offsetX, -offsetY)`**。

这导致：
- 不旋转时：Image 中心偏移 `(pw/2, ph/2)`（被当作左上角而非中心点）
- 旋转时：旋转中心错误（变为 `(px+pw/2, py+ph/2)` 而非 Konva 的 offset 点）

**Konva 坐标系验证**：
```
Group: x=px+pw/2, y=py+ph/2, offsetX=pw/2, offsetY=ph/2
  → Group 自身 (0,0) 映射到舞台 (px, py)  [即 sticker.x/y 是中心点]
Image: x=-pw/2, y=-ph/2 (在 Group 自身坐标系中)
  → Image 中心在 Group (0,0) → 舞台 (px, py)  [中心点对齐 ✓]
```

### 修复方案：三段 translate 完整复刻 Konva transform

```typescript
// 完整复刻 Konva Group transform: T(x,y) * R(θ) * T(-offsetX, -offsetY)
ctx.translate(px + pw / 2, py + ph / 2);  // T(x, y)
ctx.rotate(rotation);                       // R(θ)
ctx.translate(-pw / 2, -ph / 2);           // T(-offsetX, -offsetY)
// 此后坐标系 = Group 自身坐标系，与编辑器完全一致
ctx.drawImage(img, -pw / 2, -ph / 2, pw, ph);  // Image 在 Group 自身坐标系中的位置
```

**数学验证**（旋转 90° 时 Image 中心位置）：
- 编辑器：`T(px+pw/2, py+ph/2) * R(90) * T(-pw/2,-ph/2) * (0,0)` = `(px+pw/2+ph/2, py+ph/2-pw/2)` ✓
- 修复后导出：同上 ✓
- 修复前导出（一段 translate）：`T(px+pw/2, py+ph/2) * R(90) * (-pw/2,-ph/2)` = 偏移错误 ✗

### 修复内容

#### exportEngine.ts（导出引擎）
- **[drawSticker](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L997)**：
  - 三段 translate 完整复刻 Konva Group transform
  - 固定像素值（shadowBlur/shadowOffsetY）乘以 scale
  - 阴影 opacity 0.15→0.2，Math.max 最小尺寸 20*scale
- **[drawStickyNote](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L922)**：
  - 三段 translate 完整复刻 Konva Group transform
  - 固定像素值（cornerRadius/shadowBlur/shadowOffsetY/tape尺寸/padding）乘以 scale
  - 新增边框、空文本处理、Math.max 最小尺寸、文字高度限制
- **[drawTextElement](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L868)**：新增空文本处理 + `te.height ?? 0`

#### thumbnailCore.ts（缩略图核心）
- **[drawSticker](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L483)**：三段 translate + 阴影 opacity 0.2
- **[drawStickyNote](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L407)**：三段 translate + 边框/空文本/最小尺寸/高度限制
- **[drawTextElement](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L353)**：空文本处理

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误
- 数学验证：三段 translate 在旋转/不旋转/翻转场景下均与 Konva transform 一致

### 涉及文件
- `src/utils/exportEngine.ts` — 三段 translate + 固定像素值缩放 + 边框/空文本/最小尺寸/高度限制
- `src/utils/thumbnailCore.ts` — 三段 translate + 边框/空文本/最小尺寸/高度限制/阴影opacity统一

---

## 2026-07-29 · Bug44/45/46/47 修复 + 渲染管线一致性优化

### 背景
用户反馈四个问题：Bug45（GP模式照片位删除未完全修复）、Bug47（页面设置弹窗尺寸显示优化）、Bug46（创建新相册快速预设默认标准）、Bug44（页面内容显示一致性问题——编辑器/缩略图/网格/全屏/封面/导出之间内容不一致）。

### 修复内容

#### Bug45：GP 模式 extraSlots 删除修复验证
- **验证**：[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts) `removeSlotFromPage` 中 `extraSlot` 删除分支已移至 `albumSize` 检查之前，无需 `albumSize` 即可完成删除。GP 重排操作（addPhotoToPage/shufflePagePhotos/removeSlotFromPage/rotatePageLayout/shufflePageLayout）均已保留空 extraSlot 的 placement。

#### Bug47：页面设置弹窗尺寸显示优化
- **[PageSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageSettings.tsx)**：
  - 尺寸选项从 `ALBUM_SIZES`（5个）切换为 `SIZE_PRESETS`（14个），与创建新相册弹窗一致
  - 新增 `showSizeOptions` 折叠态：默认只显示当前尺寸 + "修改页面"按钮，点击后展开全部尺寸
  - 展开态使用 4 列网格布局（与 CreateDialog 一致）
  - 弹窗宽度从 440px 加宽至 560px，整体布局更协调
  - `handleConfirm`/`handleReset`/预览逻辑同步适配 `SIZE_PRESETS`

#### Bug46：创建新相册快速预设默认标准
- **[CreateDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CreateDialog.tsx)**：
  - 快捷预设默认值从 `PAGE_MARGIN_DEFAULT/PAGE_GAP_DEFAULT/DEFAULT_SLOT_CORNER_RADIUS`（5px）改为"标准"预设值（margin=15, gap=5, cornerRadius=8），通过 `STANDARD_PRESET` 常量统一管理
  - 打开弹窗和创建后的重置逻辑均使用 `STANDARD_PRESET`
- **[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)**：
  - `handleCreateAlbum` 中新增 `setApplyMarginToAll(true)`，创建新相册时"应用到全部页面"默认开启

#### Bug44：页面内容显示一致性（核心修复）

**渲染差异修复（exportEngine.ts + thumbnailCore.ts）**：

| 差异项 | 编辑器 | 修复前（导出/缩略图） | 修复后 |
|--------|--------|----------------------|--------|
| 竖排文字（春联） | rotation===-90 逐字竖排 | 始终横排 | ✅ 逐字竖排，从右到左 |
| 荧光笔混合模式 | `globalCompositeOperation='multiply'` | `source-over` | ✅ `multiply` |
| 便利贴文字内边距 | 6px（width=pw-12） | 8px（width=sw-16） | ✅ 6px（width=sw-12） |
| 文字换行宽度 | pw-8（4px×2 padding） | tw（全宽） | ✅ tw-8 |
| 缩略图背景 | 纯色/渐变/纹理 | 仅纯色 | ✅ 支持三种类型 |

- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：`drawBrushStroke` 添加荧光笔 multiply 混合；`drawTextElement` 添加竖排文字支持 + 修正换行宽度；`drawStickyNote` 内边距 8→6px
- **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**：同上三项修复 + 新增 `drawPageBackground`/`parseCssGradientColors`/`getTextureBaseColor` 支持渐变/纹理背景

**缩略图渲染统一（BottomNav + Home PageThumbnail）**：
- 新增 [CanvasPageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CanvasPageThumbnail.tsx)：通用 Canvas 2D 缩略图组件，基于 `renderPageThumbnailInWorker` 渲染所有元素类型，内置缓存
- [BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx) PageThumbnail：CSS 渲染 → Canvas 2D（`cacheSuffix='nav'`）
- [home/PageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/PageThumbnail.tsx)：CSS 渲染 → Canvas 2D（`cacheSuffix='home'`）

**缓存哈希修复**：
- [gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts) `computeContentHash`：补充 `extraSlots`、文字 `rotation`、笔触 `brushType`，确保这些字段变化时缓存正确失效

### 验证
- TypeScript 类型检查（`tsc --noEmit`）：零错误
- EXE 构建（`npm run desktop:build`）：成功
  - Vite 前端构建：18.76s
  - Rust release 编译：2m 23s（增量编译）
  - NSIS 打包完成
  - 输出：`src-tauri/target/release/bundle/nsis/MemBook_1.0.0_x64-setup.exe`（13.59 MB）
  - 环境变量：`TAURI_BUNDLER_TIMEOUT=360`、`TAURI_BUNDLER_TOOLS_GITHUB_MIRROR=https://gh-proxy.com/https://github.com`

### 涉及文件
- `src/services/pageLayoutService.ts` — Bug45 验证（无修改）
- `src/components/editor/PageSettings.tsx` — Bug47 页面设置弹窗优化
- `src/components/home/CreateDialog.tsx` — Bug46 快捷预设默认标准
- `src/components/views/HomeView.tsx` — Bug46 applyMarginToAll 默认开启
- `src/utils/exportEngine.ts` — Bug44 导出引擎渲染修复
- `src/utils/thumbnailCore.ts` — Bug44 缩略图核心渲染修复 + 背景支持
- `src/utils/gridThumbnailRenderer.ts` — Bug44 缓存哈希修复
- `src/components/common/CanvasPageThumbnail.tsx` — Bug44 新增 Canvas 2D 通用缩略图组件
- `src/components/editor/BottomNav.tsx` — Bug44 底部导航缩略图改用 Canvas 2D
- `src/components/home/PageThumbnail.tsx` — Bug44 主页封面缩略图改用 Canvas 2D
- `src/i18n/locales/zh-CN.json` / `en-US.json` — Bug47 新增 i18n 键

---

## 2026-07-29 · 缩略图渲染统一为 Canvas 2D（BottomNav + Home PageThumbnail）

### 背景
BottomNav 与 Home 的 PageThumbnail 之前使用 CSS 渲染，仅显示照片，完全缺失文字/便签/贴纸/画笔笔触。需统一为 Canvas 2D 渲染，与编辑器画布及网格视图保持一致。

### 修改内容
- **新增 [CanvasPageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CanvasPageThumbnail.tsx)**：通用 Canvas 2D 缩略图组件
  - 基于 `renderPageThumbnailInWorker` 渲染所有元素类型（照片/文本/便签/贴纸/画笔笔触）
  - 内置 LRU + IDB 二级缓存（复用 gridThumbnailRenderer），支持 `cacheSuffix` 隔离命名空间
  - 先查缓存（`getCachedThumbnailUrl`）→ 命中跳过预加载；未命中则并行预加载照片与贴纸后 Worker 渲染
  - `useEffect` + `cancelled` 标志防止卸载后状态更新；`memo` 避免不必要重渲染
  - 加载中显示浅灰占位，失败显示白色兜底
- **[BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx) PageThumbnail**：移除 CSS 渲染（`PageSlotPreview` + `PhotoThumb` + `calcPagePreviewFit` + `photoMap`），改用 `CanvasPageThumbnail`（`cacheSuffix='nav'`、`scale=1`）；`PhotoThumb` 定义保留并导出以备复用
- **[home/PageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/PageThumbnail.tsx)**：移除 CSS 照片渲染与本地 `PhotoThumb`/`rebuildPhotoUrl`，改用 `CanvasPageThumbnail`（`cacheSuffix='home'`）；保留 null-template 占位与容器结构，用 ResizeObserver 测量容器尺寸

### 验证
- TypeScript 类型检查（`tsc --noEmit -p tsconfig.app.json`）：零错误

### 涉及文件
- `src/components/common/CanvasPageThumbnail.tsx` — 新增：Canvas 2D 通用缩略图组件
- `src/components/editor/BottomNav.tsx` — PageThumbnail 改用 Canvas 2D，移除 CSS 相关导入
- `src/components/home/PageThumbnail.tsx` — 照片渲染改用 Canvas 2D，清理 CSS 渲染死代码

---

## 2026-07-29 · Bug41/42/43 修复 + P1 代码审查问题修复 + EXE 构建

### 背景
用户反馈三个优化问题（Bug41/42/43），并要求按全项目代码审查结果完成所有 P1 问题修复，最后构建 EXE 验证。

### 修复内容

#### Bug43：照片位层级顺序未实际应用
- **问题**：拖拽照片到重叠槽位时，命中检测未按视觉渲染顺序选择顶层槽位（zIndex 相同时选了第一个模板槽位而非渲染在后的）。
- **修复**：[useDragDrop.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useDragDrop.ts) 候选槽位排序增加 `slotOrder` 索引作为 tiebreaker：zIndex 降序 → slotOrder 索引降序（渲染在后=视觉顶层），与 Canvas.tsx `globalLayerElements` 渲染顺序一致。

#### Bug42：多选包围盒 + 单选跨类型互斥
- **问题1**：单选时不同类型元素未严格互斥（选照片位时未清空文字/贴纸选中）。
- **修复1**：[selectionSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/selectionSlice.ts) `setSelectedSlot`/`setSelectedTextId`/`setSelectedStickyId`/`setSelectedStickerId` 改为清空所有其他类型的单选 ID + 所有多选状态。
- **问题2**：多选时缺少包围盒控制（移动/缩放），跨类型多选也缺少。
- **修复2**：新增 [useMultiElementGroupSelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useMultiElementGroupSelect.tsx) hook：
  - 统一收集 slot/text/sticky/sticker 元素几何（px 坐标，mm×MM_TO_PX 转换）
  - 计算包围盒 + 渲染 8 控制点（复用 selection-engine 的 computeBBox/computeScaledSlots/computeMovedSlots）
  - 移动/缩放过程中仅更新预览，松手一次性提交（slot→batchUpdateSlotOverrides，装饰元素→updateXxx with recordHistory=false + 统一 pushSnapshot）
  - 预览几何通过 `previewRectMap` 应用到 Canvas.tsx 的 slot/text/sticky/sticker 渲染
- **Canvas.tsx 接入**：导入并调用 hook，stage onMouseMove/onUp 链式调用 marqueeGroup → multiElementGroup，Layer 内渲染 `multiElementGroup.renderGroupBox()`。

#### Bug41：GP 模式 extraSlots 无法彻底删除
- **问题**：GP 布局页面点击删除照片位时，用户通过"添加照片位"创建的 extraSlots 未被删除（进入 GP 重排分支而非删除分支）。
- **修复**：[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts) `removeSlotFromPage` 将 `extraSlot` 检查移出 GP 条件分支，统一处理所有页面类型：extraSlots 中的槽位从 `extraSlots`/`placements`/`slotOrder`/`slotOverrides`/`slotZIndices` 全部清除。

#### P1-5：pageSlice.setCurrentPage 缺 pushSnapshot
- **修复**：[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts) 懒计算分支（dirtyMarginPageIds 命中时）修改 pages 后添加 `pushSnapshot(get)`。纯翻页导航（仅改 currentPageIndex）不需要快照。

#### P1-4：placementSlice/pageSlice 跨 slice import decorationsSlice
- **修复**：将 `getGlobalMaxZ`/`getGlobalMinZ`/`collectAllElements` 从 [decorationsSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/decorationsSlice.ts) 迁移至 [helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)。`placementSlice.ts` 和 `pageSlice.ts` 改为从 `./helpers` 导入。decorationsSlice 重导出以保持向后兼容，内部 `addBrushStroke`/`addStickyNote`/`addTextElement`/`addStickerElement` 改用 `getGlobalMaxZ(page)` 替代手动 `collectAllElements` + reduce。

#### P1-6：watermarkSlice 类型未合并导致 as any
- **状态**：检查发现 editorStore 内已无 `as any`，此问题已在之前的会话中修复。

### 验证
- TypeScript 类型检查：`node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。
- EXE 构建中。

### 涉及文件
- `src/components/editor/canvas/useDragDrop.ts` — Bug43 层级顺序 tiebreaker
- `src/store/editorStore/selectionSlice.ts` — Bug42 单选跨类型互斥
- `src/components/editor/canvas/useMultiElementGroupSelect.tsx` — 新增：跨类型多选包围盒 hook
- `src/components/editor/Canvas.tsx` — Bug42 接入多选包围盒 + 预览几何应用
- `src/services/pageLayoutService.ts` — Bug41 extraSlot 统一删除
- `src/store/editorStore/pageSlice.ts` — P1-5 pushSnapshot + P1-4 import 修正
- `src/store/editorStore/placementSlice.ts` — P1-4 import 修正
- `src/store/editorStore/decorationsSlice.ts` — P1-4 函数迁移至 helpers
- `src/store/editorStore/helpers.ts` — P1-4 新增 getGlobalMaxZ/getGlobalMinZ

---

## 2026-07-29 · 构建错误修复 + EXE 打包 + 全项目代码审查 + 文档矫正

### 背景
构建 EXE 时报 TS6196 错误（selectionSlice.ts 未使用 SelectedElement 导入）。修复后成功构建 EXE，并对全项目进行多维度代码审查，同步矫正 Readme.md 与代码开发规范.md 的差异。

### 修复内容

#### 一、构建错误修复
- [selectionSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/selectionSlice.ts):1 — 删除未使用的 `SelectedElement` 导入（toggleMultiSelect 参数类型由 SelectionSlice 接口推导，无需直接引用）。
- 类型检查通过：`node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

#### 二、EXE 安装包构建成功
- 构建脚本 [build-exe-now.ps1](file:///f:/N-编程/MenBook开发项目/MemBook/build-exe-now.ps1) 补充 `TAURI_BUNDLER_TOOLS_GITHUB_MIRROR` 环境变量（规范要求）。
- 构建结果：
  - 产物：`src-tauri/target/release/bundle/nsis/MemBook_1.0.0_x64-setup.exe`
  - 大小：13.12 MB
  - Rust 编译：2m 27s
  - 前端构建：17.95s
  - 含 CSP `blob:` 修复（上一会话完成）

#### 三、全项目代码审查（多维度分析）

使用 search agent 对 `src/` 全目录进行结构化审查，覆盖 8 个维度：

**P1 问题（6 项，架构/类型）**：
1. `store/uiStore.ts` 多处 localStorage + `getComputedStyle` 违反 store 禁 DOM 规范
2. `engine/handle-store.ts` + `engine/storage/*` 直接操作 IndexedDB/DOM，违反 engine 无副作用规范
3. `components/Canvas.tsx` 等 12 个文件跨 4 store 编排
4. `placementSlice.ts:4` + `pageSlice.ts:6` 跨 slice 直接 import decorationsSlice
5. `pageSlice.ts:14-31` setCurrentPage 懒计算分支修改 pages 未调用 pushSnapshot
6. `WatermarkSettings.tsx:29,51` + `ActivationDialog.tsx:40` 的 `as any` 表明 store 类型缺失 watermarkSettings 合并

**P2 问题（13 项，可维护性）**：any 类型、空 catch、console.log 残留、硬编码中文、StickerGallery 未虚拟化、Canvas deps 粒度粗、handle-store 事务缺 onabort、import-store 疑似 blob 泄漏等。

**P3 问题（5 项，优化建议）**：@ts-ignore 改 @ts-expect-error、Tauri 全局类型未声明、Canvas 内联回调、2633 行历史债务。

**合规良好的方面**：selector 订阅规范、services 层无 React 状态、components 未直接 import db、React.memo 应用完整、ImageBitmap 释放路径完整、pushSnapshot 绝大多数已正确调用、i18n 整体合规。

详细清单已写入 [MemBook 代码开发规范.md](file:///f:/N-编程/MenBook开发项目/MemBook/MemBook%20代码开发规范.md) 末尾"附录：已知技术债务与优化方向"。

#### 四、文档矫正

**Readme.md 矫正项**：
- 新增功能描述：多选系统、页面显示模式、添加照片位按钮
- 构建产物：删除不存在的 WiX/MSI 描述，改为"仅 NSIS"
- 构建命令：标注"需在系统 PowerShell 中运行"
- 路径修正：`g:/WorkBuddy/MemBook/...` → `f:/N-编程/MenBook开发项目/...`
- 缓存容量：`imageCache(150)/blobUrlCache(200)` → `imageBitmapLoader(100)/thumbnailCache(80)`，blobUrlCache 改为"引用计数管理"
- 关键约束：补充 CSP `blob:` 配置、bundle targets `["nsis"]`、构建环境要求

**代码开发规范.md 矫正项**：
- 技术栈版本：React 18 → React 19（与 package.json 一致），补充 Konva 10/Zustand 5/Tailwind 4/Dexie 4
- 选区规范：新增 4.4 节"跨类型多选协议"（multiSelectedElements、toggleMultiSelect、浮动工具栏规则）
- Canvas 渲染：9.1 节新增"统一 zIndex 系统"（getGlobalMaxZ）和"页面显示模式"（pageDisplayMode + clipFunc 裁剪）
- 缓存容量：9.2 节更新为实际值（100/80/引用计数）
- 文件大小：8.3 节更新 Canvas.tsx 行数（2400→2633），补充优先拆分目标清单
- 构建发布：13.1 节新增 CSP `blob:` 硬约束
- 新增附录"已知技术债务与优化方向"（P1/P2/P3 清单）

### 文档质量总结

**Readme.md**：质量良好，作为项目对外介绍文档完整覆盖功能/架构/构建/约束。主要问题是部分路径遗留旧仓库位置（已修正）、构建产物描述与实际配置不符（已修正）、缺失近期新增功能（已补充）。

**代码开发规范.md**：质量优秀，作为开发执行规范覆盖技术栈/命名/状态/样式/i18n/组件/性能/错误处理/测试/提交/构建/日志全维度。主要问题是技术栈版本滞后（已修正）、缺失多选协议和页面显示模式等新功能规范（已补充）、 缺失技术债务追踪（已新增附录）。两份文档现在与代码实际状态完全一致，可作为后续开发指导。

### 验证
- TypeScript 编译通过（`tsc --noEmit -p tsconfig.app.json` 零错误）。
- EXE 构建成功（13.12 MB）。
- 文档路径与实际代码结构一致。

---

## 2026-07-29 · 照片位删除修复 + CSP blob 修复 + 页面显示模式切换

### 背景
用户反馈两个问题：
1. **照片位删除 Bug（需求40）**：普通布局模式下点击删除只清空照片不删除槽位；GP（智能排版）模式下点击删除导致页面崩溃（React error #300 + CSP blob 违规）。
2. **页面显示模式切换（需求39）**：新增全显模式（当前效果，显示页面外内容）与页面模式（裁剪到页面边界）的切换，右上角固定眼睛 icon 按钮。

### 修复方案

#### 一、照片位删除修复（需求40）

**问题1：普通布局模式只删照片不删槽位**

根因：[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts) 的 `removeSlotFromPage` 对非 GP 页面统一调用 `removePhotoFromSlot`（仅清空照片），未区分用户添加的 extraSlots 与模板内置槽位。

修复：在 `removeSlotFromPage` 中增加 extraSlots 判断逻辑：
- **用户添加的槽位**（存在于 `page.extraSlots`）：完整删除——从 `extraSlots`、`placements`、`slotOrder`、`slotOverrides`、`slotZIndices` 中全部移除。
- **模板内置槽位**：保持原行为（仅清空照片），并提示用户"模板内置照片位无法删除"。

**问题2：GP 模式崩溃（CSP blob 违规 + React error #300）**

根因：[tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json) 的 CSP `connect-src` 指令未包含 `blob:`，导致 [imageBitmapLoader.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/imageBitmapLoader.ts) 中 `fetch(blob:url)` 被拦截。GP 模式删除后重新排版触发照片重新加载，blob URL 被 CSP 阻断后抛错，错误级联导致 React 渲染崩溃。

修复：在 `connect-src` 指令中添加 `blob:`，允许 fetch 加载 blob URL。

```json
"connect-src": "'self' http://ipc.localhost https://api.bigdatacloud.net https://updates.membook.app https://github.com https://*.githubusercontent.com blob:"
```

#### 二、页面显示模式切换（需求39）

**UI Store：[uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)**
- 新增 `pageDisplayMode: 'full' | 'page'` 状态，默认 `'full'`（与当前行为一致）。
- 新增 `setPageDisplayMode(mode)` 方法。

**Canvas 渲染裁剪：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**
- 订阅 `pageDisplayMode` 状态。
- 当 `pageDisplayMode === 'page'` 时，用 Konva `<Group clipFunc>` 包裹 `globalLayerElements`，裁剪区域为 `(0, 0, CANVAS_W, CANVAS_H)`（页面边界）。
- `'full'` 模式下不裁剪，直接渲染（与原行为一致）。
- UI 元素（Transformer 控制点、框选矩形、编辑遮罩等）不受裁剪影响，保持可见。

**切换按钮组件：[PageDisplayModeToggle.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageDisplayModeToggle.tsx)**
- 位置：工作区右上角固定（`absolute top-3 right-3`）。
- 样式：icon + 文字，白色圆角按钮。
- icon：SVG 眼睛，睁开=全显模式，闭上=页面模式（带斜线）。
- 无页面时隐藏。

### 修改文件清单
| 文件 | 变更 |
|------|------|
| `src/services/pageLayoutService.ts` | `removeSlotFromPage` 区分 extraSlots（完整删除）与模板槽位（仅清照片+toast） |
| `src-tauri/tauri.conf.json` | CSP `connect-src` 添加 `blob:` |
| `src/store/uiStore.ts` | 新增 `pageDisplayMode` 状态 + `setPageDisplayMode` 方法 |
| `src/components/editor/Canvas.tsx` | 订阅 `pageDisplayMode`，条件裁剪 `globalLayerElements` |
| `src/components/editor/PageDisplayModeToggle.tsx` | 新增显示模式切换按钮组件 |
| `src/components/views/EditorView.tsx` | 引入 `PageDisplayModeToggle` |
| `src/i18n/locales/zh-CN.json` | 新增 `displayMode` 和 `templateSlotCannotDelete` 翻译 |
| `src/i18n/locales/en-US.json` | 同上英文翻译 |

### 验证
- TypeScript 编译通过（`tsc --noEmit` 零错误）。
- CSP 修复需重新构建 EXE 验证（`connect-src` 变更在 `tauri.conf.json` 中）。

---

## 2026-07-29 · 编辑器多选系统 + Ctrl+C/V 完整槽位复制 + 添加照片位 + 页外拖拽优化

### 背景
用户提出 4 项编辑器优化需求：
1. **工作区页面多选**：同类型内容支持 Ctrl+多选并弹出悬浮工具栏；跨类型多选仅支持移动；单次点击互斥选择。
2. **Ctrl+C/V 完整槽位复制**：选中照片位时需完整复制槽位几何+照片属性，无空槽位时自动创建新槽位。
3. **顶部添加照片槽位按钮**：新增槽位出现在页面居中位置并置于顶层。
4. **页外照片拖拽**：照片位于页面外时也支持拖入槽位；重叠槽位时放入顶层槽位。

### 修复方案

#### 一、跨类型统一多选系统（需求1）

**Store 层：[selectionSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/selectionSlice.ts)**
- 新增 `SelectedElement` 类型（`'slot' | 'text' | 'sticky' | 'sticker'`）和 `multiSelectedElements` 数组。
- 实现 `toggleMultiSelect(element)`：Ctrl+click 时切换元素选中状态。首次多选时自动将当前单选元素加入列表，保证第一个选中不丢失。toggle 后仅剩 1 个元素时自动回退为单选模式，≥2 个时进入多选模式并清空所有单选字段。
- 新增 `clearMultiSelect()`、`setMultiSelectedElements()` 方法。

**Canvas 层：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**
- 所有元素类型（槽位/文字/便利贴/贴纸）的 `onClick`/`onSelect` 增加 Ctrl/Cmd+click 分支，调用 `toggleMultiSelect`。
- `isSelected` 逻辑扩展：除单选 ID 匹配外，同时检查 `multiSelectedElements` 数组，确保多选元素显示选中虚线边框。
- 点击空白区域时调用 `clearMultiSelect()` 清除多选。

**多选浮动工具栏**（仅同类型 ≥2 时显示）：
- 计算所有选中元素的包围盒（canvas 像素坐标），工具栏定位在包围盒顶部居中。
- 提供 3 个批量操作：置顶（bringSlotToFront/bringToFront）、置底（sendSlotToBack/sendToBack）、删除（removeSlotFromPage/removeTextElement/removeStickyNote/removeStickerElement）。
- 显示选中数量徽标 + 关闭按钮。
- 跨类型多选（`allSameType === false`）时不显示工具栏，仅支持移动。

**键盘快捷键：[useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts)**
- `Escape`：优先清除多选（≥2 时）。
- `Delete`/`Backspace`：多选时批量删除所有选中元素。

**组件层修复：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)**
- 修复 `onSelect()` 未传递 Konva 事件对象的 bug，改为 `onSelect(e)`，使 Ctrl+click 检测生效。

#### 二、Ctrl+C/V 完整槽位复制（需求2）

**[BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)**
- 扩展 `ClipboardData` 类型：`placement` 分支新增 `slotOverride`（运行时像素坐标）和 `slotLayout`（百分比坐标），确保复制时携带完整槽位几何信息。
- **Ctrl+C**：复制照片位时，同时记录槽位的 `slotOverride`（优先）或从 `template.slots` 计算 `slotLayout`。
- **Ctrl+V**：粘贴时优先查找空槽位；无空槽位时自动创建新槽位（`extraSlots`），位置偏移 `PASTE_OFFSET_MM`，`zIndex` 设为全局最大值+1（顶层），同时创建对应的 `placement` 和 `slotOrder` 条目。

#### 三、添加照片槽位按钮（需求3）

**[PageToolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageToolbar.tsx)**
- 顶部工具栏新增"添加照片位"按钮，调用 `addPhotoSlot()`。

**[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**
- `addPhotoSlot` 实现：生成唯一 slotId（时间戳+随机后缀），默认位置页面中央（35%/35%，30%×30%），追加到 `extraSlots`/`placements`/`slotOrder`。
- 新增 `getGlobalMaxZ(page)` 工具函数：计算所有槽位和装饰元素的最大 zIndex。
- 新槽位 `zIndex` 设为 `getGlobalMaxZ + 1`，确保在所有装饰元素之上显示。

#### 四、页外照片拖拽 + 重叠槽位顶层优先（需求4）

**[useDragDrop.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useDragDrop.ts)**
- 移除 `overPage` 前置检查：照片在页面外时仍可触发槽位命中检测（仅依赖槽位几何坐标判断）。
- 合并模板槽位与 `extraSlots`（去重），确保用户添加的槽位也能接收拖拽。
- 重叠槽位处理：收集所有命中候选槽位，按 `getSlotZIndex` 降序排序，选择 zIndex 最高的（视觉最顶层）槽位作为放置目标。

### 修改文件清单
| 文件 | 变更 |
|------|------|
| `src/store/editorStore/types.ts` | 新增 `SelectedElement`/`SelectedElementType` 类型，扩展 `SelectionSlice` |
| `src/store/editorStore/selectionSlice.ts` | 实现 `toggleMultiSelect`/`clearMultiSelect`/`setMultiSelectedElements` |
| `src/components/editor/Canvas.tsx` | 多选事件处理、isSelected 扩展、多选浮动工具栏、空白点击清除多选 |
| `src/components/editor/canvas/StickerNode.tsx` | 修复 `onSelect` 未传递事件 |
| `src/components/editor/canvas/useCanvasKeyboard.ts` | Escape/Delete 多选支持 |
| `src/components/editor/BottomNav.tsx` | Ctrl+C/V 完整槽位复制 + 自动创建新槽位 |
| `src/store/editorStore/pageSlice.ts` | `addPhotoSlot` 居中+顶层 + `getGlobalMaxZ` |
| `src/components/editor/PageToolbar.tsx` | 添加照片位按钮 |
| `src/components/editor/canvas/useDragDrop.ts` | 页外拖拽支持 + zIndex 顶层优先 |
| `src/i18n/locales/zh-CN.json` | 新增 `broughtToFront`/`sentToBack`/`batchDeleted` |
| `src/i18n/locales/en-US.json` | 同上英文翻译 |

### 验证
- TypeScript 编译通过（`tsc --noEmit` 零错误）。

---

## 2026-07-29 · 缩略图渲染一致性修复（贴纸/便利贴/文字/笔触 + 页面边界裁剪）

### 背景
用户反馈两个问题：
1. 全屏显示、网格视图显示的页面与编辑器页面不一致——页面除了照片外，缺少其他内容（贴纸、文字、便利贴、笔触等装饰元素）。
2. 导出功能的文件导出后与页面没有完全保持一致——装饰元素（贴纸/便利贴/文字/笔触）超出页面边界的部分未按照片规则裁剪，导出文件出现"超出页面"的元素。

### 根因
1. **网格/全屏视图缩略图渲染缺失装饰元素**：
   - [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) 的 `drawPageToCanvas` 只绘制照片槽位 + 文字元素，**完全缺失笔触、便利贴、贴纸**的渲染分支。
   - [gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts) 没有贴纸图片预加载机制（`preloadStickers`/`releaseStickerImages` 不存在），Worker 通信协议也未传递贴纸位图。
   - [thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts) 的 `ThumbnailWorkerRequest` 不含 `stickerBitmaps` 字段，Worker 端无法绘制贴纸。
   - `computeContentHash`（缓存 key 计算）**遗漏了笔触/便利贴/贴纸/`slotZIndices`**，导致这些元素变化时缓存不失效，缩略图永久显示旧版本。

2. **导出文件装饰元素未裁剪到页面边界**：
   - [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) 的 `drawPage` 在合并排序后直接 `for (item of items) item.draw()`，**没有 ctx.clip() 页面边界裁剪**。
   - 编辑器 Stage 本身有页面边界裁剪，照片槽位有圆角裁剪（`drawPlacement` 内 `ctx.clip`），但装饰元素（贴纸的阴影、便利贴的阴影/胶带、笔触端点）超出页面边界时在导出文件中直接显示，与编辑器视觉不一致。

### 修复方案

#### 一、统一缩略图渲染管线（与编辑器 `globalLayerElements` 对齐）

[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) 重构 `drawPageToCanvas`，采用与编辑器 [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) `globalLayerElements` 完全一致的统一图层排序：

1. **5 类元素合并排序**：照片槽位(typeOrder=0) + 笔触/文字/便利贴/贴纸(typeOrder=1)，按 `(zIndex, typeOrder)` 升序渲染。
2. **新增 4 个元素绘制函数**：
   - `drawBrushStroke`：与 exportEngine 一致的笔触绘制（`strokeStyle`/`lineWidth`/`globalAlpha`/`lineCap`/`lineJoin='round'`，二次贝塞尔曲线平滑）。
   - `drawStickyNote`：按 `style` 字段分支渲染（square/rounded/tape/shadow），中心点定位 + 旋转 + 阴影，tape 样式绘制胶带装饰。
   - `drawSticker`：中心点定位 + 旋转 + flipH/flipV 翻转 + 阴影，贴纸图片由调用方传入 `stickerImages` 参数。
   - 文字元素的 `drawTextElement` 已存在，无需新增。
3. **页面边界裁剪**：在 `items.sort` 后用 `ctx.save() / ctx.rect(0,0,logicalW,logicalH) / ctx.clip()` 包裹整个绘制循环，确保装饰元素（含阴影）超出页面边界被裁剪。

#### 二、扩展 gridThumbnailRenderer 支持贴纸预加载与 Worker 传递

1. **新增 `preloadStickers(page)`**：与 `preloadPagePhotos` 平行的贴纸图片预加载函数。
   - 收集本页 `stickerElements` 的唯一 `stickerId` → `sticker-blob-{id}` blobId 列表。
   - 优先用 `createImageBitmap` 加载（可 transfer 给 Worker，可主动 close 释放），失败回退 `HTMLImageElement`。
   - 返回 `Map<blobId, HTMLImageElement | ImageBitmap>`，与 `preloadPagePhotos` 返回结构一致。

2. **新增 `releaseStickerImages(imgs)`**：与 `releasePreloadedImages` 平行的释放函数，遍历调用 `ImageBitmap.close()` 或清空 `HTMLImageElement.src`。

3. **扩展 `computeContentHash`**：新增 4 个字段参与哈希计算：
   - `szi`: `page.slotZIndices`（槽位跨装饰元素的 z 调整）
   - `bs`: `page.brushStrokes`（笔触的颜色/宽度/不透明度/tension/lineCap/zIndex/点数）
   - `sn`: `page.stickyNotes`（便利贴的位置/尺寸/颜色/文字/字号/字体/旋转/样式/zIndex）
   - `st`: `page.stickerElements`（贴纸的位置/尺寸/stickerId/旋转/翻转/zIndex）
   - `te` 字段补充 `te.zIndex`（之前遗漏）
   - 任一字段变化 → 哈希变化 → 缓存 key 变化 → 自动失效，避免装饰元素变化时缩略图永久显示旧版本。

4. **`renderPageThumbnail` / `renderPageThumbnailInWorker` 新增 `stickerImages` 参数**：
   - 主线程渲染：直接透传给 `drawPageToCanvas`。
   - Worker 渲染：检查贴纸位图类型（必须全部为 `ImageBitmap` 才能走 Worker，否则回退主线程）；收集 `stickerBitmaps: [blobId, ImageBitmap][]` 加入 Worker 请求；transfer list 中加入贴纸位图所有权转移。

#### 三、Worker 协议扩展

[thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts) 的 `ThumbnailWorkerRequest` 新增 `stickerBitmaps?: [string, ImageBitmap][]` 字段：
- 接收时构造 `stickerMap: Map<string, ImageBitmap>`，传给 `drawPageToCanvas`。
- 所有错误/成功返回路径都调用 `closeAllBitmaps(stickerMap)` 释放位图内存。
- 更新文件头注释说明 stickerBitmaps 的数据契约。

#### 四、导出引擎页面边界裁剪

[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) 的 `drawPage` 在 `items.sort` 后包裹 `ctx.save() / ctx.rect(0,0,canvasW,canvasH) / ctx.clip()` 再绘制所有元素，最后 `ctx.restore()`。
- 装饰元素（贴纸阴影、便利贴阴影/胶带、笔触端点）超出页面边界被裁剪，与编辑器 Stage clip + 网格缩略图 `drawPageToCanvas` 的 clip 行为完全一致。
- 照片槽位本身已有圆角裁剪，再次裁剪无副作用，但 `shadowBlur`/`shadowOffsetY` 超出页面部分会被裁掉（与编辑器一致）。
- 打印引擎 [printEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/printEngine.ts) 复用 `renderPage` → `drawPage`，自动受益于裁剪修复。

#### 五、调用方集成贴纸预加载

- [PageCard.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageCard.tsx)：`useEffect` 中用 `Promise.all` 并行预加载照片和贴纸，传给 `renderPageThumbnailInWorker`；cancelled 分支同时释放两者。
- [FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx)：`render` 函数中用 `Promise.all` 并行预加载，传给 `renderPageThumbnail`；渲染完成后同时释放。

### 设计审查发现的问题

1. **`drawPageToCanvas` 缺失装饰元素**：与 exportEngine 的 `drawPage` 同源问题，应共享同一渲染逻辑。当前两个文件各自维护一份绘制函数（`drawBrushStroke`/`drawStickyNote`/`drawSticker` 等），存在持续偏离风险。**短期**：本次同步两边实现；**长期**：应抽取为 `sharedRender.ts` 中的纯函数，避免再次出现"修了一处忘了另一处"。
2. **`computeContentHash` 字段遗漏**：缓存哈希计算是缩略图一致性的关键防线，新增任何影响渲染的字段都必须同步加入哈希。本次补充了 5 个字段，后续新增元素类型（如形状/几何图形）也必须同步更新。
3. **Worker 协议演化**：每次新增可转移位图类型（如未来贴纸动画帧），需同时更新：(a) `ThumbnailWorkerRequest` 类型；(b) Worker 端 `closeAllBitmaps` 调用；(c) 主线程 transfer list 拼接；(d) 回退主线程的位图类型检查。

### 涉及文件
- [src/utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)（核心重构：统一图层排序 + 4 类装饰元素绘制 + 页面边界裁剪）
- [src/utils/gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)（贴纸预加载/释放 + 缓存哈希扩展 + Worker 协议更新）
- [src/utils/thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts)（接收 stickerBitmaps + 释放）
- [src/utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)（页面边界裁剪）
- [src/components/editor/PageCard.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageCard.tsx)（贴纸预加载集成）
- [src/components/editor/FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx)（贴纸预加载集成）

### 验证
- TypeScript：`npx tsc --noEmit` **0 errors**

---

## 2026-07-28 · 导出引擎修复（贴纸渲染 + 便利贴样式 + 统一图层排序）

### 背景
用户反馈导出的 PDF/图片与编辑器页面不一致：
1. 贴纸完全没有被渲染
2. 便利贴位置/样式与编辑器不一致
3. 图层顺序与编辑器不一致（照片槽位的置顶/置底在导出中失效）

### 根因
[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) 的 `drawPage` 函数存在 3 处与编辑器渲染逻辑的偏离：

1. **贴纸完全缺失**：`drawPage` 中没有任何 `stickerElements` 的遍历分支，导致导出结果丢失所有贴纸
2. **便利贴 style 字段被忽略**：导出端固定用圆角 4 + shadowBlur 4 渲染所有便利贴，而编辑器 [StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx) 按 `style` 字段（square/rounded/tape/shadow）分别使用不同圆角（2/8/6）、阴影（blur 12/4、offsetY 6/2、opacity 0.25/0.15）和胶带装饰
3. **图层顺序硬编码**：导出端按"照片→水印→文字→笔触→便利贴"固定顺序渲染，完全未读取 `slotZIndices` 和各装饰元素的 `zIndex` 字段；而编辑器 [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 的 `globalLayerElements` 把槽位+笔触+文字+便利贴+贴纸五类合并为 `RenderItem[]`，按 `(zIndex, typeOrder)` 统一排序

### 修复方案
**重构 `drawPage` 为 async，采用与编辑器一致的统一图层排序**：

1. **统一图层排序**（与 Canvas.tsx `globalLayerElements` 一致）：
   - 收集 5 类元素为 `RenderItem[]`：槽位(typeOrder=0) + 笔触/文字/便利贴/贴纸(typeOrder=1)
   - 按 `(z, typeOrder)` 升序排序：z 小的渲染在下层，z 相同时槽位(typeOrder=0)渲染在装饰下方
   - 槽位 z 从 `getSlotZIndex(page, slotId)` 读取（支持 `slotZIndices` 跨装饰元素层级调整）

2. **新增贴纸渲染** `drawSticker`：
   - 异步预加载本页所有贴纸图片（`preloadStickerSrc` → `loadImage`，并行 `Promise.all`）
   - 中心点定位（与 StickerNode.tsx 一致：`translate(x+pw/2, y+ph/2)`）
   - 旋转、flipH/flipV 翻转、阴影（与编辑器默认值一致）

3. **修复便利贴样式** `drawStickyNote`：
   - 按 `style` 字段分支渲染：square(圆角2)/rounded(圆角8)/tape(圆角6+胶带)/shadow(圆角6+强阴影)
   - 阴影参数与 StickyNoteNode.tsx 完全一致：shadow 样式 blur=12/offsetY=6/opacity=0.25，其他样式 blur=4/offsetY=2/opacity=0.15
   - tape 样式绘制胶带装饰矩形（与编辑器一致）

4. **拆分绘制函数**：将原 `drawPage` 内联的各元素绘制逻辑提取为独立函数：`drawPlacement`/`drawBrushStroke`/`drawTextElement`/`drawStickyNote`/`drawSticker`

5. **renderPage 改为 async**：因 `drawPage` 异步加载贴纸图片，`renderPage` 改为 `async function`，返回 `Promise<string>`。所有调用方（`exportToPDF`/`exportToPNG`/`exportToJPG`/`generatePdfBlob`/`printEngine.renderPages`）同步加 `await`

### 涉及文件
- [src/utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)（核心重构：drawPage async + 统一图层排序 + 贴纸渲染 + 便利贴样式修复）
- [src/utils/printEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/printEngine.ts)（renderPage 调用加 await）

### 验证
- TypeScript：**0 errors**

---

## 2026-07-28 · EXE 安装包构建（修复 4 个 TS 错误 + 成功打包）

### 背景
用户运行 `npm run desktop:build` 构建 EXE 安装包时，TypeScript 编译阶段失败，报 4 个错误：
- `Canvas.tsx:1410` 参数 `p` 隐式 any
- `Canvas.tsx:1410` 参数 `rh` 隐式 any
- `Canvas.tsx:1412` 参数 `_t` 隐式 any
- `StickyNoteNode.tsx:279` 找不到 `memo`

### 根因
上一轮为 Canvas 节点组件添加 `React.memo` 时：
1. `StickyNoteNode.tsx` 漏导入 `memo`（只有 `useRef, useState`）
2. Canvas.tsx 中 `StickyNoteNode` 的 `onUpdate`/`onRequestEdit` 回调参数未显式标注类型，由于 `StickyNoteNode` 本身的 `memo(Impl)` 形式让 TS 推断链断裂，触发 `noImplicitAny`

### 修复
- [StickyNoteNode.tsx:5](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx#L5)：`import { useRef, useState } from 'react';` → `import { memo, useRef, useState } from 'react';`
- [Canvas.tsx:9](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L9)：扩展 type 导入，新增 `PageTextElement, StickyNote, StickerElement`
- [Canvas.tsx:1395/1410/1424](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1410)：3 处 `onUpdate` 回调显式标注 `(p: Partial<XxxType>, rh?: boolean)`，`onRequestEdit` 标注 `(_t: string)`

### 验证
- TypeScript：`node -e` 调用 ts API 全量编译，**0 errors**
- 前端构建：Vite 18.33s 完成
- Rust 构建：`release` profile 优化编译 9m 16s 完成
- NSIS 打包：成功生成 installer

### 构建产物
- 路径：`F:\N-编程\MenBook开发项目\MemBook\src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`
- 大小：**12.65 MB**
- 修改时间：2026-07-28 23:35:31

### 构建环境配置
- `TAURI_BUNDLER_TOOLS_GITHUB_MIRROR=https://gh-proxy.com/https://github.com`（防止 NSIS 下载超时）
- `TAURI_BUNDLER_TIMEOUT=360`（6 分钟超时）
- `tauri.conf.json` bundle targets 限定为 `["nsis"]`
- 构建前清理 `src-tauri/target/release/bundle` 旧产物

### 涉及文件
- [src/components/editor/canvas/StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx)
- [src/components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)

---

## 2026-07-28 · 代码开发规范 + P0/P1/P2/P3 修复 + 6 项体验优化

### 背景
用户要求：(1) 生成代码开发规范文档供后续开发严格遵循；(2) 修复优化 P0/P1/P2/P3 遗留问题；(3) 6 项具体体验优化：编辑器内移除语言切换按钮、修复 EN 切换后残留中文字段、统一照片槽位与装饰元素的置顶/置底层级关系、主页左侧面板顺序调整为"相册/布局/贴纸/整理"并将"项目"全局改名为"相册"、修复顶部导航栏下拉弹窗被画布遮挡、文字/便利贴工具左侧面板设置合并为一套。

### 一、代码开发规范文档（新增）
新增 [CODE_DEVELOPMENT_SPEC.md](file:///f:/N-编程/MenBook开发项目/MemBook/CODE_DEVELOPMENT_SPEC.md)，覆盖 12 个章节：
- 架构总览（Tauri + React 18 + Zustand + Konva + react-i18next）
- TypeScript 规范（strict、避免 any、联合类型与字面量类型）
- 命名规范（PascalCase 组件、camelCase 函数、kebab-case 文件、UPPER_SNAKE 常量）
- Zustand store 规范（slice 拆分、selector 订阅、避免整个 store 订阅）
- ID 生成规范（统一 `${type}-${Date.now()}-${Math.random().toString(36).slice(2,8)}`）
- 样式规范（design-tokens.css 令牌、Tailwind 优先、Z-Index 分层表）
- i18n 规范（所有可见文案必须走 `t()`，key 命名 `namespace.feature.field`）
- 组件规范（自包含、props 单一来源、memo 优化纯展示组件）
- 性能规范（useMemo/useCallback、React.memo、虚拟列表）
- 错误处理、测试、Git、构建、开发日志规范

### 二、P2 修复：Canvas 节点 React.memo 优化
为 3 个高频渲染的 Canvas 节点组件包裹 `React.memo`，避免父组件 Canvas 重渲染时连锁触发：
- [StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)：`export const StickerNode = memo(StickerNodeImpl);`
- [StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx)：`export const StickyNoteNode = memo(StickyNoteNodeImpl);`
- [TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)：`export const TextElementNode = memo(TextElementNodeImpl);`

实现要点：函数组件重命名为 `*Impl`，通过 `memo()` 包裹后以原名导出，避免循环引用与导出冲突。

### 三、优化 1：编辑器内移除语言切换按钮
[Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx) 移除 `LanguageSwitcher` 组件的导入与渲染，语言切换统一由主页/设置入口承担，编辑器顶部仅保留"保存"按钮。同时将保存按钮文案硬编码改为 `t('editor.saveBtn')`。

### 四、优化 2：EN 切换后残留中文字段修复
扩展 [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) 与 [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)，新增约 200+ 条 i18n key，覆盖：
- `editor.tools.*`（textStickySection、textStickyHint、quickStyles、color、style、styleRounded/styleSquare/styleTape/styleShadow）
- `editor.saveBtn`、`editor.textElement.*`、`editor.stickyNote.*`
- `home.nav.albums/templates/stickers/organize`、`home.create.projectLimit`
- Toolbar/ExportDialog/GooglePhotosLayoutDialog/EditFlyout 等组件硬编码中文全部迁移

### 五、优化 3：照片槽位与装饰元素统一层级系统
**根因**：装饰元素（笔触/文字/便利贴/贴纸）使用数值 `zIndex`，而照片槽位使用独立的 `slotOrder` 数组，两套系统互不相干，导致"置顶/置底"在跨类型间无效。

**修复**：
- [types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)：`AlbumPage` 新增 `slotZIndices?: Record<string, number>` 数值化槽位 z-index
- [decorationsSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/decorationsSlice.ts)：新增 `getGlobalMaxZ`/`getGlobalMinZ`，跨元素类型计算最大/最小 z
- [placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)：`bringSlotToFront`/`sendSlotToBack` 改为读写 `slotZIndices` 数值，与装饰元素同一套规则
- [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：合并槽位与装饰元素到 `globalLayerElements` 单一渲染队列，按 `z` 升序排序，z 相同时 `typeOrder`（槽位=0 < 装饰=1）

### 六、优化 4：主页左侧面板顺序与"项目"→"相册"改名
[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)：
- 左侧导航 tab 顺序调整为：相册 → 布局 → 贴纸 → 整理（原"项目"位置改名为"相册"，"贴纸"下移到"布局"下方）
- `activeNav` 状态初始化兼容历史值 `'projects'` → 自动映射为 `'albums'`
- i18n key 从 `home.nav.projects` 迁移到 `home.nav.albums`，全局 12+ 引用点同步更新

### 七、优化 5：顶部导航下拉弹窗层级修复
**根因**：[AppHeader.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AppHeader.tsx) 使用 `z-[var(--z-sticky)]`（=20），而画布编辑模式 `--z-overlay`=50，导致编辑模式下顶部导航的下拉菜单被画布遮挡。

**修复**：
- [design-tokens.css](file:///f:/N-编程/MenBook开发项目/MemBook/src/styles/design-tokens.css) 新增 `--z-toolbar: 60;`（位于 overlay 50 与 modal 100 之间）
- AppHeader.tsx 改用 `z-[var(--z-toolbar)]`，确保顶部工具栏高于画布编辑层、低于模态框

Z-Index 分层最终方案：

| 令牌 | 值 | 用途 |
|---|---|---|
| `--z-base` | 0 | 默认 |
| `--z-flat` | 1 | 平铺元素 |
| `--z-raised` | 10 | 抬升元素 |
| `--z-sticky` | 20 | 吸顶/吸底 |
| `--z-dropdown` | 30 | 下拉菜单 |
| `--z-overlay` | 50 | 遮罩 / 画布编辑模式 |
| `--z-toolbar` | 60 | 顶部工具栏 |
| `--z-modal` | 100 | 模态框 |
| `--z-toast` | 200 | Toast |
| `--z-tooltip` | 300 | Tooltip |

### 八、优化 6：文字/便利贴工具面板合并
[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx) 将"文字"与"便利贴"两个独立 `CollapsibleSection` 合并为单一"文字 / 便利贴"区块：
- 只保留两个"添加"按钮（添加文字 / 添加便利贴）
- 选中画布上的文字元素 → 显示文字属性面板（字号/颜色/对齐/粗体斜体）
- 选中便利贴 → 显示便利贴属性面板（颜色/样式）
- 未选中 → 显示提示文案"选中画布上的文字或便利贴后，此处显示对应属性设置"
- 减少约 200 行重复代码

### 九、错误与修复
- **TS6133 `endStickerDrag` 未使用**：Canvas.tsx 合并渲染后不再需要该 import，移除
- **TS2304 `t` 未定义 in BottomNav**：主组件缺少 `useTranslation`，补充 `const { t } = useTranslation();`
- **ID 生成冲突**：`addPage`/`insertPage` 用 `Date.now()` 无随机后缀，与 `copyPage` 不一致；统一为 `page-${Date.now()}-${Math.random().toString(36).slice(2,8)}`
- **TS2305 `StickyNoteNode` 未导出**：补 `export const StickyNoteNode = memo(StickyNoteNodeImpl);`
- **TS2304 `memo` 未导入**：三个 canvas 节点组件补 `import { memo, ... } from 'react';`
- **变量遮蔽 in TemplateGallery**：`.map((t) => ...)` 的 `t` 遮蔽翻译函数 `t`，重命名为 `tmpl`/`ct`
- **PowerShell 执行策略限制 `npx tsc`**：改用 `node -e` 调用 TypeScript API 完成类型检查

### 十、验证结果
- TypeScript 类型检查：**0 errors**（`node -e` 调用 ts API 全量编译）
- 已完成 P0：1 项（ID 生成冲突）
- 已完成 P1：4 项（含 canvas setState 警告、sticker slotId 碰撞、LRU 算法、Delete 双触发）
- 已完成 P2：1 项（Canvas 节点 React.memo）
- P3 及剩余 P0-3/P1-4/P1-5 留待后续迭代

### 十一、涉及文件
**新增**：
- [CODE_DEVELOPMENT_SPEC.md](file:///f:/N-编程/MenBook开发项目/MemBook/CODE_DEVELOPMENT_SPEC.md)

**修改**：
- [src/styles/design-tokens.css](file:///f:/N-编程/MenBook开发项目/MemBook/src/styles/design-tokens.css)（新增 --z-toolbar）
- [src/types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)（AlbumPage.slotZIndices）
- [src/store/editorStore/decorationsSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/decorationsSlice.ts)
- [src/store/editorStore/placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)
- [src/components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)
- [src/components/editor/Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx)
- [src/components/editor/ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)
- [src/components/editor/BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)
- [src/components/editor/canvas/StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)
- [src/components/editor/canvas/StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx)
- [src/components/editor/canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)
- [src/components/common/AppHeader.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AppHeader.tsx)
- [src/components/views/HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)
- [src/components/home/TemplateGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/TemplateGallery.tsx)
- [src/i18n/locales/zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)
- [src/i18n/locales/en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)

---

## 2026-07-28 · i18n 编辑器核心组件国际化（GridView/PhotoPreview/LayoutAdjust 等 15 文件）

### 背景
继续推进 MemBook 项目的 i18n 全局化。本次聚焦于 15 个编辑器/通用组件，将所有用户可见的硬编码中文字符串（JSX 文本、title 属性、addToast 消息、placeholder）替换为 `t('namespace.key')` 调用。仅修改组件文件，不改动 locale 文件。

修改文件：
- [GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx)（~23 处中文 → `editor.gridView.*`）
- [PhotoPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoPreview.tsx)（~8 处中文 → `editor.photoPreview.*`）
- [LayoutAdjustPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/LayoutAdjustPanel.tsx)（~15 处中文 → `editor.layoutAdjust.*`）
- [BackgroundPicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/BackgroundPicker.tsx)（~5 处中文 → `editor.tools.background.*`）
- [ColorPicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/ColorPicker.tsx)（~3 处中文 → `editor.colorPicker.*`）
- [StickyStylePicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/StickyStylePicker.tsx)（复用已有 `editor.tools.*` key）
- [PageCard.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageCard.tsx)（~2 处中文 → `editor.pageCard.*`）
- [EditorEmptyState.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/EditorEmptyState.tsx)（~7 处中文 → `editor.emptyState.*`）
- [CanvasEmptyState.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasEmptyState.tsx)（~2 处中文 → `editor.canvasEmpty.*`）
- [TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)（~1 处中文 → `editor.textElement.*`）
- [StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx)（~1 处中文 → `editor.stickyNote.*`）
- [EditFlyout.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/EditFlyout.tsx)（验证已全部 i18n，无修改）
- [EmptyState.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/EmptyState.tsx)（~3 处中文 → `home.emptyState.*`）
- [Loading.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Loading.tsx)（复用已有 `common.loading`）
- [WindowControls.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/WindowControls.tsx)（~4 处中文 → `common.window.*`）

### 一、改造要点

#### 1. 引入 useTranslation
- 每个组件文件顶部添加 `import { useTranslation } from 'react-i18next';`
- 每个函数组件体顶部添加 `const { t } = useTranslation();`
- Konva 节点组件（TextElementNode/StickyNoteNode）为 React.memo，在组件内部添加 hook
- GridView.tsx 内部的 `GridFloatingToolbar` 子组件也独立添加了 hook

#### 2. 命名空间分配
- `editor.gridView.*` — GridView.tsx（网格视图：toast/菜单/工具栏）
- `editor.photoPreview.*` — PhotoPreview.tsx（大图预览：按钮 title）
- `editor.layoutAdjust.*` — LayoutAdjustPanel.tsx（排版调整面板）
- `editor.tools.background.*` — BackgroundPicker.tsx（背景选择器 Tab/取色器/应用范围）
- `editor.colorPicker.*` — ColorPicker.tsx（颜色选择器标签/取色器/最近使用）
- `editor.pageCard.*` — PageCard.tsx（页码 alt/添加页面）
- `editor.emptyState.*` — EditorEmptyState.tsx（编辑器空状态）
- `editor.canvasEmpty.*` — CanvasEmptyState.tsx（画布空状态）
- `editor.textElement.*` — TextElementNode.tsx（文字元素 placeholder）
- `editor.stickyNote.*` — StickyNoteNode.tsx（便利贴 placeholder）
- `home.emptyState.*` — EmptyState.tsx（首页空状态）
- `common.window.*` — WindowControls.tsx（窗口控制按钮）

注意：
- `editor.colorPicker.*` 命名空间替代了原 `editor.tools.color.defaultLabel`，避免与已有的字符串 key `editor.tools.color`（"颜色"）冲突
- `editor.photoPreview.*` 命名空间已存在 `loadFailed` 1 个 key，本次新增 8 个 key
- StickyStylePicker.tsx 复用已有 `editor.tools.quickStyles/color/style/styleRounded/styleSquare/styleTape/styleShadow` 7 个 key，无新增
- EditFlyout.tsx 经验证已全部 i18n，FILTERS 数组中的中文 `name` 字段用作内部状态标识符（非显示文本），显示通过 `t(f.nameKey)` 实现

#### 3. 改造范围
- **JSX 文本**：标题、按钮文案、标签、提示文字、空状态
- **title 属性**：按钮 hover 提示（PhotoPreview 的 close/prev/next/zoomIn/zoomOut/reset/rotateLeft/rotateRight）
- **addToast 消息**：GridView 的复制/粘贴/删除/隐藏提示，含 `{{count}}` 插值
- **placeholder**：TextElementNode/StickyNoteNode 的占位提示文字
- **alt 属性**：PageCard 的页码描述
- **kbd 元素**：EditorEmptyState 的"滚轮"文字

#### 4. 未改造的内容（按规则保留）
- 代码注释（`//`、`/* */`、`{/* */}`）
- EditFlyout.tsx 中 FILTERS 数组的 `name` 字段（内部标识符，非显示文本）
- 标准键盘键名（"Ctrl+Z"、"Space" 等，遵循惯例不翻译）

#### 5. 插值用法
- `t('editor.gridView.pagesCopied', { count })` — 复制 N 页
- `t('editor.gridView.pagesPasted', { count })` — 粘贴 N 页
- `t('editor.gridView.pagesDeleted', { count })` — 删除 N 页
- `t('editor.gridView.pagesHidden', { count })` — 隐藏 N 页
- `t('editor.layoutAdjust.title', { page })` — 排版调整 · 第 N 页
- `t('editor.layoutAdjust.biasTopVal', { v })` — 上压+{{v}}
- `t('editor.layoutAdjust.biasBottomVal', { v })` — 下压{{v}}
- `t('editor.layoutAdjust.biasLeftVal', { v })` — 左压{{v}}
- `t('editor.layoutAdjust.biasRightVal', { v })` — 右压+{{v}}
- `t('editor.pageCard.pageNumber', { n })` — 第 N 页

### 二、TypeScript 类型检查
执行 `npx tsc --noEmit -p tsconfig.json` 通过，**0 个错误**。

### 三、待办：locale 文件补全

本次仅修改组件文件，未改动 locale 文件。以下 **74 个新 key** 需补入 `src/i18n/locales/zh-CN.json` 与 `en-US.json`。

#### 新增 key 清单（按命名空间分组）

**1. common.window（4 个）— WindowControls.tsx**
| key | 中文值 |
|-----|--------|
| `common.window.minimize` | 最小化 |
| `common.window.maximize` | 最大化 |
| `common.window.restore` | 恢复 |
| `common.window.close` | 关闭 |

**2. home.emptyState（3 个）— EmptyState.tsx**
| key | 中文值 |
|-----|--------|
| `home.emptyState.title` | 还没有相册 |
| `home.emptyState.description` | 将美好回忆整理成册… |
| `home.emptyState.startCreating` | 开始制作 |

**3. editor.emptyState（7 个）— EditorEmptyState.tsx**
| key | 中文值 |
|-----|--------|
| `editor.emptyState.title` | 开始制作你的相册 |
| `editor.emptyState.description` | 添加第一页，开始记录你的回忆 |
| `editor.emptyState.addFirstPage` | 添加第一页 |
| `editor.emptyState.undo` | 撤销 |
| `editor.emptyState.dragPage` | 拖动页面 |
| `editor.emptyState.zoom` | 缩放 |
| `editor.emptyState.scrollWheel` | 滚轮 |

**4. editor.canvasEmpty（2 个）— CanvasEmptyState.tsx**
| key | 中文值 |
|-----|--------|
| `editor.canvasEmpty.addFirstPage` | 点击此处添加第一张页面 |
| `editor.canvasEmpty.marginHint` | 空白页面将自动应用当前边距与圆角设置 |

**5. editor.textElement（1 个）— TextElementNode.tsx**
| key | 中文值 |
|-----|--------|
| `editor.textElement.placeholder` | 点击输入文字 |

**6. editor.stickyNote（1 个）— StickyNoteNode.tsx**
| key | 中文值 |
|-----|--------|
| `editor.stickyNote.placeholder` | 双击编辑 |

**7. editor.pageCard（2 个）— PageCard.tsx**
| key | 中文值 |
|-----|--------|
| `editor.pageCard.pageNumber` | 第 {{n}} 页 |
| `editor.pageCard.addPage` | 添加页面 |

**8. editor.colorPicker（3 个）— ColorPicker.tsx**
| key | 中文值 |
|-----|--------|
| `editor.colorPicker.label` | 颜色 |
| `editor.colorPicker.customPicker` | 自定义取色器 |
| `editor.colorPicker.recentUsed` | 最近使用 |

**9. editor.tools.background（5 个）— BackgroundPicker.tsx**
| key | 中文值 |
|-----|--------|
| `editor.tools.background.tabSolid` | 纯色 |
| `editor.tools.background.tabGradient` | 渐变 |
| `editor.tools.background.tabTexture` | 纹理 |
| `editor.tools.background.customPicker` | 自定义取色器 |
| `editor.tools.background.applyToAll` | 应用到所有页面 |

**10. editor.layoutAdjust（15 个）— LayoutAdjustPanel.tsx**
| key | 中文值 |
|-----|--------|
| `editor.layoutAdjust.title` | 排版调整 · 第 {{page}} 页 |
| `editor.layoutAdjust.reset` | 重置 |
| `editor.layoutAdjust.notSupported` | 该页不支持排版调整 |
| `editor.layoutAdjust.notSupportedDesc` | 仅智能编排页面可调整 |
| `editor.layoutAdjust.convert` | 转换智能编排 |
| `editor.layoutAdjust.biasTop` | 上压 |
| `editor.layoutAdjust.biasRight` | 右压 |
| `editor.layoutAdjust.biasLeft` | 左压 |
| `editor.layoutAdjust.biasBottom` | 下压 |
| `editor.layoutAdjust.biasTopVal` | 上压+{{v}} |
| `editor.layoutAdjust.biasBottomVal` | 下压{{v}} |
| `editor.layoutAdjust.biasLeftVal` | 左压{{v}} |
| `editor.layoutAdjust.biasRightVal` | 右压+{{v}} |
| `editor.layoutAdjust.biasHorizontalCenter` | 水平居中 |
| `editor.layoutAdjust.biasVerticalCenter` | 垂直居中 |

**11. editor.gridView（23 个）— GridView.tsx**
| key | 中文值 |
|-----|--------|
| `editor.gridView.selectPageFirst` | 请先选择页面 |
| `editor.gridView.pagesCopied` | 已复制 {{count}} 页 |
| `editor.gridView.noCopiedPages` | 暂无复制页面 |
| `editor.gridView.pagesPasted` | 已粘贴 {{count}} 页 |
| `editor.gridView.pageInserted` | 已插入新页面 |
| `editor.gridView.pageReordered` | 页面已复制 |
| `editor.gridView.keepAtLeastOne` | 至少保留一页 |
| `editor.gridView.pageDeleted` | 页面已删除 |
| `editor.gridView.pagesDeleted` | 已删除 {{count}} 页 |
| `editor.gridView.pagesHidden` | 已隐藏 {{count}} 页 |
| `editor.gridView.pageHidden` | 页面已隐藏 |
| `editor.gridView.noPages` | 暂无页面 |
| `editor.gridView.allPagesHidden` | 所有页面已隐藏 |
| `editor.gridView.showAllPages` | 显示全部页面 |
| `editor.gridView.copyPage` | 复制页面 |
| `editor.gridView.hidePage` | 隐藏页面 |
| `editor.gridView.deletePage` | 删除页面 |
| `editor.gridView.selectAll` | 全选 |
| `editor.gridView.deselectAll` | 取消全选 |
| `editor.gridView.showHidden` | 显示隐藏项 |
| `editor.gridView.copy` | 复制 |
| `editor.gridView.hide` | 隐藏 |
| `editor.gridView.delete` | 删除 |

**12. editor.photoPreview（8 个新增，1 个已有）— PhotoPreview.tsx**
| key | 中文值 | 状态 |
|-----|--------|------|
| `editor.photoPreview.loadFailed` | 无法加载 | 已有 |
| `editor.photoPreview.close` | 关闭 (Esc) | 新增 |
| `editor.photoPreview.prev` | 上一张 (←) | 新增 |
| `editor.photoPreview.next` | 下一张 (→) | 新增 |
| `editor.photoPreview.zoomOut` | 缩小 (-) | 新增 |
| `editor.photoPreview.zoomIn` | 放大 (+) | 新增 |
| `editor.photoPreview.reset` | 重置 (0) | 新增 |
| `editor.photoPreview.rotateLeft` | 向左旋转 | 新增 |
| `editor.photoPreview.rotateRight` | 向右旋转 | 新增 |

#### 复用的已有 key（无需新增）
- `common.loading` — Loading.tsx
- `editor.tools.quickStyles/color/style/styleRounded/styleSquare/styleTape/styleShadow` — StickyStylePicker.tsx
- `editor.editFlyout.*` — EditFlyout.tsx（全部已有）

---

## 2026-07-28 · i18n 编辑器对话框组件国际化（watermark/pageSettings/print/createTemplate）

### 背景
继续推进 MemBook 项目的 i18n 全局化。本次聚焦于 4 个编辑器/首页对话框组件，将所有用户可见的硬编码中文字符串（JSX 文本、属性、addToast 消息、模块级常量 label）替换为 `t('namespace.key')` 调用。仅修改组件文件，不改动 locale 文件。

修改文件：
- [WatermarkSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/WatermarkSettings.tsx)（~15 处中文 → `editor.watermark.*`）
- [PageSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageSettings.tsx)（~20 处中文 → `editor.pageSettings.*`）
- [PrintDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PrintDialog.tsx)（~36 处中文 → `editor.print.*`）
- [CreateTemplateDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CreateTemplateDialog.tsx)（~31 处中文 → `home.createTemplate.*`）

### 一、改造要点

#### 1. 引入 useTranslation
- 每个组件文件顶部添加 `import { useTranslation } from 'react-i18next';`
- 每个函数组件体顶部添加 `const { t } = useTranslation();`
- 4 个组件均为单组件文件，无需为子组件独立添加 hook

#### 2. 命名空间分配
- `editor.watermark.*` — WatermarkSettings.tsx（时间水印设置对话框）
- `editor.pageSettings.*` — PageSettings.tsx（页面尺寸/边距/间距设置对话框）
- `editor.print.*` — PrintDialog.tsx（打印对话框，复用部分已有 key）
- `home.createTemplate.*` — CreateTemplateDialog.tsx（自定义布局创建/编辑对话框）

注意：
- `editor.print.*` 命名空间已存在 7 个 key（title/printer/selectPrinter/noPrinter/printing/printComplete/printFailed），本次新增 33 个 key 补充完整打印对话框所需文案，并复用了其中的 title/printer/selectPrinter 3 个 key。
- `editor.watermark.*` 命名空间已存在 12 个 key，本次复用了其中 locationGranularity/coarse/standard/detailed 4 个 key，并新增 14 个 key（dialogTitle、enableTimeWatermark、showContent 等），未与已有 enable/showDate/showLocation 等冲突。

#### 3. 改造范围
- **JSX 文本**：对话框标题、按钮文案、标签文本、提示文字、空状态、选项标签等
- **属性**：placeholder、title
- **addToast 消息**：成功/失败/警告提示，含 `{{var}}` 插值（如 sizeOutOfRange、generating）
- **模块内变量**：presets 数组中的 label/desc 改为 `t()` 调用（CreateTemplateDialog 中的 presets 数组定义在组件函数体内，可直接使用 t()）
- **动态拼接**：pageSizeLabel 使用模板字符串拼接尺寸数值与 t() 调用
- **条件渲染**：previewLoading 多分支使用 generating/generatingSimple/totalPages 三个 key

#### 4. 未改造的内容（按规则保留）
- 代码注释（`//`、`/* */`、`{/* */}`）
- `logger.error('...')` 日志消息（开发调试用，非用户可见）
- WatermarkSettings.tsx 中的预览示例数据（`'2026年3月21日'`、`'浙江省-杭州市-西湖区-灵隐街道'`，这些是展示水印效果的样例数据，非 UI 文案）
- `types/index.ts` 中的 `ALBUM_SIZES` 和 `PAGE_MARGIN_PRESETS` 共享常量中的硬编码中文（跨组件共享，修改会影响其他模块，留给主代理统一处理）

#### 5. 插值用法
- `t('editor.pageSettings.sizeOutOfRange', { min, max })` — 页面尺寸超限提示
- `t('editor.pageSettings.customSizeDesc', { w, h })` — 自定义尺寸描述
- `t('editor.pageSettings.recommendedRange', { min, max })` — 建议范围
- `t('editor.print.totalPages', { count })` — 总页数
- `t('editor.print.generating', { loaded, total })` — 生成进度
- `t('editor.print.pagePreviewAlt', { n })` / `t('editor.print.pageNumber', { n })` — 页码
- `t('home.createTemplate.regionCount', { count })` — 区域数
- `t('home.createTemplate.snap', { grid })` — 网格吸附倍数
- `t('home.createTemplate.photoSlots', { count })` / `t('home.createTemplate.textZones', { count })` — 槽位数
- `t('home.createTemplate.photoZoneN', { n })` — 照片框序号

### 二、TypeScript 类型检查
执行 `npx tsc --noEmit -p tsconfig.json` 通过，**0 个错误**。

### 三、待办：locale 文件补全

本次仅修改组件文件，未改动 locale 文件。以下 **98 个新 key** 需补入 `src/i18n/locales/zh-CN.json` 与 `en-US.json`（其中 editor.print 复用 3 个已有 key，editor.watermark 复用 4 个已有 key，common.* 复用已有 cancel/confirm/loading）。

详细 key 清单与英文翻译建议见会话输出。

---

## 2026-07-28 · i18n 整理工具组件国际化（organize/*）

### 背景
继续推进 MemBook 项目的 i18n 全局化。本次聚焦于"整理"页面的 6 个工具组件，将所有用户可见的硬编码中文字符串（JSX 文本、属性、addToast 消息）替换为 `t('home.organize.*')` 调用。仅修改组件文件，不改动 locale 文件。

修改文件：
- [shared.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/shared.tsx)（PrimaryButton loading 文本）
- [ExifTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ExifTool.tsx)（~30 处）
- [ConvertTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ConvertTool.tsx)（~12 处）
- [DedupeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/DedupeTool.tsx)（~17 处，含 DedupeResults 子组件）
- [OrganizeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/OrganizeTool.tsx)（~22 处）
- [LibraryPickerDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/LibraryPickerDialog.tsx)（~11 处）

### 一、改造要点

#### 1. 引入 useTranslation
- 每个组件文件顶部添加 `import { useTranslation } from 'react-i18next';`
- 每个含中文文案的函数组件体顶部添加 `const { t } = useTranslation();`
- DedupeTool.tsx 的子组件 `DedupeResults` 独立添加 hook
- shared.tsx 的 `PrimaryButton` 组件独立添加 hook

#### 2. 命名空间分配
- `home.organize.shared.*` — shared.tsx
- `home.organize.exif.*` — ExifTool.tsx
- `home.organize.convert.*` — ConvertTool.tsx
- `home.organize.dedupe.*` — DedupeTool.tsx
- `home.organize.organize.*` — OrganizeTool.tsx
- `home.organize.libraryPicker.*` — LibraryPickerDialog.tsx

注意：`home.organize.tools.*` 下已有的 title/desc/shortDesc 是工具卡片标题描述，本次新增 key 用于工具内部 UI 文案。

#### 3. 改造范围
- **JSX 文本**：按钮文案、标签文本、提示文字、空状态、标题等
- **属性**：title、description、placeholder、aria-label、disabledReason
- **addToast 消息**：成功/失败/警告/信息提示，含 `{{var}}` 插值
- 含条件分支的 toast 使用两个 key（如 `toastSuccess` / `toastSuccessWithFail`）
- OrganizeTool 预览 toast 的多段拼接使用 `toastSeparator` key 处理中文逗号

#### 4. 未改造的内容（按规则保留）
- 代码注释（`//` 和 `/** */`）
- 内部 `throw new Error('读取失败')`（被 catch 后仅写入 logger，非用户可见）
- 文件系统路径字符串 `'MemBook照片整理/'`（实际目录名，非 UI 文案）
- logger 日志消息（开发调试用）

### 二、待办：locale 文件补全

本次仅修改组件文件，未改动 locale 文件。以下 **73 个新 key** 需补入 `src/i18n/locales/zh-CN.json` 与 `en-US.json` 的 `home.organize.*` 命名空间下：

```json
{
  "zh-CN": {
    "home": {
      "organize": {
        "shared": {
          "processing": "处理中..."
        },
        "exif": {
          "title": "批量改 EXIF",
          "description": "修改拍摄日期 / 添加 GPS（支持 JPEG/PNG/WebP，当前 {{count}} 张可处理）",
          "noWritablePhotos": "当前无可处理的照片（支持 JPEG / PNG / WebP）",
          "modeDate": "修改日期",
          "modeGps": "添加 GPS",
          "excludeSorted": "排除已在\"MemBook照片整理\"目录中的文件",
          "targetDate": "目标日期",
          "targetDatePlaceholder": "如 2024-01-15 或 2024:01:15 14:30",
          "invalidDateFormat": "无法识别的日期格式",
          "parsedAs": "解析为：{{date}}",
          "preserveTime": "保留原照片的时分秒（仅改日期）",
          "dateScopeTitle": "修改范围（按拍摄日期）",
          "scopeWithDate": "有拍摄日期的照片（{{count}} 张）",
          "scopeWithoutDate": "无拍摄日期的照片（{{count}} 张）",
          "dateScopeHint": "取消勾选的组不会被修改。例如只勾选「无拍摄日期」，则仅对这些照片写入日期，已有日期的照片保持不变。",
          "gpsModeCoord": "输入坐标",
          "gpsModePlace": "地名查询",
          "placeNamePlaceholder": "输入地名，如 北京天安门",
          "query": "查询",
          "longitude": "经度",
          "latitude": "纬度",
          "gpsScopeTitle": "修改范围（按是否已有 GPS）",
          "scopeWithGps": "已有 GPS 的照片（{{count}} 张，将覆盖为上方坐标）",
          "scopeWithoutGps": "无 GPS 的照片（{{count}} 张，将写入上方坐标）",
          "gpsScopeHint": "取消勾选的组不会被修改。例如只勾选「无 GPS」，则仅给缺失 GPS 的照片写入坐标，已有 GPS 的照片保持不变。",
          "libraryDownloadHint": "库内模式下修改后的文件将下载到本地",
          "webDownloadHint": "Web 模式下修改后的文件将下载到本地",
          "executeDate": "修改日期 ({{count}} 张)",
          "executeGps": "写入 GPS ({{count}} 张)",
          "toastLocated": "已定位: {{name}}",
          "toastPlaceNotFound": "未找到该地点",
          "toastGeocodeFailed": "查询失败: {{message}}",
          "toastSuccess": "成功修改 {{ok}} 个",
          "toastSuccessWithFail": "成功修改 {{ok}} 个，失败 {{fail}} 个",
          "toastExecuteFailed": "执行失败: {{message}}"
        },
        "convert": {
          "title": "格式转换",
          "description": "多种格式 → JPG（当前 {{count}} 个可转换）",
          "disabledReason": "当前文件夹无可转换的图片文件",
          "noConvertibleFiles": "无可转换文件",
          "selectFormats": "选择转换的格式（点击排除）",
          "jpgQuality": "JPG 质量",
          "deleteOriginal": "转换后删除原文件（移入回收站，可恢复）",
          "moreFiles": "... 还有 {{count}} 个",
          "libraryDownloadHint": "库内模式下转换后的文件将逐个下载",
          "webDownloadHint": "Web 模式下转换后的文件将逐个下载",
          "executeButton": "转换 {{count}} 个文件",
          "toastSuccess": "转换成功 {{ok}} 个",
          "toastSuccessWithFail": "转换成功 {{ok}} 个，失败 {{fail}} 个",
          "toastFailed": "转换失败: {{message}}"
        },
        "dedupe": {
          "title": "照片去重",
          "description": "查找并移除完全相同的重复照片（SHA256 精确匹配）",
          "startScan": "开始扫描重复",
          "cancel": "取消",
          "noDuplicatesResult": "未发现重复照片。",
          "foundSummary": "发现 {{groups}} 组重复，{{count}} 个冗余文件",
          "canFree": "可释放 {{size}}",
          "filesCount": "{{count}} 个 · {{size}}",
          "keep": "保留",
          "confirmDeleteButton": "确认删除 {{count}} 个文件",
          "confirmExecuteDelete": "确认执行删除",
          "backToPreview": "返回预览",
          "toastFound": "发现 {{groups}} 组重复，{{count}} 个冗余文件 ({{size}})",
          "toastNoDuplicates": "未发现重复照片",
          "toastDedupeFailed": "去重失败: {{message}}",
          "toastDeleted": "已删除 {{ok}} 个重复文件",
          "toastDeletedWithFail": "已删除 {{ok}} 个重复文件，{{fail}} 个失败",
          "toastDeleteFailed": "删除失败: {{message}}"
        },
        "organize": {
          "title": "按时间归类",
          "description": "按拍摄日期将照片整理到 MemBook照片整理/年/月 目录结构",
          "disabledReasonLibrary": "库内照片不支持文件归类，请使用文件夹模式",
          "disabledReasonDesktop": "需要选择真实文件夹且在桌面端运行",
          "needDesktopHint": "需在桌面端选择文件夹后使用",
          "selectFormats": "参与归类的格式（点击排除）",
          "excludeSorted": "排除已在\"MemBook照片整理\"目录中的文件",
          "useFileDate": "无拍摄日期时使用文件修改日期",
          "previewButton": "预览归类",
          "movedSummary": "{{count}} 个文件将移动到 MemBook照片整理/年/月 目录",
          "moreFiles": "... 还有 {{count}} 个",
          "noDateSummary": "{{count}} 个文件无法识别拍摄日期，已跳过",
          "allIdentified": "全部文件均已识别日期",
          "executeButton": "执行移动 ({{count}})",
          "cancel": "取消",
          "toastSeparator": "，",
          "toastPreviewMoved": "{{count}} 个待移动",
          "toastPreviewAlready": "{{count}} 个已就位",
          "toastPreviewNoDate": "{{count}} 个无法识别日期已跳过",
          "toastPreviewFailed": "预览失败: {{message}}",
          "toastExecuteResult": "移动 {{moved}} 个，跳过 {{skipped}} 个",
          "toastExecuteResultWithFail": "移动 {{moved}} 个，跳过 {{skipped}} 个，失败 {{failed}} 个",
          "toastExecuteFailed": "执行失败: {{message}}"
        },
        "libraryPicker": {
          "ariaLabel": "选择要整理的项目",
          "title": "选择要整理的项目",
          "closeAriaLabel": "关闭",
          "searchPlaceholder": "搜索项目名称...",
          "loading": "正在加载项目...",
          "noProjectsTitle": "还没有项目",
          "noProjectsDesc": "先去首页创建一个相册吧",
          "noMatchTitle": "未找到匹配的项目",
          "noMatchDesc": "试试其他关键词",
          "photoCount": "{{count}} 张",
          "footerHint": "选择项目后将加载该项目中的所有照片进行整理"
        }
      }
    }
  }
}
```

**英文翻译建议**（en-US.json）：

```json
{
  "en": {
    "home": {
      "organize": {
        "shared": {
          "processing": "Processing..."
        },
        "exif": {
          "title": "Batch EXIF Editor",
          "description": "Edit date taken / add GPS (JPEG/PNG/WebP, {{count}} editable)",
          "noWritablePhotos": "No editable photos (JPEG / PNG / WebP supported)",
          "modeDate": "Edit Date",
          "modeGps": "Add GPS",
          "excludeSorted": "Exclude files already in \"MemBook照片整理\" directory",
          "targetDate": "Target Date",
          "targetDatePlaceholder": "e.g. 2024-01-15 or 2024:01:15 14:30",
          "invalidDateFormat": "Unrecognized date format",
          "parsedAs": "Parsed as: {{date}}",
          "preserveTime": "Preserve original time (date only)",
          "dateScopeTitle": "Scope (by date taken)",
          "scopeWithDate": "Photos with date taken ({{count}})",
          "scopeWithoutDate": "Photos without date taken ({{count}})",
          "dateScopeHint": "Unchecked groups won't be modified. E.g. checking only \"without date\" writes dates only to those photos.",
          "gpsModeCoord": "Coordinates",
          "gpsModePlace": "Place Name",
          "placeNamePlaceholder": "Enter a place, e.g. Tiananmen Square",
          "query": "Search",
          "longitude": "Longitude",
          "latitude": "Latitude",
          "gpsScopeTitle": "Scope (by existing GPS)",
          "scopeWithGps": "Photos with GPS ({{count}}, will be overwritten)",
          "scopeWithoutGps": "Photos without GPS ({{count}}, will be written)",
          "gpsScopeHint": "Unchecked groups won't be modified. E.g. checking only \"without GPS\" writes coordinates only to missing photos.",
          "libraryDownloadHint": "Modified files will be downloaded locally in library mode",
          "webDownloadHint": "Modified files will be downloaded locally in web mode",
          "executeDate": "Edit Date ({{count}})",
          "executeGps": "Write GPS ({{count}})",
          "toastLocated": "Located: {{name}}",
          "toastPlaceNotFound": "Place not found",
          "toastGeocodeFailed": "Search failed: {{message}}",
          "toastSuccess": "Successfully modified {{ok}}",
          "toastSuccessWithFail": "Modified {{ok}}, failed {{fail}}",
          "toastExecuteFailed": "Execution failed: {{message}}"
        },
        "convert": {
          "title": "Format Converter",
          "description": "Various formats → JPG ({{count}} convertible)",
          "disabledReason": "No convertible image files in current folder",
          "noConvertibleFiles": "No convertible files",
          "selectFormats": "Select formats to convert (click to exclude)",
          "jpgQuality": "JPG Quality",
          "deleteOriginal": "Delete original files after conversion (moved to recycle bin, recoverable)",
          "moreFiles": "... {{count}} more",
          "libraryDownloadHint": "Converted files will be downloaded one by one in library mode",
          "webDownloadHint": "Converted files will be downloaded one by one in web mode",
          "executeButton": "Convert {{count}} files",
          "toastSuccess": "Converted {{ok}} successfully",
          "toastSuccessWithFail": "Converted {{ok}}, failed {{fail}}",
          "toastFailed": "Conversion failed: {{message}}"
        },
        "dedupe": {
          "title": "Photo Deduplication",
          "description": "Find and remove exact duplicate photos (SHA256 matching)",
          "startScan": "Scan for Duplicates",
          "cancel": "Cancel",
          "noDuplicatesResult": "No duplicates found.",
          "foundSummary": "Found {{groups}} duplicate groups, {{count}} redundant files",
          "canFree": "Can free {{size}}",
          "filesCount": "{{count}} files · {{size}}",
          "keep": "Keep",
          "confirmDeleteButton": "Delete {{count}} files",
          "confirmExecuteDelete": "Confirm Delete",
          "backToPreview": "Back to Preview",
          "toastFound": "Found {{groups}} duplicate groups, {{count}} redundant files ({{size}})",
          "toastNoDuplicates": "No duplicates found",
          "toastDedupeFailed": "Deduplication failed: {{message}}",
          "toastDeleted": "Deleted {{ok}} duplicate files",
          "toastDeletedWithFail": "Deleted {{ok}} duplicate files, {{fail}} failed",
          "toastDeleteFailed": "Delete failed: {{message}}"
        },
        "organize": {
          "title": "Organize by Date",
          "description": "Organize photos into MemBook照片整理/year/month directory structure by date taken",
          "disabledReasonLibrary": "Library photos don't support file organizing, please use folder mode",
          "disabledReasonDesktop": "Requires a real folder and desktop app",
          "needDesktopHint": "Select a folder in desktop app to use",
          "selectFormats": "Formats to organize (click to exclude)",
          "excludeSorted": "Exclude files already in \"MemBook照片整理\" directory",
          "useFileDate": "Use file modification date when no date taken",
          "previewButton": "Preview",
          "movedSummary": "{{count}} files will be moved to MemBook照片整理/year/month",
          "moreFiles": "... {{count}} more",
          "noDateSummary": "{{count}} files couldn't be dated, skipped",
          "allIdentified": "All files have dates identified",
          "executeButton": "Move ({{count}})",
          "cancel": "Cancel",
          "toastSeparator": ", ",
          "toastPreviewMoved": "{{count}} to move",
          "toastPreviewAlready": "{{count}} already in place",
          "toastPreviewNoDate": "{{count}} skipped (no date)",
          "toastPreviewFailed": "Preview failed: {{message}}",
          "toastExecuteResult": "Moved {{moved}}, skipped {{skipped}}",
          "toastExecuteResultWithFail": "Moved {{moved}}, skipped {{skipped}}, failed {{failed}}",
          "toastExecuteFailed": "Execution failed: {{message}}"
        },
        "libraryPicker": {
          "ariaLabel": "Select project to organize",
          "title": "Select Project to Organize",
          "closeAriaLabel": "Close",
          "searchPlaceholder": "Search project name...",
          "loading": "Loading projects...",
          "noProjectsTitle": "No projects yet",
          "noProjectsDesc": "Create an album on the home page first",
          "noMatchTitle": "No matching projects",
          "noMatchDesc": "Try other keywords",
          "photoCount": "{{count}} photos",
          "footerHint": "All photos in the selected project will be loaded for organizing"
        }
      }
    }
  }
}
```

---

## 2026-07-28 · i18n SmartLayoutView 智能编排视图国际化

### 背景
继续推进 MemBook 项目的 i18n 全局化。本次聚焦于编辑器中最复杂的 `SmartLayoutView.tsx` 智能编排视图，将约 65 处硬编码中文字符串替换为 `t('editor.smartLayout.*')` 调用。仅修改组件文件，不改动 locale 文件。

修改文件：[SmartLayoutView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/SmartLayoutView.tsx)

### 一、改造要点

#### 1. 引入 useTranslation
- 顶层 `import { useTranslation } from 'react-i18next';`
- `SmartLayoutView` 函数体顶部添加 `const { t } = useTranslation();`
- 子组件 `PageThumbCard`（`memo` 包装）内部也添加 `const { t } = useTranslation();`，因为它独立渲染页码与照片信息文本

#### 2. 4 个模块级常量数组改造（labelKey / descKey 模式）
- `TIER_PATTERN_LABELS` → 重命名为 `TIER_PATTERN_LABEL_KEYS`，类型仍为 `Record<TierPattern, string>`，但值改为 i18n key 字符串（如 `'editor.smartLayout.pattern.leader'`）。原 20 个中文 label 全部映射到 `editor.smartLayout.pattern.*` 已有 key（`hero-first`→`leader`、`highlight`→`mainPair`、`all-hero`→`fullBleed`、`hero-tail`→`heroTail`、`double-hero`→`doubleHero`、`panorama-hero`→`panorama` 等键名转换）。
- `RHYTHM_OPTS`：`{ id, label, desc }` → `{ id, labelKey, descKey }`，对应 `editor.smartLayout.rhythm.{id}.label/desc`。
- `DENSITY_OPTS`：`{ id, label, desc }` → `{ id, labelKey, descKey }`，对应 `editor.smartLayout.density.{id}.label/desc`。
- `DATE_OPTS`：`{ id, label, desc }` → `{ id, labelKey, descKey }`，对应 `editor.smartLayout.date.{id}.label/desc`。
- 使用处：`{t(item.labelKey)}` / `{t(item.descKey)}`，`{t(TIER_PATTERN_LABEL_KEYS[pattern])}`。

#### 3. addToast 消息（含插值）
- 3 处 info toast：
  - `当前页面照片数量不足，无法重排` → `t('editor.smartLayout.toast.shuffleInsufficient')`
  - `请先点击选择某一页，再进行照片位置重排` → `t('editor.smartLayout.toast.selectPageForShuffle')`
  - `请先点击选择某一页，再进行旋转` → `t('editor.smartLayout.toast.selectPageForRotate')`
- 1 处 success toast（含插值）：
  - `已编排 ${selectedPhotos.length} 张照片，共 ${newPages.length} 页` → `t('editor.smartLayout.layoutSuccess', { photos: selectedPhotos.length, pages: newPages.length })`
- 1 处 error toast（含插值）：
  - `编排失败: ${err.message}` → `t('editor.smartLayout.layoutFailed', { message: (err as Error)?.message ?? '' })`
- `handleExecute` 的 useCallback 依赖数组补充 `t`

#### 4. JSX 文本与按钮文案
- 标题：`<h1>智能编排</h1>` → `<h1>{t('editor.smartLayout.title')}</h1>`
- 摘要：拆分为 `spec` + `summary` 两次 `t()` 调用拼接（保留原有 · 分隔符）
- 滑杆标签：`照片密度` / `排版节奏` → `t('editor.smartLayout.densityLabel')` / `t('editor.smartLayout.rhythmLabel')`，并附加 `pageSuffix` 拼接「 · 第N页」
- 滑杆档位标签：`紧凑/智能/宽松` 与 `统一/智能/多变` 复用已有 `editor.smartLayout.compact` / `density.auto.label` / `loose` 与 `uniform` / `rhythm.auto.label` / `varied`
- 排版方案区：标题 + 切换提示 + 3 个 `方案 A/B/C` 标签 + `自动` 兜底
- 高级设置：折叠标题 + `精确密度` / `精确节奏` 子标题 + 2 个 `恢复全局` 按钮
- XY 偏压盘：4 角方位标签 + 偏压显示文本（嵌套 `t()` 调用：`biasDisplay` 内嵌 `biasRightVal/biasLeftVal/biasCenter/biasTopVal/biasBottomVal`）
- 日期分组 / 插入位置 / 追加到末尾 / 插入到第 / 页后
- 底部按钮：`编排中...` / `确认生成（N页）` / `继续添加` / `取消`
- 右侧工具栏：`预览全部` / `N 页` / `照片位置重排` / `旋转 90°` / `照片` / `构图` / `缩略图`
- 两个错误兜底页：`请先创建相册并设置尺寸` / `页面对所选照片来说太小...` 与 `返回` 按钮（复用 `common.back`）

### 二、TypeScript 类型检查
运行 `npx tsc --noEmit` 通过，exit code 0，全项目 0 错误。

### 三、待办：locale 文件补全
本次仅修改组件文件，未改动 locale 文件。大量 key 已存在于 `src/i18n/locales/zh-CN.json` 与 `en-US.json` 的 `editor.smartLayout.*` 命名空间下（density/rhythm/pattern/date 子对象、bias* 系列、pageN/pageInfo/pageSuffix/spec/summary 等）。

**以下 18 个新 key 需补入两个 locale 文件**（英文翻译已与项目现有术语对齐：`编排`→Layout/Arrange、`密度`→Density、`节奏`→Rhythm、`旋转`→Rotate、`缩略图`→Thumbnail）：

```json
{
  "zh-CN": {
    "editor": {
      "smartLayout": {
        "densityLabel": "照片密度",
        "rhythmLabel": "排版节奏",
        "layoutSchemes": "排版方案",
        "clickToSwitch": "点击下方切换整体效果",
        "schemeA": "方案 A",
        "schemeB": "方案 B",
        "schemeC": "方案 C",
        "auto": "自动",
        "advancedSettings": "高级设置",
        "preciseDensity": "精确密度",
        "preciseRhythm": "精确节奏",
        "continueAdd": "继续添加",
        "shufflePhotos": "照片位置重排",
        "rotate90": "旋转 90°",
        "confirmGenerate": "确认生成（{{pages}}页）",
        "toast": {
          "shuffleInsufficient": "当前页面照片数量不足，无法重排",
          "selectPageForShuffle": "请先点击选择某一页，再进行照片位置重排",
          "selectPageForRotate": "请先点击选择某一页，再进行旋转"
        }
      }
    }
  },
  "en-US": {
    "editor": {
      "smartLayout": {
        "densityLabel": "Photo Density",
        "rhythmLabel": "Layout Rhythm",
        "layoutSchemes": "Layout Schemes",
        "clickToSwitch": "Click below to switch overall effect",
        "schemeA": "Scheme A",
        "schemeB": "Scheme B",
        "schemeC": "Scheme C",
        "auto": "Auto",
        "advancedSettings": "Advanced Settings",
        "preciseDensity": "Precise Density",
        "preciseRhythm": "Precise Rhythm",
        "continueAdd": "Continue Adding",
        "shufflePhotos": "Shuffle Photos",
        "rotate90": "Rotate 90°",
        "confirmGenerate": "Generate ({{pages}} pages)",
        "toast": {
          "shuffleInsufficient": "Not enough photos on current page to shuffle",
          "selectPageForShuffle": "Please click a page first before shuffling photos",
          "selectPageForRotate": "Please click a page first before rotating"
        }
      }
    }
  }
}
```

### 四、备注
- `common.back`（"返回" / "Back"）已存在于 locale 文件，两个错误兜底页的返回按钮直接复用。
- `editor.smartLayout.summary` 现有模板 `已选 {{photos}} 张 · 共 {{pages}} 页` 与代码使用方式（独立拼接）一致，未拆分。
- `editor.smartLayout.confirm` 现有 key 为「确认编排（{{pages}}页）」，但代码原文为「确认生成（N页）」语义略不同，故新增 `confirmGenerate` 以保留原文措辞，未复用 `confirm`。
- `editor.smartLayout.advancedOptions`（"高级选项"）与代码原文「高级设置」语义不同，故新增 `advancedSettings` 而非复用。

---

## 2026-07-28 · i18n hooks/services/license 国际化（6 个文件）

### 背景
继续推进 MemBook 项目的 i18n 全局化，本次聚焦于 hooks、services、license 等非组件文件中的硬编码中文 UI 文案。这些文件多数不是 React 组件，无法直接使用 `useTranslation` hook，采用 i18n 实例直接调用的方案。仅修改源代码文件，不改动 locale 文件（zh-CN.json / en-US.json）。

### 一、修改文件清单与命名空间

| 文件 | 命名空间 | 字符串数 | 方案 |
|------|---------|---------|------|
| [usePhotoImport.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/usePhotoImport.ts) | `hooks.photoImport.*` | 16 | i18n 实例 |
| [pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts) | `services.pageLayout.*` | 17 | i18n 实例 |
| [useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts) | `hooks.canvasKeyboard.*` | 3 | i18n 实例 |
| [useDragDrop.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useDragDrop.ts) | `hooks.dragDrop.*` | 3 | i18n 实例 |
| [licenseService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/license/licenseService.ts) | `license.service.*` | 3 | i18n 实例 |
| [ActivationDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/license/ActivationDialog.tsx) | `license.activation.*` | 13 | useTranslation hook |

### 二、实现要点

#### 1. i18n 实例方案（hooks/services）
- 通过 `import i18n from '../i18n';`（或 `../../../i18n`）引入 i18next 实例
- 使用 `i18n.t('namespace.key')` 替代硬编码字符串
- 该方案优势：无需修改函数签名/调用方；在回调（setTimeout / addEventListener / 异步闭包）中不会出现 `t` 闭包过期问题
- 适用文件：usePhotoImport、pageLayoutService、useCanvasKeyboard、useDragDrop、licenseService

#### 2. useTranslation hook 方案（React 组件）
- 仅 ActivationDialog.tsx 使用：`import { useTranslation } from 'react-i18next';` + `const { t } = useTranslation();`
- 移除原模块级常量 `TRIAL_HINT_DEFAULT`（含中文），改为组件内 `t('license.activation.trialHint')` 调用

#### 3. Toast 消息国际化
- `addToast({ message: '中文' })` → `addToast({ message: i18n.t('key') })` 或 `addToast({ message: t('key') })`
- 含变量的 toast 使用插值：`i18n.t('hooks.photoImport.skippedDuplicates', { count: n })`

#### 4. licenseService error 字段
- `activateLicense` 返回的 `error` 字段（UI 显示用）改用 `i18n.t('license.service.*')`
- `logger.error()` 中的中文为开发者日志，保留不国际化

#### 5. ActivationDialog 插值处理
- `机器标识：{machineId}` → `t('license.activation.machineIdLabel', { machineId })`
- `MBK-XXXX-XXXX-XXXX-XXXX` 作为格式占位符保留原样不翻译

### 三、保留未国际化的内容
- 代码注释（含中文）— 遵循"不修改代码注释"约定
- `logger.warn` / `logger.error` 的日志输出 — 面向开发者，非 UI 显示
- `MBK-XXXX-XXXX-XXXX-XXXX` 格式占位符 — 与激活码格式强绑定

### 四、待办：locale 文件补全
本次仅修改源代码文件，未改动 locale 文件。以下新增 key 需补入 `src/i18n/locales/zh-CN.json` 与 `en-US.json`：

```json
{
  "zh-CN": {
    "hooks": {
      "photoImport": {
        "skippedDuplicates": "跳过 {{count}} 张重复照片",
        "allExist": "所有照片已存在，无需导入",
        "noValidImages": "未选择有效图片",
        "processing": "正在处理 {{count}} 张照片...",
        "cancelled": "已取消导入",
        "persistFailed": "照片处理完成，但持久化失败，刷新页面后可能需要重新导入",
        "moreCount": " 等{{count}}张",
        "importedWithFailures": "已导入 {{successCount}} 张，失败 {{failedCount}} 张：{{names}}",
        "imported": "已导入 {{count}} 张照片",
        "importFailed": "导入失败: {{message}}",
        "reading": "正在读取 {{count}} 张照片...",
        "readCancelled": "已取消读取",
        "noValidFilesRead": "未读取到有效图片文件",
        "readFailed": "读取文件失败: {{message}}",
        "noValidImagesSelected": "未选择有效的图片文件"
      },
      "canvasKeyboard": {
        "saved": "💾 已保存",
        "undone": "已撤销",
        "redone": "已重做"
      },
      "dragDrop": {
        "photoPlaced": "照片已放入页内",
        "photosPlaced": "已放入 {{count}} 张照片",
        "onlySmartLayoutSupportAdd": "仅智能排版页面支持添加照片"
      }
    },
    "services": {
      "pageLayout": {
        "insufficientPhotos": "当前页面照片数量不足，无法重排",
        "shuffled": "已重排当前页照片位置",
        "onlySmartLayoutSupportAdd": "仅智能排版页面支持添加照片",
        "photosAlreadyInPage": "选中的照片已在当前页",
        "photoInfoMissing": "照片信息缺失",
        "layoutFailed": "排版失败",
        "tooManyPhotos": "照片较多，已放入部分，页面已重新排版",
        "movedPhotos": "已移入 {{count}} 张照片，页面已重新排版",
        "addedPhotos": "已添加 {{count}} 张照片，页面已重新排版",
        "onlySmartLayoutSupportSwitch": "仅智能排版页面支持布局切换",
        "rotated": "布局已旋转 90°",
        "layoutShuffled": "布局已随机切换",
        "noPhotosToConvert": "当前页面没有照片，无法转换为智能排版",
        "convertFailed": "转换失败，请重试",
        "converted": "已转换为智能排版页面"
      }
    },
    "license": {
      "service": {
        "invalidCodeFormat": "激活码格式错误",
        "noMachineId": "无法获取机器指纹，请在桌面端应用中激活",
        "invalidCodeOrSignature": "激活码无效、签名校验失败或机器码不匹配"
      },
      "activation": {
        "trialHint": "你正在使用 MemBook 7 天完整试用版，到期后智能编排、数据迁移、导出文件等高级功能将需要激活才能继续使用。提前激活可立即解锁永久使用。",
        "activationSuccess": "激活成功！已解锁全部高级功能。",
        "activationFailed": "激活失败",
        "close": "关闭",
        "activateTitle": "激活 MemBook",
        "activateHint": "激活后可解锁无限项目、智能编排、数据迁移、导出文件等高级功能。",
        "activated": "已激活",
        "activatedDesc": "当前设备已激活，可正常使用全部功能。",
        "machineIdLabel": "机器标识：{{machineId}}",
        "copyMachineId": "复制机器标识码",
        "copied": "已复制",
        "copy": "复制",
        "clearActivation": "清除激活状态",
        "activationCode": "激活码",
        "signature": "签名",
        "signaturePlaceholder": "粘贴生成器给出的签名...",
        "later": "稍后再说",
        "activating": "激活中...",
        "activateNow": "现在激活"
      }
    }
  },
  "en-US": {
    "hooks": {
      "photoImport": {
        "skippedDuplicates": "Skipped {{count}} duplicate photos",
        "allExist": "All photos already exist, no import needed",
        "noValidImages": "No valid images selected",
        "processing": "Processing {{count}} photos...",
        "cancelled": "Import cancelled",
        "persistFailed": "Photos processed, but persistence failed. You may need to re-import after refreshing the page",
        "moreCount": " and {{count}} more",
        "importedWithFailures": "Imported {{successCount}}, failed {{failedCount}}: {{names}}",
        "imported": "Imported {{count}} photos",
        "importFailed": "Import failed: {{message}}",
        "reading": "Reading {{count}} photos...",
        "readCancelled": "Read cancelled",
        "noValidFilesRead": "No valid image files read",
        "readFailed": "Failed to read files: {{message}}",
        "noValidImagesSelected": "No valid image files selected"
      },
      "canvasKeyboard": {
        "saved": "💾 Saved",
        "undone": "Undone",
        "redone": "Redone"
      },
      "dragDrop": {
        "photoPlaced": "Photo placed on page",
        "photosPlaced": "Placed {{count}} photos",
        "onlySmartLayoutSupportAdd": "Only smart layout pages support adding photos"
      }
    },
    "services": {
      "pageLayout": {
        "insufficientPhotos": "Not enough photos on the current page to reshuffle",
        "shuffled": "Photos on the current page have been reshuffled",
        "onlySmartLayoutSupportAdd": "Only smart layout pages support adding photos",
        "photosAlreadyInPage": "Selected photos are already on this page",
        "photoInfoMissing": "Photo information is missing",
        "layoutFailed": "Layout failed",
        "tooManyPhotos": "Too many photos, placed partially and re-laid out",
        "movedPhotos": "Moved {{count}} photos, page re-laid out",
        "addedPhotos": "Added {{count}} photos, page re-laid out",
        "onlySmartLayoutSupportSwitch": "Only smart layout pages support layout switching",
        "rotated": "Layout rotated 90°",
        "layoutShuffled": "Layout randomly switched",
        "noPhotosToConvert": "The current page has no photos to convert to smart layout",
        "convertFailed": "Conversion failed, please retry",
        "converted": "Converted to smart layout page"
      }
    },
    "license": {
      "service": {
        "invalidCodeFormat": "Invalid activation code format",
        "noMachineId": "Unable to get machine fingerprint, please activate in the desktop app",
        "invalidCodeOrSignature": "Invalid activation code, signature verification failed, or machine ID mismatch"
      },
      "activation": {
        "trialHint": "You are using MemBook's 7-day full trial. After it expires, advanced features such as smart layout, data migration, and file export will require activation. Activate now to unlock permanent usage.",
        "activationSuccess": "Activated! All advanced features unlocked.",
        "activationFailed": "Activation failed",
        "close": "Close",
        "activateTitle": "Activate MemBook",
        "activateHint": "Activation unlocks unlimited projects, smart layout, data migration, file export, and other advanced features.",
        "activated": "Activated",
        "activatedDesc": "This device is activated and all features are available.",
        "machineIdLabel": "Machine ID: {{machineId}}",
        "copyMachineId": "Copy machine ID",
        "copied": "Copied",
        "copy": "Copy",
        "clearActivation": "Clear activation status",
        "activationCode": "Activation code",
        "signature": "Signature",
        "signaturePlaceholder": "Paste the signature from the generator...",
        "later": "Later",
        "activating": "Activating...",
        "activateNow": "Activate now"
      }
    }
  }
}
```

---

## 2026-07-28 · i18n 编辑器与通用组件国际化（8 个组件）

### 背景
继续推进 MemBook 项目的 i18n 全局化，本次会话聚焦于编辑器与通用组件中剩余的 8 个硬编码中文 UI 文案文件，将其替换为 `t('key')` 调用。仅修改组件文件，不改动 locale 文件（zh-CN.json / en-US.json）。

### 一、修改文件清单与命名空间

| 文件 | 命名空间 | 字符串数 |
|------|---------|---------|
| [LeftNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/LeftNav.tsx) | `editor.leftNav.*` | 4 |
| [BottomTabs.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomTabs.tsx) | `editor.bottomTabs.*` | 5 |
| [ErrorBoundary.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/ErrorBoundary.tsx) | `common.errorBoundary.*` | 4 |
| [ProgressDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/ProgressDialog.tsx) | `common.progressDialog.*` | 1 |
| [PhotoPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoPreview.tsx) | `editor.photoPreview.*` | 1 |
| [PreviewModal.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PreviewModal.tsx) | `editor.previewModal.*` | 2 |
| [ThemePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ThemePanel.tsx) | `editor.themePanel.*` | 7 |
| [MarketPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/MarketPanel.tsx) | `editor.marketPanel.*` | 8 |

### 二、实现要点

#### 1. 统一引入 useTranslation
- 所有 8 个文件均添加 `import { useTranslation } from 'react-i18next';`
- 在各组件函数体顶部添加 `const { t } = useTranslation();`
- ErrorBoundary 为 class 组件，仅在其内部 `DefaultFallback` 函数子组件中使用 hook
- PhotoPreview 在子组件 `PhotoFullImg` 中使用 hook（`无法加载` 文案位于该子组件）
- MarketPanel 在主组件与子组件 `TemplateCategory` 中均使用 hook

#### 2. 静态数组采用 labelKey 模式
- LeftNav.tsx 与 BottomTabs.tsx 的导航项数组定义在模块顶层（组件外），沿用项目已有约定（参考 LeftPanel.tsx）：将 `label: string` 改为 `labelKey: string`，存储 i18n key，在使用处 `{t(item.labelKey)}` 调用

#### 3. Toast 消息国际化
- ThemePanel / MarketPanel 中的 `addToast({ message: '中文' })` 改为 `addToast({ message: t('namespace.key') })`

#### 4. JSX 文本与属性
- JSX 文本如 `<span>中文</span>` → `<span>{t('key')}</span>`
- MarketPanel 的 `TemplateCategory` 通过 props 接收已翻译的 title / description 字符串

#### 5. MarketPanel slotsCount 插值
- `{tmpl.slots.length} 个照片位` → `t('editor.marketPanel.slotsCount', { count: tmpl.slots.length })`
- translation 模板：`{{count}} 个照片位` / `{{count}} photo slots`

#### 6. MarketPanel 变量命名冲突处理
- `TemplateCategory` 子组件原 `TEMPLATES.filter((t) => t.category === category)` 的回调参数 `t` 与 `useTranslation` 解构的 `t` 同名遮蔽，重命名为 `tmpl` 以避免冲突（必要支撑改动）

### 三、key 命名修正说明
任务清单中 MarketPanel 的 key 列表将 `editor.themePanel.title` 误列于 MarketPanel 命名空间下。由于：
1. 与该行声明的 `editor.marketPanel.*` 命名空间模式不符
2. 会与 ThemePanel 自身的 `editor.themePanel.title`（对应 `页面背景`）发生冲突
3. `布局中心` 实为 MarketPanel 的标题

故 MarketPanel 头部 `布局中心` 使用 `editor.marketPanel.title`，与命名空间一致且语义正确。

### 四、TypeScript 类型检查
运行 `npx tsc --noEmit` 通过，exit code 0，全项目 0 错误。

### 五、待办：locale 文件补全
本次仅修改组件文件，未改动 locale 文件。以下新增 key 需补入 `src/i18n/locales/zh-CN.json` 与 `en-US.json`（英文翻译已与项目现有术语对齐：`布局`→Layouts、`照片`→Photos、`主题`→Theme、`相册`→Album、`页面`→Page、`照片位`→photo slot）：

```json
{
  "zh-CN": {
    "editor": {
      "leftNav": { "photos": "照片", "templates": "布局", "tools": "工具", "settings": "设置" },
      "bottomTabs": { "photos": "照片", "templates": "布局", "theme": "主题", "tools": "工具", "market": "布局中心" },
      "photoPreview": { "loadFailed": "无法加载" },
      "previewModal": { "prevPage": "上一页", "nextPage": "下一页" },
      "themePanel": { "noPage": "请先创建相册页面", "bgUpdated": "背景已更新", "title": "页面背景", "current": "当前", "pureWhite": "纯白背景", "presets": "预设颜色", "resetWhite": "重置为白色背景" },
      "marketPanel": { "noPage": "请先创建相册页面", "applied": "布局已应用", "title": "布局中心", "classicTitle": "经典布局", "classicDesc": "规整有序的经典照片排列", "creativeTitle": "创意布局", "creativeDesc": "打破常规的创意照片排布", "slotsCount": "{{count}} 个照片位" }
    },
    "common": {
      "errorBoundary": { "title": "出现了问题", "description": "应用遇到了意外错误。你可以尝试重新加载，或返回主页继续使用。", "reload": "重新加载", "backHome": "返回主页" },
      "progressDialog": { "cancel": "取消" }
    }
  },
  "en-US": {
    "editor": {
      "leftNav": { "photos": "Photos", "templates": "Layouts", "tools": "Tools", "settings": "Settings" },
      "bottomTabs": { "photos": "Photos", "templates": "Layouts", "theme": "Theme", "tools": "Tools", "market": "Layouts Center" },
      "photoPreview": { "loadFailed": "Failed to Load" },
      "previewModal": { "prevPage": "Previous Page", "nextPage": "Next Page" },
      "themePanel": { "noPage": "Please create an album page first", "bgUpdated": "Background updated", "title": "Page Background", "current": "Current", "pureWhite": "Pure white background", "presets": "Preset colors", "resetWhite": "Reset to white background" },
      "marketPanel": { "noPage": "Please create an album page first", "applied": "Layout applied", "title": "Layouts Center", "classicTitle": "Classic Layouts", "classicDesc": "Neat and orderly classic photo arrangements", "creativeTitle": "Creative Layouts", "creativeDesc": "Unconventional creative photo arrangements", "slotsCount": "{{count}} photo slots" }
    },
    "common": {
      "errorBoundary": { "title": "Something Went Wrong", "description": "The app encountered an unexpected error. You can try reloading, or return to the home page to continue.", "reload": "Reload", "backHome": "Back to Home" },
      "progressDialog": { "cancel": "Cancel" }
    }
  }
}
```

---

## 2026-07-28 · i18n 全局化修复（OrganizePanel + TemplateGallery）

### 背景
继续完成 MemBook 项目的 i18n 全局化修复，确保切换到英文(US)时没有中文字段残留。本次会话聚焦于剩余两个未国际化的组件：OrganizePanel.tsx 和 TemplateGallery.tsx。

### 一、OrganizePanel.tsx 国际化完成

[OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)：

#### 1. 主组件硬编码文案替换
- 主标题 "照片整理" → `t('organize.title')`
- 副标题 "批量整理与优化你的照片（可同时打开多个路径）" → `t('organize.subtitle')`
- 关闭路径按钮 title "关闭此路径" → `t('organize.closePath')`
- "添加路径" 按钮 → `t('organize.tabs.addPath')`
- "项目库" 按钮 → `t('organize.library')`
- "已达上限 N" 提示 → `t('organize.maxTabsReachedShort', { max: MAX_TABS })`
- 库标签前缀 "项目库 · {name}" → `t('organize.libraryPrefix', { name: activeTab.name })`
- 照片计数 "N 张照片" → 拆分为 `<strong>N</strong> {t('organize.photoUnit')}` 以保留粗体样式
- "重新扫描" 按钮 → `t('organize.rescan')`

#### 2. Toast 消息国际化
- `handleOpenHistory` 中的 toast 提示：
  - "最多同时打开 N 个路径" → `t('organize.tabs.maxTabsReached', { max: MAX_TABS })`
  - "该路径已打开，已切换至对应标签" → `t('organize.pathAlreadyOpen')`
  - "项目已不存在，已从历史移除" → `t('organize.projectNotFound')`
  - "打开失败: {msg}" → `t('organize.openFailed', { message })`
- 为 `handleOpenHistory` 的 useCallback 依赖数组补充 `t`

#### 3. EmptyState 组件国际化
- 为 `EmptyState` 函数组件添加 `const { t } = useTranslation();`
- 引导式大卡片入口：
  - "第一步 · 必选" → `t('organize.emptyState.step1Label')`
  - "扫描中..." / "选择照片文件夹" → `t('organize.emptyState.scanning')` / `t('organize.emptyState.step1Title')`
  - "打开一个包含照片的文件夹，支持子目录递归扫描" → `t('organize.emptyState.step1Desc')`
  - "可选 · 从已有项目导入" → `t('organize.emptyState.step2Label')`
  - "扫描项目库" → `t('organize.emptyState.step2Title')`
  - "从 MemBook 项目中加载已导入的照片" → `t('organize.emptyState.step2Desc')`
- 引导步骤说明 → `t('organize.emptyState.dataSourceHint')`
- 4 个功能卡（去重/归类/EXIF/转换）的 title 和 desc 全部改用 `t('organize.tools.*.title')` 和 `t('organize.tools.*.shortDesc')`
- 最近打开列表：
  - "最近打开" → `t('organize.emptyState.recentTitle')`
  - "· 点击快速重开" → `t('organize.emptyState.recentHint')`
  - "项目" 徽章 → `t('organize.emptyState.projectBadge')`
  - "项目 ID · {id}..." → `t('organize.projectIdPrefix', { id })`
  - "打开" 按钮 → `t('organize.emptyState.openButton')`
  - "从历史移除" title → `t('organize.emptyState.removeFromHistory')`

### 二、TemplateGallery.tsx 国际化完成

[TemplateGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/TemplateGallery.tsx)：

#### 1. 引入 useTranslation
- 添加 `import { useTranslation } from 'react-i18next';`
- 在 `TemplateGallery`、`TemplateCard`、`CreateTemplateCard` 三个组件中分别添加 `const { t } = useTranslation();`

#### 2. 筛选器数组重构
- `COUNT_FILTERS` 数组：`label: string` → `labelKey: string`，使用 `home.templateGallery.countAll/count1/count2/count3/count4/countMulti`
- `CATEGORY_FILTERS` 数组：`label: string` → `labelKey: string`，使用 `home.templateGallery.categoryAll/categoryClassic/categoryCreative/categoryPersonality/categoryCustom`
- 筛选按钮渲染：`{f.label}` → `{t(f.labelKey)}`

#### 3. 模板分类标签国际化
- `allTemplates` useMemo 中的 `catMap`：
  - '经典布局' → `t('home.templateGallery.categoryClassicLabel')`
  - '创意布局' → `t('home.templateGallery.categoryCreativeLabel')`
  - '个性布局' → `t('home.templateGallery.categoryPersonalityLabel')`
  - '自定义' → `t('home.templateGallery.categoryCustomLabel')`
- 为 useMemo 依赖数组补充 `t`

#### 4. 模板复制后缀国际化
- "（副本）" → `t('home.templateGallery.copySuffix')`（新增 key）

#### 5. JSX 硬编码文案替换
- 标题 "布局库" → `t('home.templateGallery.title')`
- 副标题 "选择一个布局开始制作你的专属相册" → `t('home.templateGallery.subtitle')`
- "创建布局" 按钮 → `t('home.templateGallery.createTemplate')`
- 空状态 "该分类暂无布局" / "试试其他筛选条件" → `t('home.templateGallery.noTemplate')` / `t('home.templateGallery.tryOther')`
- 创建相册弹窗 title "使用布局创建相册" → `t('home.templateGallery.useTemplateCreate')`
- 删除确认弹窗：
  - "删除布局" → `t('home.templateGallery.deleteTitle')`
  - "确定要删除此布局吗？此操作不可撤销。" → `t('home.templateGallery.deleteConfirm')`
  - "取消" → `t('home.templateGallery.cancel')`
  - "删除" → `t('home.templateGallery.delete')`

#### 6. TemplateCard 组件文案
- 槽位计数 "N位 · {category}" → `t('home.templateGallery.slotsCount', { count, category })`
- 默认分类 "布局" → `t('home.templateGallery.defaultCategory')`
- "自定义" 徽章 → `t('home.templateGallery.customBadge')`
- "更多操作" title → `t('home.templateGallery.moreActions')`
- 菜单项 "创建相册" / "复制" / "编辑布局" / "删除布局" → 对应 `t('home.templateGallery.createAlbum/copy/editLayout/deleteLayout')`

#### 7. CreateTemplateCard 组件文案
- "创建新布局" → `t('home.templateGallery.createTemplateCard')`
- "从零开始设计布局" → `t('home.templateGallery.createTemplateDesc')`

### 三、Locale 文件更新

#### zh-CN.json 新增 key
- `organize.photoUnit`: "张照片"
- `home.templateGallery.copySuffix`: "（副本）"

#### en-US.json 新增 key
- `organize.photoUnit`: "photos"
- `home.templateGallery.copySuffix`: "(Copy)"

### 四、TypeScript 类型检查

运行 `npx tsc --noEmit` 通过，0 错误。

### 五、剩余未处理项

之前会话遗留的待处理项（本次未涉及）：
- P0-3 · 贴纸文字未使用 i18n
- P1-4 · 贴纸删除后存在孤立元素
- P1-5 · Canvas 渲染未使用 useMemo
- 10 个 P2/P3 重构与体验优化

---

## 2026-07-28 · i18n 全局生效修复 + 代码质量分析 + 残留 P0 修复

### 背景
用户反馈 EN/CN 语言切换未全局生效，并要求对整体代码质量进行分析、给出优化方案。本次会话在完成 i18n 修复后，顺带根据分析报告修复了 2 个残留 P0 问题。

### 一、EN/CN 切换未全局生效根因与修复

**根因**（3 条）：
1. **整个 EditorView 及其子组件未使用 `useTranslation()` hook** — 即使 locale 文件中有 key，这些组件既不订阅 `languageChanged` 事件，也不重渲染。所有文案硬编码中文。
2. **[LanguageSwitcher.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/LanguageSwitcher.tsx) 用 `getCurrentLanguage()` 同步读取**而非 `useTranslation()` hook，切换后高亮态不更新。
3. **编辑器顶栏无语言切换入口** — `LanguageSwitcher` 仅在 [HomeView.tsx:441](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx) 一处挂载。

**修复内容**：

#### 1. LanguageSwitcher 改用 useTranslation 订阅
[LanguageSwitcher.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/LanguageSwitcher.tsx)：
- 移除 `getCurrentLanguage()` 同步读取
- 改用 `const { i18n } = useTranslation(); const current = i18n.language;`
- 切换语言时组件自动重渲染，高亮态同步更新

#### 2. 编辑器顶栏添加语言切换入口
[Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx)：
- 在保存按钮前挂载 `<LanguageSwitcher compact className="mr-1" />`
- 用户在编辑器内也能切换语言

#### 3. 扩展 locale JSON 命名空间
[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json) 同步新增：
- `common.gotIt`
- `home.stickers.*`（galleryTitle/gallerySubtitle/filterAll/filterCustom/empty/emptyFiltered/emptyHint/emptyFilteredHint/customBadge）
- `license.layoutAdjustRequiresActivation` / `layoutSwitchRequiresActivation` / `photoShuffleRequiresActivation`
- `editor.canvas.*`（dragToSlotHint/addPhotosConfirmTitle/addPhotosConfirmDesc/pageNumber/photosMovedFromPages/dropPhotoPlaceholder）
- `editor.toolbar.*`（stickers/editText/bringToFront/sendToBack/flipHorizontal/flipVertical/resetDefault/hideWatermark/showWatermark/watermarkHidden/clearPhoto/clear/deleteSlot）
- `editor.bottomNav.*`（30+ key：）
- `editor.sticker.*`（title/upload/uploadedCount/uploadNone/uploadFailed/deleted/deleteFailed/empty/emptyHint/dragHint/deleteTitle/deleteConfirm/delete）
- `editor.pageToolbar.*`（22 key）
- `editor.tools.*`（40+ key）
- `editor.dragDrop.noValidImages`
- `editor.project.*`（opened/photosLoadFailed/created）
- `editor.autosave.*`（warning/backupNow）
- `editor.backDialog.*`（title/continue/discard/saving/saveAndBack/message）

#### 4. 8 个编辑器组件硬编码文案 i18n 改造
| 文件 | 改造点数 | 备注 |
|---|---|---|
| [StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx) | 19 处 | 含 `uploadedCount` 插值 |
| [StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx) | 21 处 | 筛选数组 label→labelKey |
| [PageToolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageToolbar.tsx) | 20+ 处 | 含 `clearedPhotos` 插值 |
| [ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx) | 40+ 处 | BRUSH_TYPES 改 labelKey；FONT_FAMILIES 保留 |
| [BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx) | 34 处 | 修复遗漏的 useTranslation hook |
| [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) | 26+ 处 | 移除未使用的 endStickerDrag 导入 |
| [LeftPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/LeftPanel.tsx) | 8 处 | tabs 数组 label→labelKey |
| [EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx) | 14 处 | 含 2 处 `opened` + 2 处 `unnamedAlbum` |

### 二、代码质量分析与残留 P0 修复

根据代码质量分析报告（见下方"代码质量分析报告摘要"），发现并修复了 2 个 P0 问题：

#### P0-1 · addPage/insertPage 的 pageId 碰撞
[pageSlice.ts:42, 68](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)：
- 原：`id: \`page-${Date.now()}\``（无随机后缀，快速调用会碰撞）
- 修复：`id: \`page-${Date.now()}-${Math.random().toString(36).slice(2, 8)}\``（与 copyPage 对齐）

#### P0-2 · addPhotoSlot 注释与代码矛盾
[pageSlice.ts:251-253](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)：
- 原注释声称"使用 crypto.randomUUID + 时间戳"，实际代码是 `Date.now() + Math.random()`
- 修复：更正注释为"使用时间戳 + 随机后缀"，消除误导

### 三、代码质量分析报告摘要

完整分析报告已存档。核心发现按优先级：

**P0（已修复）**：
- addPage/insertPage pageId 碰撞 ✅
- addPhotoSlot 注释与代码矛盾 ✅
- 模板 slotId 跨页复用（slotId 仅页内作用域，文档化即可，非 Bug）

**P1（待修复）**：
- 选区状态散落 6 字段无互斥（[selectionSlice.ts:11-16](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/selectionSlice.ts)）
- 历史快照节流逻辑两处复制 + EditorView 绕过（pageLayoutService / slotEditService / EditorView.tsx:217,250,314）
- 平行拖拽系统重复（drag-manager.ts vs sticker-drag.ts）
- 浮动工具栏三份重复（Canvas.tsx 文字/便利贴/贴纸工具栏）
- ID 生成遍布全仓 28 处不统一

**P2（重构机会）**：
- Canvas.tsx 2400 行内联回调破坏子组件 memo
- StickerNode/StickyNoteNode/TextElementNode 未用 React.memo
- LRU 缓存两套实现（useStickerSrc.ts vs utils/lruCache.ts）
- Canvas.tsx 单文件 2400 行应拆分

**P3（体验优化）**：
- 选区命名风格混搭
- 服务层与 Store 边界模糊
- dataURLCache 上限硬编码 200

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，0 类型错误
- 注意：项目根 tsconfig.json 使用 project references 且 `files: []` 为空，必须用 `-p tsconfig.app.json` 才能真正检查 src/ 代码

### 涉及文件
**i18n 基础设施**：
- `src/components/common/LanguageSwitcher.tsx` — 改用 useTranslation
- `src/components/editor/Toolbar.tsx` — 挂载 LanguageSwitcher
- `src/i18n/locales/zh-CN.json` / `en-US.json` — 扩展命名空间

**i18n 改造**：
- `src/components/editor/StickerPanel.tsx`
- `src/components/home/StickerGallery.tsx`
- `src/components/editor/PageToolbar.tsx`
- `src/components/editor/ToolsPanel.tsx`
- `src/components/editor/BottomNav.tsx`
- `src/components/editor/Canvas.tsx`
- `src/components/editor/LeftPanel.tsx`
- `src/components/views/EditorView.tsx`

**P0 修复**：
- `src/store/editorStore/pageSlice.ts` — pageId 加随机后缀 + 注释更正

---

## 2026-07-28 · 底部导航栏与画布浮动工具栏 i18n 改造（BottomNav / Canvas）

### 背景
继续推进编辑器界面文案国际化，将 [BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx) 与 [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 两个组件中硬编码的中文 UI 文案替换为 `react-i18next` 的 `t()` 调用。所有 key 已预先定义在 [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) 与 [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json) 中，无需新增翻译条目。

### 修改内容

#### 1. [src/components/editor/BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)
- 顶部新增 `import { useTranslation } from 'react-i18next';`
- `BottomNav` 函数体开头新增 `const { t } = useTranslation();`（修复此前遗漏的 hook 调用，见下方"问题修复"）
- `InsertZone` 子组件（forwardRef）函数体开头同样新增 `const { t } = useTranslation();`
- **Toast 文案（16 处）**：`pageInserted`、`pageCopied`、`pageStyleApplied`、`pageDeleted`、`slotCopied`、`textCopied`、`stickyCopied`、`stickerCopied`、`pageCopiedToast`、`clipboardEmpty`、`pagePasted`、`noEmptySlot`、`photoPasted`、`textPasted`、`stickyPasted`、`stickerPasted`
- **JSX 文案（18 处）**：
  - Tooltip/标题：`pageActions`（title ×2）、`zoomLevel`（带 percent 插值）、`resetZoom`、`hideThumbnails`/`showThumbnails`（三目）、`clickToJump`
  - 文字标签：`noPages`、`pages`、`pageCount`（带 count 插值，×2）、`collapsedPageLabel`（带 current/total 插值）
  - 视图模式（复用 `editor.viewModes.*`）：`grid`（Tooltip + 文字 ×2）、`fullscreen`（Tooltip + 文字 ×2）
  - InsertZone 菜单项：`copyPage`、`copyPageStyle`、`deletePage`

#### 2. [src/components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)
- 顶部新增 `import { useTranslation } from 'react-i18next';`
- `Canvas` 函数体开头新增 `const { t } = useTranslation();`
- **画布提示文案（6 处，`editor.canvas.*`）**：`dragToSlotHint`、`addPhotosConfirmTitle`（带 count 插值）、`addPhotosConfirmDesc`、`pageNumber`（带 n 插值，用于拼接页码列表）、`photosMovedFromPages`（带 pages 插值）、`dropPhotoPlaceholder`
- **浮动工具栏文案（20+ 处，`editor.toolbar.*`）**：
  - 文字/便利贴工具栏：`editText`、`bringToFront`（×4）、`sendToBack`（×4）
  - 贴纸工具栏：`flipHorizontal`、`flipVertical`、`bringToFront`、`sendToBack`
  - 水印工具栏：`editText`、`resetDefault`、`hideWatermark`、`showWatermark`、`watermarkHidden`
  - 照片槽工具栏：`bringToFront`、`sendToBack`、`clearPhoto`、`clear`、`deleteSlot`

### 问题修复
本次改造中发现并修复了两个 TypeScript 编译错误：
1. **BottomNav 缺失 `useTranslation` hook 调用**：`BottomNav` 主组件函数体中使用了 `t()` 但未声明 `const { t } = useTranslation();`（仅 `InsertZone` 子组件声明了）。导致 34 处 `TS2304: Cannot find name 't'` 错误。已在 `BottomNav` 函数体开头补上 hook 调用。
2. **Canvas 未使用导入**：`endStickerDrag` 从 `../../engine/sticker-drag` 导入但未在文件中使用（`TS6133`）。已从 import 语句中移除。

### 验证
- `node "node_modules\typescript\bin\tsc" --noEmit -p tsconfig.app.json` 通过，`TSC_EXIT_CODE=0`，0 类型错误
- **注意**：项目根 `tsconfig.json` 使用 project references 且 `files: []` 为空，直接运行 `tsc --noEmit`（不带 `-p`）不会检查任何文件，会误报 0 错误。必须使用 `-p tsconfig.app.json` 才能真正检查 `src/` 下的代码。
- Grep 复查两文件无残留硬编码中文 UI 文案（注释除外）

### 涉及文件
- `src/components/editor/BottomNav.tsx` — 底部导航栏 i18n 改造 + 修复缺失的 useTranslation hook
- `src/components/editor/Canvas.tsx` — 画布及浮动工具栏 i18n 改造 + 移除未使用的 endStickerDrag 导入

---

## 2026-07-28 · 编辑器面板组件 i18n 改造（PageToolbar / ToolsPanel）

### 背景
继续推进编辑器界面文案国际化，将 [PageToolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageToolbar.tsx) 与 [ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx) 两个组件中硬编码的中文 UI 文案替换为 `react-i18next` 的 `t()` 调用。所有 key 已预先定义在 [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) 与 [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json) 中，无需新增翻译条目。

### 修改内容

#### 1. [src/components/editor/PageToolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageToolbar.tsx)
- 顶部新增 `import { useTranslation } from 'react-i18next';`，`PageToolbar` 函数体开头新增 `const { t } = useTranslation();`
- Toast 文案（6 处）：
  - 激活提示：`license.layoutAdjustRequiresActivation`、`license.layoutSwitchRequiresActivation`、`license.photoShuffleRequiresActivation`
  - `editor.pageToolbar.noPhotos`、`editor.pageToolbar.clearedPhotos`（带 count 插值）、`editor.pageToolbar.layoutReset`、`editor.pageToolbar.slotAdded`、`editor.pageToolbar.pageDeleted`
- JSX 文案（标题 + 按钮，共 20 处）：`expand`/`collapse`（三目 title）、`layoutAdjustHint`/`layoutSwitchHint`/`photoReorderHint`/`addSlotHint`/`resetLayoutHint`/`clearAllHint`/`deletePageHint`（button title）及对应按钮文字 `addSlot`/`resetLayout`/`clearAll`/`deletePage`
- 按钮文字使用 `editor.layout.*` 命名空间：`layoutAdjust`、`layoutSwitch`、`photoReorder`

#### 2. [src/components/editor/ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)
- 顶部新增 `import { useTranslation } from 'react-i18next';`，`ToolsPanel` 函数体开头新增 `const { t } = useTranslation();`
- **模块级数组处理**：`BRUSH_TYPES` 数组定义在组件外（模块级），无法直接调用 hook，故将 `label` 字段值改为 i18n key 字符串（`editor.tools.brushPencil`/`brushBrush`/`brushMarker`/`brushHighlighter`），渲染处 `{bt.label}` → `{t(bt.label)}`
- **数据数组保留**：`FONT_FAMILIES = ['思源黑体', '宋体', '微软雅黑', '楷体', '黑体', '仿宋']` 为字体名数据（非 UI 文案），按要求未改动；`handleAddText`/`handleAddSticky` 中 `fontFamily: '思源黑体'` 为元素默认字体数据，同样保留
- 画笔区：`brushSection`（section 标题）、`brush`/`eraser`（按钮）、`brushType`、`strokeWidth`、`opacity`、`eraserHint`、`eraserSize`、`clearAllStrokes`（带 count 插值）、`brushHint`
- 文字区：`textSection`、`addText`、`quickStyles`、`textProperties`、`fontFamily`、`fontSize`、`bold`、`italic`、`textColor`、`align`、`alignLeft`/`alignCenter`/`alignRight`（数组 label）、`textDirection`、`horizontal`/`vertical`、`bringToFront`/`sendToBack`（复用 `editor.toolbar.*`，文字与便利贴两处共 4 次）
- 便利贴区：`stickySection`、`addSticky`、`defaultSize`、`sizeSmall`/`sizeMedium`/`sizeLarge`（默认大小与大小调整两处共用同一组 key）、`stickyProperties`、`color`、`style`、`styleRounded`/`styleSquare`/`styleTape`/`styleShadow`、`size`、`stickyHint`
- 背景区：`backgroundSection`
- Header 标题：`editor.tools.title`

### 注意事项
- 保持原有逻辑、样式、事件处理完全不变，仅替换文案
- 模块级数组（BRUSH_TYPES）采用「label 存 key + 渲染时 `t(bt.label)`」模式，与 LeftPanel 的 `labelKey` 方案一致
- 内联数组（align/size/style 选项）因处于组件作用域内，直接在 label 值中调用 `t()`
- 代码注释中的中文未改动，仅替换用户可见文案

### 验证
- `node "node_modules\typescript\bin\tsc" --noEmit` 通过，`TSC_EXIT_CODE=0`，0 类型错误
- Grep 复查两文件无残留硬编码中文 UI 文案（剩余中文均为注释、FONT_FAMILIES 数据及元素默认字体值）

### 涉及文件
- `src/components/editor/PageToolbar.tsx` — 画布浮动工具栏 i18n 改造
- `src/components/editor/ToolsPanel.tsx` — 左侧工具面板 i18n 改造

---

## 2026-07-28 · 贴纸组件 i18n 改造（StickerPanel / StickerGallery）

### 背景
落实 [需求审查 P0/P1 问题修复](#2026-07-28--需求审查-p0p1-问题修复) 中遗留的 **P0-3 贴纸功能文案未走 i18n** 问题（30+ 处中文硬编码）的第一批：编辑器贴纸面板与主页贴纸库两个组件的硬编码中文文案替换为 `t()` 调用。

### 修改内容

#### 1. [src/components/editor/StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)
- 顶部新增 `import { useTranslation } from 'react-i18next';`
- `StickerPanel` 与 `StickerGridItem` 两个组件函数体开头各添加 `const { t } = useTranslation();`
- Toast 文案（5 处）：`uploadedCount`（带 count 插值）、`uploadNone`、`uploadFailed`、`deleted`、`deleteFailed`
- JSX 文案（10 处）：`title`、`upload`（×2）、`empty`、`emptyHint`、`dragHint`、`deleteTitle`、`deleteConfirm`、`common.cancel`、`common.delete`
- 图标按钮 `title="删除贴纸"` → `title={t('editor.sticker.delete')}`

#### 2. [src/components/home/StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)
- 顶部新增 `import { useTranslation } from 'react-i18next';`
- `StickerGallery` 与 `StickerCard` 两个组件函数体开头各添加 `const { t } = useTranslation();`
- 筛选标签数组 `CATEGORY_FILTERS` 的 `label` 字段改为 `labelKey`（存 i18n key），渲染处 `{f.label}` → `{t(f.labelKey)}`
  - `home.stickers.filterAll`、`home.stickers.filterCustom`
- Toast 文案（5 处）：与 StickerPanel 相同的 5 个 `editor.sticker.*` key
- JSX 文案：`galleryTitle`、`gallerySubtitle`、`upload`（×2）、`empty`/`emptyFiltered`（三目）、`emptyHint`/`emptyFilteredHint`（三目）、`deleteTitle`、`deleteConfirm`、`common.cancel`、`common.delete`
- `StickerCard` 自定义徽章 `自定义` → `{t('home.stickers.customBadge')}`
- 图标按钮 `title="删除贴纸"` → `title={t('editor.sticker.delete')}`

### i18n key 来源
所有 key 均已存在于 [src/i18n/locales/zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) 与 [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)（`common.*`、`editor.sticker.*`、`home.stickers.*` 命名空间），无需新增翻译条目。

### 验证
- `node "node_modules\typescript\bin\tsc" --noEmit` 通过，`TSC_EXIT=0`，0 类型错误
- Grep 复查两文件无残留硬编码中文 UI 文案（注释除外）
- 原有逻辑、样式、事件处理完全未改动，仅替换文案字面量为 `t()` 调用

### 涉及文件
- `src/components/editor/StickerPanel.tsx` — 贴纸面板 i18n 改造
- `src/components/home/StickerGallery.tsx` — 贴纸库 i18n 改造

### 备注
P0-3 原描述为"30+ 处中文硬编码"，本次处理两个贴纸组件共约 30 处。如其他模块（如画布贴纸浮动工具栏、贴纸粘贴 toast 等）仍有硬编码，留待后续批次处理。

---

## 2026-07-28 · LeftPanel 与 EditorView 文案 i18n 改造

### 背景
推进编辑器界面文案国际化，将 [LeftPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/LeftPanel.tsx) 与 [EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx) 中硬编码的中文文案替换为 `react-i18next` 的 `t()` 调用。所有 key 已预先定义在 [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) 与 [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json) 中。

### 改造内容

#### 1. LeftPanel.tsx
- 顶部新增 `import { useTranslation } from 'react-i18next';`，组件函数体开头新增 `const { t } = useTranslation();`
- `tabs` 数组定义在模块级（组件外），无法直接调用 `t()`，故将 `TabItem.label: string` 改为 `labelKey: string`，数组中存储 i18n key（`editor.toolbar.photos/templates/stickers/tools`），渲染时用 `t(item.labelKey)` 调用
- 主题切换按钮：`title` 改为 `isDark ? t('theme.switchToLight') : t('theme.switchToDark')`；底部文字改为 `isDark ? t('theme.light') : t('theme.dark')`

#### 2. EditorView.tsx
- 顶部新增 `import { useTranslation } from 'react-i18next';`，组件函数体开头新增 `const { t } = useTranslation();`
- 替换 14 处硬编码文案（含 2 处 `已打开项目「${project.name}」` 与 2 处 `未命名相册`，均全部替换）：
  - 拖拽导入：`t('editor.dragDrop.noValidImages')`
  - 项目打开 toast：`t('editor.project.opened', { name: project.name })`（2 处）
  - 未命名相册兜底：`t('home.create.unnamedAlbum')`（2 处：setProjectName 兜底 + createAndSaveProject 新建）
  - 照片加载失败：`t('editor.project.photosLoadFailed', { count: failedCount })`
  - 新建相册 toast：`t('editor.project.created')`
  - 自动保存失败横幅：`t('editor.autosave.warning')` + 按钮文案 `t('editor.autosave.backupNow')` + 关闭按钮 `title={t('common.gotIt')}`
  - 返回主页 Modal：`title`/`continue`/`discard`/`saving`/`saveAndBack`/`message` 全部改用 `editor.backDialog.*`

### 注意事项
- 保持原有逻辑、样式、事件处理完全不变，仅替换文案
- `setAutoSaveStatusHandler` 回调中闭包使用 `t`，未将其加入 useEffect 依赖数组（`t` 来自 react-i18next 身份稳定，且原 effect 未含该依赖，避免改变重新注册行为）
- 代码注释中的中文（如 `// ── 页面离开/返回主页时强制保存 ──`）未改动，仅替换用户可见文案

### 验证
- `node node_modules\typescript\bin\tsc --noEmit` 通过，0 类型错误（TSC_EXIT_CODE=0）

---

## 2026-07-28 · 需求审查 P0/P1 问题修复

### 背景
针对 [需求1-7设计合理性审查](#2026-07-28--需求1-7设计合理性审查) 中识别的 3 个 P0 + 部分 P1 问题进行代码修复。验证：`npx tsc --noEmit` 通过，零类型错误。

### 修复内容

#### P0-1 · Delete 快捷键双触发（[BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)）
**根因**：BottomNav 在**捕获阶段**注册 Delete 删整页（`addEventListener('keydown', handler, true)`），[useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts) 在**冒泡阶段**注册 Delete 删元素。捕获先于冒泡，导致选中元素按 Delete 时先触发删整页而非删元素。

**修复**：在 BottomNav 的 Delete 处理中先读取 `useEditorStore.getState()` 获取所有选中字段（`selectedSlotId/selectedTextId/selectedStickyId/selectedStickerId`），若任一选中元素在当前页确实存在则 `return`，让画布的冒泡处理器处理元素删除。同时校验元素在当前页存在，避免选中态残留导致 Delete 永久失效。

#### P0-2 · Canvas 渲染期间 setState 警告（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）
**根因**：便利贴浮动工具栏（第 2155 行）和贴纸浮动工具栏（第 2199 行）在 JSX 渲染回调中调用 `setSelectedStickyId(null)` / `setSelectedStickerId(null)`，违反 React 渲染期间不可触发 setState 的规则，会触发警告并可能导致无限重渲染。

**修复**：
- 移除两处 JSX 回调中的 setState 调用，改为只 `return null`
- 新增两个独立 `useEffect` 监听 `selectedStickyId` / `selectedStickerId` 与 `currentPage`：当选中元素在当前页不存在时（被删除或切页），自动清空选中态。这是处理"选中态残留"的安全位置。

#### P1-1 · Ctrl+C/V 不支持贴纸（[BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)）
**根因**：[需求7](#2026-07-28--产品需求1-7整体分析与开发总览) 承诺"选中内容时复制内容"但 BottomNav 的 Ctrl+C/V 遗漏了 `selectedStickerId`，贴纸无法复制粘贴。

**修复**：
- `ClipboardData` 联合类型新增 `{ type: 'sticker'; sticker: StickerElement }` 分支
- Ctrl+C：在便利贴之后增加贴纸复制分支（查找 `page.stickerElements` 并深拷贝）
- Ctrl+V：新增 `clip.type === 'sticker'` 分支，生成新 ID（`sticker-el-${Date.now()}-${random}`）+ 位置偏移 10mm，调用 `addStickerElement`
- useEffect 依赖数组新增 `addStickerElement`

#### P1-2 · extraSlots slotId 用 Date.now() 易碰撞（[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)）
**根因**：`addPhotoSlot` 用 `slot-${Date.now()}` 生成 ID，快速连点"添加照片位"可能产生相同 ID，`resolveTemplate` 去重会把第二个槽位过滤掉。

**修复**：改为 `slot-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`，加 6 位随机后缀避免碰撞。

#### P1-3 · useStickerSrc LRU 算法错误（[useStickerSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/useStickerSrc.ts)）
**根因**：原实现用 `Map.keys().next()` 取最早插入的 key 淘汰，但 Map 只在 `set` 时记录插入顺序，**读取命中时不更新顺序**。结果是热点贴纸（频繁读取但只插入一次）会被错误地当作"最旧"淘汰，与 LRU 语义相反。

**修复**：利用 Map 的"set 已存在的 key 不更新顺序，需先 delete 再 set"特性，新增 `touchCache(blobId)` 辅助函数，在所有命中点调用：
- `cacheDataURL`：若 key 已存在先 `delete` 再 `set`（移到末尾），否则按容量淘汰第一个 key
- `touchCache`：命中时 delete+set 把条目移到 Map 末尾
- `getCachedStickerSrc` / `preloadStickerSrc` / `useStickerSrc` 的 `useState` 初始化和 `useEffect` 命中分支均调用 `touchCache`

### 验证
- TypeScript 类型检查通过（`npx tsc --noEmit` 0 错误）
- 修复对现有功能无破坏：BottomNav 的 Delete 仍可在无选中时删整页；Canvas 的选中态清理逻辑与原 JSX 内联清理等价

### 涉及文件
- `src/components/editor/BottomNav.tsx` — Delete 选中态判断 + Ctrl+C/V 贴纸支持
- `src/components/editor/Canvas.tsx` — 渲染期 setState 修复 + 选中态清理 useEffect
- `src/store/editorStore/pageSlice.ts` — slotId 加随机后缀
- `src/hooks/useStickerSrc.ts` — LRU 算法修正

### 未处理的审查问题
以下问题在本次修复中未处理，留待后续迭代：
- **P0-3 贴纸功能文案未走 i18n**（30+ 处中文硬编码）
- **P1-4 删除贴纸后页面引用未清理**（孤儿元素显示空白虚线框）
- **P1-5 Canvas 渲染未 useMemo 缓存**
- P2/P3 共 10 项重构与体验优化

---

## 2026-07-28 · 任务栏图标模糊修复 + favicon 视图框修复

### 背景
用户反馈应用打开后，Windows 任务栏中 MemBook 图标显示模糊。同时排查其他场景的图标显示问题。

### 根因分析

**任务栏图标模糊（核心问题）**：
- [scripts/make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs) 原使用 **PNG-in-ICO** 格式构建 `icon.ico`
- PNG-in-ICO 虽被 Windows Vista+ 支持，但任务栏使用 GDI/Direct2D 渲染图标时，对 BMP 格式有原生优化；PNG 需额外解码步骤，某些 DPI 缩放场景下会引入模糊
- BMP-in-ICO 的 32 位 BGRA 模式同样支持完整 8 位 alpha 透明通道，渲染质量更稳定
- 原 .ico 大小 79.6 KB（PNG 压缩），8 个尺寸条目（16/24/32/48/64/96/128/256）

**favicon.svg 视图框裁剪**：
- [public/favicon.svg](file:///f:/N-编程/MenBook开发项目/MemBook/public/favicon.svg) 原 `viewBox="0 0 100 80"`，但路径内容 y 轴延伸到 85（六边形底部），底部 5px 被裁剪
- `width="32" height="32"`（1:1）与 viewBox 100:80（5:4）宽高比不匹配，导致 SVG 被纵向拉伸变形

**其他场景图标检查结果（无问题）**：
- 应用内标题栏 Logo（[Logo.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Logo.tsx)）：使用 1060×805 高分辨率 PNG，显示尺寸 20-64px，`object-contain` 保持比例，无模糊
- Alt+Tab / 文件管理器 / 桌面快捷方式：均使用 .exe 内嵌图标资源，与任务栏同源，BMP-in-ICO 修复后同步解决
- NSIS 安装程序图标：`tauri.conf.json` 配置 `installerIcon: "icons/icon.ico"`，修复后同步生效

### 修复内容

#### 1. .ico 格式从 PNG-in-ICO 改为 BMP-in-ICO
[scripts/make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs) `makeIco` 函数重写：
- 原：sharp 生成 PNG buffer → 直接嵌入 ICO 容器
- 新：sharp 输出 RGBA raw 像素 → 转 BGRA + 行序翻转（BMP 自下而上）→ 构建 BITMAPINFOHEADER(40B) + XOR 位图(BGRA) + AND 掩码(全零) → 嵌入 ICO 容器
- 32 位 BGRA 完整支持 alpha 透明通道，AND 掩码全零由 alpha 通道处理透明
- 尺寸列表不变：`[16, 24, 32, 48, 64, 96, 128, 256]`，覆盖任务栏所有 DPI 场景
- 新 .ico 大小 401.0 KB（BMP 无压缩，体积增大但渲染质量优先）

#### 2. 重新生成 icon.ico
基于已有的 `icon-source-prepared.png`（1376×1376，5% 边距）重新生成 `src-tauri/icons/icon.ico`：
- 8 个 BMP 32-bit 条目，结构验证通过
- 每个 BITMAPINFOHEADER `biHeight = 2 * iconHeight`（XOR + AND 掩码）
- `biBitCount = 32`，`biCompression = BI_RGB`

#### 3. favicon.svg 视图框修复
[public/favicon.svg](file:///f:/N-编程/MenBook开发项目/MemBook/public/favicon.svg)：
- `viewBox` 从 `0 0 100 80` 改为 `0 0 100 100`（正方形，与 width/height 1:1 匹配）
- 路径内容用 `<g transform="translate(0, 5)">` 包裹，垂直居中（原 y=5-85 → y=10-90）
- 修复底部裁剪 + 宽高比变形

### 验证
- .ico 结构二进制验证：8 条目全部为 BMP 32-bit 格式，尺寸/偏移量正确
- favicon.svg：viewBox 100×100 正方形，内容居中无裁剪

### 生效方式
> **需要重新构建 EXE 安装包**才能看到任务栏图标修复效果（.ico 嵌入在 .exe 资源中）。
> 在普通 PowerShell 窗口（非 TRAE IDE）运行：
> ```powershell
> cd "f:\N-编程\MenBook开发项目\MemBook"
> .\build-exe.ps1
> ```
> favicon.svg 修复在 `npm run dev` 或 `npm run build` 后即时生效。

### 涉及文件
- `scripts/make-icons.cjs` — `makeIco` 函数重写为 BMP-in-ICO
- `src-tauri/icons/icon.ico` — 重新生成（79.6 KB PNG → 401.0 KB BMP）
- `public/favicon.svg` — viewBox 修复 + 内容居中

---

## 2026-07-28 · 需求1-7设计合理性审查

### 背景
对刚完成的 7 项需求（滚动条优化、贴纸功能、模板→布局文案、文字/便利贴工具栏重构、添加照片位、Ctrl+C/V 优化）进行架构/状态管理/代码复用/性能/交互一致性/边界场景/国际化 7 维度审查。仅分析不修改代码。

### 审查结论
总体架构方向合理（extraSlots 通过 resolveTemplate 统一合并、贴纸 schema 复用 blobs 表、状态切片清晰），但存在 **3 个 P0、5 个 P1、5 个 P2、5 个 P3** 共 18 个问题需按优先级修复。

### 问题清单（按优先级）

#### P0（必须修复）
1. **Delete 快捷键双触发** — [BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx) 在捕获阶段 `addEventListener('keydown', handler, true)` 注册 Delete 删整页，[useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts) 在冒泡阶段注册 Delete 删元素。选中元素按 Delete 会先触发删整页，而非删元素。修复：BottomNav 的 Delete 处理增加选中态判断，有选中元素时 return。
2. **渲染期间 setState 警告** — [Canvas.tsx:2199](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 在 JSX 渲染回调中调用 `setSelectedStickerId(null)`，触发 React 警告，潜在无限重渲染。修复：迁到 `useEffect` 中。
3. **贴纸功能文案未走 i18n** — Canvas.tsx / StickerPanel.tsx / StickerGallery.tsx 共 30+ 处中文硬编码，英文版用户完全不可用。修复：在 zh-CN.json / en-US.json 添加 `editor.stickers` 命名空间。

#### P1（下个迭代修复）
4. **Ctrl+C/V 不支持贴纸** — [BottomNav.tsx:366](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx) 遗漏 `selectedStickerId`，需求 7 承诺"选中内容时复制内容"未覆盖贴纸。
5. **删除贴纸后页面引用未清理** — [db/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/db/index.ts) `deleteSticker` 未扫描各项目 `stickerElements` 的 `stickerId` 引用，孤儿元素显示空白虚线框。
6. **extraSlots slotId 用 Date.now() 易碰撞** — [pageSlice.ts:251](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts) 快速连点"添加照片位"可能产生相同 ID，`resolveTemplate` 去重会把第二个槽位过滤掉。修复：用 `crypto.randomUUID()`。
7. **useStickerSrc LRU 算法错误** — [useStickerSrc.ts:16-27](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/useStickerSrc.ts) `Map.keys().next()` 取最早插入而非最久未访问，热点贴纸会被错误淘汰。修复：访问命中时 delete+set 移到末尾。
8. **Canvas 渲染未 useMemo 缓存** — [Canvas.tsx:1719-1731](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 每次 Canvas 重渲染都 forEach+push+sort+map，元素多时性能差。

#### P2（重构机会）
9. extraSlots 与 slotOverrides 坐标系不一致（百分比 vs 像素），`calcMarginOverrides` 会重排手动添加的槽位
10. 选中态 5 个独立字段分散（selectedSlotId/PhotoId/TextId/StickyId/StickerId），互斥靠手动调用，违反开闭原则
11. 拖拽系统两套平行实现（[drag-manager.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/drag-manager.ts) vs [sticker-drag.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/sticker-drag.ts)），结构 100% 同构
12. 三种浮动工具栏（文字/便利贴/贴纸）代码重复，应抽 `<FloatingToolbar>` 容器组件
13. resolveTemplate 每次重新合并 extraSlots，可用 WeakMap 缓存

#### P3（体验优化）
14. 贴纸浮动工具栏缺少"复制"按钮，文字/便利贴有
15. 贴纸默认 60mm 宽硬编码，小尺寸相册占比过大
16. addPhotoSlot 不验证重叠，多次添加完全重叠
17. StickerNode 拖拽越界无约束，可拖出页面
18. PageToolbar / ToolsPanel 部分文案硬编码未走 i18n

### 涉及审查的文件
- `src/types/index.ts` — extraSlots / resolveTemplate / StickerElement 类型
- `src/db/index.ts` — schema version(5) + StickerRecord + deleteSticker
- `src/store/editorStore/selectionSlice.ts` — 5 个选中态字段
- `src/store/editorStore/pageSlice.ts` — addPhotoSlot slotId 生成
- `src/store/editorStore/decorationsSlice.ts` — 贴纸 CRUD
- `src/store/editorStore/helpers.ts` — calcSlotPixelSize / calcMarginOverrides
- `src/engine/drag-manager.ts` + `src/engine/sticker-drag.ts` — 两套平行拖拽系统
- `src/hooks/useStickerSrc.ts` — LRU 缓存算法
- `src/components/editor/Canvas.tsx` — 贴纸渲染 + 浮动工具栏 + 拖拽监听
- `src/components/editor/canvas/StickerNode.tsx` — Konva 贴纸节点
- `src/components/editor/canvas/useCanvasKeyboard.ts` — Delete/Escape 快捷键
- `src/components/editor/PageToolbar.tsx` — 添加照片位按钮
- `src/components/editor/BottomNav.tsx` — Ctrl+C/V 快捷键
- `src/components/editor/StickerPanel.tsx` + `src/components/home/StickerGallery.tsx` — 贴纸面板
- `src/components/editor/ToolsPanel.tsx` — 文字/便利贴属性面板
- `src/i18n/locales/zh-CN.json` / `en-US.json` — 仅 home.nav.stickers 一个贴纸翻译键

### 后续行动建议
1. 立即修复 3 个 P0 问题（Delete 双触发、渲染期 setState、贴纸 i18n）
2. 下个迭代修复 5 个 P1 问题
3. 重构机会：抽象统一拖拽框架 + 浮动工具栏组件 + 选中态单字段

---

## 2026-07-28 · 产品需求1-7整体分析与开发（总览）

### 背景
根据产品需求文档（图一需求1-7），对 MemBook 进行全面的功能分析、优化修复和新功能开发。所有7项需求均已实现并通过 TypeScript 类型检查。

### 需求完成清单

| 序号 | 需求 | 类型 | 状态 |
|:---:|:---|:---|:---:|
| 1 | 工作区滚动条显示优化 + 重置按钮居中 | 优化修复 | ✅ |
| 2 | 贴纸功能（主页导航 + 编辑器面板 + 拖拽 + 编辑） | 新增功能 | ✅ |
| 3 | 全局"模板"文案改为"布局" | 术语优化 | ✅ |
| 4 | 文字工具栏重构（悬浮栏跟随 + 设置移到左侧面板） | 工具栏重构 | ✅ |
| 5 | 便利贴工具栏重构 + 文字实时输入修复 | 工具栏重构 | ✅ |
| 6 | 编辑器顶部"添加照片位"按钮 | 新增功能 | ✅ |
| 7 | Ctrl+C/V 快捷键优化（选中内容时复制内容） | 交互优化 | ✅ |

### 涉及文件统计
- 新建文件：8 个（StickerGallery, StickerPanel, StickerNode, stickerService, useStickerSrc, sticker-drag, 等）
- 修改文件：20+ 个（types, db, store, Canvas, ToolsPanel, PageToolbar, BottomNav, HomeView, LeftPanel, i18n 等）
- 验证：`npx tsc --noEmit` 通过，零类型错误

### 详细记录
各项需求的详细开发记录见下方独立条目（按倒序排列）。

---

## 2026-07-28 · 贴纸功能完整实现（需求2）

### 背景
实现完整的"贴纸"功能，包括主页贴纸库展示和编辑器贴纸拖拽添加。用户可以自行上传贴纸图片，永久保存后在编辑器中拖拽到画布使用，支持缩放、旋转、镜像、图层管理等操作。

### 改动详情

#### 步骤1：类型定义（`src/types/index.ts`）
- 新增 `StickerElement` 类型：页面内贴纸元素，包含位置(mm)、尺寸(mm)、引用贴纸ID、旋转角度、水平/垂直镜像、zIndex
- 在 `AlbumPage` 类型中添加 `stickerElements?: StickerElement[]`
- 修改 `PanelTab` 类型添加 `'stickers'`
- 修改 `HomeTab` 类型添加 `'stickers'`

#### 步骤2：数据库（`src/db/index.ts`）
- 新增 `StickerRecord` 接口：id、name、blobId、category、width、height、createdAt
- Dexie schema 新增 version(5) 添加 `stickers` 表：`id, name, createdAt, category`
- 贴纸图片 Blob 复用现有 photo blob 存储机制（`savePhotoBlob`/`getPhotoBlob`/`deletePhotoBlob`）
- 新增方法：`saveSticker`、`listStickers`、`deleteSticker`、`readStickerBlobUrl`

#### 步骤3：状态管理（`src/store/editorStore/`）
- `types.ts`：`SelectionSlice` 添加 `selectedStickerId` 和 `setSelectedStickerId`；`DecorationsSlice` 添加 `addStickerElement`、`updateStickerElement`、`removeStickerElement`；`bringToFront`/`sendToBack` 的 type 参数添加 `'sticker'`
- `selectionSlice.ts`：实现 `selectedStickerId` 状态和 `clearSelection` 清空
- `decorationsSlice.ts`：实现贴纸元素 CRUD，zIndex 管理与文字/便利贴一致

#### 步骤4：主页贴纸导航和展示
- `HomeView.tsx`：左侧导航栏添加"贴纸"按钮（位于"布局"下方），内容区渲染 `StickerGallery`
- 新建 `StickerGallery.tsx`：网格布局展示所有贴纸，支持上传、分类筛选（全部/自定义）、删除（hover显示删除按钮）

#### 步骤5：编辑器贴纸面板
- `LeftPanel.tsx`：tabs 数组添加贴纸 tab（位于模版和工具之间），`getPanelContent` 添加 case 'stickers'
- 新建 `StickerPanel.tsx`：网格展示贴纸，顶部上传按钮，支持 mousedown 拖拽到画布

#### 步骤6：贴纸上传和服务层
- 新建 `stickerService.ts`：`uploadStickerFromFile`、`uploadStickersFromFiles`，读取图片原始尺寸并持久化
- 新建 `useStickerSrc.ts` Hook：`useStickerSrc`（dataURL/object URL 加载+缓存）、`useStickerImage`（HTMLImageElement 供 Konva.Image 使用）、`preloadStickerSrc`/`getCachedStickerSrc`

#### 步骤7：画布渲染
- 新建 `StickerNode.tsx`：Konva Group 渲染贴纸图片，支持 8 方向 resize + 旋转控制点、镜像翻转（flipH/flipV）、选中虚线边框、点击选中/拖拽移动
- `Canvas.tsx`：
  - 导入 StickerNode，添加 `addStickerElement`/`updateStickerElement`/`removeStickerElement`/`selectedStickerId`/`setSelectedStickerId` 等状态
  - 在全局图层中按 zIndex 排序渲染 stickerElements
  - 添加贴纸浮动工具栏：水平镜像、垂直镜像、置顶、置底、删除
  - 所有元素点击选中时清除贴纸选中，反之亦然

#### 步骤8：拖拽到工作区
- 新建 `sticker-drag.ts`：与 photo drag-manager 平行的轻量拖拽系统，包含 `StickerDragState`、`startStickerDrag`、`updateStickerDrag`、`endStickerDrag`、`onStickerDragStateChange`
- `StickerPanel.tsx`：贴纸卡片 mousedown 启动拖拽，确保 dataURL 预加载到缓存
- `Canvas.tsx`：监听 `onStickerDragStateChange`，拖拽时显示跟随鼠标的预览图，释放时计算 mm 坐标并调用 `addStickerElement`（默认宽度 60mm，保持图片宽高比）
- `useCanvasKeyboard.ts`：添加贴纸的 Delete/Backspace 删除和 Escape 退出选中快捷键

#### 国际化
- `zh-CN.json` / `en-US.json`：nav 添加 stickers 翻译

### 验证
- `npx tsc --noEmit` 通过，零类型错误

### 涉及文件
- `src/types/index.ts` — 类型定义
- `src/db/index.ts` — 数据库 schema 和 CRUD
- `src/store/editorStore/types.ts` — Store 类型
- `src/store/editorStore/selectionSlice.ts` — 选中状态
- `src/store/editorStore/decorationsSlice.ts` — 贴纸元素 CRUD
- `src/services/stickerService.ts` — 贴纸上传服务（新建）
- `src/hooks/useStickerSrc.ts` — 贴纸图片加载 Hook（新建）
- `src/engine/sticker-drag.ts` — 贴纸拖拽系统（新建）
- `src/components/views/HomeView.tsx` — 主页导航和内容
- `src/components/home/StickerGallery.tsx` — 主页贴纸展示（新建）
- `src/components/editor/LeftPanel.tsx` — 编辑器左侧面板
- `src/components/editor/StickerPanel.tsx` — 编辑器贴纸面板（新建）
- `src/components/editor/canvas/StickerNode.tsx` — Konva 贴纸渲染组件（新建）
- `src/components/editor/Canvas.tsx` — 画布渲染、工具栏、拖放
- `src/components/editor/canvas/useCanvasKeyboard.ts` — 键盘快捷键
- `src/i18n/locales/zh-CN.json` / `en-US.json` — 国际化

---

## 2026-07-28 · 文字和便利贴浮动工具栏重构（需求4、需求5）

### 背景
两个编辑器交互问题需要重构：
1. **文字工具栏固定顶部**：文字编辑的浮动工具栏（字体、字号、加粗等）固定在画布顶部中央，不跟随输入框位置，且工具栏功能过于密集。
2. **便利贴工具栏冗余 + 文字编辑不对齐**：便利贴浮动工具栏包含颜色/样式快速切换（与左侧面板重复），且便利贴文字输入时 textarea 与贴纸大小不对齐、padding 不一致。

### 改动详情

**需求4：文字功能工具栏重构**

- `src/components/editor/ToolsPanel.tsx`：
  - 新增 store 订阅：`selectedTextId`、`setSelectedTextId`、`updateTextElement`、`bringToFront`、`sendToBack`，以及 `selectedTextEl` 派生查找。
  - 在"文字"折叠区域中，当选中文字元素时显示**文字属性面板**：字体下拉、字号滑块、加粗/斜体切换、颜色选择器（color + hex 输入）、对齐方式（左/中/右）、文字方向（横向/竖向春联）、层级（置顶/置底）。
  - 优化 `handleAddText`：添加文字后自动选中（`setSelectedTextId`）并进入编辑模式（`setPendingTextEditId`）。
- `src/components/editor/Canvas.tsx`：
  - 文字浮动工具栏从 `fixed` 顶部居中改为 `absolute` 动态定位在文字元素上方（`toolX = (el.x + el.width/2) * MM_TO_PX * canvasZoom + groupOX`，`toolY = el.y * ... - 36`）。
  - 工具栏只保留基础操作：编辑、置顶、置底、删除、关闭，采用 icon+文字呈现方式（与照片槽工具栏风格一致）。
  - 移除工具栏中的字体、字号、加粗、斜体、颜色、对齐、文字方向控件（已迁移至左侧面板）。

**需求5：便利贴功能工具栏重构**

- `src/components/editor/ToolsPanel.tsx`：
  - 新增 store 订阅：`selectedStickyId`、`setSelectedStickyId`、`updateStickyNote`，以及 `selectedStickyNote` 派生查找。
  - 便利贴区域新增"添加便利贴"按钮（与文字添加按钮风格一致）。
  - 当选中便利贴时显示**便利贴属性面板**：颜色选择（6色）、样式切换（圆角/方形/胶带/阴影）、大小调整（小/中/大，直接修改选中便利贴尺寸）、层级（置顶/置底）。
  - 优化 `handleAddSticky`：添加便利贴后自动选中并进入编辑模式（复用 `setPendingTextEditId` 机制）。
- `src/components/editor/Canvas.tsx`：
  - 便利贴浮动工具栏移除颜色快速切换和样式快速切换，只保留：编辑、置顶、置底、删除、关闭，采用 icon+文字呈现方式。
  - 工具栏偏移从 -40 调整为 -36，与文字工具栏保持一致。

**便利贴文字编辑修复**

- `src/components/editor/Canvas.tsx` 编辑浮层：
  - textarea padding 从固定 4px 改为根据元素类型动态计算：文字元素 4px、便利贴 6px（匹配 Konva Text 内部 `x={-pw/2+6}` 的 6px padding）。
  - 新增便利贴旋转支持：当便利贴有旋转角度时，textarea 容器应用 `transform: rotate()` + `transformOrigin: center center`，与 Konva Group 的中心旋转对齐。
- `src/components/editor/Canvas.tsx` `pendingTextEditId` effect：
  - 扩展为同时支持文字元素和便利贴的自动编辑：先在 `textElements` 中查找，再在 `stickyNotes` 中查找，找到后设置对应选中态并进入编辑模式。

### 验证
- `npx tsc --noEmit` 类型检查通过，零错误。

### 涉及文件
- `src/components/editor/ToolsPanel.tsx`
- `src/components/editor/Canvas.tsx`

---

## 2026-07-28 · 编辑器顶部"添加照片位"按钮功能（需求6）

### 背景
相册编辑器顶部工具栏缺少手动添加照片槽位的能力。用户需要在已有布局基础上追加额外的照片位，点击按钮即可在当前页生成一个新的照片槽。

### 改动详情

**核心架构：引入 extraSlots 机制支持动态槽位**

现有架构中，页面槽位来自静态模板（`TEMPLATES` 常量），`slotOverrides` 仅能覆盖已有槽位的几何位置（像素坐标），无法新增槽位。为支持动态添加槽位且保持百分比坐标（与模板槽位一致），新增 `extraSlots` 字段并改造 `resolveTemplate` 统一解析入口。

- `src/types/index.ts`：
  - `AlbumPage` 类型新增 `extraSlots?: SlotLayout[]` 字段，存储用户添加的额外槽位（百分比坐标 0-100，与模板槽位格式一致）。
  - `resolveTemplate()` 函数改造：解析静态模板或虚拟模板后，将 `page.extraSlots` 中不重复的槽位合并到返回的 `slots` 列表。这样 Canvas、缩略图、导出引擎等所有通过 `resolveTemplate` 获取槽位列表的消费方均自动支持新槽位，无需逐个修改。

**Store 层：addPhotoSlot 方法**

- `src/store/editorStore/types.ts`：`PageSlice` 接口新增 `addPhotoSlot: () => void` 方法签名。
- `src/store/editorStore/pageSlice.ts`：
  - 导入 `SlotLayout` 类型。
  - 实现 `addPhotoSlot` 方法：生成唯一 `slotId`（`slot-{timestamp}`），创建默认槽位（居中 35%/35%，尺寸 30%×30%），追加到 `page.extraSlots`；同步添加空 placement（`photoId: null`）和 `slotOrder`（新槽位在末尾=显示在最上层）；设置 `selectedSlotId` 自动选中新槽位并清空多选；操作后调用 `pushSnapshot` 记录历史（支持 Ctrl+Z 撤销）。

**UI 层：PageToolbar 按钮**

- `src/components/editor/PageToolbar.tsx`：
  - 从 store 获取 `addPhotoSlot` 方法。
  - 新增 `handleAddPhotoSlot` 处理函数：调用 `addPhotoSlot()` 并弹出成功 toast。
  - 在工具栏"照片重排"与"重置布局"之间添加"添加照片位"按钮，样式与"重置布局"等普通按钮一致（灰色文字 + hover 高亮），图标为照片框+加号的 SVG。

### 设计要点
- 新槽位使用百分比坐标（与模板槽位一致），页面尺寸变化时自动等比缩放。
- 新槽位无 `slotOverride` 时几何来自 `extraSlots` 百分比；用户拖拽后生成的 `slotOverride`（像素坐标）优先级更高，行为与模板槽位完全一致。
- `resolveTemplate` 合并时做 slotId 去重，避免重复渲染。

### 验证
- `npx tsc --noEmit` 类型检查通过，零错误。

### 涉及文件
- `src/types/index.ts`
- `src/store/editorStore/types.ts`
- `src/store/editorStore/pageSlice.ts`
- `src/components/editor/PageToolbar.tsx`

---

## 2026-07-28 · 工作区滚动条显示修复、重置缩放居中、Ctrl+C/V 选中元素复制粘贴

### 背景
两个编辑器交互问题需要修复：
1. **工作区滚动条不可见**：编辑器工作区放大超过视口后，上下/左右滚动条完全透明不可见，用户无法感知当前页面位置和缩放状态。
2. **重置缩放不居中**：点击缩放"重置"按钮时仅设置 zoom=1，页面不会回到视口中央，与最大化/最小化的居中行为不一致。
3. **Ctrl+C/V 仅复制整页**：快捷键始终复制/粘贴整个页面，无法复制选中的单个元素（照片槽、文字、便利贴）。

### 改动详情

**任务1：工作区滚动条显示 + 重置按钮居中**

- `src/hooks/useScrollbarVisibility.ts`：`onEnter` 回调增加水平方向溢出检查（`scrollWidth > clientWidth`），原逻辑仅检查垂直方向。画布工作区放大后横向溢出时滚动条也能正确显现。
- `src/components/editor/Canvas.tsx`：
  - 导入 `useScrollbarVisibility` hook，以 `externalRef: containerRef` 方式合并到已有 ref。
  - 两处 `data-canvas-container` 容器（空状态 + 正常渲染）添加 `ps-scroll` 工具类 + `canvasScroll.className` + `canvasScroll.handlers`，使滚动条在 hover / 滚动时优雅显现。
- `src/utils/sharedRender.ts`：新增 `computeCenteredScroll` 纯函数，按与 `useCanvasCentering` / `computeZoomedScroll` 完全一致的 Stage 尺寸口径计算居中 scrollLeft/scrollTop。
- `src/components/editor/BottomNav.tsx`：重置按钮 `onClick` 从单纯 `setCanvasZoom(1)` 改为先设 zoom 再通过 `computeCenteredScroll` 计算居中位置并在下一帧应用 `scrollLeft`/`scrollTop`，与最大化/最小化居中行为一致。

**任务2：Ctrl+C/V 选中元素复制粘贴**

- `src/store/editorStore/types.ts`：`SelectionSlice` 接口新增 `selectedTextId`、`selectedStickyId` 及对应 setter。
- `src/store/editorStore/selectionSlice.ts`：实现上述状态与 setter；`clearSelection` 同步清空文字/便利贴选中态。
- `src/components/editor/Canvas.tsx`：将 `selectedTextId`、`selectedStickyId` 从 `useState` 局部状态迁移到 `useEditorStore` 订阅，使 BottomNav 等全局快捷键可读取当前选中元素。`editingTextId` 保持局部状态不变。
- `src/components/editor/BottomNav.tsx`：
  - 引入 `ClipboardData` 联合类型（`page` / `placement` / `text` / `sticky`），替换原 `copiedPageRef`。
  - **Ctrl+C**：优先检查选中元素 —— 照片槽（含照片的 placement）→ 文字元素 → 便利贴 → 无选中则复制整页。通过 `useEditorStore.getState()` 读取实时选中状态，避免闭包过期。
  - **Ctrl+V**：根据剪贴板类型分流 —— 整页保持原有"插入新页并跳转"行为；照片槽粘贴到当前页第一个空照片位（跳过源槽位，携带全部编辑状态）；文字/便利贴粘贴为新元素（新 ID + 10mm 位置偏移，zIndex 由 addTextElement/addStickyNote 自动分配）。

### 验证
- `npx tsc --noEmit` 类型检查通过，零错误。
- `npx vite build` 构建成功，无新增告警。

### 涉及文件
- `src/hooks/useScrollbarVisibility.ts`
- `src/components/editor/Canvas.tsx`
- `src/utils/sharedRender.ts`
- `src/components/editor/BottomNav.tsx`
- `src/store/editorStore/types.ts`
- `src/store/editorStore/selectionSlice.ts`

---

## 2026-07-28 · 全局用户可见文案"模板"统一改为"布局"

### 背景
产品术语统一：将所有用户可见的"模板"文案改为"布局"，使 UI 用词与实际功能含义（页面照片排版布局）保持一致。仅修改用户可见的显示文字，不改动代码中的变量名、函数名、类型名等标识符。

### 改动详情
共修改 14 个文件，覆盖编辑器组件、首页组件、数据层、数据迁移工具与 i18n 国际化文件：

**编辑器组件（src/components/editor/）：**
- `LeftNav.tsx`：左侧导航"模板"→"布局"
- `BottomTabs.tsx`：底部标签"模板"→"布局"、"模板中心"→"布局中心"
- `TemplatePanel.tsx`：面板标题"模板"→"布局"
- `TemplateSwitchDialog.tsx`：弹窗标题"切换模板"→"切换布局"、说明文字"新模板"→"新布局"
- `PageToolbar.tsx`：Toast"已重置模板布局"→"已重置布局"、tooltip 与按钮文字"重置模板"→"重置布局"、清除照片 tooltip"模板和槽位保留"→"布局和槽位保留"
- `MarketPanel.tsx`：Toast"模板已应用"→"布局已应用"、标题"模板中心"→"布局中心"
- `OnboardingTour.tsx`：新手引导文案中"重置模板"→"重置布局"、"模板"→"布局"
- `Toolbar.tsx`：备份提示"自定义模板"→"自定义布局"

**首页组件（src/components/home/）：**
- `TemplateGallery.tsx`：11 处文案（模板库→布局库、创建模板→创建布局、删除模板→删除布局、编辑模板→编辑布局、创建新模板→创建新布局、该分类暂无模板→该分类暂无布局等）
- `CreateTemplateDialog.tsx`：弹窗标题"编辑模板/创建模板"→"编辑布局/创建布局"、占位符、保存按钮"保存模板"→"保存布局"、边距说明"模板内固定"→"布局内固定"、默认名称"未命名模板"→"未命名布局"等 7 处

**数据层（src/db/）：**
- `index.ts`：默认名称"未命名模板"→"未命名布局"（normalizeCustomTemplate 与 createCustomTemplate 各 1 处）

**数据迁移（src/utils/）：**
- `dataMigration.ts`：导出进度提示"打包模板"→"打包布局"、导入进度提示"恢复模板"→"恢复布局"、错误信息"模板"→"布局"

**i18n 国际化：**
- `zh-CN.json`：7 处（home.nav.templates、editor.toolbar.templates、editor.layout.templateSwitch、导出/导入确认提示中的"自定义模板"、导入策略说明中的"模板"、templatesAdded 统计文案）
- `en-US.json`：对应 6 处英文 Templates→Layouts / Template Switch→Layout Switch / custom templates→custom layouts / templates added→layouts added

### 注意事项
- 仅修改用户可见的显示文案，未改动任何变量名、函数名、类型名
- 代码注释中的"模板"保持不变（不影响用户）
- 代码逻辑完全不变，仅字符串内容替换

### 涉及文件
- `src/components/editor/LeftNav.tsx`
- `src/components/editor/BottomTabs.tsx`
- `src/components/editor/TemplatePanel.tsx`
- `src/components/editor/TemplateSwitchDialog.tsx`
- `src/components/editor/PageToolbar.tsx`
- `src/components/editor/MarketPanel.tsx`
- `src/components/editor/OnboardingTour.tsx`
- `src/components/editor/Toolbar.tsx`
- `src/components/home/TemplateGallery.tsx`
- `src/components/home/CreateTemplateDialog.tsx`
- `src/db/index.ts`
- `src/utils/dataMigration.ts`
- `src/i18n/locales/zh-CN.json`
- `src/i18n/locales/en-US.json`

---

## 2026-07-28 · NSIS 下载超时修复与 EXE 安装包构建成功

### 背景
Rust 编译已成功（`membook.exe` 9.75 MB），但 Tauri 打包 NSIS 安装程序时尝试从 GitHub 下载 `nsis-3.11.zip` 超时（`timeout: global`），导致构建失败。

### 根因分析
1. **NSIS 下载超时**：Tauri 在验证 NSIS 包后尝试从 `https://github.com/tauri-apps/binary-releases/releases/download/nsis-3.11/nsis-3.11.zip` 下载，中国大陆直连 GitHub 经常超时。
2. **TRAE 沙盒限制**：通过 TRAE IDE 运行 `npm run desktop:build` 时，`cargo.exe`（未签名二进制）被 Windows Defender Application Control (WDAC) 策略阻止创建 `.cargo-build-lock` 文件，报错 `拒绝访问 (os error 5)`。即使通过 `Start-Process` 启动子进程也受限。

### 修复措施
1. **设置 GitHub 镜像环境变量**：通过 `TAURI_BUNDLER_TOOLS_GITHUB_MIRROR=https://gh-proxy.com/https://github.com` 将 Tauri 的 GitHub 下载请求代理到国内镜像。已设为系统级环境变量（`Machine` 作用域），重启后持久生效。
2. **WMI 进程创建绕过沙盒**：使用 `Invoke-CimMethod -ClassName Win32_Process -MethodName Create` 在 TRAE 进程树之外创建独立 PowerShell 进程运行构建，成功绕过 WDAC 限制。
3. **更新 `build-exe.ps1`**：在构建步骤前自动设置镜像环境变量（若未设置），并添加 NSIS 缓存验证。更新故障排除提示。
4. **NSIS 缓存已就绪**：NSIS 3.11 及 `nsis_tauri_utils.dll` (v0.5.3, SHA1: 75197FEE3C6A814FE035788D1C34EAD39349B860) 已缓存在 `%LOCALAPPDATA%\tauri\NSIS\`，后续构建无需重新下载。

### 构建结果
- **安装包**：`src-tauri/target/release/bundle/nsis/MemBook_1.0.0_x64-setup.exe`
- **大小**：12.1 MB
- **SHA256**：`714012A7AA0AE7C599092AF077E7CB5322F2B348D8A520A497C7BCCDF20BE690`
- **构建耗时**：约 3 分钟（Rust 编译缓存命中，仅前端重建 + NSIS 打包）

### 后续构建方式
在**普通 PowerShell 窗口**（非 TRAE IDE 内）运行：
```powershell
cd "f:\N-编程\MenBook开发项目\MemBook"
Set-ExecutionPolicy Bypass -Scope Process -Force
.\build-exe.ps1
```
或直接运行：
```powershell
cd "f:\N-编程\MenBook开发项目\MemBook"
npm run desktop:build
```
> 注意：不要在 TRAE IDE 内运行构建，因为 TRAE 沙盒会阻止 cargo 创建锁文件。

### 涉及文件
- `build-exe.ps1`（已更新，添加 GitHub 镜像设置 + NSIS 缓存验证 + 故障排除提示）
- `开发日志.md`（本条目）
- 系统环境变量 `TAURI_BUNDLER_TOOLS_GITHUB_MIRROR`（已设为 Machine 级别）

---

## 2026-07-27 · Rust 工具链 STATUS_DLL_NOT_FOUND 根因定位与修复

### 背景
系统重装后，`rustc.exe` 和 `cargo.exe` 均返回退出码 -1073741515 (0xC0000135 = STATUS_DLL_NOT_FOUND)，无法构建 EXE 安装包。`rustfmt.exe` 和 `rustdoc.exe` 则正常运行。

### 根因分析
通过 PE 导入表解析、LoadLibraryEx 测试、manifest 提取等多轮诊断，定位到根本原因：

1. **Manifest 差异**：`rustc.exe` 和 `cargo.exe` 的 PE 资源段包含 manifest，其中有 `<activeCodePage>UTF-8</activeCodePage>` 设置；`rustfmt.exe` 和 `rustdoc.exe` 无资源段/manifest，因此不受影响。
2. **系统代码页冲突**：系统重装后 ACP=936 (GBK 中文)，UTF-8 Beta 功能未启用。Windows 加载器在处理 `activeCodePage>UTF-8` manifest 时，因系统不支持 UTF-8 代码页而导致进程创建失败。
3. **rustup.exe 损坏**：`.cargo\bin\rustup.exe` 为 0 字节，所有代理文件 (cargo.exe/rustc.exe 等) 为 0 字节的符号链接 (ReparsePoint)，指向已损坏的 rustup.exe。

### 修复措施
1. **启用 UTF-8 Beta**：修改注册表 `HKLM\SYSTEM\CurrentControlSet\Control\Nls\CodePage`，将 ACP/OEMCP/MACCP 设为 65001 (UTF-8)，需重启生效。
2. **创建重启后构建脚本** `after-reboot-build.ps1`：自动修复 rustup.exe、验证 Rust 工具链、安装 VS Build Tools、构建 EXE。
3. **保留 `.cargo/config.toml`**：已恢复标准 MSVC link.exe 配置（移除 rust-lld workaround）。

### 当前状态
- 注册表已修改，**系统需要重启**才能使 UTF-8 Beta 生效。
- 重启后运行 `after-reboot-build.ps1` 完成全部构建流程。

### 涉及文件
- `after-reboot-build.ps1`（新建，重启后一键构建脚本）
- `build-exe.ps1`（已更新，含 Rust 代理修复 + VS Build Tools 安装 + 预构建验证）
- `.cargo/config.toml`（已恢复标准 MSVC 配置）

---

## 2026-07-27 · EXE 安装包构建尝试与阻塞点

### 背景
用户要求构建 MemBook 桌面端 EXE 安装包。此前存在 TRAE 沙箱内无法调用系统 Rust/MSVC 工具链的问题，本轮尝试先走系统全局安装，再回退到 TRAE 内置 wrapper/MinGW 方案。

### 已尝试路径

#### 1. 系统全局依赖安装脚本 `build-exe.ps1`
- 重写 `build-exe.ps1` 为纯 ASCII 编码，避免中文 Windows 默认 GBK 解码导致语法错误。
- Node.js：通过 winget 成功安装 v24.18.0 / npm 11.16.0。
- Rust：通过 rustup-init.exe 成功安装 stable 1.97.1。
- VS Build Tools 2022：winget 检测到已安装但无法升级，回退到 `vs_BuildTools.exe` 静默安装；安装进程在后台运行约 107 分钟仍未完成，疑似挂起，最终终止进程。

#### 2. TRAE 沙箱 wrapper 方案
- 发现上一轮遗留的 wrapper：`rustc-wrapper.exe`、`cargo-wrapper.exe`、TRAE 内置 Node.js、MinGW（WinLibs）。
- 重命名 `build-env.ps1` → `build-env.bat` 并重写为 ASCII/CRLF 格式，修复 `npm` 作为批处理文件必须用 `call` 调用的问题。
- 直接调用 `cargo.exe`/`rustc.exe`（不经过 wrapper）可正常工作；`cargo-wrapper.exe` 在 `cargo metadata` 阶段触发 `git2` 的 `RefCell already borrowed` panic，因此弃用 wrapper。
- 在 `.cargo/config.toml` 中配置 `linker = "rust-lld"` 以绕过缺失的 MSVC `link.exe`。

#### 3. 当前阻塞点
- 前端 `vite build` 成功完成（988 modules transformed）。
- Rust/Tauri 编译阶段失败：`rust-lld` 无法找到 Windows SDK/MSVC 导入库：
  - `kernel32.lib`、`ntdll.lib`、`userenv.lib`、`ws2_32.lib`、`dbghelp.lib`
  - `/defaultlib:msvcrt`
- 系统中未找到上述 `.lib` 文件，VS Build Tools 未实际完成安装。

### 下一步选项
1. **完成 VS Build Tools / Windows SDK 安装**：系统安装后重新运行 `npm run desktop:build`。
2. **使用 MinGW + gnu target**：已安装 MinGW，但当前 Rust 工具链只有 `x86_64-pc-windows-msvc`，需补充 `x86_64-pc-windows-gnu` target 并切换 cargo target。
3. **下载便携 MSVC 库**：例如使用 `portable-msvc` 项目仅下载链接所需 `.lib`。

### 涉及文件
- `build-exe.ps1`（已重写为 ASCII 版本）
- `build-env.bat`（新建，基于上一轮 `build-env.ps1` 改造）
- `.cargo/config.toml`（新增 `linker = "rust-lld"`）

---

## 2026-07-26 · 智能排版引擎测试修复（hero 行高测试 + SpanGroup subRows 修正）

### 背景
延续前序 P0+P1+P2 智能排版引擎优化工作。验证阶段发现 `hero 行的行高应大于 standard 行（视觉权重正确）` 测试用例持续失败（139/140 通过）。该测试在 P2 集成前即已存在失败现象，属于"已完成未集成"阶段遗留的回归问题，需要根因分析并修复。

### 根因分析

通过 [debug-hero-test.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/debug-hero-test.ts)（临时调试脚本，已删除）打印实际 layoutRows 结构，定位到两个叠加因素导致测试失败：

**因素 1：SpanGroup 吞噬了所有顶层非 hero 行**

测试场景：15 张同尺寸横图（4000×3000）+ 同日，balanced 密度。
- 评分系统对全横图 + 同日 + 无 contentInfo 的场景，所有照片得分相同（score=4）
- `heroIds = scoredPhotos.filter(s => s.score >= bestScore)` 把全部 15 张照片都纳入 heroIds
- `detectSpanOpportunities` 在每张单照片行都满足 `isCandidate`（aspect < 1.8 且在 heroIds 中）
- 单照片行触发 SpanGroup，把后续 1-2 行吸收为 `subRows`，导致顶层只剩 hero 行 + SpanGroup
- 测试用 `r.type !== 'span'` 过滤后，`nonHeroRows` 为空 → `foundHeroVsNonHero` 永远 false

实际 Page 0 layoutRows（pattern='opening'）：
```
Row 0: tier=hero rowH=131.25 photos=2       ← 顶层 hero
Row 1: SPAN portraitPhotoId=photo-3 totalH=207.50 tier=detail
  subRow 0: tier=detail rowH=101.25 photos=1 ← 真正的非 hero 行被藏在 subRows 里
```

**因素 2：MAX_SINGLE_RATIO 上限放大单张行高**

[google-photos-layout.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/google-photos-layout.ts) 的 `computeRowForGroup` 为了避免单张超宽图把整行拉得太矮，引入了 `MAX_SINGLE_RATIO = 0.6` 上限：
```typescript
const cap = MAX_SINGLE_RATIO * aspectSum;
// 单张 aspect 1.33 → cap = 0.6 * 1.33 = 0.8 → aspect 被压缩到 0.8
// baseH = 180 / 0.8 = 225（原本应该是 180 / 1.33 = 135）
```
对于 1 张 4000×3000 的照片：
- 未启用上限：baseH = 135
- 启用上限：baseH = 225（被人为放大 1.67 倍）

Page 2（pattern='closing'）实际数据：
```
SPAN portraitPhotoId=photo-15 totalH=525.63 tier=standard
  subRow 0: tier=standard rowH=65.63 photos=2  ← 多张行，倍率正常
  subRow 1: tier=standard rowH=225.00 photos=1 ← 单张行被 MAX_SINGLE_RATIO 放大
Row 1: tier=hero rowH=124.69 photos=2          ← hero 行反而比单张 standard 矮
```

若直接比较 max hero (124.69) vs max non-hero (225.00)，1.2 倍率断言会失败——但这并非 tier 倍率错误，而是 MAX_SINGLE_RATIO 的设计副作用。

### 修复方案

更新 [google-photos-layout.test.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/google-photos-layout.test.ts) 中 `hero 行的行高应大于 standard 行` 测试：

1. **展开 SpanGroup 的 subRows**：把 span group 内部 subRows 的 `tier`/`rowHeight`/`photoIds` 一并纳入比较集——subRows 携带的是原始 `JustifiedRow` 信息（nextRow.rowHeight / nextNextRow.rowHeight），可独立比较，不应因被 span 吞噬而排除
2. **仅比较多照片行（photos >= 2）**：单张照片行受 `MAX_SINGLE_RATIO` 上限约束，aspect 被压缩导致行高被人为放大，无法反映 tier 倍率差异；多张行无此干扰，可直接验证 `getTierMultiplier` 的 hero > standard 关系
3. **保留 1.2 倍率断言**：在多张行场景下，hero 倍率（1.5-2.0）相对 standard 倍率（1.0）的比值 ≥ 1.5，1.2 阈值仍合理

```typescript
// 展开所有行：top-level 非 span 行 + span group 的 subRows
const allRows: { tier?: RowTier; rowHeight?: number; photos: number }[] = [];
for (const r of pageRows) {
  if (r.type === 'span') {
    for (const sr of r.subRows ?? []) {
      allRows.push({ tier: sr.tier, rowHeight: sr.rowHeight, photos: sr.photoIds?.length ?? 0 });
    }
  } else {
    allRows.push({ tier: r.tier, rowHeight: r.rowHeight, photos: r.photoIds?.length ?? 0 });
  }
}
// 仅比较多照片行（避免单张照片的 MAX_SINGLE_RATIO 上限干扰 tier 倍率比较）
const heroRows = allRows.filter(r => r.tier === 'hero' && r.rowHeight && r.photos >= 2);
const nonHeroRows = allRows.filter(r => r.tier !== 'hero' && r.rowHeight && r.photos >= 2);
```

同步把 `RowTier` 类型加入 import，避免 `tier?: RowTier` 推断为 `any` 失去类型保护。

### 验证

Page 1（pattern='alternate'）展开后：
- subRow 0: tier='standard', rowH=65.63, photos=2
- subRow 1: tier='hero', rowH=98.44, photos=2
- maxHero=98.44, maxNonHero=65.63, 比值 1.5 ≥ 1.2 ✓

- tsc --noEmit：0 errors
- vitest：140/140 通过（原 139 + 1 修复）
- Tauri 构建：NSIS + MSI 双格式安装包生成成功

### 附加修复：SmartLayoutView 类型错误

构建 `npm run tauri build` 阶段暴露出 3 个 TS 错误（`tsc --noEmit` 与 `tsc -b` 配置不同，后者更严格）：

[SmartLayoutView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/SmartLayoutView.tsx)
1. **未使用 import**：`googlePhotosLayout`、`computeSmartObjectPosition` 已不再直接调用（被 `generateMultipleLayouts` 和 `computeCssObjectPosition` 替代），删除以通过 `tsc -b` 的 `noUnusedLocals` 检查
2. **RefObject 类型错误**：`PageThumbCard` 的 `contentInfoCache` prop 期望 `Map<string, PhotoContentInfo>`，但 line 1488 传的是 `contentInfoCacheRef`（`RefObject<Map<...>>`），改为 `contentInfoCacheRef.current` 取出实际 Map 引用。该 bug 此前未被 vitest 覆盖（测试不渲染组件），运行时调用 `.get()` 会抛 "not a function"——已在构建前修复。

### 构建产物

| 安装包 | 路径 | 大小 |
|---|---|---|
| NSIS | `src-tauri/target/release/bundle/nsis/MemBook_1.0.0_x64-setup.exe` | 12.11 MB |
| MSI  | `src-tauri/target/release/bundle/msi/MemBook_1.0.0_x64_zh-CN.msi` | 15.62 MB |

构建耗时：前端 22.64s + Rust release 编译 3m 35s + 打包 ~30s。

### 经验沉淀

- 测试断言基于"实现细节"（顶层行结构）而非"语义"（tier 倍率关系）时，引擎内部架构变化（SpanGroup 吞噬行）会导致测试假阳性失败
- 集成测试若依赖具体几何数值，需对引擎内部约束（如 MAX_SINGLE_RATIO）的副作用有充分认知；当某行因约束被人为放大/缩小时，应排除该场景或在断言中体现差异
- SpanGroup 的 subRows 是合法的"行"语义载体，应作为公开 API 暴露 tier/rowHeight，不应仅靠顶层结构推断布局质量

---

## 2026-07-26 · P2 智能排版引擎集成（content-aware + 人脸检测 + 多版本择优）

### 背景
上一轮分析发现智能排版引擎深度优化的 P2 高级特性中，P2-1（content-aware.ts 内容感知模块）和 P2-2（generateMultipleLayouts 多版本生成 + 美学评分择优）虽代码完整但未集成进产品主流程，构成"已完成未集成"的死代码。用户要求执行优化，并在智能排版中增加人脸检测功能，使裁切时能感知主体位置避免裁到人脸。

### 集成内容

#### P2-1 集成：content-aware 模块（含人脸检测）
[SmartLayoutView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/SmartLayoutView.tsx)
- 安装依赖：`face-api.js@0.22.2`（按需动态 import，未加载模型时回退到同步主色分析）
- 新增 `contentInfoCache`：模块级 Map<photoId, PhotoContentInfo> 缓存，跨重渲染共享避免重复推理
- 新增 `precomputeContentInfoBatch`：进入智能编排时并发限流 6 路预计算所有照片 contentInfo，已缓存的照片跳过
- 新增 `computeCssObjectPosition`：从 contentInfo 计算 CSS object-position 百分比（与 computeSmartObjectPosition 算法对齐）
- `PageThumbCard` 新增 `contentInfoCache` prop，照片模式下用 `computeCssObjectPosition` 替代固定 `'center'`，把人脸焦点对齐槽位中心
- `memo` 比较函数加入 `contentInfoCache` 引用相等性检查
- 组件卸载时调用 `clearContentInfoCache()` 清空缓存避免泄漏
- 增量更新：`selectedPhotos` 变化时仅对未缓存照片触发预计算，已缓存照片不重复分析

[content-aware.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/content-aware.ts)
- 复用现有 `analyzePhotoWithFaces`（动态加载 face-api.js → TinyFaceDetector 模型 → 检测人脸 → 取最大人脸中心略上移 10% 作为焦点）
- face-api.js 未安装/模型未加载/检测失败时回退到 `analyzePhotoContent` 同步主色分析（焦点 0.5/0.5 居中），保证渐进增强

#### P2-2 集成：generateMultipleLayouts 多版本择优
[SmartLayoutView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/SmartLayoutView.tsx)
- 删除 `PREVIEW_SEEDS = [101, 202, 303]` 固定 seed 方案
- 删除 `previewConfigs` 中间层
- `previewResults` 改为直接调用 `generateMultipleLayouts(selectedPhotos, baseConfig, 5, 3)` 拿 Top-3：
  - 生成 5 个版本（每个用不同 seed `i*37+11` 注入每页 `seed + p*7`）
  - `evaluateLayoutQuality` 评分（模式多样性 +30 / 跨页 hero 位置变化 / 每页照片数合理性 / 平均页数）
  - 第一个版本 score ≥ 80 时提前结束（避免不必要的计算）
  - 按 score 降序取 Top-3 作为 A/B/C 三方案展示给用户
- 用户看到的 A/B/C 现在是评分 Top-3，而非随机 3 个，质量更稳定

### 工作流程
1. 用户进入智能编排 → 触发 `precomputeContentInfoBatch` 后台并发分析所有照片（含人脸检测）
2. 每张照片调用 `analyzePhotoWithFaces`：动态 import face-api.js → 加载 TinyFaceDetector 模型 → 检测人脸 → 取最大人脸中心略上移 10% 作为焦点
3. face-api.js 未安装/模型未加载/检测失败时回退到 `analyzePhotoContent`（同步主色分析，焦点居中）
4. contentInfo 写入模块级缓存 `Map<photoId, PhotoContentInfo>`
5. `generateMultipleLayouts` 生成 5 版排版 + 评分择优取 Top-3 作为 A/B/C 三方案
6. 用户切换 A/B/C 时 `PageThumbCard` 用 `computeCssObjectPosition` 计算每张照片的 `object-position`，把人脸焦点对齐槽位中心
7. 照片被 cover-fit 放大时焦点居中显示，避免裁切到人脸

### 渐进降级
- face-api.js 安装失败：模块仍可工作（回退到同步主色分析，焦点居中）
- 人脸检测模型未加载（如 `/models/face-detection/` 目录不存在）：回退到同步分析
- 单张照片检测异常：返回 `DEFAULT_CONTENT_INFO`（居中、无人脸），不影响其他照片
- contentInfo 未就绪时：`computeCssObjectPosition` 返回 `'50% 50%'`（居中），与原行为一致

### 验证
- tsc --noEmit：0 errors
- vitest：140/140 通过

### 注意事项
- 首次进入智能编排时 face-api.js 模型加载有 ~1-2s 延迟（动态 import + 模型下载），加载完成后会自动重新分析已缓存的照片
- 模型文件需放在 `public/models/face-detection/` 目录（tiny_face_detector_model-weights_manifest.json + .bin shard 文件），未放置时回退到同步分析
- 用户首次使用时浏览器会下载模型（约 190KB），后续从浏览器缓存加载

---

## 2026-07-26 · PhotoPanel 间隙与下拉弹窗 z-index 修复

### 背景
用户反馈上轮修复后仍有 UI 问题：1）智能编排按钮行与日期行之间的缝隙还比按钮上方间隙大 4px；2）点击「展开层级」下拉弹窗被年份 sticky 头遮挡。

### 修复内容

#### 1. 缩小按钮行与日期行之间的缝隙
[PhotoPanel.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoPanel.tsx)
- 问题：年份 sticky 头 `py-2`（8px 顶 + 8px 底），加上 Row2 底部 `py-2`（8px），下方总间隙 16px；上方间隙约 12px（上传区 marginBottom 4px + Row1 顶部 8px）
- 修复：年份 sticky 头 `py-2` → `pt-1 pb-2`（顶部 4px，底部 8px），下方间隙变为 12px，与上方一致
- 同步调整月份/日期 sticky 头的 top 值（年份高度减少 4px）：
  - 月份 `top-[34px]` → `top-[30px]`
  - 日期 `top-[62px]` → `top-[58px]`

#### 2. 展开层级下拉弹窗置顶
[PhotoPanel.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoPanel.tsx)
- 问题：PhotoFilterBar 外层 div 无 position/z-index，年份 sticky 头 `z-30` 会盖过下拉弹窗（`z-20`）
- 修复：
  - PhotoFilterBar 外层 div 加 `relative z-40 bg-[var(--color-surface)]`，在堆叠上下文中高于年份 sticky
  - 下拉弹窗 `z-20` → `z-50`，确保弹窗在 PhotoFilterBar 内部最顶层

### 验证
- tsc --noEmit：0 errors
- vitest：140/140 通过

### 智能排版引擎 P0+P1+P2 优化生效性分析

经代码审查，10 项优化中 **8 项真实生效**，**2 项已实现但未集成进产品主流程**：

| 优化点 | 状态 | 证据 |
|---|---|---|
| P0-1 页数计算死代码 | 生效 | `google-photos-layout.ts:755` 已正确赋值 |
| P0-2 recentPatterns LRU | 生效 | `google-photos-layout.ts:494-515` + 4 处 markPatternUsed 调用 |
| P0-3 竖图评分偏见 | 生效 | `google-photos-layout.ts:297-304` 改为奖励极端 aspect ratio |
| P0-4 行级交换保时间序 | 生效 | `google-photos-layout.ts:1027-1048` placeHeroesIntoGroups |
| P1-1 planCrossPageRhythm | 生效 | `google-photos-layout.ts:795-844`，主入口 1591 行调用 |
| P1-2 MAX_SINGLE_RATIO=0.6 | 生效 | `google-photos-layout.ts:951-980`，多处复用 |
| P1-3 尾页 <3 合并 | 生效 | `google-photos-layout.ts:766-784` |
| P2-1 content-aware.ts | **未生效** | 文件存在但无任何 importer，构成死代码 |
| P2-2 generateMultipleLayouts | **未生效** | UI 用 `PREVIEW_SEEDS` 自行实现 3 版预览，未调用择优函数 |
| P2-3 SpanGroup tier 传递 | 生效 | 接口 + detectSpanOpportunities + 三处序列化全链路保留 |

**未生效原因：**
- P2-1：`content-aware.ts` 模块完整实现但无任何文件 import，未集成进排版管线
- P2-2：`generateMultipleLayouts` + `evaluateLayoutQuality` 函数存在且测试通过，但 `SmartLayoutView.tsx` 用自己的 `PREVIEW_SEEDS = [101, 202, 303]` 直接调 `googlePhotosLayout` 三次，未走"5 选 3 择优"逻辑

**用户实际受益：** P0/P1/P2-3 共 8 项优化在智能编排时真实生效；P2-1（内容感知裁切）和 P2-2（美学评分择优）未进入产品体验。

---

## 2026-07-26 · 智能编排与照片列表 UI 修复

### 背景
用户反馈智能编排功能存在三个问题：1）照片/构图预览模式下页面缩略图不显示照片；2）密度/节奏滑块条与高级选项不一一对应，滑到智能区域无反应；3）编辑器照片列表中智能编排按钮行与日期行之间有大缝隙，露出照片。

### 修复内容

#### 1. 智能编排缩略图不显示照片
[SmartLayoutView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/SmartLayoutView.tsx)
- 问题：`generateThumbnail` 直接用 `photo.src` 加载图片，对 import 模式（blob URL 可能已过期）和 direct 模式（Tauri `asset://` 跨域）均不可靠，导致 Canvas 绘制失败或 `toDataURL` 抛 SecurityError
- 修复：新增 `resolvePhotoSrcForCanvas` 函数，复用 `gridThumbnailRenderer.ts` 的 URL 解析策略：
  - 优先从 IndexedDB 读取 `thumbBlobId`（256px），降低解码与内存开销
  - 回退 `photo.src`（仅限 `blob:`/`data:` 同源 URL）
  - direct 模式通过 `makeDirectPhotoUrl` + `readFileAsBlobUrl` 转为同源 blob URL，避免 Canvas 污染
- 跨域保护：`toDataURL` 包裹 try-catch，污染时返回 null 让占位色块兜底

#### 2. 密度/节奏滑块与高级选项不对齐
[SmartLayoutView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/SmartLayoutView.tsx)
- 问题：滑块 `step=1`（201 个位置）但只有 5 个选项，滑到智能区域（auto=0）只是一个点，难以精确命中；且 `handleRhythmSliderChange` 在无选中页时未清除单页节奏覆盖，导致全局节奏变化对已有覆盖的页面无反应
- 修复：
  - 滑块 `step` 改为 `50`，5 档吸附位置：`-100 / -50 / 0 / 50 / 100`
  - 重写 `sliderToDensity`/`sliderToRhythm`：用范围阈值（≤-75, ≤-25, <25, <75, else）映射到 5 个选项
  - 重写 `densityToSlider`/`rhythmToSlider`：返回 `-100/-50/0/50/100` 与吸附位置一一对应
  - `handleRhythmSliderChange` 在 `selectedPageIdx == null` 时补上 `clearAllRhythmOverrides()` + `setLayoutRecalcKey()`，确保全局节奏变化对所有页面生效

#### 3. 智能编排按钮行与日期行之间的缝隙
[PhotoPanel.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoPanel.tsx)
- 问题：照片列表滚动容器有 `py-2` 顶部 padding（8px），在 PhotoFilterBar 与年份 sticky 头之间形成空白缝隙；PhotoFilterBar 外层 div 缺少 `shrink-0`，flex 布局下可能被压缩
- 修复：
  - 滚动容器 `py-2` 改为 `pb-2`（移除顶部 padding），年份 sticky 头紧贴 PhotoFilterBar
  - PhotoFilterBar 外层 div 添加 `shrink-0`，防止 flex 压缩导致布局错位

### 验证
- tsc --noEmit：0 errors
- vitest：140/140 通过

---

## 2026-07-26 · 智能排版引擎深度优化（P0+P1+P2）

### 背景
用户反馈智能排版功能需要更好的算法和更好的视觉排版效果。经过深度分析，发现引擎存在多处可优化点：评分偏见、死代码 bug、跨页节奏单调、内容感知缺失等。本轮优化分 P0（关键 bug）、P1（视觉质量）、P2（高级特性）三个优先级全面升级。

### 优化内容

#### P0：关键 Bug 修复

**P0-1：页数计算死代码 bug**
[google-photos-layout.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/google-photos-layout.ts)
- 问题：`buildPageSpecs` 中 `Math.max(2, rem - 2)` 计算结果未赋值给 `count`，是空语句，导致尾页只剩 1 张照片时无法并入上一页
- 修复：`count = Math.max(2, rem - 2)` 正确赋值，当剩余照片减去本页后只剩 1 张时，缩小本页容量把那 1 张并入

**P0-2：recentPatterns 无界 Set 导致跨页去重失效**
- 问题：`recentPatterns` 是无界 Set，N 页后所有模式都被标记为"用过"，`fresh` 永远为空，跨页去重完全失效
- 修复：实现 LRU 窗口管理（`RECENT_PATTERNS_MAX = 5`），`markPatternUsed` 超出窗口时淘汰最早的模式，旧模式可再次使用

**P0-3：竖图评分偏见**
- 问题：aspect ratio 评分只奖励横图（aspect >= 1.5），竖图（aspect < 0.7）永远得 0 分，无法成为 hero 候选
- 修复：改为奖励极端 aspect ratio（横竖都奖励），`aspect >= 2.0 || aspect < 0.5 → +3`，`0.5 ≤ aspect ≤ 2.0 → +1`

**P0-4：页内时间顺序被破坏**
- 问题：`placeHeroesIntoGroups` 把高分照片从原行移到 hero 行，破坏同页照片按时间排列
- 修复：改为行级交换（swap 整行）而非移动单张照片，保留页内时间顺序

#### P1：视觉质量优化

**P1-1：跨页节奏规划（planCrossPageRhythm）**
- 新增 `planCrossPageRhythm` 函数：连续 2 页 heroPlacement 相同时，把当前页替换为 heroPlacement 不同的等价 pattern
- 按 heroPlacement 类别维护切换池（first/last/center/first-and-last/first-and-second/none），保证连续 3 页不使用相同 pattern

**P1-2：行视觉权重优化**
- 新增 `MAX_SINGLE_RATIO = 0.6`：单张照片宽度上限为行宽 60%，防止全景图独占行高
- `computeRowForGroup` 中收缩超占比照片：`aspect > cap(0.6 × aspectSum)` 时收缩到 cap

**P1-3：尾页照片智能合并**
- 尾页照片 < 3 张时合并到上一页，避免视觉单薄
- 合并后重新计算上一页 LayoutParams（保留原 rhythm/density/seed 避免引入新随机性）

#### P2：高级特性

**P2-1：内容感知裁切模块**
[content-aware.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/content-aware.ts) 新建
- `analyzePhotoContent`：同步轻量分析（降采样到 32×32 提取主色 + HSV 饱和度）
- `analyzePhotoWithFaces`：异步人脸检测（动态加载 face-api.js，未安装时回退到同步分析）
- `computeSmartObjectPosition`：替代 CSS `object-position: center` 的智能偏移，让焦点对齐槽位中心
- `isColorConflict`：跨页色彩冲突检测（饱和度 > 0.4 且色相差 < 25°）
- 设计原则：渐进增强（无 contentInfo 时回退到居中）、本地优先（不上云）、可缓存

**P2-2：多版本生成 + 美学择优（generateMultipleLayouts）**
- 跑 N 个 seed 生成多个版本，用 `evaluateLayoutQuality` 启发式评分挑出 Top-K
- 评分维度：模式多样性（+30 上限）、跨页 hero 位置变化（连续相同扣 8）、每页照片数分布（3-6 张 +2，极端 -3）、平均页数合理性（4-5 张 +10）
- 第一遍获取页数，后续版本为每页注入不同 seed 影响 `selectTierPattern` 的 idx，产生真实版本差异

**P2-3：SpanGroup tier 传递**
- `SpanGroup` 接口新增 `tier: RowTier` 字段，subRows 也增加 tier
- `detectSpanOpportunities` 创建 span 时捕获原始行的 tier
- `layoutRows` 序列化时传递 tier（googlePhotosLayout + layoutSinglePage + reflow 三处）

### 测试覆盖
[google-photos-layout.test.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/google-photos-layout.test.ts) 新增 6 个测试套件、13 个用例：
- **视觉质量与节奏**：连续 3 页不重复 pattern、hero 行高 ≥ standard × 1.2、LRU 窗口可复用
- **边界场景**：尾页 ≥ 3 张、N=6 无 1 张尾页、全竖图友好 pattern、含全景图正常排版、单张不崩溃
- **微调参数**：biasX/biasY 极端值不破坏安全区、手动锁定 tierPattern 被尊重
- **评分公平性**：竖图也能成为 hero 候选
- **多版本生成**：generateMultipleLayouts 返回 Top-K 按 score 降序

### 验证修复
- `face-api.js` 未安装时 TS 模块解析失败 → 定义本地最小类型 + `@ts-ignore` 跳过可选依赖
- `generateMultipleLayouts` 中 `pageOverrides` 未使用 → 改为两遍策略：第一遍获取页数，后续版本注入 seed
- 测试 `require('./google-photos-layout')` ESM 不支持 → 改为顶层 import
- SpanGroup tier 未传递到 layoutRows → span group 和 subRows 都增加 tier 字段
- hero/standard 行高比较被 span group 干扰 → 测试改为比较非 span 行的 hero vs 非 hero

### 验证
- tsc --noEmit：0 errors
- vitest：140/140 通过（原 127 + 新增 13）
- Rust release 编译：2m 55s
- 安装包：
  - NSIS：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`
  - MSI：`src-tauri\target\release\bundle\msi\MemBook_1.0.0_x64_zh-CN.msi`

---

## 2026-07-25 · 大图查看加载彻底修复 / 照片重排按槽位排序 / 照片列表年月日全 sticky

### 背景
用户反馈上一轮修复后仍存在 3 个问题：
1. 大图查看模式仍有部分照片加载失败
2. 照片重排面板的照片顺序与页面槽位不对应：布局切换/角度切换后，槽位位置变化但面板顺序仍按原 placement 数组顺序，用户无法对应拖拽
3. 照片列表向下滑动时年份出现"阴影底色"（frosted glass）效果，不美观；且只有年份 sticky，希望月份和日也固定展示

### 优化内容

#### 1. 大图查看加载失败的根因修复
**根因**：[usePhotoSrc.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/hooks/usePhotoSrc.ts) preview/full 档直接返回 `photo.src`，但 `photo.src` 可能是被 LRU 回收的失效 blob URL：
- EditorView 启动时通过 `readPhotoFromDB(previewId)` 创建 refCount=0 的 blob URL 存入 `photo.src`
- LRU 缓存（容量 200）淘汰该 blobId 后，10s 宽限期后 URL 被 revoke
- `usePhotoSrc` 仍返回失效 URL → `<img>` 加载失败 → onError 触发 handleError
- handleError 用 `readPhotoFromDB` 重建，但新 URL 也是 refCount=0，可能在 img 加载完成前再次被回收

**修复**：
1. [import-store.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage/import-store.ts) 新增 `isBlobUrlAlive(url)` 辅助函数：检查 URL 是否仍在 blobUrlRegistry 或 pendingRevocation 中
2. [usePhotoSrc.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/hooks/usePhotoSrc.ts) preview/full 档逻辑：
   - `useState` 初始值仅在 `isBlobUrlAlive(photo.src)` 时使用，避免初始渲染显示失效 URL
   - resolve 时校验 `photo.src && isBlobUrlAlive(photo.src)`，失效则走 `acquirePhotoUrl(blobId)` 获取 refCounted URL（不会被 LRU 回收）
   - thumb 档 fallback 也加上 `isBlobUrlAlive` 校验
3. [storage-engine.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage-engine.ts) 统一导出 `acquirePhotoUrl / releasePhotoUrl / isBlobUrlAlive`

#### 2. 照片重排按槽位视觉位置排序
[PhotoReorderDialog.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoReorderDialog.tsx)

**问题**：原 `filledItems` 按 `page.placements` 数组顺序，布局切换/角度切换后槽位坐标变化但顺序不变，用户在面板里看到的照片顺序与页面上槽位顺序不对应。

**修复**：
1. 新增 `getSlotVisualPos(slotId, page, template, albumSize)` 工具函数：
   - Google Photos 动态布局页面：从 `slotOverrides[slotId]` 读像素坐标，除以画布像素尺寸转百分比
   - 静态模板：直接读 `template.slots[].x/y` 百分比
2. `filledItems` useMemo 中按视觉位置排序：先按 y（顶部→底部，0.5% 容差视作同一行），再按 x（左→右）
3. 布局切换/角度切换后 `slotOverrides` / `template.slots` 坐标变化 → `filledItems` 自动重新排序，面板顺序与页面照片位置一一对应

#### 3. 照片列表年月日全 sticky + 去除阴影底色
[PhotoPanel.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoPanel.tsx)

**问题**：
- 年份 sticky header 用 `bg-[var(--color-surface)]/95 backdrop-blur-sm` → 滚动时出现 frosted glass 阴影底色
- 只有年份 sticky，月份和日不 sticky，滚动时看不到当前所在月份/日期

**修复**：嵌套 sticky header，三级都固定展示：
1. **年份** `sticky top-0 z-30`：去掉 `/95` 透明度和 `backdrop-blur-sm`，改为 `bg-[var(--color-surface)]`（实色无 frosted）
2. **月份** `sticky top-[34px] z-20`：紧贴年份下方，实色背景
3. **日期** `sticky top-[62px] z-10`：紧贴月份下方，实色背景

效果：滚动时三级 header 依次堆叠在顶部，用户始终能看到当前所在的「年 / 月 / 日」，且无 frosted 阴影底色。

### 验证
- tsc --noEmit：0 errors
- vitest：127/127 通过
- Rust release 编译：2m 25s
- 安装包：
  - NSIS：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`
  - MSI：`src-tauri\target\release\bundle\msi\MemBook_1.0.0_x64_zh-CN.msi`

---

## 2026-07-25 · 大图查看/照片重排/返回保存确认/工具栏交互修复

### 背景
用户反馈上一轮优化后存在 5 个问题：
1. 双击照片进入大图查看模式，照片显示加载失败
2. 照片重排功能：①照片加载失败 ②拖拽交换后松开鼠标仍处于拖拽状态 ③交换后照片槽漏白，未重新铺满
3. 编辑器左上角 logo 返回主页时无保存提醒，直接退出
4. 编辑器左上角页面按钮与撤销/重做按钮间竖线不够明显
5. 顶部悬浮工具栏"清除照片"按钮位置不合理，应放在"删除此页"左侧

### 优化内容

#### 1. PhotoPreview 大图查看加载失败修复
[PhotoPreview.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoPreview.tsx)
- 根因：`PhotoFullImg` 子组件直接用 `photo.src` 加载，photo.src 可能是 asset:// 或已失效的 blob URL
- 修复：改用 `usePhotoSrc(photo, { level: 'preview' })` hook，走 LOD 解析链（photo.src → previewBlobId → readPhotoFromDB → direct fallback），并保留 `onError` 兜底重建逻辑
- 同时简化结构：transform 直接应用到 `<img>` 元素，避免嵌套 transform 容器导致的渲染问题

#### 2. PhotoReorderDialog 照片重排三连修
[PhotoReorderDialog.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoReorderDialog.tsx)

**2-1 照片加载失败**
- 根因：直接用 `photo?.src` 加载缩略图，asset:// URL 在 Tauri WebView2 中加载不稳定
- 修复：抽取 `ReorderThumbnail` 和 `DragGhostThumbnail` 子组件，分别用 `usePhotoSrc(photo, { level: 'thumb' })` 加载，走 blobUrlCache → IndexedDB 解析链
  - thumb 档（256px）降低内存占用
  - 拖拽 ghost 通过 blobUrlCache 即时取图，无加载延迟

**2-2 拖拽松开后仍处于拖拽状态**
- 根因：`useEffect` 依赖 `[dragIndex, currentPageIndex, filledItems, swapPagePhotoPlacements]`，`filledItems` 每次渲染都重建数组 → mousemove 中 `setDragPos` 触发重渲染 → effect 反复注销/注册 mousemove/mouseup 监听 → 监听在 mouseup 前被移除，拖拽状态未清理
- 修复：
  1. `filledItems` / `photoMap` 用 `useMemo` 包裹，依赖 page.placements / allPhotos 引用，渲染期间不再重建
  2. 新增 `filledItemsRef` / `currentPageIndexRef` / `swapRef` 三个 ref 镜像最新值
  3. `useEffect` 依赖仅保留 `[dragIndex]`，mousemove/mouseup 回调内通过 ref 读取最新值，避免监听反复注册

**2-3 交换后照片槽漏白**
- 根因：[placementSlice.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/store/editorStore/placementSlice.ts) `swapPagePhotoPlacements` 将 `panX/panY/panScale` 也跟着照片交换，但这些是槽位相对坐标，跨槽位迁移后无意义，导致照片在新槽位中偏移出可视区域 → 漏白
- 修复：`photoStateKeys` 移除 `panX/panY/panScale`，交换后显式重置为 `undefined`，由渲染层按新槽位尺寸回退到居中 cover-fit，确保照片铺满槽位不漏白
- 保留 `panRotation`（旋转角度有意义）和 `filter/adjustments/crop/flipH/flipV`（非位置属性）

#### 3. 左上角 logo 返回主页保存确认 Modal
[EditorView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/EditorView.tsx)
- 旧逻辑：点击 logo 直接保存并返回主页，无确认步骤
- 新逻辑：点击 logo → 弹出 `Modal` 确认弹窗，三个操作：
  - **保存并返回**（主按钮，品牌色）：await 保存项目+照片 → 清理缓存 → 返回主页，带 loading 状态
  - **不保存返回**（次按钮）：直接清理缓存 → 返回主页，丢弃未保存更改
  - **继续编辑**（取消）：关闭弹窗，留在编辑器
- 参考 Figma / Notion / VSCode 业内常用设计，弹窗内含警示图标 + 提示文案
- `handleBack` 改为 `useCallback` 只负责打开弹窗，实际保存/返回逻辑下沉到 `executeSaveAndBack` / `executeDiscardAndBack`

#### 4. 页面按钮与撤销/重做间竖线更明显
[Toolbar.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/Toolbar.tsx)
- 旧样式：`bg-[var(--color-border-light)]/60`（浅色 + 60% 透明度，几乎不可见）
- 新样式：`bg-[var(--color-gray-300)]`（实色，无透明度，分隔更清晰）

#### 5. 顶部悬浮工具栏"清除照片"按钮位置调整
[PageToolbar.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PageToolbar.tsx)
- 旧顺序：清除照片 → 排版变化 → 布局切换 → 照片重排 → 重置模板 → 删除此页
- 新顺序：排版变化 → 布局切换 → 照片重排 → 重置模板 → 清除照片 → 删除此页
- 清除照片与删除此页都属于"破坏性操作"，放在一起更符合分组逻辑

### 验证
- tsc --noEmit：0 errors
- vitest：127/127 通过
- Rust release 编译：2m 15s
- 安装包：
  - NSIS：`src-tauri\target\release\bundle\nsis\MemBook_1.0.0_x64-setup.exe`
  - MSI：`src-tauri\target\release\bundle\msi\MemBook_1.0.0_x64_zh-CN.msi`

---

## 2026-07-25 · 照片大图查看模式优化（PhotoPreview 重构）

### 背景
用户反馈编辑器-照片列表-双击进入照片大图查看模式（[PhotoPreview.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoPreview.tsx)）存在以下不足：
1. 关闭按钮在右上角、左右切换按钮在两侧中部，但都嵌套在拖拽容器内，图片变换时位置不固定
2. 不支持滚轮缩放
3. 不支持旋转
4. 缺少基础的图片查看交互（拖拽平移、双击缩放、键盘快捷键）

### 优化内容

#### 1. 按钮位置固定（脱离图片变换层）
- 旧结构：所有按钮嵌套在 `useDraggable` 的拖拽容器内，按钮随容器 transform 移动
- 新结构：按钮直接挂在全屏容器（`fixed inset-0`）下，与图片 transform 层分离
  - **关闭按钮**：`top-6 left-1/2 -translate-x-1/2`（顶部居中，按用户要求）
  - **上一张/下一张**：`left-5 top-1/2 -translate-y-1/2` / `right-5 top-1/2 -translate-y-1/2`（左右两侧垂直居中，符合主流图片查看器布局：微信/macOS Preview/Windows Photos/Google Photos）
  - **底部工具栏**：`bottom-6 left-1/2 -translate-x-1/2`（底部居中圆角胶囊）
- 移除了 `useDraggable`（原本仅做装饰性拖拽，对图片无实际效果）

#### 2. 微信风格滚轮缩放
[PhotoPreview.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PhotoPreview.tsx) `applyZoom`：
- **放大时以鼠标位置为原点**，缩放中心跟随鼠标
- **缩小时以画面中心为原点**（微信图片查看器同款行为）
- 缩放范围 1x ~ 5x，步长 0.15
- 使用 [useWheel](file:///g:/WorkBuddy/MemBook/membook-backup/src/hooks/useWheel.ts) hook 绑定原生非 passive wheel 事件，阻止默认滚动

**数学推导**（`transform-origin: center`，等价于"图像中心 = 视口中心 + pan"）：
```
new_pan = pan * r + d * (1 - r)
其中 r = new_zoom / old_zoom, d = 鼠标到视口中心的偏移
```
旋转不影响该公式（旋转绕图像中心，pan 调整与旋转无关）。

#### 3. 照片角度旋转
- 向左/向右旋转按钮，每次 90°
- 通过 `transform: rotate(${rotation}deg)` 实现
- 旋转状态在切换照片时自动重置

#### 4. 拖拽平移（仅放大时启用）
- `zoom > 1` 时启用鼠标拖拽平移
- 拖拽时禁用 transition（即时反馈），松开后恢复 transition（平滑动画）
- 拖拽阈值 3px，避免误触
- 拖拽过程中的 click 不触发关闭（通过 `dragRef.moved` 标志区分）

#### 5. 双击切换缩放
- 双击图片：`zoom > 1` 时重置视图；`zoom = 1` 时放大到 2x（以双击点为原点）

#### 6. 键盘快捷键扩展
| 按键 | 功能 |
|------|------|
| `Esc` | 关闭 |
| `←` / `→` | 上一张 / 下一张 |
| `+` / `=` | 放大 |
| `-` | 缩小 |
| `0` | 重置视图 |
| `R` | 向右旋转 90° |

#### 7. 底部工具栏（信息 + 控件合并）
原底部独立信息栏合并进工具栏，单行布局：
```
[名称 · 尺寸 · 序号] | [缩小] [缩放%] [放大] [重置] | [左旋] [右旋]
```
- 名称支持 truncate + title 提示
- 缩放百分比实时显示
- 缩小/放大按钮在边界时禁用并降透明度
- 重置按钮在初始状态时禁用

### 状态管理稳定性
为避免快速连续 wheel 事件读到旧 state（React 批量更新导致的时序问题），使用 ref 镜像 zoom/pan：
- `zoomRef` / `panRef` 同步保存最新值
- `setZoom` / `setPan` 是稳定 setter（`[]` deps），同时更新 ref 和 state
- `applyZoom` / `handleWheel` / `handleMouseDown` 均为稳定回调，避免 listener 频繁重绑

### 切换照片时自动重置
`useEffect([index])` 重置 zoom/pan/rotation 到初始值，避免上一张的缩放/旋转状态污染下一张。

### 验证
- tsc --noEmit：0 错误
- vitest：127/127 通过

---

## 2026-07-25 · 打印弹窗预览性能优化 + 漂移修复

### 背景
用户反馈两个问题：
1. 页数很多时打印预览加载很慢，需优化图片加载性能（缩略图 + 滚动加载）
2. 打印弹窗首次打开关闭时窗口漂移到右下角，未正常关闭

### 根因

#### 问题 1：预览加载慢
- [PrintDialog.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PrintDialog.tsx) 用 `generatePrintPreviews` 一次性返回所有页 dataUrl，全部 setState 后才显示
- 预览 DPI=96（页面 794×1063px），渲染慢
- 无虚拟滚动，所有页同时 `.map()` 渲染到 DOM

#### 问题 2：弹窗漂移
对比其他弹窗发现 PrintDialog 的定位写法错误：
- [ExportDialog.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/ExportDialog.tsx) / [PageSettings.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PageSettings.tsx)：`className="absolute"` + `style={{ left: drag.pos.x || '50%', top: drag.pos.y || '50%', transform: 'translate(-50%, -50%)' }}`
- PrintDialog（错误）：`className="relative"` + `typeof drag.pos.x === 'number'` 判断（0 也是 number，初始就跳过居中分支，且 left/top 对 relative 元素只是偏移量）

### 修复

#### 1. 预览 DPI 96 → 48（缩略图，渲染速度 ~4x）
[printEngine.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/printEngine.ts) `PREVIEW_DPI` 从 96 降到 48。预览图宽度 360px，48 DPI 像素 397×531 足够清晰。

#### 2. 新增流式生成 API `generatePrintPreviewsStream`
[printEngine.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/printEngine.ts)：
- `renderPagesForPrint` 新增 `onPage?: (pageIndex, dataUrl) => void` 回调，每生成一页立即触发
- 新增 `generatePrintPreviewsStream(options, onPage, onProgress)` 公共 API
- 内部仍用 `SlidingPhotoCache` 控制内存峰值

#### 3. PrintDialog 流式增量渲染
[PrintDialog.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PrintDialog.tsx)：
- 改用 `generatePrintPreviewsStream`，每页生成后追加到 `pendingUrlsRef`
- 用 `requestAnimationFrame` 批量 flush 到 state（同一帧内多次 onPage 合并为一次 re-render）
- 头部显示"生成中 X/Y 页"实时进度

#### 4. 虚拟滚动（只渲染可见页 + 缓冲页）
[PrintDialog.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PrintDialog.tsx)：
- 根据相册尺寸计算每页槽位高度 `slotHeight = previewHeight + gap`
- `handleScroll` 根据 scrollTop/clientHeight 计算可见范围 `[start, end]`，前后各 3 页缓冲
- 占位 div 保持总高度（滚动条正确），每页 absolute 定位到 `top: idx * slotHeight`
- 未生成的页显示骨架屏（`animate-pulse`）
- 8px 阈值避免微小滚动触发频繁 setState

#### 5. 弹窗定位修复
[PrintDialog.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/PrintDialog.tsx) 弹窗根 div：
- `className`：`relative` → `absolute`（与 ExportDialog/PageSettings 一致）
- `style`：删除 `typeof` 判断，改为 `left: drag.pos.x || '50%', top: drag.pos.y || '50%', transform: 'translate(-50%, -50%)'`

### 效果

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 100 页预览 | 全部生成后才显示（~10s 等待） | 前几页 0.1s 内显示，后续流式追加 |
| 500 页 DOM | 500 个 img 节点同时存在 | 仅 ~15 个可见页 + 6 个缓冲页 |
| 单页渲染 | 96 DPI 794×1063px | 48 DPI 397×531px（~4x 更快） |
| 弹窗拖拽 | 漂移到右下角（relative + 错误判断） | 正确居中 + 拖拽（absolute + translate -50%） |

### 验证
- tsc --noEmit：0 错误
- vitest：127/127 通过

---

## 2026-07-25 · 清理 P2 死代码并构建 EXE 安装包

### 背景
执行 `npm run desktop:build` 构建安装包时，tsc 报错 `TS6133: 'importPhotoThumbOnly' is declared but its value is never read`。这是上一轮"P0-2 删除 P2 后台任务"后遗留的死代码：`importPhotoThumbOnly` 及其依赖 `compressImageToThumbOnly` 不再被任何业务代码调用，但仍保留在 import/re-export 中。

### 修复
彻底删除 P2 死代码链（3 个文件，5 处修改）：

1. [storage-engine.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage-engine.ts)：
   - 移除 re-export 中的 `importPhotoThumbOnly`
   - 移除 import 中的 `importPhotoThumbOnly`

2. [import-store.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage/import-store.ts)：
   - 移除 import 中的 `compressImageToThumbOnly`
   - 删除 `importPhotoThumbOnly` 函数定义（含注释）

3. [image-compressor.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage/image-compressor.ts)：
   - 删除 `compressImageToThumbOnly` 函数定义（含注释）

### 验证
- tsc --noEmit：0 错误
- vitest：127/127 通过
- `npm run desktop:build`：构建成功，生成 2 个安装包：
  - MSI：`src-tauri/target/release/bundle/msi/MemBook_1.0.0_x64_zh-CN.msi`
  - NSIS EXE：`src-tauri/target/release/bundle/nsis/MemBook_1.0.0_x64-setup.exe`
- Rust release 编译耗时 2m 55s

---

## 2026-07-25 · direct 模式原文件反复读取根因修复（无感知释放内存）

### 背景
前几轮修复（terminateWorkerPool、clearBitmapCache、Canvas Stage 销毁、store 清空、acquire/release 配对、readFile+Blob 替代 fetch(asset://)）后，用户反馈 direct 模式导入 500 张照片完成后内存仍 1.7GB，必须重启应用才释放。深入排查发现真正的根因不是"无法释放"，而是"反复读取原文件"。

### 根因（前几轮未触达）
**direct 模式即使已有 previewBlobId（P2 已完成），项目加载时仍走 makeDirectPhotoUrl 生成 asset:// URL，完全不检查 previewBlobId**：

1. [EditorView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/EditorView.tsx) 项目加载时，direct 模式总是 `makeDirectPhotoUrl(p)` 生成 asset:// URL，不检查 previewBlobId
2. [HomeView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/HomeView.tsx) handleOpenProject 同样总是用 asset://
3. 画布 `loadCachedImage` 对 asset:// URL 执行 `readFile` 原文件（5-10MB）+ `createImageBitmap` 解码（5000x3000 = 60MB 位图峰值）
4. **P2 后台生成的 preview blob 白做了**——从未被使用
5. 原文件在项目生命周期内被反复读取，WebView2 缓存持续累积

此外，旧的两阶段压缩方案有冗余 IO：
- Phase 1 只生成 thumb(256px)，photo.src = asset:// 原文件
- Phase 2 后台再 readFile 原文件生成 preview(1200px)
- 原文件被读两次（Phase 1 解码 + Phase 2 readFile）

### 修复

#### P0-1：EditorView + HomeView 加载时优先用 previewBlobId
[EditorView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/EditorView.tsx) + [HomeView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/HomeView.tsx) direct 模式照片恢复逻辑：
- 之前：`if (storageMode === 'direct' && hasDirect) url = makeDirectPhotoUrl(p)` → 总是读原文件
- 现在：`if (previewBlobId) url = readPhotoFromDB(previewBlobId); if (!url && hasDirect) url = makeDirectPhotoUrl(p)` → 优先用 1200px preview blob，无才回退 asset://

#### P0-2：Phase 1 合并生成 thumb + preview，删除 P2 后台任务
[import-store.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage/import-store.ts) 新增 `importPhotoThumbAndPreview` 函数：
- 一次解码原文件，同时生成 thumb(256px) + preview(1200px)
- 复用已有的 `compressImageToThumbAndPreview`（原本用于 import 模式）
- 返回 previewUrl，photo.src 直接是 preview blob URL，无需 asset:// 临时阶段

[storage-engine.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage-engine.ts) `processOneFileInner` direct 模式分支：
- 之前：`importPhotoThumbOnly` 只生成 thumb，photo.src = asset:// 原文件
- 现在：`importPhotoThumbAndPreview` 生成 thumb + preview，photo.src = preview blob URL

[usePhotoImport.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/hooks/usePhotoImport.ts) 删除 P2 后台任务：
- 移除 `runWithConcurrency(directPhotos, 2, ...)` 整段 P2 生成逻辑
- 移除 `generatePreviewForDirectPhoto`/`invalidateBlobUrlCache`/`deletePhotoBlob`/`evictDirectPhotoUrl`/`imageCache` 等不再使用的导入
- 导入完成后直接 `terminateWorkerPool()`（不再需要等 P2 完成）

### 效果
| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 导入 500 张 | 1.7GB（原文件累积） | ~600MB（Phase 1 峰值）→ ~400MB（完成后） |
| 打开项目 | 每张照片读原文件 5-10MB | 用 IndexedDB preview blob（200-500KB） |
| 画布滚动 | readFile 原文件 → 60MB 位图峰值 | readPhotoFromDB → 5.5MB 位图 |
| WebView2 缓存 | 持续累积 asset:// 解码位图 | 不再有 asset:// 加载 |

### 用户感知
- 导入速度：Phase 1 稍慢（2-3x，因为同时生成 thumb + preview），但无 P2 后台任务拖累，总耗时更低
- 画布滚动：更快（preview blob 比 readFile 原文件快得多）
- 内存：导入完成后自然下降，无需重载或重启

### 验证
- tsc --noEmit 通过
- vitest 127/127 通过

### 跳过的方案
P0-3 Tauri 原生降采样命令（Rust 端用 image crate 做降采样）：P0-1/2 已消除原文件读取路径，preview blob 的 createImageBitmap 峰值仅 5.5MB（可接受），Rust 降采样边际收益不大但改造成本高，暂缓。

---

## 2026-07-25 · 导入完成后内存 1.7GB 深度优化（进程级缓存绕过）

### 背景
前几轮修复（terminateWorkerPool、clearBitmapCache、Canvas Stage 销毁、store 清空、acquire/release 配对）后，用户反馈 direct 模式导入 500 张照片完成后内存仍 1.7GB，必须重启应用才释放。

### 根因分析（深度排查）

经深入排查，JS 可控内存仅 ~290MB，1.7GB 的主要来源是 **JS 不可控的进程级缓存**：

| 来源 | 估算大小 | 说明 |
|------|----------|------|
| **WebView2 decoded image cache** | 500MB-1GB | `fetch(asset://)` 触发 WebView2 缓存原文件解码位图（5000x3000 × 4 = 60MB/张），500 张累积，无法从 JS 侧清理 |
| **V8 堆高位水位** | 200-400MB | 500 次 `readFile` 产生 500 个 5-10MB Uint8Array，进入 large-object-space，GC 后 V8 不主动归还 OS |
| **blobUrlRegistry 待回收队列** | 132MB | 500 个 preview blob URL 创建后，440 条被 LRU 淘汰，60s 延迟回收窗口内常驻 |

**关键发现**：`bitmapCache`（容量 100，理论 500MB-1GB）是**死代码**，`loadImageBitmap` 从未被外部调用，clearBitmapCache 清理的是空缓存。

**fetch(asset://) 是 WebView2 缓存膨胀的直接原因**：每次 `fetch(asset://)` 不仅产生 JS 可见的 5-10MB 临时 blob，还触发 WebView2 内部的 HTTP 缓存和 decoded image cache，后者无法从 JS 侧清理。

### 修复

#### P0-1：blobUrlRegistry 延迟回收 60s → 10s + 容量 60 → 30（-141MB）
[import-store.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/engine/storage/import-store.ts)：
- `REVOCATION_DELAY_MS` 60s → 10s：导入完成后 440 条被 LRU 淘汰的 blob URL 在 10s 内释放（-132MB），而非 60s
- `BLOB_URL_CACHE_CAPACITY` 60 → 30：LRU 内常驻减半（-9MB）
- 10s 宽限期足够覆盖 React 重渲染和虚拟滚动回收

#### P0-2：imageCache 容量 24 → 16（-44MB）
[CanvasPhotoRenderer.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/canvas/CanvasPhotoRenderer.tsx) `IMAGE_CACHE_CAPACITY` 24 → 16。画布同时显示 8-12 张照片，16 足够覆盖当前页+相邻页，每条 5.5MB，节省 8 × 5.5MB = 44MB。

#### P0-3：loadCachedImage 用 readFile 替代 fetch(asset://)（核心优化，-500MB~1GB）
[CanvasPhotoRenderer.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/canvas/CanvasPhotoRenderer.tsx) `loadCachedImage` 对 asset:// URL 改用 readFile + createObjectURL：

**原路径**（触发 WebView2 缓存）：
```
fetch(asset://) → 5-10MB blob → createImageBitmap → imageCache
  ↓
WebView2 内部缓存原文件解码位图（60MB/张），无法清理
```

**新路径**（绕过 WebView2 缓存）：
```
readFile(filePath) → Uint8Array → new Blob → createObjectURL → createImageBitmap → imageCache
  ↓
blob URL 不进 HTTP 缓存，createImageBitmap 后立即 revokeObjectURL 释放引用
```

新增 `assetUrlToPath()` 函数逆向 convertFileSrc，从 asset:// URL 提取文件路径。readFile 失败时回退到 fetch（兼容非 Tauri 环境）。

### 预期效果
- blobUrlRegistry 10s 内释放：-132MB
- imageCache 容量降低：-44MB
- **WebView2 decoded image cache 不再膨胀**：-500MB~1GB（核心收益）
  - 原路径每次 fetch(asset://) 都让 WebView2 缓存 60MB 解码位图
  - 新路径用 readFile 读取文件字节，blob URL 不触发 WebView2 的 asset:// 缓存
  - createImageBitmap 后立即 revoke blob URL，无残留引用
- V8 堆高位水位仍存在（不可控），但不再持续膨胀

### 不可控部分说明
- **V8 堆高位水位**（200-400MB）：500 次大 Uint8Array 分配后 V8 不主动归还 OS，需重启进程才能完全释放。这是 V8 引擎特性，无法从 JS 侧解决。
- **WebView2 decoded image cache**：虽然新路径绕过了 asset:// 的 HTTP 缓存，但 createImageBitmap 仍可能在 Chromium 内部产生解码缓存。这部分不可控，但由于 imageCache 容量限制（16 条），缓存规模有限。

### 验证
- tsc --noEmit 通过
- vitest 127/127 通过

---

## 2026-07-25 · 导入 500 张照片后内存 1.7GB 退出主页不释放深度修复

### 背景
上一轮修复（terminateWorkerPool、evictDirectPhotoUrl、acquire/release 配对）后，用户反馈 direct 模式导入 500 张照片后内存仍 1.7GB，从编辑器退出到主页也不释放，必须关闭应用进程重启才释放。

### 根因（4 个清理缺失协同作用）

#### 根因 A：bitmapCache（100 条 ImageBitmap，500MB-1GB）未清理
[imageBitmapLoader.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/imageBitmapLoader.ts) `bitmapCache` 容量 100，存放 `loadImageBitmap` 加载的 ImageBitmap（每条 5-10MB）。全屏视图和网格视图的 `preloadPagePhotos` 调用 `loadImageBitmap` 填充此缓存。`cleanupProjectResources` **未调用 `clearBitmapCache()`**，是退出编辑器后内存不释放的最大单一泄漏源。

#### 根因 B：Konva Stage 未销毁，持有所有 Konva.Image 节点位图引用
[Canvas.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/Canvas.tsx) 无 `useEffect` cleanup 销毁 Stage。EditorView 卸载后，Konva.Stage 内部对象图（Layer → Group → Konva.Image → CanvasImageSource）仍驻留内存。即使 `imageCache.clear()` 关闭了缓存中的 ImageBitmap，**已被 Konva.Image 节点引用的位图不会被 close**。

#### 根因 C：photoStore.photos（500 张 Photo）未清空，锚定 WebView2 缓存
[EditorView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/EditorView.tsx) `handleBack` 未调用 `setPhotos([])`。Photo 对象持有 `src`（asset:// URL），锚定 WebView2 内部 decoded image cache。只要 Photo 对象存活，WebView2 认为资源仍"在用"不回收内部缓存。

#### 根因 D：editorStore.pages 和 historyStore.snapshots 未清空
`handleBack` 未调用 `setPages([])` 和 `historyStore.clear()`。pages 中 placements 持有 photoId → photoMap → Photo 对象，形成引用链。historyStore.snapshots 持有 pages 深拷贝，进一步加重内存占用。

### 修复

#### P0-1：cleanupProjectResources 增加 clearBitmapCache
[photoService.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/services/photoService.ts) `cleanupProjectResources` 新增第 6 项 `clearBitmapCache()`，清空 100 条 ImageBitmap 缓存（~500MB-1GB）。

#### P0-2：Canvas unmount 时销毁 Konva Stage
[Canvas.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/Canvas.tsx) 新增 unmount useEffect：
- `stage.destroy()` 递归销毁所有子节点（Layer/Group/Image/Transformer）并解绑事件
- `setKonvaStage(null)` 清除全局句柄
- 释放 Konva.Image 节点持有的 CanvasImageSource 引用

#### P0-3：handleBack 清空 store 状态
[EditorView.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/views/EditorView.tsx) `handleBack` 在 `cleanupProjectResources()` 后新增：
- `setPages([])` — 清空 editorStore.pages，切断 placements → photoId 引用链
- `setPhotos([])` — 清空 photoStore.photos，释放 Photo 对象及其 src（asset:// URL）引用，让 WebView2 内部缓存可回收
- `useHistoryStore.getState().clear()` — 清空历史快照

### 之前修复在 Tauri 桌面端的实际效果
- `evictDirectPhotoUrl`/`clearAllDirectPhotoUrls` 在 Tauri 模式下是 **no-op**：directUrlCache 只用于浏览器 FSA 模式，Tauri 模式下 photo.src 是 `asset://` URL（convertFileSrc 生成），从不进入 directUrlCache
- `terminateWorkerPool` 生效：导入完成后 Worker 池被终止
- `acquire/release` 配对生效：blobUrlCache 引用计数平衡

### 预期效果
- 导入完成后内存从 1.7GB 降至 ~700MB（bitmapCache 释放 -500MB~1GB）
- 退出编辑器到主页后内存降至 ~300MB（与重启后一致）：
  - bitmapCache 清理：-500MB~1GB
  - Konva Stage 销毁：-50~200MB
  - photoStore/editorStore/historyStore 清空：切断引用链，让 WebView2 内部缓存可回收

### 验证
- tsc --noEmit 通过
- vitest 127/127 通过

---

## 2026-07-25 · 导入 800+ 照片后排版 236 页内存 1.7GB 优化

### 背景
用户反馈：导入 800+ 张照片排版 236 页后，任务管理器内存 1.7GB，关闭应用重进后降到 300MB。说明运行时内存未释放，需重启才释放。

### 根因
经分析，运行时常驻内存主要来源（按大小排序）：

| 排名 | 来源 | 大小 | 问题 |
|------|------|------|------|
| 1 | **Worker 池（8 个）** | 160-400MB | 导入完成后未终止，常驻直到退出编辑器 |
| 2 | **directUrlCache（15 条原文件）** | 150MB | direct 模式 P2 生成 preview 后未清理旧 asset:// blob URL |
| 3 | **imageCache（24 条 1200px）** | 120MB | 设计如此，画布复用 |
| 4 | **blobUrlCache（60 条 preview）** | 30MB | CanvasPhotoRenderer acquire 后无配对 release，URL 被 pin 永不回收 |

**为什么重启后 300MB**：重启后从 IndexedDB 重新加载，只重建 preview blob URL（60 × 500KB = 30MB）+ thumbUrlCache（120 × 50KB = 6MB），Worker 池不重建。

### 修复

#### P0-1：导入完成后终止 Worker 池（-160~400MB）
[usePhotoImport.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/hooks/usePhotoImport.ts) `doImport` 完成后调用 `terminateWorkerPool()`：
- direct 模式：await P2 后台任务完成后再终止（之前是 fire-and-forget，Worker 常驻）
- import 模式：Phase 2 完成后直接终止
- 下次导入时 `getWorkerPool` 自动重建 Worker 池，功能不受影响

#### P0-2：direct P2 完成后清理 directUrlCache（-150MB）
[usePhotoImport.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/hooks/usePhotoImport.ts) P2 任务中，`photo.src` 从 `asset://` 更新为 preview blob URL 后，调用 `evictDirectPhotoUrl(photo.relativePath)` 清理 directUrlCache 中的原文件 blob URL（5-10MB/条）。

#### P0-3：CanvasPhotoRenderer acquire/release 配对（-30MB，长期累积更多）
[CanvasPhotoRenderer.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/canvas/CanvasPhotoRenderer.tsx) retry 逻辑中 `acquirePhotoUrl` 后配对 `releasePhotoUrl`：
- 新增 `acquiredBlobIdRef` 记录 acquire 的 blobId
- retry URL 变化时 release 上一次 acquire 的 blobId
- photo 变化时 release acquire 的 blobId
- 组件卸载时 release acquire 的 blobId
- 之前 acquire 后从不 release → URL 被 pin 住（refCount 永远 > 0）→ LRU 无法淘汰 → blobUrlCache 60 条全被 pin → 内存泄漏

#### P1：后台 P2 并发度 4 → 2
[usePhotoImport.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/hooks/usePhotoImport.ts) direct 模式后台 P2 任务并发度从 4 降到 2，降低峰值内存（4 × 10MB 原文件 = 40MB → 2 × 10MB = 20MB）。P2 改为 await 后整体耗时会感知到，降并发避免与用户操作争抢 CPU。

### 预期效果
- 导入完成后内存从 1.7GB 降至 ~700MB（-1GB）
  - Worker 池释放：-160~400MB
  - directUrlCache 清理：-150MB
  - blobUrlCache 回收：-30MB+
- 运行时内存与重启后内存差距从 1.4GB 缩小到 ~400MB（imageCache + blobUrlCache 设计如此）

### 验证
- tsc --noEmit 通过
- vitest 127/127 通过

---

## 2026-07-25 · 网格视图关闭重进后部分页面空白修复

### 背景
用户反馈：网格视图模式下部分已添加照片的页面显示空白，编辑器画布/底部导航/全屏显示均正常。关闭应用重进相册后出现，疑似与重进操作有关。

### 根因
网格视图缩略图渲染链存在两个独立缺陷，叠加导致"残缺 dataURL 被永久缓存"：

#### 根因 A：contentHash 不含 photo blob 状态
[gridThumbnailRenderer.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/gridThumbnailRenderer.ts) `computeContentHash` 仅含 `photo.id + width + height`，**不含 thumbBlobId/previewBlobId/blobId**。底层 blob 变化（direct 模式 P2 后台生成 preview、import 模式重新生成 blob、照片被替换）时哈希不变 → 命中 IDB 中旧的残缺 dataURL → 网格视图部分页面永久空白。

#### 根因 B：Worker 返回残缺 dataURL 被写入 IDB
[thumbnailCore.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/thumbnailCore.ts) `drawPageToCanvas` 对照片缺失（photoImages 中无该 photoId）的处理是 `continue` 跳过，但仍画背景+槽位，生成"空白但有底色"的 PNG dataURL。

[gridThumbnailRenderer.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/gridThumbnailRenderer.ts) `attachWorkerListener` 收到 Worker 返回的 dataURL 后**无内容校验**直接写入 LRU + IDB。只要 dataURL 非空就持久化，残缺图被跨会话复用。

#### 为什么编辑器/底部导航/全屏正常
- 编辑器画布：用 Konva 实时渲染，每张照片独立加载，不缓存整页 dataURL
- 底部导航：缓存的是"可重新解析的 URL"（指向 IDB blob），坏了能清掉重查；`<img>` onError 有重试+破图兜底
- 全屏显示：使用独立 `cacheSuffix='fs'`，缓存 key 与网格视图隔离
- 网格视图：缓存的是"渲染产物 dataURL"，坏了无法识别也无法重建

### 修复

#### 1. contentHash 纳入 photo blob 状态
[gridThumbnailRenderer.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/gridThumbnailRenderer.ts) `computeContentHash` 的 `photoDims` 加入 `thumbBlobId/previewBlobId/blobId`：
- 底层 blob 变化时哈希自然变化 → 缓存 key 变化 → IDB miss → 重新渲染
- 避免命中旧的残缺 dataURL

#### 2. drawPageToCanvas 返回绘制计数
[thumbnailCore.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/thumbnailCore.ts) `drawPageToCanvas` 返回 `drawnPhotoCount`（实际成功绘制的照片数）。

#### 3. Worker 返回 drawnPhotoCount + 主线程校验
[thumbnail.worker.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/thumbnail.worker.ts) `ThumbnailWorkerResponse` 新增 `drawnPhotoCount` 字段。

[gridThumbnailRenderer.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/utils/gridThumbnailRenderer.ts)：
- `attachWorkerListener`：比较 `drawnPhotoCount` 与 `expectedPhotoCount`
  - 校验通过：写 LRU + IDB（跨重载复用）
  - 校验失败：仅写 LRU（本会话软缓存，避免重复渲染），**不写 IDB**（避免跨会话污染）
- `renderPageThumbnail`（主线程路径）：校验失败时返回 null，不写 LRU 也不写 IDB
- `PendingWorkerJob` 新增 `expectedPhotoCount` 字段

### 验证
- tsc --noEmit 通过
- vitest 127/127 通过

---

## 2026-07-25 · 智能编排首次画布空白修复（第5轮根因修复）

### 背景
前4轮修复（见下方）聚焦于 CanvasPhotoRenderer 的 src 解析和 retry 逻辑，未解决根因。用户反馈：**新创建相册，首次进入，使用智能编排生成页面后画布空白**，返回主页重新进入或再次使用智能编排均正常。

### 根因（前4轮未触达）
`clearRuntimeBitmapCache` **故意不清 imageCache**（注释称"清了会导致画布重新加载失败"），但这恰恰是空白的根因：

1. **EditorView 卸载/重挂**：App.tsx 条件渲染，进入 SmartLayoutView 时 EditorView 完全卸载，返回时重新挂载，Konva Stage 是全新实例
2. **imageCache 保留旧 ImageBitmap**：旧 ImageBitmap 在上一个 Konva Stage 上下文中创建，可能已失效（direct 模式 P2 后台任务 `imageCache.evict(oldSrc)` 已 close 了它们）或与新 Stage 不兼容
3. **cachedImage.current 残留**：新 CanvasPhotoRenderer 实例从 imageCache 获取旧 ImageBitmap 后，`cachedImage.current` 指向它。当 loadSrc 变化（P2 更新 photo.src）导致 useEffect 重新执行但缓存 miss 时，**未清除 cachedImage.current**，KonvaImage 继续渲染已 close 的位图 → 黑块/空白

**为什么重新进入项目正常**：handleOpenProject 调用 `cleanupProjectResources`（包含 `imageCache.clear()`），清空所有旧 ImageBitmap，EditorView 重新挂载后 CanvasPhotoRenderer 重新加载，新 ImageBitmap 在新 Stage 上创建 → 正常。

**为什么再次编排正常**：重新进入后 photo.src 已重建，imageCache 已清空并有有效条目。编排返回后 imageCache 中的 ImageBitmap 仍可在新 Stage 上复用 → 正常。

### 修复

#### 1. clearRuntimeBitmapCache 增加 imageCache.clear()
[photoService.ts](file:///g:/WorkBuddy/MemBook/membook-backup/src/services/photoService.ts) `clearRuntimeBitmapCache` 增加 `imageCache.clear()`：

- 编排返回后清空 imageCache，强制 CanvasPhotoRenderer 重新加载所有图片
- 不清 blobUrlCache/directUrlCache：photo.src 仍有效，避免裂图
- 重载安全性由 usePhotoSrc + retry 回退链保障（前4轮已修复）：
  - usePhotoSrc：photo.src → IndexedDB → 文件系统多级回退
  - retry：acquirePhotoUrl(previewBlobId) 从 IndexedDB 重建 URL + makeDirectPhotoUrl 回退

#### 2. CanvasPhotoRenderer 缓存 miss 时清除 cachedImage.current
[CanvasPhotoRenderer.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/canvas/CanvasPhotoRenderer.tsx) 加载 useEffect 修复：

- `loadSrc` 变为空时：清除 `cachedImage.current = null`
- 缓存 miss 时：清除 `cachedImage.current = null`，避免 KonvaImage 渲染已 close/失效的旧 ImageBitmap
- 异步加载完成后再设置新位图

### 验证
- tsc --noEmit 通过
- vitest 127/127 通过

---

## 2026-07-25 · 智能编排首次画布空白修复（第4轮根因修复）

### 背景
前3轮修复（见下方）聚焦于缓存清理策略，未解决根因。用户反馈：**新创建相册，首次进入，使用智能编排生成页面后画布空白**，返回主页重新进入或再次使用智能编排均正常。

### 根因（前3轮未触达）
`CanvasPhotoRenderer` 的 retry 逻辑在 Tauri direct 模式下**必然失败**：
1. `retrySrcRef` 用 `readDirectPhoto(photo.relativePath)` 重建 URL——该函数使用 File System Access API（浏览器 API），在 Tauri 桌面端 `directoryHandle` 为 null，**必然返回 null**
2. 返回 null 后无重建 URL，retry 放弃，画布**永久空白**
3. 重试上限仅 2 次，不足以覆盖多级回退

此外，`loadCachedImage` 的 asset URL 检测只匹配 `asset://`，**遗漏了 Tauri v2 Windows 的 `http://asset.localhost` 格式**，导致原文件降采样失效（5000x3000 原图占 ~60MB 位图内存）。

### 修复
[CanvasPhotoRenderer.tsx](file:///g:/WorkBuddy/MemBook/membook-backup/src/components/editor/canvas/CanvasPhotoRenderer.tsx) 全面重构照片 src 解析与加载回退链：

1. **使用 `usePhotoSrc` hook 替代直接 `photo.src`**：与底部缩略图/全屏一致的多级回退（photo.src → IndexedDB → 文件系统）
2. **修复 retry 逻辑**：
   - 两种模式统一优先 `acquirePhotoUrl(previewBlobId)` 从 IndexedDB 获取 blob:// URL（fetch 必定成功）
   - direct 模式回退 `makeDirectPhotoUrl`（Tauri 下 `convertFileSrc` → asset 协议，替代失效的 `readDirectPhoto`）
   - 重试上限 2 → 5 次
3. **修复 asset URL 检测**：新增 `isAssetUrl()` 同时检测 `asset://`、`http://asset.localhost`、`https://asset.localhost`
4. **`DragPreviewPhoto` 同步改用 `usePhotoSrc`**

### 验证
- tsc --noEmit 通过
- vitest 127/127 通过
- EXE 构建成功（NSIS + MSI）

---

## 2026-07-24 · [已压缩] 性能优化体系 + 内存泄漏修复 + 智能编排空白修复 + EXE 构建

> 原始日志已压缩（超过 10天）。摘要：
> 1. **P0/P1/P2 性能优化**：LRU 缓存（imageCache/blobUrlCache/directUrlCache）、react-virtuoso 虚拟滚动、Zustand useShallow 细粒度订阅、LOD 三级图片体系（thumb 256px / preview 1200px / full 4096px）、IndexedDB 缩略图持久化（v4 migration）、Web Worker + OffscreenCanvas 缩略图渲染、photoStore photoMap 归一化（O(n)→O(1)）、blobUrlCache 引用计数（acquirePhotoUrl/releasePhotoUrl + 延迟回收）
> 2. **内存泄漏修复（减少 ~800MB+）**：compress.worker ImageBitmap 不 close（480MB泄漏）、CompressWorkerPool 永不终止（160-400MB常驻）、缓存容量过大（blobUrlCache 200→60、imageCache 40→24、directUrlCache 100→30）、directUrlCache evict 改立即 revoke、tempUrls 延迟创建（导入峰值 5GB→200MB）、direct 模式分阶段压缩（导入 3分钟→25秒）
> 3. **智能编排后画布空白/裂图修复**：根因为 clearRuntimeBitmapCache 清空 imageCache 或 cleanupProjectResources revoke blob URL 后 photo.src 仍引用旧 URL；修复为 clearRuntimeBitmapCache 只清位图缓存不 revoke URL
> 4. **项目切换内存不释放修复**：新增 cleanupProjectResources() 统一清理所有模块级缓存，EditorView.handleBack 和 HomeView 项目切换时调用
> 5. **首次进入编辑器空白修复**：invalidateThumbnailCache 误清 IDB thumbnails 表，改为 clearThumbnailMemoryCache 仅清 LRU 内存
> 6. **网格视图显示异常修复**：PageCard LRU 返回陈旧缓存、Worker 并发渲染丢响应（改用自增序列号 job id）、底部缩略图 blob URL 被 revoke 后永久破损、PageCard catch 分支永久空白
> 7. **EXE 安装包构建**：NSIS .exe + WiX .msi 双格式，签名+自动更新支持
> 8. **Tauri 自动更新 + Sentry 错误监控**：tauri-plugin-updater 被动安装模式、Sentry 生产环境错误上报 + ErrorBoundary
> 9. **UI 优化**：智能编排按钮设计优化、网格视图 ESC 退出、右下角按钮 icon+文字统一

---

## 2026-06-17 · [已压缩] Phase 1 完成：V3 架构 + 编辑器核心

> 原始日志已压缩（超过 10 天）。摘要：建立 V3 架构（引擎层/UI 层分离，Zustand 状态管理，Dexie 持久化）；基于 Konva 的高性能 2D 画布渲染；12+ 内置模板系统 + 自定义模板创建器（拖拽/缩放槽位）；主题系统（背景色/装饰/水印）；主页项目管理（项目网格/模板画廊/项目封面渲染）；编辑器布局重构（顶部工具栏简化 + 左侧三 Tab 导航 + Canvas 全局缩放联动 + 底部导航栏）。涉及 Toolbar/LeftPanel/Canvas/BottomNav/EditorView 核心组件重写 + CreateTemplateDialog/TemplateGallery 新增。

---

## 开发日志维护说明

- 每次 AI 辅助开发后，在文件**顶部**追加新条目（按时间倒序）
- 条目格式：`## YYYY-MM-DD · 功能标题`
- 必含字段：背景 / 已完成项 / 错误与修复（如有）/ 验证结果 / 涉及文件
- 文件引用使用 `[文件名](file:///绝对路径)` 格式，便于 IDE 跳转
