# MemBook 开发日志

> 记录 AI 辅助开发的功能情况，按时间倒序排列。

---

## 2026-08-20 · 自动更新交互重做：主页右上角圆 icon 后台下载 → 「更新」按钮 → 弹窗确认重启安装
>
> 用户需求：检测到新版本后不打扰用户，只在主页任务栏最右上角用圆 icon 体现后台下载（圆外能量圈表示进度、中间下载动效图标）；下载完成后图标变为动效按钮（icon + 「更新」）；点击弹出更新弹窗，说明最新版本号与更新内容，底部「取消 / 立即更新」，点「立即更新」重启应用完成安装。

### 原因
原更新流程在检测到新版后于右下角弹「安装并重启」提示，会打断用户当前操作；且下载与安装耦合在一起，用户无「先后台下载、再择机确认安装」的路径。需改为**全自动后台下载、用户主动点按钮再确认安装**的交互，并将进度与就绪提示收敛到主页任务栏一角。

### 改动
**[store/uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)**：新增 `AutoDownloadStatus`（`phase: 'downloading'|'ready'`、`version`、`progress`）与 `uiStore.autoUpdate` / `uiStore.readyUpdate` / `uiStore.updateDialog` 三状态及对应 setter（`setAutoUpdate`/`setAutoUpdateProgress`/`setReadyUpdate`/`setUpdateDialog`），统一承载更新指示器与更新弹窗。

**[components/common/UpdateIndicator.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/UpdateIndicator.tsx)**（新建）：主页顶栏右上角更新指示器——
- 下载中：圆形 icon，外圈「能量圈」按进度描弧（stroke-dasharray），中间上下弹动下载图标（`downloadBounce`），只读不打扰。
- 下载完成：变动效按钮（icon + 「更新」+ `updatePulse` 呼吸光晕），点击 `setUpdateDialog(readyUpdate)` 打开弹窗。两态均无 `autoUpdate` 时返回 null。

**[components/views/HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)**：在主页 `AppHeader` 内右侧数据迁移工具之后挂载 `UpdateIndicator`。

**[App.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/App.tsx)**：移除旧的右下角轻量进度条与本地更新态变量，改走 `uiStore`；启动检查命中新版后 `setAutoUpdate` 置下载态 → `startAutoDownload` 后台下载（进度 `setAutoUpdateProgress`）→ 完成 `setAutoUpdate(null)` + `setReadyUpdate(getPendingUpdateInfo())`；失败静默结束。更新弹窗由 `uiStore.updateDialog` 驱动 `UpdateDialog`。

**[components/common/UpdateDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/UpdateDialog.tsx)**：底部按钮改「取消 / 立即更新」——「取消」= `markChecked()` 记 24h 冷却 + 关窗，**保留已下载包与右上角按钮**（`readyUpdate` 不清，可随时再次打开）；「立即更新」= `installPrepared()` 安装并 `relaunchApp()` 重启。

**[utils/updater.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/updater.ts)**：新增「重装低版本放行冷却」规则——`recordKnownVersion` 在检测到新版时记录曾见最高版本（localStorage `membook-update-known-version`）；`isWithinCheckCooldown` 先判断「当前版本 < 曾见版本」则直接放行（返回 false），否则再看 24h 时间戳。解决「卸载未删数据后重装低版本 + 残留冷却标记 → 再也不检查更新」的问题。

**[styles/globals.css](file:///f:/N-编程/MenBook开发项目/MemBook/src/styles/globals.css)**：新增 `updatePulse`（就绪按钮呼吸光晕）与 `downloadBounce`（下载图标上下弹跳）关键帧。

**[i18n/locales/zh-CN.json / en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**：新增 `updater.autoDownloading` / `updater.update` 及弹窗「取消 / 立即更新」文案。

### 验证
- `npm run typecheck` / `npx eslint`：0 错误。
- 需端到端验证：旧版应用启动 → 顶栏出现下载圆 icon → 完成后变「更新」按钮 → 点击弹窗显示最新版本号+更新内容 → 点「立即更新」安装并重启。

---

## 2026-08-20 · 自动更新接通 GitHub Releases：检测→后台自动下载→就绪弹窗重启安装
>
> 用户需求：在 GitHub 上推送新版后，应用能自动检测到新版本并自动下载升级，无需手动下载安装包。

### 原因
原更新源 `https://updates.membook.app/{{target}}/{{arch}}/{{current_version}}` 域名未提供服务（实测无法访问），导致虽然 CI 已发布带签名安装包到 GitHub Releases，但应用检查更新时拿不到清单，自动升级链路整体断裂。且原交互为「被动模式」：检测到新版仅弹窗，用户需手动点「下载」按钮。

### 改动
**[tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json#L86-L88)**：updater `endpoints` 指向 GitHub Releases 最新清单——
`https://github.com/zhonghuoquan/MemBook/releases/latest/download/update-{{target}}-{{arch}}.json`（去掉 `{{current_version}}`；`releases/latest/download` 为稳定跳转，恒取最新发布）。

**[.github/workflows/release.yml](file:///f:/N-编程/MenBook开发项目/MemBook/.github/workflows/release.yml)**：Windows/macOS 两个任务各新增「生成 update manifest」步骤 —— 读取构建产物 `.sig`、按 tag 去 `v` 取版本、拼 GitHub Release 安装包 URL，生成 `update-windows-x86_64.json` / `update-darwin-universal.json`（version/pub_date/notes/platforms.signature/url），并加入 upload-artifact 与 action-gh-release 的 files。

**[utils/updater.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/updater.ts)**：
- 新增模块级 `pendingUpdate` 句柄 + `toInfo`，`checkForUpdate` 检测到新版即暂存句柄（复用同一次 check）。
- `downloadAndInstall`/`installUpdate` 拆分为 `startAutoDownload`（`update.download()` **只下不装**，后台跑、进度回调）与 `installPrepared()`（`update.install()` + `relaunchApp()`，install 后 `close()` 释放资源）。

**[App.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/App.tsx)**：启动检查（沿用 24h 冷却 + 3s 延迟）命中新版后直接 `startAutoDownload` 后台自动下载，右下角轻量进度条提示「正在后台下载新版 vX」；下载完成才 `getPendingUpdateInfo()` 弹出「就绪重启」框。下载失败静默结束不打扰。

**[components/common/UpdateDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/UpdateDialog.tsx)**：由「手动下载」改为**就绪态安装框**——保留 idle/installing/error 三态，主按钮改「安装并重启」（`installPrepared`），去掉下载进度条与下载按钮。

**[i18n/locales/zh-CN.json / en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json#L2233)**：新增 `updater.autoDownloading`（`正在后台下载新版 v{{version}}`）。

**[Readme.md](file:///f:/N-编程/MenBook开发项目/MemBook/Readme.md#L360-L361)**：关键约束同步更新——更新源改为 CI 生成的 GitHub Releases 清单；CSP 需放行 `github.com`/`*.githubusercontent.com`。

### 验证
- `npm run typecheck`：0 错误；无残留 `downloadAndInstall`/`installUpdate` 调用。
- 需手动验证：本地打一个新 tag 触发 CI，确认 Release 附件含 `update-*.json`；旧版应用启动应自动后台下载并弹出「安装并重启」。

---

## 2026-08-19 · 修复：编辑封面文字后切模板再撤销，第一次撤销回到新模板（副标题/日期内容不对）
>
> 用户需求：设计封面（副标题/日期）应用新模板后撤销，撤销后位置对但内容不对——显示了新模板的效果，未回到用户上一版封面。

### 原因
**冗余快照**：用户编辑封面文字（副标题等）未退出编辑态就切模板时，TextDomNode 卸载清理会调用 `updateTextElement(元素id, ...)` 提交文本。迁移已把该元素（cover-text-* 预设）丢弃，元素在新页上不存在，但 [decorationsSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/decorationsSlice.ts) 的 update 函数**无条件 `pushSnapshot` 压一条快照** → 历史栈多出一条「未变化的冗余快照」→ 第一次 Ctrl+Z 恢复到切换后的新模板（而非旧封面），表现为「撤销后应用了模板效果、内容不对」。用回归测试复现：修复前一次撤销回到 cover-3，修复后回到 cover-2。

### 改动
**[store/editorStore/decorationsSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/decorationsSlice.ts#L52-L111)**：`updateTextElement`/`updateStickyNote`/`updateStickerElement`/`updateShapeElement` 改为**仅在实际修改到元素时才压快照**——`map` 中标记 `changed`，元素不存在（如切模板/撤销后已移除）时既不修改也不压快照，避免冗余快照导致撤销错位。

**[store/editorStore/coverSwitch.undo.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/coverSwitch.undo.test.ts)**：新增回归测试 5 条——纯切换撤销 / 编辑副标题/日期/书脊日期切换撤销 / 「编辑未退出切模板不产生冗余快照、第一次撤销回到旧封面」BUG 回归。

### 验证
- `npm run build`：0 错误；`vitest run`：297 个测试全过（含新增 5 条）。
- 需手动 `npm run desktop:dev` 验证：编辑封面副标题后直接切新模板 → 按一次 Ctrl+Z 应一步回到旧封面（含用户编辑内容），而非停留在新模板。

---

## 2026-08-19 · 修复：切换封面模板后撤销，用户设计的封面内容被清除
>
> 用户需求：用户设计的封面（照片调整/文字/形状/书脊等）应用新封面后撤销，撤销后用户原来的内容被清除了，撤销未按用户调整过的内容恢复。

### 原因
两层问题叠加：
- **切换模板时内容保留不完整**：[applyCoverTemplate](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts) 切换时只迁移照片 ID + 用户新增文字/形状，**照片编辑属性（pan/缩放/裁剪/旋转/滤镜/明暗/阴影）被重建为 `{slotId, photoId}` 归零**；书脊设置（宽度/底色/logo 色）重置为模板默认；书脊文字（相册名/日期）重建丢弃用户编辑；封面贴纸/便利贴完全不迁移。
- **撤销/重做被编辑态残留干扰**：[useCanvasKeyboard](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts) 的 `isTextEditing` 只要 `editingTextId` 非空就拦截应用级撤销；切模板后文字被替换/移除时 `editingTextId` 残留 → Ctrl+Z 被拦截无反应，且恢复后旧文字因"编辑态隐藏"（opacity:0）显示为不可见 → 看起来像"内容被清除"。

### 改动（按「场景1 切换 / 场景2 撤销重做」区分，2026-08-19 确认）
**[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts#L520-L614)**（applyCoverTemplate + applyBackCoverTemplate 切换分支）：
- **照片编辑属性按 photoId 迁移**：用 `makePlacementMigrator`（已导出自 helpers）按新旧槽位尺寸重映射 pan，保留 pan/缩放/裁剪/旋转/滤镜/明暗/阴影；新槽位覆盖由 `calcCoverOverrides`（已从 pageMarginService 导出）计算。
- **书脊设置（场景1 规则）**：`spineWidth`/`spineAnchorMm` **沿用旧封面**（用户调整过的不重置）；**`spineColor`/`spineLogoColor` 重置为新模板默认**（底色=`template.spineColor`，logo 色=空=按底色深浅自动黑/白），不沿用旧封面——避免新模板仍显示旧模板书脊底色/logo 色、与模板设计不协调。
- **书脊文字沿用用户版本**（相册名/日期编辑保留），旧封面无书脊文字时回退新模板生成（按保留的书脊宽居中一致）。
- **贴纸/便利贴随切换迁移**。
- **场景2 撤销/重做**：切换压**一次**快照（封面+封底成套），撤销精确还原切换前封面（所有内容一致），重做完整还原切换后新模板（全部内容保持）。

**[components/editor/canvas/useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts#L99-L130)**：
- 撤销/重做前 `setEditingTextId(null)`，避免恢复后编辑浮层残留 / 文字被隐藏渲染。
- `isTextEditing` 判定改为「编辑元素仍存在于当前页」才拦截（textElements 或 stickyNotes 中能找到 `editingTextId`）；元素已不存在的残留 `editingTextId` 不再卡死撤销。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误；`vitest run`：292 个测试全过。
- 需手动 `npm run desktop:dev` 验证：① 场景1——设计封面（调整照片/加文字/改书脊宽度）→ 切换新封面：书脊宽度/锚点/书脊文字/照片调整/贴纸保留，**书脊底色与 logo 色变为新模板默认**；② 场景2——切换新封面后 Ctrl+Z 一步精确还原切换前封面（所有内容一致），Ctrl+Y 完整还原新模板（全部内容保持）；③ 编辑封面文字中途切模板后 Ctrl+Z 不再卡死、文字不再消失。

---

## 2026-08-19 · 修复：应用封面模板（封面+封底成套）撤销需按两次
>
> 用户需求：点击封面模板同时设置封面和封底，但撤销需要撤销两次，应一次撤销。

### 原因
[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts) `applyCoverTemplate` 内部先 `pushSnapshot(get)` 压一次快照（封面），随后 `await applyBackCoverTemplate(...)` 在封底分支内又 `pushSnapshot(get)` 压一次（封底）→ 一次操作产生两条历史，Ctrl+Z 需按两次。

### 改动
**[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts#L608-L615) / [types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/types.ts#L68-L73)**：
- `applyCoverTemplate`：删除中间的 `pushSnapshot`，封面/边距/封底全部 set 完成后统一在末尾压**一次**快照（封面+封底成套一次撤销）。
- `applyBackCoverTemplate` 新增 `recordHistory = true` 可选参数，被封面模板成套联动调用时传 `false` 跳过自身快照；GridView「添加封底」独立调用保持默认记一次快照。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：点击封面模板后按一次 Ctrl+Z 即整体撤销封面+封底；「添加封底」独立操作仍一次撤销。

---

## 2026-08-19 · 撤销系统全面修复：文字编辑撤销 + 快照覆盖审计 + 滑块历史去刷屏
>
> 用户需求：修复「文字编辑后撤销不生效」；分析并修复所有撤销不生效的操作。

### 原因
- **问题1（核心）**：文字用 contentEditable 浮层编辑，但 [useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts#L99-L126) 里 Ctrl+Z 无条件被应用级撤销拦截（preventDefault + 恢复 store 快照）。两头落空：① 编辑浮层是「非受控 div」，store 恢复了但浮层文字不刷新 → 视觉上撤销无反应；② 浏览器原生文本撤销（可撤刚输入的字符）被 preventDefault 阻止。
- **问题2**：[CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx#L182-L184) 确认时调 `setPages` 改数据但未压快照 → 书脊/圆角设置无法撤销。
- **问题3**：[albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts#L175-L196) `setSlotGap` 改间距未压快照 → 间距调整无法撤销。
- **问题4**：文字/形状/照片属性面板滑块 onChange 每次拖动都压一条快照 → 历史被刷屏，撤销要走几十步才回拖动前。

### 改动
**[canvas/useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts#L99-L126)**：新增 `isTextEditing` 判定（`editingTextId` 或 `document.activeElement.isContentEditable`），编辑态下 `Ctrl+Z`/`Ctrl+Y` **放行交给浏览器原生文本撤销/重做**（编辑中撤销字符、退出编辑后再撤销整次操作，PPT 行为）；非编辑态才走应用级 undo/redo。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx#L182-L184)**：确认书脊/圆角设置后补 `useHistoryStore.getState().pushSnapshot(...)`。

**[store/editorStore/albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts#L175-L196)**：`setSlotGap` 补 `pushSnapshot(get)`。

**[store/editorStore/decorationsSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/decorationsSlice.ts#L94-L107) / [placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts#L121-L134) / [services/slotEditService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/slotEditService.ts#L84-L90)**：`updateTextElement`/`updateStickyNote`/`updateStickerElement`/`updateShapeElement`/`updatePlacementAdjustments`/`updatePlacementFilterIntensity`/`updatePlacementPan`/`updatePlacementPanRotation` 新增 `recordHistory?: boolean` 参数，内部 `if (recordHistory !== false) pushSnapshot(get)`。

**[panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx#L172-L175) / [ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx#L89-L90) / [EditFlyout.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/EditFlyout.tsx#L277-L282)**：滑块统一「拖动 `recordHistory=false` 实时更新不记历史 → 松手 `onPointerUp` 传 `true` 提交一条快照」，覆盖字号/行距/字距/圆角/切角/透明度/旋转/描边宽/亮度/对比度/滤镜强度等。

**[CODE_DEVELOPMENT_SPEC.md](file:///f:/N-编程/MenBook开发项目/MemBook/CODE_DEVELOPMENT_SPEC.md#L140-L148)**：4.3 历史快照节更新——recordHistory 参数约定替代旧 mergeKey 方案、编辑态原生撤销规则。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：① 编辑文字输入后 Ctrl+Z 撤销字符、退出编辑后 Ctrl+Z 撤销整次修改；② 封面设置/间距调整确认后 Ctrl+Z 可撤销；③ 拖动滑块松手后一步撤销回拖动前（历史不刷屏）。

---

## 2026-08-19 · 关于弹窗「主要功能」卖点重新设计 + 布局优化
>
> 用户需求：根据 MemBook 更新的情况，重新设计调整「关于」弹窗里的功能卖点，并优化布局。

### 原因（现状问题）
- [AboutDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AboutDialog.tsx) 「主要功能」卖点仍是旧 6 项（自定义尺寸/一键成册/滤镜调色/画笔便利贴文字/本地优先/数据迁移），「滤镜调色」「数据迁移」辨识度低，而 MemBook 最新亮点（书脊精装设计、翻页书预览、封面模板库、标尺与对齐、照片整理工具集）完全未体现，卖点与产品现状脱节。
- 布局为 6 张等权小卡片（小 emoji + 标题 + 描述），无视觉层次。

### 改动
**[components/common/AboutDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AboutDialog.tsx)**：
- **卖点重组为 8 项**（2 列 × 4 行）：📖 书脊精装设计 / 🎠 翻页书预览 / ✨ 一键成册 / 🖼️ 封面模板库 / 🧲 标尺与对齐 / 🖌️ 创作工具 / 🛠️ 照片整理工具集 / 💾 本地优先；移除「滤镜调色」「数据迁移」「自定义尺寸」「画笔便利贴文字」旧键。
- **布局升级**：每项卡片改为「品牌色圆角图标块（w-10 h-10，`bg-primary-50`+品牌色 emoji）+ 加粗标题 + 两行截断描述（line-clamp-2）」，hover 品牌色描边，层次更精致。

**[i18n/locales/zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)**：`about.featuresList` 按 8 项新卖点重写（新增 spineCover/flipPreview/coverTemplates/rulerAlign/creativeTools/organizeTools，改写 localFirst；删除 customSize/filterAdjust/brushStickyText/dataMigration）。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误；确认无其他文件引用被删除的旧卖点键。
- 需手动 `npm run desktop:dev` 验证：主页「关于」弹窗功能卖点显示 8 张新卡片，图标/标题/描述正确，布局在窄屏自动回退单列。

---

## 2026-08-19 · 修复：主页「关于」弹窗层级不足，被封面书元素遮挡
>
> 用户需求：主页顶部「关于」弹窗弹出后没有置于顶层，封面书的其他元素会遮挡住弹窗。

### 原因
[AboutDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AboutDialog.tsx) 自定义弹窗外层用**固定 `z-50`**（`--z-overlay` 级别），而封面书实物效果共享组件 [HardcoverFrame.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/HardcoverFrame.tsx) 内部铰链/高光/折痕等绝对定位元素 `zIndex` 高达 **50/55/56/60**（`--z-toolbar`=60 级）；标准 Modal 组件用 `z-[var(--z-modal)]`=100。弹窗 z-50 低于封面书元素 zIndex 60 → 封面书元素在根层叠上下文中盖住弹窗。

### 改动
**[components/common/AboutDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AboutDialog.tsx)**：
- 主弹窗外层 `z-50` → `z-[var(--z-modal)]`（100，与 Modal 组件同源，高于封面书 60）。
- 「清除激活」确认子弹窗 `z-[60]` → `z-[calc(var(--z-modal)+1)]`（101，仍高于主弹窗）。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：主页打开「关于」弹窗，封面书折痕/高光等元素不再穿透遮挡弹窗内容与背景；清除激活确认框层级正常。

---

## 2026-08-19 · 照片整理列表懒加载：无日期/无 GPS/格式转换列表滚动到底自动加载全部
>
> 用户需求：修改拍摄信息里「无拍摄时间」列表照片很多时，下拉到最后只显示「还有 XX 张照片」就没了；格式转换也一样。希望继续下拉也能显示，并做懒加载优化性能。

### 原因
三处列表都是**硬编码 `slice` 截断 + 静态剩余提示**，截断后无加载机制，剩余照片永远渲染不出来：
- [ExifTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ExifTool.tsx)：无日期列表 `slice(0,200)`、无 GPS 列表 `slice(0,200)`，各配底部「还有 N 张」提示（`exif.morePhotos`）。
- [ConvertTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ConvertTool.tsx)：已选文件预览 `slice(0,20)` + 「还有 N 个文件」提示（`convert.moreFiles`）；照片选择网格为全量 `map` 渲染（照片多时卡顿）。

### 改动
**[organize/shared.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/shared.tsx#L68-L94)**：新增共享 `useLazyList(total, batch)`——`visibleCount` 初始一批，列表末尾哨兵进入视口（提前 300px）自动追加下一批直到全部；总数减少时自动收敛。返回 `{ visibleCount, sentinelRef }`。

**[ExifTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ExifTool.tsx)**：无日期/无 GPS 列表改 `slice(0, visibleCount)` + 哨兵，删除静态「还有 N 张」提示（初始/增量各 200）。

**[ConvertTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ConvertTool.tsx)**：照片选择网格（初始/增量 200）与已选文件预览（初始/增量 50）均改懒加载 + 哨兵，删除静态「还有 N 个文件」提示。

### 效果
- 无日期/无 GPS/文件预览列表滚动到底自动加载下一批，最终全部照片可展示，不再被「还有 XX 张」封顶。
- 网格与列表初始只渲染一批，配合 `ThumbImage` 的 IntersectionObserver 懒加载，照片很多时显著降低首屏 DOM 与缩略图解码压力。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：构造 >200 张无日期照片，展开列表滚动到底能持续加载至全部；格式转换选择网格与文件预览同理；修改/转换照片移出列表后已加载数量正确收敛。

---

## 2026-08-19 · 修复：封面页辅助线按含书脊总宽计算（排除书脊、按页面实际尺寸定位）
>
> 用户需求：封面/封底的边距辅助线与页面辅助线当前按「页面宽 + 书脊宽」的总尺寸计算，应排除书脊、按封面实际页面尺寸定位，与普通页一致。

### 原因
- 封面页书脊是**页面左侧物理扩展区**（书脊 `x: 0~spinePx`，封面正页内容实际从 `x: spinePx` 开始），`CANVAS_W = (页面宽 + 书脊宽) × MM_TO_PX` 含书脊。
- [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2601-L2637) 辅助线段直接用含书脊的 `CANVAS_W` 当基准且未加 `spinePx` 起始偏移——边距框偏右、内缩比例错，中线/三分线整体被书脊"挤偏"。

### 改动
**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2601-L2637)**：辅助线计算引入「封面页内容实际区域」——`contentX = isCoverLike ? spinePx : 0`、`contentW = isCoverLike ? CANVAS_W - spinePx : CANVAS_W`：
- **边距虚线**：左右边按 `contentX + contentW × (margin/页面宽)` 计算，上下边不变（Y 无书脊）。
- **页面辅助线**：三分线/中线 X 坐标基于 `contentX + contentW × 比例`；水平线两端限制在 `[contentX, contentX+contentW]` 不穿书脊，垂直位置不变。
- 普通页/封底 `spinePx=0`，行为与原先完全一致。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：封面页开启「边距」「辅助线」后，框线与中线/三分线落在封面正页区域内（书脊右侧、按页面实际尺寸），普通页/封底不受影响。

---

## 2026-08-19 · 底部导航新增「边距」「辅助线」快速开关按钮
>
> 用户需求：把页面设置里的「边距辅助线」「页面辅助线」做成按钮，放在底部导航「对齐」「标尺」按钮旁，方便快速开关。

### 原因
- 「边距辅助线」（`showMarginGuide`，安全区虚线）与「页面辅助线」（`showGuides`，中线/三分线）原本只藏在「页面设置」弹窗里，开启需层层进入，操作不便。
- Store 已有 `setShowGuides` / `setShowMarginGuide` 直接设置接口，画布（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2600-L2629)）已直接读取这两个字段渲染，无需改动渲染逻辑。

### 改动
**[components/editor/BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)**：左侧分组（对齐/标尺）后新增「**边距**」（虚线框图标）与「**辅助线**」（井字图标）两个胶囊按钮，样式与对齐/标尺一致，点击直接 `setShowMarginGuide` / `setShowGuides` 切换、即时生效。

**[i18n/locales/zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)**：新增 `editor.bottomNav.marginGuide/guides/enableMarginGuide/disableMarginGuide/enableGuides/disableGuides`。

> 说明：「页面设置」弹窗打开时从 store 同步本地开关（`PageSettings` useEffect），底部按钮切换后打开弹窗可读到最新值，两处状态一致。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：底部导航「边距」「辅助线」按钮可快速开关对应辅助线，与「对齐」「标尺」紧挨；页面设置弹窗内开关状态同步。

---

## 2026-08-19 · 修复：底部导航「对齐」「标尺」按钮间距过大
>
> 用户需求：标尺按钮与对齐按钮距离太远，应紧挨放在旁边。

### 原因
[BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx) 工具栏容器为 `flex ... gap-4`，`gap` 作用于**所有直接子元素**（「对齐」「标尺」「右侧功能区」三者之间各 16px），导致「对齐」与「标尺」两个按钮被拉开。

### 改动
将「对齐」+「标尺」包进左侧分组 `<div className="flex items-center gap-1.5">`，两按钮紧挨成组；容器 `gap-4` 仅保留在左分组与右侧功能区之间，不影响右侧布局。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：底部导航左侧「对齐」「标尺」两按钮紧挨相邻。

---

## 2026-08-19 · 修复：标尺与参考线六项优化（拖出方向 / mm 刻度 / 数字分层 / 珊瑚粉虚线 / 顶部控件避让）
>
> 用户需求：修复标尺/参考线交互与显示问题——① 拖出方向反了；② 标尺刻度按逻辑像素（420）而非 mm（210）显示；③ 数字与刻度线重叠；④ 标尺按钮放「对齐」旁；⑤ 参考线虚线改粉红色；⑥ 标尺开启时顶部悬浮工具栏下移不遮挡。

### 原因（现状问题）
- **拖出方向反**：[CanvasRulers.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CanvasRulers.tsx) `startGuide` 绑定方向与参考线方向不符——顶部横尺绑 `'vertical'`（拖出竖线）、左侧竖尺绑 `'horizontal'`（拖出横线），与直觉（横尺拖横线、竖尺拖竖线）相反。
- **刻度单位错误**：画布宽 = `(页面宽 + 书脊宽) × MM_TO_PX(2)` 逻辑像素，`drawTicks` 直接按逻辑像素标刻度，页面 210mm → 标尺显示 0~420，期望按 mm（0~210）。
- **数字与刻度重叠**：主刻度线几乎画满整条标尺（24px→4px），数字画在主刻度区域内，视觉混乱。
- **顶部控件遮挡**：`PageToolbar`（top-3，z-30）与右上角 `PageDisplayModeToggle`（top-3）盖在标尺（24px 高、z-20）上。
- **颜色**：参考线为蓝色虚线（#0EA5E9）。

### 改动
**[components/editor/CanvasRulers.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CanvasRulers.tsx)**
- **修复方向**：顶部横尺 `startGuide('horizontal')`（拖出**水平参考线**，位置随 Y）、左侧竖尺 `startGuide('vertical')`（拖出**垂直参考线**，位置随 X）；同步修正注释。
- **刻度按 mm 显示**：`RulerDrawOpts` 增加 `mmToPx`（`MM_TO_PX`，从 `./canvas/constants` 引入）；主刻度间距从 `1/2/5/10/20/50/100/200mm` 中按 `mm×mmToPx×zoom ≥ 60px` 自适应选择，页面长传 `canvasW/MM_TO_PX`（mm），标签显示 mm 整数。
- **数字与刻度分层**：`textBaseline='top'`，数字放标尺外侧（水平尺顶部 y2~11、垂直尺左侧 x3~12），刻度线放内侧（主 24→13 / 中 24→16 / 次 24→19），两者不再重叠。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：参考线虚线颜色 `#0EA5E9` → `#FF6B8B`（珊瑚粉），覆盖已保存参考线（横/竖）与从标尺拖出中的临时预览线三处。

**[components/editor/PageToolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageToolbar.tsx) / [PageDisplayModeToggle.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageDisplayModeToggle.tsx)**：读取 `uiStore.rulerEnabled` + `RULER_SIZE`（从 CanvasRulers 导出），标尺开启时顶部定位 `top` 由 `12px` 下移至 `RULER_SIZE + 12px`（44px），不遮挡标尺；关闭时恢复。

**[components/editor/BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)**：确认「标尺」按钮已在底部导航左侧「对齐」旁，无需改动。

### 效果
- 顶部横尺拖出横线、左侧竖尺拖出竖线，方向符合直觉。
- 标尺刻度按 mm 显示（210mm 页面标 0~210），数字与刻度线分层清晰不重叠。
- 参考线为珊瑚粉虚线；标尺开启时顶部悬浮工具栏与右上角显示模式按钮自动下移避让。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- 需手动 `npm run desktop:dev` 验证：拖出方向正确；刻度显示 0~210mm；数字不与刻度重叠；虚线为粉红；开启标尺后顶部工具栏/右上角按钮下移。

---

## 2026-08-19 · 标尺功能：PS 风格刻度尺 + 可拖出参考线（相册级持久化 + 参与对齐吸附）
>
> 用户需求：在相册编辑画布增加类似 **PS 的标尺功能**——顶部/左侧刻度尺 + 从标尺拖出的参考线；开关按钮放在**底部导航栏左侧「对齐」旁边**。

### 原因（现状）
- 画布此前**无标尺/参考线**实现，仅对齐引导线（`guidesLayer`）与静态辅助线。
- 参考线需是**编辑辅助**：不能写入页面数据（避免污染导出/缩略图/打印/undo），需独立持久化与渲染。

### 改动
**[types/album.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/album.ts)**：新增 `AlbumGuideLine`（id/orientation/position，position 为 **mm** 页面坐标），`AlbumProject` 增加 `guideLines?`。

**[store/editorStore/albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts) / [types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/types.ts)**：新增 `guideLines`（默认 []）+ `setGuideLines`。

**持久化（随项目保存/恢复）**：[EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx) 加载时 `setGuideLines(project.guideLines ?? [])`、保存时携带；[Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx)、[useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts)（Ctrl+S）保存携带；[db/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/db/index.ts) 自动保存扩展 provider + `scheduleAutoSave` 写入 guideLines。旧数据无此字段默认空，无需迁移。

**[store/uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)**：新增 `rulerEnabled`（默认关闭）+ `setRulerEnabled`，持久化 localStorage `membook_ruler_enabled`。

**[components/editor/BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)**：左侧「对齐」旁新增「**标尺**」胶囊按钮（i18n `editor.bottomNav.ruler/enableRuler/disableRuler`）。

**[components/editor/CanvasRulers.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CanvasRulers.tsx)**（新增）：PS 风格顶部横尺 + 左侧竖尺 + 角块，**固定于滚动容器视口边缘**（Canvas 主 return 外包一层 relative wrapper，滚动容器改 absolute inset-0；wrapper pointer-events-none、标尺条 pointer-events-auto）。刻度用 canvas 绘制，主刻度间距按 `canvasZoom` 自适应（≥60px 屏幕），页面内容范围高亮 + 边界线；随滚动/缩放/尺寸变化实时重绘。**在横尺/竖尺上按下拖动 → 拖出参考线**（实时回调页面逻辑 px 预览）。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**
- 新增**参考线 Layer**（Stage 顶层、内容层之上）：按 `groupOX/OY + 位置mm×MM_TO_PX×zoom` 正确坐标渲染（蓝色虚线），**拖拽移动**（dragEnd 一次提交，避免拖拽中重渲染抖动）、**双击删除**；从标尺拖出中的临时参考线以半透明预览。
- **对齐吸附接入**：`buildAlignTargets` 追加参考线目标（零宽/零高细长 bounds 表示单轴对齐点），仅标尺开启时参与（避免吸附到隐藏参考线）；参考线随对齐开关门控生效。
- **修复历史偏移 bug**：`updateGuideLines` 引导线坐标补加 `groupOX/groupOY`（引导线 Layer 在 Stage 顶层，此前漏加导致对齐引导线整体偏离页面内容 offset）。

### 效果
- 底部导航左侧「对齐」旁「标尺」按钮开启/关闭标尺与参考线，默认关闭、状态持久化。
- 顶部/左侧刻度尺随缩放平移正确变化，可拖出参考线、拖拽移动、双击删除；参考线跨页共享、随相册保存，不参与导出/缩略图/打印；开启标尺时参考线作为对齐吸附目标。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- `npx vitest run`：27 文件 292 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：开启「标尺」后视口顶部/左侧出现刻度尺且随滚动/缩放正确；从标尺拖出参考线、拖拽移动、双击删除；切页/重进编辑器参考线保留；拖动元素靠近参考线时吸附 + 显示引导线；关闭标尺后参考线隐藏。

---

## 2026-08-19 · 对齐系统优化：底部导航左对齐开关 + 对齐扩展到全部元素类型
>
> 用户需求：相册编辑模式的对齐系统（拖拽/缩放时自动吸附 + 对齐参考线）存在不合理设计，需整体优化、体验更好，并支持用户**手动开启/关闭对齐功能**——开关放在**底部导航栏、左对齐**。

### 原因（现状问题）
- **动态对齐/吸附无开关**：`findSnap`（alignment-engine.ts）在照片位拖拽/缩放时永远开启，仅按住 Alt 键临时禁用——多数用户不知道 Alt，也无法全局关闭。
- **对齐只覆盖照片位**：`findSnap` 仅挂在照片槽的 `onDragMove`/缩放；文字/便利贴/贴纸/形状/多选组拖拽时无吸附、无引导线，体验不一致。
- **引导线/开关语义混乱**：静态「边距/中线/三分线」开关藏在页面设置弹窗；动态对齐引导线不受任何开关控制，两者叠加易混淆。
- **开关位置不合理**：无统一入口，开关埋在设置弹窗深处。

### 改动
**[store/uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)**：新增 `alignEnabled`（默认 `true`）+ `setAlignEnabled`，持久化 localStorage `membook_align_enabled`。

**[components/editor/BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)**：工具栏改为 `justify-between`，**左侧新增「对齐」胶囊按钮**（对齐图标 + 文案，品牌色高亮 = 开启，与「页面/网格视图」按钮同风格），点击切换、即时生效。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**
- **对齐开关接入**：照片位 resize/拖拽的 `findSnap` 调用按 `alignEnabled` 门控——关闭时不吸附、`clearGuideLines()`；`useEffect` 监听开关关闭时清掉残留引导线。
- **`buildAlignTargets` 全元素化**：对齐目标从「仅其他照片槽」扩展为「照片位（含 extraSlots）+ 文字/便利贴/贴纸/形状」全元素（text/sticky 左上角、sticker/shape 中心，统一转页面逻辑像素左上角基准），照片位与元素共用同一目标集（拖拽照片位也能对齐到文字/贴纸等）。
- **新增稳定 `alignDrag` 回调**（useCallback，经 ref 持有最新 updateGuideLines/clearGuideLines/buildAlignTargets，避免陈旧闭包与 memo 击穿）：封装「开关门控 + Alt 临时禁用 + findSnap + 更新引导线 + 返回吸附偏移」，传给 4 类装饰节点与多选 hook。

**[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx) / [StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx) / [StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx) / [ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx)**：各节点新增 `alignDrag` prop + `alignCandidate` 辅助（按自身坐标语义算包围盒：text/sticky 左上角、sticker/shape 中心），拖拽移动/结束时叠加吸附偏移（text/sticky/shape 写 store 时应用；sticker 不写 store、直接写 Konva 节点位置再于 dragEnd 读取提交，与照片位行为一致）。

**[canvas/useMultiElementGroupSelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useMultiElementGroupSelect.tsx)**：多选组移动时以组包围盒检测对齐，偏移叠加到移动增量；排除全部选中元素 id（避免组内自对齐）。

**[i18n/locales/zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)**：新增 `editor.bottomNav.align`（对齐/Align）、`enableAlign`（开启对齐吸附与参考线）、`disableAlign`（关闭对齐吸附与参考线）。

### 效果
- 底部导航栏左侧「对齐」开关一键开/关全部对齐能力（吸附 + 引导线），默认开启，状态持久化；按住 Alt 仍可临时禁用。
- 文字/便利贴/贴纸/形状/多选组拖拽时与照片位一致：可对齐页面边/中线/边距/其他元素，实时显示引导线并吸附。
- 照片位与装饰元素共用同一对齐目标集，拖照片位也能对齐到文字等元素，全画布对齐体验统一。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- `npx vitest run`：27 文件 292 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：底部导航左侧「对齐」按钮可切换；开启时拖动文字/贴纸/便利贴/形状/多选组有吸附 + 引导线；关闭后无吸附、无引导线；重开应用记住开关状态。

---

## 2026-08-19 · 翻页预览性能优化：渐进式渲染——首屏快进 + 后台分批热替换
>
> 用户需求：主页 → 相册 → 点击相册书左上角预览按钮进入预览后，封面加载出来，点击封面想进入翻页时，若相册页面很多要等很久才进得去。希望优化这段等待时间。

### 原因
- **串行全量渲染**：原 `boot()` 先渲染封面/封底，随后把**全部内容页一页页渲染完**才创建翻页书（PageFlip）——页面越多累计耗时越长（每页从 IndexedDB 读照片、解码、绘制 1440px 画布），期间点击封面被忽略，体验是"等很久才进得去"。
- 预览渲染用 `noCache:true`，每次打开都全量重算。

### 改动
**[components/editor/BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **首屏快进**：封面/封底渲染后，只渲染**首批 `FIRST_BATCH=8` 张内容页**即创建翻页书；未渲染内容页先用浅灰「加载占位页」`LOADING_PAGE_DATAURL` 填充（页数完整、可立即创建翻页书），首屏对开 [衬页|内容1] 即可真实显示。
- **后台分批热替换**：其余内容页按批（每批 8 张）后台渲染，页间 `await new Promise(r=>setTimeout(r,0))` 让出主线程防卡顿；每批完成 `pageImagesRef` 更新后 `flip.updateFromImages(...)` 热替换。翻页进行中（`getState() !== 'read'`）先置 `pendingUpdateRef`，`changeState` 回 `'read'` 静止后再替换，不打断翻页动画。
- **就绪前点击不丢失**：翻页书未创建时点封面/封底（`toFlipFromCover`/`openFromBack`）走 `pendingOpenRef` 标记，翻页书创建完成（`boot` 尾部）自动 `openFlipBook`，不再静默忽略。
- **渲染进度提示**：新增 `renderProgress` 状态 + 底部「正在生成预览页面 x/N」浅色胶囊（`pointer-events-none` 不挡书不拦点击），全部渲染完清除。
- **渲染函数重构**：`renderAllPages` → `renderPageBatch`（按批预加载本批照片 + 页间让出主线程 + 逐页进度回调），封面/封底仍走 `renderSinglePage`。

**[i18n/locales/zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)**：新增 `editor.bookPreview.generatingPages`（「正在生成预览页面 {{current}}/{{total}}」/「Generating preview pages {{current}}/{{total}}」）。

### 效果
点击封面进入翻页的等待时间，从「全部内容页渲染完」缩短到「封面 + 首批 8 页」即可进入翻页书；其余页面在进入翻页后后台分批生成、热替换，并有进度提示，不打断翻页动画。

### 验证
- `npm run build`（tsc -b && vite build）：0 错误。
- `npx vitest run`：27 文件 292 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：多页面相册点封面能快速进入翻页；进入后未渲染页显示浅灰占位，随后逐批变为真实内容；翻页进行中热替换不打断动画；底部显示「正在生成预览页面 x/N」直至完成。

---

## 2026-08-19 · 书脊相册名/日期文字默认字体改为应用默认「思源黑体」
>
> 用户需求：书脊上的相册名称、日期文字字体改为**默认字体「思源黑体」**（与应用新建文字元素默认字体一致）。

### 改动
**[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`buildSpineElements` 中 `mk` 的 `fontFamily` 由 `'Helvetica Neue', Arial, sans-serif` 改为 `'思源黑体'`（新创建的书脊名称/日期使用默认字体）。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：`applySpineSettings` 确认时，对书脊文字（`spine-text-*`）若 `fontFamily` 仍为旧默认 `'Helvetica Neue', Arial, sans-serif` 则迁移为 `'思源黑体'`（只迁移旧默认，**用户手动改过的字体保留**），使既有相册下次确认封面设置即生效。

### 效果
书脊相册名/日期文字使用应用默认字体「思源黑体」，画布/导出/缩略图一致；新相册直接生效，旧相册确认一次封面设置后生效。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 292 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：书脊相册名/日期字体为思源黑体；打开封面设置点确认后旧相册也切换为思源黑体。

---

## 2026-08-19 · 日期文字对齐调整为「水平居中 + 垂直底部」+ 文字属性面板字段统一
>
> 用户调整：日期文字对齐从「水平右对齐」改为「**水平居中 + 垂直底部对齐**」（`align:'center'` + `verticalAlign:'bottom'`，位置底边距 15mm 不变）；并把文字属性面板「对齐」标签改为「**水平对齐**」，对齐/垂直对齐按钮在中文界面下悬浮显示中文提示。

### 改动
**[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`buildSpineElements` 的 `mk` 增加 `verticalAlign` 参数；日期创建改为 `align:'center', verticalAlign:'bottom'`（底边距仍固定 `SPINE_DATE_BOTTOM_MM=15`）。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：`applySpineSettings` 确认时日期改为 `align:'center', verticalAlign:'bottom'` + `y=页高−15−盒高`（处理旧数据）。

**[components/editor/panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：水平对齐/垂直对齐按钮 `title` 从 `key`（英文 left/center/right/top/center/bottom）改为 i18n 键——水平用 `alignLeft/alignCenter/alignRight`、垂直用 `alignTop/alignMiddle/alignBottom`（中文：左对齐/居中/右对齐、顶端对齐/垂直居中/底端对齐）。

**[i18n/locales/zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)**：`editor.tools.align` 文案「对齐→水平对齐」/「Align→Horizontal Align」。

### 效果
- 日期文字基于文本框**水平居中 + 垂直底部**（竖排语义：align=水平、verticalAlign=垂直），底边距仍固定 1.5cm。
- 文字属性面板字段统一：左/右侧对象面板同步显示「水平对齐」，按钮悬浮提示中英文本地化。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 292 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：书脊日期水平居中且贴文本框底部、距底边 1.5cm；文字面板显示「水平对齐」，悬浮按钮提示为中文。

---

## 2026-08-19 · 书脊日期文字右对齐 + 底边距固定 15mm + 修复打开封面设置日期下移
>
> 用户需求：日期文字对齐从「居中」改为「**右对齐**」（只改水平对齐 `align`，垂直对齐 `verticalAlign` 保持居中不动）；位置与顶部 logo 镜像——**距下边缘固定 1.5cm（15mm）**；并修复「打开封面设置后日期往下移动一点」的 bug。

### 原因
- **日期下移 bug 根因**：日期 y 公式三处不一致——新建（buildSpineElements）用 `底边距 = 书脊宽×0.7`（y=页高−X−盒高）；预览（renderTextForSpine）/确认（applySpineSettings）却用 `y=页高−X−盒高/2`（把 y 当中心）→ 打开封面设置（预览激活）时日期整体下移 盒高/2。
- **右对齐一致性隐患**：编辑器显示层 TextDomNode 对**竖排**文字把 `align` 当"垂直对齐"、`verticalAlign` 当"水平对齐"，与共享排版 `buildTextLayout`（导出/缩略图）及封面预览**相反**——直接设 `align:'right'` 会在编辑器不显示、导出却偏移（四端不一致）。另 `buildTextLayout` 竖排右对齐公式多减 `fs`（右对齐反而左移）需修正。

### 改动
**[utils/sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)**：新增共享常量 `SPINE_DATE_BOTTOM_MM = 15`（日期底边距，与 logo 顶边距 `SPINE_LOGO_TOP_MM` 镜像对称；三处共用禁止硬编码）。

**[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`buildSpineElements` 的 `mk` 增加 `align` 参数；日期以 `align:'right'` 创建、`dateCenterY = 页高 − SPINE_DATE_BOTTOM_MM − 盒高/2`（删除失效的 `logoTopOffset=书脊宽×0.7`）。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：`renderTextForSpine` 预览日期 `y = 页高 − SPINE_DATE_BOTTOM_MM − 盒高`（y 为盒顶，修正原 `−盒高/2` 的下移 bug，与数据/确认一致）。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：`applySpineSettings` 确认时对日期**固定 `align:'right'` + `y=页高−SPINE_DATE_BOTTOM_MM−盒高`**（处理旧数据），书脊宽度变化才重新居中 x/收窄盒宽（公式与预览一致）。

**[components/editor/canvas/TextDomNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextDomNode.tsx)**：竖排文字 `align/verticalAlign` 映射统一——`justify`(垂直) 由 `verticalAlign` 决定、`alignItems`(水平) 由 `align` 决定（与 buildTextLayout/封面预览同源）；现有竖排均为 center/center 无视觉变化，仅使非默认值对齐一致。

**[utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**：修复竖排右对齐公式多减 `fs`（`tx+tw−fs−pad−maxX` → `tx+tw−pad−maxX`，与左对齐对称）。

**[utils/thumbnailCore.textLayout.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.textLayout.test.ts)**：新增竖排右对齐断言（块右缘贴盒内容右缘，且比居中更靠右）。

### 效果
- 日期文字**水平右对齐**（垂直对齐仍居中）、**底边距固定 15mm**（随书脊宽度变化位置相对固定，像 logo 一样）；编辑器/导出/缩略图四端一致。
- 打开封面设置**不再把日期往下移**（预览/确认/数据三处公式统一）。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 292 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：打开封面设置 → 日期不上下跳动、距底边固定 1.5cm、右对齐（垂直仍居中）；确认后保持；导出与画布一致。

---

## 2026-08-19 · 修复：书脊文字双击编辑时控制变形框/工具栏/多选包围盒位置错位
>
> 用户反馈：书脊相册名/日期文字居中后，**正常显示位置正确，但双击进入编辑时选中变形框（虚线框 + 控制点）位置不对**，框和文字对不上。

### 原因
书脊文字在画布有**两条坐标路径**，显示层改对了、交互层漏了：
- **显示层**（Konva 选中框 + DOM 文字层）统一走 `renderTextForSpine`：书脊文字不加 delta、直接用数据坐标（居中后），封面区文字 +delta → 显示正确。
- **编辑控制框**（`TextEditHandles`）、**文字浮动工具栏**、**多选包围盒**却仍走 `shiftTextForSpine`（对**所有**文字 `x + delta`，delta = 当前书脊宽 − 锚点）→ 书脊宽 ≠ 锚点（调整过书脊宽度）时被多偏移 delta，框与显示文字分离。

### 改动
**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
- 编辑控制框（TextEditHandles）：改用 `renderTextForSpine(editingEl)`（L2947），写回逆偏移仅对封面区文字生效 `!id.startsWith('spine-text-') && spineDeltaMm ? unshiftPatch(p) : p`（L2958-2959）。
- 文字浮动工具栏：改用 `renderTextForSpine(el)`（L2971）。
- 多选包围盒（text 分支）：改用 `renderTextForSpine(el)` 取渲染坐标（L3456-3457）。

### 效果
书脊文字在**预览/确认后/编辑态**坐标全部统一走 `renderTextForSpine`——双击进入编辑时，控制变形框、文字浮层、选中框与显示文字完全重合；书脊宽 ≠ 锚点时不再错位。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：调整书脊宽度后 → 双击书脊相册名/日期 → 编辑框与文字重合、拖控制点缩放/旋转位置正确；选中书脊文字浮动工具栏、书脊文字参与多选时包围盒位置正确。

---

## 2026-08-19 · 书脊相册名/日期文字也居中于书脊（确认时数据层重新居中，与 logo 一致）
>
> 用户确认：书脊处的**相册名称与日期文字**也要像 logo 一样，在书脊宽度变化时**始终水平居中于书脊**（随书脊中心移动），而不是「保持绝对位置不动」。此前改为「固定不动」是错误方向，需恢复居中。

### 原因
- 上一轮（本日志下方）曾把书脊文字改为「统一按 delta 渲染右移（绝对位置固定）、确认时不重排」——但预览端 `renderTextForSpine` 仍实时居中，确认后却不居中，预览/确认不一致；导出端因 renderPage 整体平移 (书脊宽−锚点)，书脊文字也被右移 delta → 三端都不居中。
- 用户重新确认预期：书脊文字（`spine-text-*`，相册名/日期）应**居中于书脊**，与 logo 行为一致。

### 改动
**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：`applySpineSettings` 在**书脊宽度变化**时（仅改色/Logo 不动位置）对 `spine-text-*` 做**数据层重新居中**——`x = 新书脊宽/2 − 盒宽/2`（盒宽 `min(原宽, 新宽)`，超宽收窄）、日期 `y = 页高 − 新书脊宽×0.7 − 盒高/2`（与画布预览 `renderTextForSpine` 公式完全一致）；新增 `pageHeightMm` 参数（取 `albumSize.height`）。确认后数据即居中，画布（非预览）/缩略图/导出读数据即一致居中。

**[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：
- `drawPage` 对书脊文字（`spine-text-*`）**逆补偿 delta**（绘制 x = 数据 − (书脊宽−锚点)），抵消 renderPage 的整体平移，使导出书脊文字与画布一致居中于书脊（与 logo 补偿同理）。
- 修复**书脊底色色块偏移**：renderPage 的书脊底色 `fillRect` 原在整体平移后从 0 起画（书脊宽≠锚点时色块整体右移、左侧露页面背景），改为绘制 x = `(锚点 − 书脊宽)×MM_TO_PX`（与 logo/书脊文字同坐标系，最终落在逻辑 0..书脊宽），画布/导出两端一致。

### 效果
- 调整书脊宽度并确认后，书脊上的**相册名/日期始终水平居中于书脊**（随书脊中心移动）；封面区内容仍固定不动（锚点模型不变）；画布/导出/缩略图三端一致；logo 行为不变。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：封面设置 → 书脊宽度增减 → 预览/确认后书脊相册名与日期始终居中于书脊、封面区内容不动；导出与画布一致。

---

## 2026-08-19 · 修正：书脊 logo 由「固定不动」改为「居中于书脊 + 尺寸自适应（封顶 12mm）」
>
> 用户确认：logo「固定不动」不是想要的效果。期望 logo 在书脊宽度变化时**始终居中于书脊**（随书脊中心移动）、**尺寸随书脊自适应**（`min(书脊宽×0.6, 12)` 封顶）；顶部距页边保持固定的 15mm（SPINE_LOGO_TOP_MM）。

### 改动
**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：logo 渲染恢复居中于书脊——`x = ((书脊宽 − logoW)/2)`、`logoW = min(书脊宽×0.6, 12)`、`y = SPINE_LOGO_TOP_MM`（15mm）。

**[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：logo 同步恢复居中——`logoX = 锚点 − 书脊宽/2 − logoW/2`（当前坐标系已平移 (书脊宽−锚点)，绘制 x = 居中位置 − 平移）、`logoW = min(书脊宽×0.6, 12)`、`y = SPINE_LOGO_TOP_MM`，与画布一致。

### 效果
- 书脊变宽时：logo 保持在书脊正中（随书脊中心移动）、尺寸按书脊宽度自适应放大（封顶 12mm）、距顶边固定 15mm。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。

---

## 2026-08-19 · 修复：文字显示层（DOM）左移 + logo 固定（顶边 15mm）+ 全量渲染坐标统一
>
> 用户确认最终预期：文字分「书脊区（相册名/日期）」与「封面区（封面文案）」，调整书脊宽度时**所有文字都不移动**（与缩略图/网格/全屏一致）；书脊顶部 logo 固定不动，始终位于**顶边向下 15mm**。

### 真正的根因（上一轮修漏了文字 DOM 显示层）
- 文字在画布有**两个渲染层**：Konva 选中框层（TextElementNode）上一轮已加 delta（固定）；但**真正显示文字的 DOM 层（TextDomNode）用的还是原始数据坐标（未加 delta）** → 书脊向左扩展时 groupOX 补偿，可见文字往左移动，而选中框不动（框/字分离）。这就是用户看到"文字左移"的根因。
- 另有多处 DOM 定位（文字编辑手柄、文字/便利贴/贴纸/形状/多选浮动工具栏、便利贴编辑浮层）同样用原始坐标，选中/编辑时会与元素错位。

### 改动
**[utils/sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)**：新增共享常量 `SPINE_LOGO_TOP_MM = 15`（logo 距页面顶边固定距离，画布/导出共用）。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
- 组件级辅助函数 `shiftTextForSpine`（渲染坐标 = 数据 + delta）与 `unshiftPatch`（写回逆偏移），全组件共用，删除 items 闭包内的重复实现。
- **文字 DOM 显示层（TextDomNode）改用 `shiftTextForSpine`**（数据 + delta）→ 可见文字固定不动；文字编辑手柄（TextEditHandles）同步 + delta 且写回逆偏移；文字浮动工具栏改用渲染坐标定位。
- 便利贴编辑浮层、便利贴/贴纸/形状/多选浮动工具栏均改用渲染坐标（封面正面 + delta，与各自 Konva 渲染一致）。
- **logo 固定**：尺寸 `min(锚点宽×0.6, 12)`（按原始书脊宽，不随当前宽度缩放）、水平居中于原始书脊（折线锚点）、顶部距页边 `SPINE_LOGO_TOP_MM=15mm`，渲染坐标 + delta 抵消 groupOX 左移 → 书脊宽度调整时 logo 不移动。
- **放置坐标补偿**：画笔/文字/形状点击放置、贴纸拖拽放置的数据坐标统一 `- delta`（渲染层再 +delta，保证新元素落到点击处；书脊宽 ≠ 锚点时不再偏移）。

**[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：logo 同步固定——尺寸按锚点、绘制 x = `1.5×锚点 − 书脊宽 − logoW/2`（抵消内容整体平移）、y = `SPINE_LOGO_TOP_MM`，与画布一致。

### 效果
- 书脊宽度调整时：封面所有内容（照片/形状/文字/logo）均固定不动，仅书脊向左扩展；工作区与导出/缩略图一致；新添加元素落位准确。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：调整书脊宽度 → 书脊区/封面区文字均不动、logo 固定在顶边 15mm 不动；选中/编辑文字、拖入贴纸位置正确。

---

## 2026-08-19 · 修复：书脊宽度调整时文字位置移动（书脊文字不再重新居中，统一固定不动）
>
> 用户反馈：调整书脊宽度时，照片位、形状保持原位，但**文字**（书脊上的相册名/日期）在工作区画布中会移动——要求文字与其他内容一样固定不动。

### 原因
- 会移动的是**书脊自动文字**（`spine-text-*`，相册名/日期）。它们被设计为「水平居中于书脊」：书脊宽度一变就重新居中（预览时画布居中、确认时数据重排）。书脊向**左侧**扩展时书脊中心随之左移，文字跟着左移 → 看起来文字位置在变化。
- 缩略图/全屏/网格（`drawPageToCanvas`）把书脊**整体裁掉**、只显示封面正面，书脊文字被裁出画布不可见 → 那里"文字不动"其实是根本看不见；工作区画布会显示书脊，所以看到文字在动。
- 导出端（`renderPage` 整体平移）本就按「固定偏移」处理书脊文字，与工作区"重新居中"不一致。

### 改动
**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：文字渲染改为**所有文字（含 `spine-text-*`）统一按 delta 渲染右移**（绝对位置固定），删除书脊文字的预览居中分支与 `isCoverFrontEl` 判定；`onUpdate` 逆偏移对全部文字生效。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：`applySpineSettings` 移除 `repositionSpineTexts`（书脊文字不再按新宽度重新居中），删除不再使用的 `repositionSpineTexts` 函数、`pageHeightMm` 参数与 `PageTextElement` 引用。

### 效果
- 书脊宽度调整时，封面所有内容（照片/形状/文字含书脊文字）均保持绝对位置不动；工作区与导出行为一致。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：调整书脊宽度 → 书脊文字与封面内容均不动、书脊向左扩展。

---

## 2026-08-19 · 书脊宽度调整改为「书脊向左扩展、封面内容固定」（锚点模型，替代内容右移）
>
> 用户再次拍板采用更简单的「书脊从左边增加」方案：书脊变宽时，新宽度从书脊**左侧外沿**向外扩展，封面正面内容位置**永远固定**。
> 实现为**锚点模型**：内容数据坐标不再随书脊宽度变化移动，各渲染端统一按 `(当前书脊宽 − 锚点)` 偏移内容；预览与确认后状态共用同一逻辑。

### 需求确认（反转首条日志「内容右移」决策）
- 用户提出：书脊宽度增加时从书脊左边增加，右侧封面内容位置固定不偏移，这样更简单。
- 分析确认：预览模式本就实现该视觉（书脊向左凸、封面内容稳定、折线不动），只是「确认」时把内容真正右移写进了数据；本次把预览行为固化为**永久行为**——内容数据永不移动，删掉 `applySpineSettings` 的内容平移逻辑。

### 核心设计
- 新增 `AlbumPage.spineAnchorMm`（书脊偏移锚点 = 内容烘焙偏移量 / 折线位置）；`applyCoverTemplate` 写入 = 内容偏移量；旧数据缺省回退为当前 `spineWidth`（其烘焙偏移即当前书脊宽），**无需迁移**。
- 书脊宽度变化时内容数据不再移动；各渲染端统一按 `(当前书脊宽 − 锚点)` 偏移封面正面内容（书脊区域元素/书脊文字保持原位或按新宽居中）。
- Canvas 的 `spineDeltaMm` 由「预览专用」改为「永久」；`spineAnchorPxRef`（进入预览瞬间临时锚点）+ 两个 effect 移除，groupOX 补偿改用稳定的 `spineAnchorPx`（锚点×MM_TO_PX）。

### 改动
**[types/photo.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/photo.ts)**：`AlbumPage` 新增 `spineAnchorMm?: number`。

**[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`applyCoverTemplate` 写入 `spineAnchorMm = contentOffset`。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：`applySpineSettings` **删除内容右移逻辑**（文字/形状/贴纸/便利贴/笔触/槽位不再平移），仅写入书脊底色/宽度/Logo 颜色 + 冻结锚点 + 书脊文字按新宽度居中；删除不再使用的 `isSpineText` 与 `MM_TO_PX` 引用。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
- `spineWidthMm` 提前定义（顶部统一计算）；`spineDeltaMm = 当前书脊宽 − 锚点`（永久，非仅预览）。
- 封面正面判定 `x >= 旧书脊宽` 全部改为 `x >= spineAnchorMm`；书脊文字预览居中改用 `spineWidthMm`。
- 移除 `spineAnchorPxRef` 及其 effect，groupOX 补偿改用稳定 `spineAnchorPx`（预览/确认共用）。
- 槽位/笔触/文字/便利贴/贴纸/形状的渲染偏移与 onUpdate/onMove 逆偏移逻辑复用，仅由「预览时生效」变为「始终生效」。

**[utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**：封面去书脊偏移由 `spineWidth` 改为 `spineAnchorMm ?? spineWidth`。

**[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：`renderPage` 在出血/装订平移后整体平移 `(书脊宽 − 锚点)×MM_TO_PX`（背景/书脊底色/内容/logo 同坐标系连续；锚点 == 书脊宽即旧数据时平移为 0，行为不变）。

**[services/pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts)**：`calcCoverOverrides` 槽位偏移由 `spineWidth` 改为 `spineAnchorMm ?? spineWidth`。

**[store/editorStore/albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts)**：`rescaleCoverDecorations` 缩放锚点 `spX` 由 `spineWidth` 改为 `spineAnchorMm ?? spineWidth`。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：封面设置 → 书脊宽度增减 → 画布封面内容位置不变、书脊从左侧扩展/收缩、书脊文字居中；确认后内容仍在原位；导出/缩略图与画布一致。

---

## 2026-08-19 · 封面设置书脊优化：顺序调整 + 内容随书脊右移（预览实时）+ Logo 封顶 + 书脊文字居中 + 减跳动
>
> 封面设置书脊区四项优化：
> 1. **排版顺序**：书脊区改为「书脊宽度 → Logo 颜色 → 书脊底色」。
> 2. **书脊宽度增加时封面内容位置**：保持相对封面区域位置不变（书脊变宽 → 封面正面内容整体随书脊右移 delta，不被书脊盖住）。
> 3. **Logo 尺寸封顶**：Logo 宽 = min(书脊宽×0.6, 12mm)，书脊很宽时不再膨胀。
> 4. **书脊相册名称/日期自动居中** + 顺带优化调宽度时封面内容的跳动感。

### 需求确认（用户澄清）
- 用户曾提出「书脊从左边增加、封面内容不动」的更简单想法；经分析该方案需重构书脊坐标模型（书脊元素定位/导出/打印/缩略图都改），实现更复杂。**确认保持「内容右移」**（印刷一体、导出一致、物理正确）。
- 跳动感优化：确认顺带做。

### 改动
**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：
- 书脊区顺序：宽度 → Logo 颜色 → 底色。
- 新增 `applySpineSettings`：确认时封面页写入底色/宽度/Logo 颜色，且书脊宽度变化 delta 时——**封面正面内容**（文字/形状/贴纸/便利贴，`x >= 原书脊宽`）右移 delta、槽位 slotOverrides 右移 `delta×MM_TO_PX`、笔触偶索引右移 delta；**书脊区域元素**（`x < 原书脊宽`，含用户手动添加）保持原位；书脊自动文字（`spine-text-*`）按新宽度居中。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
- 预览实时偏移：`spineDeltaMm = 预览书脊宽 - 原书脊宽`。封面正面内容/槽位/笔触/文字/便利贴/贴纸/形状渲染坐标右移 delta（书脊区域元素与书脊文字除外）；书脊文字（spine-text-*）按预览宽度实时居中（含日期按新宽度重定位）。
- 写回防污染：封面正面元素的 `onUpdate`/`onMove` 对 x 做逆偏移（减 delta），避免预览时拖拽把预览偏移写进页面数据。
- Logo 封顶：`logoWmm = min(书脊宽×0.6, 12)`，顶部边距 = 书脊宽×0.7（mm）。
- **跳动感优化**：`spineAnchorPxRef` 记录进入预览瞬间的书脊/封面交界线；预览中书脊变宽时补偿 `groupOX`（`+（锚点 − spinePx）× zoom`），使封面内容在视口中保持稳定，视觉上书脊向左凸出、封面内容不动。groupOX 被渲染/点击坐标/工具栏/DOM 浮层共用，补偿自动全链路一致。

**[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：Logo 封顶 12mm 与画布同步（导出数据为确认后的正式内容，无需额外偏移）。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：封面设置 → 书脊宽度滑块 → 画布封面内容随书脊右移、书脊文字居中、Logo 不再膨胀、视觉上封面内容稳定（书脊向左凸出）；确认后内容正式右移 + 文字居中。

---

## 2026-08-19 · 修复：封面设置面板点击画布空白即消失
>
> 打开封面设置右侧面板后，点击一下画布空白区域，面板就消失了——不符合预期，期望面板保持打开、画布可正常操作。

### 原因
上一轮实现「点面板外自动取消」：挂载了 `document` 级 `mousedown` 监听，点击画布（面板外）即触发 `handleCancel` 关闭面板并清预览。同时右侧面板沿用了居中弹窗的 `ModalGuard`，它把 `body` 设为 `pointer-events:none`，会屏蔽画布交互。

### 改动
**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：
- **移除「点面板外部自动取消」**：删除 `document` 级 `mousedown` 监听与 `panelRef`。面板仅通过「关闭按钮 / 取消按钮 / Esc」关闭，点击画布不会关闭面板。
- **移除 `ModalGuard`**：右侧面板是常驻设置面板（非遮罩弹窗），`ModalGuard` 会把 `body` 设为 `pointer-events:none` 屏蔽画布交互；移除后点画布可正常选中/拖动/缩放，同时预览仍实时生效。
- Esc 取消 / Enter 确认的 `useDialogHotkeys` 保留。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证：打开封面设置面板 → 点击画布空白/选中元素/切换页面，面板保持打开、画布正常操作；点关闭/取消/Esc 才关闭并恢复原状。

---

## 2026-08-19 · 封面设置整体 UI/UX 重做：右侧面板 + 画布实时预览（确认才生效）
>
> 「封面设置」由**居中遮罩弹窗**整体重做为**编辑器右侧设置面板**，并引入**画布实时预览**：打开面板后修改圆角 / 书脊底色 / 书脊 Logo 颜色 / 书脊宽度时，真实画布上的封面立即跟随变化（所见即所得）；点「确认」才正式写入页面数据（含书脊文字按新宽度重排），点「取消」/点画布其他处/按 Esc 则恢复原状。

### 需求确认（用户拍板）
- 布局：**右侧面板**，参考文字/形状工具的面板设计风格与颜色设置保持一致。
- 预览：**不在弹窗内画示意图，直接在真实画布上体现**，确认后才应用。
- 生效/回退：确认才生效（含书脊文字重排）；取消恢复原状。
- 圆角：封面/封底照片位圆角**保持统一**设置（不拆分）。

### 原因（现状 UI/UX 痛点）
- 居中遮罩弹窗会盖住画布中央，改参数看不到效果，只能「改→确认→关→看→再开」反复试错。
- 「书脊」概念无可视化；圆角与书脊两大块平铺、控件样式混搭（色板+hex/checkbox/数字/滑块混用）、字号间距不齐。
- 所有改动要等点「确认」才生效，无任何即时反馈。

### 改动
**[store/uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)**：新增非持久化 `coverSettingsOpen`（面板开关）、`coverPreview`（预览覆盖，含 `slotCornerRadius/spineColor/spineWidth/spineLogoColor`）及 setter。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：封面渲染数据改为**优先读预览覆盖**——书脊底色 `spineBgColor`、书脊宽度 `spineWidthMm`（预览实时改变画布总宽）、书脊 Logo 颜色 `spineLogoColor`、封面/封底照片位圆角 `slotCornerRadius`（预览圆角对封面/封底均生效）。仅当前页为封面/封底时生效；未打开面板时行为与之前完全一致。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：整体重写为右侧面板——
- 布局参考对象属性面板（`absolute right-0 top-0 bottom-0 z-50 w-[300px]` + 头部图标/标题/关闭 + 底部操作栏）；控件风格参考文字/形状面板（`text-[10px]` 标签、滑块+数字联动、`accent-brand` 滑块、focus ring）。
- 颜色用 `SpineColorField`（标准色 + 10 列色盘 + 自定义取色器，样式与 `ColorPalette` 纯色 Tab 一致，仅支持纯色 hex，避免渐变 css 污染 `spineColor`）。
- 分区：`封面 / 封底 · 照片位圆角`（统一/每角单独 + 滑块/数字）、`书脊`（底色 / Logo 颜色自动开关+色盘 / 宽度滑块+数字+mm）。
- 影子状态变化 → 实时 `setCoverPreview` 驱动画布；确认 → 写 `store.pages`（圆角+书脊+书脊文字 `repositionSpineTexts` 重排）+ 清预览 + 关闭；取消/点面板外/Esc → 清预览 + 关闭恢复原状；打开时清空对象选中（避免右侧对象面板重叠）。

**[components/editor/Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx)**：菜单入口改调 `useUIStore.getState().setCoverSettingsOpen(true)`，移除本组件内挂载。
**[components/views/EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx)**：画布容器内（`ObjectPropertiesPanel` 旁）挂载 `<CoverSettings />`。

**[i18n zh-CN/en-US]**：新增 `coverSettings.coverSection`（封面 / 封底）。

### 说明（书脊宽度预览的折中）
预览书脊宽度时**只改变书脊色块宽度 + 画布总宽**（封面正面内容与书脊文字不实时挪动，避免预览期大范围坐标重排）；确认后才正式写入新宽度并重排书脊文字。预览与最终布局在「书脊色带宽度」上保持一致。

### 验证
- `npm run build`：0 错误（tsc -b 真实校验 + vite 构建）。
- `npx vitest run`：27 文件 291 tests 全部通过。
- 需手动 `npm run desktop:dev` 验证交互：菜单 → 封面设置 → 右侧面板；改书脊底色/宽度/Logo 颜色/圆角，画布实时变化；确认后生效、取消后恢复。

---

## 2026-08-19 · 封面设置：书脊 Logo 颜色自定义 + 书脊底色渲染修复 + 书脊宽度可调
>
> 封面「书脊」设置三件事：
> 1. **新增书脊 Logo 颜色设置**——此前书脊顶部 MemBook logo 是自动黑/白（按书脊底色深浅二选一），用户无法自选颜色；期望自由指定 logo 颜色（并保留「自动」兼容旧行为）。
> 2. **修复书脊底色设置**——封面设置里已有「书脊底色」色块，但改动后画布/导出毫无变化（`spineColor` 是「只读不画」的死字段）；期望书脊区域真实显示所选底色。
> 3. **新增书脊宽度设置**——`spineWidth` 此前仅有数据无 UI；期望封面设置可调宽度(mm)，改宽后书脊文字自动重新居中。

### 原因
- 书脊底色不生效：画布把「书脊+封面正面」当**一整块连续背景**渲染（`PageBackgroundRect` 用 `page.background`），导出 `ctx.fillStyle = page.background` 整块铺满——`spineColor` 从未参与绘制，只被拿去选 logo 黑/白版。
- Logo 不能自定义颜色：logo 是两张单色位图（logo-light 黑线 / logo-dark 白线），位图本身无法直接改任意色，需「单色位图重着色」能力。

### 改动
**[types/photo.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/photo.ts)**：`AlbumPage` 新增 `spineLogoColor?: string`（未设置=按书脊底色深浅自动黑/白，兼容旧数据）。

**[utils/sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)**：新增共享三函数——`isDarkBackground`（由 canvas/constants.ts 上移，再导出保持原导入方不变）、`resolveSpineLogoColor`（取色：自定义优先，否则自动黑/白）、`tintMonochromeImage`（单色位图 `source-in` 重着色为任意色，返回同尺寸 canvas）。**画布/导出两端共用，禁止各自内联选色/改色**。

**[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
- 书脊底色独立渲染：整块背景之上叠一层 `Rect`（`x:0 width:spinePx` 高整页，`fill=spineBgColor`），与封面正面背景区分。
- logo 改着色：先加载 logo-light 单色位图，再按 `resolveSpineLogoColor(spineBgColor, spineLogoColor)` 用 `tintMonochromeImage` 着色后渲染（`spineTintedLogo`），替换原黑/白两图二选一；删除 logoDark 导入。

**[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：
- 书脊底色：`renderPage` 在 `drawPage` 前、装订偏移后，封面页逻辑坐标 `0..spineMm` 填充 `page.spineColor`（仅设置了才填，未设置沿用整页背景）。
- logo 改着色：模块级缓存单色位图 `loadSpineBaseLogo`，`tintMonochromeImage` 着色后 `drawImage`；删除 `spineLogoSrc` 与 logoDark/isDarkBackground 相关导入。printEngine 复用同一 `renderPage` 自动同步。

**[components/editor/CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**：
- 书脊区新增「Logo 颜色」：`自动` 复选（勾选=自动黑/白，不写 `spineLogoColor`）+ 自定义色板/十六进制输入。
- 书脊区新增「书脊宽度(mm)」输入（6~60，默认当前宽度）。
- 确认写入：`spineWidth` / `spineColor` / `spineLogoColor`（自动则 undefined）；宽度变化时用 `repositionSpineTexts` 重排书脊自动文字（`spine-text-*` 水平重新居中于新宽度、日期元素按新宽度重定位：底边距=新宽×0.7）。

**[i18n zh-CN/en-US]**：新增 `coverSettings.spineLogoColor / spineLogoAuto / spineWidth`。

**[Readme.md / MemBook 代码开发规范.md]**：封面/封底设计系统与 9.6 封面/书脊域规范同步更新（书脊底色独立渲染、logo 着色共享函数、书脊宽度可调）。

### 验证
- `npm run build`：0 错误（tsc -b 真实校验 + vite 构建）。
- `npx vitest run`：27 文件 287 tests 全部通过。
- 说明：缩略图/网格/全屏按约定不显示书脊，未改动（用户确认保持现状）。

---

## 2026-08-18 · 照片整理·无日期列表：缩略图拉伸到与右侧信息区等高
>
> 上一版缩略图固定 56×56px，右列信息（文件名/日期输入/按钮）约 90px 高，缩略图显得偏小、左右不齐。期望：**缩略图高度与右侧信息区对齐**（用户确认走"拉伸填充等高"）。

### 改动
**[shared.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/shared.tsx)**：`ThumbImage` 增加 `aspect='fill'` 模式（容器 `h-full w-full`、图片 `object-cover` 铺满，替代固定 aspect），向后兼容不影响其它调用方。
**[ExifTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ExifTool.tsx)**：`NoDatePhotoRow` 缩略图容器由 `w-14 h-14` 改为 `w-14 self-stretch`，配合 `aspect="fill"` —— 高度随右侧信息区拉伸等高，图片 object-cover 铺满，识别色条仍贴底。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 287 tests 全部通过。

---

## 2026-08-18 · 照片整理·修改拍摄信息：无日期照片列表改自适应多列 + 加大缩略图
>
> 「修改拍摄信息-无拍摄日期照片列表」是单列卡片，每行只放一张 40px 缩略图 + 文件名 + 日期输入 + 按钮，横向空间大量浪费；缩略图偏小不便查看。期望：**列表改多列展示，缩略图加大**（用户确认：横向卡片、列数随宽度自适应、缩略图不锁死 2 倍取合适尺寸）。

### 改动
**[ExifTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ExifTool.tsx)**
- 列表容器由 `space-y-1.5` 单列改为**自适应网格** `grid-cols-[repeat(auto-fill,minmax(205px,1fr))]`：面板越宽列数越多（典型宽度约 3~4 列），窄时自动少列不拥挤。
- `NoDatePhotoRow` 重排为**横向卡片**：左侧缩略图加大 `w-14 h-14`（40→56px），右侧自上而下为「文件名（点击快捷复制）→ 日期输入框 → 无效日期/失败提示 → 按钮行」；识别状态改为缩略图底部色条（绿=已识别/深灰=未识别）。
- 新增**点击文件名复制**：`navigator.clipboard` + textarea 降级，成功弹 toast。

**[zh-CN.json / en-US.json]**：新增 `home.organize.exif.copyNameHint / copyNameToast`。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 287 tests 全部通过。

---

## 2026-08-18 · 修复：添加文字后未进入输入模式（改为同步进入编辑态）
>
> 点击画布放置文字后，本应**立即进入文字输入模式**（可直接打字），实际却停留在非编辑态，需再双击文本框才可输入。

### 原因
放置文字依赖 `setPendingTextEditId` → **effect 异步**进入编辑（需要多一次 commit 且受 effect 时序影响），存在放置后未即时进入编辑态的间隙/竞态。

### 改动
**[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**
- 文字落点处理器改为**同步 `setEditingTextId(el.id)`**（`editingTextId` 为本组件本地状态，可直接设置），并 `setPendingTextEditId(null)` 消除对异步 effect 的依赖；同时设置 `setTextEditCursorIndex(null)`。
- 元素在**同一 commit 即以编辑态挂载** → `TextDomNode` 直接 `contentEditable` 并聚焦，放置后光标即在框内可立即输入。

### 验证
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 287 tests 全部通过。

---

## 2026-08-18 · 文字编辑优化：空文本灰色占位提示（有输入即自动消失）
>
> 添加文字后**立即进入编辑**已生效（确认）。但空文本框的占位"双击输入文字"仅在**非编辑态**显示、且是作为真实 DOM 文本渲染；进入编辑态（空框）无任何提示，体验不直观。期望：**空文本时始终显示灰色占位提示（编辑/非编辑态均生效），一旦有输入则自动消失**。

### 改动
**[TextDomNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextDomNode.tsx)**
- 显示态 children 改为只渲染真实文本 `el.text`（去掉 `|| placeholder` 内联占位，避免占位被当成内容绘制）。
- 新增**绝对定位占位浮层**：当 `!el.text` 时显示灰色（`#999` + 斜体）"双击输入文字"，与文字框内边距、字号、字体、行高、字距、对齐/垂直对齐一致；`pointer-events:none` 不拦截输入。
- 占位由 **store 实时文本（`onLiveText`→`updateTextElement`）驱动**：用户输入第一个字符即自动隐藏，删除空后重新出现。编辑态（`contentEditable` 空框）也显示，提供输入提示。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json`：0 错误。
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 287 tests 全部通过。

---

## 2026-08-18 · 文字工具交互优化：点击画布落点生成文本框（替代固定位置）
>
> 原「添加文字」点击后**立刻在固定位置 (30,30)** 生成文本框并直接进入编辑，位置与用户意图脱节。期望：点击「添加文字」后，**指针变成文本输入光标（I 形）**，用户在画布上**单击**后，在**点击处**生成文本框；一次性（点一次放一个即退出）。

### 改动
**[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**
- 「添加文字」按钮由"直接生成"改为**工具开关**：点击进入文字模式（高亮），再次点击取消；不再新增固定坐标逻辑，删除原 `handleAddText`（含不再使用的 `addTextElement` 选择器与 `DEFAULT_TEXT_LINE_HEIGHT/LETTER_SPACING` 导入）。
- `AddButton` 增加 `active` 态样式；进入文字模式时显示 `textModeHint` 提示。

**[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**
- `isToolMode` 纳入 `'text'`：文字模式下内容元素 `listening=false`，点击穿透到 Stage，避免误选中已有元素。
- 画布光标在文字模式下显示 **I 形（`text`）**。
- Stage `onMouseDown` 新增 `activeTool === 'text'` 分支：按 `(pos - groupOY/OX)/zoom/MM_TO_PX` 换算**点击处为左上角**，生成文本框（宽 150、高随字号 `20*1.2+4`、沿用默认样式），选中并 `setPendingTextEditId` 进入编辑，随后 `setActiveTool('none')` **退出文字模式**（一次性）。

**[uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)**：切换离开「工具」面板时一并取消 `text` 模式。

**[zh-CN.json / en-US.json]**：新增 `editor.tools.textModeHint`。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json`：0 错误。
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 287 tests 全部通过。

---

## 2026-08-18 · 修复：字体下拉高度不遮挡底部缩略导航栏
>
> 上一个版本高度自适应只按窗口高取 `below/above` 较大一侧，**未扣减底部缩略导航栏**（90–280px，可拖拽），面板贴底展开时会把缩略导航盖住。期望：**下拉面板永不遮挡底部缩略导航栏**。

### 改动
**[FontFamilySelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/FontFamilySelect.tsx)**
- 引入 `useUIStore((s) => s.bottomNavHeight)`，以「窗口高 − `bottomNavHeight`」为**有效下边界 `viewBottom`**，`below` 基于 `viewBottom` 计算。
- 选定 top 后再**兜底钳制 `top + height ≤ viewBottom`**：即使 `below ≥ above` 分支被 `PANEL_MIN_HEIGHT` 撑高，也不越过有效下边界 → 缩略导航栏始终可见可操作。
- 导航高度为响应式状态，用户拖拽后再次展开即按最新值避让。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json`：0 错误。

---

## 2026-08-18 · 字体下拉：高度随可用空间自适应 + 列表滚动条

### 需求
字体下拉此前最大高度写死 `320`，不随右侧面板/窗口可用高度变化，面板高时列表"不够长、没利用空间"。期望：**长度（上下）根据右侧面板窗口可用高度自适应加长**，且**列表带可见滚动条**。

### 改动
**[FontFamilySelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/FontFamilySelect.tsx)**
- 面板高度自适应：`below`（按钮下方）/`above`（上方）取空间较大一侧，clamp 到 `[PANEL_MIN_HEIGHT=160, PANEL_MAX_HEIGHT=640]`，并 `top=max(8,top)` 防越界 → 高度越长越短跟随可用空间，始终不溢出。
- 宽度保持 `260` 不变。
- 列表容器改用动态 `maxHeight = pos.height - 44`，并加**样式化滚动条**（`scrollbar-width: thin` + `scrollbar-color`），长列表清晰可见、可滚动。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json`：0 错误。
- `npm run build`：0 错误。
- `npx vitest run`：287 tests 全部通过。

---

## 2026-08-18 · 字体下拉重设计：自定义列表（分组 + 搜索 + 字体预览 + 不溢出）

### 需求
字体下拉此前是原生 `<select>`，样式是浏览器默认、与本应用面板风格不搭；且 A+B 方案后选项达几百个，原生下拉**宽度约束不住、高度按内容撑开，向右/向下溢出右边面板**。期望重做成自定义列表：样式统一、限制高度滚动、宽度适配、不往外溢出。

### 改动
**[FontFamilySelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/FontFamilySelect.tsx)**（新增）
- 触发器按钮显示当前字体名**并以该字体预览** + 三角箭头，样式与面板一致。
- 弹出面板：fixed 定位锚定按钮下方，**最大高度 320px 内部滚动**、**固定宽 260px**，按可视区 clamp（右缘不够右对齐、底部不够向上翻）→ 不溢出面板/窗口。
- **分组小标题**：内置艺术 / 系统可用 / 本机字体。
- 每项以自身字体渲染（所见即所得）；**顶部搜索框**按名过滤。
- 点击外部 / Esc 关闭；展开触发 `onOpen`（加载本机字体）。

**[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**
- 用 `<FontFamilySelect>` 替换原生 `<select>`，透传 `value`/`fontList`/`localFonts`/`onOpen`。

**[i18n]**：新增 `fontGroupBundled/System/Local`、`fontSearchPlaceholder`、`fontNoMatch`。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json`：0 错误。
- `npm run build`：0 错误。
- `npx vitest run`：27 文件 / **287 tests 全部通过**。

---

## 2026-08-18 · 文字字体支持本机已装字体（A+B 方案）

### 需求
文字功能字体下拉目前是固定硬编码名单（系统字体名 + 内置艺术字体）。希望**直接加载用户电脑上已安装的字体**（含用户自装的第三方字体），并能直接选用。用户确认走 A+B 方案。

### 现状
- 下拉名单 `TEXT_FONT_FAMILIES`（TextProperties.tsx）写死约 47 项：内置艺术字体靠 `fonts.css` @font-face 打进 exe（离线可用）；系统字体项**只写名字**，本机没装就悄悄回退默认字体，无法用任意本机字体。
- `el.fontFamily` 为普通字符串，落到编辑器/导出/预览/缩略图四端，用同一浏览器字体栈渲染。

### 改动
**[availableFonts.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/availableFonts.ts)**（新增，零依赖，避免与组件循环）
- `BUNDLED_FONT_NAMES`：内置艺术字体集合（与 fonts.css 一一对应），恒显示、不受过滤。
- `probeSystemFontAvailable(family)`：`document.fonts.check`（兜底宽高测量、极端兜底 `true`）判定本机可用。
- `filterByAvailability(base)`：内置恒显 + 系统项按可用性过滤。
- `EXTRA_SYSTEM_FONT_CANDIDATES` / `detectExtraSystemFonts()`：追加额外常见系统字体探测。
- `queryInstalledFontFamilies()`：`window.queryLocalFonts()` 枚举全部已装字体（Local Font Access）；需用户手势；不支持/拒绝/异常返回空（回退仅 A）；模块级缓存避免反复弹授权。

**[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**
- 下拉改用动态 `fontList = 基础过滤 + 额外系统探测 + localFonts(全部) + 当前选中兜底`；`onFocus` 触发一次 `queryLocalFonts`（用户手势中调用）。

### 四端一致性 / Worker
- A/B 拿到的都是本机已装系统字体，编辑/导出/预览/缩略图（OffscreenCanvas）共用 OS 字体栈即可一致；**无需**给缩略图 Worker 投递字节（Worker canvas 可用系统字体；不可用的是 @font-face 内嵌字体）。已写入代码开发规范 9.9（含未来 C 方案的多端约束备忘）。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json`：0 错误。
- `npm run build`：0 错误构建。
- `npx vitest run`：27 文件 / **287 tests 全部通过**。

---

## 2026-08-18 · 修复：重排照片（随机/拖动）后缺少"落位"动效

### 需求
»重排照片«（随机重排 / 拖动换位）后，照片落到新照片位的动效应与「从照片列表把照片拖进照片位」一致——即带 Q 弹入场动画。

### 根因
照片落位的 Q 弹动画由 CanvasPhotoRenderer 的模块级 `justPlacedKeys`（`${slotId}-${photoId}`）触发（Konva.Tween：BackEaseOut 0.4s，缩放 0.7→1、透明→实）。该标记**只有 `useDragDrop`（照片列表拖入槽位）会调用 `markPhotoJustPlaced`**；重排路径走 pageLayoutService 直接 setState，从不打标 → 画布原地跳位、无动画，两种操作体验不一致。

### 改动
**[photoJustPlaced.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/photoJustPlaced.ts)**（新增，零依赖）
- 抽离 `justPlacedKeys` / `markPhotoJustPlaced` / `consumePhotoJustPlaced` / `hasPhotoJustPlaced`，供 useDragDrop 与重排服务共用，避免 services 层反向依赖大组件。

**[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)**
- 改用 `photoJustPlaced` 导入的 `hasPhotoJustPlaced` / `consumePhotoJustPlaced`；`export { markPhotoJustPlaced } from './photoJustPlaced'` 保持 useDragDrop 导入不变（行为不变）。

**[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts)**
- 新增 `markMovedPhotosForAnimation(oldPlacements, newPlacements)`：对比新旧，对"槽位发生改变"的照片打 `markPhotoJustPlaced`。
- 在随机重排（模板页）、拖动换位、GP 随机重排三处 **setState 之前**调用，让换位照片复用与"从列表拖入"一致的 Q 弹落位动效。

### 验证
- `npm run build`（tsc -b + vite build）：0 错误。
- `npx vitest run`：27 文件 / **287 tests 全部通过**。

---

## 2026-08-18 · 修复：照片位置重排（随机/拖动）后露白、照片未填满照片位

### 需求/问题
»重排照片«（照片位置重排弹窗的「随机重排」按钮与长按拖动换位）后，部分照片边缘出现白边/露白，照片没有铺满照片位。用户确认：露白照片此前做过**平移 + 旋转 + 缩放**。期望：重排后自动重拟铺满、无白边。

### 根因
渲染端 cover-fit 本身不漏白（`calcCoverFit` 取最大缩放比必填满；`calcCoverFitWithRotation` 偏向过缩放）。问题在**重排搬运每张照片的编辑状态时，绝对平移 `panX/panY` 被原样搬到宽高比不同的槽位**：
- 拖动换位 `swapPagePhotoPlacements`：整块 placement（含 rotation/panX/panY/panScale）原样搬到对方槽，未按新槽几何重拟合/夹紧。
- 模板页随机重排 `shufflePagePhotos` 非 GP 分支：同样把绝对 panX/panY 等原样洗到别的槽。
- 对比：GP 随机重排走 `makePlacementMigrator`（`computePanForResizedSlot`+`clampPhotoToSlotBounds` 重拟合），是安全的。
- 结果：被平移/旋转/缩放的照片换到不同宽高比槽位后偏移对不上 → 盖不满 → 露白。

### 改动
**[photoGeometry.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/photoGeometry.ts)**
- 新增纯函数 `refitPlacementPan(photoW,H,oldSlotW,H,newSlotW,H,edit)`：保留 rotation/panScale，用 `computePanForResizedSlot`（按新旧槽尺寸比例映射相对位置）+ `clampPhotoToSlotBounds`（多边形夹紧）重拟 panX/panY；无平移编辑或几何缺失时返回空 → 调用方回退居中（cover-fit 必填满）。

**[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts)**
- 新增共享工具 `reorderPagePhotoAssignments(page, assignments)`：按「目标槽→照片」重建 placements，把每张照片平移重拟合到目标槽（GP 用 slotOverrides、模板用 `getSlotRect` 兜底，与渲染端同源）。
- 模板页随机重排分支：不再原样搬运绝对 panX/panY，改为走 `reorderPagePhotoAssignments`。
- 新增 `swapPagePhotos(pageIndex, from, to)`（拖动换位）：交换照片位并把各自平移重拟合到对方槽；GP 页同步 swap mmLayout。与随机重排共用同一套重拟逻辑。

**[PhotoReorderDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoReorderDialog.tsx)**
- 拖动换位改调 `pageLayoutService.swapPagePhotos`（替代原 store 的 `swapPagePhotoPlacements` 原样交换）。

### 测试（新增 5）
**[photoGeometry.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/photoGeometry.test.ts)**：
- 无平移编辑返回空（居中）；宽槽→窄槽具体数值（200 窄槽只能居中 panX=-125，不露白）；
- 4 组「旧槽→新槽 + 旋转/缩放」场景重拟后 pan 均在 `computePhotoBounds` 可行范围内（覆盖四角不露白）；panScale>1 保留；照片尺寸非法回退居中。

### 验证
- `npm run build`（tsc -b 真实类型校验 + vite build）：0 错误，构建成功。
- `npx vitest run`：27 文件 / **287 tests 全部通过**（含新增 5；google-photos 20 通过、photoGeometry 5 通过）。

---

## 2026-08-18 · P2（续3）：编辑器画布装饰节点回调稳定化（React.memo 生效）

### 需求/方案
P2 第三刀：编辑器画布渲染 memo 化。先全盘扫描（见会话），结论：
- **`globalLayerElements` IIFE 不能直接套 useMemo**：注释记录曾因 hooks 顺序问题（React #310）从 useMemo 回退成 IIFE；且其内部读取 canvasZoom/选中态/工具态/多选预览 等覆盖几乎全部状态，套 memo 后任何状态变化都重算，加速近零。
- **真正浪费**：ShapeNode/StickerNode/StickyNoteNode 已用 `memo(Impl)` 包裹，但 IIFE 内每元素的内联箭头回调（onUpdate/onSelect/onMove/onRemove）每次渲染都是新引用，击穿 memo 浅比较 → 任意父级重渲染（工具切换/悬停/选他元素）都连带全部装饰节点重渲染。
- 内联回调创建在 forEach 循环内，无法循环内 useCallback（hooks 规则）。方案：**叶子节点回传自身 id + 父级 useCallback 稳定处理器**。

### 改动
**[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**
- `globalLayerElements` 上方新增 10 个 `useCallback` 稳定处理器（shape/sticker/sticky 的 update/move/moveEnd/remove/requestEdit/select），仅 `currentPageIndex` 或对应 store action 变化时重建（页面切换本就必须全量重渲染）。
- 三个装饰循环改为传 `id={...}` + 稳定处理器；TextElementNode 保留内联（依赖 canvasZoom + 多选预览几何，缩放本就必须重渲染，稳定化无收益且引入 risk）。

**[ShapeNode.tsx / StickerNode.tsx / StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas)**
- Props 新增 `id: string`，回调签名改为 `(id, ...)`；组件内用 `useCallback` 定义本地包装注入 id（内部既有调用点签名零改动），`useCallback` 同时避免 effect 依赖每次渲染重建导致 Konva 事件反复重绑。
- 死 prop `onRemove`/`canDrag` 签名同步（未接线处保留类型、不产生调用）。

### 说明（收益边界）
- 三个装饰节点内部都 `useUIStore(canvasZoom)` 订阅（用于选中虚线框 `[5/canvasZoom,...]`），**缩放时经 Zustand 独立推送仍会重渲染** —— 本刀对最高频的缩放场景无收益，收益在于**非缩放类父级重渲染**不再扰动无关装饰节点（memo 真正生效），并消除文档记载的反模式（内联回调击穿 memo）。
- Canvas.jsx 无单测兜底，以 `tsc` 强约束回调调用点（错 arity 即编译失败）保证完整接线。

### 验证
- `npm run build`（tsc -b 真实类型校验 + vite build）：0 错误，构建成功（警告均为既有大 chunk 提示）。
- `npx vitest run`：27 文件 / **307 tests 全部通过**。

---

## 2026-08-18 · P2（续2）：缓存清理接线审计——修复内容感知/缩略图缓存永不释放

### 需求/方案
续 audit，重点核验**清理函数是否真实接线**（定义≠被调用）。结论：
- 已健全：`import-store` blob URL（refCount+延迟回收）、`imageBitmapLoader`（LRU+close）、`exportEngine` 滑动窗口、`thumbnail.worker`（绘制后 close 全部位图）均正确释放且有测试。
- **真实缺口**：`content-aware` 的 `photoContentCache`/`pendingAnalysis`/`failedAnalysisCache` 三个 Map（keyed by photoId）与 `thumbCache` 的 `clearThumbCache`，**清理函数定义了但无任何生产调用方**（仅测试引用）。照片删除路径与 `cleanupProjectResources` 都不清，导致被删照片的 contentInfo、分析中间态、HEIC/人脸缩略图 URL 永久驻留。

### 改动
**[photoService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/photoService.ts)**
- `cleanupPhotoBlobs(photo)`（`removePhoto`/`removePhotos` 共用）：新增 `invalidatePhotoContentCache(photo.id)` + `evictFromCache(photo.id)`，补齐编辑器 PhotoPanel 删除路径对内容感知/缩略图缓存的清理。
- `cleanupProjectResources()`：新增 `clearPhotoContentCache()` + `clearThumbCache()`（含 HEIC 缓存），项目切换时一并释放。

**[shared.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/shared.tsx)**
- `deletePhotos`（库内模式，时间线/日历/人脸识别等共用）：新增 `invalidatePhotoContentCache(f.id)`（此前只 `evictFromCache` 缩略图，漏了内容感知缓存）。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json`：0 错误。

---

## 2026-08-18 · P2（续）：缓存生命周期与泄漏审计（thumbCache）

### 需求/方案
P2 第二刀：审计照片/缩略图/URL 缓存的生命周期。结论：
- **blob URL 缓存（import-store.ts）已健全且有测试**（refCount 引用计数 + LRU 200 + 延迟回收 + 强制失效，import-store.test.ts 覆盖），无需改。
- **thumbCache.ts 存在两处真实问题**，本次修复。

### 改动
**[thumbCache.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/thumbCache.ts)**
- **人脸裁剪缓存删除时泄漏（修复）**：`evictFromCache(photoId)` 原来只匹配 `${photoId}:` 前缀，而人脸缩略图键是 `face:${photoId}:...`，照片删除后人脸 blob URL 不被回收、还残留悬挂 URL。抽出新判定 `isPhotoCacheKey`（同时命中普通与人脸前缀），`evictFromCache` 改用之。
- **HEIC 转换缓存无界（修复）**：`heicConvertedCache` 原是普通 Map 按 photoId 存转换后 JPEG Blob，浏览大量 HEIC 时无限增长。新增有界写入 `setHeicConvertedCache`（上限 `HEIC_CACHE_LIMIT=100`，超限淘汰最旧），`generateThumb`/`getFaceThumbUrl` 两处改用。
- 新增 `getHeicConvertedCacheSize`（供测试断言有界）。

### 测试（新增 5）
**[thumbCache.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/thumbCache.test.ts)**：
- `isPhotoCacheKey` 命中普通/人脸前缀、不误伤他照片与相似 id（`p1` 不匹配 `p11`）；
- HEIC 缓存超上限淘汰最旧（120 次写入 → size 恒 100）；`clearThumbCache` 彻底清空 HEIC 缓存。
- 注：真实缩略图生成依赖 DOM canvas + createImageBitmap，不在 jsdom 单测范围，故只测纯判定与有界性。

### 验证
- `npm run build`（`tsc -b` 真实类型校验 + vite build）：0 错误，构建成功（警告均为既有）。
- `npx vitest run`：27 文件 / **307 tests 全部通过**（新增 5）。

---

## 2026-08-18 · P2 · 性能：人脸/内容感知分析并发限制 + 去重解码

### 需求/方案
P2 第一刀聚焦"人脸/内容感知分析异步与去重"。全盘扫描（见会话）确认两处真实热点：①一次批量触发大量照片分析时，所有 face-api 检测**无并发上限**同时开跑，内存飙升 + 主线程卡顿；②`analyzePhotoWithFaces` 对同一张照片**多次全分辨率解码**（analyzePhotoContent 一次 + detectAllFaces 一次）。

### 改动
**[concurrency.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/concurrency.ts)**（新增）
- `createConcurrencyLimiter(limit)`：轻量信号量/队列，最多 `limit` 个并发，其余排队补位；结果与失败语义与直接调用一致，仅调度时机变化。

**[content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)**
- `ensurePhotoAnalyzed` 的 `analyzePhotoWithFaces` 经 `faceAnalysisLimiter`（`FACE_ANALYSIS_CONCURRENCY = 3`）排队执行——导出预热/智能编排/网格渲染批量触发时，face-api 最多 3 张同时跑，避免主线程卡顿。
- `analyzePhotoWithFaces` 改为**顶部只 `loadImageSafely` 一次**，主色/清晰度分析（新抽 `analyzePhotoContentFromImage`）、全图/上半区人脸检测、能量回退、catch 回退**全部复用同一张已加载图**——消除重复全分辨率解码。
- `analyzePhotoContent` 外部行为不变（加载 → 调 `analyzePhotoContentFromImage`）。

### 测试（新增 8）
- **[concurrency.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/concurrency.test.ts)**（4）：并发峰值 ≤ limit；串行按入队顺序结果一致；异常传播不阻塞后续；非正 limit 兜底 1。
- **[content-aware.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.test.ts)**（4）：`computeSmartObjectPosition` 居中 / 焦点对齐 / 贴边 clamp 不露白；`isColorConflict` 缺失或低饱和度不冲突。（人脸/能量本体依赖 DOM canvas + face-api，不在 jsdom 单测范围内。）

### 验证
- `npm run build`（`tsc -b` 真实类型校验 + vite build）：0 错误，构建成功（警告均为既有）。
- `npx vitest run`：26 文件 / **302 tests 全部通过**（新增 8）。

---

## 2026-08-18 · P1（续）：types 基础层不变量安全网 + Canvas 拆分结论

### 背景
P1 item 3 完成 types 与 google-photos-layout 拆分后，为刚拆的**基础层补安全网**。同时评估 Canvas.tsx：其唯一模块级纯函数 `estimateTextIndexAtPoint` 依赖 canvas 2D 测量（jsdom 无 canvas 不可单测），组件内部其余计算均触碰 Konva/store/ref，无干净可抽、可测切片 → 维持"不做整体重排"结论。

### 改动
**[registry.invariants.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/registry.invariants.test.ts)**（新增 8 cases，纯只读断言）
- 模板库未截断：TEMPLATES 非空、id 唯一、含封面/封底模板、库 >50 个；
- 封面/封底判定按前缀、普通页不误判；
- resolveTemplate 按 id 命中；带 slotOverrides 的空 id → Google Photos 虚拟模板（`__google_photos__`）、无 overrides 为 undefined（实证契约）；
- findTemplateById 命中/未命中；normalizeSlotCornerRadius（undefined→2 / number / 数组均值）；
- SIZE_PRESETS/ALBUM_SIZES/ALBUM_TYPES 结构完好；SHAPE_TYPES 含矩形/圆/三角/直线；STICKY_COLORS/BRUSH_STYLE_MAP 非空；水印默认值形状正确。
- 作用：数据被搬丢、TEMPLATES 被误改、索引失配时立即失败，锁住拆分结果。

### 验证
- `npx vitest run`：24 文件 / **294 tests 全部通过**（新增 8）。
- `npm run build`（`tsc -b` 真实类型校验 + vite build）：0 错误，构建成功。

---

## 2026-08-18 · P1 · 可维护性：巨型文件拆分 + react-konva StrictMode 加固

### 需求/方案
按 P1 可维护性项 3、4 落地：拆巨型文件 + 处理 react-konva StrictMode 崩溃。先全盘分析（见会话），结论：**types 与 google-photos-layout 安全可拆；Canvas 无测试兜底且 jsdom 无法挂载 react-konva Stage，不做整体重排**；StrictMode 加固不改全局、不重开 StrictMode。

### 改动
**1. types/index.ts 按领域拆分（barrel 无损）**
- 新建 `src/types/{album,template,photo,elements,background,ui,watermark}.ts`，把原 2288 行定义按域搬入（template.ts 含最大块 TEMPLATES 数组）。
- `src/types/index.ts` 改为纯 barrel：`export * from './xxx'`。**现有 `import ... from '../types'` 全部零改动**；跨文件依赖均为 `import type`，无运行时循环。

**2. google-photos-layout 拆布局原语（20 测试兜底）**
- 拆入 `src/engine/googlePhotosLayout/{types,primitives,index}.ts`：types 集中类型；primitives 收叶子原语（clamp/日期/色彩/tier 取法/行生成）；index 收编排与对外函数。
- 原 `google-photos-layout.ts` 改为 `export * from './googlePhotosLayout'` 收口，公开 API 不变。

**3. react-konva StrictMode 加固**
- **Canvas.tsx**：Transformer 绑定清理——选中切换/阶段切换/组件卸载前先 `transformerRef.nodes([])` 再销毁节点；卸载先释放绑定再 `stage.destroy()`，避免 Transformer 引用已被拆节点引发 `setAttrs` undefined（react-konva 19 + React19 StrictMode 崩溃根因，上游 issue #840 未修）。
- **main.tsx**：注释升级为完整根因 + 已知 issue 链接 + 「若想重开 StrictMode 的人工验证清单」。不重开全局 StrictMode（整 app 双调用风险无自动化兜底）。

**4. 借真实类型校验修复 2 处潜在真 bug（build 暴露）**
- **暴露根因**：根 `tsconfig.json` 是空引用解决方案（`files:[]`），`npx tsc --noEmit` **不校验任何源码**；真实校验是 `npm run build` 里的 `tsc -b`（或 `tsc -p tsconfig.app.json`）。此前 `--noEmit` 通过是假通过。
- 修复：
  - `exportEngine` 照片槽位：`buildPhotoPlacementPlan` 的 `params` 可能为 null（零尺寸照片），原直接传给非空参数类型；现局部常量 + 空值 continue 守卫，避免导出一个进度即崩。
  - `exportEngine` `calcExportMaxDim`：re-export（`export {x} from`）不产生模块内局部绑定，导致内部 4 处调用 `Cannot find name`；改为 `import` 进本地 + `export { calcExportMaxDim }`。

### 验证
- `npm run build`（`tsc -b` 真实类型校验 + vite build）**0 错误，构建成功**（警告均为既有动态/静态 import 与大 chunk 提示）。
- `npx vitest run`：23 文件 / **286 tests 全部通过**（google-photos 20 通过，其余 import `../types` 的测试全部通过，证明 barrel 拆分无损）。

### 说明
- Canvas.tsx（3376 行）本次**不拆**：无组件级自动化兜底（jsdom 无法跑 react-konva Stage，未引入 canvas mock），先保留在收敛范围内做纯命令数学抽取（留待后续迭代），避免高风险整体重排。

---

## 2026-08-18 · 导出/打印链路加固：抽离导出几何纯模块 + 水印逻辑补测

### 背景
P0（四端同源 + spec 快照）已完成。下一阶段聚焦"用户真正输出的导出/打印链路"：把导出关键路径（页码尺寸/封面书脊/出血/DPI、时间水印）的**确定性计算**收成纯函数并测死，锁住印刷质量正确性。沿用"有界抽取 + 行为零变化"。

### 改动
**[exportGeometry.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportGeometry.ts)**（新增纯模块，无 store/DOM/jsPDF/Tauri 依赖）
- `pageExportWidthMm`：导出页面物理宽（封面含设计书脊、封底无书脊、两侧出血）；
- `calcExportCanvasSize`：导出画布逻辑/像素尺寸（DPI 换算 / 四周出血 / 封面书脊扩展）；
- `calcExportMaxDim`：照片降采样阈值（最长边 × DPI 像素 × 2）；
- `pxPerMmAt`：DPI → 每毫米像素。

**[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**
- `renderPage` 尺寸数学改调 `calcExportCanvasSize`；删除本地 `pageExportWidthMm`/`calcExportMaxDim`/`DOWNSCALE_FACTOR`；
- `export { calcExportMaxDim } from './exportGeometry'` 保 re-export，`printEngine` 等外部调用不受影响。**行为零变化**。
- 抽离原因：直接 import exportEngine 会触发 jsPDF/Tauri 浏览器依赖，测试环境难以加载；独立纯模块让导出几何可单测且唯一来源。

### 测试（新增 30 cases）
- **[exportGeometry.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportGeometry.test.ts)**（11）：封面书脊/封底无书脊/出血物理宽；DPI 像素尺寸与逻辑尺寸、封面书脊扩展、出血仅扩像素不影响逻辑坐标；降采样阈值。
- **[watermarkRenderer.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/watermarkRenderer.test.ts)**（19）：`formatLocation`（coarse/standard/detailed、直辖市、旧数据）;`formatDate`（形状 + 非法/空）;`shouldShowWatermark`（未开启/无日期/首页/改档日期拦截/每 6 页强制/同天同地去重）;`getWatermarkText`（仅日期/仅地点/多地点去重/全关空串）;`calcPageSafeArea`/`calcWatermarkPosition`。
- 说明：`formatDate` 具体"年月日"依赖运行时区，故只断言日期格式形状与非法兜底，不锁具体日期。

### 验证
- `npx tsc --noEmit` 零错误。
- `npx vitest run`：23 文件 / **286 tests 全部通过**（新增 30；stderr 为截图/缓存/LRU 测试正常日志噪音）。

---

## 2026-08-18 · P0 阶段2（续）：编辑器（Konva）形状端接入共享 buildShapePaintSpec

### 背景
阶段 2 已把导出端切到四端同源。本步把**编辑器（Konva）端**也接上同一份画刷 spec，让形状在画布与导出/缩略图/预览的填充、描边、渐变方向、透明度、描边下限完全一致。经评估，仅**形状**可安全接入：照片（CanvasPhotoRenderer，可拖/缩放/旋转/pan 编辑）与文字（Konva `<Text>` 自动排版 + contentEditable 浮层）是交互模型，强行套纯函数会破坏编辑能力，故不接入。

### 改动
**[ShapeGlyph.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeGlyph.tsx)**
- 画刷判定改为消费共享 `buildShapePaintSpec`：填充（纯色/线性/**径向**）、描边（纯色/线性）、描边宽下限 `MIN_STROKE_WIDTH`、透明度缺省 1、渐变 stop 统一 rgba 解析。仅保留 Konva 属性适配 + 本地原点平移（矩形 x=-pw/2 的渐变点 +halfW/+halfH）。
- spec 基于逻辑坐标（×MM_TO_PX），乘 `k = pw/spec.pw` 映射回画布像素，渐变方向/半径按编辑器实际盒子重算，行为与原有内联计算一致。
- **修复编辑器缺失的形状径向渐变**：此前 ShapeGlyph 一律按线性填（`gradientType==='radial'` 被忽略），导出/缩略图却是径向 → 画布与导出不一致；现补 `fillRadialGradient*` 对齐。
- 移除不再使用的 `gradientFlatStops`/`MIN_STROKE_WIDTH` 导入。

### 说明
- `buildShapePaintSpec.fill.stops` 与原有 `gradientFlatStops` 的 rgba 解析逐字一致（均 `alpha<1 → toRgba`），故换源不改变渐变颜色。
- 照片/文字端本次不动，避免破坏交互编辑模型（见背景）。

### 验证
- `npx tsc --noEmit` 零错误。
- `npx vitest run`：21 文件 / **256 tests 全部通过**（stderr 为截图/缓存/LRU 测试正常日志噪音）。

---

## 2026-08-18 · P0 阶段2：导出端切到四端同源 + 新增 spec 不变快照

### 背景
阶段 1 已把照片/文字/形状三套布局判定抽成共享纯函数并测死；导出端照片已切共享（`buildPhotoPlacementPlan`）。阶段 2 收尾：把导出端**文字、形状**也切到共享 spec（四端同源闭环），并为三套纯函数加**spec 不变快照**，冻结当前确定性子集，防四端被某一端静默改坏。

### 改动
**[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**
- `drawShape` 改为消费共享 `buildShapePaintSpec`：填充（纯色/线/径向渐变）、描边（纯色/线性渐变）、尺寸/描边下限（`MIN_SHAPE_SIZE_MM`/`MIN_STROKE_WIDTH`）、透明度/旋转等确定性判定都来自同一 spec；本函数仅做 `scale = mmToPx/MM_TO_PX` 导出像素映射 + 几何路径构造。新增 `applyGradientStops` 把 spec 扁平 stop 数组写入 CanvasGradient。移除不再直接使用的 `MIN_SHAPE_SIZE_MM` 与形状段内联的 `toRgba`/`linearGradientEndpoints`。
- `drawTextElement`（横排 + 竖排）改为消费共享 `buildTextLayout`：断行/水平对齐/垂直对齐/竖排逐字统一由 spec 计算，旋转变换改为**绕文字框中心旋转 + 页面绝对坐标绘制**（与旧"本地坐标"分帧逻辑位等），锚点乘 `scale` 映射回导出像素。移除内联 `pad`/垂直段节点推算、不再使用的 `wrapTextLines` 导入。
- 行为零变化：导出坐标系中 `mmToPx` 与 `MM_TO_PX(=2)` 在常规路径相等，`scale≈1`，输出一致；封面书脊路径也通过乘 `scale` 精确保留。

**[thumbnailCore.specSnapshot.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.specSnapshot.test.ts)**（新增，3 cases）
- 用代表性复合页（照片槽位 + 横排文字 + 竖排文字 + 纯色/渐变填充/渐变描边形状）跑三个共享纯函数，用 `toMatchInlineSnapshot` **冻结确定的规格输出**：
  - 照片布局：槽位坐标 / cover-fit 不露白 / 层级 z=0；
  - 文字排版：横排 + 竖排写出指令逐条冻结；
  - 形状画刷：纯色 / 渐变填充 stop（含 rgba 半透明）/ 渐变描边全量冻结。
- 文字测量用确定性伪测量 `s.length * 8`，跨平台稳定。任一端的重构若悄悄改变布局判定，快照 diff 即暴露。

### 说明
- 画布（Konva）端仍是交互渲染模型，定位/排版与位图 spec 在文档层面保持一致，未强行套用纯函数（交互节点需保留各自节点语义）。
- 快照含少量浮点尾数（如 `50.00000000000001`、`82.39999999999999`），为纯函数跨平台确定性的 IEEE 表示，冻结后逐次稳定比对。

### 验证
- `npx tsc --noEmit` 零错误。
- `npx vitest run`：21 文件 / **256 tests 全部通过**（新增 3；stderr 为截图/缓存/LRU 测试正常日志噪音）。

---

## 2026-08-17 · P0 阶段1（续）：抽取纯函数 buildShapePaintSpec（形状画刷同源）

### 背景
阶段 1 前两块完成（照片布局、文字排版）。继续按"有界抽取 + 行为零变化"覆盖**形状画刷**：填充/描边/渐变解析 + 尺寸/描边下限 + 透明度/旋转，首个进可测区。

### 改动
**[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**
- 新增导出纯函数 `buildShapePaintSpec(sh)` 返回 `ShapePaintSpec`（不含 ctx）：
  - 尺寸下限 `MIN_SHAPE_SIZE_MM`、描边宽下限 `MIN_STROKE_WIDTH`、透明度/旋转换算；
  - 填充：纯色 或 经 `linearGradientEndpoints` + 渐变 stop 解析（半透明 `toRgba`）的线/径向梯度；
  - 描边：纯色 或 线性渐变；
- `drawShape` 改为消费该 spec（绘制路径构造逻辑不变），**渲染行为与原实现完全一致**。

### 测试
- 新增 **[thumbnailCore.shapeSpec.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.shapeSpec.test.ts)**（8 cases）：
  - 位置 mm→px、透明度/旋转透传、透明度缺省 1；
  - 极小尺寸抬到下限、描边宽下限；
  - 纯色填充/描边；
  - 线性渐变端点 + 半透明 stop 解析为 rgba；
  - 径向渐变原点 + radius = min(pw,ph)/2；
  - 无填充/无描边为 null；
  - 描边线性渐变解析。

### 验证
- `npx tsc --noEmit` 零错误。
- `npx vitest run`：20 文件 / **253 tests 全部通过**（新增 8；stderr 为截图测试正常日志噪音）。

---

## 2026-08-17 · P0 阶段1（续）：抽取纯函数 buildTextLayout（文字排版同源）

### 背景
阶段 1 第一块完成（照片布局 buildPhotoPlacementPlan）。继续按"有界抽取 + 行为零变化"覆盖**文字排版**：横排断行/对齐/垂直对齐 + 竖排逐字，同样首次进入可测区。

### 改动
**[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**
- 新增导出纯函数 `buildTextLayout(te, measure)` 返回 `TextLayoutWrite[]`（text + 锚点 x/y + textAlign）：
  - 横排：断行 `wrapTextLines`（与 DOM 文字层同源）、垂直对齐 top/center/bottom、水平对齐锚点（左/居/右）；
  - 竖排：逐字节点列 + 水平/垂直对齐平移；
  - 纯函数、无 ctx 依赖；测量宽度通过 `measure(s)` 回调注入（draw 端传 `ctx.measureText`）。
- `drawTextElement` 改为：先设 `ctx.letterSpacing` → 调 `buildTextLayout` → 按指令逐条 `fillText` → 复位。
  **渲染顺序/位置与原竖向+横向两分支完全一致**；顺带移除不再使用的局部 `pad`。
- 注意：测量传入 `ctx.measureText(s).width` + `wrapTextLines` 内 `letterSpacing` 参数，与原实现（先设 `ctx.letterSpacing` 再测量）在字距口径上保持一致。

### 测试
- 新增 **[thumbnailCore.textLayout.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.textLayout.test.ts)**（5 cases）：
  - 横排居中锚点在盒宽中点；
  - 垂直居中 y 落内容区中线；
  - 内容超高分行且行距递增；
  - 竖排逐字同列、垂直居中；
  - 竖排显式换行开新列（x 左移、y 对齐）。

### 验证
- `npx tsc --noEmit` 零错误。
- `npx vitest run`：19 文件 / **245 tests 全部通过**（新增 5；stderr 为截图测试正常日志噪音）。

---

## 2026-08-17 · P0 阶段1：抽取纯函数 buildPhotoPlacementPlan（照片布局同源，首个进可测区）

### 背景
阶段 0 已为纯几何补上测试。阶段 1 目标是让"四端（画布/导出/缩略图/预览）同源"的照片布局首次进入可测区。采用**有界抽取**而非 909 行整体重写：只把最易回归、正确性最关键的"照片摆放判定"抽成纯函数，绘制循环改为消费它，**行为零变化**。

### 改动
**[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**
- 新增导出纯函数 `buildPhotoPlacementPlan(page, photos, logicalW, logicalH, margin?, contentInfoMap?)`，返回 `PhotoPlanItem[]`：
  - 槽位坐标 `getSlotRect`（overrides/边距/等比缩放/封面偏移）；
  - 照片摆放 `calcPhotoRenderParams`（cover-fit 铺满不露白/旋转/pan）；
  - 层级 `getSlotZIndex`（slotZIndices，默认 0）；
  - 渲染顺序 `slotOrder`（未定义则回退模板 slots 顺序，对 overlay 模板关键）；
  - 纯函数、无 ctx / 图片 / store 依赖。
- `drawPageToCanvas` 照片绘制块改为遍历该计划（取图 + 入列绘制），**渲染顺序与结果与原内联计算完全一致**；移除不再使用的 `photoMap` 局部变量。

### 测试
- 新增 **[thumbnailCore.photoPlan.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.photoPlan.test.ts)**（5 cases）：
  - 按 `slotOrder` 排序、返回槽位坐标与 `slotZIndices` 层级；
  - 照片 cover-fit 铺满槽位（draw 区域四向覆盖，不露白）；
  - 照片不存在 / 零尺寸被排除；
  - 无 slotOrder 时集合完整、z 默认 0（顺序由模板/虚拟模板决定，只锁集合不断言）；
  - 空输入返回空计划。

### 说明
- 测试发现 `resolveTemplate('')` 会经 `buildVirtualTemplate` 返回模板，故无 slotOrder 的顺序依赖模板，对合成测试页不可控 → 该 case 只断言集合与 z，避免误判。
- 阶段 1 暂覆盖"照片布局"这一最高价值切片；文字/形状等其他元素的完整 spec 抽取留待后续（保持有界、可验证）。

### 验证
- `npx tsc --noEmit` 零错误。
- `npx vitest run`：18 文件 / **240 tests 全部通过**（新增 5；stderr 为截图测试正常日志噪音）。

---

## 2026-08-17 · P0 阶段0：为"四端同源"纯几何原语补测试（生产代码零改动）

### 背景
综合评估提出 P0：核心表意系统缺测试、四端（画布/导出/缩略图/预览）同源靠注释维系。分阶段落地；本阶段为**阶段 0**——只给最易测、最核心的纯几何原语补测试，不动任何生产代码，先建立保护网。

### 改动（仅新增测试，未改 src 生产代码）
- **[coverScale.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/coverScale.test.ts)**（12 cases）：`coverSlotSize`（全幅/贴边槽逐轴拉伸 vs 等比）、`coverElementSize`（区域拉伸 vs 图形等比）、`coverAnchorPosition`（贴左/贴右/贴顶/贴底/居中/百分比六类锚点语义）、`isMaskShape`。
- **[shapeGeometry.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/shapeGeometry.test.ts)**（11 cases）：三角/菱形/五边/六边/星/平行四边形/梯形/切角矩形/对角切矩形等最外边缘四边贴合控制盒；菱形中心对称；切角矩形顶点数。
- **[sharedRender.test.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.test.ts)**（19 cases）：`calcCoverFitWithRotation` 旋转后覆盖槽位不露白、`calcPagePreviewFit` 保比例居中适配、`wrapTextLines` 断行契约（无空格英文不强制断行/英文按空格分词/CJK 逐字/letterSpacing）、`getSlotRect`（覆盖分支/未知槽位）、`calcPhotoRenderParams`（铺满不露白/默认居中/零尺寸）、`getTextureBaseColor`。

### 说明
- 初版三处断言按"错误直觉"改写，修正为符合真实契约：
  - 无空格长英文按 `word-break:normal` **不强制断行**；
  - hexagon 经 `fitToBox` **异向缩放**后并非等角（正常化到控制盒），不再断言等角。
- 测试实现沿用既有范式：mm 容差断言、纯函数、jsdom 可跑，import 依赖（content-aware 等）确认无副作用。

### 验证
- `npx vitest run`：17 文件 / **235 tests 全部通过**（含新增 42）；（stderr 为截图测试正常日志噪音，不影响通过）。
- 生产代码零改动，无回归风险。

---

## 2026-08-17 · 主页封面设计库：新增左上角眼睛 + 大图弹窗封面↔封底翻转预览

### 需求
封面设计库每张模板卡片左上角加「眼睛」图标；点击弹出大图展示该款模板封面，在弹窗内点击封面/封底进行切换（封面↔封底来回切换，带过渡动效），让用户直观感受模板"封面+封底"整体风格。

### 改动
**[CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx)**
- 每张 `CoverCard` **左上角新增眼睛按钮**（与右上角编辑按钮对称，悬浮显示，`stopPropagation` 不触发新建/编辑），点击设置 `previewTemplate` 打开大图弹窗。
- 新增 `CoverFlipPreview`：单张 `CoverPreview` 按「封面(`template` + `variant='front'`) ↔ 封底(`template.backCover` + `variant='back'`)」翻转切换。
  - 点击触发 3D 翻面动画：先向外翻开（`perspective` + `rotateY(-38deg)` + 压低透明度 0.3s），换面后回正落定，无生硬跳变。
  - 无配套封底的模板不进入翻面（`hasBack` 守卫）；下方标注当前面（封面/封底）+ 切换提示。
- 弹窗复用共享 `Modal`，宽 600px；封面按真实竖版比例 `210 / 280` 渲染（含书脊/硬壳效果）。

**[zh-CN.json / en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**
- `home.coverGallery` 新增 `previewBtn`/`previewTitle`/`previewCover`/`previewBack`/`previewToggleHint` 文案。

### 优化（对齐预览功能封面切换动效）
初版用「同一容器 0.3s 整体 rotateY + 降透明度」效果不贴近预览封面。改为复刻预览封面切换的两段式：
- 当前面先 3D 翻开淡出（`cover-flip-away-front/back` keyframes，`perspective + translateX(∓38%) + rotateY(∓34deg)` → opacity 0，0.55s，`forwards`）；
- 随后 `setFace` 换面并以 `face` 作 key 整棵重挂 → 触发新面柔和落定（`cover-settle-front/back`，`perspective rotateY(∓20deg)` → 0，0.5s）；
- 翻开/落定方向按当前面镜像：封面书脊在左向左翻开、封底书脊在右向右翻开。

### 优化2（悬浮放大 + 弹窗放大）
- 弹窗 `maxWidth` 600px → **740px**；封面宽 `min(400px, 52vw)` → `min(580px, 56vw)`，封面显示更大。
- 在稳定外层（不冲突翻面/落定的 transform）应用悬浮放大：`group-hover:scale-[1.04]` + 上浮 `-translate-y-2`，0.3s 缓动。

### 优化3（封面完整显示在弹窗内，不被撑破）
- 问题：封面只约束宽度（580px→高约773px），加上标题/留白/标签总高超 `90vh`，封面溢出被裁。
- 改动：封面宽度改为**三重约束** `min(580px, 56vw, calc((90vh - 240px) * 0.75))`——新增"高度反推"的宽度上限（竖版 210:280），以**完整显示**为硬上限、尽量放大，任意视口大小封面都完整落在弹窗内，不裁剪不滚动。

### 优化4（右上角尺寸切换：横向/方形/竖向，默认方形）
- `CoverFlipPreview` 新增 `sizeKey` 状态，默认 `square`；三档 `COVER_SIZES`：横向 280×210、方形 210×210、竖向 210×280。
- 封面右上角三个比例示意图标（小矩形按比例高 + `w×h` 数字），点击切换，选中项品牌色高亮。
- 封面宽高比随所选尺寸变化（`aspectRatio` 用当前尺寸），宽度上限按当前比例反推（`calc((90vh - 220px) × (w/h))`），保证横向/方形/竖向都**完整落在弹窗内**。
- **删除封面下方"封面/封底 + 点击切换"文字**；封面↔封底点击翻面交互保留。

### 优化5（尺寸切换移到弹窗顶部 + 图标重设计 + 弹窗固定尺寸）
- **[Modal.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Modal.tsx)**：新增可选 `height` 属性，传值时弹窗高度固定（`style.height`/`maxHeight`），不随内容/比例变化。
- **[CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx)**：
  - 弹窗改为**固定尺寸**：`maxWidth="760px"` + `height="min(86vh, 700px)"`，切换横向/方形/竖向时窗口不变。
  - 尺寸选择器移到**弹窗顶部**（标题下方第一行，右对齐），重做为**三合一分段控件**（灰色容器 + 选中白色凸起胶囊）。
  - 图标改为 **SVG 矩形按真实比例绘制**（横向宽高比 >1、方形 =1、竖向 <1），选中项品牌色填充高亮，配合 `w×h` 小字。
  - 封面宽度上限用固定窗口高度反推 `min(560px, calc(min(86vh,700px) - 230px) × ratio)`，随所选比例变化但窗口恒定、封面始终完整。

### 优化6（尺寸图标进标题行右侧 + 线条化简化设计）
- **[Modal.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Modal.tsx)**：新增可选 `headerRight`（标题右侧操作区），标题行改 flex 左右布局（`pl-8 pr-14` 让开关闭按钮）。
- **[CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx)**：
  - `sizeKey` 状态提升到 `CoverGallery`，经 `headerRight` 把 `SizeSwitcher` 放弹窗标题行右侧，与"封面预览"标题同一行。
  - 新增**线条化 `SizeSwitcher`**：三个**描边矩形**按 280:210 / 210:210 / 210:280 真实比例（固定高 16px、宽按比例），**纯图形去掉了 `w×h` 数字**，页面风格参考封面线条化 icon；选中项品牌色描边加粗 + 浅色底 + ring 圈出。
  - `CoverFlipPreview` 改由受控 `sizeKey` prop 驱动，删除原内容区顶部的分段控件行。

### 优化7（封面在展示区上下左右居中 + 眼睛提示文案）
- **[Modal.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Modal.tsx)**：新增可选 `centerContent`，开启后内容区 `flex` + 内层 `m-auto`，内容矮时上下居中、超高仍可滚动。
- **CoverGallery**：封面预览弹窗开启 `centerContent`；`CoverFlipPreview` 根节点加 `justify-center min-h-full` 兜底 → 封面水平/垂直居中。
- 眼睛按钮悬浮提示文案由"查看大图预览"改为**"点击查看封面 / 封底"**（含封面翻转点击 hint 同文案）。

### 验证
- `npx tsc --noEmit` 零错误。

### 优化8（窗口与封面整体放大一档）
- 弹窗：`maxWidth` `760px→860px`，`height` `min(86vh,700)→min(90vh,780)`。
- 封面宽度上限：`min(660px, calc(min(90vh,780px) - 210px) × 比例)`（原 `560px` / `-230px`），方形占满更高、横向/竖向仍完整，居中与固定尺寸不变。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-17 · 预览：仅有封面+封底（无内容页）时封面↔封底直接切换

### 需求
相册只有封面和封底、没有任何内容页时，打开预览此前会卡死在封面（点击无反应、看不到封底）。要求此时点击封面→切到封底、点击封底→切回封面，来回切换。

### 根因
`renderAllPages` 会过滤掉封面/封底只渲染内容页，无内容页时返回 `[]`；预览初始化对 `contentImages.length === 0` 直接 `return`，导致翻页书（PageFlip）从未创建、`flipRef.current` 恒为 null；而封面/封底点击入口 `toFlipFromCover`/`openFromBack` 都以 `!flipRef.current` 为由直接返回 → 点击全部失效，页面永久停在封面。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- 新增 `noContentPagesRef` 标记：初始化时 `noContentPagesRef.current = contentImages.length === 0`；为真则**不创建翻页书**，保留已完成渲染的封面/封底真实图直接展示。
- `toFlipFromCover`：无内容页时点击封面 → `setOpening` 动画后 `setStage('back')`（封面↔封底切换）。
- `openFromBack`：无内容页时点击封底 → `setOpeningBack` 动画后 `setStage('cover')`。
- 切换沿用封面/封底既有进出场动画（翻开/落定），无生硬跳变；因从未进入 `flip` 阶段，底部进度条与两侧翻页按钮自动不显示。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-17 · 封面硬壳书脊边缘：加厚度感 + 高光 + 圆角（共享 HardcoverFrame）

### 需求
封面页左侧书脊的边做一点点厚度感和高光，体现边的一点点圆角，增强立体感。

### 改动
**[HardcoverFrame.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/HardcoverFrame.tsx)**
- 在书板之上新增「书脊边缘」元素（`zIndex:56`，宽 `1.6cqw`，front 贴左、back 镜像贴右）：
  - 高光：从最外沿开始「细暗线（书板裁切边）→ 受光亮带 → 向内柔和淡出」，模拟圆润书脊棱受光（`spineGrad`，front left→right / back 反向）。
  - 厚度感：外沿细暗线 + 亮带层次。
  - 圆角：靠外缘一侧 `borderRadius`（front 左、back 右），叠加顶/底端内阴影微暗（圆角转折处离光，`boxShadow inset ±1.1cqw 0.7cqw`）。
- 因是共享组件，主页相册封面 / 封面模板库 / 翻页预览封面/封底等所有入口一致生效。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（24.1s）。

---

## 2026-08-17 · 预览支持时间水印（内容页显示、封面/封底不显示）+ 加快封面加载

### 需求
1. 打开时间水印后，预览的内容页也正常显示时间水印；封面/封底不显示。
2. 进入预览时封面有时呈灰色加载 3~4 秒才显示内容，需加快。

### 改动
**[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)**
- `RenderOptions` 新增 `watermarkPages?`：绘制时间水印时使用的相册页数组（默认回退全局 store pages）。预览/主页渲染非 store 相册时显式传入，保证水印日期/地点判定正确。

**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **① 水印**：`renderAllPages` 改为接收完整相册 `pages`，内部过滤出内容页并计算各自的**绝对页索引** `pageIndex`，连同 `watermarkPages: pages` 传入 `renderPageThumbnail` → 内容页正常画时间水印；`renderSinglePage`（封面/封底）不传 `pageIndex` → 不显示水印。
- **② 封面加快**：`resolvePhotoSrc` 优先复用主页已解析的 `photo.src`（`data:` 永不失效 / `blob:` 用 `isBlobUrlAlive` 守卫），跳过冷启动时重新读取 IndexedDB 的耗时，封面/内容更快显示、不回落到灰占位。`preloadSharedPhotos` 从 storage-engine 追加导入 `isBlobUrlAlive` 并透传。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（25.7s）。

---

## 2026-08-17 · 修复冷启动后主页预览空白（首页直接点眼睛显示空白，先进编辑器才正常）

### 需求
主页相册卡片眼睛进入预览，应用刚启动（冷启动）后点进去封面/页面内容均空白；先进一次相册编辑器回主页再预览才正常。

### 根因
预览调用 `renderSinglePage`/`renderAllPages` → `renderPageThumbnail` 时**没有显式传入 `albumSize`**，而 `renderPageThumbnail` 内部用 `options?.albumSize ?? useEditorStore.getState().albumSize`。冷启动时编辑器 store 的 `albumSize` 为 null → `calcThumbSize(null)` 返回 null → 整页渲染返回 null → 封面占位、内容空白页。进入编辑器一次后 store 有了相册尺寸，预览随之正常。（与主页封面卡片「必须按当前相册尺寸渲染而非依赖全局 store」的既有约束同源。）

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- `boot` 中构造 `renderAlbumSize = { width: albumSize?.width ?? 210, height: albumSize?.height ?? 280 }`，显式传给 `renderSinglePage`/`renderAllPages`。
- `renderSinglePage`/`renderAllPages` 增加 `albumSize` 形参并写入 `renderPageThumbnail` 的 `RenderOptions.albumSize`。
- 兜底不变：内容页单页失败仍回退 `BLANK_PAGE_DATAURL`，封面失败保持占位，避免缺页/崩坏。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23.7s）。

---

## 2026-08-17 · 预览封面/封底居中补偿书脊 + 去掉封面/封底提示文案

### 需求
1. 进入预览后，封面/封底硬壳页面水平居中偏左一点，需修正为真正居中。
2. 封底有一行「翻到最后一页，展示封底」提示文字；封面也有图形化点击提示——均去掉。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **居中补偿书脊**：相册封面页自带左侧书脊，硬壳封面视觉重心偏左。按展示比例换算书脊像素宽 `spinePx = (spineWidth×coverW)/mmW`，封面右移 `spinePx/2`、封底（书脊在右）左移 `spinePx/2`，使整本书（正面+书脊）以容器中心对称。取封面页 `page.spineWidth` 计算，仅有书脊的相册生效。
- **去掉提示**：删掉封底 `backHint` 文字行、封面 `hint-tap` 图形化点击提示及其 CSS（`.hint-tap`/`.hint-ring`/动画）。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23.2s）。

---

## 2026-08-17 · 主页预览的顶部任务栏与编辑器预览态保持一致（自绘预览任务栏）

### 需求
主页相册卡片眼睛入口进入预览后，顶部任务栏也要与编辑器预览功能一致（标题居中 + 退出预览，隐藏功能性按钮）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- 新增 `topBarTitle` prop：提供时视为「主页入口」。此时覆盖层 `top:0` 顶到窗口最上、盖住主页 AppHeader，并自绘预览态顶部任务栏（高 `--layout-toolbar-height`，标题居中 + 「退出预览」按钮 + `WindowControls` 窗口控制），风格与编辑器预览态（标题居中 + 退出预览）一致。
- 编辑器入口（无 `topBarTitle`）保持原样：覆盖层 `top` 为任务栏高、由编辑器 Toolbar 承担预览态任务栏，不重复自绘。

**[ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)**
- 传入 `topBarTitle={fullscreenProject.name}`，主页预览显示该相册标题。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23.4s）。

---

## 2026-08-17 · 主页相册卡片左上角眼睛入口改为打开「真实效果预览」

### 需求
主页相册卡片左上角的眼睛入口，从原来的全屏查看（FullscreenView）改为点击进入「预览功能」（实物书翻页预览 BookPreviewOverlay）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- 新增可选外部数据 props：`pages?`/`albumSize?`/`photos?`（与 FullscreenView 同模式）。内部「外部传入优先，否则回退编辑器/全局 store」——主页直接复用预览组件，无需先进入编辑器。

**[ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)**
- 眼睛入口渲染由 `FullscreenView` 改为 `BookPreviewOverlay`，仍传 `fullscreenProject.pages`/`allPhotos`/`size`，退出后仍停留相册主页。

**i18n**：`fullscreenView` 文案键由「全屏查看/Fullscreen view」改为「真实效果预览/Realistic preview」。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（22.3s）。

---

## 2026-08-17 · 预览优化：封底快捷按钮图标改向下箭头 + 进度条跳页加入翻页动画

### 需求
1. 封底按钮图标原来是「加号」，改成「向下箭头」（与封面向上箭头相反）。
2. 点击进度条切换页时要有翻页动画（之前跳页是瞬移，无效果）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **封底图标**：`M12 5v14M5 12h14`（加号）→ `M12 5v14M5 12l7 7 7-7`（向下箭头），与封面向上箭头 `M12 5v14M5 12l7-7 7 7` 镜像对称。
- **进度条跳页带翻页效果**：根因是此前用 `turnToPage()`（库源码 `= pages.show()`，直接瞬移到目标对开，无动画）。改为 `flip(target, 'bottom')`（库源码 `= flipController.flipToPage()`，会相对目标位置向前/后翻一页落地，带动画）。在 `handleProgressToPage` 中带范围守卫 + try/catch 回退到 `turnToPage`。

**[page-flip.d.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/page-flip.d.ts)**
- 补充 `flip(page, corner)` 类型声明（此前未声明，调用会 TS 报错）。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（22.3s）。

---

## 2026-08-17 · 预览底部悬浮区 UI 重设：进度条拉长铺满中部 + 按钮文字简化 + 控件上移

### 需求
1. 进度条变得很短，整体布局效果不对，需重新设计底部悬浮区 UI。
2. 快捷按钮文字简化：「封面 / 封底」。
3. 按钮和进度条贴底太死，整体上移一点。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **整行布局**：悬浮区改为全宽一行（`max-w-[880px]` 居中），封面按钮在左、**进度条 `flex-1` 占满中部**、封底按钮在右；去掉进度条内部 `max-w` 上限（改为 `w-full min-w-0`），进度条随之铺满中部，不再缩成短条。
- **上移**：`FLOAT_BAR_H=40`（> 页面预留区 `OVERLAY_RESERVE=36`）+ 容器 `paddingBottom: 8`，控件整体上移约 6px，不与底边贴死。
- **按钮文字简化**：i18n `goCover`/`goBack` 由「返回封面/返回封底」改为「封面/封底」（en: `Cover`/`Back`），胶囊按钮文案更简洁。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（22.5s）。

---

## 2026-08-17 · 预览底部悬浮区：新增「返回封面/封底」快捷按钮 + 修复进度条 hover 唤不起

### 需求
1. 底部悬浮区加「封面 / 封底」快捷按钮，翻页时一键返回封面/封底硬壳页。
2. 修复：鼠标移到底部悬浮区无法唤起进度条（怀疑图层在最底收不到事件）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **hover 唤不起根因**：隐藏时把 `pointer-events-none` 加到了承载 `onMouseEnter` 的**同一个外层元素**上，隐藏后自身不可交互，收不到 hover。**修复**：外层容器（`absolute bottom-0`，高 `OVERLAY_RESERVE`）始终保持可交互、持有 `onMouseEnter/onMouseLeave`；只有内层内容（进度条/按钮）在隐藏时才 `pointer-events-none + opacity/translate-y 滑出`。
- **新增快捷按钮**：悬浮区内进度条两侧新增 `QuickJumpButton`（图标+文案胶囊），「返回封面」=`closeBook('front')`、「返回封底」=`closeBook('back')`，播放合书动画平滑切回对应硬壳页（对齐全屏交互）。新增 i18n 键 `goCover`/`goBack`（zh/en）。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（22.6s）。

---

## 2026-08-17 · 预览书大小/位置改为按页面计算（不扣进度条占位）+ 进度条改底部悬浮覆盖

### 需求
相册书的大小和位置不要预留进度条那一行的高度计算，改为按页面内容区计算；进度条做成底部悬浮覆盖层（确认方案：底部留薄悬浮区给进度条，弹出时不遮书）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **尺寸按页面算**：删掉 `PROGRESS_ROW_H=48` 占位常量，新增 `OVERLAY_RESERVE=36`（薄悬浮区）。书体可用高 `availH = vh − 任务栏 − OVERLAY_RESERVE − 边距`，不再按进度条整体扣一档；配合删除底部 48px 的固定 Flex 行，书体高度增大、更贴近页面内容区。
- **进度条改底部悬浮覆盖**：从 Flex 布局中的固定行，改为内容区内 `absolute bottom-0` 的悬浮层（高度 `OVERLAY_RESERVE`），保留自动收起逻辑（进入翻页显示、2.5s 渐隐、底部 hover 唤起）。封面/封底/翻页书的居中容器都加了 `paddingBottom: OVERLAY_RESERVE`，书体居中于其上方的页面区域内，进度条弹出只落在底部薄悬浮区、不遮书。
- **居中基准按页面**：内容区 = 整页高度（`vh − 任务栏`）的 `flex-1`，书体在扣除底部悬浮区后的区域垂直居中，位置随页面自适应，不再被独立进度条行抬高/占用。

### 验证
- `PROGRESS_ROW_H` 无残留引用；`npm run build`（`tsc -b && vite build`）零错误，构建成功（22.6s）。

---

## 2026-08-17 · 预览白底缓冲减半（书更大）+ 底部进度条改「悬浮自动收起」

### 需求
1. 把翻页书四周的白色缓冲画布高度减半，释放上下空间，让实物书放得更大。
2. 底部进度条改成自动收起设计：平时隐去，滑入唤起、数秒无操作渐隐滑出（对齐全屏查看交互）。确认方案：进度条仅隐藏，腾出的布局高度不留作书体增长（书体尺寸不变，避免翻页书动态重构）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **白底缓冲减半**：`PAD_Y` 110 → **55**（上下各减 55px）。尺寸预算 `(availH − 2×PAD_Y)×ar` 随之放宽容限，书体高度约增加 110px。代价是极端页角上/下翻时的画布余量减弱，正常翻页不裁边。
- **进度条悬浮自动收起**：新增 `progressVisible` 状态 + 收起定时器（2.5s）；底部进度条改为覆盖层，`transition fade + translate-y` 滑出。
  - 进入翻页先显示进度条、随后自动收起；非翻页态一律隐藏；
  - 底部区域 `onMouseEnter=showProgress` / `onMouseLeave=hideProgressDelayed`（对齐全屏查看）；
  - 底部 `PROGRESS_ROW_H` 高度恒定保留，内容区高度三态一致，书体垂直居中基准不变。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23.0s）。

---

## 2026-08-17 · 修复预览书放大后穿帮：翻页白色缓冲底溢出到任务栏

### 需求
上一版把书放大到铺满可用区后，翻页书四周的白色缓冲画布（用于避免页角被裁）随之放大，向上溢出到顶部任务栏，产生穿帮，效果差。要求在「尽量大」与「不穿帮到任务栏/进度条」之间平衡——预留白底缓冲，让书体完好落在预览区内。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- 将翻页缓冲 `PAD_X`/`PAD_Y` 纳入尺寸预算，单页双约束改为：
  - 宽：`(availW − 2×PAD_X)/2`（对开书体 + 左右白底 ≤ 可用宽）；
  - 高：`(availH − 2×PAD_Y)×ar`（书体 + 上下白底 ≤ 可用高）。
- 保证「书体 + 四周白底」完整落在预览区内，不再向任务栏/进度条方向溢出穿帮；书体在剩余空间内仍然尽量放大。

### 验证
- 800px 高屏竖版推演：书体、缓冲白底均不超出内容区（含任务栏下方与进度条上方留白）。
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23.8s）。

---

## 2026-08-17 · 实物书预览放大：书尽量占满屏幕（移除设计尺寸上限 + 高度预算用满）

### 需求
实物书预览中，希望用户看到的相册书尽量大（预览追求视觉大，真实翻阅感更强）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **移除「设计尺寸 ×1.1」硬顶**：旧逻辑 `coverW = min(designW×1.1, vw×0.5, vh×0.6×ar, (vw−180)/2)`，高度预算只用到 60% 视口，且 1.1 倍设计上限进一步压小。
- **改为「占满可用预览区」**：按真实宽高比让单页同时满足双约束——`coverW = min(availW/2, availH×ar)`，其中
  - `availW = vw − 180`（两侧圆形翻页按钮留白）；
  - `availH = vh − 56(任务栏) − 48(进度条行) − 12(上下留白)`（实际可用高度预算用满，而非固定 60%）；
  - 兼顾竖版（书不低于可用高）与横版（书不超可用宽），页高/页宽由真实比例互推。
- 封面单页 / 翻页书对开 / PageFlip 画布三处仍共用一个 `pageSizeRef`，尺寸完全同源，保持在分辨率变化下对齐。

### 验证
- 竖版 210×280、横版 280×210 推演：两方向约束下书均能贴近屏幕边沿放大，且不溢出任务栏/进度条区域。
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23.9s）。

---

## 2026-08-17 · 翻页预览观感优化：降折角阴影透明度 + 收窄页堆厚度变化幅度 + 放慢过渡

### 需求
1. 翻页时上下页角变"透明"失真。
2. 翻页时左右页数堆叠的移动幅度太明显，观感不自然（要求变化更小、过渡更慢更柔）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **降阴影**：PageFlip 配置 `maxShadowOpacity` 0.85 → **0.55**，翻页折角阴影更淡，减少折角透明度失真。
- **页堆变化收窄**：`MAX_STACK` 26 → **16**（`BASE_STACK` 保持 6），两侧页堆厚度增减幅度从 20px 收窄到 10px。
- **过渡更慢更柔**：`.page-stack-left/right` 的 `width` 过渡由 `0.35s cubic-bezier(0.22,1,0.36,1)` 改为 **`0.5s cubic-bezier(0.33,1,0.68,1)`**。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（22.95s）。

---

## 2026-08-17 · 翻页预览三连修复：书本与书框错位（尺寸竞态）/ 翻页穿模（层级）/ 尺寸不符用户设定

### 需求
1. 点击封面进入翻页后，翻页书与设计的书框（书缘/页堆/沟槽装饰层）位置不一致。
2. 翻页时翻起的页面未置于顶层，上下效果边框出现"穿模"（翻起页被书缘条遮挡）。
3. 实物书的尺寸没有按用户设定的相册尺寸生成。

### 根因（PageFlip 源码级定位）
- **尺寸竞态（1+3 同源）**：初始化 effect 依赖仅 `[open]`。会话恢复时 store 异步 hydrate，`albumSize`/`pages` 可能晚于 `open` 到达 —— boot 按默认 210×280 建书后**永不重建**；而装饰书框（JSX）随后按真实尺寸渲染 → 书本与书框比例/位置错位、尺寸不符设定。CanvasUI 构造时 `el` 被 PageFlip 写入 `width:100%/minWidth/minHeight/display:block` 并注入 `.stf__parent{position:relative}` CSS（覆盖 Tailwind `.absolute`）。
- **穿模（2）**：el 被 PageFlip 变为 `position:relative`（z:auto），装饰层 page-edge(z:4)/page-stack(z:5)/gutter(z:4) 均为正 z-index → 画布整体沉在装饰层之下；翻起的软页翅出书体画进上下/左右缓冲区时被书缘条/页堆**压住**，形成穿模。
- 补充源码结论：canvas 位图同步在 `ui.update()→resizeCanvas()`（读 `getComputedStyle`），`loadFromImages` 后 +1ms 的 `init` 定时器触发；因此 `applyFlipBuffer` 在 `loadFromImages` 之后同步调用即可在首次绘制前生效（位图=扩容尺寸，书本按 boundsRect 在 el 内居中，不随扩容移动）。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **尺寸竞态修复**：新增 `sizeKey = ${w}x${h}`，初始化 effect 依赖改为 `[open, sizeKey, pages.length]` —— albumSize/pages 迟到或变化时自动销毁重建翻页书，书本严格按用户设定尺寸生成，与装饰书框（每次渲染按最新 albumSize 计算）始终同源。
- **翻起页置顶层（穿模修复）**：`applyFlipBuffer` 给 el 设 `z-index:10` —— 翻起的软页盖过书缘(page-edge z:4)/页堆(page-stack z:5)（实物书中翻起纸页在书口断面之上）；静态时画布内容只在书体区内、装饰条在书体外，提层级不影响静态观感。
- **书脊沟槽分层**：`.book-gutter` z-index 4→12（置于画布之上，静态时沟槽阴影仍叠在纸面中央）；监听 PageFlip `changeState` 事件（`read|user_fold|fold_corner|flipping`），翻动中给 gutter 加 `gutter-hidden`（opacity 0 + 0.25s 过渡）淡出，静止后淡入还原 —— 避免沟槽阴影压在翻起页上形成新穿模。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（24.9s）。
- 几何推演复核：el 盒 = 书体四向外扩 PAD_X/PAD_Y，boundsRect 居中 → 书体仍精确覆盖 bookWrap（与装饰层 0 偏差，维持上轮实测 ≈0px 对齐基线）；层级变为 shadow(1) < edge/stack(4/5) < canvas(10) < gutter(12)。

---

## 2026-08-17 · 翻页书与封面尺寸/位置统一 + 翻页位置偏移/页角裁剪根因修复

### 需求
1. 翻页时页面尺寸、位置要与封面（实物效果空白页面）在页面上对齐；此前翻页位置偏上/错位。
2. 翻页时卷起的页面页角被裁剪，显示不全。

### 根因（代码层面定位）
- **尺寸不一致**：封面宽用 CSS calc 字符串（`coverWidthCss`），翻页书用另一套 `spreadWidthCss`（含额外 `100vw-220` 上限），两处数字不同源；PageFlip 初始化又去量 `invisible` 容器，取值不可靠。窄窗口下翻页书单页被额外 cap 压得比封面小。
- **偏移 + 页角裁剪（同一根因）**：StPageFlip 会把 `.stf__parent`（containerRef 挂载的 el）**强制改为 `position:relative` 并写入显式 `width/height` = 书体尺寸**。原代码用 CSS 负偏移 `left/top:-CORNER_PAD` 想"外扩缓冲"，对 relative + 固定宽高的元素负偏移只是**平移**（把整本书上移 PAD、左移 PAD，破坏对齐），且 canvas 缓冲仍是书体大小，软页翻起时页角伸出书体就被位图边界裁剪。
- **位置偏上**：翻页模式下底部进度条使 `flex-1` 内容区变矮，翻页书居中基准整体上移；封面是 `[书本, 点击提示]` 纵向列 `justify-center`，提示元素又把封面书本往上顶——两种模式垂直居中基准不统一。

### 改动
**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **尺寸单源统一**：新增 `pageSizeRef` 组（`coverW`/`spreadW`/`pageH`），在渲染期由 `mm × MM_TO_PX` 计算并复用同一数值——封面/封底 `width: coverW`、翻页书 wrapper `width: spreadW(=2×coverW)` + `aspectRatio: spreadW/pageH`、PageFlip 初始化 `width: coverW`（单页）`height: pageH`（size:'fixed'）三处完全同源。先钳制单页宽（不再对总宽追加 `100vw-220` cap），保证窄窗口下翻页书单页 == 封面单页。
- **画布缓冲正确做法**：新增 `applyFlipBuffer`，在 `loadFromImages` 后（及 ResizeObserver 内）把 el 盒尺寸四向扩成 `spreadW+2PAD_X × pageH+2PAD_Y` 并设 `left/top=-PAD` 使其关于书体居中；PageFlip 按 boundsRect 在 el 内居中绘制书体，故只扩缓冲不移动书体。将 `CORNER_PAD` 拆为 `PAD_X=64`/`PAD_Y=110`，el JSX 改 `inset:0`（书体居中、与封面/装饰书框对齐）。
- **垂直居中基准统一**：底部进度条区域改为恒定高度 `PROGRESS_ROW_H=48` 且各阶段始终占位，保证内容区高度在封面/翻页/封底三态一致；封面/封底书本单独居中（移除纵向 flex 列 + gap），点击提示/文字改为 `absolute bottom:9vh left:1/2` 覆盖式（不参与布局）；翻页书 wrapper 去掉不对称 `pt-6 pb-8`。三态书本共用同一垂直居中基准。
- 内容页/封面渲染分辨率 `baseWidth` 720→1440 提升清晰度。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23s）。
- 浏览器实测：翻书页身垂直中心与封面卡片垂直中心差 ≈0px（不再 ~110px 上移 / ~64px 左移）；翻页动画中途画布像素扫描 `edgeHit=0`，页身四周留白缓冲（上下 ≈111px、左右 13–26px），卷起页角完整显示、不再被裁剪。

---

## 2026-08-17 · 翻页预览十项修复优化（页角裁剪根治/四向书缘/真实页堆/任务栏预览态等）

### 需求
1. 翻页按钮外移留空间感；2. 预览态标题居中+隐藏保存/导出；3. 翻页书光标与封面一致；4. 书缘四向阴影（原缺上下）+效果增强；5. 翻页时页角被裁剪；6. 页堆真实化（内衬+纸页效果）；7. 翻页阴影真实度；8. 整体尺寸偏大；9. 无修改返回主页不弹保存确认；10. 封面打开提示图形化动画。

### 改动

**[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)**
- **页角裁剪根治（第5项根因）**：StPageFlip 用 canvas 渲染，画布位图尺寸=容器尺寸，软页翻起时页角超出书体矩形被画布边界裁剪。修复：翻页画布容器 `absolute` 上下各外扩 `CORNER_PAD=44px`（宽度不变），画布内书体按宽度驱动自动垂直居中（`pageWidth=blockWidth/2`，`pageHeight=pageWidth/ratio` 不超画布高即宽度驱动），与装饰层精确对齐；书体投影从 `.stf__parent` 移到独立 `.book-body-shadow` 层（精确贴合书体矩形，不随画布外扩变形）。
- **四向书缘+阴影增强（第4项）**：新增 `.page-edge-top/bottom` 上下书缘薄纸断面（3px 竖向条纹+上下投影）；书体投影升为四层（贴地接触/中距离/环境/远场大柔影）；左右页堆投影加深。
- **页堆真实化（第6项）**：左右页堆改为「约 2.2px/页米白纸页条纹 + 靠书芯渐暗渐变 + 最外侧 `stack-endpaper` 米色内衬色带（4.5px，前后内衬贴硬壳内侧的实物结构）」，保留随对开进度左增右减的厚度变化与 0.35s 平滑过渡。
- **翻页阴影（第7项）**：`maxShadowOpacity` 0.65 → 0.85（页下投影+页内转印阴影更接近真实纸张光影）；书脊沟槽阴影略加深（0.08→0.10 峰值）。
- **尺寸放大（第8项）**：封面 `min(designW×1.1, 66vw, 76vh×ar)`（原 62vw/62vh）；翻页书 `min(designW×2.2, 86vw, 80vh×ar×2)`（原 72vw/76vh）。
- **按钮外移（第1项）**：NavArrow 从 flex 布局（紧贴书体 gap-7）改为绝对定位 `left-7/right-7 + top-1/2` 贴容器两缘，与书体脱耦。
- **光标（第3项）**：bookWrap 加 `cursor-pointer`；`.book-preview-flip canvas { cursor: pointer }`（StPageFlip 事件挂在 canvas 即 distElement 上）。
- **封面提示图形化（第10项）**：删除文字提示，改为光标箭头点按动画（2s 循环下压）+ 双波纹错峰扩散（品牌色，`hint-ring` scale 0.25→1.15 淡出），保留 title/aria-label 无障碍语义。

**[Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx) / [AppHeader.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/AppHeader.tsx)（第2项）**
- 标题改绝对居中：AppHeader header 加 `relative`，标题容器 `absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2`（max-w 收窄 320px 防与两侧按钮重叠），预览态隐藏左侧功能区后标题仍精确居中于整个任务栏。
- 预览态隐藏保存/导出按钮（`!isBookPreviewOpen` 条件渲染），仅保留「退出预览」。

**[EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx)（第9项）**
- `handleBack`：`!useHistoryStore.getState().canUndo()`（无可撤销编辑步骤=进入后未做任何修改）时直接走 `executeSaveAndBack` 静默返回（保存为轻量 no-op，兜底 2s 防抖内的照片变更），不弹保存确认弹窗；有修改才弹窗。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（23.1s）。

---

## 2026-08-17 · 封面硬壳效果四处统一 + 翻页预览全面修复优化（尺寸/按钮/任务栏/合书/阴影/页堆）

### 需求
1. 主页封面设计库、编辑器封面左侧面板、翻页预览的封面均与主页相册封面样式一致。
2. 预览：翻页书分页尺寸与封面一致（此前过小、页堆与页面不一致）；上一页/下一页改为翻页书两侧圆形图标按钮（照片查看大图样式）；顶部任务栏保持编辑器样式，预览按钮切换为「退出预览」，预览时隐藏文件/页面/撤销/重做；点击前衬页合上回封面硬壳、点击后衬页合上到封底硬壳（与翻开流程互逆）；书缘增加阴影避免白色书缘融入白色背景；拖拽翻页时页堆厚度同步变化。

### 改动
- 新增 [HardcoverFrame.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/HardcoverFrame.tsx)：精装硬壳书板共享外框（纸板边缘高光/暗边 + 铰链压槽 + 表面光泽 + cqw 定尺寸多层浮起投影 + 落地投影），`variant: front/back` 支持封底镜像。[CoverPageCard.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverPageCard.tsx)（主页相册卡片）、[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)（模板库/编辑器面板）、[BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)（预览封面/封底）四处统一改用此组件。
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx) 重构：
  - 尺寸：封面单页宽与翻页书对开宽同源于设计 mm（`designW = mmW × MM_TO_PX`），`usePortrait: false` 强制对开，单页宽 = 封面页宽，翻页书不再缩小；`spreadWidthCss = min(2×designW, 72vw, 76vh×ar×2)`。
  - 衬页合书：`downPosRef` 记录按下位置（位移 >6px 视为拖拽），首个对开点击左半（前衬页）→ `closeBook('front')` 合上回封面；末个对开点击右半（后衬页）→ `closeBook('back')` 合上到封底；合书动效 `book-closing-left/right`（3D 收拢）+ 封面/封底 `book-settle-front`/`book-preview-close` 落定动效，与翻开互逆。
  - 翻页按钮：两侧 `NavArrow` 圆形图标按钮（hover 缩放、backdrop-blur，照片查看大图样式），首对开再向前 → 合上回封面，末对开再向后 → 合上到封底。
  - 书缘阴影：`.stf__parent` 多层空间投影（贴地接触 + 中距离 + 环境）+ 页堆层自带边缘投影 + `book-gutter` 书脊沟槽静态阴影，白底上书缘清晰浮起。
  - 页堆：`page-stack-left/right` 独立纸条纹层，厚度 = `BASE_STACK(6) + progress×20` 左增右减，进度条拖拽 `turnToPage` 与 flip 事件均更新 `pageIndex` 驱动平滑过渡。
  - 页码：内容页数 `contentTotal = total - 2`（去前后衬页），进度条值映射对开右页。
- [Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx)：预览态（`isBookPreviewOpen`）隐藏文件/页面菜单、分隔线与撤销/重做；预览按钮变切换式「预览 ↔ 退出预览」（退出态 X 图标）；预览浮层改为 fixed 定位于任务栏下方（`top: var(--layout-toolbar-height)`），任务栏保持编辑器样式。
- [page-flip.d.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/page-flip.d.ts)：`FlipEvent.data` 改为联合类型 `number | FlipEventData`（flip 事件 data 为纯数字页码，init 事件为 `{page, mode}`），新增 `FlipEventData` 接口；BookPreviewOverlay 回调同步简化为双格式兼容解析。
- i18n：zh-CN/en-US 新增 `editor.exitBookPreviewBtn`（退出预览 / Exit Preview）。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功（24.5s）。

---

## 2026-08-17 · 修复翻页预览构建报错（flip 事件类型 + 未使用参数）

### 需求
`npm run desktop:build` 的 `beforeBuildCommand: npm run build` 报 3 个 TS 错误，构建中断。

### 根因
1. `flip.on('flip')` 回调类型写成了 `(e: { data: number })`，与库声明的 `FlipEvent`（`data: { page, mode }`）不兼容；且运行时 data 的确是 `{ page, mode }`，原写法把整个对象赋给了 `pageIndex`。
2. `renderSinglePage` / `renderAllPages` 两处 helper 的 `albumSize` 形参从未使用（封面/封底/内容页渲染实际走缩略图引擎，不需尺寸参数）。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：`flip.on('flip')` 改为类型化 `(e: FlipEvent)` 并读取 `e.data.page`（`FlipEvent` 用 `import type` 导入以满足 `verbatimModuleSyntax`）；移除两处 helper 的 `albumSize` 形参及其 3 处调用实参。

### 验证
- `npm run build`（`tsc -b && vite build`）零错误，构建成功。

---

## 2026-08-16 · 翻页书"出现即消失"最终根因：边界 effect 对初始 pageIndex=0 误判

### 需求
点击封面进入翻页后翻页书仍消失，回到"点击封面打开相册"。

### 根因
进入 flip 时 `pageIndex` 状态仍是初始值 0；边界 effect 依赖 `[pageIndex, stage]`，`stage` 变为 `'flip'` 即触发 effect，此时 `pageIndex===0` 命中 `pageIndex <= 0` 分支 → 450ms 后 `setStage('cover')`，把刚显示的翻页书又切回封面。

### 方案
- 新增 `prevPageRef` 记录上一次页码：边界切换仅在「页码真的发生变化（prevPage!==pageIndex）」时生效，排除进入 flip 时初始 pageIndex=0 的误判。
- `toFlipFromCover`（点封面）进入 flip 时同步 `setPageIndex(1)` + `prevPageRef.current=1`，使 state 与基准一致。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增 `prevPageRef`；边界 effect 增加页码变化判定；点封面进入时同步页码为 1。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 修复点击封面后翻页书"出现即消失/回封面"（书体尺寸 + flip 边界误切）

### 需求
实测仍复现：点击封面进入翻页，翻页书出现一下后消失，界面回到"点击封面打开相册"。

### 根因（两处叠加）
1. **书体尺寸错乱**：`usePortrait=true`（竖版）时 StPageFlip 的 `pageWidth = 整本书宽`（单页宽），而非对开。此前翻页书容器用了对开尺寸 `spreadWidthCss=2×designW` + `spreadAspectCss=2×mmW/mmH`，使 StPageFlip `size:'stretch'` 把书拉开到设计尺寸约 2 倍宽、超出 `maxWidth` 约束，导致拉伸错乱/不可见。
2. **flip 边界误切**：翻页书初始化/`startPage` 自动 `show` 就会触发一次 `flip` 事件（页码=起始页），此前在 flip 事件里直接切 `stage('cover'/'back')`，无守卫时把刚进入的翻页误切回封面。

### 方案
- **书体改为单页宽**：翻页书容器 `bookWidthCss = coverWidthCss`（单页）、`bookAspectCss = aspectCss`，与封面单页/相册设计尺寸 `designW×designH` 同源，消除拉伸错乱。
- **flip 事件只同步页码**：从 flip 事件里移除 `setStage` 边界切换，仅 `setPageIndex`（供页堆厚度/进度条）。恢复/封面/封底切换改由其他可控入口（上/下页、进度条）触发，避免初始化幻影事件切走阶段。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：翻页书容器改用单页宽高（删 `spreadWidthCss/spreadAspectCss`）；`flip.on('flip')` 移除 stage 切换，只同步页码；删除 `enteredFlipRef`。

### 验证
- `npx tsc --noEmit` 零错误；`npx vite build` 成功。

---

## 2026-08-16 · 修复点击封面进入翻页后翻页书"出现即消失"

### 需求
实测：点击封面进入翻页，翻页书出现一下然后就消失了。

### 根因
翻页书记载 `flip` 事件即触发 `setStage('cover'/'back')` 边界切换，但该事件在**初始化/startPage 自动 show** 时也会触发一次（页码=startPage）。此前无守卫，导致翻页书显示瞬间被误切到封面/封底层而"消失"。

### 方案
- 新增 `enteredFlipRef`：**只有用户主动进入翻页后才允许 flip 边界触发 stage 切换**。
- `toFlipFromCover`（点封面）时置 `enteredFlipRef.current = true`；`flip.on('flip')` 中先检查该标记，未进入则不切 stage，仅同步页码。
- 避免初始化/startPage 的 flip 事件误切阶段导致翻页书闪退。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增 `enteredFlipRef` 守卫；`flip.on('flip')` 边界切换前校验；点封面时置位。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 修复翻页书页堆厚度不随翻页动态变化

### 需求
实测发现页堆厚度左右不会随翻页动态变化。

### 根因
**StPageFlip 的 flip 事件 data 传的是纯数字页码**（`PageFlip.updatePageIndex` → `trigger('flip', this, newPage)`），而非 `{ page }` 对象。此前代码用 `e.data.page` 取值恒为 `undefined`，被 `Number.isFinite` 守卫拦下，`pageIndex` 从不更新，页堆厚度也随之静止。

### 方案
- 修正 flip 事件处理：`data` 为数字页码时直接使用，`const page = typeof e.data === 'number' ? e.data : NaN`。
- 页码正确更新后，`safePage` → `progress` → `stackLpx/stackRpx`（含 `BASE_STACK` 基础厚度）随之动态变化，左右页堆随翻页平滑过渡。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：flip 事件 data 改为按纯数字页码解析。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 修复翻页书左侧页堆厚度缺失

### 需求
实测发现翻页书只有右侧有页堆厚度，左侧没有。

### 根因
页堆厚度按 `progress × MAX_STACK` 分配左右，起始页 `safePage=1` 时 `progress` 极小，左侧厚度趋近 0（仅约 3px），不可见。

### 方案
- 左、右页堆各设最小基础厚度 `BASE_STACK=6px`，翻开后两侧始终清晰可见。
- 厚度仍随进度「左增右减」：`stackLpx = BASE + progress×(MAX-BASE)`、`stackRpx = BASE + (1-progress)×(MAX-BASE)`，左代表已翻页、右代表未翻页。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：页堆厚度公式加入 `BASE_STACK` 基础厚度；`clamp` 改用 `Math.min/max`。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 修复预览相册尺寸（封面单页 / 翻页书对开同源）

### 需求
实测反馈预览相册尺寸不对。

### 根因
封面/封底是单页（合上的书 = 单页宽），而翻页书摊开是**对开 2 页并列**。此前误将翻页书容器宽度设成单页宽 `coverWidthCss`，导致对开两页被横向压缩失真，尺寸与相册实际不一致。

### 方案
- 区分「单页宽」（封面/封底硬壳书板）与「对开宽」（翻页书摊开）：
  - `coverWidthCss = min(designWpx, 70vw, 70vh*ar)`（单页，210×280）
  - `spreadWidthCss = min(2*designWpx, 92vw, 92vh*(2*ar))`、`spreadAspectCss = 2×mmW / mmH`（对开）
- 封面/封底保持单页宽（物理正确）；翻页书容器改用 `spreadWidthCss` + `spreadAspectCss`，与封面同源比例，摊开不再失真。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增 `spreadWidthCss`/`spreadAspectCss`，翻页书容器改用对开尺寸；封面/封底单页宽不变。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 翻页预览重制为「硬壳书板 + 内容软翻页」

### 需求
用户实测反馈：封面/封底合上时应为**硬壳精装书板**效果（参考封面模板 CoverPreview），而非软翻页；翻页书立体感（页堆厚度）未实现、书与背景融为一体；跳页时页堆厚度突变不自然；去掉落地投影。

### 方案
- **硬壳书板与软页分离**：封面/封底改为「硬壳书板」——优先用 `CoverPreview`（含书脊/铰链压槽/投影）呈现合上（封底盖顶）质感，模板缺失回退真实渲染图；翻页书只承载软页 `[衬页 | 内容1..N | 衬页]`。三段式 cover → flip → back 通过过渡衔接。
- **页堆厚度重做**：弃用 `.stf__parent` 伪元素（不生效），改为**独立 `page-stack-left/right` 兄弟层**，相对书体包裹容器 absolute 贴边，`repeating-linear-gradient` 纸条纹，宽度随进度 `progress×MAX_STACK` 分配，且带 `transition: width 0.35s` 平滑过渡（修复跳页突变）。
- **立体感**：翻页书 `.stf__parent` 保留多层空间投影（贴地接触+中距离+环境）；**移除落地投影**（椭圆 radial + blur），背景保持纯白。
- 页码语义修正：内容页数 `contentTotal=total-2`，内容页序号与翻页书索引映射修正（索引0=前衬页，索引c=第c内容页），消除 NaN 与错位。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：封面/封底改硬壳书板（CoverPreview/renderImage）；翻页书只含软页；新增 `page-stack-left/right` 独立页堆层；移除落地投影；页码/索引映射修正。

### 验证
- `npx tsc --noEmit` 零错误；`npx vite build` 成功；浏览器加载首页 UI 正常渲染、无前端错误（预览依赖 Tauri 后端，需桌面端实测）。

---

## 2026-08-16 · 翻页预览重制为「整本真实书·连续」（修复 NaN/进度条/立体感）

### 需求
用户反馈此前效果与需求不符。确认后方向：**整本真实书·连续**，所有页面（封面/衬页/内容/封底）统一放入同一翻页书、无弹层跳切；封面作为首页单页即「合上」、封底作为末页单页盖顶即「合上」；修复截图暴露的「第 NaN 页」页码 bug、进度条位置/当前页状态、翻页书立体感（页堆厚度/投影）。

### 方案
- **整本连续模型**：删除原「封面/封底弹层 + 阶段(stage)跳切」逻辑，改为单一翻页书，页序 `[封面(首页单页) | 衬页 | 内容1..N | 衬页 | 封底(末页单页)]`。`showCover:true` 使封面作为首页单页呈现闭合状态（初始即合上的书）；封面翻开第一屏为 `[衬页 | 内容1]`，实现「左边空白内页、右边第1张内容」；封底为末页单页盖顶（合上状态），继续向前可翻回内容。
- **奇数页补位**：封面之后到封底前的内页 `[衬页, 内容…, 衬页]` 若为奇数则补一张白色衬页，保证封底在末对开单独成页盖顶。
- **NaN 修复**：页码状态经 `Number.isFinite` 守卫，渲染前再钳制 `safePage = clamp(pageIndex, 0, total-1)`；flip 事件仅接受有限数页码，杜绝 NaN 溢出到 UI。
- **进度条**：移至底部固定（翻页时显示），`PageProgressBar value=safePage+1 total=总页数`；拖拽回传 1-based 页码，`handleProgressToPage` 转 0-based 后 `turnToPage`（修正此前错位 bug）。
- **立体感**：`.stf__parent` 叠加三层空间投影（贴地接触+中距离+环境）+ `::before`/`::after` 左右纸条纹页堆边缘，宽度由 CSS 变量 `--book-stack-l/r` 随进度实时变化（`progress×22px`），合上时厚度集中一侧；底部椭圆落地投影让书从白底浮起。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：重写为整本连续模型；删封面/封底弹层与 stage 逻辑；`totalPagesRef` 计数全部页；页码安全钳制；进度条底部固定且修复数组越界；页堆厚度改 CSS 变量驱动。

### 验证
- `npx tsc --noEmit` 零错误；`npx vite build` 成功（翻页书 ESM bundle 正确打包）。

---

## 2026-08-16 · 翻页书真实页序 + 动态页堆厚度（重制预览交互）

### 需求
按真实精装书页序重制预览：翻页书页序 `[空白衬页 | 内容页… | 封底前白色补位 | 封底页]`；翻到末页(封底)继续翻 → 合上展示封底硬壳实物；初始封面硬壳实物、点击翻开进入翻页书；第一页往前翻回封面合上、最后一页往后翻回封底合上；进度条下方显示；动态页堆厚度随进度实时变化 + 空间投影。

### 方案
- **封底纳入翻页书**：翻页书页序改为 `[endpaper, ...contentImages, whitePage(白色补位), backPageImage]`。封底用模板应用后的真实平面渲染图（`renderSinglePage(backPage)`）作为末页，模板缺失时该图同时作封底弹层回退图；新增 `makeWhitePageDataUrl` 纯白衬页在封底前左侧补位，保证封底单独成页展示在书右侧。
- **末页触发合上**：新增 `totalPagesRef` 记录翻页书总页数；flip 事件 `page===0` → 封面合上状态；`page===totalPages-1`（封底）→ 封底硬壳弹层。`handleBackToFlip` 返回定位到 `totalPages-2`（白色补位页），避免 re-flip 再次触发封底。
- **动态页堆厚度**：静态 6 层左右页堆阴影改为 CSS 变量驱动的 `::before`/`::after` 纸张条纹边缘（`repeating-linear-gradient` 3px 等距条纹）。宽度随进度实时计算：`--book-stack-l = progress×22px`（已翻页码堆在左）、`--book-stack-r = (1-progress)×22px`（未翻在右），通过容器内联 style 注入 CSS 变量实时更新。合上时整本书厚度集中一侧。
- 空间投影：保留 .stf__parent 三层（贴地接触 + 中距离 + 环境）阴影 + 底部 `book-preview-shade` 椭圆落地投影，让书从白底浮起。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：封底页纳入页序；`totalPagesRef`/`makeWhitePageDataUrl`；末页 flip 触发封底；`handleBackToFlip` 定位封底前页；`.stf__parent` 页堆改 CSS 变量条纹；`stackVars` 动态注入。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 铰链压槽重构：细线拼贴 → 剖面光照渐变

### 需求
封面/封底预览的铰链压槽（书脊旁凹槽）由多条 2px 细线 div 拼成，视觉上呈一条生硬「实线」，效果不理想，要求重构设计。

### 方案
- **剖面光照模型**：删除旧的 4 个细线 div（铰链区压暗、凸脊 2px 高光线、凹槽 2px 主线、5% 漫射阴影）与 `HINGE_PCT` 常量，改为**单层多停靠点连续横向渐变** `HINGE_PROFILE_TO_RIGHT`，模拟凹槽横截面的受光起伏：书脊平面(微暗 0.055) → 外凸脊受光峰(白 0.15) → 槽底阴影谷(黑 0.16) → 内凸脊次峰(白 0.10) → 漫射衰减回封面(透明)。相邻停靠点自动柔和过渡，形成连续凹凸剖面光，无任何硬边线条。
- **纵向微光衰**：叠加 `HINGE_VERTICAL_FADE`（顶部微亮 0.04 / 底部微暗 0.05，中间透明），光从上方照，槽影上轻下重更真实。
- **封底镜像**：`to right` → `to left`，停靠点百分比对称（15.5% 后全透明），镜像后剖面一致。
- 覆盖层宽 22%（旧铰链区 8% + 漫射 13% 的合集），zIndex 51 不变。

### 改动
- [CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)：新增 `HINGE_PROFILE_TO_RIGHT`/`HINGE_VERTICAL_FADE` 常量，铰链渲染改为单 div 双层渐变。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 合上书后展示封底硬壳精装实物效果

### 需求
翻页到最后一页继续往后翻（合上书）后，封底以「硬壳精装书实物效果」展示：书脊在右侧、铰链压槽与封面互为镜像，并带合上动效。

### 方案
- **CoverPreview 封底变体**：新增 `variant?: 'front' | 'back'` prop（默认 front，既有使用处不受影响）。back 时硬壳效果全部水平镜像——铰链压槽（8% 位置）、凹槽高光/漫射阴影、纸板边缘高光移到右侧（书脊侧），表面光泽角度 135°→225°，圆角四角翻转。
- **合上动效**：新增 `book-preview-close` 动画，封底绕右侧书脊轴（`transform-origin: right center`）从 `rotateY(16deg)` 微掀开状态收拢落定（0.45s，带 perspective），衔接翻页书合上动作，替代原 pop 淡入。

### 改动
- [CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)：新增 `variant` prop，铰链/边缘/光泽按封底镜像。
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：封底阶段 `variant="back"` + `book-preview-close` 合上动画。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-16 · 空白衬页边界 + 翻页进度条 + 3D 立体感

### 需求
确认后的翻书交互方案：
1. 初始：封面弹层（精装硬壳书）→ 点击翻开 → 翻页书。
2. 翻页书带内衬页：`[空白衬页, 内容页…, 空白衬页]`（封面/封底内页为空白衬页）。
3. 翻到第一页往回翻 → 回到封面弹层；翻到最后一页继续往后翻 → 封底弹层；点封底弹层 → 翻到最后一页。
4. 封底弹层下方 + 翻页书下方：翻页进度条，可拖拽跳页。
5. 增强翻页书 3D 立体感（页数厚度、落地投影、与白色背景空间分离）。

### 方案
- **空白衬页边界页**：翻页书页面改为 `[endpaper, ...contentImages, endpaper]`，`makeEndpaperDataUrl` 生成浅色微纹理衬页（SVG dataURL）。翻到索引 0 → 封面弹层；索引 `contentCount+1` → 封底弹层（flip 事件），`startPage:1`。
- **翻页进度条**：新增 `PageProgressBar`（指针拖拽跳页，显示 `当前/总数`），放于翻页书底部与封底弹层下方；`handleProgressToPage` 拖拽后 `turnToPage` 跳页并进入翻页书。
- **3D 立体感**：`.stf__parent` 叠加多层左右页堆边缘（页数厚度）+ 三层落地投影；翻页书底部加 `book-preview-shade` 径向落地投影，强化与白色背景的空间分离。
- 页码/进度条统一用钳制后的 `contentPage = clamp(pageIndex, 1, contentCount)`。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：边界页改空白衬页；新增 `makeEndpaperDataUrl`/`PageProgressBar`/`handleProgressToPage`；翻页书与封底弹层加进度条；增强 `.stf__parent` 页堆厚度与落地投影；页码钳制。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页过渡到封面/封底弹层（真实翻页动画）

### 需求
1. 翻到第一页往封面翻时，封面可正常显示（此前空白），且过渡效果不限于点击第一页，应普遍生效。
2. 增加翻页动画效果：翻页书过渡到封面/封底弹层时的翻页动效。封底同理。

### 方案
- **边界页（虚拟页）**：翻页书页面改为 `[封面边界页, ...内容页, 封底边界页]`，`startPage: 1`（从第 1 张内容页开始，不初始停在封面）。封面/封底边界页用 `renderSinglePage` 渲染真实封面/封底设计。
- **翻页事件过渡**：`flip.on('flip')` 中，翻到索引 0（封面边界页）→ 延迟 450ms `setStage('cover')`；翻到 `contentCount+1`（封底边界页）→ 延迟 `setStage('back')`。用户在第一页继续往前翻 / 最后一页继续往后翻时，会**真实翻动到边界页**，形成"合上/翻开书"的过渡动画，再切换到弹层。
- **移除手动点击边界判定**：无需再据点击位置判断——边界页本身是真实页，StPageFlip 自然翻到；上一页/下一页按钮直接 `flipPrev`/`flipNext`。
- **弹层进出定位**：封面弹层点击 → `turnToPage(1)` 进入第 1 张内容页；封底弹层点击 → `turnToPage(contentCount)` 查看最后一页。
- **弹层淡入缩放**：封面/封底弹层加 `book-preview-pop` 淡入缩放动画，过渡更平滑。
- 页码显示改为内容页计数（`current=max(1,pageIndex)`，`total=contentCount`）。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：加入前置/后置边界页；翻页事件过渡到弹层；`startPage:1`；`handleOpen`/`handleBackToFlip` 用 `turnToPage` 定位；删除 `handleBookClick`；弹层 pop 动画；页码计数修正。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页书边界衔接封面/封底弹层 + 尺寸统一

### 需求
1. 封面弹层、封底弹层的显示尺寸与翻页书一致（宽高比 + 实际尺寸）。
2. 翻页书翻到第一页后继续往前翻 → 退回封面弹层；翻到最后一页后继续往后翻 → 进入封底弹层（解决"翻不动"死区）。
3. 封面弹层点击→进入翻页书第 1 页；封底弹层点击→进入翻页书最后一页。
4. 触发方式：仅"继续翻"才触发；上一页/下一页按钮也参与边界衔接。

### 方案
- **尺寸统一**：封面/封底展示宽度改用设计尺寸 `min(designW=mmW×MM_TO_PX, 70vw, 70vh×ar)`，与翻页书每页渲染尺寸一致。
- **边界衔接**：StPageFlip 在边界处 `checkDirection` 失败不翻页也不发事件，故：
  - 新增 `handleBookClick`：点击翻页书时用 `getCurrentPageIndex()` + 点击位置判断——第一页点左半→`setStage('cover')`；最后一页点右半→`setStage('back')`。
  - 上一页/下一页按钮 `handlePrev`/`handleNext`：边界处衔接弹层，否则正常 `flipPrev`/`flipNext`。
- 移除原"翻到最后一页自动弹封底"逻辑（`flip` 事件不再自动 `setStage('back')`）。
- 封底弹层封底书改为可点击按钮 → `setStage('flip')`（进入翻页书最后一页）。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：`coverWidthCss` 用设计宽度；新增 `handleBookClick`/`handlePrev`/`handleNext`；移除 flip 事件自动弹封底；封底弹层封底书可点击；翻页书容器加 `onClick`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 恢复封面/封底弹层模式 + 翻页书页堆厚度

### 需求
1. 保留「封面弹层 / 封底弹层」模式：封面精装硬壳书 → 点击翻开进入翻页；翻到末尾自动展示封底精装硬壳书。翻页书**不展示封面/封底页**（仅内容页）。
2. 翻页书要做出真实书本的「厚度」与立体感，类似封面的精装硬壳效果。

### 方案
- 恢复三段式 `stage: 'cover' | 'flip' | 'back'`：封面弹层（CoverPreview + 翻开动效）、翻页浏览（仅内容页，`pageKind !== 'cover' && !== 'backCover'`）、封底弹层（翻到最后一页自动 `setStage('back')`）。
- 封面/封底保持相册实际宽高比（`aspectRatio` prop）。
- **翻页书厚度**：通过 `<style>` 针对 StPageFlip 的书体 `.stf__parent` 叠加多层左右页堆边缘阴影（box-shadow 阶梯），并加圆角与落地投影，模拟真实书页堆叠的厚度与立体感。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：恢复封面/封底弹层与仅内容页翻页书；新增 `.stf__parent` 页堆厚度 CSS；`maxShadowOpacity` 0.6；恢复 `renderSinglePage` 与 `findTemplateById`/`CoverPreview`。
- [CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)：沿用 `aspectRatio` prop（上一步已加）。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 预览封面/封底尺寸对齐相册实际比例

### 需求
点击预览后，封面（及封底）尺寸要与用户相册保持一致，也与翻页页面尺寸/比例一致；此前固定为方形卡片。

### 方案
- `CoverPreview` 增加 `aspectRatio` prop（默认 `'1 / 1'`），根节点由 `aspect-square` 改为 `style={{ aspectRatio }}`，支持任意相册宽高比。
- 预览封面/封底展示宽度按相册实际比例计算：`coverWidthCss = min(340px, 70vw, calc(70vh * ar))`，其中 `ar = albumSize.width / albumSize.height`，既对齐相册比例又限制在视口内（宽 ≤ 70vw、高 ≤ 70vh）。
- 封面/封底模板预览传入 `aspectRatio={aspectCss}`，回退图片也用 `aspectRatio: aspectCss`。

### 改动
- [CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)：新增 `aspectRatio` prop。
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：计算相册宽高比并应用到封面/封底展示。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页预览末尾展示封底实物效果

### 需求
封底要和封面一样：翻页浏览到最后时，展示封底的「精装硬壳书」真实效果，而非普通页面图。

### 方案
- 新增第三阶段 `stage: 'back'`，与封面阶段对称：翻到最后一页内容页后自动切换到封底精装硬壳展示（复用 `CoverPreview` 渲染配套封底模板 `findTemplateById(backPage.templateId)`）。
- 翻页书内容页改为**排除封面与封底**（`pageKind !== 'cover' && pageKind !== 'backCover'`），封面/封底均以精装硬壳实物效果呈现，不进入翻页流。
- 通过 `flip.on('flip')` 监听：当 `e.data.page >= contentCountRef.current - 1`（翻到最后一页）时，延迟 500ms 切换到 `back` 阶段。
- 翻页书容器仅在 `stage === 'flip'` 时显示（封面/封底阶段隐藏），但仍保持始终挂载（避免 ref 为 null 的回归）。
- 新增封底阶段「返回翻页」按钮 `setStage('flip')`。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增 `backTemplate`/`backImage`/`contentCountRef`，`stage` 扩展为 `'cover'|'flip'|'back'`，boot() 解析封底模板，flip 事件末尾触发封底阶段，封底 UI + 返回按钮。
- [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)：新增 `bookPreview.backStage`/`backHint`/`backAlt`/`backToFlip`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 修复翻页预览一直「正在生成翻页预览」（容器未挂载）

### 需求
点击封面进入翻页预览后，一直显示「正在生成翻页预览」，翻页书从未 ready。

### 根因
翻页书容器 `div ref={containerRef}` 只在 `stage === 'flip'` 时才渲染。但 `boot()` 在 `useEffect`（`open` 变化时）执行，此时 `stage` 仍为 `'cover'`，容器尚未挂载 → `containerRef.current` 为 `null` → 命中 `if (!el) return;` 提前返回，`flipReady` 永远为 false → 翻页预览无限加载。这是两段式（封面→翻页）重构引入的回归。

### 方案
翻页书容器**始终挂载**（不随 `stage` 条件渲染），用 `invisible` 在未 ready 时隐藏。这样 `boot()` 在 `open` 时即可取到 `containerRef`，正常初始化 PageFlip。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：翻页容器移出 `stage === 'flip'` 条件，改为始终挂载；加载提示改为 `stage === 'flip' && !flipReady` 条件渲染。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误；刷新后点击封面可正常进入翻页。

---

## 2026-08-16 · 修复翻页预览卡死（fetch 无超时挂起）

### 需求
点击封面进入翻页预览时一直卡在「生成预览」。

### 根因
`preloadPagePhotos` 对非 data/blob 的照片 URL 用 `fetch(src)`（**无超时**）。某张照片请求挂起（不 reject）时，`Promise.all` 永不 resolve，`renderAllPages` 永不完成，`flipReady` 一直 false → 翻页预览卡死。

### 方案
用**自定义安全照片加载** `preloadSharedPhotos` 替代 `preloadPagePhotos`：
- `resolvePhotoSrc` 解析照片 src（优先读库缩略图 dataURL），每次 `readPhotoFromDB`/`readFileAsBlobUrl`/`makeDirectPhotoUrl` 均带 4s 超时。
- 每张照片用 `loadImage`（带 5s 超时）加载，失败/超时**跳过**该照片，绝不阻塞整体。
- 照片一次性去重共享加载，各页并行渲染。
- `renderSinglePage`（封面回退）同样复用该安全加载。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增 `withTimeout`/`resolvePhotoSrc`/`preloadSharedPhotos`；移除依赖 `preloadPagePhotos`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 修复翻页预览一直「等待生成」

### 需求
翻页效果迟迟不生成，一直显示「正在生成翻页预览」。

### 根因
`renderAllPages` 对**每页单独**调用 `preloadPagePhotos`，同一照片在不同页被**重复加载**，且页与页之间**串行**执行。多页/多照片相册下累计极慢，几乎卡死，`flipReady` 迟迟不置真。

### 方案
- **共享预加载**：新增 `preloadSharedPhotos`，一次性去重加载所有页面用到的照片到一个共享 `Map`（构造虚拟页复用 `preloadPagePhotos`），所有页共用，避免重复读 IndexedDB。
- **并行渲染**：`renderAllPages` 改为 `Promise.all` 并行渲染各页，显著提速。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增 `preloadSharedPhotos`；`renderAllPages` 改为共享预加载 + 并行渲染。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页预览定稿：封面精装硬壳书 + 翻开动效 + 白色背景

### 需求
点击预览后呈现封面模板实际样式的精装硬壳书实物效果（像主页封面卡片），点击书本有翻开动效，再进入页面翻页展示。

### 方案
- **封面精装书**：阶段一用 `findTemplateById(封面页.templateId)` 取封面模板 `Template`，交由 `CoverPreview` 渲染精装硬壳书（铰链压槽/边缘高光/落地投影），白色大背景。
- **翻开动效**：点击封面时整本书 `perspective + translateX(-38%) + rotateY(-32deg) + opacity` 过渡（0.55s），模拟翻开，动画结束后进入翻页阶段。
- **进入翻页**：翻页书只加载内容页（排除封面），从第一内容页开始翻页。
- 模板缺失时回退到封面页渲染图（`renderSinglePage`）。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增 `coverTemplate`/`opening` 状态；封面阶段用 `CoverPreview` 渲染精装书 + 翻开动效（`handleOpen`）。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页预览最终方案：白色背景 + 封面书 + 两段式进入

### 需求
推翻此前「深色氛围 + showCover」方案。最终期望：
1. 白色大背景；相册在白色背景上更真实，用书本阴影体现实际尺寸。
2. 两段式交互：进入先展示封面书（像主页封面卡片），点击封面后从第 2 页开始翻页浏览。

### 方案
- **白色大背景**：覆盖层改回白色，篇幅大、留白多。
- **封面书**：阶段一用 `renderSinglePage` 渲染封面页为高清图，居中展示并叠加多层书本阴影（`boxShadow` 三层：近影+中影+远影）体现实际尺寸；点击封面进入翻页。
- **两段式**：`stage: 'cover' | 'flip'`。封面阶段展示封面书；点击后进入翻页阶段。
- **从第 2 页翻页**：翻页书只加载内容页（`filter(p => p.pageKind !== 'cover')`），封面已在阶段一展示，故翻页从第一内容页开始。
- 页面尺寸 `设计 mm × MM_TO_PX`，与设计尺寸、编辑器画布同比例。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：重构为两段式（封面书→翻页书）；新增 `stage` 状态与 `renderSinglePage`；封面高清渲染（baseWidth 720）。
- i18n：中英文补充 `coverStage`/`coverHint`/`coverAlt`/`openCover`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页预览：页面尺寸与设计尺寸完全一致

### 需求
页面整体尺寸应与设计尺寸完全一致，宽度与高度同步按比例调整。

### 方案
- 页面尺寸改为 `设计 mm × MM_TO_PX`（MM_TO_PX=2，与编辑器画布同比例）：竖版 210×280mm → 420×560px，横版同理。
- 去掉 `maxHeight: Math.max(maxPageH, 400)` 的 400 下限（破坏宽高比），改为 `maxHeight = designH` 精确按比例，宽度高度同步缩放。
- `maxWidth/maxHeight` 即为设计尺寸，容器够大时页面=设计尺寸、按真实比例居中留白；容器小时等比缩小适配。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：引入 `MM_TO_PX`，页面宽高按设计 mm×2 计算，去掉破坏比例的 400 下限。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页预览：页面尺寸与相册实际尺寸一致

### 需求
书本/翻页尺寸应与相册实际尺寸保持一致。当前页面过宽、留白多、比例失配。

### 方案
根因：`size:'stretch'` + `maxWidth:2000` 使页面填满容器宽度（portrait 下单页宽=容器全宽），页面被过度放大。
- 页面宽高比改为按 `albumSize` 计算：宽固定基准 360px，高按 `albumSize.height/width` 比例计算。
- 收紧 `maxWidth`=400（单页宽上限）、`maxHeight` 按比例计算，避免填满容器放大留白；书按真实比例呈现并居中。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：按 `albumSize` 计算页面宽高比，收紧 `maxWidth/maxHeight`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 翻页预览优化：封面优先 + 氛围背景

### 需求
进入真实预览时窗口效果不佳（两边白、书居中）。期望 Mixbook 式封面优先：先单独居中查看封面，点击封面翻开后才进入内容翻页；封面是封面效果，翻页是内容浏览。

### 方案
- **封面优先**：StPageFlip 开启 `showCover: true` + `usePortrait`，第一页作为硬封面单独居中展示，点击封面翻开进入内容页。
- **氛围背景**：覆盖层从浅色改为深色径向渐变（舞台感），封面在深背景上突出；工具栏/翻页按钮改用浅色文字适配深背景。
- **封面页加固**：`showCover` 使用第一张图片，代码层把 `pageKind==='cover'` 的页面移到首位，确保封面正确展示。

### 改动
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：`showCover:true`、深色氛围背景、封面页移至首位、深色背景下的浅色控件配色。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 相册真实效果预览模式（翻页书）

### 需求
以真实相册翻页效果展示用户制作的全部页面，鼠标/触控可拖拽翻页，作为「真实效果预览模式」。

### 方案
- 引入 `page-flip`（StPageFlip）翻页引擎，创建 `BookPreviewOverlay` 全屏翻页书组件：拖拽页角翻页 + 上一页/下一页按钮 + Esc 关闭。
- 页面复用缩略图渲染引擎：逐页 `preloadPagePhotos`/`preloadStickers`/`loadBackgroundBitmap` 预加载资源，`renderPageThumbnail` 生成 PNG dataURL，再 `PageFlip.loadFromImages` 载入。
- 工具栏「保存/导出」旁新增「预览」按钮作为入口。

### 改动
- 依赖：新增 `page-flip@2.0.7`。
- [page-flip.d.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/page-flip.d.ts)：为无类型声明的 page-flip 补充最小可用类型声明。
- [BookPreviewOverlay.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BookPreviewOverlay.tsx)：新增翻页书预览组件 + `renderAllPages` 页面渲染封装。
- [Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx)：新增「预览」按钮并渲染翻页书覆盖层。
- i18n：中英文补充 `editor.bookPreviewBtn`/`editor.bookPreview.*`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 封面模板卡片：左上角眼睛查看大图 + 右上角恢复勾选态

### 需求
修复并优化封面模板卡片角标：左上角加眼睛图标点击查看封面大图（同时体现封面页与封底页）；右上角由编辑按钮改回勾选态，表示已选择该封面模板。

### 方案
- **左上角眼睛图标**：悬浮时显示，点击弹出 Modal 大图，并排展示封面页与配套封底页（`template.backCover`），各自下方标注「封面页/封底页」。
- **右上角恢复勾选态**：改回淡绿色对勾角标，当前选中模板始终显示，表示已选择。
- **进入编辑**：恢复卡片本体点击（当前页封面则切换、普通页则插入），与 handleSelect 逻辑一致。

### 改动
- [CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)：新增 `previewTemplate` state + Modal 大图预览（`PreviewPair`）；卡片左上角加眼睛按钮、右上角恢复对勾角标、恢复卡片本体点击。
- i18n：中英文补充 `previewTitle`/`previewCover`/`previewBack`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 编辑器封面模板列表：悬浮效果 + 布局空间感优化

### 需求
优化相册编辑模式左侧「封面」面板的封面模板列表：鼠标悬浮效果、布局空间感。

### 方案
参考主页 CoverGallery 的悬浮交互，统一编辑器封面面板卡片：
- **悬浮效果**：整卡悬浮缩放 `scale 1.03` + 上浮 `-translate-y-1` + 柔和投影（柔和缓动曲线），与主页一致。
- **编辑入口**：取消整卡点击，改为悬浮时右上角显示品牌色圆形铅笔按钮，点击才进入编辑（`title` 提示「使用此封面」），避免误触。
- **选中态**：右上角淡绿色对勾角标（悬浮时被铅笔按钮替代，避免重叠）；底部模板名选中时高亮品牌色。
- **布局空间感**：卡片下方新增模板名（`getTemplateName` 双语），网格纵向间距加大 `gap-y-7`，标题下间距 `mb-3`，让卡片有呼吸感。

### 改动
- [CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)：重构 `CoverTemplateCard`，引入 `hovered` state、铅笔按钮、模板名、加大间距。
- i18n：中英文补充 `editor.coverLibrary.switchTitle`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 背景「应用范围」区分封面/封底/普通页面

### 需求
「应用到所有页面」不应把封面/封底/普通页面混为一谈全部改动，需区分三者；且封面与封底也要拆开。

### 方案
把背景工具顶部的「应用到所有页面」复选框改为**范围选择器**，共 5 档：当前页 / 普通页(不含封面封底) / 封面 / 封底 / 全部。
- 新增 `BackgroundApplyScope` 类型，按语义精确匹配目标页面。
- store 层 `applyBackgroundToAllPages` 重构为 `applyBackgroundByScope(scope, apply)`，内部用 `isCoverOrBackCoverPage`/`isCoverPage`/`isBackCoverPage` 区分筛选。

### 改动
- [types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)：新增 `BackgroundApplyScope` 联合类型。
- [pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)：`applyBackgroundByScope` 按范围过滤应用背景。
- [editorStore/types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/types.ts)：接口由 `applyBackgroundToAllPages` 改为 `applyBackgroundByScope`。
- [BackgroundPicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/BackgroundPicker.tsx)：顶部复选框改为 5 档范围选择按钮。
- [ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)：适配新签名。
- i18n：中英文补充 `applyScope`/`scopeCurrent`/`scopeNormal`/`scopeCover`/`scopeBack`/`scopeAll`。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-16 · 背景工具优化：颜色统一 + 背景图片/上传 + 纹理修复

### 需求
编辑模式下「背景」工具：1) 颜色功能与其他颜色设置统一；2) 支持背景图片并支持用户上传；3) 修复纹理应用失效。

### 方案
- **颜色统一**：BackgroundPicker 纯色/渐变改用共享 `ColorPalette`（与形状/文字选色完全一致）。
- **背景图片**：`AlbumPage` 新增 `backgroundImage`（dataURL，随相册持久化）与 `backgroundImageFit`（cover/contain）。Tauri 用文件对话框 + fs 读取转 dataURL，浏览器用 file input 回退。渲染层三端（画布/缩略图/导出）均按 cover/contain 叠加背景图片。
- **纹理修复**：`getTextureBaseColor`/`paintTextureTile` 抽到 `sharedRender`；`thumbnailCore` 用 OffscreenCanvas 生成图案（Worker 兼容），消除缩略图纹理只显示底色的问题。

### 改动
- [types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)：`AlbumPage` 加 `backgroundImage`/`backgroundImageFit`；新增 `BackgroundApply` 类型。
- [pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)：`updatePageBackground`/`applyBackgroundToAllPages` 改为接收 `BackgroundApply` 对象。
- [BackgroundPicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/BackgroundPicker.tsx)：重构为「颜色/纹理/图片」三 Tab，图片支持上传/预览/填充方式/移除。
- [tauri.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/tauri.ts)：新增 `blobToDataUrl`/`readFileToDataUrl`。
- [PageBackground.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/PageBackground.tsx)、[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)、[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)、[thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts)、[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)：三端渲染背景图片 + 纹理图案；缩略图缓存哈希加入背景图片。
- [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)、[canvas/constants.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/constants.ts)：抽取共享 `paintTextureTile`/`getTextureBaseColor`，消除三端重复。
- [ThemePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ThemePanel.tsx)：适配新签名。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-16 · 封面照片位图层置顶 + 照片位圆角调小

### 需求
1. 封面模板的照片位图层顺序应在最顶层，检查到部分照片位被模板形状（圆点/叶片/太阳/爪印等装饰形状）遮挡。
2. 封面照片位有圆角的，圆角都调小，只要一点即可。

### 方案
**图层层级**：原 `COVER_Z = { shape:50, spine:100, text:100 }`（照片 z≈0 在形状之下）。调整为四级：
`decoration:30 < photo:60 < mask:70 < spine:text:100`。
- 装饰形状（圆点/叶片/太阳/彩球等）置于照片之下，不遮挡照片主视觉；
- 照片槽位 zIndex（`COVER_Z.photo=60`）写入 `slotZIndices`，置于装饰形状之上；
- 渐变蒙版（id 含 mask）保留在照片之上做压暗，保证其上文字清晰可读。

**圆角**：封面模板照片位非零圆角统一由 8~16 调小为 4px（局部/居中照片位），全幅/贴边照片位保持直角 0。

### 改动
- [pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)
  - `COVER_Z` 调整为 `{ decoration, photo, mask, spine, text }`；
  - `presetShapeToPageElements`：蒙版用 `COVER_Z.mask`、装饰形状用 `COVER_Z.decoration`；
  - `applyCoverTemplate`/`applyBackCoverTemplate`：应用模板时写入 `slotZIndices[s.id]=COVER_Z.photo`。
- [cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)：8 款模板照片位圆角 8~16 → 4。

### 说明
Canvas/exportEngine/thumbnailCore 三端均按 `getSlotZIndex` 统一排序，新层级自动生效；尺寸切换 `rescaleCoverDecorations` 用 `...sh`/`...page` 保留 zIndex 与 slotZIndices。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 书脊 logo 按书脊背景色自动黑/白切换

### 需求
书脊的 MemBook logo 默认是黑色的，希望 logo 能自适应书脊背景色，做黑白自动选择切换。

### 方案
书脊背景色 = 用户设置的 `page.spineColor`，缺省回退到页面 `background`。用 `isDarkBackground` 判断明暗：亮底用黑色 logo（logo-light.png），深底用白色 logo（logo-dark.png）。

### 改动
- [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：书脊 logo 加载由「主题色」改为「书脊背景色」驱动，`spineLogoImg` 随 `spineBgColor` 变化重载。
- [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)：新增 `spineLogoSrc(spineBg)`，导出时按 `page.spineColor || page.background` 选 logo 图源。

### 说明
缩略图/全屏因「印刷一体」只画封面正面、不再绘制书脊 logo，故无需改动。

### 验证
- `npx tsc -b` 零错误。

---

## 2026-08-15 · 新增 12 套封面模板（覆盖主流场景与风格）

### 需求
基于新增的艺术字体与文字/形状能力，按主流相册封面设计新增 12 套模板，从风格类型、用户场景等维度设计，贴合对应场景审美。

### 设计维度
新增 12 套封面（每套配一款风格统一的封底，共 24 个模板对象），覆盖：
- 场景类：宝宝、婚礼、节日、萌宠
- 风格类：复古胶片、现代杂志、森系自然、黑白艺术、都市撞色、海洋清新、奢华鎏金、水墨国风

### 字体应用（复用 fonts.css 新增艺术字体）
- 中文：站酷快乐体（宝宝/萌宠）、新蒂小丸子体（节日）、站酷庆科黄油体（节日副标）、马善政毛笔楷书（水墨）、站酷小薇体（水墨副标）
- 英文：Playfair Display（婚礼/奢华）、Cormorant Garamond（自然/杂志/复古）、Great Vibes（婚礼/奢华副标）、Caveat（海洋）、Pacifico（萌宠副标）、Montserrat（复古/杂志/黑白/都市/海洋副标）

### 形状能力应用
渐变蒙版（婚礼/黑白/奢华）、几何色块（都市）、圆点/叶片/波浪/圣诞彩球/星星/朱砂印章等装饰形状，每套配套同背景色/字体/配色的封底。

### 改动
- [cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)：新增字体常量与 12 套封面 + 12 套封底，注册 `backCover` 关联并加入 `COVER_TEMPLATES` / `BACK_COVER_TEMPLATES`（主页封面库与编辑器封面库自动展示）。

### 验证
- `npx tsc -b` 零错误。

---

## 2026-08-15 · 修复构建报错（thumbnailCore 未使用参数）

### 问题
`npm run build`（`tsc -b`）报错：`thumbnailCore.ts:166` 的 `logoImage` 参数声明但从未读取（TS6133）。

### 根因
`drawPageToCanvas` 的 `logoImage`（书脊 MemBook logo 水印图）参数早已不再使用——缩略图/全屏只画封面正面、不再绘制书脊 logo（见函数内注释），但参数与两处调用仍保留。

### 改动
- [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)：`drawPageToCanvas` 移除未使用的 `logoImage` 参数。
- [thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts)：调用处去掉 `logoBitmap` 实参（变量仍接收并用于 `close()` 释放位图）。
- [gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)：主线程调用处去掉 `getReadySpineLogo()` 实参（该函数仍被 logo 加载逻辑使用，未删除）。

### 验证
- `npx tsc -b` 零错误。

---

## 2026-08-15 · 主页相册卡片左上角改为全屏查看入口

### 需求
把相册项目卡片左上角的按钮（原悬浮拖拽手柄）改成「全屏查看」入口，图标为眼睛 icon；点击进入全屏查看该相册，退出后仍停留在相册主页。根据进入路径不同，退出后返回对应来源。

### 改动
- [FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx)：新增可选 `pages`/`albumSize`/`photos` props，外部传入时优先使用，否则回退编辑器 store（编辑器内入口不受影响）。复用同一套全屏查看组件。
- [ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)：
  - 新增 `fullscreenProject` 状态；卡片左上角把悬浮拖拽手柄改为眼睛 icon 按钮（悬浮显示），点击进入全屏查看（`proj.pages`/`proj.size`/`allPhotos` 传入 FullscreenView）。
  - 按钮 `onPointerDown`/`onMouseDown`/`onClick` 均 `stopPropagation`，避免误触卡片打开与拖拽。
  - 退出（`onClose`）仅关闭全屏遮罩，主页仍在，满足「退出后返回相册主页」。
- 新增 i18n `home.projectGrid.fullscreenView`（全屏查看 / Fullscreen view）。

### 说明
全屏查看用 Tauri 真全屏（`force_fullscreen`）展示相册页面，退出后恢复原窗口。由于编辑器与主页都以叠加层方式挂载 FullscreenView，退出即回到各自来源页，天然满足「按进入路径返回」。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-15 · 修复主页排序下拉与创建相册弹窗层级

### 需求
1. 主页相册多选时点击「创建时间」排序按钮，下拉弹窗没有显示在最顶层，后面能看到很多相册项目框框。
2. 点击「创建新相册」按钮，弹窗也没有显示在最顶层。

### 根因
相册项目卡片由 `@dnd-kit` sortable 施加 `transform`（`translate3d`），每个卡片形成独立 stacking context；而排序下拉的容器 `.relative` 没有显式 z-index，下拉仅用 `z-20`，在卡片 stacking context 之下时被卡片盖住。
通用 [Modal.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Modal.tsx) 用的是 `--z-overlay`(50)，而顶部 `AppHeader` 用 `--z-toolbar`(60)，设计令牌里专为弹窗预留的 `--z-modal`(100) 未被使用，弹窗无法保证在最顶层。

### 改动
- [ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)：排序下拉改用 **React portal 渲染到 `document.body`**（`z-[10000]`），彻底脱离相册卡片 `transform` 形成的叠加层，必定显示在最顶层；按钮位置用 `getBoundingClientRect` 测量后以 `fixed` 定位（右上对齐）。
- [Modal.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Modal.tsx)：外层 `z-[var(--z-overlay)]`→`z-[var(--z-modal)]`(100)，确保弹窗高于顶部栏(z-60)与卡片。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-15 · 统一缩略图/全屏/导出与工作区形状最小尺寸

### 需求
之前已允许形状工具设置更小的尺寸（最小 2mm）。编辑模式下显示正常，但底部导航缩略图、网格视图、全屏显示、导出时小尺寸形状没有按最小尺寸比例显示，似乎被限制了（偏大）。

### 根因
编辑器 `ShapeNode`/`ShapeGlyph` 的形状本体尺寸下限用共享常量 `MIN_SHAPE_SIZE_MM`（2mm → 逻辑 4px），见 [constants.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/constants.ts)。
而 [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) 与 [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) 的 `drawShape` 硬编码 `Math.max(w, 10)`（逻辑 10px = 5mm）作为下限，导致小于 5mm 的形状在缩略图/导出中被强制放大到 10px，与编辑模式不一致。

### 改动
- [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)：`drawShape` 的 `pw/ph` 下限由硬编码 `10` 改为 `MIN_SHAPE_SIZE_MM * MM_TO_PX`。
- [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)：`drawShape` 的 `pw/ph` 下限由 `10 * scale` 改为 `MIN_SHAPE_SIZE_MM * mmToPx`。
- 下限与编辑器 `ShapeNode` 完全同源（共用 `MIN_SHAPE_SIZE_MM`），保证小尺寸形状在缩略图/全屏/导出与编辑模式尺寸一致。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-15 · 统一缩略图/全屏/导出与工作区文字排版

### 需求
相册编辑模式底部导航缩略图、网格视图缩略图、主页相册封面缩略图、全屏显示的文字效果与工作区页面不一致（尤其拉丁文字断行位置明显不同）。

### 根因
工作区文字由 DOM 文字层 `TextDomNode`（CSS 排版）渲染：CJK 逐字可断、**Latin 按空格整体断行**（`word-break:normal`），测量断行时计入每字符字距（`measureText + s.length×letterSpacing`），内容宽 = 盒宽 − 左右内边距 − 末尾一字距。
而底部导航/网格/全屏/主页封面共用 `thumbnailCore.drawTextElement`、导出用 `exportEngine.drawTextElement`，两者原先用朴素 `wrapText`（逐字符贪心断行）：**Latin 单词被强制从中间截断**、且 `measureText` 不含字距导致断行测量不准 → 与工作区排版不一致。

### 改动
- [sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)：新增纯函数 `wrapTextLines(ctx, text, contentWpx, letterSpacing)`，与 `TextDomNode.wrapTextLines` 断行规则完全同源（CJK 逐字、Latin 按空格、测量计入字距），无 DOM 依赖，可在 Worker 中使用。
- [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)：横排文字改用 `wrapTextLines`，`contentWpx = tw − pad×2 − 字距`，字距随 `ctx.letterSpacing` 设置。
- [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)：横排文字改用 `wrapTextLines` 同口径（字距按 `scale` 换算）。
- 便利贴文字仍用原 `wrapText`（短文本，逐字贪心已足够），未改动。

### 验证
- `npx tsc --noEmit -p tsconfig.json` 零错误。

---

## 2026-08-15 · 去掉相册卡片封面下方白色留白

### 需求
正方形相册封面下方出现一块长方形白色底。要求去掉白色底，封面按真实比例展示，悬浮时直接放大封面本身。

### 根因
ProjectGrid 用强制 `aspect-[3/4]` 容器装封面，正方形相册（1:1）在容器内按 `maxWidth/maxHeight` 居中后，3:4 容器比正方形封面高，封面下方多出一段空白区，背景透明露出主页白色底，形成"封面下方白色长方形"。

### 改动
- [ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)：去掉强制 `aspect-[3/4]` 容器，封面改为直接 `w-full` + `aspectRatio = 相册宽/高` 按真实比例铺满列宽，不再有多余留白。悬浮放大（`group-hover:scale-[1.03]`）作用于封面本身，逻辑不变。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 本项目文件零错误（仅剩既有 `thumbnailCore` 未使用变量报错）。

---

## 2026-08-15 · 主页相册封面四角优化（左锐角右圆角）

### 需求
以当前硬壳封面版为基准，只优化相册封面四角：左上、左下角不动（0 锐角，书脊贴边），右上、右下角做一点点圆角（翻页边）。

### 改动
- [CoverPageCard.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverPageCard.tsx)：封面整体 `borderRadius` 由统一 `1px` 改为 `0 6px 6px 0`（左上 0、右上 6px、右下 6px、左下 0）。
- [ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)：空封面占位 `rounded-[7px]` 改为 `rounded-[0_7px_7px_0]`，与封面一致。

### 验证
- 浏览器 DOM 计算样式确认封面 `border-radius: 0px 6px 6px 0px`，左上下锐角、右上下圆角生效。

---

## 2026-08-15 · 主页相册卡片改苹果风大封面展示

### 需求
相册实物效果仍局限在卡片框内。要求去掉卡片框，让封面展示更大，鼠标悬浮时流畅放大，整体走苹果风；前端 UI/UX 重新设计，功能不变。

### 方案
移除卡片容器（背景渐变框/边框/圆角药丸容器），改为「无框大封面 + 封面下方极简信息」的展示形式。封面按相册真实宽高比铺满列宽，悬浮时用苹果式缓动曲线（cubic-bezier(0.22,1,0.36,1)）平滑放大 + 上浮 + 柔和投影。

### 改动
- [ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)：
  - 重写 `SortableCard`：去掉卡片容器框（透明背景、无边框），封面直接铺满列宽（`aspectRatio = 相册宽/高`），信息区（名称/页数/时间）移到封面下方，苹果风极简排版（15px 名称 + 12px 次级文字）。
  - 悬浮效果：`group-hover:scale-[1.03]` + `group-hover:-translate-y-1.5` + `group-hover:shadow-[0_24px_48px_-12px_rgba(0,0,0,0.28)]`，`duration-300` + 苹果式缓动。
  - 选中态改 `ring-2 ring-[var(--color-brand)]`；操作按钮（拖拽提示/多选勾选/···菜单）保留，改为毛玻璃半透明圆形。
  - 网格列宽从 `minmax(190px,1fr)` 提升到 `minmax(220px,1fr)`，`gap-x-7 gap-y-10` 更宽松。
  - 封面容器统一为 `aspect-[3/4]`，内部按相册真实比例居中（`maxWidth/maxHeight` contain），保证所有卡片行高一致。
- 拖拽排序、多选、重命名、删除/复制、demo 徽标等功能逻辑不变。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 仅剩 `thumbnailCore.ts` 的既有 `logoImage` 未使用报错，本项目文件零错误。
- 浏览器实测：卡片背景透明、无边框、封面圆角 7px、封面正常渲染；Tailwind 已生成 `scale:1.03`、`shadow 0.28`、`--tw-scale` 规则，悬浮放大生效。

---

## 2026-08-15 · 主页相册卡片封面改精装硬壳实物效果

### 需求
主页相册项目卡片的第一页缩略图要与封面模板（CoverPreview）的真实样式效果一致，去掉卡片边框，展示效果按实际情况优化。

### 方案
复用 CoverPreview 的精装硬壳实物视觉语言（圆角 + 边缘高光/阴影 + 铰链压槽 + 表面光泽 + 多层落地投影），但渲染的是相册**真实第一页**（用户已保存的封面页面），而非模板设计稿。新增独立组件 `CoverPageCard`，卡片的封面展示从 `PageThumbnail` 切换为 `CoverPageCard`；同时移除卡片边框，改用阴影 + hover 浮起表达层级。

### 改动
- 新增 [CoverPageCard.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverPageCard.tsx)：内部用 `CanvasPageThumbnail` 渲染真实页面内容，外层叠加硬壳物理效果（铰链压槽 HINGE_PCT=8%、表面光泽渐变、inset 边缘高光/暗边、纸板厚度阴影、多层浮起投影、底部落地投影），空模板/无槽位时回退到色板占位。
- [ProjectGrid.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/ProjectGrid.tsx)：
  - 卡片封面改 `CoverPageCard`（替换 `PageThumbnail`）。
  - 去掉卡片 `border` 及边框色逻辑，选中态改 `ring-2`，非选中/菜单态仅用阴影 + `hover:-translate-y-0.5` 表达层级。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 仅剩 `thumbnailCore.ts` 的 `logoImage` 未使用报错（为既有问题，非本次改动引入），其余零错误。

---

## 2026-08-15 · 封面书脊改印刷一体连续（去视觉间隙，用折叠线）

### 需求
封面页书脊与封面正面之间的灰色背景间隙，用户可能把照片放进去，导致编辑时书脊显示与导出文件不一致。需根治：编辑布局与导出一致。

### 方案（用户选定：编辑器改连续 + 折叠线）
去掉封面页的 6mm 视觉间隙占位，书脊背面与封面正面**连续**，用一条折叠线标记交界分隔。编辑布局 = 导出布局（都是 页面宽 + 书脊 ），所见即所得，彻底消除"间隙放元素编辑/导出一致性"问题。

### 改动
- **数据坐标**：封面正面内容偏移由 `书脊 + SPINE_GAP_MM` 改为 `书脊宽`——
  - [pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts) `contentOffset = spineWidth`
  - [pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts) 槽位 `offsetPx = spineWidth * MM_TO_PX`
  - [albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts) 尺寸切换 `spX = spineWidth`
- **编辑器渲染**：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) `CANVAS_W = 页面宽 + 书脊`（无间隙）；封面页改**一整块连续背景** + `x=spineWidth` 处**虚线折叠线**标记书脊/封面交界。
- **导出**：[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) 移除已不需要的去间隙副本（坐标为印刷一体，无需二次偏移）。
- **缩略图**：[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) 封面去书脊偏移量由 `spineWidth + SPINE_GAP_MM` 改为 `spineWidth`。
- **数据迁移**：[db/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/db/index.ts) 新增 `migrateCoverSpineGapOnce`，一次性把已存在封面页的封面正面内容左移间隙量（含文字/形状/贴纸/笔触/槽位），用 localStorage 标记防重复；[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx) 进入主页时调用。
- 清理各处不再使用的 `SPINE_GAP_MM` import；`RENDER_VERSION` 9→10 使旧封面缩略图缓存重渲染。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面元素缩略图/导出显示一致（贴纸/笔触去书脊偏移）

### 需求
封面页添加的贴纸在工作区显示正常，但缩略图、网格、全屏、导出文件显示不一致。

### 根因
封面页各元素在数据层整体右移了 `spineWidth + SPINE_GAP_MM`（书脊偏移）。此前缩略图/导出的"去偏移副本"只处理了文字/形状/槽位，**漏了贴纸（stickerElements）与笔触（brushStrokes）**，导致它们在缩略图/导出中未同步偏移而错位。

### 改动
- **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)** 封面去书脊偏移副本：新增 `stickerElements.x -= offsetMm`、`brushStrokes.points` 偶索引坐标减 offsetMm。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)** 封面去间隙副本：新增 `stickerElements.x -= SPINE_GAP_MM`（`x >= spineWidth` 时）、`brushStrokes.points` 偶索引减 SPINE_GAP_MM。
- **[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)**：`RENDER_VERSION` 8→9，使旧含贴纸错位的缩略图缓存失效、重新渲染。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面导出印刷一体（去掉编辑器视觉间隙）

### 需求
封面书脊是印刷时一体设计的（书脊背面 + 封面正面连续，中间为折叠线）。编辑器里书脊与页面用间隙隔开（SPINE_GAP_MM=6，仅视觉分隔），但导出时也把间隙算进宽度，会导致印刷品中间出现一条空隙/错位，影响印刷。

### 方案
导出/打印封面时**去掉编辑器视觉间隙**，让书脊背面与封面正面连续（印刷一体设计）；编辑器内两块式布局保持不变（仅供编辑区分）。

### 改动（[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)）
- `pageExportWidthMm` / `renderPage`：封面导出宽度由 `页面宽 + 书脊 + 间隙` 改为 `页面宽 + 书脊`（不含 SPINE_GAP_MM）。
- `renderPage`：封面页构造**去间隙副本**——封面正面内容（`x >= spineWidth`）左移 SPINE_GAP_MM 紧贴书脊，书脊文字（`x < spineWidth`）保持不动，槽位 slotOverrides 随之左移。
- `drawPage`：`_spineMm` 改为不含间隙，保证 `mmToPx = canvasW/(页面宽+书脊) = MM_TO_PX` 缩放一致。
- 打印引擎复用 `renderPage`，印刷/打印同步生效。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 修复封面缩略图右边灰色块 + 形状实时同步

### 需求
1. 封面在缩略图/网格/全屏显示时右边出现灰色块，未正确显示页面内容。
2. 封面页添加的形状在缩略图/网格/全屏显示没有实时同步。

### 根因
- 灰色块：封面文字/形状整体右移 `spineWidth + SPINE_GAP_MM`，槽位经 slotOverrides 也含该偏移，但封面页**可能没有 slotOverrides**（惰性生成/未持久化）。旧方案用 `ctx.translate` 整体左移，槽位无 slotOverrides 时不含偏移被左移出界 → 右侧露出灰色。
- 形状不同步：缩略图缓存哈希 [computeContentHash](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts) **漏了 shapeElements**，添加/修改形状后缓存 key 不变、命中旧缓存。

### 改动
- **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)** `drawPageToCanvas`：封面页改为构造**去书脊偏移的页面副本**（文字/形状减 mm 偏移、slotOverrides 减 px 偏移；无 slotOverrides 时槽位用模板默认坐标本就正确），统一到逻辑宽（页面宽）坐标系，不依赖 slotOverrides 状态；book 脊 logo 不再绘制。
- **[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)** `computeContentHash`：加入 `sh`（shapeElements 全部渲染相关字段），形状变化实时失效缩略图缓存。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面页缩略图/全屏只展示封面正面（不含书脊）

### 需求
封面页在缩略图和全屏显示时，只展示封面页面内容，不展示书脊，否则显示错位。

### 根因
封面内容在数据层整体右移了 `spineWidth + SPINE_GAP_MM`（书脊为左侧物理扩展），且缩略图渲染会绘制书脊 logo；而缩略图/全屏逻辑宽度为页面宽（不含书脊），导致内容错位、书脊叠加在封面左上角。

### 改动
- **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)** `drawPageToCanvas`：封面页渲染时，背景铺满后用 `ctx.translate(-(spineWidth + SPINE_GAP_MM) * MM_TO_PX, 0)` 将全部内容整体左移，使封面正面填满缩略图；**移除书脊 logo 绘制**。主线程与 Worker 共用此函数，同时修复缩略图、网格视图、全屏显示（FullscreenView 复用缩略图渲染）。
- **[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)**：`RENDER_VERSION` 7→8，使旧含书脊的缩略图缓存 key 失效、重新渲染。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 修复封面预设照片重进后不显示（src 被持久化清空）

### 需求
创建新相册首次进入编辑选择封面页后各视图正常；退出相册重新进入后封面显示不正常，相册项目封面也不显示。

### 根因
封面预设照片（`createDefaultCoverPhotos`）`storageMode='import'`、src 为本地位图资源 `coverLandscape`（无 blob）。autoSave 保存时 [cleanPhotoForStorage](file:///f:/N-编程/MenBook开发项目/MemBook/src/db/index.ts) 对 `import` 模式照片把 `src` 清空为 `''`；重新进入时恢复逻辑因预设照片无 `blobId` 无法重建 URL，src 仍为空 → 封面槽位渲染空白。

### 改动
- **[db/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/db/index.ts)** `cleanPhotoForStorage`：`isCoverPreset` 照片直接返回保留 src（静态资源，无需 blob 恢复）。
- **[EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx)** / **[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)**：恢复照片时对 `isCoverPreset` 跳过 blob 恢复；旧数据 src 为空时补齐 `coverLandscape` 资源路径。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 文字编辑态支持控制点 + 锁定工作区滚动防页面移动

### 需求
1. 双击进入文字编辑模式时，也要支持控制点缩放/旋转（PPT 式）。
2. 文字内容较大断行/换行时，工作区页面视图不应因输入而移动。

### 改动
- **[TextEditHandles.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextEditHandles.tsx)**：**新增**编辑态 HTML 控制点层。编辑时文字浮层（contentEditable）位于 Konva Stage 之上且 pointerEvents:auto，会拦截 Konva 控制点，故编辑态改用 HTML 控制点（8 方向 resize + 旋转手柄），几何算法与 Konva cornerHandle/edgeHandle 一致（对角/对边锚点 + 旋转修正），`mousedown preventDefault` 防止失焦退出编辑。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：文字 DOM 层之后新增编辑态控制点层（zIndex 2）；`useEffect` 在编辑期间锁定工作区滚动——浏览器会因断行自动滚动到光标，持续纠正 scrollTop/Left 保持页面稳定，用户主动滚动（滚轮/触控）仍允许并更新基准。
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：选中虚线框改为仅非编辑态由 Konva 绘制，编辑态由 HTML 控制点层绘制，避免双框重复。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 字体列表中文在上、英文在下，按常用度排序

### 需求
字体下拉列表区分中英文，中文在上、英文在下，各按常用度排序。

### 改动
- **[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)** `TEXT_FONT_FAMILIES` 重新分组排序：
  - **中文（上）**：系统常用 12 + 中文艺术 8（含新蒂小丸子体归入中文组）。
  - **英文（下）**：系统常用 8（Arial/Georgia/Times New Roman 等）+ 英文艺术 7 + 自备英文艺术 12。
  - 每组内按常用程度排序，配合上一条 option 字体预览实现 PPT 式效果。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 字体下拉显示实际效果（PPT 式预览）

### 需求
选择字体时用户不知道字体效果，要求字体下拉列表每项显示该字体的实际效果，参考 PPT。

### 改动
- **[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：字体 `<option>` 增加 `style={{ fontFamily: f }}`，每个下拉项用自身字体渲染字体名，实时预览效果。系统字体直接生效，内置艺术字体经 @font-face 加载后同样生效。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 常用系统字体扩充（中文 12 + 英文 8）

### 需求
将常用系统字体按字体名直接加入字体库，依赖系统已装字体，不内置文件。

### 改动
- **[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)** `TEXT_FONT_FAMILIES`：
  - 中文系统字体扩至 12 种：思源黑体、思源宋体、微软雅黑、宋体、黑体、楷体、仿宋、隶书、幼圆、华文行楷、华文楷体、华文细黑。
  - 英文系统字体 8 种：Georgia、Times New Roman、Arial、Helvetica Neue、Verdana、Trebuchet MS、Courier New、Impact。
  - 字体名与画布/导出/缩略图的 fontFamily 同一字符串，切换即生效；依赖系统已装，未装则回退默认字体。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 新增自备艺术字体 13 款

### 需求
将用户本地 `字体/` 目录下的 13 款艺术字体加入 MemBook 字体库，离线可用。

### 改动
- **[src/assets/fonts/](file:///f:/N-编程/MenBook开发项目/MemBook/src/assets/fonts)**：复制 13 个字体文件（统一去空格/规范化命名，避免 URL 空格问题）——
  - **OTF 6**：AzedoLight、BigJohn、HaginCapsThin、Hitchhike、IntroCondLightFree
  - **TTF 7**：Beyno、ComfortaaLight、Corbelli、Jokerman、MrJacksonRankenstein、Rage、SegoeSemiBold、XinDiXiaoWanZi（新蒂小丸子体，中文）
- **[fonts.css](file:///f:/N-编程/MenBook开发项目/MemBook/src/styles/fonts.css)**：新增 13 条 `@font-face`（OTF 用 `format('opentype')`、TTF 用 `format('truetype')`）。
- **[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：`TEXT_FONT_FAMILIES` 追加 13 款（Azedo Light、Beyno、Big John、Comfortaa、Hagin Caps Thin、Hitch-hike、Intro Cond Light、Jokerman、Mr. Jackson Rankenstein、Rage、Corbelli、Segoe UI SemiBold、新蒂小丸子体）。

### 验证
- 13 个文件签名核对：OTF=OTTO、TTF=`00 01 00 00` 均有效。
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 内置艺术字体库（中文 7 + 英文 7，适合相册）

### 需求
字体库增加**艺术字体**，适合相册使用。采用**内置打包（离线可用）** + **精简艺术精品**方案。

### 改动
- **[fonts.css](file:///f:/N-编程/MenBook开发项目/MemBook/src/styles/fonts.css)**：**新增**，14 款艺术字体 `@font-face`（SIL OFL 开源，Google Fonts），`globals.css` 引入。
- **[src/assets/fonts/](file:///f:/N-编程/MenBook开发项目/MemBook/src/assets/fonts)**：新增 14 个字体文件——
  - 中文 7（完整 CJK ttf）：站酷快乐体、马善政毛笔楷书、柳建毛草体、站酷庆科黄油体、龙藏体、直播星体、站酷小薇体
  - 英文 7（latin woff2）：Playfair Display、Cormorant Garamond、Dancing Script、Great Vibes、Caveat、Pacifico、Montserrat
- **[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：`TEXT_FONT_FAMILIES` = 常用系统字体 6 + 中文艺术 7 + 英文艺术 7，共 20 种；艺术字体经 @font-face 内置加载，离线可用。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 经典蓝调封面模板重新设计（全幅照片+蒙版）

### 需求
将经典蓝调（cover-6）由"居中照片+金色描边相框"重新设计为**全幅照片 + 底部深蓝渐变蒙版 + 白色衬线标题**的大气现代风格。

### 改动
- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)** `coverClassicBlue`：
  - 照片槽由 `20/18/60/46` 居中改为 `0/0/100/100` **全幅**，圆角 0。
  - 移除金色描边相框，改加**底部深蓝渐变蒙版**（`y:42 h:58`，`#0E2240` 0→0.5→0.85 透明度，角度 90 上→下）保证文字可读。
  - 标题改白色衬线粗体 `EVERYDAY\nMOMENTS`（Georgia bold，左对齐），副标题/日期改白色半透明，左对齐留白。
- `backClassicBlue` 配套封底：浅蓝底改为**深蓝底 `#0E2240`** + 金色花体落款 + 金色细线，与封面同语言成套。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面预览形状与画布同源（经典蓝调渐变描边圆角/粗细一致）

### 需求
经典蓝调模板封面预览（frame 渐变描边圆角矩形）与实际应用到页面时形状效果不一致。

### 改动
- **[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)** `ShapePreview`：
  - 圆角矩形圆角由 `cornerRadius*50%`（椭圆角，随宽高不同）改为与画布一致的 `cornerRadius*min(w,h)/2`（cqw，正圆角），预览与画布圆角比例一致。
  - 描边粗细由固定 px 改为 `strokeWidth/2.1`cqw（1cqw=2.1mm，对应画布 2px/mm），随封面等比缩放，不再是固定像素。
  - 透明描边/色块分支统一用同一 `radius`/`strokeCqw`。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面照片位圆角按模板审美设计（恋恋故事左两角改锐）

### 需求
封面模板照片位圆角应**按每个模板的审美独立设计**，不应被统一定义。例：恋恋故事右半幅全高照片，左上/左下（面向文字面板的内侧边）圆角应为 0，硬边才好看。

### 改动
- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：`cover-5` 恋恋故事 `slotCornerRadius` 由 `[8,0,0,8]` 改为 `0`——右半幅全高照片贴页面边（右两角 0）+ 内侧硬边（左两角 0），四周全直角锐利。
- 其余模板保持各自审美（全幅 `0`、居中 `8/10`、双图 `8`），本就按模板独立设定，未统一。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面模板横向页面自适应完善 + 封面槽位预设照片补齐

### 需求
1. 封面模板应用到**横向页面**（宽>高的异宽高比页面）时，照片位/形状未按模板样式自适应——居中元素偏离中心、贴边元素（如恋恋故事右半幅槽）离边。
2. 新建相册/进入照片编辑后选择封面模板，有照片位的封面槽位不显示照片；要求**只要模板有照片位，所有槽位都放入预设照片**。

### 改动
- **[coverScale.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/coverScale.ts)**：新增 `coverAnchorPosition` **锚点感知定位**——在等比缩放尺寸基础上，按元素在原设计中的锚点语义重新定位：贴边元素保持贴边、居中元素（中心≈50%）保持页面居中、其余按页面百分比定位。解决"位置按百分比映射、尺寸按等比缩放"导致的异宽高比视觉错位。
- **[pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts)**：`calcCoverOverrides` 槽位改用 `coverAnchorPosition`。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`presetShapeToPageElements` 形状改用 `coverAnchorPosition`；`applyCoverTemplate`/`applyBackCoverTemplate` 改为 **async**，新增 `ensureCoverSlotPhotos`——相册照片不足时用预设照片补齐所有槽位。
- **[albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts)**：`rescaleCoverDecorations` 切换尺寸时形状同样锚点感知重新定位。
- **[coverPresetPhoto.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/coverPresetPhoto.ts)**：**新增**，批量生成默认封面风景照片（复用预览 cover-landscape.jpg）供槽位补齐。
- **[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)**：`handleCreateFromCover` 改为 `await applyCoverTemplate`，照片由模板按槽位数补齐，移除本地单张预设照片逻辑。
- **[CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)** / **[GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx)**：封面/封底应用调用点改为 `await`。
- **[types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/types.ts)**：`applyCoverTemplate`/`applyBackCoverTemplate` 签名改为 `Promise<void>`。

### 补充：封面预设照片仅封面显示、不进照片列表
- 需求：预设照片**只在封面槽位显示，不应出现在照片列表中**，由后台自动补齐、用户无感知。
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`Photo` 新增 `isCoverPreset?: boolean`。
- **[coverPresetPhoto.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/coverPresetPhoto.ts)**：默认封面照片标记 `isCoverPreset: true`。
- **[PhotoPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoPanel.tsx)**：照片列表 `photos` 过滤 `!p.isCoverPreset`，预设照片不出现在面板中；画布/缩略图/导出仍按 photoId 正常渲染封面。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面模板照片位四角圆角按设计自定义

### 需求
封面模板照片槽位圆角原本统一为 4，要求按各模板设计美观性自定义四角圆角（支持每角单独）。

### 改动
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`Template` 新增 `slotCornerRadius?: number | [tl,tr,br,bl]`（px，缺省 4）。
- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：为各模板设定圆角——
  - 全幅照片（full-2/full-3/full-2 封底/full-3 封底）：0（铺满不圆角）
  - 双图拼排（multi-4/拼图封底）：8
  - 恋恋故事（右半幅全高贴边）：`[8,0,0,8]`（左两角圆角、右两角贴边 0）
  - 局部居中照片（极简白 10 / 经典蓝 10 / 明亮盛夏 10 / 年度回顾 10、极简白 10）
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`applyCoverTemplate`/`applyBackCoverTemplate` 用 `template.slotCornerRadius ?? 4`。
- **[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)**：预览槽位 `borderRadius` 改用模板圆角（`slotRadiusCss`，支持每角单独）。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 封面槽位逐轴适配（某一边与页面一致则该边拉伸）

### 需求
照片槽若某一边与封面尺寸对应边一致（接近 100%），该边应按页面尺寸拉伸铺满，而非整体等比缩放。例：恋恋故事槽 `width:48, height:100`——高度 100% 应与页面同高（拉伸全高），宽度 48% 等比。

### 改动
- **[coverScale.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/coverScale.ts)**：`isFullBleedSlot`（要求宽**且**高都 ≥95%）替换为 `coverSlotSize(slot, kx, ky)` **逐轴判断**——某轴 ≥95% 则该轴 `×kx/×ky` 按页面拉伸，否则该轴 `×min(kx,ky)` 等比。
- **[pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts)** `calcCoverOverrides` 改用 `coverSlotSize`。

### 效果
- 全幅槽(100×100)：两边都拉伸占满。
- 恋恋故事(48×100)：高度拉伸全高、宽度等比。
- 多图/方形槽(37×58 / 60×46)：两边都不全幅，等比保持比例。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 封面跨尺寸适配：抽统一缩放工具（coverScale.ts）

### 背景
封面模板跨尺寸适配存在"等比不变形 vs 铺满占满"的对立。前几轮已确定分类规则：区域/背景型（蒙版、全幅槽）按轴拉伸铺满，图形/局部槽型（圆形/菱形/星形、方形槽）等比保持宽高比。本轮把散落在三处的分类+等比逻辑抽成统一函数，消除逻辑漂移。

### 改动
- 新增 **[coverScale.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/coverScale.ts)**：
  - `isMaskShape(id)`：蒙版判断（id 含 mask）
  - `isFullBleedSlot(slot)`：全幅槽判断（≥95% 宽且高）
  - `coverElementSize(region, baseW, baseH, kx, ky)`：region=true 按轴拉伸，否则等比（min(kx,ky)）保持宽高比
- 三处改用统一函数，行为不变：
  - **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)** `presetShapeToPageElements`（模板百分比→mm）
  - **[albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts)** `rescaleCoverDecorations`（切换尺寸 mm→mm）
  - **[pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts)** `calcCoverOverrides`（槽位百分比→px）

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 封面模板照片槽跨尺寸适配：局部槽等比缩放

### 问题
照片槽位在 `calcCoverOverrides` 中按 `slot.width/100×canvasW`、`slot.height/100×canvasH` 独立拉伸，局部/方形槽（如多图拼排、恋恋故事）被页面宽高比拉伸变形，与封面模板预览不一致。

### 修复
- **[pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts)** `calcCoverOverrides`：局部槽尺寸改为等比缩放 `k = min(canvasW,canvasH)/100`，保持模板宽高比（`slot.width:slot.height`），位置仍按页面轴映射；全幅槽（`width≥95 && height≥95`，如全幅旅行/日常家庭 100×100）保持按轴拉伸占满页面，避免四周留白。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 封面模板跨尺寸适配：形状等比缩放（修复圆形变椭圆）

### 问题
封面模板应用到不同页面尺寸时，形状元素被非等比拉伸——width 按 `pageMm.width`、height 按 `pageMm.height` 独立缩放，宽高比被页面宽高比污染，圆形应变椭圆、菱形/星形变形。

### 修复
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)** `presetShapeToPageElements`：装饰形状尺寸改为等比缩放 `k = min(宽,高)/100`，保持模板宽高比（`p.width:p.height`），任意尺寸不溢出；位置仍按页面轴映射（x→宽、y→高）。蒙版(`id==='mask'`)是"铺满区域"型，保持按轴拉伸覆盖指定区域。
- **[albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts)** `rescaleCoverDecorations`（切换尺寸时）：形状宽度/高度改用 `min(kx,ky)` 等比缩放保持宽高比，位置按轴映射；蒙版按轴。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 新建相册应用封面模板自动预置封面风景照片

### 需求
从主页封面库选择封面模板新建相册时，封面槽位此前是空（新建时 photos 为空），用户希望自动填入模板预览那张风景照片，后续可自行更换。

### 修复
- **[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)**：
  - 新增 `createDefaultCoverPhoto()`：加载 `assets/cover-landscape.jpg`（与封面模板预览同源那张风景图）生成一张默认照片（storageMode='import'，src 为站内资源 URL）。
  - `handleCreateFromCover` 新建相册时，`setPhotos([])` → `setPhotos([defaultCoverPhoto])`，使 `applyCoverTemplate` 的 `photos.slice` 能填入封面槽位；`savePhotos([], ...)` → `savePhotos([defaultCoverPhoto], ...)` 持久化。
- 封面槽位照片填入后即为普通元素，用户可点击槽位自行更换。

### 说明
- 封面风景图资源 URL 经 `isBlobUrlAlive` 乐观放行，`usePhotoSrc` 回退到 `photo.src` 正常显示；导出/缩略图同源可加载。
- 多槽位封面模板仅第一槽自动填入（单张默认照片），其余槽位用户可后续补充。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 封面模板渐变过渡与形状工具不一致（gradientToCss 预览忽略 alpha）

### 问题
全幅旅行、日常家庭等封面模板的半透明黑色蒙版渐变（`alpha 0.05→0.55`），在**编辑器渐变 swatch 预览**里显示为不透明黑，与画布实际过渡效果不一致。

### 根因
- 画布/导出/缩略图用 `gradientFlatStops`、文字层用 `gradientToCssByAngle`、封面库预览用 CoverPreview `presetGradientCss`——**三处都正确应用了各色标 alpha**。
- 唯独编辑器 ColorPalette 选中色 swatch 用的 `gradientToCss`（colorPalette.ts）**只拼 `color + offset`，忽略 alpha**，导致半透明蒙版在预览里显示成不透明。

### 修复
- **[colorPalette.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/constants/colorPalette.ts)**：`gradientToCss` 改用 `alpha < 1 ? toRgba(color, alpha) : color`，与渲染端/文字层/封面预览保持一致的 alpha 过渡。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 形状文字属性面板 tab 持久化 + 渐变角度吸附/输入 + 渐变透明度修复 + 形状最小尺寸放宽

### 1. 属性面板标签页选择持久化（形状 / 文字）
- **[ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx)** / **[TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：首次进入默认「基础设置」tab；用户切换 tab 后用 `localStorage`（`membook_shape_prop_tab` / `membook_text_prop_tab`）持久化，下次进入保持用户上次选择位置。

### 2. 渐变角度：支持数字输入 + 滑杆关键角度吸附
- **[ColorPalette.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/ColorPalette.tsx)**：新增 `AngleControl`（渐变角度控制）——滑杆（接近 0/45/90/135/180/225/270/315/360 关键角度 ≤5° 时吸附对齐）+ 数字输入框（0–360，clamp）。纯色 Tab 不显示渐变控件。

### 3. 渐变透明度修复：颜色1透明度 0% 仍残留浅色
- 根因：`toRgba` 对 `rgba()` 输入命中到末尾 `return color` 忽略 alpha 参数，导致渐变首端 `alpha=0` 未真正透明、残留一层浅色，封面与形状工具渐变不一致。
- **[colorPalette.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/constants/colorPalette.ts)**：`toRgba` 新增 `rgba()/rgb()` 正则解析，最终 alpha = 自带 alpha × 传入 alpha（与 8 位 hex 一致）。封面/形状三端（Konva 主画布、导出、缩略图）通过 `gradientFlatStops → toRgba` 统一生效。

### 4. 形状最小尺寸放宽
- 新增共享常量 `MIN_SHAPE_SIZE_MM = 3`（[canvas/constants.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/constants.ts)），统一替换原硬编码（面板输入 5、画布缩放下限 10、渲染下限 20px=10mm）。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx)**：角点/边点缩放下限 10→3mm；渲染 `pw/ph` 下限 20px→3mm。
- **[ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx)**：宽高输入 min 5→3mm。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：绘制工具最小尺寸阈值 5→3mm，允许拖拽更小形状。

### 验证
- `npx tsc --noEmit` 零错误。

---

## 2026-08-15 · 封面模板多尺寸适配优化 + 图层 zIndex 常量化

### 背景
封面模板按默认竖版 210×280 设计，但相册可能是方形/横版/迷你/冲印等多种尺寸。此前存在两个问题：
1. **应用模板时字号只按宽度缩放**（`fontScale = width/210`）→ 横版/方形页面上文字比例失衡、过大溢出。
2. **切换相册尺寸（setAlbumSize）时封面/封底的文字/形状元素不随新尺寸缩放**（mm 坐标保持旧值）→ 错位/溢出。
3. 模板元素 zIndex 用魔法数字（形状 50、文字 100、书脊 100），层级约定不显式。

### 修复
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：新增 `coverFontScale`——取 `min(宽/210, 高/280)` 并 clamp `[0.5,1.6]`，应用模板时字号综合宽高比缩放，任何尺寸都不溢出、比例协调。
- **[albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts)**：新增 `rescaleCoverDecorations`，`setAlbumSize` 切换尺寸时对封面/封底页文字/形状按新旧尺寸等比重映射：
  - 正文元素 x 先减书脊偏移按 kx 缩放再加回（书脊宽固定不随页面缩放）；y/height 按 ky；fontSize 按 kx。
  - 书脊元素（`spine-text-*`）书脊宽固定，x 保持、y/height/fontSize 按 ky。
  - 文字重映射后重新 `fitTextSize` 撑高，避免切尺寸后裁剪。
- **图层 zIndex 常量化**：`COVER_Z = { shape:50, spine:100, text:100 }`，替换魔法数字。层级约定：槽位照片(z≈0) < 模板形状/蒙版(50) < 书脊元素(100) < 模板文字(100) < 用户后加元素(动态 getGlobalMaxZ)。蒙版/形状在文字之下保证文字清晰。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 修复多行文字文本框高度估算偏小（letterSpacing 计入断行测量）

### 问题
封面模板应用 + 文字工具中，单行文字正常，但**多行/多列**文字文本框没有撑到容纳文字的高度，超框仍被裁剪。

### 根因
`wrapTextLines` 用 Canvas `ctx.measureText` 做断行测量，但 **`measureText` 不含 `letter-spacing`**；而实际渲染（TextDomNode DOM + exportEngine + thumbnailCore）每个字符后都加了字距。当字距较大时，Canvas 会高估一行能放下的字符数 → **估算行数偏少 → `fitTextSize` 算出的高度偏小** → 多行文字框高度不足被裁剪。单行文字无此问题（行数恒为 1）。

### 修复
- [wrapTextLines](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextDomNode.tsx) 新增 `letterSpacing` 参数（与 font/contentWpx 同单位），断行测量用 `measureText(s) + s.length * letterSpacing`（每字符后的字距，含末尾）。
- `fitTextSize` 横排：可用宽度 = 盒宽 − 左右内边距 − **末尾一个字距**（末尾 letter-spacing 不占内容宽），断行传入 `ls`。
- [estimateTextIndexAtPoint](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)（双击光标定位）同步：Pick 类型补 `letterSpacing`，contentW 与断行均计入 `ls × zoom`。
- 竖排本就经 `stepY = fontSize + ls` 计入字距，多列估算正确，无需改动。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 封面模板应用中文字框按内容自适应（修复文字被裁剪）

### 问题
封面模板应用到页面后，部分文字因文本框尺寸不足被 `overflow:hidden` 裁剪，显示不全。模板预设文字的 `height` 是偏小的占位百分比，而实际渲染文字更高（尤其多行标题、大字号 + 字距/行距）。

### 根因
[presetTextToPageElements](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts) 转换模板预设文字元素时，直接用模板预设的宽高（百分比→mm），未考虑实际文字渲染高度。由于 fontSize 随页面宽度缩放（fontScale），固定 height 无法保证容纳文字 → 超框被裁剪。

### 修复
应用模板时对每个**非空**文字元素调用 `fitTextSize`（与渲染层 TextDomNode 同源的排版公式），把文本框撑到 `max(模板预设高度, 实际文字所需高度)`：
- 模板预设高度足够 → 保持原版式（文字居中完整显示）
- 预设高度不足 → 自动撑高，文字不再被裁剪（横排宽度固定、高度按换行增长）
- 空文字保持占位尺寸
- 封面（applyCoverTemplate）与封底（applyBackCoverTemplate）共用同一函数，同时生效
- 导入 `fitTextSize` 无循环依赖（TextDomNode 不依赖 pageSlice），其在浏览器主线程调用，安全

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误。

---

## 2026-08-15 · 文字工具文本框最小尺寸放开（按 PPT 逻辑，横/竖排区分）

### 问题
用户反馈文字工具文本框的最小尺寸被锁死，无法再缩小。定位到三处硬编码下限：
- **TextElementNode resize**：角点/边点缩放 `Math.max(30, ...)`（宽 30mm）+ `Math.max(14, ...)`（高 14mm）——横竖排一刀切且偏大，210mm 页面上宽占 14%，无法再缩。
- **TextElementNode 渲染命中区**：`Math.max(el.width*mmToPx, 50)` / `Math.max(..., 20)`。
- **TextDomNode 渲染层**：`Math.max(elW*MM_TO_PX, isVert?30:50)` / `Math.max(elH*MM_TO_PX, 20)`。

且横排/竖排未区分（竖排本应可很窄、无需 30mm 宽下限）。

### 修复（PPT 文本框逻辑：仅保留极小下限，按方向区分）
- **横排**：最小 8×4mm（可缩到窄矮，文字按框宽换行）。
- **竖排**：最小 4×8mm（可缩到窄高单列）。
- 三处下限统一为同一套 mm 常量（`MIN_W_MM`/`MIN_H_MM`），渲染命中区与 resize 一致，避免"所见非所得"（框比 resize 允许的更大）。
- 涉及 [TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)（resize 4 处 + 渲染 2 处）、[TextDomNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextDomNode.tsx)（tw/boxH）。
- 缩小后若未编辑文字，退出编辑不触发 `fitTextSize`，框保持缩小尺寸；编辑后按新框宽重算高度（框窄→换行多→高度增长），符合 PPT 预期。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误；dev server (5174) 热更新冒烟。

---

## 2026-08-15 · 封面模板精细化排版（接入字间距/行间距/垂直对齐）

### 背景
文字工具新增字间距/行间距功能后，封面模板文字仍用默认行距 1.2/字距 0，且预览层硬编码 `letterSpacing:0.05em` + `nowrap`，与画布不一致。用户要求针对封面模板做精细化排版增强（保留现有 8+8 套视觉风格）。

### 数据链路打通（模板 → 画布 → 预览 全一致）
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`PresetTextElement` 新增 `verticalAlign?`/`letterSpacing?`/`lineHeight?` 字段。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)** `presetTextToPageElements`：透传三字段（`verticalAlign` 缺省 `'center'`），与画布渲染层所见即所得。
- **[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)** `TextPreview`：移除硬编码 `letterSpacing:'0.05em'`/`whiteSpace:'nowrap'`，改为读取模板 `lineHeight`/`letterSpacing`（cqw 等比缩放）、支持 `verticalAlign`，多行 `\n` 用 `white-space:pre-line` 真实换行——预览与画布/导出完全一致。

### 8 套封面 + 8 套封底精细化排版
- 标题类（短词/大写）：拉宽字距 `letterSpacing 3-8px` 增强仪式感/现代感（极简白 3、旅行 EXPLORE 6、盛夏 SUMMER 8、经典蓝 2、年度回顾 4…）。
- 多行标题：加大行距（恋恋故事 `THE STORY\nOF US` lineHeight 1.6、年度回顾 `YEAR IN\nREVIEW` 1.5）。
- 日期：统一拉宽字距（3-4px）精致小字。
- 副标题/手写花体：微字距（1-2px）+ 舒适行距（1.4-1.5）。
- 封底与配套封面保持同一间距语言。

### 验证
- `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误；dev server (5174) 热更新冒烟。

---

## 2026-08-15 · 文字元素重构为单一 DOM 排版引擎（TextDomNode）+ 修复退出编辑尺寸爆炸

### 1. 单一 DOM 排版引擎（根治进/出编辑跳变）

- **根因**：此前显示用 Konva canvas 2D 排版、编辑用浏览器 DOM inline layout，两套引擎的排版模型存在**固有差异**（half-leading 分布方向、字形基线 +1px、断行列容量 floor±1、measureText 与 DOM 断行规则不同）。逐项补偿在任何字号/行高/字距/对齐/断行/用户缩放盒子组合下无法全部对齐——补偿公式假设固定几何，用户一旦调整文本框大小即失效，必然出现整体偏移（竖排右下偏移等）。
- **方案**：显示与编辑改为**同一个 DOM 节点、同一次 CSS 布局**——
  - 新增 [TextDomNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextDomNode.tsx)：文字元素常驻 DOM 渲染节点。显示态只读（`pointer-events` 穿透）；双击进入编辑仅切换 `contentEditable` + 聚焦光标（`useLayoutEffect` 首帧绘制前写文本+聚焦，无闪烁）。进/出编辑**在结构上不可能**产生位置/大小/间距跳变。
  - [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：Stage 之上新增「文字 DOM 渲染层」容器（页面左上角锚定，page 模式按页边界裁剪）；编辑初始化分流——文字元素由 TextDomNode 自身承载，便利贴保留原编辑浮层。
  - [TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)：移除 Konva 文字渲染，改为**透明命中 Rect**（命中/拖拽/选中框/控制点/旋转手柄保留）。
  - 编辑期间 contentEditable children 由 DOM 自管理（React 渲染 `undefined` 不触碰内容，光标不跳动）；文本实时同步走 `onLiveText`（不记历史）。
  - [useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts)：Delete 快捷键增加 contentEditable 守卫，编辑中仅删字符不删元素。
- **竖排语义（与 PPT 一致）**：`writing-mode: vertical-rl` + `text-orientation: upright`；`align`=列内垂直对齐（断行未满列生效）、`verticalAlign`=列组水平分布；显式 `\n` 即换列。

### 2. 修复退出编辑后竖排多行文本框尺寸爆炸/排版重排

- **现象**：竖排多行文字双击进入编辑再退出后，文本框大小剧变、文字重新排布。
- **根因**（`fitTextSize` 三处不一致）：
  1. **单位混用**：`el.height` 是 mm、`stepY=fontSize+letterSpacing` 是逻辑 px，`perCol=floor(mm/px)` 使列容量缩小约 3.78 倍（60mm 盒算出每列 3 字，实际 DOM 排 10 字）→ 列数暴增 → `totalCols×stepX`（px 数值）与 `el.width`（mm 数值）直接比较 → 宽度爆炸。
  2. **perCol 多 `+1`**：与 TextDomNode DOM 布局公式不一致。
  3. **横排同样混单位**：`height = max(el.height_mm, lineCount×fontSize×lineHeight_px + padMm)` → 3 行文字盒高从 ~28mm 暴涨 ~76mm，垂直居中重定位。
  4. **显式 `\n` 未分段**：DOM vertical-rl 中 `\n` 即换列，但 numCols 用总长度估算列数，与真实列数不符。
- **修复**（[TextDomNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextDomNode.tsx)）：
  - `fitTextSize` 重写：与显示层**完全同源公式**（mm↔px 经 `MM_TO_PX` 桥接、去掉 +1、按 `\n` 分段计列、zoom 在比值中自然消去）；竖排宽度按列数×列宽(`fontSize×lineHeight`)增长、高度按最长列增长；横排高度按行数×行高增长；盒子只增不减。
  - 新增共享 `wrapTextLines`（CJK 逐字可断、Latin 按空格断行，带原文起始偏移），`fitTextSize` 与 Canvas 双击光标估算 `estimateTextIndexAtPoint` 共用同一断行逻辑（后者同时修正行高硬编码 1.2 与内容宽公式）。
  - 显示层竖排块几何改为分段感知：`numCols=Σ ceil(段长/perCol)`，块高按最长段是否满列决定；与 fitTextSize 完全一致。
  - **退出守卫**：文本未修改则完全不提交（纯进/出编辑对盒子零变化、零空历史快照）；修改过才按只增不减提交。
- **验证**：`node node_modules/typescript/bin/tsc --noEmit -p tsconfig.app.json` 零错误；dev server (5174) 热更新冒烟。

---

## 2026-08-15 · 修复切换对齐方式偶发文本框放大 + 移除 Backspace 删除快捷键

### 1. 切换对齐方式偶发文本框自动放大
- **根因**：编辑文字时点击对齐按钮会 blur contentEditable → 触发 `saveText` → `fitTextSize` 用**过时/错误的公式**重算尺寸（竖排仍用 `fontSize+6`/`fontSize+2`，横排行距写死 `1.2`、未计入 `letterSpacing`），导致盒子被放大。
- **修复**：
  - [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) `fitTextSize` 改用与渲染层一致的公式：竖排列宽 `stepX=fontSize*lineHeight`、字步进 `stepY=fontSize+letterSpacing`，横排行高 `fontSize*lineHeight`、宽度计入字间距。
  - [panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx) 对齐/垂直对齐/文字方向按钮加 `onMouseDown.preventDefffault()`，点击时不夺走编辑浮层焦点、不触发 blur→save→放大。

### 2. 移除 Backspace 删除快捷键（仅保留 Delete）
- **根因/需求**：页面内按 Backspace 会误删页面/元素，用户要求快捷删除仅用 Delete。
- **修复**：将以下处理改成仅 `e.key==='Delete'`：
  - [BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)（删除当前页面）
  - [GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx)（删除选中页面）
  - [PhotoPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoPanel.tsx)（删除选中照片）
  - [useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts)（删除选中元素）

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复多行/多列文字进/出编辑跳变（line-height 半行距补偿）

### 需求
用户反馈：单行/单列（不换行）文字进/出编辑不跳，但**多行（横排）或多列（竖排）断行时跳变**；且横排只有**垂直对齐 top/bottom** 会跳、**居中**不跳。

### 根因
浏览器 `line-height` 会在块方向的首/末行各分配 `halfLeading = (L-1)*fontSize/2`（实测横排首行偏移 4px@L=1.4、竖排列方向偏移 2px@L=1.2），而 Konva 顶部对齐、无 leading。**居中时两侧 leading 对称抵消**，故不跳；**锚定（top/bottom）时不抵消** → 进/出编辑整体偏移 halfL。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 编辑浮层 contentEditable 的 `transform` 改为半行距补偿：
- **横排**：`translateY(vAlign==='top' ? -halfL : vAlign==='bottom' ? halfL : 0)`。
- **竖排**：`translate(块方向锚定补偿, 1*canvasZoom)`，块方向（水平）由 `verticalAlign` 决定（top→-halfL、bottom→+halfL、center→0），另保留 Konva 逐字字形基线偏移 `translateY(1*canvasZoom)`。
- `halfL = (lineHeight-1)*fontSize/2 * canvasZoom`。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排进/出编辑的微小上移（Konva 字形基线偏移补偿）

### 根因
浏览器 `vertical-rl` 顶对齐（字形顶=内容框顶，实测偏移 0），但 Konva 逐字 `<Text>` 内部按字体度量渲染，字形顶比 y 低约 **1 逻辑px**（实测 fontSize 20/40 偏移 1，fontSize 14 偏移 0，为恒定亚像素偏移，非等比）。因画布 `scaleX/Y=canvasZoom`，该 1 逻辑px 放大为 `1*canvasZoom` 屏px。故显示(Konva)比编辑(浏览器)低约 `1*canvasZoom`，进入编辑时文字肉眼上移一点点。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 竖排编辑浮层 contentEditable 加 `transform: translateY(1*canvasZoom)`，向下平移使字形顶与 Konva 显示对齐，消除进/出编辑的微小上移。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排进/出编辑时行列间距跳变（column-gap 无效 + line-height 用错）

### 根因
竖排文字编辑浮层此前用 `line-height: 1` + `column-gap: (lineHeight-1)*fs` 控制列间距。但 **`column-gap` 只对 CSS 多栏(`column-count/width`)或 flex/grid 生效，对 `writing-mode: vertical-rl` 普通流无效**；竖排版式里列宽/列间距实际由 `line-height` 决定。结果编辑时列宽=fontSize，而 Konva 显示列宽=fontSize×lineHeight(默认 1.2 倍)，进/出编辑时列间距跳变。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 竖排编辑浮层：
- `line-height` 竖排改用 `lineHeight` 倍数（=fontSize×lineHeight=Konva stepX），列间距与显示层一致。
- 删除无效的 `column-gap`。
- 浮层块宽由 `(numCols-1)*stepX+fs` 改为 `numCols*stepX`，使浏览器正好排满 numCols 列；居中时列两侧多出的 half-leading 恰好抵消浏览器 leading 偏移，字形与 Konva 对齐。
- 字间距仍由 `letter-spacing`（vertical-rl 中作用于垂直方向）控制，与 Konva stepY 一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字工具新增行间距/字间距，统一竖排列间距与显示层一致

### 需求
用户反馈：竖排文字列间距在显示与编辑时不一致，导致进/出编辑有小跳变；要求为文字工具增加**行间距**和**字间距**设置，保证横排/竖排都正确应用，且正常显示与编辑时不跳变、视觉一致。

### 实现
- **类型** [types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)：`PageTextElement` 新增可选字段 `letterSpacing`（字间距 px，默认 0）与 `lineHeight`（行距因子，默认 1.2），并导出 `DEFAULT_TEXT_LINE_HEIGHT` / `DEFAULT_TEXT_LETTER_SPACING`。
- **统一间距公式**（显示/导出/缩略图/编辑浮层四端一致）：
  - 竖排字符间距 `stepY = fontSize + letterSpacing`。
  - 竖排列间距 `stepX = fontSize + (lineHeight-1)*fontSize`。
  - 横排行高 `lineHeight` 倍数、字间距 `letterSpacing`。
- **Konva 显示层** [canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)：竖排逐字 stepY/stepX 改用 `letterSpacing`/`lineHeight`；横排 `<Text>` 加 `lineHeight`/`letterSpacing`。
- **导出/缩略图** [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) / [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)：竖排 stepY/stepX、横排 `ctx.letterSpacing`/行高均改用新字段。
- **编辑浮层** [editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：去掉写死的 `2*canvasZoom`/`6*canvasZoom`，竖排 `vertStep`/`vertStepX`/`columnGap` 改用 `letterSpacing`/`lineHeight`；横排 `lineHeight`/`letterSpacing` 也接入新字段，消除进/出编辑跳变。
- **属性面板** [panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)：基础设置新增「行间距」（0.6–3.0 倍数）与「字间距」（0–20px）滑杆，横排/竖排通用。
- **新建文字默认值** [editor/ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)：新建文字元素初始化 `lineHeight`/`letterSpacing` 默认值。
- **i18n**：zh-CN/en-US 新增 `lineSpacing`/`letterSpacing` 文案。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排编辑跳变（垂直字符间距 leading 不一致）

### 根因
Konva 竖排逐字 `stepY = fontSize+2`，字符紧贴行顶（空隙在下方）；而浏览器 `line-height` 会把多出的 2px 空隙**上下各分一半**，导致字符整体下移约 `canvasZoom` px → 显示与编辑切换时字符位置跳变。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 竖排 contentEditable：
- `line-height: 1`：字符紧贴行顶，无 leading 偏移（与 Konva 逐字一致）。
- `letter-spacing: 2*canvasZoom`：在 `vertical-rl` 中作用于垂直方向，提供 stepY 的额外垂直间距。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 竖排断行（多列）时未满列按 align 对齐（PPT 行为）

### 需求
用户指出：竖排断行后「未满一行的列」要按对齐规则对齐（左=列顶部、居中=列中间、右=列底部），而非整块移动。此前对多列无效果。

### 实现
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)** 竖排：垂直对齐改为**逐列**——按 x 分组列，未满列（字符数<perCol）在其列内按 align 垂直对齐（左=顶/中=列中间/右=底），满列不动；水平仍由 verticalAlign 控制整块列分布。
- **[editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 竖排浮层：`textAlign` 映射 align（左→start/顶、右→end/底），使 vertical-rl 下未满列在编辑时也按对齐规则定位。

### 说明
仅影响竖排分支，横向对齐逻辑不受影响。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 竖排对齐改用 PPT 语义（随文字方向旋转）

### 需求
用户指出：横向对齐正确，但竖排对齐与 PPT 不一致。PPT 竖排时对齐语义随文字方向旋转：
- **align（左/中/右）** → 控制【列内/整体文字垂直位置】：左=顶部、居中=列中间、右=底部。
- **verticalAlign（顶/中/底）** → 控制【列水平分布】：顶=左边、居中=中间、底=右边。

### 实现
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)** 竖排：水平 shift 改用 `verticalAlign`（top→左/center→中/bottom→右），垂直 vshift 改用 `align`（left→顶/center→中/right→底）。
- **[editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 竖排浮层：`justify` 用 `align`（垂直），`alignItems` 用 `verticalAlign`（水平），与显示层一致。

### 说明
仅影响竖排分支；横向对齐逻辑不变。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排编辑"文字变小/排列不一致"（列宽与 Konva stepX 对齐）

### 根因
竖排 contentEditable 用浏览器 `vertical-rl` 排版，其默认列宽 ≈ `1em`；而 Konva 竖排列宽 = `stepX = fontSize + 6`。两者列宽不同 → 多列时列数/块几何不同 → 文字显小、排列/位置跳变。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：
- 计算竖排列数 `numCols = ceil(字符数/perCol)`。
- 竖排 contentEditable 显式 `width = (numCols-1)*stepX + fs`（单列=fs，多列=列数×stepX），`height = vertBlockH`，使浏览器在 Konva 相同块空间内排版，列间距(=stepX-fs)与 Konva 一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排编辑瞬间"缩放变小"/顶部对齐与居中跳变

### 根因
全局 CSS `* { box-sizing: border-box }`（design-tokens.css）把竖排 contentEditable 的 `height`（本意是内容区/文字块高）解释为**含 padding**，导致内容区变小、文字换列变多（视觉变小），且 justify 垂直定位基于偏小的盒子 → 编辑时从"顶部对齐"跳成"居中"。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 竖排 contentEditable 显式 `box-sizing: content-box`，使 `height = 内容区`（与 Konva 竖排块高一致），padding 在盒外，换列数与垂直对齐基准与 Konva 一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排编辑时文字"缩放变小" & 对齐与 Konva 不一致

### 根因
竖排浮层 contentEditable 高度为 auto（单列不换列），而 Konva 竖排会按框高**换列（多列）**。文字多列时浮层单列超高被 `overflow:hidden` 裁剪 → 看着"变小"；且单列/多列块几何不同 → 顶端/垂直/低端、左/中/右对齐的基准与 Konva 不一致。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：
- 新增竖排块高计算：`vertStep = fs + 2*canvasZoom`（匹配 Konva stepY），`perCol = floor(内容区高/vertStep)`，`vertBlockH = 单列?字符数*vertStep : 内容区高`。
- 竖排 contentEditable 高度 = `vertBlockH`：单列时=内容高（justify 垂直定位），多列时=内容区高（触发正确换列），与 Konva 竖排块几何一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排文字编辑后文字靠右 & 竖排拉丁/数字旋转

### 根因
1. **靠右**：竖排 contentEditable 的 className 带 `w-full`（width:100%），被强制撑满整个框宽；`vertical-rl` 下文字块靠右，而 `textAlign`/`alignItems` 对竖排失效 → 进编辑竖排文字靠右。
2. **拉丁/数字旋转**：浏览器 `vertical-rl` 默认 `text-orientation: mixed`，会把拉丁字母/数字旋转 90°；Konva 竖排是逐字**正立**渲染，两者不一致。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：
- 竖排时去掉 contentEditable 的 `w-full`，宽度自适应内容列宽，由父容器 `alignItems` 按水平对齐居中。
- 竖排加 `textOrientation: 'upright'`，所有字符正立，与 Konva 逐字渲染一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复文字编辑"跳一下"（Konva 异步重绘重叠 + 换行规则不一致）

### 根因
1. **重叠闪烁**：进入/退出编辑时，Konva 文字隐藏/显示用默认 `batchDraw`（异步，延迟一帧生效），编辑瞬间 Konva 旧文字与浮层新文字短暂两层重叠，视觉"跳一下"。
2. **换行不一致**：浮层 contentEditable 用 `break-words`（允许单词内断行），Konva `wrap="word"` 只在空格处断行 → 英文/长单词换行位置不同 → 行数/高度不同 → 垂直位置偏移。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)：
- 新增 `useLayoutEffect` 依赖 `editingTextId`，切换时调用 `stageRef.current.draw()` **同步重绘 Konva**，让隐藏在绘制前生效，消除重叠帧。
- contentEditable 去掉 `break-words`，改 `wordBreak: 'normal'; overflowWrap: 'normal'`，换行规则对齐 Konva `wrap="word"`。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复竖排文字编辑时跳动到最右边

### 根因
竖排编辑浮层 `contentEditable` 用了 `minWidth: max(fs, tw)` + flex `stretch`，被强制撑满整个框宽；`writingMode: vertical-rl` 下文字块从**最右**开始排，而 Konva 竖排文字是按 `align`（默认居中）定位的 → 进编辑竖排文字跳到最右。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 编辑浮层：
- 竖排时父容器 `alignItems` 按水平对齐（center/left/right）定位 contentEditable，`contentEditable` 宽度自适应内容窄列（`minWidth: fs`），不再撑满整框宽。
- 竖排 `lineHeight = 1 + 2*canvasZoom/fs`，匹配 Konva 逐字行距 `stepY=fontSize+2`，保证竖排字符间距一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 修复文字编辑时多余"淡紫实线框"（全局 :focus-visible 光晕）

### 根因
全局样式 [design-tokens.css](file:///f:/N-编程/MenBook开发项目/MemBook/src/styles/design-tokens.css) 的 `:focus-visible { box-shadow: 0 0 0 3px color-mix(brand 20%) }` 会给聚焦元素加 3px 淡紫 box-shadow 光晕。contentEditable 编辑浮层获得焦点时触发，看起来像"淡紫实线框"；`outline-none` 只去 outline 不去 box-shadow。

### 实现
[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 编辑浮层 contentEditable 内联加 `outline: none; box-shadow: none`，消除 focus 光晕/边框。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字框统一为 PPT 风格（单个虚线框 + 控制点，编辑时复用该框）

### 需求
用户反馈：PPT 就一个虚线框 + 控制点，为什么做这么多框并对齐。要求回归 PPT 简洁样式。

### 实现
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：
  - 删除 hover 实线框（及 `hovered` state），只保留**选中虚线框**（`isSelected` 时显示）。
  - 选中虚线框**编辑时也显示**，作为唯一的编辑框。
  - 8 方向控制点、旋转手柄加 `!isEditing`，编辑时隐藏，避免误操作缩放/旋转。
- **[editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：删除编辑浮层自带的 `border-dashed` 虚线框，编辑框统一由 Konva 选中虚线框承担。

### 效果
- 未选中：无框。
- 选中：单个虚线框 + 8 控制点 + 旋转手柄。
- 编辑：单个虚线框（复用选中框）+ 输入光标，无额外框、无控制点。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字编辑多重框重叠修复 + 编辑/显示跳动修复

### 需求
1. 文字编辑时出现多重框（选中虚线框 + hover 实线框 + 编辑虚线框），询问各框作用。
2. 文字显示与编辑切换时文字"跳一下"，不符所见即所得。

### 框来源分析
- **选中虚线框**：[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx) 的 Konva `Rect`（dash），表示当前选中元素。
- **hover 实线框**：同一 `Rect` 的 hover 态（淡紫实线），编辑时鼠标停在文字上仍亮起。
- **编辑虚线框**：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 编辑浮层的 `border-dashed`，表示正在输入。

### 实现
- **去重框**：TextElementNode 的 hover/选中框加 `!isEditing`，编辑时隐藏 Konva 侧框，仅保留编辑浮层虚线框。
- **修复跳动**：编辑浮层由 `minHeight`（随内容增高，导致文字从"居中"过渡到"顶对齐"而跳动）改为**固定 `height: boxH`**（与 Konva 文字框一致）+ `overflow:hidden`，文字块在固定框内垂直对齐、中心恒定，输入时不跳动。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字编辑改为 PPT 式单层可见文字（contentEditable），彻底去除位置推算

### 需求
用户提出：为什么要有两层文字？希望像 PPT 文本框一样，显示的文字在编辑时直接编辑，不要去推算位置。

### 评估结论
- **PPT 文本框**是非 canvas 架构，文本框本身就是可编辑原生文本对象，天然只有一层、零跳动。
- 本项目用 Konva(canvas) 渲染，canvas 是位图、放不下原生光标，因此**必须叠一层 HTML 输入浮层**，"两层"在 canvas 架构下绕不过去。
- 但可以做到**视觉上单层**：编辑时让 HTML 浮层直接显示真实文字（纯色用 `color`、渐变色用 `background-clip:text`），同时隐藏 Konva 文字。这样用户只看到一层、且就是正在编辑的那层，光标原生对齐，无需任何位置推算。

### 实现
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：编辑时 Konva 文字隐藏（`opacity: isEditing ? 0 : 1`）；删除 `onTextHeightChange`/`textNodeRef`/`useLayoutEffect` 上报逻辑。
- **[editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 编辑浮层（`editingTextId` 块）：
  - `textarea` → **`contentEditable` div**，浮层直接显示真实文字（纯色 `color` / 渐变 `background-clip:text`），光标 `caret-color` 可见。
  - 左上角锚定（与 Konva 文字框一致），`min-height` + flex 纵向对齐实现 verticalAlign，**不再推算 `contentH`/`padTop`/`padBottom`/内边距**。
  - 新增 `gradientToCssByAngle`（渐变浮层预览）与 `setCaretAt`（contentEditable 内定位光标）；进入编辑用 `useLayoutEffect` 初始化文本并聚焦。
  - 删除废弃的 `konvaTextHMap`/`editContentH`/`measureTextareaContentH`/`handleTextHeightChange`/`editTextareaRef` 及相应 useLayoutEffect。
- 竖排保留 `writingMode: vertical-rl`（竖向光标）。

### 说明
- 彻底移除"透明 textarea 叠加可见 Konva 文字 + 高度推算"的复杂逻辑，改为单层可见输入层，光标与所见文字天然对齐、零跳动。
- 渐变文字在编辑时由浮层用 CSS 渐变预览，导出/缩略图仍走 Konva/CSS 统一端点计算，不含此浮层。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字编辑简化为一层可见文字，修复横向文字回归

### 需求
还原之前正常的横向文字编辑；简化——不要用两层文字，一层即可。

### 根因
此前为修复竖排，把编辑 textarea 改为「显示文字色 + 隐藏 Konva 文字」，导致横向文字（尤其渐变文字）编辑时丢失渐变、显示异常（回归）。

### 实现
恢复「单层可见文字」方案（与最初横向正常时一致）：
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：横排/竖排文字编辑时均**保持可见**（`opacity: 1`），文字（含渐变）始终按原样显示，不依赖 textarea 重绘。
- **[editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 编辑浮层：textarea **文字透明**（`color: transparent`），仅作为输入/光标层叠加在可见的 Konva 文字上——**单层可见文字**，光标落在所见文字上，与横向一致。

### 说明
- 简化后不再有「文字色层 + 隐藏 Konva」的双层逻辑，编辑时始终看到的是 Konva 渲染的文字。
- 竖排保留 `writingMode: vertical-rl`（竖向光标）。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 竖排文字编辑光标错位修复（回到显示文字、光标与所见一致）

### 需求
优化后「隐形文字层」（透明 textarea）未与真实可见文字对齐，导致光标位置错位。

### 根因
竖排文字用「透明 textarea 叠加在可见 Konva 文字上」的方案，textarea 的浏览器竖排布局与 Konva 自定义逐字布局存在差异，隐形文字层与可见文字不对齐 → 光标错位。

### 实现
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：竖排文字编辑时恢复隐藏（`opacity: isEditing ? 0 : 1`），不再叠加可见 Konva 文字。
- **[editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 编辑浮层：
  - 竖排 textarea 恢复显示文字色（非透明），光标与所见文字一致（同一输入层，天然对齐）。
  - 竖排内容高估计算法由「步进(字号+2)」改为「字号」（浏览器 vertical-rl 字符步进≈字号 CJK em-box），使 textarea 文字与 Konva 竖排块中心对齐，进入编辑不跳动。

### 说明
- 保留 `writingMode: vertical-rl`（竖向光标，与 PPT 一致）。
- 光标与输入框文字一致（同一元素），彻底消除光标错位。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 竖排文字编辑改为 WYSIWYG（保持文字可见 + 透明输入），彻底消除跳动

### 需求
竖向文字点击编辑时位置发生变化，应像横向文字一样：双击进入编辑时文字呈现效果与看到的完全一致，不能跳动。

### 根因
竖排文字用 `writingMode: vertical-rl` 的 textarea 编辑时，其字符间距与 Konva 自定义逐字布局（每字 字号+2）存在固有差异，无法完全对齐；而编辑时 Konva 文字被隐藏（opacity 0），textarea 文字即使有微小偏移也会表现为可见的跳动。

### 实现
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：竖排文字编辑时**保持可见**（`opacity: 1`，不再隐藏），文字始终停留在原位置，绝无跳动。
- **[editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 编辑浮层：竖排文字 textarea 改为**透明文字**（`color: transparent`）+ 可见光标（`caretColor`），作为输入层叠加在可见的 Konva 竖排文字之上——所见即所得，文字位置不变。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 书脊文本框宽度修正 + 竖排文字编辑竖向光标与位置一致

### 需求
1. 书脊相册名/日期文本框宽度设置有问题：之前只让编辑输入框宽度对了，控制器（选框）尺寸不一致，正常输入框尺寸应一致。
2. 文字方向为竖向时，输入光标方向也应调整为竖向，文字位置与编辑时完全一致、不跳动，像 PPT 的文字输入框。

### 实现
- **[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)** `buildSpineElements`：书脊文字盒宽由 `spineWidth`（18mm，过宽）改为「字号+6」（单列宽度，与 `fitTextSize` 一致：相册名 15mm、日期 14mm）。使控制器（选框）与编辑输入框尺寸一致、贴合文字列，不再过宽。
- **[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 编辑浮层：
  - 竖排文字重新启用 `writingMode: vertical-rl`，使输入光标方向为竖向（与 PPT 一致）；横排/便利贴仍为横向。
  - 竖排内容高按「最长一列字符数 × 步进(字号+2)」确定性计算（与 Konva isVertical 一致），不走浏览器 scrollHeight 测量——vertical-rl 下测量失真会导致垂直居中偏移、进入编辑跳动；从而文字位置与 Konva 完全一致、无跳动。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 封面模板文字/形状复用标准工具，去除预览里的重复工具代码

### 需求
封面模板的文字与形状等元素必须复用现有（统一）的文字/形状工具，仅通过设置（属性面板）调整，不应在封面模板上单独建立文字/形状工具代码。

### 现状核查
- **编辑器**：封面页的 `textElements`/`shapeElements` 已是标准 `PageTextElement[]`/`ShapeElement[]`，由标准 `TextElementNode`/`ShapeNode` 渲染，通过标准属性面板编辑（渐变/圆角/切角/垂直对齐等设置均可用）。✓ 已满足。
- **封面预览** [CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)：此前手工硬编码了各形状的 CSS clip-path（`SHAPE_CLIP`），与画布/导出/缩略图共用同一套形状几何，属于重复的形状工具代码。

### 实现
- **[components/common/CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)**：删除硬编码 `SHAPE_CLIP`，改为 `shapeClipPath(type, cornerCut)` 复用共享形状几何 `getShapePolygonPoints`（画布/导出/缩略图三端同一几何），使预览形状与真实工具完全一致，不再重复定义形状工具代码。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 竖排/书脊文字输入框改用正常文本框，修复跳动与字符成方块

### 需求
书脊相册名/日期以及所有文字方向为竖向的文字，双击输入时应与正常用户文本框一致——不产生跳动、不出现字符成方块，与横向文字输入效果一致。

### 根因
编辑 textarea 对竖排文字（`isVertical`/书脊）启用了 `writingMode: 'vertical-rl'`，导致编辑输入框样式与正常横向文本框不一致：光标准位异常产生跳动，且竖向书写渲染个别字符成方块。

### 实现
- **[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 编辑浮层 textarea：移除 `writingMode: isVert ? 'vertical-rl' : undefined`，所有文字（含竖排/书脊）统一使用**正常横向文本框**输入。
  - 文字最终显示方向仍由 Konva / 导出按 `isVertical` 决定；编辑输入一律横向，与正常文字工具一致，无跳动、无方块。
  - 竖排窄列的最小编辑宽（30px）保留，确保窄书脊/窄列可编辑。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 书脊品牌 logo 全屏/缩略图/导出不渲染修复

### 需求
封面上书脊的 MemBook 品牌 logo，在编辑器画布正常显示，但全屏显示、底部导航栏缩略图、网格视图缩略图、导出时均未渲染。

### 根因
- 书脊 logo 只在 [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx) 硬编码绘制，未进入页面数据模型，也不在各渲染引擎中统一绘制。
- **缩略图/全屏**（thumbnailCore / gridThumbnailRenderer / thumbnail.worker）完全没有绘制书脊 logo。
- **导出**（exportEngine）虽有 logo 代码，但坐标直接用 `mm` 而非逻辑像素（`mm × MM_TO_PX`），导致 logo 极小且错位，实际不可见。

### 实现
- **[utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)** `drawPageToCanvas`：新增 `logoImage` 参数，封面页书脊顶部绘制 logo（半透明 0.5，`x=(书脊-0.6×书脊)/2`、`y=0.7×书脊`，逻辑像素，与画布一致）。
- **[utils/gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)**：
  - 主线程路径预载 `logoLight`，`drawPageToCanvas` 传 `getReadySpineLogo()`。
  - Worker 路径为封面页 `getSpineLogoBitmap()`（每次新建 ImageBitmap 避免重复 transfer），随请求转移，Worker 端绘制后 close。
  - `RENDER_VERSION` 6→7，使旧缓存缩略图（无 logo）自然失效重渲染。
- **[utils/thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts)**：请求新增 `logoBitmap`，传给 `drawPageToCanvas`，绘制后 close。
- **[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：logo 坐标改为逻辑像素（`mm × MM_TO_PX`），与画布/缩略图一致，导出正确显示。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 书脊文本框宽度与书脊宽度保持一致

### 需求
书脊相册名/日期文本框宽度收小，与书脊宽度保持一致（贴合书脊左右边缘）。

### 实现
- **[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)** `buildSpineElements`：
  - 文本框宽度由 `min(字号+6, spineWidth)` 改为直接取 `spineWidth`，`x = centerX - spineWidth/2`（即 x=0），使文本框左右与书脊边缘对齐、宽度一致；文字仍居中于书脊。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 封面书脊相册名放画册中线 + 日期放底部对称

### 需求
书脊相册名放在画册中线上；日期放在底部书脊下方，其与底部距离与顶部 logo 到顶距离保持一致。

### 实现
- **[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)** `buildSpineElements`：
  - 相册名 `centerY` 由 `pageHeight*0.42` → `pageHeight*0.5`，放在画册纵向**中线上**。
  - 日期移到**底部书脊**：底边距与顶部 logo 顶边距一致。
    - 顶部 logo 顶边距 = `spineWidth*0.7`（与画布 logo 渲染 `y = spinePx*0.7` 对应）。
    - 日期中心 `centerY = pageHeight - logoTopOffset - 日期盒高/2`，使日期底边距 = logo 顶边距，上下对称。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 封面书脊相册名/日期文本框尺寸与输入方式修复

### 需求
文字工具改动后，重新修复封面模板书脊：相册名与日期的文本框尺寸大小、输入方式，并复查封面模板的竖向/横向文字。

### 根因
- **相册名文本框过窄**：书脊文字盒宽固定 `min(6, spineWidth*0.5)`（6mm），而竖排正立单字按字号 9mm 渲染，字符比盒宽还大，选框与文字不贴合。
- **日期文本框过矮**：日期盒高固定 20mm，而竖排 4 位日期需约 40mm+（每字 字号+2），文字溢出盒外。
- **输入方式**：编辑浮层对所有文字强制 50px 最小宽，在窄书脊（约 18mm）上编辑时浮层溢出书脊。

### 实现
- **[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)** `buildSpineElements`：
  - 盒宽改为「字号 + 6」（单列宽度，与 `fitTextSize` 一致，`min(字号+6, spineWidth)`），选框与正立竖排文字贴合。
  - 盒高改为按内容自适应：相册名 `max(50, 字数×(字号+2)+8)`，日期 `max(20, 字数×(字号+2)+8)`，不再溢出。
- **[components/editor/Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)** 编辑浮层：竖排文字用更小的 30px 最小宽（`isVert ? 30 : 50`），编辑浮层贴合窄书脊列，不再溢出；横排文字仍为 50px 不变。

### 复查
- 封面模板**横向**预设文字：`verticalAlign: 'center'` 已对齐，字号/换行/渐变正常。
- 封面模板**竖向**（书脊）文字：逐字正立、尺寸贴合、编辑浮层适配，画布/缩略图/导出一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字工具：竖排（isVertical）垂直居中没有生效修复

### 需求
文字方向设为竖向（竖排）时，垂直对齐（居中/底部）未生效，竖排文字始终顶部对齐。

### 根因
`isVertical` 竖排渲染是**逐字正立、自上而下排列**，只按 `align` 做了水平定位，从未套用 `verticalAlign`（top/center/bottom）。因此竖排时垂直居中/底部对齐不起作用，始终顶对齐。

### 实现
为三端竖排渲染统一补上「垂直对齐」逻辑：构建字符节点后，按整块文本的纵向范围（minY..maxY+fontSize）相对内容盒（高 = 框高 − 上下内边距）计算下移量——
- `center`：`vshift = (contentH − blockH) / 2`
- `bottom`：`vshift = contentH − blockH`
- `top`：不偏移（`vshift ≤ 0` 时不移动，避免内容超出时产生负偏移）

改动文件（三端一致，保证画布/缩略图/导出效果一致）：
- [canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)
- [utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)
- [utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 封面书脊相册名文字角度错误修复 + 日期默认填 2026

### 需求
修复封面书脊上相册名称文字角度不对的问题；添加日期时默认填写为 2026 年。

### 根因
- **书脊文字角度错误**：[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts) 的 `buildSpineElements` 同时设置了 `isVertical: true` 与 `rotation: -90`。`isVertical` 已把每个字**正立**自上而下竖排（正是书脊所需），再额外旋转 `-90°` 会把整块正立文字旋转成横躺，导致相册名角度错误。
- **日期默认值不对**：占位符日期用 `dateRange || text` 兜底，而 `deriveDateRange` 在照片无日期时返回 `undefined`，于是落到模板硬编码的 `'2024'`，而非 2026。

### 实现
- **[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：
  - `buildSpineElements` 书脊文字 `rotation: -90` → `rotation: 0`，配合 `isVertical` 逐字正立竖排，角度正确。
  - `deriveDateRange`：照片无日期时返回**当前年份**（`new Date().getFullYear()`，即 2026）作为默认，封面/封底日期占位符与书脊日期统一填 2026；有照片日期时仍按实际年份区间填充。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 封面模板文字进入编辑跳动修复 + 精简模板形状

### 需求
封面里的文字点击进入编辑时仍会跳动，而相册正常页面不会；且要求封面形状元素按实际封面设计添加，不要随便乱加点缀。

### 根因
- **封面文字跳动**：普通文字工具创建元素时固定写入 `verticalAlign: 'center'`（[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)），而封面模板文字转换时未写该字段。Konva 渲染对缺省 `verticalAlign` 按居中（`TextElementNode` 缺省 `'middle'`），但编辑浮层（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）对「不存在该字段」的元素按 `'top'` 处理 → Konva 居中、编辑浮层置顶，进入编辑时文字跳顶。
- **形状误加**：上版模板补充了过多随机点缀（星形/三角/六边形/切角矩形/菱形等），与封面实际设计无关。

### 实现
- **[store/editorStore/pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`presetTextToPageElements` 为封面文字补写 `verticalAlign: 'center'`，与普通文字工具一致，进入编辑不再跳动。
- **[types/cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：**精简形状元素**，仅保留有实际设计作用的形状——
  - 保留：照片蒙版（全幅模板文字可读性）、照片相框（经典蓝调渐变描边边框）、分隔细线、渐变笔触条纹（年度回顾设计语言）、太阳图形（盛夏主题标志）。
  - 移除：多图拼排的菱形/切角装饰、恋恋故事的菱形点缀、经典蓝调的角标菱形、盛夏的星形/三角、年度回顾的六边形、封底配套的菱形/星形等随机点缀。
  - 顺带清理未使用的渐变常量（SKY/CREAM/MINT/DUSK/SUNSET）。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 根据文字/形状工具优化重新生成封面模板（渐变 + 多样形状）

### 需求
文字/形状工具已支持线性渐变填充、渐变描边、圆角/切角及多种形状（三角/菱形/多边形/星形/切角矩形等），据此重新设计封面与封底模板，让模板直接复用这些新能力，形成更丰富的视觉层次。

### 实现
- **[types/cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：
  - 新增常用渐变常量（GOLD 金色 / INKDARK 墨蓝 / ROSEGOLD 玫瑰金 / CORAL 珊瑚 / RAINBOW / BLUEVIOLET / FOREST 等），供多套模板复用，避免重复定义。
  - 封面 8 款全部升级：标题/副标题改用**渐变文字**（如金色衬线标题、墨蓝衬线标题、珊瑚粗体标题、蓝紫渐变标题）；装饰改用**渐变形状**（菱形 GOLD/ROSEGOLD、星形 CORAL、三角 SUNSET、六边形 RAINBOW）；全幅照片模板的底部蒙版改为**从透明→半透明的竖向渐变**（视觉更自然）；经典蓝调用**渐变描边 roundedRect 相框**（strokeGradient + cornerRadius）。
  - 封底 8 款同步成套升级，配色/字体/渐变与封面一致。
- **[types/cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：`import type { Template, GradientStop }`，复用 `GradientStop` 类型定义渐变数组。

### 说明
- 渐变字段（`gradient`/`gradientAngle`/`strokeGradient`/`strokeGradientAngle`/`cornerRadius`/`cornerCut`）已由 `presetTextToPageElements`/`presetShapeToPageElements` 映射到页面元素，画布/缩略图/导出三端复用既有渐变渲染逻辑，模板应用后即为可编辑元素。
- 模板应用约定的 `backCover` 成套关联、`spineColor` 书脊、`preview` 预览模式均保持不变。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 垂直居中文字进入编辑时跳动到顶部（WYSIWYG 不一致）修复

### 需求
垂直居中的文字，双击进入编辑时文字会跳动（先前跳到顶部，修正后仍小幅下跳），修改完成后又跳回，位置未保持在原处，与所见即所得不符。

### 根因
用 textarea 的 `padding` 做垂直居中，但「内容高度」的测量不对：textarea 的 `scrollHeight` 在盒高大于内容时返回**盒高**（而非内容高），导致垂直偏移量算错、文字位置与 Konva 不一致而跳动。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
  - **消除残余抖动（对齐 Konva 实际渲染高）**：新增 `konvaTextHMap`（keyed by 元素 id），记录 Konva 文字实际渲染高；编辑浮层的垂直偏移改用 **Konva 的实际渲染高**（node-local × canvasZoom）而非浏览器测量，与 Konva 垂直对齐完全一致 → 零抖动。
  - 浏览器测量 `measureTextareaContentH`（置高 1px + padding 置 0 读 scrollHeight）作为 Konva 上报缺失（竖排/未渲染）时的兜底。
  - `handleTextHeightChange` 用 `useCallback` 稳定化，避免内联箭头导致子组件 `useLayoutEffect` 每次重跑成环。
  - 垂直偏移用 `padding-top/bottom`：`padTop = textPad + (center→over/2 / bottom→over / top→0)`，`over = max(0, 内容盒高 − 内容高)`；textarea 高固定盒高、`box-sizing: border-box`。
  - 便利贴保持顶部对齐（padTop=padBottom=textPad）、透明文字由 Konva 展示，行为不变。
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：横向 Konva `<Text>` 挂 ref，`useLayoutEffect` 读取 `textArr.length`，按 Konva 公式（`行数×lineHeight×fontSize`）上报实际渲染高（`onTextHeightChange(id, h)`）；`canvasZoom`/`isVertical` 相关依赖已处理。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字编辑光标落在「隐形文字层」上、与可见文字不对应修复

### 需求
编辑文字时光标感觉落在「一层看不见的文字」上，与实际看得见的文字位置不完全对应。

### 根因
编辑 textarea 为 `color: transparent`（隐形文字仅用于定位光标），可见文字由 Konva 渲染。DOM textarea 与 Konva canvas 的字体行盒/行高渲染存在细微差异（尤其垂直对齐后），导致隐形文字层与可见文字错位，光标落在隐形的空位置。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：编辑 textarea 的 `color` 由 `transparent` 改为文字元素的实际颜色（`el.color`），使编辑时「所见文字即编辑文字」，光标与文字同属一个 DOM 元素、必然对齐；便利贴仍保留透明（由其 Konva 文本展示，避免与便签底色对比问题）。
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：编辑态（`isEditing`）隐藏 Konva 文字层（`opacity: 0`，横排与竖排逐字两处），避免与 textarea 实色文字叠加。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 文字编辑垂直对齐时光标与可见文字错位修复

### 需求
文字工具设置垂直对齐（居中/底部）后，进入编辑时输入光标与实际看到的文字位置不对齐。

### 根因
编辑 textarea 撑满整个文字框（`h-full`），textarea 文本始终「顶部对齐」；而 Konva 渲染的文字按 `verticalAlign`（top/center/bottom）在框内垂直定位。框高大于内容时（如设置了居中/底部），textarea 文字停在顶部，光标与可见文字错位。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
  - 新增 `editTextContentH` 状态，`useEffect` 在切换编辑对象时重置。
  - 编辑 textarea 外层套 flex 垂直容器，`justify-content` 按 `verticalAlign`（top→flex-start / center→center / bottom→flex-end）对内容垂直定位。
  - textarea 高度由 `h-full` 改为「实际文本内容高」（经 `scrollHeight` 于 focus/onChange 时测量），内边距移到 flex 容器，使 textarea 文本块与 Konva 文字位置一致，光标随之对齐。便利贴保持顶部对齐（`h-full`）。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 右侧对象属性面板 UI 优化 + 去除重复标题

### 需求
1. 优化形状/文字右侧属性设置面板整体 UI 效果。
2. 去除重复标题（面板头部与属性组件内部各出现一次「形状/文字属性」）。

### 实现
- **[ObjectPropertiesPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ObjectPropertiesPanel.tsx)**：头部新增对象类型图标（文字/便利贴/形状），标题跟随图标更清晰；去除属性内容外层冗余卡片（`bg-surface-hover` 双层 padding），面板宽度 264→272px，间距统一。
- **[panels/ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx)** / **[panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)** / **[panels/StickyProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/StickyProperties.tsx)**：移除组件内部重复的标题（标题统一由面板头部展示）。
- 统一 Tab 切换为分段控件样式（`rounded-lg p-1 gap-1`，激活态白底品牌色 + 阴影）；数字/下拉输入统一 `rounded-lg` + 聚焦品牌色描边与光环。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 渐变仅线性 + 渐变位置全形状覆盖 + 描边渐变修复

### 需求
1. 渐变仅支持线性，删除「线性/径向」切换按钮。
2. 渐变位置（offset）应相对整个形状定义，而非一半——画布矩形/文字渐变只覆盖形状左上半，与导出/缩略图不一致。
3. 形状描边应用渐变后显示为黑色，未实际生效。

### 根因
- **位置只覆盖一半**：`ShapeGlyph` 的矩形类用 `x=-pw/2 / y=-ph/2` 定位到左上角，而 Konva 渐变点相对节点本地原点（= 矩形左上角）；此前直接用中心系端点（-half..half），导致渐变只覆盖形状上一半。文字 `Text` 同样以左上角定位（`x=-pw/2+4`），存在同样问题。导出/缩略图路径以形状/文字中心为原点绘制，故与画布不一致。
- **描边渐变黑色**：描边 Tab 的 `ColorPalette` 未接渐变回调，选择渐变时 fallback 写入 CSS 字符串到 `shape.stroke`，Konva `stroke` 不认 CSS 渐变 → 渲染黑色。

### 实现
- **[tools/ColorPalette.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/ColorPalette.tsx)**：删除「线性/径向」切换按钮，渐变仅线性；移除 `gradientType`/`onGradientTypeChange` props。
- **[panels/ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx)** / **[panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：移除渐变类型 props；描边 Tab 接入渐变回调（写到 `strokeGradient`/`strokeGradientAngle`）。
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`ShapeElement` 新增 `strokeGradient?: GradientStop[]`、`strokeGradientAngle?: number`。
- **[canvas/ShapeGlyph.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeGlyph.tsx)**：
  - 填充/描边渐变统一线性；矩形类用 `rectGradient`/`rectStrokeGradient`（中心系端点 + pw/2 / +ph/2），使渐变铺满整个形状。
  - 新增描边渐变渲染（`strokeLinearGradient*`，Konva 原生支持），有渐变时 `stroke=undefined`。
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：`textGradientProps` 增加节点原点参数，渐变按文字内容盒（pw-8 × ph-8）铺满并换算到节点本地坐标，覆盖整个文字 box（横向与竖向每字符均正确）。
- **[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)** / **[utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**：`drawShape` 支持描边渐变（`strokeGradient` → `ctx.strokeStyle` 线性渐变），与画布一致。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-15 · 渐变逐色标编辑 + 角度/位置 + 无填充 + 全链路一致性

### 需求
1. 渐变色标（stops）不应在「主题色/纯色」Tab 显示，选择渐变后切到纯色不应残留。
2. 渐变色标百分比数字溢出框外，需优化布局。
3. 渐变支持 PPT 式角度设置与色标位置（offset）设置。
4. 渐变起点/终点颜色支持「无填充」（该端透明）。
5. 页面画布渲染效果与缩略图、全屏、导出的渐变效果须一致（红色→白色且终点透明度 0% 的场景）。

### 实现
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`GradientStop` 新增 `alpha?: number`（0-1，默认 1，逐色标不透明度）；`ShapeElement` / `PageTextElement` 新增 `gradientAngle?: number`（0-360，默认 45 = 左上→右下）。
- **[constants/colorPalette.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/constants/colorPalette.ts)**：
  - 新增 `linearGradientEndpoints(w, h, angleDeg)`：按角度投影到包围盒四角求起止点，供各渲染路径统一计算线性渐变端点。
  - 新增 `gradientFlatStops(stops)`：将结构化 stops 展平，`alpha < 1` 时用 `toRgba(color, alpha)`，保证 Konva 与 Canvas 一致。
- **[tools/GradientStopEditor.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/GradientStopEditor.tsx)**：全新逐色标编辑器，每色标含「起点/终点标签 + 颜色取色 + 无填充切换 + 位置(offset 0-100%) + 透明度(0-100%)」，用 `shrink-0` + `tabular-nums` 固定宽度避免百分比溢出。
- **[tools/ColorPalette.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/ColorPalette.tsx)**：渐变编辑功能内聚到「渐变色」Tab；仅当存在 stops 时显示「线性/径向切换 + 角度滑杆 + 逐色标编辑」，纯色 Tab 不再出现任何渐变控件；新增 `onGradientTypeChange` / `onGradientAngleChange` / `onGradientStopsChange` 回调。
- **[panels/ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx)** / **[panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：改用新的 `ColorPalette` 渐变 props，移除旧渐变编辑器；`alpha=0` 色标写回历史。
- **渲染一致性（全链路）**：[canvas/ShapeGlyph.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeGlyph.tsx) / [canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx) 与 [utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts) / [utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) 统一使用 `linearGradientEndpoints` 计算端点 + `gradientFlatStops`/`toRgba` 处理逐色标 alpha，保证画布、缩略图、全屏、导出渐变完全一致。
- **i18n**：zh-CN / en-US 新增 `editor.tools.gradientStops/gradientStart/gradientEnd/gradientStopOpacity/gradientPosition/gradientNoFill/gradientAngle`。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-14 · 色盘增强：自定义取色器 + 鲜艳色系 + 鲜艳渐变

### 需求
1. 色盘功能：用户可自定义选择任意颜色。
2. 以鲜艳好看的颜色为基础色，包含主题色系。
3. 渐变颜色更多，并增加鲜艳渐变色系。

### 实现
- **[constants/colorPalette.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/constants/colorPalette.ts)**：
  - 新增 `VIBRANT_COLORS`（鲜艳色系，含黑/白/红/橙/黄/绿/青/蓝/紫/粉/天蓝，含品牌紫主题色）。
  - 新增 `MORANDI_GRADIENTS` / `VIBRANT_GRADIENTS`（莫兰迪 10 个 + 鲜艳 15 个渐变，含日落/晚霞/海洋/星空/品牌紫等）。
  - 新增分组结构 `THEME_COLOR_GROUPS` / `GRADIENT_COLOR_GROUPS`（key: morandi | vibrant）。
  - `THEME_COLORS` / `GRADIENT_COLORS` 平铺全部，供画笔等单一数组场景。
- **[tools/ColorPalette.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/ColorPalette.tsx)**：纯色/渐变 Tab 下按「莫兰迪 / 鲜艳」分组展示 swatch；纯色 Tab 顶部新增自定义取色器（`<input type="color">`）；对勾图标按颜色明暗自适配。
- **[tools/BackgroundPicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/BackgroundPicker.tsx)**：纯色 Tab 顶部新增自定义取色器。
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`GRADIENT_BACKGROUNDS` 增加 12 个鲜艳渐变背景。
- **i18n**：zh-CN / en-US 新增 `editor.colorPalette.groupMorandi/groupVibrant/customColor`。

### 验证
- `npx tsc --noEmit` 通过，零错误；Vite 热更新正常。

---

## 2026-08-14 · 莫兰迪高级灰色板重构 + 形状/文字渐变全链路打通

### 需求
1. 形状、文字、背景颜色选择偏少、不够高级、排列无规律。
2. 渐变色在形状、文字上未生效。

### 根因
- 旧色板颜色偏少且排列混乱，无统一风格。
- 形状渐变：`ShapeProperties` 的 `ColorPalette` 渐变 tab 返回 CSS 渐变字符串写入 `shape.fill`，但形状渲染（`ShapeGlyph`/`exportEngine`）只认结构化 `shape.gradient: GradientStop[]`，故不生效；`thumbnailCore.drawShape` 完全不支持渐变。
- 文字渐变：`PageTextElement` 无 `gradient` 字段，画布/导出/缩略图三端均按纯色 `color` 渲染。

### 统一方案（莫兰迪高级灰 + 结构化渐变）
- **[constants/colorPalette.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/constants/colorPalette.ts)**：
  - `THEME_COLORS` 重构为莫兰迪低饱和色盘，按「色相分组 + 明度梯度」有序排列（灰阶/红/橙/黄/绿/青/蓝/紫/粉/棕，各 4 档）。
  - 新增 `GradientPreset` 类型（name + css + stops + type），`GRADIENT_COLORS` 改为结构化渐变预设（10 个莫兰迪渐变），同时提供 `css`（背景/预览）与 `stops`（形状/文字 Konva/Canvas）。
  - 新增 `gradientToCss(stops)` 工具（结构化 stops → CSS 渐变，用于选中态回显）。
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：
  - `THEME_BACKGROUNDS` / `GRADIENT_BACKGROUNDS` 重构为莫兰迪配色（与形状/文字渐变一致）。
  - `PageTextElement` 新增 `gradient?: GradientStop[]`、`gradientType?: 'linear'|'radial'`。
- **[tools/ColorPalette.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/ColorPalette.tsx)**：新增 `onGradientChange(preset)` 回调，渐变选中时传递结构化 preset（未提供则 fallback 到 `onColorChange(css)`）。
- **[panels/ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx)**：填充色 `ColorPalette` 增加 `onGradientChange` → 写入 `shape.gradient` + `gradientType`；移除旧的「渐变填充」checkbox（改为有渐变时显示线性/径向切换）。
- **[panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)**：文字颜色 `ColorPalette` 增加 `onGradientChange` → 写入 `el.gradient` + `gradientType`。
- **[canvas/TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：新增 `textGradientProps`，文字按 `el.gradient` 渲染 Konva 渐变（线性左上→右下 / 径向中心向外），横排/竖排均支持。
- **[utils/exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)** / **[utils/thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)**：`drawTextElement` 支持文字渐变；`drawShape`（thumbnailCore）补上形状渐变支持（此前缺失）。
- **[tools/ColorPicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/ColorPicker.tsx)**：画笔颜色改用统一莫兰迪 `THEME_COLORS`。

### 验证
- `npx tsc --noEmit` 通过，零错误；Vite 热更新 `Found 0 errors`。

---

## 2026-08-14 · 左侧工具面板扁平化布局优化

### 需求
左侧「工具」面板点击后无需再展开分区即可使用，布局信息层级更清晰。

### 实现
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**：移除 `CollapsibleSection` 折叠组件，改为「绘制 / 添加元素 / 形状 / 背景」四大扁平分区，核心操作直接可见点击即用；新增 `SectionTitle`、`AddButton` 辅助组件。
- **i18n**：zh-CN / en-US 新增 `editor.tools.elementsSection/elementsHint`。

### 验证
- `npx tsc --noEmit` 通过，零错误；Vite 热更新正常。

---

## 2026-08-14 · 修复抽离属性面板导致的 ToolsPanel 报错

### 根因
抽取属性面板时误删了 ToolsPanel 中的 `setSelectedShapeId` 声明，导致 `handlePickShape` 引用未定义变量，组件渲染抛 `ReferenceError`（被 ErrorBoundary 捕获）。

### 实现
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L63)**：补回 `setSelectedShapeId = useEditorStore((s) => s.setSelectedShapeId)`。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-14 · 右侧对象属性面板（点选文字/便利贴/形状快速设计，可收起）

### 需求
页面中点击文字/便利贴/形状后，右侧弹出对象属性面板快速设计，无需切到左侧工具面板；面板支持收起。

### 实现
- **[panels/TextProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/TextProperties.tsx)** / **[StickyProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/StickyProperties.tsx)** / **[ShapeProperties.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/panels/ShapeProperties.tsx)**（新增）：从 ToolsPanel 抽出文字/便利贴/形状属性面板，供左侧工具面板与右侧对象面板共用（单一来源，避免重复）。
- **[ObjectPropertiesPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ObjectPropertiesPanel.tsx)**（新增）：画布右侧浮动对象属性面板，读取 store 中选中对象（selectedTextId/selectedStickyId/selectedShapeId）；未选中则不显示；折叠为右侧竖条（状态持久化）。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**：属性面板内联 JSX 改为引用抽取组件；清理不再使用的导入/变量。
- **[uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)**：新增 `objectPanelCollapsed` + `setObjectPanelCollapsed`（localStorage `membook_object_panel_collapsed` 持久化）。
- **[EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx#L478)**：画布容器内渲染 `<ObjectPropertiesPanel />`（absolute 右缘，z-40）。
- **i18n**：zh-CN / en-US 新增 `editor.objectPanel.title/collapse/expand`。

### 验证
- `npx tsc --noEmit` 通过，零错误；`npx vite build` 成功。
- 浏览器首页正常加载，ObjectPropertiesPanel / panels / uiStore 均无运行错误（仅浏览器环境预期的 Tauri 许可证桥接错误）。

---

## 2026-08-14 · 修复文字垂直居中对齐不生效

### 需求
文字工具垂直居中在文本框内不生效，但顶部/底部对齐生效。

### 根因
画笔 [TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L354) 的 Konva `<Text verticalAlign>` 直接传了业务值 `'center'`，而 Konva 只认 `'top' | 'middle' | 'bottom'`，`'center'` 被忽略退化为 top——导致居中在视觉上与顶部一致（看起来「不生效」），而 top/bottom 均正常。

### 实现
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L354)**：`verticalAlign` 传 Konva 前把 `'center'` 映射为 `'middle'`（默认也取 `'middle'`）。导出/缩略图引擎本就手动计算 y 处理 `'center'`，无需改动。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-14 · 切角调节手柄贴合切角斜边，随切角向中心移动

### 需求
切角黄色控制点拖动时应贴合形状切角，随切角增大沿对角线往形状中心移动。

### 实现
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L277-L314)**：切角手柄改为位于**切角斜边中点**（距角固定 gap + 切角/2，随切角增大向中心移动）；拖动映射改为 `newCut = startCut + 2×Δ`（手柄在斜边中点，手柄移动 Δ 对应切角变化 2Δ）。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-14 · 圆角改百分比制，正方形可调成正圆

### 需求
1. 圆角也按百分比（与切角一致）。
2. 100% 圆角时正方形应能变成正圆。

### 实现
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts#L546-L547)**：`ShapeElement.cornerRadius` 语义由 mm 改为 **0-1 比例**（= min(w,h)/2 的倍数）；rectangle 默认 0，roundRect 系列默认 0.15；100%（=1）时正方形圆角 = 半边长 → 变为正圆。
- **[shapeGeometry.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/shapeGeometry.ts#L129-L149)**：`getRectCornerRadii` 改为 `cornerRadius × min(pw,ph)/2`。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L228-L266)**：圆角调节手柄改为 0-1 比例映射（`Math.max(dx,dy)/maxR`，上限 1）。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L543-L562)**：圆角滑杆改百分比显示（0-100%）。
- **i18n**：`shapeCornerRadius` 文案改「圆角」/「Corner Rounding」。

### 验证
- `npx tsc --noEmit` 通过，零错误；`npx vite build` 成功。

---

## 2026-08-14 · 切角矩形支持 PPT 式调节，切角/圆角范围至形状最大

### 需求
1. 切角矩形（cutCornerRect/cutDiagonalRect）的切角也应支持调节（如圆角一样）。
2. 调节值范围要大，切角/圆角均到形状本身最大值（min(w,h)/2）为止。

### 实现
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts#L548-L549)**：`ShapeElement` 新增 `cornerCut?: number`（0-1 比例 = min(w,h)/2 的倍数，切角矩形默认 0.25）。
- **[shapeGeometry.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/shapeGeometry.ts#L61-L117)**：`getShapePolygonPoints` 新增 `cornerCut` 参数，`getCutPx` 把比例映射到像素（上限 min(w,h)/2）；新增 `isCutAdjustable`。
- **[ShapeGlyph.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeGlyph.tsx#L71)** / **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L1353)** / **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L628)**：三端渲染传入 `shape.cornerCut`。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L275-L320)**：新增**切角调节黄色菱形手柄**（仅切角矩形类），沿对角线拖动更新 `cornerCut`（0-1，上限 min(w,h)/2）。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L564-L583)**：新增「切角大小」滑杆（0-100%）。
- **i18n**：zh-CN / en-US 新增 `shapeCornerCut`。

### 验证
- `npx tsc --noEmit` 通过，零错误。

---

## 2026-08-14 · 形状贴合最外边缘 + 新增矩形变体 + PPT 式圆角调节

### 需求
1. 三角形/五边形/六边形/星形控制器框仍未按形状最外边缘贴合。
2. 控制器上下左右的控制点方块未居中在控制器框中线，位于线框内。
3. 新增单角圆角矩形、对角圆角矩形、减去对角矩形等多种矩形。
4. 矩形角支持调整（PPT 式圆角调节关系）。

### 实现
- **[shapeGeometry.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/shapeGeometry.ts)**（新增，纯函数无 React 依赖）：
  - `getShapePolygonPoints(type,pw,ph)`：先用单位外接圆生成名义顶点，再按「自然包围盒 → pw×ph」分别缩放居中，保证三角/菱形/五边/六边/星形/平行四边形/梯形/切角矩形/减去对角矩形的**最外顶点恰好贴合控制盒四边**（修复合集「min/min 外接圆 + 等倍拉伸」导致不贴合）。
  - `getRectCornerRadii(type,pw,ph,cornerRadius)`：矩形类每角圆角半径（rectangle 默认 0，roundedRect 系列默认短边 15%）。
  - `isCornerAdjustable(type)`：矩形类判断（用于显示圆角调节手柄）。
- **[ShapeGlyph.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeGlyph.tsx)**：多边形/星形/切角矩形统一改用共享 `getShapePolygonPoints` 的 `<Line closed>`；矩形类用 `getRectCornerRadii` 的 `<Rect cornerRadius>`。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L1335-L1368)** / **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L616-L641)**：drawShape 同步改共享几何 + 新增 `roundRectPerCorner`（每角独立圆角路径，兼容旧 WebView2）。thumbnailCore 直接 import shapeGeometry（纯函数，Worker 无 React 依赖）。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L226-L271)**：新增 PPT 式**黄色菱形圆角调节手柄**（仅矩形类），沿左上→中心 45° 拖动实时更新 `cornerRadius`（取 dx/dy 较大增量，上限 min(w,h)/2，`getRelativePointerPosition` 兼容旋转）。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L336-L361)**：hover/选中虚线框改为**贴合控制盒边界**（去掉外扩 padding），使 8 方向手柄与边点长方块居中于边框中线。
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts#L510-L515)**：`ShapeElement` 新增 `cornerRadius?: number`；`ShapeType` 新增 `singleRoundRect`/`diagonalRoundRect`/`cutDiagonalRect`。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L543-L563)**：新增「圆角半径」滑杆（仅矩形类，显示有效默认值）；新增 3 个矩形变体 SVG 图标。
- **i18n**：zh-CN / en-US 新增 `shape_singleRoundRect`/`shape_diagonalRoundRect`/`shape_cutDiagonalRect`/`shapeCornerRadius`。

### 验证
- `npx tsc --noEmit` 通过，零错误；`npx vite build` 成功（含 thumbnail.worker 打包 shapeGeometry）。
- 浏览器首页正常加载，shapeGeometry/ShapeGlyph/thumbnail.worker 无运行错误（仅浏览器环境预期的 Tauri 许可证桥接错误）。

---

## 2026-08-14 · 新增矩形变体形状（圆角矩形/平行四边形/梯形/切角矩形）+ 移除正方形

### 需求
1. 正方形与矩形效果一致，保留矩形移除正方形。
2. 增加矩形的多种形状：圆角矩形、平行四边形、梯形、切角矩形。

### 实现
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts#L510-L514)**：`ShapeType` 移除 `square`，新增 `roundedRect`、`parallelogram`、`trapezoid`、`cutCornerRect`；`SHAPE_TYPES` 同步更新。
- **[ShapeGlyph.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeGlyph.tsx#L80-L125)**：新增渲染分支——`roundedRect` 用 Konva `<Rect cornerRadius>`（短边 15% 圆角），`parallelogram`/`trapezoid`/`cutCornerRect` 用 `<Line closed>` 多边形绘制；导出 `shapePolygonPoints` 函数供 exportEngine 与 thumbnailCore 共享。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L1345-L1361)**：export `drawShape` 新增 `roundedRect`（roundRectPath 圆角矩形）/ `parallelogram`/`trapezoid`/`cutCornerRect`（shapePolygonPoints 多边形）分支。
- **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L627-L640)**：thumbnail `drawShape` 新增同样分支，内联 `shapePolygonPts` 函数避免 Worker React 依赖。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L745-L749)**：ShapeIcon 新增 4 个 SVG 图标——圆角矩形 ◻、平行四边形 ⏥、梯形 ⏢、切角矩形 ◰。
- **i18n**：zh-CN / en-US 移除 `shape_square`，新增 `shape_roundedRect`/`shape_parallelogram`/`shape_trapezoid`/`shape_cutCornerRect` 中英文翻译。

### 验证
- `npx tsc --noEmit` 通过，零错误。
- 画布/导出/缩略图三端渲染逻辑一致，多边形顶点坐标与 ShapeGlyph 共享 `shapePolygonPoints` 函数。

---

## 2026-08-14 · 修复形状点击仍无法选中 & 控制器框不贴合形状/缩放漂移

### 需求
1. 形状工具单击选中形状仍会无法选中。
2. 仅正方形控制器节点贴合形状边缘，其他形状（圆/三角/菱形/五边形/六边形/星形）控制器框未贴合形状实际大小，缩放时锚点漂移。

### 排查结论（根因）
- **点击无法选中**：ShapeGlyph 给所有形状本体设了 `listening: false`，导致形状 Group 无命中区域，点击事件穿透到 Stage 背景（只能框选，无法点选）。
- **控制器框不贴合**：Circle 与正多边形/星形用 `radius = min(pw,ph)/2` 绘制（视觉内切于 min×min 正方形），而 8 手柄按 `pw×ph` 渲染。当宽高不等时控制器框远大于形状本体，缩放锚点（对角的框角）与形状实际边缘不重合，产生"漂移"。

### 实现
- **[ShapeGlyph.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeGlyph.tsx)**：形状本体 `listening: true`（可点击选中，事件冒泡到 Group）；所有形状改为「填满 pw×ph 盒子」——圆→Ellipse、square→Rect(pw×ph)、正多边形/星形先以 min 为外接圆画正形再按 `pw/min、ph/min` 拉伸填满。控制器框与形状实际边缘贴合，缩放锚点固定。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L1312-L1385)** / **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L611-L642)**：导出/缩略图 `drawShape` 同步改为填满盒子（圆/三角/菱形/正多边形/星形按 halfW×halfH 绘制），保证画布、导出、缩略图一致。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-14 · 修复文字垂直居中不生效 & 形状点击选中/节点控制异常

### 需求
1. 文字工具垂直居中没有生效（切换顶/居中/底无可见变化）。
2. 形状工具：点击选中形状无效（点击后未选中）；框选能选中但节点控制不对（多选后独立手柄与多选包围盒冲突）。

### 排查结论（根因）
- **垂直居中不生效**：此前 `fitTextSize` 改为返回精确文字高度（去掉 `Math.max`），文本框高度被压缩到与文字一致，垂直对齐因「无多余空间」而不可见（居中=顶对齐）。
- **点击不选中形状**：形状工具（`activeTool==='shape'`）模式下所有元素 `listening=false`，点击形状事件穿透到 Stage → 被当作背景点击清除/开始绘制，而非选中；仅框选（Stage 级检测）能选中。
- **节点控制异常**：框选多选后，ShapeNode 的独立 8 手柄仍渲染，与多选包围盒（useMultiElementGroupSelect）同时生效，产生冲突。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L62-L95)**：`fitTextSize` 恢复 `Math.max(el.height, 文字高度)`（横排/竖排），文本框保留用户设定高度，垂直对齐有空间可见。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1772)**：形状 `interactive` 改为 `activeTool !== 'brush' && activeTool !== 'eraser'`（形状工具模式下形状仍可交互可点击，仅画笔/橡皮擦禁用）。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2020-L2037)**：形状工具 onMouseDown 检测点击是否落在已有形状（`findAncestor('.shape-node')`），命中则选中该形状而非开始绘制；Ctrl 点击交给 ShapeNode onClick 处理多选。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L23-L40, L313-L315)**：新增 `isMulti` prop；8 手柄 + 旋转手柄仅在单选（`isSelected && interactive && !isMulti`）时渲染，多选由多选包围盒统一控制，消除冲突。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-14 · 文字垂直对齐与文本框适配 + 形状改自绘 8 控制手柄

### 需求
1. 文字工具：文本框高度与文字高度保持一致（新增文字不再出现巨高空框）；增加文字垂直对齐（顶端/居中/底端）功能，默认垂直居中。
2. 文字工具：占位文案「点击输入文字」改为「双击输入文字」（双击进入编辑，单击仅选中）。
3. 形状工具：单击选中形状，选中后显示 8 个控制节点调整大小；原 Transformer 方案操作异常，改为与文字/便利贴/贴纸一致的自绘手柄体系。

### 实现
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts#L486-L488)**：`PageTextElement` 新增 `verticalAlign?: 'top'|'center'|'bottom'`（默认居中）。
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L354)**：横排 Konva `<Text>` 用 `verticalAlign={el.verticalAlign ?? 'center'}`。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L62-L95)**：`fitTextSize` 改为返回精确文字高度（去掉 `Math.max(el.height, ...)`），横排/竖排文本框高度均与文字一致。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L92-L103)**：新增文字初始化 `verticalAlign:'center'`，高度改为 `fontSize*1.2+4`（与 fitTextSize 单行一致）。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L382-L399)**：文字属性面板新增垂直对齐三按钮（顶端/居中/底端）。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L1062-L1079)** / **[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts#L451-L467)**：横排文字导出/缩略图按 `verticalAlign` 计算起始 y（默认居中），保证所见即所得。
- **i18n**：`editor.textElement.placeholder` 改「双击输入文字」/「Double-click to enter text」；新增 `verticalAlign/alignTop/alignMiddle/alignBottom` 中英文。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx)**：重写为自绘 8 方向控制手柄（角点圆形 + 边点长方块）+ 旋转手柄（15° 吸附、Shift 缩放等比、单击不旋转），坐标以中心（x/y）为基准，与 StickerNode 一致；新增 `onUpdate` prop 实时写 store。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：ShapeNode 调用新增 `onUpdate`；移除形状 Konva Transformer 的 JSX 块、ref 声明、`commitShapeTransform` 与绑定 useEffect，避免与自绘手柄双重叠加。

### 说明
- 形状由 Konva Transformer 改为自绘手柄后，与文字/便利贴/贴纸三元素完全统一（锚点视觉、15° 吸附、Shift 等比、旋转单击不跳变），修复合集 Transformer 在缩放 Group 内锚点异常的问题；四元素交互逻辑趋同，后续可提取共享 `ElementHandles` 组件。
- 文本框精确适配高度后，垂直对齐在「文本框因换行/尺寸略高于文字」时仍生效；默认居中与文字高度一致时视觉上等同顶端对齐。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-14 · 文字与形状工具交互细节深度优化（对齐 PPT 风格）

### 需求
1. 形状绘制补齐 PPT 修饰键：Shift 正形（正圆/正方形）、Alt 中心对称、Shift+Alt 中心正形、Shift 画直线/箭头吸附 15°。
2. 形状单击（无拖拽）创建默认尺寸 60×60；拖拽提交阈值从 2mm 提到 5mm，避免误触产生难选中/难删除的微形。
3. 形状单选选中态去掉双重边框（单选由 Transformer 提供，避免 ShapeNode 虚线框 + Transformer 实线框叠加）；hover 补 move 光标反馈。
4. 形状 Transformer 锚点尺寸随缩放恒定（`anchorSize/anchorCornerRadius` 除以 `canvasZoom`，与自绘元素一致）；旋转吸附统一 15° 步进（与文字/便利贴/贴纸的 `snapAngle` 一致）。
5. 文字/便利贴/贴纸去掉旋转手柄"单击即 +90°"跳变（防误触：按下即跳变导致方向感错乱），仅拖动旋转时记录历史。
6. 文字/便利贴角点 resize 支持 Shift 等比锁定（PPT 角点等比行为）；贴纸角点原为对角投影等比，无需改动。
7. 文字面板字号增加数字输入框（step 0.5），与 slider 联动，支持精确设置。
8. 三个浮动工具栏按钮集统一（编辑/置顶/置底/删除 + 关闭），形状工具栏补"关闭"按钮（文字/便利贴已有）。
9. 双击文字进入编辑时，光标定位到双击点对应字符（横排字符级定位、竖排回退到末尾），对齐 PPT 双击定位行为。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2045-L2099)**：重写形状绘制 `onMouseMove` 预览，支持 Shift/Alt 修饰键与 Shift 画线 15° 吸附（`e.evt.shiftKey/altKey`），并同步 `shapeEndRef/shapeEndState` 保证画线预览与提交一致。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2129-L2193)**：重写形状提交 `onMouseUp`，阈值 `MIN_SHAPE=5`，单击创建 `DEFAULT_SHAPE_SIZE` 默认尺寸，含修饰键包围盒提交。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L21-L40)**：新增 `showSelectionBox` prop（多选才渲染选中虚线框）；hover 设置 `move` 光标。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2625-L2637)**：形状 Transformer `anchorSize/anchorCornerRadius` 除以 `canvasZoom`；`rotationSnaps` 改为 15° 步进数组；`boundBoxFunc` 最小尺寸 4→5 与绘制阈值统一。
- **TextElementNode / StickyNoteNode / StickerNode**：旋转 `onUp` 去掉 `if(!didMove) +90°` 分支，仅拖动时 `onUpdate({}, true)`。
- **TextElementNode / StickyNoteNode**：角点 `cornerHandle` 的 `onMove` 读取 `me.evt.shiftKey`，等比推导宽高（以移动量大的维度为准）。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L322-L339)**：文字字号加 number 输入框与 px 后缀，与 slider 共用状态。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L3019-L3023)**：形状浮动工具栏补"关闭"按钮，三工具栏按钮集对齐。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L98-L165)**：新增 `estimateTextIndexAtPoint`，按与 `fitTextSize`/Konva 一致的 word-wrap 展开行并做字符宽度累加，估算双击点字符索引。
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L236-L248)**：`onDblClick` 回调携带双击点本地坐标（`getAbsoluteTransform().invert()` 换算）。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2927-L2935)**：编辑浮层 textarea `onFocus` 按索引 `setSelectionRange` 定位光标并复位；新建/工具栏/便利贴等非双击入口统一 `setTextEditCursorIndex(null)`，避免残留污染。

### 验证
- `npx tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 修复形状在画布不渲染的真正根因（sticker forEach 括号缺失致形状循环被嵌套）

### 需求
1. 形状仍不显示（此前已修 StrictMode 与渐变 stop 格式，测试后依旧不显示），且调整填充色无效果，需彻底定位。

### 排查结论（根因）
- 通过浏览器运行时检查 Konva Stage：选中形状的 Transformer 锚点/手柄存在，但形状**本体节点缺失**，且页面内容 Group 中**没有任何装饰元素**（形状/文字/贴纸/画笔全无）。
- 真正根因：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1682-L1684) 中贴纸 `stickerElements.forEach` 的 `items.push({...})` 闭合处**缺失一个 `});`**，导致紧接着的 `shapeElements.forEach` 被**意外嵌套进贴纸 forEach 回调内部**。当页面没有贴纸时，贴纸 forEach 回调不执行，形状渲染循环被整体跳过 → 形状永不渲染；对应的填充色修改因形状本体不存在而无效果。
- 旁证：文字/便利贴/画笔在贴纸 forEach **之前**（未嵌套），故正常显示；这也解释了为何只有"形状"异常。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1682-L1684, L1710-L1711)**：在贴纸 `items.push` 闭合后补一个 `});` 关闭贴纸 forEach，使形状 forEach 恢复为根级兄弟；同时删除末尾多出的一个 `});`（原先用于吸收贴纸 forEach 的闭合），保持括号平衡。

### 说明
- 此前两处修复（移除 StrictMode、渐变 stop 扁平数组）本身正确且必要，但**并非本文问题根因**；本次括号缺失才是形状不显示的直接原因。

### 验证
- `tsc --noEmit` 通过，零错误。
- 浏览器运行时确认：形状本体 Konva 节点（Rect 122×194，填充 #EF4444）已渲染到 Stage，填充色修改实时生效。

---

## 2026-08-13 · 修复形状在画布不渲染的真正根因（React StrictMode 与 react-konva Transformer 冲突）

### 需求
1. 形状工具绘制的形状在编辑页主画布仍不显示（此前已修渐变 stop 格式，测试后依旧不显示），需彻底定位并解决。

### 排查结论（根因）
- 渐变 stop 修复（ShapeNode.tsx 扁平数组）后形状仍不显示，说明另有根因。
- 真正根因：React 19 的 `<StrictMode>` 在开发期会**双挂载**组件，而 react-konva 19.x 的 `<Transformer>` 在双挂载下调用 `setAttrs` 时抛 `Cannot read properties of undefined (reading 'setAttrs')`，导致**整个 Konva Stage 崩溃、画布不渲染**（形状/槽位等全部不显示）。
- 缩略图（thumbnailCore）与全屏导出（exportEngine）走 Canvas2D 独立路径，不受 StrictMode 影响，故正常显示——这正是"只在主画布不显示"的原因。

### 实现
- **[main.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/main.tsx#L11-L18)**：移除 `<StrictMode>` 包裹。StrictMode 仅开发期生效，生产构建无影响；移除后 react-konva `<Transformer>` 不再双挂载崩溃，画布正常渲染。

### 验证
- `tsc --noEmit` 通过，零错误。
- 浏览器实测：形状、槽位在主画布正常显示。

---

## 2026-08-13 · 文字工具：竖向文字旋转修复 + 旋转按钮遮挡优化 + 便利贴输入对齐 PPT

### 需求
1. 竖向文字点击旋转按钮操作异常、没有正常显示（旋转无视觉效果）。
2. 文字旋转到 180° 时，旋转按钮被顶部悬浮导航栏遮挡，导致无法选中。
3. 竖向文字、便利贴的文字输入也同步优化，对齐 PPT 的文字输入效果。

### 排查结论（根因）
- **竖向文字旋转失效**：TextElementNode 用 `groupRotation = isVertical ? 0 : el.rotation` 强制竖排文字 Group 不旋转，导致旋转按钮改变 `el.rotation` 但无视觉效果（exportEngine/thumbnailCore 竖排路径同样不应用旋转）。而旋转角度与竖排标志应「解耦」——竖排文字也应能旋转整块。
- **旋转按钮被遮挡**：旋转手柄位于文字框下方（本地 +y），旋转到 90°~270° 时转到文字上方，与始终停靠在上方的悬浮导航栏重叠，导致无法点击。
- **便利贴输入光标错位**：编辑 textarea 内边距 6px、行高 1.2，与 Konva 便利贴渲染（内边距 8px、行高 1.4）不一致。

### 实现
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L43-L46)**：`groupRotation` 改为 `el.rotation ?? 0`（去除竖排强制为 0 的分支），竖排文字也能正常旋转整块。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L999-L1046)**：竖排文字绘制路径增加绕中心旋转变换，与画布一致。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2580-L2587)**：文字/便利贴浮动工具栏——当旋转角度使旋转手柄转到文字上方（`cos(rad) < 0`，即 90°~270°）时，工具栏翻转到元素下方，避免遮挡旋转按钮。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2635-L2638, L2685-L2687)**：编辑浮层 textarea 内边距改为便利贴 8px / 文字 4px，行高改为便利贴 1.4 / 文字 1.2，与 Konva 渲染一致，修复光标错位；编辑浮层旋转角度对竖排也应用实际旋转角度。
- 竖向文字沿用 `fitTextSize`（宽度固定、高度按需增长），对齐 PPT 输入效果。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 修复形状工具在画布渲染失败（渐变 stop 格式错误）

### 需求
1. 形状工具绘制的形状在编辑页主画布（Konva）中渲染失败（无填充/不可见），但在底部导航栏缩略图、全屏导出时正常显示。

### 排查结论（根因）
- 渐变填充的 Konva 颜色 stop 传参错误：`ShapeGlyph` 用 `shape.gradient.map((s) => [s.offset, s.color])` 生成了**嵌套数组** `[[0,'red'],[1,'blue']]`。
- Konva 的 `fillLinearGradientColorStops` / `fillRadialGradientColorStops` 需要**扁平数组** `[0,'red',1,'blue']`（见 `Shape.js __getLinearGradient`：`for (n=0; n<colorStops.length; n+=2) addColorStop(stops[n], stops[n+1])`）。
- 嵌套数组导致 `addColorStop` 收到数组而非 offset/color → Konva 渐变填充失败 → 形状在画布上不可见。而缩略图（thumbnailCore）与全屏导出（exportEngine）用 Canvas2D `addColorStop(offset, color)` 逐 stop 正确填充，故正常显示。

### 实现
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx#L37-L54)**：将渐变 stop 用 `flatMap((s) => [s.offset, s.color])` 展平为扁平数组，再赋给 `fillLinearGradientColorStops` / `fillRadialGradientColorStops`，与 PageBackground / StickyNoteNode 等其他 Konva 渐变用法保持一致。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 文字工具编辑体验优化（对齐 PPT 输入效果）

### 需求
1. 文字工具存在多处问题：双击文字后文本框大小会变化、编辑过程中文字排版跳动、文字框过小等。
2. 文字输入效果要对齐 PPT：双击进入编辑时文字框大小稳定、宽度固定（文字在其中换行）、高度按需向下增长、不因内容变化导致重排跳动。

### 排查结论（根因）
- `autoFitTextSize` 在**双击进入编辑**（onDblClick / 编辑按钮 / 新建自动编辑）时即收紧文字框，导致"双击后文本框大小变化"。
- 编辑过程中 `autoFitTextSize` **实时同时调整宽度与高度**，每敲一个字宽度变化 → 整段文字重新换行 → 排版跳动。
- 新建文字时文字为空，`autoFitTextSize` 将 150mm 宽的框收紧到最小 30mm，导致文字框过小。
- 编辑 textarea `lineHeight: 1.2` 与 Konva Text 默认 `lineHeight: 1.0` 不一致，导致光标/文字纵向错位。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：将 `autoFitTextSize` 重构为 `fitTextSize`（PPT 风格）——宽度固定为 `el.width`，高度取「当前高度」与「按固定宽度分词换行所需高度」的较大值（只增不减、向下生长、左上角不变）。双击进入编辑 / 编辑按钮 / 新建自动编辑均**不再**改动文字框大小；编辑 `onChange` 与保存 `saveText` 改用 `fitTextSize`，宽度稳定、高度按需增长，避免边打字边重排。
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L323-L335)**：横向文字 Konva `Text` 增加 `lineHeight={1.2}`，与编辑 textarea 的 `lineHeight` 保持一致，修复光标/文字纵向错位。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 封面设置独立（封面照片位圆角 + 书脊颜色 从页面设置剥离）

### 需求
1. 相册编辑模式顶部导航栏「页面」下拉下的「页面设置」中，移除「封面照片位圆角」和「书脊颜色」两个功能。
2. 在顶部导航栏「页面」下拉中新增「封面设置」功能，点击后进入独立的封面设置弹窗，在其中进行封面照片位圆角、书脊颜色设置。

### 实现
- **[CoverSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverSettings.tsx)**（新增）：独立「封面设置」弹窗，包含封面/封底照片位圆角（支持统一/每角单独 4 角设置）与书脊底色（仅封面存在时显示）。确认后批量写入 Store：对封面/封底页写 `slotCornerRadius`，对封面页写 `spineColor`，不影响普通页面设置。
- **[PageSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageSettings.tsx)**：移除「封面/封底照片位圆角」区与「书脊底色」区，以及相关影子状态（`coverCorners`/`coverUniform`/`spineColor`/`hasCover`）与读写逻辑；清理不再使用的导入（`isCoverOrBackCoverPage`/`isCoverPage`/`AlbumPage`）。
- **[Toolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Toolbar.tsx)**：在「页面」下拉中「页面设置」下方新增「封面设置」菜单项（书册图标），打开 `CoverSettings` 弹窗。
- **[zh-CN.json / en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**：新增 `editor.menu.coverSettings` 与 `editor.coverSettings.*` 双语 key。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 封面与封底成套设计（不再拆分开）

### 需求
1. 每套封面模板都应自动设计一套风格统一的封底模板，不应把封面与封底拆分成两个独立选择的列表。
2. 封面与封底要有整体性：同背景色/字体/配色，应用封面时自动同步生成配套封底。

### 实现
- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：
  - 为 8 款封面各配套 1 款风格统一的封底（`backMinimalWhite`/`backFullPhoto`/`backEveryday`/`backMultiPhoto`/`backCouplesStory`/`backClassicBlue`/`backSummerBright`/`backYearInReview`/`backBlank`），同背景色/字体/装饰配色。
  - `Template` 新增 `backCover?: Template` 字段（见 index.ts），每套封面通过 `cover.xxx.backCover = backXxx` 关联配套封底。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts#L494-L500)**：`applyCoverTemplate` 应用封面后自动调用 `applyBackCoverTemplate(template.backCover.id)` 同步应用配套封底，封面与封底整体成套、不拆分开。
- **[CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)**：移除独立的「封底模板」分区，只展示封面模板；点击封面自动应用配套封底。
- **[CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx)**：主页封面设计库移除独立「封底模板」分区，只展示封面模板。
- **[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx#L351-L353)**：`handleCreateFromCover` 统一走 `applyCoverTemplate`，自动同步生成配套封底。
- **[zh-CN.json / en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**：更新 `editor.coverLibrary.hint` / `home.coverGallery.hint` 文案，说明封面与封底成套、自动同步生成。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 封面模板文字等比缩放 + 形状/文字工具优化

### 需求
1. 封面模板文字大小应随封面尺寸等比缩放（整体位置固定，不随页面尺寸变化而失真）；主页/编辑器封面模板列表在不同像素宽度下文字相对大小也应保持一致。
2. 形状工具：以鼠标点击位置为原点绘制（PPT 风格），绘制完成后正常显示，且为一次性操作（绘制成功后自动取消形状选择）。
3. 文字工具：旋转到 90° 不应误判为竖排；文字框紧贴文字高度/宽度；竖排文字左/中/右对齐生效；Enter 断行；双击进入编辑不跳动、位置与未编辑前一致。

### 实现
- **[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)**：`TextPreview` 文字大小由固定 `px` 改为 `cqw`（1% of 容器宽）等比缩放。封面基准宽 210mm 在画布 2px/mm 下为 420px，故 `PREVIEW_FONT_CQW = 100/420`，使预览文字始终与封面保持固定比例，主页/编辑器列表在不同卡片宽度下文字相对大小一致。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts#L29-L39)**：`presetTextToPageElements` 新增 `fontScale = pageMm.width / REFERENCE_COVER_WIDTH_MM`（基准 210mm），`fontSize` 随页面宽度等比缩放，保证封面模板文字整体比例固定。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1936-L2092)**：形状绘制以鼠标按下点为起点（`shapeStartRef`），拖拽实时预览（`shapePreview`），松开后按包围盒中心提交 `addShapeElement` 并 `setSelectedShapeId`；完成后 `setPendingShapeType(null)` + `setActiveTool('none')` 实现一次性使用。
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：`isVertical` 标志与旋转角度解耦，仅 `isVertical===true` 时逐字竖排，旋转 90° 不再误判竖排；竖排支持左/中/右对齐；编辑/非编辑统一顶部对齐避免跳动；文字框按内容收紧。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 文字框紧贴文字 + 封面切换清除旧模板文字

### 需求
1. 文字工具编辑完成后，文字框应紧贴文字内容（高度/宽度自适应），不留多余空白。
2. 封面模板切换时，旧模板的预设文字/形状不应带入新模板，只保留用户手动添加的元素。

### 实现
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2579-L2647)**：`saveText` 编辑保存时自动测量文字尺寸。横向文字：用 Canvas2D `measureText` 取最长行宽度 + `scrollHeight` 取行高，转为 mm 后更新 `width`/`height`；竖向文字：`scrollWidth` 取列宽 + `measureText` 取最长列高度。保持中心点不变，同步调整 `x`/`y`。最小宽度 30mm，最小高度 `fontSize+8`。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts#L458-L465)**：`applyCoverTemplate` 切换封面时，过滤旧封面元素：`cover-text-*`（模板预设文字）和 `cover-shape-*`（模板预设形状）不再保留，仅保留用户手动添加的元素（`text-*`/`shape-*` ID）。`applyBackCoverTemplate` 同样处理。
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L319-L330)**：编辑时（`isEditing`）Konva Text 顶部对齐（匹配 textarea 光标位置），非编辑时 `verticalAlign="middle"` 居中显示。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 文字工具居中修复 + 形状渐变功能

### 需求
1. 文字工具添加文字时，横向文字框高度过大，文字未在框内垂直居中（上下边距不对称）；竖向文字（春联模式）文字框宽度过大，文字未水平居中（左右边距不对称）。导致封面模板文字未按规范居中。
2. 形状工具添加形状后，需支持透明渐变填充。

### 实现
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**：文字元素初始高度从 `fontSize * 2.5` 改为 `fontSize + 12`，单行文字框高度更合理。形状属性面板新增"渐变填充"开关（checkbox），开启后从当前填充色创建 2-stop 渐变（填充色→透明），支持线性/径向两种渐变类型切换。
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：横向文字添加 `verticalAlign="middle"` + `height={ph}` 实现垂直居中。竖向文字（春联）新增水平居中计算——遍历所有字符节点求 `minX`/`maxX`，计算内容中心偏移量 `-contentCenter`，整体平移使文字在文本框内水平居中。
- **[ShapeNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ShapeNode.tsx)**：`ShapeGlyph` 新增渐变渲染支持——检测 `shape.gradient`，线性渐变用 `fillLinearGradient*` 属性（左上→右下），径向渐变用 `fillRadialGradient*` 属性（中心→边缘）。
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：新增 `GradientStop` 类型（`{ offset: number; color: string }`），`ShapeElement` 新增 `gradient?: GradientStop[]` 和 `gradientType?: 'linear' | 'radial'` 字段。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：`drawShape` 同步支持渐变填充——`createLinearGradient`/`createRadialGradient` 替代纯色 `fillStyle`。
- **[zh-CN.json / en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**：新增 `shapeGradient` / `shapeGradientLinear` / `shapeGradientRadial` 双语 key。

### 验证
- `tsc --noEmit` 通过，零错误。

---

## 2026-08-13 · 封面模板列表展示优化（折痕右移 / 间距 / 圆角）

### 需求
封面模板展示不像真实相册书：封面应完整一体（非分块），仅左侧翻页处有折痕阴影，圆角极微小，选中态用右上角淡绿色对勾角标替代紫色光晕。主页封面模板库效果需保持一致。

### 实现
- **[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)**：封面预览重构为精装硬壳相册实物效果——完整封面，核心特征为左侧 `8%` 处铰链压槽（hinge groove）：槽左 `1px` 亮边（凸起边缘反光）+ 槽右 `4%` 宽暗面漫射阴影（凹槽内阴影），铰链区（左边缘到压槽）极微弱渐变区分翻页区。`inset boxShadow` 模拟纸板厚度边缘。`rounded` 默认 `1px` 极微小圆角。底部 `radial-gradient` 落地投影。移除 `active` 参数。
- **[CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)**：`CoverTemplateCard` 移除紫色 ring 和 `active` 传参，选中态改为右上角淡绿色对勾角标（`rgba(82,196,132,0.85)` 背景 + SVG 白色对勾），卡片圆角 `rounded-[1px]`，选中阴影改为中性黑色。
- **[CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx)**：`rounded={2}`→`rounded={1}`，悬浮遮罩圆角 `rounded-[1px]`。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 封面两块式布局 + 书脊默认内容（logo 水印 / 相册名 / 日期）

### 需求
1. 照片编辑模式封面模板列表，展示真实物理感的封面正面（含折痕效果）。
2. 编辑器页面中封面呈现为两块：左侧书脊背面、右侧封面正面，中间有间隙；设置背景时书脊背面与页面为同一整体。
3. 页面添加封面后，书脊自动带 MemBook logo 水印 / 相册名 / 日期元素。

### 实现
- **[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)**：预览改为展示封面正面（铺满正方形），左侧渲染一条书脊折痕阴影（横向渐变 div），模拟真实装订物理感；移除书脊色带与偏移。
- **[constants.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/constants.ts)**：新增 `SPINE_GAP_MM = 6`（书脊背面与封面正面之间的间隙 mm）。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：封面页画布宽度计算加入书脊宽 + 间隙；封面用两块 `PageBackgroundRect`（书脊背面 + 封面正面，同一背景色）渲染，中间留间隙；加载主题对应 logo（light/dark），在书脊背面顶部渲染半透明 MemBook logo 水印。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：新增 `buildSpineElements`，为封面生成竖排（rotation -90）的相册名 / 日期文本元素（独立可编辑）；封面正面内容整体右移 `contentOffset = spineWidth + SPINE_GAP_MM`。
- **[pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts)**：封面页槽位坐标整体右移 `(spineWidth + SPINE_GAP_MM)`。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：`renderPage` 封面页逻辑宽度加入书脊宽 + 间隙，先用背景色铺满整块（书脊背面与页面同一整体），再渲染内容；封面页书脊背面顶部绘制半透明 MemBook logo 水印。打印引擎 `printEngine.ts` 复用 `renderPage`，已自动对齐。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 修复封面元素位置偏移 + 书脊底色可调 + 书脊配色优化

### 需求
1. 模板效果应用到页面后相对位置不准确（居中的没居中、有偏移）。
2. 书脊底色颜色可调整。
3. 空白封面、极简白书脊底色为纯白；其他模板书脊底色不要太深，应与封面页颜色一致或略有差异。

### 实现
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：修复 `presetTextToPageElements`/`presetShapeToPageElements` 的坐标转换 bug —— 原实现把「位置 x 的书脊偏移 `spineOffsetX`」错误地加到了 width/尺寸上，导致元素被放大且居中元素整体偏移 `1.5×spineOffsetX`。现拆分为 `posX`（位置，含偏移）与 `scale`（尺寸，不含偏移）。
- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：书脊底色调整——空白封面、极简白改为纯白 `#FFFFFF`；多图拼排（原深棕 `#3A3230`）改为浅色 `#E9E6DF`；经典蓝调（原深蓝 `#1F3A5F`）改为浅蓝 `#D3E0EC`，均与封面底色协调。
- **[PageSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageSettings.tsx)**：新增「书脊」设置区（仅当存在封面页时显示），提供书脊底色取色器 + 十六进制输入，确认后仅写入封面页 `spineColor`。
- **[zh-CN.json / en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**：新增 `pageSettings.spine` / `pageSettings.spineColor` 双语 key。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 封底不显示书脊

### 需求
封底不需要显示书脊，仅封面保留书脊。

### 实现
书脊逻辑由「封面/封底」统一改为「仅封面」：
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`applyBackCoverTemplate` 不再写入 `DEFAULT_SPINE_WIDTH_MM`，`spineWidth` 置 0，预设元素偏移改为 0。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：画布加宽判断改为仅 `isCoverPage`，移除未用的 `isBackCoverPage` 导入。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：`drawPage`/`pageExportWidthMm`/`renderPage` 的设计书脊判断改为仅 `isCoverPage`。
- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：移除 4 款封底模板的 `spineColor`，预览不再渲染封底书脊。
- 旧数据兼容：即使旧封底页残留 `spineWidth`，渲染逻辑已按 `isCoverPage` 判断，不会显示书脊。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 编辑器封面模板列表不显示名称

### 需求
照片编辑模式「封面」面板的模板列表不需要显示模板名称，仅显示预览图。

### 实现
- **[CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)**：移除 `CoverTemplateCard` 的名称展示，卡片仅保留预览图；移除 `getTemplateName` 导入与 `isZh` 计算。主页 [CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx) 的名称展示保留。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 编辑器布局模板列表改为正方形展示

### 需求
照片编辑器模式「布局」面板的模板列表目前为长方形（3:4）竖版展示，改为正方形（1:1）展示。

### 实现
- **[TemplatePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/TemplatePanel.tsx)**：`ITEM_ASPECT` 由 `3 / 4` 改为 `1 / 1`。模板预览内的槽位坐标为百分比，自动适配正方形容器。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 封面模板名双语化 + 编辑器封面列表优化

### 需求
1. 模板名按界面语言显示：中文模式显示中文名，英文模式显示英文名。
2. 编辑器封面模板列表左上角不再显示「封面/封底」角标；封面列表新增一款「空白封面」模板。

### 实现
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`Template` 新增可选 `nameZh`（中文名）。
- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：
  - 全部 8 款封面 + 4 款封底模板补充 `nameZh`（极简白/全幅旅行/日常家庭/多图拼排/恋恋故事/经典蓝调/明亮盛夏/年度回顾、静谧/合影留念/落款签名/竖排落款）。
  - 新增空白封面模板 `coverBlank`（id `cover-blank`，无槽位/无预设元素，纯白底 + 书脊）。
  - 新增 `getTemplateName(template, isZh)` 帮助函数按语言返回名称。
- **[CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx)**：卡片名称改用 `getTemplateName`，随语言切换。
- **[CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)**：移除封面/封底角标（badge），卡片改为「预览 + 双语名称」。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 封面/封底书脊改为物理页面扩展（可编辑区）

### 需求澄清
用户明确推翻「书脊色块」方案：封面/封底页的书脊不再作为装饰色块，而是**页面左侧物理扩展出的立体书脊侧面区**，用户可在其上添加文字、形状等元素；书脊要有立体（折痕/阴影）观感。

### 实现
将书脊从「装饰元素」重构为「物理页面扩展」，贯穿数据模型、编辑器、预览、导出、打印全链路：

- **[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)**：删除 `spineShape`/`spineText` 装饰元素与 `SPINE_WIDTH` 常量；槽位坐标重置为从 0% 起（不再含书脊偏移）；所有封面/封底模板新增 `spineColor` 属性（书脊底色，随模板基调）。
- **[types/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)**：`AlbumPage` 新增 `spineWidth`（书脊宽 mm）/`spineColor`；`Template` 新增 `spineColor`。
- **[constants.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/constants.ts)**：新增 `DEFAULT_SPINE_WIDTH_MM = 18`。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`presetTextToPageElements`/`presetShapeToPageElements` 增加 `spineOffsetX` 参数，正文元素整体右移书脊宽；`applyCoverTemplate`/`applyBackCoverTemplate` 写入 `spineWidth`/`spineColor`。
- **[pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts)**：`calcCoverOverrides` 槽位坐标右移书脊宽（mm→px）。
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：封面/封底页画布宽度扩展 `+spineWidth`；左侧渲染书脊色带 `spineColor` + 右侧折痕渐变阴影（立体效果，仅预览不写数据）；元素坐标系统已右移，正文正确落在封面区域。
- **[CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)**：移除 `spineShape`/`spineText`，渲染物理书脊左侧扩展 + 折痕阴影。
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)**：新增 `pageExportWidthMm()` 按页计算含书脊的导出宽度；`renderPage`/`exportToPDF`/`generatePdfBlob` 支持设计书脊（封面/封底左侧物理扩展 + 色带填充），PDF 逐页动态尺寸。

### 说明
- 书脊是页面左侧的物理扩展区，用户可直接在画布书脊范围内自由添加文字/形状元素。
- 折痕阴影与印刷质感仅存在于预览组件，不写入数据；Canvas/导出由 `spineWidth`/`spineColor` 数据驱动。
- 旧版封面迁移：书脊装饰元素被移除，槽位坐标重置为 0% 起。

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误。

---

## 2026-08-13 · 封面模板按 Mixbook 竞品截图重新设计（全部 8 款）

### 需求
用户提供两张 mixbook.com/all-photo-books 截图，要求按 Mixbook 招牌款式设计全部封面模板。截图展示了 Mixbook 的模板画廊，包含 Full Photo Travel、Minimal White、Everyday Modern Family、Multi Photo Layout、Couple's Story、Summer Coffee Table Book、Year in Review by EttaVee 等标志性款式。

### 实现
[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts) 全部 8 款封面按 Mixbook 竞品风格重新设计：

| ID | 模板名 | Mixbook 对标款 | 照片呈现 | 文字排版 | 装饰 |
|----|--------|--------------|---------|---------|------|
| cover-1 | Minimal White | Minimal White 极简款 | 居中单图 + 下方标题/年份 | 衬线小字居中 | 细分割线 |
| cover-2 | Full Photo Travel | Full Photo Travel 全幅旅行 | 全幅铺满 + 底部渐变蒙版 | 无衬线白色粗体左对齐 | 深色蒙版 |
| cover-3 | Everyday Family | Everyday Modern Family 家庭 | 全幅 + 底部蒙版 | 衬线白色居中标题 | 底部半透明蒙版 |
| cover-4 | Multi Photo | Multi Photo Layout 双图 | 双竖图并排 | 衬线居中标题 | 细分割线 |
| cover-5 | Couple's Story | Couple's Story 情侣 | 右 48% 照片 + 左米色面板 | 衬线左对齐 + 金线 | 米白面板 + 金色装饰线 |
| cover-6 | Classic Blue | Classic Blue 蓝底经典 | 居中图 + 金色边框 | 金色花体斜体标题 | 深蓝书脊 + 金色细边框 |
| cover-7 | Summer Bright | Summer Coffee Table 明黄 | 居中图 | 无衬线粗体粉字 + 大号年份 | 明黄底 + 白色圆形太阳 + 光芒线 |
| cover-8 | Year in Review | Year in Review by EttaVee | 居中图 | 无衬线粗体大字 | 顶部三色笔触条纹(粉/蓝/绿) |

关键设计对齐：
- **Full Photo Travel** 对齐：全幅照片 + 白色粗体左对齐 "EXPLORE / The World Around You" + 底部渐变蒙版
- **Minimal White** 对齐：纯白底 + 居中单图 + 下方衬线小字 + 年份
- **Couple's Story** 对齐：米色左面板（衬线标题 "THE STORY OF US"）+ 右 48% 照片
- **Summer Bright** 对齐：明黄底 #FFDF5C + 无衬线 Bold 粉字 #E13A6E + 白色太阳图形
- **Year in Review** 对齐：白底 + 顶部三色彩色笔触色条（粉/蓝/绿）+ 无衬线粗体标题
- **Classic Blue** 对齐：浅蓝底 #EAF1F8 + 金色 Georgia italic 花体标题 + 金色细边框
- 所有模板保留 9% 书脊色块 + 竖排英文，书脊底色随模板基调调整

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误

---

## 2026-08-13 · 封面模板全部重设计：明亮简约白基调 + 多照片/文字组合

### 需求
用户参考竞品截图风格，要求整体重新设计所有封面模板。确认关键设计维度：
- **主色调**：明亮简约白（减少原有深色模板）
- **照片呈现**：全幅铺满 / 居中局部 / 多图拼贴 / 小图点缀 —— 全部覆盖
- **文字排版**：衬线大字 / 无衬线现代 / 居中精致小字 / 竖排艺术字 —— 多种组合

### 实现
[cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts) 全部 8 款封面重写，保留 9% 书脊结构：

| ID | 模板名 | 照片呈现 | 文字排版 | 基调 |
|----|--------|---------|---------|------|
| cover-1 | Minimal White | 居中大图 | 衬线居中大字 | 纯白 |
| cover-2 | Full Bleed | 全幅铺满+底部蒙版 | 无衬线现代左对齐 | 浅灰底 |
| cover-3 | Classic Studio | 居中精致小图 | 衬线居中精致小字 | 米白 |
| cover-4 | Editorial | 左 50% 大图 | 竖排衬线标题 | 近白 |
| cover-5 | Collage | 三图拼贴 | 衬线标题底部 | 米白 |
| cover-6 | Modern Sans | 居中图 | 无衬线现代粗体 | 纯白 |
| cover-7 | Banner | 顶部横幅图 | 衬线居中标题 | 米白 |
| cover-8 | Vertical Serif | 小图+边框 | 衬线斜体艺术字 | 米白 |

关键调整：
- **明亮化**：背景统一为白/米白/浅灰（#FFFFFF ~ #FBFBFB），删除原 Minimal Black（深墨底）等深色模板，Full Photo 深色蒙版改为浅灰底 + 白字
- **无衬线字体**：新增 `SANS = 'Helvetica Neue', Arial, sans-serif`，用于 Modern Sans / Full Bleed 款，呼应当下流行现代风
- **保留书脊**：所有模板仍含左侧 9% 书脊色块 + 竖排 spineText，书脊底色随模板基调调整（浅色系）
- **装饰元素**：极细线（opacity 0.3-0.5）、圆形色块（ellipse）、透明边框（stroke）、渐变蒙版（mask）
- **封底**：backGold 深褐底改为 Signature 米白底（衬线落款+细线），其余封底保持明亮基调

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误

---

## 2026-08-13 · 封面体验优化：左面板排序 / 封面圆角独立设置 / 预览增强

### 需求
1. 相册编辑器左侧面板：封面功能位置调整到"布局"功能下方，并更换更贴合的封面 icon
2. 封面模板列表（CoverLibraryPanel）不再展示模板名称，仅保留预览图 + 角标
3. 页面设置弹窗增加"封面照片位圆角"独立设置，支持每个圆角单独调整（tl/tr/br/bl）
4. 页面设置逻辑隔离：仅在"修改页面尺寸"时封面/封底自适应，其他设置（边距/间距/圆角/辅助线）均不影响封面与封底
5. CoverPreview 预览效果更贴合 Mixbook：书脊折痕阴影 + 照片槽位印刷质感

### 实现

**1. 左侧面板排序与 icon** [LeftPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/LeftPanel.tsx)
- tabs 数组调整顺序：photos → templates → covers → stickers → tools（covers 由末尾移到 templates 之后）
- covers tab icon 改为带书脊竖线的书本图标（viewBox 20×20，左侧竖线 + 右侧小矩形照片位）
- [HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx) 主页导航封面 icon 同步更换

**2. CoverLibraryPanel 去名称** [CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)
- 模板卡片移除底部 `template.name` 文本，仅保留 CoverPreview 正方形预览 + 左上角分类角标
- 卡片更紧凑，预览图占满整个可点击区域

**3. 类型扩展：slotCornerRadius 支持每角单独** [index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)
- `AlbumPage.slotCornerRadius` 由 `number` 扩展为 `number | [number, number, number, number]`
  - `number` = 统一圆角（向后兼容）
  - `[tl, tr, br, bl]` = 每角单独（顺时针：左上→右上→右下→左下）
- 新增 `normalizeSlotCornerRadius(r)` 辅助函数：将联合类型归一化为单一数值（取四角平均值）
  - 用于缩略图/预览/SmartLayout 等不支持每角单独圆角的渲染场景
  - 编辑器 Canvas 与导出引擎走 `roundRect` 原生支持联合类型，无需归一化

**4. 页面设置 UI：封面圆角独立设置** [PageSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageSettings.tsx)
- 新增本地状态 `coverCorners: [number, number, number, number]` 与 `coverUniform: boolean`
- 新增 UI 区块"封面照片位圆角"，含统一/每角单独切换按钮：
  - 统一模式：单条滑块 + 数字输入（0-24px），四角联动
  - 每角模式：2×2 网格，四个角独立数字输入（TL/TR/BL/BR 标签）
- `handleConfirm` 中封面圆角独立应用：遍历所有 `isCoverOrBackCoverPage` 页面写入 `slotCornerRadius`，不受 `applyAll` 影响
- `handleReset` 重置封面圆角为 `[4,4,4,4]` 统一值
- 弹窗打开时从第一个封面/封底页读取当前圆角同步到本地状态（数组则检测是否四角相等自动切换统一/每角模式）

**5. 页面设置逻辑隔离：封面/封底不受边距/间距/圆角影响** [albumMetaSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/albumMetaSlice.ts)
- `batchPageSettings`：`applyAll` 分支循环跳过 `isCoverOrBackCoverPage(np[i])`；非 applyAll 分支检测当前页是否封面，封面页不写入 `slotCornerRadius`
- `setPageSlotCornerRadius`：`applyMarginToAll` 分支循环跳过封面/封底页
- 封面/封底仅在 `setAlbumSize`（修改尺寸）时自适应（slotOverrides 清除触发等比缩放 + dirty 重算）
- [pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts) 已对封面页走 `calcCoverOverrides` 分支（按模板百分比 × 整页像素，不应用 pageMargin）

**6. 导出引擎每角圆角渲染** [exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)
- `roundRect(ctx, x, y, w, h, r)` 签名扩展支持 `number | [number, number, number, number]`
  - number 时四角统一；数组时按 `[tl, tr, br, bl]` 分别绘制 quadraticCurveTo
- `drawPage` 中 `slotCornerRadius` 缩放逻辑同步处理数组类型（每角 × cornerScale）

**7. CoverPreview 预览增强** [CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)
- 新增书脊折痕阴影：在书脊 shape 右侧（书脊与封面正面交界处）叠加 3% 宽度的横向渐变 div（rgba(0,0,0,0.22) → 透明），模拟真实装订凹槽的立体感
- 照片槽位新增 `boxShadow: inset 0 0 0 1px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.18)`，增加印刷照片的微阴影质感
- 仅预览层叠加，不写入数据（实际 Canvas/导出仍由 shapeElements 数据驱动渲染）

**8. i18n 补齐** [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)
- `editor.pageSettings` 新增 key：`coverSlotRadius` / `uniform` / `perCorner` / `cornerTL` / `cornerTR` / `cornerBR` / `cornerBL`

### 类型修复
扩展 `slotCornerRadius` 联合类型后，5 处旧消费点报错，统一用 `normalizeSlotCornerRadius` 归一化：
- [PageSlotPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/PageSlotPreview.tsx) L38：缩略图圆角 × scale
- [PageSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageSettings.tsx) L69：弹窗打开时同步当前页圆角到本地 number 状态
- [SmartLayoutView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/SmartLayoutView.tsx) L1068：智能排版缩略图圆角缩放
- [thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts) L168：缩略图核心渲染（drawTemplateSlot / drawPlacement 均消费 number）

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过，零错误

---

## 2026-08-13 · 封面页坐标修复：slots 与预设元素坐标系不一致导致位置错乱

### 问题
封面页应用模板后，照片槽位与预设文字/形状位置错乱。根因：
- **slots 被 pageMargin 内缩**：`applyCoverTemplate` 调用 `calcMarginForPage` → `calcMarginOverrides`，把 slots 缩放到 pageMargin 安全区内
- **预设文字/形状不受 pageMargin 影响**：`presetTextToPageElements`/`presetShapeToPageElements` 用整页尺寸（mm）转换，渲染时 × MM_TO_PX = 整页像素
- 两者坐标系不一致（slots 在安全区内，预设元素贴整页边缘），导致书脊、标题、装饰线与照片位置错位

### 修复
[pageMarginService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageMarginService.ts) — 对封面/封底页做特殊处理：
- `calcMarginForPage` 检测 `isCoverPage(page) || isBackCoverPage(page)`，走 `calcCoverOverrides` 分支
- `calcCoverOverrides`：按模板百分比 × 整页像素（albumSize × MM_TO_PX）直接生成 slotOverrides，不应用 pageMargin
- 这样 slots 和预设文字/形状都在整页坐标系下，位置完全一致
- 所有 12 个 `calcMarginForPage` 调用点（albumMetaSlice 4 处 + pageSlice 6 处 + pageMarginService 2 处）自动受益，无需逐个修改

### 验证
- `npx tsc --noEmit` 通过

---

## 2026-08-13 · 封面功能重写：Mixbook 风格预设模板系统（含书脊 + 全英文文案）

### 需求
用户反馈封面设计感太 low，要求参考 Mixbook 竞品效果，正方形画布展示，含书脊侧边（可编辑），元素皆可移动调整，结合形状工具做出好看封面，文案改全英文显高级感。

### 方案
推翻之前的"智能封面生成引擎"方案（cover-generator/coverDecoration 已删除），改为**预设模板系统**：
- 每款模板自带预设文字 + 形状 + 背景色 + 书脊，应用即成型，用户只改照片和文字
- slots 与 preset 元素同为百分比坐标（0-100），自适应所有页面尺寸
- 所有元素应用后皆为独立可编辑元素（可拖动/改色/删改），可用工具栏形状工具继续扩展

### 实现

**1. 类型扩展** [index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)
- Template 新增 `presetTextElements` / `presetShapeElements` / `presetBackground` 字段
- 新增 `PresetTextElement`（含 placeholder: albumName|date|none）和 `PresetShapeElement` 类型
- 移除旧的 `CoverFields` 类型和 `coverFields` 字段

**2. 模板库重写** [cover-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/cover-templates.ts)
- 8 款封面：Minimal White / Full Photo / Editorial / Minimal Black / Collage / Elegant Serif / Classic Border / Travel Log
- 4 款封底：Quiet / Group Photo / Gold / Vertical
- 每款封面含**书脊**（左侧 9% rectangle shape + 竖排英文文字 rotation:-90）
- 全英文衬线文案（Georgia 字体，大字距），placeholder 仅用于 date 自动填充年份区间
- 装饰元素：极细线（1px opacity 0.3-0.6）、圆形色块（ellipse）、双层边框、渐变蒙版

**3. 应用逻辑** [pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)
- `applyCoverTemplate(templateId)` / `applyBackCoverTemplate(templateId)`：构造完整封面页（书脊+预设元素+照片槽位），已有封面则替换（保留已填照片）
- 辅助函数 `presetTextToPageElements`（百分比→mm，placeholder 替换）、`presetShapeToPageElements`（百分比→mm，中心点转换）、`deriveDateRange`（照片年份区间）

**4. 共享预览组件** [CoverPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CoverPreview.tsx)（新增）
- 正方形画布预览：书脊色块 + 装饰形状 + 照片槽位（picsum 真实图片占位）+ 蒙版 + 预设文字（横排/竖排 rotation 支持）
- 形状渲染支持 rectangle/ellipse/line/透明边框（fill 透明 + strokeWidth > 0 时渲染 border）
- 文字缩放因子 0.42（实际 pt → 预览 px），最小 5px
- 被 CoverLibraryPanel（编辑器）和 CoverGallery（主页）共用，保证两处预览一致

**5. 面板升级**
- [CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)：2 列网格，点击应用/切换模板
- [CoverGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CoverGallery.tsx)：3-5 列网格，点击新建相册

**6. 引用清理**
- [GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx)：`addCoverPage()`/`addBackCoverPage()` → `applyCoverTemplate('cover-1')`/`applyBackCoverTemplate('backcover-1')`
- [HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)：`handleCreateFromCover` 改用 `findCoverTemplateById`，先设置相册状态再 `applyCoverTemplate` 构造完整封面页，保存更新后的 pages

### 验证
- `npx tsc --noEmit` 通过，零错误
- 残留旧引用仅 i18n 文案 key（不影响功能）

---

## 2026-08-13 · 左侧面板标题去重 + 封面切换保留用户内容

### 需求
编辑器左侧面板（布局/封面/贴纸/工具）展开后顶部仍显示标题，与左侧 Tab 栏标签重复；封面面板上方有冗余文案说明；切换封面模板时用户已编辑的文字/形状丢失。

### 实现
- **[CoverLibraryPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/CoverLibraryPanel.tsx)**：移除顶部标题栏（`border-b` header）和提示文案（`onCoverPageHint`/`onNormalPageHint`）；`currentIsCover`/`currentIsBackCover` 简化为 `currentPage?.pageKind` 直接比对；移除未使用的 `isCoverPage`/`isBackCoverPage` 导入。
- **[TemplatePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/TemplatePanel.tsx)**：移除顶部标题栏（`border-b` header）。
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**：移除顶部标题栏（`border-b` header）。
- **[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)**：移除标题文字，保留上传按钮（`justify-end` 右对齐）。
- **[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)**：`applyCoverTemplate` 切换封面时保留旧封面用户编辑的文字（排除 `spine-text-*` 书脊自动生成元素）和形状，合并到新模板预设元素之后；`applyBackCoverTemplate` 同理保留封底用户文字/形状。

### 验证
- TypeScript 编译通过（0 错误）

---

## 2026-08-11 · 照片整理"加入相册"后照片数据缺失修复

### 需求
用户反馈：整理照片功能的"加入相册"后，照片在相册中导入异常，没有导入照片的数据（相册中照片无数据/无法加载）。

### 根因
`AlbumBridgeDialog` 的 direct 策略（引用原数据不复制）未校验照片是否有可引用的数据来源：
1. **library 模式 direct 分支**：保存照片时 `src: ''`、`blobId: p.blobId`，但**未设置 `relativePath`**。
   - 当原库照片是 direct 模式（无 blobId）时，`p.blobId` 为空，保存后的照片 `src=''`、`blobId=undefined`、`relativePath=undefined` → **完全没有任何数据来源**，相册中照片无法加载任何图像数据。
   - `photoToFileInfo` 将 library 模式 direct 照片的原文件相对路径放在 `path` 字段，但 `AlbumBridgeDialog` 未使用。
2. 任何模式下若照片既无文件路径也无 blobId，direct 策略都会保存出"空数据"照片。

### 修复
[AlbumBridgeDialog.tsx](src/components/home/organize/AlbumBridgeDialog.tsx)：
- **新增 direct 数据来源校验**：`hasDirectSource` 判断照片是否有可引用的数据来源（folder 模式看 `relativePath`/`path`；library 模式看 `blobId`/`path`）。无数据来源时**强制走 import 策略**（复制数据到相册库内），避免保存出无数据的照片。
- **library 模式 direct 分支补充 `relativePath: p.path`**：让 Tauri direct 模式的库内照片在相册侧能通过文件系统读取到数据。

### 验证
- `npm run typecheck` 通过。
- 顺带修复弹窗打开加载项目列表的 `useEffect` 中同步 setState 触发的 `react-hooks/set-state-in-effect` lint 报错（原代码即存在，阻塞 pre-commit 提交）。将状态重置与加载逻辑移入异步函数，行为不变。

---

## 2026-08-11 · 人脸识别聚类阶段 "Cannot set properties of undefined (setting '0')" 修复

### 背景
检测阶段正常完成，但**检测完成后**报错 `Cannot set properties of undefined (setting '0')`。用户反馈"检测过程中是正常在读取照片的"，说明错误不在检测阶段，而在**聚类阶段**。

### 根因
`agglomerativeCluster` 函数的距离矩阵初始化有 bug：

```typescript
// 错误代码：
const distMatrix: Float32Array[] = new Array(limitedN);
for (let i = 0; i < limitedN; i++) {
  distMatrix[i] = new Float32Array(limitedN);  // 只初始化 distMatrix[i]
  for (let j = i + 1; j < limitedN; j++) {
    distMatrix[i][j] = dist;  // ✓
    distMatrix[j][i] = dist;  // ✗ distMatrix[j] 还未初始化！
  }
}
```

当 `i=0, j=1` 时，`distMatrix[1]` 还是 `undefined`（因为外层循环还没到 i=1），执行 `distMatrix[1][0] = dist` 就是 `undefined[0] = dist` → `Cannot set properties of undefined (setting '0')`。

错误 `setting '0'` 中的 `'0'` 是索引 `i=0`，正好对应第一次外层循环。

之前没暴露是因为人脸检测一直失败，根本没走到聚类阶段。检测修复后，这个 bug 就显现出来了。

### 修复
将距离矩阵的初始化和填充分两个循环：
```typescript
// 先初始化所有行
for (let i = 0; i < limitedN; i++) {
  distMatrix[i] = new Float32Array(limitedN);
}
// 再填充距离值
for (let i = 0; i < limitedN; i++) {
  for (let j = i + 1; j < limitedN; j++) {
    distMatrix[i][j] = dist;
    distMatrix[j][i] = dist;  // 现在 distMatrix[j] 已初始化
  }
}
```

### 验证
- `npm run typecheck` 通过，零错误

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)

---

## 2026-08-11 · 人脸识别 70-80% 进度崩溃修复（内存泄漏 + 错误传播）

### 背景
切换到 @vladmandic/face-api 后，人脸识别仍在检测进度 70-80% 时报错 `Cannot set properties of undefined (setting 'o')`。从代码层面分析发现两个问题：

1. **内存泄漏**：`extractFaceDescriptors` 每次推理创建 Canvas + Image 对象，但推理完成后没有显式释放。处理 70+ 张照片后，累积的 Canvas/Image 内存压力导致 tfjs 内部对象变为 undefined，抛出 "Cannot set properties of undefined"。

2. **错误传播路径**：`onProgress` 回调在 for 循环的 try/catch 之外，如果回调抛出错误（如 React state 更新异常），错误会逃逸到 `detectFaces` 的顶层 try/catch，返回 `modelLoadFailed: true`。但更严重的是，如果错误在异步回调中抛出，可能不被任何 try/catch 捕获，直接到达 `handleStart` 的 catch 块，显示 "人脸识别失败：" toast。

### 修改内容

#### 1. 显式内存清理（根因修复）
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `extractFaceDescriptors` 的 finally 块中：
  - Canvas 释放：`canvas.width = 0; canvas.height = 0;` — 释放 GPU 纹理内存
  - Image 释放：`img.src = '';` — 解码图像数据
  - URL 释放：`URL.revokeObjectURL(url);` — 释放 Blob 引用

#### 2. descriptor 数据独立拷贝
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `detections.map` 中使用 `d.descriptor.slice(0)` 创建 Float32Array 独立拷贝
- 避免 descriptor 引用 tfjs 内部 tensor，tensor 被释放后 descriptor 仍可用

#### 3. onProgress 回调安全包装
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `onProgress?.()` 调用包装在 try/catch 中
- 防止 React state 更新异常逃逸到 `detectFaces` 顶层 catch

#### 4. TfModule 接口扩展
- 新增 `TfEngine` 接口（`startScope/endScope/state`）和 `engine()` 方法
- 新增 `tidy<T>()` 方法声明（备用，当前未使用）

### 验证
- `npm run typecheck` 通过，零错误

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)

---

## 2026-08-11 · 切换 face-api.js → @vladmandic/face-api 修复 Tauri 兼容性

### 背景
人脸识别功能在 Tauri WebView2 中持续报错 `Cannot set properties of undefined (setting 'o')`，经过多次修复（TF.js backend 初始化、CPU backend 强制、运算验证、API 修正）仍未解决。

根因：`face-api.js 0.22.2` 已停止维护（2020年最后更新），依赖的 `@tensorflow/tfjs-core 1.7.0` 在现代 WebView2 中有根本性兼容性问题，内部张量操作会崩溃。

### 解决方案
切换到社区维护的 fork `@vladmandic/face-api@1.7.15`：
- 内置新版 tfjs（4.x），无外部依赖，无版本冲突
- API 完全兼容（drop-in replacement）
- 模型文件格式兼容，无需更换

### 修改内容

#### 1. 包依赖切换
- 卸载 `face-api.js`，安装 `@vladmandic/face-api`
- `@vladmandic/face-api` 无 dependencies、无 peerDependencies（全部内置）

#### 2. face-cluster.ts 更新
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**:
  - 动态 import 路径 `'face-api.js'` → `'@vladmandic/face-api'`
  - 移除 `@ts-expect-error`（@vladmandic/face-api 有完整 TypeScript 类型）
  - 移除 `getTfCore` 函数和 `TfCoreLike` 接口（不再需要单独导入 tfjs-core）
  - `trySwitchToCpuBackend` 改为使用 `faceApiModule.tf`（@vladmandic/face-api 导出的 tf 命名空间）
  - 保留 backend 验证逻辑（三重检查：setBackend 返回值 + getBackend 实际值 + 运算验证）

#### 3. content-aware.ts 更新
- **[content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)**:
  - 动态 import 路径 `'face-api.js'` → `'@vladmandic/face-api'`
  - 移除 `@ts-ignore`（@vladmandic/face-api 有完整 TypeScript 类型）

### 验证
- `npm run typecheck` 通过，零错误
- CSP 设置兼容（`script-src` 已含 `'unsafe-eval' 'wasm-unsafe-eval'`，`connect-src` 已含 `blob:`）
- 模型文件兼容（manifest 格式未变）

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)
- [content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)
- package.json / package-lock.json

---

## 2026-08-11 · 人脸识别 backend 验证 API 调用错误修复

### 背景
上一次修复添加了 backend 运算验证，但调用方式错误：使用了 `tf.dataSync(tensor)`，而 tfjs-core 1.7.0 中 `dataSync` 是 **tensor 实例方法**（`tensor.dataSync()`），不是 `tf` 命名空间的方法。导致所有 backend 验证都失败，报错 "r.tf.dataSync is not a function"。

### 修改内容
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: 修复 backend 验证代码
  - 新增 `TfTensor` 接口声明 `dataSync(): TypedArray` 和 `dispose(): void`
  - 修正 `TfModule` 接口：`tensor1d/add` 返回 `TfTensor`，移除不存在的 `dataSync` 方法
  - 验证代码改为 `c.dataSync()`（tensor 实例方法），替代错误的 `mod.tf.dataSync(c)`
  - tensor 销毁改为 `a.dispose()` 实例方法，替代 `mod.tf.dispose(a)`

### 验证
- `npm run typecheck` 通过，零错误
- 确认 tfjs-core 1.7.0 的 `dataSync` 在 `tensor.d.ts` 第 366/590/595 行声明为实例方法

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)

---

## 2026-08-11 · 人脸识别 "模型加载失败" 错误诊断增强

### 背景
上一次修复（强制 CPU backend + 运算验证）后，用户仍看到"人脸识别模型加载失败"提示，但无法知道具体失败原因。根因是 `setBackend` 在 tfjs-core 1.7.0 中可能返回 `false`（初始化失败）而不抛错，之前的代码没检查返回值；且错误信息被笼统地显示为"模型加载失败"。

### 修改内容

#### 1. 检查 setBackend 返回值
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `setBackend` 返回 `Promise<boolean>`，`false` 表示初始化失败
- 改为循环尝试 `['cpu', 'webgl']`，检查返回值 + `getBackend()` 实际值 + 运算验证三重检查
- 每个候选 backend 的失败原因都被记录

#### 2. 错误信息透传到 UI
- **[types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/types.ts)**: `FaceDetectionResult` 新增 `loadErrorMessage?: string` 字段
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: 新增 `lastLoadError` 模块变量缓存失败原因，`detectFaces` 返回时携带
- **[FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)**: 模型失败时显示具体错误（如"所有 TF.js backend 均不可用。最后错误: ..."），而非笼统的"模型加载失败"

#### 3. i18n 新增 key
- **[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**: 新增 `toastModelFailedDetail`: "人脸识别初始化失败：{{message}}"
- **[en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)**: 同步英文翻译

### 验证
- `npm run typecheck` 通过，零错误

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)
- [types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/types.ts)
- [FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)
- [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)
- [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)

---

## 2026-08-11 · 人脸识别 "Cannot set properties of undefined (setting '0')" 崩溃修复

### 背景
上一次修复（初始化 TF.js backend）后，人脸识别仍然报错 `Cannot set properties of undefined (setting '0')` 并导致 React 崩溃。根因是 Tauri WebView2 的 WebGL 实现不可靠 — `tf.ready()` 只验证 backend 注册状态，不验证实际 GPU 运算，导致 WebGL backend 通过初始化检查但在张量运算时崩溃。

### 修改内容

#### 1. 强制 CPU backend + 运算验证
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: 改为**优先 CPU backend**（之前优先 WebGL）
- 新增 backend 运算验证：`tf.tensor1d([1,2,3]) + tf.tensor1d([4,5,6])` → `dataSync` → 检查结果 `[5,7,9]`
- 验证失败时自动降级到 CPU backend
- CPU backend 验证也失败则直接返回 `modelLoadFailed`

#### 2. Canvas 中转 + 尺寸限制
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `extractFaceDescriptors` 改为先将图片绘制到 Canvas 再传给 `detectAllFaces`
  - 避免 face-api.js 直接处理 HTMLImageElement 的跨域/tainted canvas 问题
  - 限制输入图片最大尺寸 1600px，防止超大图 WebGL 纹理溢出或 OOM
  - 超大图自动等比缩放，人脸坐标按缩放后 Canvas 尺寸归一化

#### 3. TfModule 接口扩展
- 新增 `tensor1d` / `add` / `dataSync` / `dispose` 类型声明，支持 backend 验证运算

### 验证
- `npm run typecheck` 通过，零错误

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)

---

## 2026-08-11 · 人脸识别 "Cannot set properties of undefined" 修复

### 背景
用户反馈开始人脸识别后报错："人脸识别失败:Cannot set properties of undefined (setting')"。根因是 face-api.js 0.22.2 内部使用 TensorFlow.js，但**未初始化 TF.js backend** 就直接调用 `loadFromUri` / `detectAllFaces`，导致 `loadWeightMap` 内部写入 undefined tensor 抛出此错误。

### 修改内容

#### 1. TF.js backend 初始化（根因修复）
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `loadFaceApiForClustering` 中在模型加载前增加：
  - `await mod.tf.setBackend('webgl')` — 优先使用 WebGL 加速
  - 失败时回退 `await mod.tf.setBackend('cpu')`
  - `await mod.tf.ready()` — 确保 backend 完全就绪
- 新增 `TfModule` 接口声明 `tf` 命名空间类型

#### 2. 模型加载细粒度错误追踪
- 三个模型（tinyFaceDetector / faceLandmark68Net / faceRecognitionNet）各自独立 try/catch
- 错误消息区分具体哪个模型失败（如"加载 TinyFaceDetector 模型失败: ..."）

#### 3. detectFaces 顶层 try/catch 兜底
- `detectFaces` 函数体包裹顶层 try/catch
- 未预期错误（非 AbortError）转为 `modelLoadFailed: true` 返回，不再向上抛异常
- UI 显示 `toastModelFailed`（明确提示模型加载失败）而非 `toastFailed`（笼统的"人脸识别失败"）

### 验证
- `npm run typecheck` 通过，零错误
- `mod.tf` 已确认存在于 face-api.js 0.22.2 的导出（`export { draw, utils, tf }`）

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)

---

## 2026-08-11 · 人脸识别多人物分类修复 + 聚类算法重构 + UI 增强

### 背景
用户反馈人脸识别功能始终只分出一个人脸组，无法识别多个人物进行分类。经分析，根因是聚类算法使用 single linkage（单链接）导致链式合并：A~B、B~C 会合并成 {A,B,C}，即使 A 和 C 差异很大。同时余弦相似度在 face-api.js L2 归一化 descriptor 上的区分度不如欧氏距离。

### 修改内容

#### 1. 聚类算法重构：single linkage → complete linkage + 欧氏距离
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: 用 `euclideanDistance` 替代余弦相似度（与 face-api.js FaceMatcher 一致，范围 [0,2]，默认阈值 0.6）
- 实现 complete linkage 层次聚类：两簇合并当且仅当跨簇所有 face pair 的最大距离 ≤ 阈值，彻底防止链式合并
- 算法：预计算距离矩阵 → 收集所有 pair 按距离升序排序 → 逐对尝试合并（complete linkage 检查跨簇最大距离）
- 新增距离矩阵统计日志（min/max/avg），便于诊断检测与聚类质量

#### 2. 检测与聚类分离（支持调阈值即时重聚类）
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: 拆分为 `detectFaces`（只提取 descriptor）+ `recluster`（纯聚类，毫秒级）
- 新增 `FaceDetectionResult` 类型缓存中间结果，调阈值时复用检测结果无需重新扫描
- `clusterFaces` 保留为兼容入口（检测 + 聚类一体化）

#### 3. UI 增强：合并组 + 组命名 + 模型失败提示
- **[FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)**:
  - 阈值滑块变化自动重聚类（useEffect 监听 threshold，复用 detection 缓存）
  - 新增聚类组合并功能：勾选多个组的复选框 → 合并按钮 → 合并 faces 和 photos
  - 新增组命名功能：点击组名编辑 → 输入名称（如"妈妈"）→ Enter 确认
  - 模型加载失败时显示明确 toast（`toastModelFailed`），不再误报"未检测到人脸"
  - 阈值标签从"相似度阈值"改为"距离阈值"，hint 说明改为"值越小分越细，值越大合越多"
  - 检测按钮文案区分首次/重测（"开始识别" / "重新检测"）

#### 4. i18n 双语同步
- **[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)**: 新增 `redetect`/`statFailed`/`toastModelFailed`/`toastMerged`/`selectForMerge`/`mergeSelected`/`confirmMerge`/`cancelMerge`/`namePlaceholder` 共 9 个 key
- **[en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)**: 同步全部英文翻译，threshold 改为 "Distance Threshold"

#### 5. 代码规范修复
- 移除 `modelLoadFailed` 模块级冗余变量（信息已通过 `FaceDetectionResult` 返回值传递）
- 移除 `selectedIdsArr`/`repPhoto`/`repFace` 未使用变量
- `// @ts-ignore` 改为 `// @ts-expect-error - face-api.js 为可选依赖，未安装时无类型声明`

### 验证
- `npm run typecheck` 通过，零错误
- 模型文件已确认存在：tiny_face_detector / face_landmark_68 / face_recognition_model
- complete linkage 算法正确性验证：A~B(0.3) + B~C(0.5) + A~C(0.7) + threshold=0.6 → 正确分出 {A,B} 和 {C} 两组（single linkage 会错误合并为 1 组）

### 涉及文件
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)
- [FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)
- [types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/types.ts)
- [index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/index.ts)
- [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)
- [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)

---

## 2026-08-11 · 照片整理工具状态指示 + 结果保留 + UI 一致性优化

### 背景
用户反馈照片整理功能存在以下问题：
1. 智能整理工具（人脸识别/照片去重等）开始扫描后，侧栏按钮无状态指示，切换到其他功能再回来结果被取消
2. "加入相册"按钮位置写死，部分不需要此功能的工具也显示
3. 浏览查看（时间线/日历）的 UI 样式与其他色块工具不一致

### 修改内容

#### 1. ToolSidebar 按钮状态指示器
- **[ToolSidebar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ToolSidebar.tsx)**: 新增 `ToolStatus` 类型（`'idle' | 'running' | 'done'`）和 `toolStatuses` prop
- 按钮右上角显示状态徽章：running 蓝色旋转动画 / done 绿色勾选（仅非活跃工具显示）
- 描述文字动态替换：running 显示"扫描中..." / done 显示"已完成，点击查看" / idle 显示原始描述

#### 2. 工具状态追踪（OrganizePanel）
- **[OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)**: 新增 `toolStatuses: Map<ToolId, ToolStatus>` 状态
- 扩展 `handleBusyChange`：busy=true → status='running'，busy=false 且之前是 'running' → status='done'
- 切换标签时重置 `toolStatuses`（不同标签的照片集不同，旧结果无效）

#### 3. 工具结果跨切换保留（visitedTools + hidden 模式）
- **[OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)**: 新增 `visitedTools: Set<ToolId>` 追踪已访问工具
- 用 `hidden` CSS 类隐藏非活跃工具（替代原 useMemo switch + key 强制 unmount 方式）
- 懒挂载：首次切换到某工具才渲染，之后保持挂载以保留扫描结果
- 切换标签时重置 visitedTools（key 仍含 activeTabId，确保不同标签数据隔离）
- 每个工具有独立滚动容器，滚动位置互不干扰

#### 4. "加入相册"按钮按需显示
- **[OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)**: 仅 `activeTool === 'timeline' || 'calendar'` 时显示全局"加入相册"浮动按钮
- FaceClusterTool 保留内部独立的"加入相册"按钮（使用自己的 selectedIds）
- 其他工具（去重/归类/EXIF/重命名/转换）不显示"加入相册"按钮

#### 5. Timeline/Calendar 包裹 ToolCard 统一样式
- **[OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)**: TimelineView 和 CalendarView 用 `<ToolCard>` 包裹，与其他工具（DedupeTool/FaceClusterTool 等）的色块标题+图标样式一致
- 两者使用 blue 色系（与 ToolSidebar 中的定义一致）

### 验证
- `npm run typecheck` 通过，零错误

### 涉及文件
- [ToolSidebar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ToolSidebar.tsx)
- [OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)

---

## 2026-08-11 · 照片整理代码漏洞修复与优化

### 背景
综合分析照片整理功能代码后，发现多项内存安全、错误处理、并发控制、数据完整性等方面的漏洞与逻辑问题，需按优先级修复优化。

### 修改内容

#### P0: fullDataCache 内存上限 + 人脸矩阵上限检查
- **[hash.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/hash.ts)**: fullDataCache 添加 LRU 上限 200 项，防止 Phase 3 缓存大量 ArrayBuffer 导致 OOM；Phase 4 完成后主动 `clear()`
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `agglomerativeCluster` 添加硬限制 MAX_FACES=5000，防止 O(n²) 相似度矩阵导致内存溢出；所有 `n` 引用改为 `limitedN`

#### P1: EXIF 静默吞异常 + JPG 重编码误杀修复
- **[exif.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/exif.ts)**: `readExifDate`/`readExifFull`/`readExifGps` 的空 catch 块添加 `logger.warn` 日志，EXIF 解析失败不再静默
- **[livp-converter.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/livp-converter.ts)**: JPG 重编码路径（`convertToJpg` 中 `.jpg/.jpeg` 分支）先读取原始 EXIF 再重编码后写回，避免误杀原始 EXIF 数据

#### P1: 人脸失败照片统计 + thumbCache 清理
- **[types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/types.ts)**: `FaceClusterResult` 新增 `failedPhotos` 字段
- **[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)**: `clusterFaces` 增加 `failedCount` 跟踪，异常路径 `failedCount++`；返回对象包含 `failedPhotos`
- **[FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)**: 统计卡片新增"失败"红色计数卡；`StatCard` 支持 `red` 颜色
- **[thumbCache.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/thumbCache.ts)**: 新增 `evictFromCache(photoId)` 导出，按 photoId 清理所有尺寸的缓存条目 + HEIC 转换缓存
- **[DedupeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/DedupeTool.tsx)**: library 模式删除照片时调用 `evictFromCache(f.id)` 清理 thumbCache

#### P1: organize.ts TOCTOU + 冲突循环 + 路径分隔符
- **[organize.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/organize.ts)**: TOCTOU 修复——rename 失败后验证目标文件是否存在（而非源文件是否不在），避免竞态条件误判；catch 块中重新计算 `finalTargetPath` 解决作用域问题

#### P1: rename.ts geocode 限流 + 路径分隔符
- **[rename.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/rename.ts)**: geocode 缓存增加 `inflightGeocode` 并发去重 Map，相同 GPS 的并发请求仅发起一次
- **[organize.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/organize.ts)**: `previewOrganize` 的 geocode 同样增加 `inflightGeocode` 并发去重

#### P2: 删除撤销 toast
- **[DedupeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/DedupeTool.tsx)**: 新增 `lastDeleted` 状态保存删除前的照片列表；删除成功后显示撤销栏（倒计时 5 秒自动消失）；点击"撤销"通过 `onPhotosUpdate` 恢复照片

#### P2: 批量失败重试机制
- **[organize.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/organize.ts)**: `executeOrganize` 主循环结束后，对失败项（源文件仍存在）重试一次
- **[rename.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/rename.ts)**: `executeRename` 主循环结束后，对失败项（源文件仍存在）重试一次

### 验证
- `npm run typecheck` 通过，零错误

### 涉及文件
- [hash.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/hash.ts)
- [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)
- [exif.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/exif.ts)
- [livp-converter.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/livp-converter.ts)
- [organize.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/organize.ts)
- [rename.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/rename.ts)
- [types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/types.ts)
- [FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)
- [DedupeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/DedupeTool.tsx)
- [thumbCache.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/thumbCache.ts)

---

## 2026-08-10 · 照片整理整体 UI 视觉优化（色块化重构）

### 背景
用户反馈照片整理功能存在 UI 设计问题：工作区出现双层标题框（外层 header 与内层 ToolCard 重复显示标题/描述）、日历格子有过多独立 border 导致视觉碎片化、时间线分组卡片各自独立缺乏整体感。需在保持功能不变的前提下进行全面 UI 视觉优化。

### 修改内容

#### 1. 工作区去掉外层标题框（OrganizePanel.tsx）
- 删除外层 `activeToolMeta` header 栏（标题+描述+加入相册按钮），消除与内层 ToolCard 的重复标题
- "加入相册"按钮改为 `absolute top-2 right-3 z-20` 浮动定位，固定在工作区右上角
- 工作区内容容器去掉 `bg-white border rounded` 包裹，改为纯 `overflow-y-auto` 无边框容器
- 由内部 ToolCard（自带色块标题+图标+描述）提供唯一视觉标题
- 清理未使用的 `activeToolMeta` useMemo 和 `TOOL_LIST` import

#### 2. 日历色块化重构（CalendarView.tsx）
- 整个日历网格包裹在一个 `rounded-xl bg-[var(--color-surface-panel)] overflow-hidden` 容器中，形成统一色块
- 日期格子去掉所有 `border`，改用纯背景色区分状态：有照片→白底+hover 品牌色微染；无照片→白底；非当月→灰底+透明；异常→红底；选中→品牌色底+inset ring
- 格子间距用 `gap-px bg-[var(--color-border)]/30` 实现细线分隔（背景透出形成网格线），替代独立 border
- 有照片的日期数字改为品牌色加粗，增强视觉指引
- 星期标题行区分周末（灰色）和工作日（次级文字色）
- 月份导航按钮去掉 border，改为纯 hover 背景效果
- 选中日期展开区从 `border` 改为 `bg-[var(--color-surface-panel)]` 色块
- 格子高度从 `min-h-[68px]` 微调至 `min-h-[64px]`

#### 3. 时间线色块化（TimelineView.tsx）
- 分组列表整体包裹在 `rounded-xl bg-[var(--color-surface-panel)] overflow-hidden` 容器中
- 分组间用 `gap-px bg-[var(--color-border)]/30` 形成细线分隔，替代独立 `rounded-xl border` 卡片
- 每个分组改为 `bg-white`（异常分组 `bg-red-50/60`，未知日期 `bg-amber-50/60`），无独立 border
- 分组头 padding 从 `py-3` 收紧至 `py-2.5`，标题字号统一为 `text-sm`
- 全选/取消全选按钮去掉 `border`，改用 `bg-[var(--color-surface-panel)]` 色块风格
- "在日历中查看"按钮同样去掉 border
- 分组模式切换器背景从 `bg-[var(--color-gray-100)]` 改为 `bg-[var(--color-surface-panel)]` 统一色系
- 缩略图网格区域加 `bg-white` 确保与头部色块区分

#### 4. 侧边栏紧凑化（ToolSidebar.tsx）
- 宽度从 `w-[220px]` 收窄至 `w-[210px]`
- 分类间距从 `gap-4` 收紧至 `gap-3`
- 工具按钮间距从 `space-y-1` 收紧至 `space-y-0.5`

### 验证
- `npm run typecheck` 通过（0 errors）
- 需手动执行 `npm run desktop:build` 构建验证

### 涉及文件
- [OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)
- [CalendarView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/CalendarView.tsx)
- [TimelineView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/TimelineView.tsx)
- [ToolSidebar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ToolSidebar.tsx)

---

## 2026-08-10 · 自定义模板分组筛选 + 多选缩放漏白修复 + 窗口位置调整

### 需求
1. 布局模板：用户自定义创建的模板也要分组筛选，编辑模式-布局下也要支持选择使用
2. Ctrl快捷键多选和鼠标框选功能统一；框选多个照片位缩放时照片漏白，需进入照片编辑模式拖拽后才正常
3. 重排照片、微调排版的窗口打开位置往下移动40px

### 修复内容

#### 1. 编辑模式布局面板新增分类筛选器（[TemplatePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/TemplatePanel.tsx)）
- 新增 `CategoryFilter` 类型（'all' | 'classic' | 'creative' | 'personality' | 'custom'）
- 新增分类筛选器 UI（Chip 风格，与主页 TemplateGallery 一致）
- 选中 'custom' 时仅显示自定义模板分组，其他分类同理
- i18n 新增 `catAll`/`catClassic`/`catCreative`/`catPersonality`/`catCustom` 键（中英文）
- 自定义模板在编辑模式-布局下已支持选择使用（IndexedDB 加载 + registerCustomTemplates + findTemplateById）

#### 2. 多选缩放实时预览阶段漏白修复（[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx) + [Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）
- **根因**：实时预览阶段（handleMove），`computeScaledSlots` 只更新槽位几何到 `previewRectMap`，但 `CanvasPhotoRenderer` 仍使用旧槽位尺寸的 `panX/panY`。当槽位尺寸变化时，cover-fit 重新计算了正确的 boundingW/boundingH，但 panX/panY 不匹配新尺寸，导致照片无法铺满新槽位出现漏白。提交阶段（commitPreview）的 `computePanForResizedSlot` 修复了最终值，但拖拽过程中仍然漏白。
- **修复**：`CanvasPhotoRenderer` 新增 `ignoreStoredPan` prop，为 true 时使用 `defaultPx/defaultPy`（居中位置）而非存储的 `rawPanX/rawPanY`。`Canvas.tsx` 在 `mePreviewRect` 存在时传递 `ignoreStoredPan={true}`，确保拖拽过程中照片始终铺满新槽位。提交时由 `computePanForResizedSlot` 计算最终正确的 pan 值。
- **多选/框选一致性**：经确认已统一为 `multiSelectedElements` 同一套逻辑（Ctrl+click 和框选都写入同一状态，由 `useMultiElementGroupSelect` 统一处理包围盒/缩放/移动），悬浮工具栏在 `multiSelectedElements.length >= 2` 且同类型时显示。

#### 3. 重排照片/微调排版窗口位置下移40px（[PhotoReorderDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoReorderDialog.tsx) + [LayoutAdjustPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/LayoutAdjustPanel.tsx)）
- `top: 112` → `top: 152`（两个窗口同步调整）

### 验收
- typecheck: 0 错误
- 编辑模式布局面板支持分类筛选（含自定义模板）
- 多选缩放拖拽过程中照片不再漏白
- 重排照片/微调排版窗口打开位置下移40px

---

## 2026-08-10 · 照片整理 HEIC 格式支持修复（去重/相册联动/人脸识别）

### 需求
修复用户反馈的 3 个 HEIC 格式相关问题：
1. 照片去重功能 HEIC 格式照片显示异常，无法正常显示
2. 照片整理添加到相册的照片，相册照片列表加载失败（HEIC 文件路径无法被浏览器解码）
3. 人脸识别功能只识别一个人物，没有体现多人物分类效果

### 问题根因分析

#### 根因：浏览器无法原生解码 HEIC 格式
HEIC（High Efficiency Image Container）是 Apple 推出的高效图像格式，Chrome/Firefox/Edge 等非 Safari 浏览器无法原生解码。项目已有 `heic2any` 库和 `ensureSupportedFormat` 转换函数（含 Tauri Rust WIC/libheif 路径 + WASM 兜底），但以下三处未调用转换：

1. **thumbCache.ts 的 `generateThumb`**：直接用 HEIC 二进制创建 blob，`createImageBitmap` 和 `Image` 都无法解码，导致缩略图加载失败。影响所有整理工具（去重/相似分析/时间线/日历/人脸识别）。
2. **AlbumBridgeDialog 的 direct 模式**：HEIC 文件路径保存到 `photo.relativePath`，相册页面通过 `usePhotoSrc` → `makeDirectPhotoUrl` → `asset://` 协议加载，但浏览器无法解码 HEIC 的 asset URL，导致照片列表全部加载失败。
3. **face-cluster.ts 的 `loadImageFromData`**：硬编码 `image/jpeg` 创建 blob，HEIC 文件无法被 `Image` 加载，导致 face-api.js 检测失败，所有 HEIC 照片进入 `noFacePhotos`，只有少数非 HEIC 照片能识别，表现为"只有一个人物"。

### 实现内容

#### 一、thumbCache.ts 支持 HEIC 转换 [thumbCache.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/thumbCache.ts)
- 新增 `heicConvertedCache`（Map<photoId, Blob>）：缓存 HEIC 转 JPEG 结果，避免同一照片不同尺寸重复转换
- `generateThumb` 函数检测 `isHeicFile(photo.name)`，是则调用 `ensureSupportedFormat` 转换为 JPEG blob
- `clearThumbCache` 同步清理 `heicConvertedCache`
- 影响：去重、相似分析、时间线、日历、人脸识别所有工具的 HEIC 缩略图都能正常显示

#### 二、AlbumBridgeDialog HEIC 强制走 import 模式 [AlbumBridgeDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/AlbumBridgeDialog.tsx)
- 新增 `effectiveStrategy`：HEIC 文件强制走 `import` 模式（即使用户选 `direct`），因为 direct 模式的 `asset://` URL 无法解码 HEIC
- import 模式下 HEIC 文件通过 `ensureSupportedFormat` 转换为 JPEG，再由 `importPhotoToDB` 生成 thumbBlobId/previewBlobId/originalBlobId
- 相册照片列表通过 `usePhotoSrc` 从 IndexedDB 读取 JPEG blob，正常显示
- direct 模式下获取尺寸时也支持 HEIC：先 `ensureSupportedFormat` 转换再 `createImageBitmap`
- 保留原文件名（`p.name`），便于用户识别，即使扩展名变为 .jpg

#### 三、face-cluster.ts loadImageFromData 支持 HEIC [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)
- `loadImageFromData` 新增 `photoName` 参数，用于判断是否为 HEIC
- HEIC 文件调用 `ensureSupportedFormat` 转换为 JPEG blob，再创建 Image
- `extractFaceDescriptors` 调用时传入 `photo.name`
- 修复后所有 HEIC 照片都能被 face-api.js 检测，多人物分类效果自然体现

#### 四、FaceClusterTool UI 优化 [FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)
- 分组头显示"人脸数"信息（当 `cluster.faces.length > cluster.photoCount` 时显示"N 个人脸"）
- 让用户更直观看到一组中包含多个人脸（如一张合照中识别到多个人）

### 关键决策
1. **HEIC 缓存按 photoId**：`heicConvertedCache` 用 photoId 作 key，同一 HEIC 照片请求 tiny/small/medium 多个尺寸时只转换一次，复用同一 JPEG blob
2. **AlbumBridgeDialog 强制 import 模式**：direct 模式下 HEIC 的 `asset://` URL 无法解码，必须走 import 模式生成 JPEG blob。虽然多一次 IO 和转换，但保证相册照片列表能正常显示
3. **保留原文件名**：HEIC 转 JPEG 后扩展名变为 .jpg，但 `p.name` 保留原名（如 `IMG_1234.HEIC`），便于用户识别
4. **不改聚类算法和阈值**：single linkage 层次聚类 + 0.6 阈值是 face-api.js 推荐值，算法正确。问题根因是 HEIC 导致识别失败，修复后多人物分类自然工作

### 验证
- `npm run typecheck` 通过
- 需用户手动执行 `npm run desktop:build` 构建验证桌面应用效果

---

## 2026-08-10 · 照片整理 6 项修复优化（缩略图压扁/红叉/紫色色块/日历重复按钮/相册尺寸缺失/DedupeTool不显示）

### 需求
修复用户反馈的 6 个问题：
1. 加入相册后的照片在相册页面显示被压扁
2. 照片相似分析缩略图压扁 + 选中删除应用红叉（不是红勾）
3. 照片去重功能照片不显示
4. 时间线月份列表上方有两个紫色色块（无实际效果）+ 部分照片未加载
5. 日历有两个加入相册按钮 + 时间标题不居中 + 月份列表照片未加载 + 日历表格照片太小
6. 照片整理功能缩略图压扁

### 问题根因分析

#### 根因 1：thumbCache.ts 的 createImageBitmap 压扁图片（影响所有缩略图）
`createImageBitmap(blob, { resizeWidth: 256, resizeHeight: 256 })` 同时设置 `resizeWidth` 和 `resizeHeight` 为相同值，会把 4000×3000 的照片强制缩成 256×256 正方形，**非正方形图片全部被压扁**。这是缩略图压扁的根本原因，影响相似分析、去重、时间线、日历、人脸识别所有工具。

#### 根因 2：AlbumBridgeDialog 创建的照片 width/height 为 0（影响相册页面）
`PhotoFileInfo.width` 和 `height` 是可选字段，folder 模式扫描时可能为 `undefined`。AlbumBridgeDialog 用 `p.width || 0` 创建 Photo 对象，当 width/height=0 时，CanvasPhotoRenderer 的 `valid = photo.width > 0 && photo.height > 0` 为 false，回退到 `{ imgW: slotW, imgH: slotH }`，Konva.Image 用槽位尺寸显式拉伸图像（等效 object-fit: fill），导致照片在相册画布上被压扁。

#### 根因 3：IntersectionObserver rootMargin 太小（影响部分照片未加载）
原来 `rootMargin: '200px'` 预加载范围不够，在快速滚动时部分照片未触发懒加载。同时失败后无重试机制，一次失败永久显示失败占位。

### 实现内容

#### 一、修复 thumbCache.ts 缩略图压扁 [thumbCache.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/thumbCache.ts)
- **修改前**：`createImageBitmap(blob, { resizeWidth: targetDim, resizeHeight: targetDim })` — 强制正方形
- **修改后**：`createImageBitmap(blob)` 不设 resize 选项，先解码获取原始宽高，再用 `Math.min(dim/w, dim/h, 1)` 计算等比缩放尺寸，Canvas drawImage 到正确尺寸
- fallback 路径（Image+Canvas）本来就正确，无需修改

#### 二、修复 AlbumBridgeDialog 照片尺寸缺失 [AlbumBridgeDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/AlbumBridgeDialog.tsx)
- direct 模式下当 `p.width || p.height` 为 0 时，通过 `readPhotoData` + `createImageBitmap` 读取实际尺寸
- `relativePath` 从 `p.path`（绝对路径）改为 `p.relativePath || p.path`（优先用相对路径）
- progress message 从 `strategy === 'import' ? p.name : undefined` 改为始终显示 `p.name`（direct 模式也需要读取数据获取尺寸，有耗时）

#### 三、SimilarTool 选中删除改为红叉 [SimilarTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/SimilarTool.tsx)
- 标记删除徽章从 `✓`（对勾）改为 SVG `X`（红叉）
- 删除 `textShadow` 样式，改用 `strokeWidth="2.5"` 的 SVG path `M4 4l8 8M12 4l-8 8`

#### 四、TimelineView 删除紫色高度条 [TimelineView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/TimelineView.tsx)
- 删除月份照片量高度条（`groupMode === 'month'` 时的紫色柱状图，第 516-539 行）
- 删除不再使用的 `maxCount` useMemo
- 保留总照片数统计徽章（淡紫底色，有实际功能）

#### 五、CalendarView 修复 [CalendarView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/CalendarView.tsx)
- **删除重复加入相册按钮**：删除 CalendarView 内部的"加入相册"按钮（第 309-326 行），保留 OrganizePanel 视图切换栏的固定按钮
- 删除 `handleAddToAlbum`、`hasSelection`、`onAddToAlbum` prop（不再需要）
- OrganizePanel 调用 CalendarView 时移除 `onAddToAlbum` 传参
- **时间标题居中**：月份切换行从 `justify-between`（左组+右按钮）改为 `flex`（prev + flex-1 justify-center 标题 + next），标题在两个按钮之间居中
- **增大日历表格照片**：StackedThumb 从 `w-7 h-7`（28px）改为 `w-10 h-10`（40px），每格显示从 3 张改为 2 张，格子最小高度从 72px 改为 92px

#### 六、ThumbImage 失败重试 + 增大预加载范围 [shared.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/shared.tsx)
- IntersectionObserver `rootMargin` 从 `200px` 增大到 `400px`，扩大预加载范围
- 添加失败重试机制：加载失败后自动重试最多 2 次（间隔 500ms），避免一次失败永久显示失败占位
- `retryCount` state 触发 useEffect 重新执行 `getThumbUrl`

### 关键决策
1. **createImageBitmap 不设 resize 选项**：原生 resize 会强制目标尺寸（压扁），改为先解码原图再 Canvas 等比缩放。虽然多一步 Canvas 操作，但保证宽高比正确
2. **AlbumBridgeDialog direct 模式读取数据获取尺寸**：虽然多一次 IO，但只在 width/height 缺失时执行，且 createImageBitmap 解码很快。避免照片在相册画布上被拉伸
3. **日历每格 2 张 40px 缩略图**：用户要求"尺寸大一些，数量可以少"，2 张 40px 的缩略图比 3 张 28px 更清晰，格子高度 92px 容纳日期+徽章+缩略图
4. **删除 CalendarView 内部加入相册按钮**：OrganizePanel 的按钮已固定常显示，CalendarView 内部的按钮是冗余的
5. **失败重试 2 次**：避免无限重试消耗资源，2 次足以覆盖临时性失败（如并发 IO 限制）

### 验证
- `npm run typecheck` 通过
- 需用户手动执行 `npm run desktop:build` 构建验证桌面应用效果

---

## 2026-08-10 · 照片整理缩略图性能优化（懒加载+Canvas缩小+LRU缓存+尺寸分级）

### 需求
照片相似分析、人脸识别、时间线、日历、照片去重等需要显示照片缩略图的功能，在照片很多时很卡顿。需要做性能优化：
- 懒加载，只显示用户看到的页面区域
- 日历里的照片缩略图像素显示优化（日历表上的照片很小，用不到那么高清的图）

### 问题分析
原有 ThumbImage 组件存在 3 个性能瓶颈：
1. **无懒加载**：所有缩略图挂载即加载，日历有 100+ 个 ThumbImage 同时加载
2. **加载完整原图**：每次调用 `readPhotoData(photo)` 读取完整照片字节（可能几 MB），日历 28px 小图也加载几 MB 原图，浪费内存和 CPU
3. **无缓存**：同一照片在多处重复加载；DedupeTool 还有重复的 ThumbImage 实现

### 实现内容

#### 一、缩略图缓存模块 [thumbCache.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/thumbCache.ts)
新建 `thumbCache.ts` 模块，提供 3 大优化：

1. **Canvas 缩小**：加载完整数据后用 `createImageBitmap` + `resizeWidth/Height` 原生缩小到目标尺寸，原始 ArrayBuffer 立即释放，blob URL 只持有缩小后的小图（几 KB）
   - 优先用 `createImageBitmap`（Chromium/WebKit 均支持原生 resize，最快）
   - fallback 到 `Image` 元素 + `Canvas.drawImage`（兼容性更好）
   - 输出 `image/jpeg` 质量 0.75（缩略图不需要高质量）

2. **全局 LRU 缓存**：按 `photoId:sizeKey` 缓存，同一照片同一尺寸只生成一次，跨组件复用
   - 缓存上限 400 个（每个缩略图几 KB，总内存约 1-2MB）
   - LRU 淘汰：超限时淘汰最久未使用的条目，revoke blob URL 释放内存

3. **并发去重**：用 `inflight` Map 缓存正在进行的 Promise，同一照片并发请求只发起一次 IO

4. **尺寸分级**（displayPx 是 CSS 显示尺寸，dim 是 2x DPI 渲染尺寸）：
   | size | dim | 适用场景 | 显示尺寸 |
   |------|-----|----------|----------|
   | tiny | 64px | 日历格子 | 28px |
   | small | 128px | 列表项 | 72px |
   | medium | 256px | 网格 | 120-140px |
   | full | 原图 | 大图预览 | 全屏 |

#### 二、ThumbImage 组件改造 [shared.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/shared.tsx)
- **IntersectionObserver 懒加载**：只在进入视口（提前 200px `rootMargin`）才触发加载，避免一次性加载几百张照片
  - `lazy` prop 默认 true，可禁用
  - 进入视口后 `observer.disconnect()`，不再重复检测
- **size prop**：新增 `size: ThumbSize` prop，默认 'small'，通过 `getThumbUrl` 获取对应尺寸的缓存缩略图
- **复用缓存**：不再每次创建独立 blob URL，改用 `getThumbUrl` 从全局缓存获取（缓存命中时 O(1)）
- 删除了原有的独立 `readPhotoData` + `URL.createObjectURL` 逻辑

#### 三、DedupeTool 复用 shared ThumbImage [DedupeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/DedupeTool.tsx)
- 删除 DedupeTool 中重复的本地 ThumbImage 实现（约 100 行）
- 改为从 `./shared` 导入 `ThumbImage`
- 调用处传入 `aspect="4/3"` 和 `size="medium"`（去重网格 140px+ 显示）

#### 四、各工具传入 size prop
| 工具 | 组件 | size | 原因 |
|------|------|------|------|
| CalendarView | StackedThumb | tiny (64px) | 日历格子 28px 显示 |
| CalendarView | DayGridThumb | small (128px) | 展开列表约 80px |
| TimelineView | 列表项 | small (128px) | 列表缩略图约 72px |
| FaceClusterTool | 无人脸网格 | small (128px) | 72px minmax |
| FaceClusterTool | 人脸分组 | small (128px) | 6 列网格 |
| SimilarTool | 相似分组 | medium (256px) | 140px minmax |
| DedupeTool | 去重网格 | medium (256px) | 140px+ 显示 |

### 关键决策
1. **不手动清理缓存**：让 LRU 自然淘汰，避免用户在标签间来回切换时重复加载。CACHE_LIMIT=400 足够大
2. **createImageBitmap resizeWidth 而非 Image+Canvas**：原生 resize 性能更好，且不需要等 Image.onload。fallback 到 Image+Canvas 保证兼容性
3. **rootMargin: 200px**：提前 200px 加载，避免滚动时出现空白闪烁。不用 500px 避免预加载太多
4. **JPEG 0.75 质量**：缩略图不需要高质量，0.75 在视觉上几乎无差异，但文件更小
5. **full 模式不缩小**：大图预览需要原图质量，直接创建 blob URL（PhotoQuickView 预览时用）

### 性能提升预估
- **内存**：日历 100 张缩略图，原来 100 × 3MB = 300MB，现在 100 × 4KB = 400KB（降低 750 倍）
- **加载速度**：IntersectionObserver 懒加载，首屏只加载可见的 10-20 张（原来 100+ 张同时加载）
- **重复加载**：LRU 缓存 + 并发去重，同一照片只加载一次

### 验证
- `npm run typecheck` 通过
- 需用户手动执行 `npm run desktop:build` 构建验证桌面应用效果

---

## 2026-08-10 · 照片整理 5 项问题修复（重命名双重路径/geocode 变量/人脸聚类/相似蔓延/成册引用警告）

### 需求
代码评审发现 5 个问题需要修复（含 2 个必现 bug + 3 个体验/边界风险）：
1. 批量重命名执行时路径双重拼接（必现：`rootPath` + 完整路径拼接导致文件找不到）
2. 重命名模板 `{location}` 变量永远输出"未知"（`RenameTool` 未传 `reverseGeocode`）
3. 人脸聚类：模型加载失败后永远无法重试 + blob URL 5 秒定时器提前释放 + 聚类算法实际 O(n³)（注释误标 O(n²)）
4. 相似照片组"链式蔓延"（并查集传递合并导致组内出现差异过大照片）
5. 一键成册 library 源 direct 模式跨项目引用隐患（原项目删照片后新相册失效）

### 实现内容

#### 一、批量重命名路径双重拼接 [rename.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/rename.ts)
- **根因**：Rust 端扫描返回的 `photo.path` 已是完整绝对路径（如 `D:\照片\2024\IMG_001.jpg`），`executeRename` 却用 `join(rootPath, photoPath)` 二次拼接，产生 `D:\照片\D:\照片\...` 双重路径，重命名全部失败
- **修复**：`oldPath` 直接用 `normalize(photoPath)`；`newPath = normalize(join(dirPart, item.newName))`（目录部分 + 新文件名）；`rootPath` 仅作分隔符推断兜底（`firstPath = items.find(...)?.photo.path ?? rootPath`）

#### 二、重命名 {location} 变量失效 [RenameTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/RenameTool.tsx)
- **根因**：`previewRename` 依赖传入的 `reverseGeocode` 回调才能解析地名，`RenameTool` 未传，导致 `{location}` 永远输出"未知"，而实时示例却用"北京市"占位展示，界面与实际不符
- **修复**：参照 `OrganizeTool` 补传 `reverseGeocode`（Tauri `invoke('reverse_geocode')`，非 Tauri 环境传 `undefined`），导入 `logger` 记录失败日志

#### 三、人脸聚类 3 处修复 [face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)
- **失败后允许重试**：`loadFaceApiForClustering` 失败时重置 `faceApiModule = null`、`recognitionModelLoaded = false`，并在 promise resolve 为 null 后清除 `faceApiLoadPromise`，模型文件补全后无需重启即可重试
- **blob URL 立即释放**：`loadImageFromData` 改为返回 `{ img, url }`；`extractFaceDescriptors` 在检测完成（成功/失败）的 `finally` 中 `URL.revokeObjectURL(url)`，去掉原 5 秒定时器（避免大图 4 并发下检测中途被提前释放）
- **聚类算法 O(n³) → O(n²)**：用 Prim 最大生成树 + 阈值切边重写 `agglomerativeCluster`（数学等价于原 single-linkage 层次聚类），替代暴力"每次合并遍历所有簇对"的三层循环；修正注释中错误的 O(n²) 标注。n=2000 时从约 8×10⁹ 次操作降至约 4×10⁶

#### 四、相似照片组二次质检防蔓延 [hash.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/hash.ts)
- **根因**：并查集传递合并（A≈B、B≈C → A、B、C 同组），但 A 与 C 的 pHash 距离可能远超 `maxDistance`，一组内出现差异很大的照片
- **修复**：
  - 新增 `splitRunawayGroup`：对每组按"与基准成员距离 ≤ maxDist"贪心拆分，保证组内任意成员与基准的差异在合理范围
  - 组内 `maxDistance`/`avgDistance` 改用 pHash 全量两两计算（覆盖间接合并的成员对，替代原 `comparedPairs` 仅含直接比对对）
  - 清理死代码：删除 `comparedPairs` 声明及 3 处 `set` 调用

#### 五、一键成册 library 源 direct 引用警告 [AlbumBridgeDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/AlbumBridgeDialog.tsx) + [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) + [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)
- library 源 + direct 策略时，在存储方式下方显示琥珀色警告框：库内照片以引用方式加入目标相册，原库删除会导致照片无法显示，建议改用"复制到库内"
- 新增 i18n key：`albumBridge.libraryDirectWarning`（中英文均已补充）

### 关键决策
1. **executeRename 不再拼接 rootPath**：`photo.path` 为完整路径是 Rust 端扫描的既定契约（`organize.ts` 的 `executeOrganize` 一直直接使用 `photo.path`），重命名工具应与之一致
2. **Prim MST 等价性**：single-linkage 每次合并"相似度最高簇对"与按边权降序构建生成树等价，聚类结果 = MST 中保留权值 ≥ 阈值的边后的连通分量，因此可用 O(n²) Prim 精确替代，无近似误差
3. **blob URL 生命周期归调用方**：`loadImageFromData` 只负责创建，释放统一在 `extractFaceDescriptors` 的 `finally` 中执行，避免"使用方未用完就释放"与"永不释放"两种极端
4. **拆组采用贪心而非暴力**：按基准成员距离拆组是实用的近似（严格"任意两两 ≤ maxDist"是 clique 问题），已满足防蔓延需求
5. **library 源选择"警告而非强制 import"**：不改变用户既有预期（部分用户明确希望省空间引用），仅提示风险让用户自行决策

### 验证
- `tsc --noEmit -p tsconfig.app.json` 通过
- ESLint 无错误
- `vitest run` 全部单元测试通过
- 重命名双重路径 bug 需用户在实际 Tauri 桌面端执行一次"批量重命名"确认（原必现失败，修复后应全部成功）

---

## 2026-08-10 · 照片整理 9 项优化修复（缩略图/全选/日历跳转/重命名示例/滚动条/功能改名）

### 需求
针对照片整理功能做 9 项优化修复：
1. 相似照片聚类：缩略图不显示 + 大图查看（参考去重功能）
2. 人脸聚类：改名"人脸识别"，识别后增加"加入相册"后续操作
3. 时间线：缩略图显示 + 月份/路径级全选 + 月份列表"在日历中查看"跳转按钮
4. 日历：缩略图显示 + 固定常显"加入相册"按钮（未选置灰+点击提示）+ 当天全选
5. 批量重命名：CN 模式下模板变量中文释义 + 实时示例效果展示
6. 相似照片聚类：名称改"照片相似分析"，按钮改"分析相似照片"
7. 按时间归类：名称改"照片归类"
8. 批量改 EXIF：名称改"Exif修改"
9. 照片整理功能所有上下滚动窗口增加滚动条样式

### 实现内容

#### 一、共享缩略图组件提取 [shared.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/shared.tsx)
- 提取 `ThumbImage` 共享组件：当 `photo.thumbUrl` 为空时，通过 `readPhotoData` 异步加载照片数据并创建 Blob URL
- 统一了 SimilarTool / TimelineView / CalendarView / FaceClusterTool 的缩略图加载逻辑
- 大图查看：复用 DedupeTool 的 `PhotoQuickView` 组件，支持键盘导航、缩放、旋转

#### 二、OrganizePanel 视图联动 [OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)
- 新增 `calendarInitialView` 状态 + 清理 effect（CalendarView 在 mount 时通过 useState lazy initializer 消费 initialView，消费后立即清除避免后续 remount 跳转到旧月份）
- TimelineView 调用补全 props：`readPhotoData` / `onSelectionChange` / `onViewInCalendar`
- CalendarView 调用补全 props：`readPhotoData` / `selectedIds` / `onSelectionChange` / `onAddToAlbum` / `initialView`
- 视图切换栏"加入相册"按钮改为固定常显示：
  - 未选照片：置灰 `bg-gray-100 text-gray-400 cursor-not-allowed`，点击 toast 提示"请先选择照片"
  - 已选照片：`bg-brand text-white`，显示选中数量 `· N`
- 手动切换视图（点击 tools/timeline/calendar 按钮）时清空选中
- 时间线 → 日历跳转：清空选中（月份/路径选择上下文与日历的按天选择不同）+ 设置 calendarInitialView + setViewMode('calendar')

#### 三、TimelineView 全选 + 日历跳转 [TimelineView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/TimelineView.tsx)
- 月份分组：`全选本月` / `取消本月` 按钮
- 路径分组：`全选此路径` / `取消此路径` 按钮
- 月份分组头部新增"在日历中查看"按钮（calendar 图标 + 文案），调用 `onViewInCalendar(year, month)`
- 顶部新增 `取消选择` 按钮（清空全部选中）

#### 四、CalendarView 固定按钮 + 当天全选 [CalendarView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/CalendarView.tsx)
- 当天照片列表头部新增 `全选当天` / `取消当天` 按钮
- 接收 `onAddToAlbum` 回调，由 OrganizePanel 统一处理（未选 toast 提示，有选打开 AlbumBridgeDialog）
- `initialView` 通过 useState lazy initializer 在 mount 时消费，跳转到指定月份

#### 五、FaceClusterTool 加入相册 [FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)
- 选中计数 + "加入相册"按钮（未选置灰 + 点击 toast 提示）
- 集成 `AlbumBridgeDialog`，支持将选中的人脸分组照片加入相册项目
- 功能名称从"人脸聚类"改为"人脸识别"（i18n key `faceCluster.title`）

#### 六、RenameTool 中文释义 + 实时示例 [RenameTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/RenameTool.tsx)
- 新增 `localPreviewTemplate` 函数：根据模板 + 当前 seq 设置，对样例照片（或第一张照片）做字符串替换，不调用 geocode（location 用"北京市"占位）
- 模板变量 chip 改为可点击按钮：点击插入到输入框光标位置（`setSelectionRange` 异步聚焦），chip 显示 `{var}` + 中文释义
- 新增"实时示例效果"区域：随模板/seqStart/seqDigits 变化即时更新，绿色等宽字体显示
- i18n key 复用：`templateVarDate` / `templateVarLocation` / `templateVarSeq` / `templateVarCamera` / `templateVarOriginal` + `templateExampleLabel` / `templateExampleEmpty`

#### 七、功能名称统一修改 [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) + [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)
- `similar.title`: "相似照片聚类" → "照片相似分析"（EN: "Similar Photo Analysis"）
- `similar.startScan`: "开始聚类" → "分析相似照片"（EN: "Analyze Similar"）
- `organize.title`: "按时间归类" → "照片归类"（EN: "Photo Categorize"）
- `exif.title`: "批量改 EXIF" → "Exif修改"（EN: "Batch EXIF Editor" → "EXIF Edit"）
- `faceCluster.title`: "人脸聚类" → "人脸识别"（EN: "Face Recognition"）
- `faceCluster.description`: 增加"可加入相册"说明
- 新增 i18n key：`albumBridge.selectPhotosFirst`（"请先选择照片"）

#### 八、滚动条样式统一 [globals.css](file:///f:/N-编程/MenBook开发项目/MemBook/src/styles/globals.css)
- 为以下滚动容器添加 `custom-scrollbar` class：
  - OrganizePanel 根容器（`h-full overflow-auto p-6`）
  - OrganizePanel 历史路径列表（`max-h-[280px] overflow-y-auto`）
  - AlbumBridgeDialog 内容区（`flex-1 overflow-y-auto`）
  - LibraryPickerDialog 项目列表（`flex-1 overflow-y-auto`）
- 已有 custom-scrollbar 的组件：DedupeTool / ConvertTool / ExifTool / FaceClusterTool / OrganizeTool / RenameTool / SimilarTool / TimelineView（确认无遗漏）

### 关键决策
1. **calendarInitialView 清理时机**：CalendarView 通过 useState lazy initializer 在 mount 时消费 initialView，所以必须在 mount 后清除（用 `setTimeout 0` 让 React 完成 mount 再清除），避免后续 CalendarView remount（切到 tools 再切回 calendar）时仍跳转到旧月份
2. **timeline → calendar 跳转清空选中**：月份/路径的选择上下文与日历的按天选择不同，跨视图保留选中会让用户困惑
3. **加入相册按钮固定常显示**：用户要求"固定位置常显示"，所以即使无选中也显示按钮（置灰），点击 toast 提示"请先选择照片"
4. **RenameTool 实时示例不调用 geocode**：示例仅展示模板效果，location 用"北京市"占位，避免每次输入触发 geocode 请求
5. **变量 chip 点击插入光标位置**：用 `input.selectionStart/End` 获取光标位置，`setSelectionRange` 异步聚焦移到插入后位置，比直接 append 到末尾更友好

### 验证
- `npm run typecheck` 通过
- 需用户手动执行 `npm run desktop:build` 构建验证桌面应用效果

---

## 2026-08-10 · 照片整理 6 大新功能一次性实现（人脸/地点/相似/重命名/时间线日历/一键成册联动）

### 需求
照片整理工具栏新增 6 个高价值功能，一次性实现：
1. 按人物归类（人脸聚类）
2. 按地点归类（GPS + 逆向 geocode）
3. 时间线 / 日历视图
4. 一键成册联动（整理→相册）
5. 相似照片聚类（非精确重复）
6. 批量重命名（模板变量）

### 实现内容

#### 一、数据类型扩展 [types.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/types.ts)
- 新增统一类型定义，覆盖 6 大功能：
  - `OrganizeMode = 'time' | 'location' | 'time_location'`：归类模式
  - `LocationLevel = 'province' | 'city' | 'district' | 'full'`：地点层级（用户确认默认 `full` = 省/市/区县三级）
  - `SimilarGroup`：相似照片分组（保留项 + 待删项）
  - `RenamePreviewItem` / `RenameTemplateVars`：重命名预览和模板变量
  - `FaceRecord` / `FaceCluster` / `FaceClusterResult`：人脸记录、聚类、结果
  - `TimelineGroup` / `CalendarDay`：时间线和日历视图数据

#### 二、按人物归类（人脸聚类）[face-cluster.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/face-cluster.ts)（新建）
- 基于 face-api.js：`TinyFaceDetector`（复用 content-aware 模型）+ `FaceLandmark68Net` + `FaceRecognitionNet`
- 流程：检测人脸 → 提取 68 关键点 → 计算 128 维 descriptor → 余弦相似度 + 层次聚类（agglomerative clustering，single linkage）
- 模型路径：`/models/face-detection/`（复用）+ `/models/face-recognition/`（新增 landmark + recognition 模型文件）
- `loadFaceApiForClustering` 单例 + 模型加载状态追踪，避免重复加载
- `extractFaceDescriptors` 支持 4 worker 并发提取 descriptor
- `clusterFaces` 主流程：阈值默认 0.5（可调 0.3-0.7），按聚类结果构建 `FaceCluster[]`，无人脸照片归到 "未识别" 组
- UI [FaceClusterTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/FaceClusterTool.tsx)：相似度阈值滑块 + 分组卡片（每组展示人脸缩略图 + 照片数）+ "标记删除非主项" 操作（移至回收站）

#### 三、按地点归类 [organize.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/organize.ts)
- 扩展 `getTargetDirEx` 支持三种模式：`time`（原逻辑）/ `location`（仅按地点）/ `time_location`（时间+地点组合）
- `parseLocationLevel(location, level)` 解析省/市/区县：
  - 识别后缀：省（`省$|自治区$|特别行政区$`）、市（`市$`）、区县（`区$|县$|市$|镇$`）
  - `full` 模式返回 `省/市/区县` 三级路径
  - 直辖市场景：第一个 part 即为市级
- `previewOrganize` 扩展 options 接受 `mode`、`locationLevel`、`reverseGeocode`，动态生成预览目录
- UI [OrganizeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/OrganizeTool.tsx)：新增"归类模式"下拉框（时间/地点/时间+地点）+ 地点层级下拉框（省/市/区县/省市区县）
- [geocode.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/geocode.ts)：逆向 geocode 函数（GPS 坐标 → 地名）

#### 四、相似照片聚类（非重复）[hash.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/hash.ts)
- 新增 `findSimilarPhotos(photos, options)`：
  - `minDistance=6`（与去重阈值 5 区分）、`maxDistance=15`
  - 复用 pHash + LSH 加速比对（复杂度近线性，避免 O(n²)）
  - 并查集（Union-Find）合并相似组
  - 每组选最佳保留项（最大分辨率/最晚修改时间）
- 与去重工具区分：阈值不同（去重 ≤5、相似 6-15）+ 独立工具卡
- UI [SimilarTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/SimilarTool.tsx)：相似组卡片（保留项高亮 + 待删项半透明）+ 阈值范围滑块 + "标记删除" 批量操作（移至回收站，可撤销）

#### 五、批量重命名 [rename.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/rename.ts)（新建）
- 模板变量：`{date}` / `{location}` / `{seq}` / `{camera}` / `{original}`
- `buildRenameVars(photo, seq, options)`：从 PhotoFileInfo 构建变量
  - `date`：`resolvePhotoDate` 解析，格式 `2024-01-15`，无日期用 "未知日期"
  - `location`：GPS + reverseGeocode + `parseLocationLevel`（默认市级）
  - `seq`：起始值 + 位数补零（默认 3 位：001, 002...）
  - `camera`：`cameraMake + cameraModel` 拼接
- `previewRename`：替换模板生成新名 + 保留原扩展名 + 同名冲突自动加 `_1, _2` 后缀 + GPS geocode 缓存
- `executeRename`：Tauri `rename` API 执行实际重命名，路径拼接复用 organize.ts 模式
- UI [RenameTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/RenameTool.tsx)：模板输入框 + 起始序号/位数设置 + 预览表（旧名→新名）+ 冲突标记 + 执行按钮

#### 六、时间线 / 日历视图
- [TimelineView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/TimelineView.tsx)（新建）：
  - 按年-月分组照片，每月显示照片数和缩略图网格
  - 支持照片多选（checkbox + 点击切换）+ 全选/取消全选
  - `onSelectionChange` 回调上传选中 ID 给父组件（用于一键成册联动）
- [CalendarView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/CalendarView.tsx)（新建）：
  - 月历视图，每天格子展示照片数 + 首张缩略图
  - 月份切换 + 回到今天
  - 点击日期展开当日照片列表

#### 七、一键成册联动 [AlbumBridgeDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/AlbumBridgeDialog.tsx)（新建）
- 桥接整理工具与相册编辑器，将选中照片加入指定相册
- 存储策略（用户确认）：
  - `direct`：引用原文件路径（folder 模式）或 photoId（library 模式），不复制数据
  - `import`：复制照片数据到相册项目库内独立管理
- 流程：加载项目列表 + 各项目照片数 → 选择目标相册 → 选择存储策略 → 进度条 + 逐张导入
- import 模式调用 `importPhotoToDB` 复制数据；direct 模式调用 `savePhotoChanges` 写入 photo 记录引用
- 防并发：`processingRef` 防止按钮 disabled 在同一 tick 未生效时重复触发

#### 八、OrganizePanel 集成 [OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx)
- 顶部新增视图切换栏：`tools` / `timeline` / `calendar` 三模式
- 工具卡区注册 3 个新工具：`SimilarTool` / `RenameTool` / `FaceClusterTool`
- 选中照片状态 `selectedPhotoIds: Set<string>` 提升到面板级别，跨视图共享
- 切换标签自动清空选中状态（`useEffect [activeTabId]`）
- 选中数 > 0 时显示"加入相册"按钮（带选中数 badge），点击打开 `AlbumBridgeDialog`
- `selectedPhotos` 用 `useMemo` 从 `activeTab.photos` 按 ID 过滤，传入对话框

#### 九、i18n 翻译 [zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json) / [en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)
- 新增翻译节：`faceCluster` / `similar` / `rename` / `albumBridge` / `viewMode` / `timeline` / `calendar`
- 涵盖按钮文案、状态提示、错误信息、模板变量说明

### 关键决策
1. **地点层级默认 `full`（省/市/区县）**：用户确认，避免单一层级过粗或过细
2. **人脸模型打包进安装包**：模型文件放 `public/models/face-recognition/`，随应用发布，首次使用无需下载
3. **相似照片与去重区分**：阈值 6-15 vs 去重 ≤5，独立工具卡，避免功能混淆
4. **批量操作安全**：所有删除走 Tauri `trash_files` 移至回收站可撤销；重命名前生成完整预览
5. **一键成册双策略**：direct 节省空间但依赖原文件，import 独立管理但占空间，用户按需选择

### 验证
- `npm run typecheck` 通过
- 需手动执行 `npm run desktop:build` 构建验证

---

## 2026-08-10 · 画笔工具交互隔离 + 文字单击选中双击编辑 + 添加按钮外置 + 面板宽度稳定

### 需求
1. 画笔/橡皮擦选中后绘制时页面内容被选中，绘画状态应让页面内容全部不可选中
2. 左侧面板文字和便利贴的添加按钮应放在展开区域标题栏右侧，无需展开后再添加
3. 文字框单击为选中状态（移动/缩放），双击为进入内容编辑状态
4. 选中文字/便利贴后左侧面板内框宽度跳动缩短，应保持宽度一致

### 实现内容

#### 一、画笔/橡皮擦工具交互隔离
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**、**[StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx)**、**[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)**：新增 `interactive` prop（默认 true），控制 Group 的 `listening` 和 `draggable`。画笔/橡皮擦模式下设为 false，事件穿透到 Stage
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：
  - 定义 `isToolMode = activeTool === 'brush' || activeTool === 'eraser'`
  - 槽位 Group `listening={!isEditing && !isToolMode}`
  - 水印 Text `listening={!isToolMode}`
  - 文字/便利贴/贴纸传 `interactive={!isToolMode}`
  - Stage onMouseDown 画笔模式不再检查 `isStageBg`，无论点击元素或空白都开始绘制（元素已 listening=false，事件穿透）
  - 笔迹保持 `listening={activeTool === 'eraser'}`（橡皮擦模式下可点击擦除）

#### 二、文字单击选中、双击编辑
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L1515)**：文字 onClick 去掉 `setEditingTextId(el.id)`，仅 `setSelectedTextId(el.id)`（单击只选中）；onDblClick 保持 `setEditingTextId(el.id)`（双击进入编辑）
- 编辑虚线框仅在 `editingTextId` 时显示，选中状态显示控制点但不显示编辑框

#### 三、添加按钮移到标题栏右侧
- **[ToolsPanel.tsx CollapsibleSection](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx#L474)**：新增 `headerAction` prop，渲染在标题栏右侧（不随展开/收起隐藏）
- 文字区块：添加按钮从内容区移到 headerAction（+图标按钮，24×24px）
- 便利贴区块：同理
- 用户无需展开即可直接点击 + 按钮添加

#### 四、面板宽度稳定
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**：统一文字/便利贴的提示框与属性面板 padding 为 `p-3`（原提示框 `px-3 py-2`），消除切换选中状态时内框宽度跳动

### 验证
- `npm run typecheck` 通过
- 需手动执行 `npm run desktop:build` 构建验证

---

## 2026-08-09 · 工具面板拆分 + 便利贴美化 + 背景纹理修复 + 吸管优化

### 需求
1. 画笔/橡皮擦光标移动到工作区后显示异常、响应问题
2. 文字工具双击编辑虚线框与显示框大小位置不一致；文字工具支持旋转
3. 便利贴角点控制不锁定等比例缩放，支持任意高度和宽度缩放
4. 文字工具左侧面板的置顶/置底按钮删除（悬浮工具栏已有）
5. 便利贴样式效果不好看，设计得好看一些
6. 便利贴左侧面板的大小、置顶/置底按钮删除（已有悬浮工具栏和尺寸自定义）
7. 工具左侧面板文字和便利贴拆分开：画笔、文字、便利贴、背景
8. 背景纹理没有应用到页面
9. 自定义取色器吸管功能体验不佳

### 实现内容

#### 一、画笔/橡皮擦光标修复
- **[ToolCursors.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ToolCursors.tsx)**：用 `createPortal` 渲染到 `document.body`，`position: fixed` + `transform: translate3d` + `requestAnimationFrame` 节流，监听 `document.mousemove` 确保事件不丢失

#### 二、文字工具旋转 + 编辑虚线框修复
- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx)**：重为中心定位（`cx = el.x + el.width/2`），支持任意角度旋转；添加旋转手柄（15° 吸附 + 单击 90°）；角点改为独立宽高缩放
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：文字编辑框中心定位 + 应用元素旋转角度；文字工具栏用 AABB 包围盒计算视觉顶部位置

#### 三、便利贴不等比缩放 + 美化
- **[StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx)**：角点移除等比缩放逻辑，改为独立宽高（`newW = startW + dx`, `newH = startH + dy`）；美化样式：
  - 顶部高光渐变层（半透明白色，增强纸张质感）
  - 底部阴影渐变层（透明到淡黑，增强深度）
  - rounded 样式右上角折角（模拟便签纸撕开）
  - shadow 样式对角渐变（右上到左下，3D 投影）
  - tape 样式米黄色胶带 + 对角倾斜 + 锯齿边缘
  - 文字行高 1.4，内边距 8px，颜色 #2c2c2c

#### 四、删除左侧面板冗余按钮
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**：删除文字属性面板的置顶/置底按钮；删除便利贴属性面板的大小、置顶/置底按钮；清理未使用的 `bringToFront`/`sendToBack` import

#### 五、工具面板拆分
- **[ToolsPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/ToolsPanel.tsx)**：原"文字/便利贴"合并区块拆为两个独立 `CollapsibleSection`：文字（添加文字按钮 + 文字属性 + 未选中提示）、便利贴（添加便利贴按钮 + 便利贴属性 + 未选中提示）
- **i18n**：新增 `stickySection`（便利贴/Sticky Notes）、`textHint`（未选中文字提示）翻译键

#### 六、背景纹理修复
- **[constants.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/constants.ts#L130)**：新增 `createTextureCanvas(texture)` 生成 32×32 纹理图案 Canvas（宣纸/牛皮纸/波点/网格/条纹/亚麻），使用确定性坐标保证缓存稳定
- **[PageBackground.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/PageBackground.tsx)**：纹理背景用 `fillPatternImage` + `fillPatternRepeat='repeat'` 渲染实际图案（原仅画底色）
- **[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L564)**：`drawPageBackground` 纹理分支用 `ctx.createPattern(canvas, 'repeat')` 渲染图案

#### 七、吸管功能优化
- **[BackgroundPicker.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/BackgroundPicker.tsx)**：添加 EyeDropper API 吸管按钮（优于系统取色器吸管，体验更流畅）；不支持时回退到 `<input type="color">`；添加 `picking` 状态视觉反馈
- **i18n**：新增 `eyeDropper`（屏幕取色）、`eyeDropperUnsupported` 翻译键

#### 八、导出引擎同步
- **[exportEngine.ts drawTextElement](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L944)**：支持任意角度旋转（中心定位 + `translate+rotate`），`rotation === -90` 保持竖排逻辑
- **[exportEngine.ts drawStickyNote](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts#L1016)**：同步美化效果（高光、阴影渐变、折角、真实胶带、行高 1.4）；修正变换偏移 bug（去掉多余的三段 translate 第三段，改为两段中心定位）

### 验证
- `npm run typecheck` 通过
- 需手动执行 `npm run desktop:build` 构建验证

---

## 2026-08-09 · 照片列表多选拖拽完成后清空选中状态

### 需求
相册编辑器照片列表多选功能：多选照片后拖拽到页面进行照片添加完成后，应清空多选内容，让用户进行下一次多选操作。

### 实现内容
- **[PhotoPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoPanel.tsx)**：
  - `PhotoThumbItem` 组件新增 `onDragEnd?: () => void` 回调 prop
  - 在 `onUp` 拖拽结束处理中，`endDrag()` 后立即调用 `onDragEnd?.()` 清空选中状态
  - 父组件传入 `onDragEnd={() => setSelectedPhotoIds(new Set())}`，拖拽完成后重置 `selectedPhotoIds`
  - 保留 `stopClick` 机制防止拖拽后误触发 click 选择切换
  - 多选模式（`multiSelectMode`）保持不变，仅清空选中内容，用户可直接开始下一次多选

### 验证
- `npm run typecheck` 通过
- 需手动执行 `npm run desktop:build` 构建验证

---

## 2026-08-09 · 画笔/橡皮擦/文字/便利贴工具交互修复优化

### 需求
1. 画笔四种笔触类型无差异；光标为十字无法感知粗细
2. 橡皮擦光标显示异常，看不见位置，大小不可感知
3. 选中画笔/橡皮擦后切换到其他面板未自动取消
4. 文字工具上下左右控制点拉伸方向与操作习惯不符（拖下边拉伸的是宽度）
5. 便利贴编辑虚线框位置偏移；悬浮工具栏位置不对；8控制点 UI 和操作逻辑与贴纸不一致

### 实现内容

#### 一、画笔笔触差异化 + 光标视觉反馈

- **[BRUSH_STYLE_MAP](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts#L342)**：统一管理四种笔触的视觉参数（线宽倍数、贝塞尔张力、透明度倍数、混合模式）
  - pencil: 1x 线宽，tension 0.3，锐利细线
  - brush: 2.5x 线宽，tension 0.5，柔和中等
  - marker: 1.8x 线宽，0.85 透明度
  - highlighter: 4x 线宽，0.4 透明度，multiply 混合模式
- **[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)**：持久化渲染和实时预览均应用 `BRUSH_STYLE_MAP`；容器 cursor 设为 `'none'`，动态计算画笔光标大小（`strokeWidth * widthMultiplier`）
- **[ToolCursors.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/ToolCursors.tsx)**：
  - `BrushCursor`：半透明填充圆环反映画笔颜色和粗细
  - `EraserCursor`：红色圆环 + 半透明背景 + 中心十字辅助瞄准，大小反映擦除范围
- **[BrushPreview.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/tools/BrushPreview.tsx)**：预览组件统一使用 `BRUSH_STYLE_MAP`，与实际渲染一致

#### 二、面板切换自动取消工具

- **[uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts#L192)**：`setActivePanel` 切换离开 `tools` 面板时，通过 `useEditorStore.getState()` 调用 `setActiveTool('none')`，自动取消画笔/橡皮擦

#### 三、文字工具控制点逻辑修复

- **[TextElementNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/TextElementNode.tsx#L49)**：`edgeHandle` 的 axes 映射颠倒——上下边（n/s-resize）原传 `'h'`（控制宽度），实际应传 `'v'`（控制高度）；左右边（w/e-resize）原传 `'v'`，实际应传 `'h'`（控制宽度）。修复后视觉形状与贴纸一致

#### 四、便利贴全面对齐贴纸组件

- **[StickyNoteNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickyNoteNode.tsx)**：参照 StickerNode 完全重写
  - **移除 offsetX/offsetY**：改为中心定位（`x={px} y={py}`），内容绘制在 `(-pw/2, -ph/2)`，修复旋转中心偏移到右下角的旧 bug
  - **8 方向控制点**：4 角圆形 + 4 边长方块，与贴纸样式一致（6px 半径，1.5px 描边）
  - **旋转感知缩放**：`screenDeltaToLocal` 转换屏幕 delta 到本地空间，锚点 = 对角/对边页面坐标（含旋转修正），缩放后反算新中心
  - **旋转手柄**：白色圆底 + ↻ 图标，以中心点旋转，15° 吸附，单击旋转 90°
- **[Canvas.tsx 编辑框](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2305)**：编辑虚线框尺寸阈值与 Konva 渲染一致（便利贴 min 40px），修复旋转后虚线框偏移
- **[Canvas.tsx 工具栏](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx#L2364)**：悬浮工具栏改用 AABB 包围盒顶部计算（`bboxTopMm = centerY - (|halfW*sinθ| + |halfH*cosθ|)`），旋转后始终在视觉上方

### 验证
- `npm run typecheck` 通过
- 需手动执行 `npm run desktop:build` 构建验证

---

## 2026-08-08 · 日志压缩归档（08-08 阶段，6 条日志摘要）

> 本段为 2026-08-08 当日 6 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。当日主题：照片整理面板性能与 EXIF 修改链路修复。

- **优化多路径标签栏显示**：[OrganizePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/OrganizePanel.tsx) 激活标签 `max-w-[180px] min-w-[120px]`、非激活 `max-w-[80px] min-w-[68px]` 差异化展示，容器 `items-stretch` + `shrink-0`，滚动条 `scrollbarWidth:'thin'`（Firefox）+ custom-scrollbar
- **无日期列表拆分为两个批量按钮**：[ExifTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ExifTool.tsx) 「全部转换」（转 JPEG）+「全部修改」（写日期）分离，纳入 `busy` 互斥；更新 i18n `convertAll*`/`writeAll*`
- **修复 JPG EXIF 修改/转格式均失败（三 bug）**：① [exif.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/exif.ts) 验证改用 `exifr.parse`（piexifjs load 返回 ASCII 字节数组 `[50,48,...]`，与字符串比对恒失败）；② [livp-converter.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/livp-converter.ts) `convertToJpg` 补 `.jpg/.jpeg` 分支（Canvas 重编码）；③ ExifTool JPG 转格式后路径不变会 `trash_files` 删掉自己——仅当 `jpgPath !== photo.path` 才删原文件
- **EXIF 修改失败照片支持转格式 + 一键修复全部**：`NoDatePhotoRow` 新增 `modifyFailed` 态显示转格式按钮，`handleConvert` 转 JPEG 后重置失败态；`handleFixAll` 批量转已识别到日期的照片 + 进度条 + 结果 toast
- **修复扫描进度条不停止 + EXIF 修改假成功**：① rAF 延迟 flush 覆盖 `scanProgress:null`——加 `scanDone` 标志 + 成功/失败路径 `cancelAnimationFrame`；② 进度条改渐变光带/渐变填充柔和样式；③ `piexif.insert` 静默失败不抛异常——JPEG 用 `piexif.load` 读回比对、PNG/WebP 用 `exifr.parse` 读回验证，不匹配抛「写入后验证失败」
- **照片整理大文件夹性能优化（数千张不卡顿）**：Rust `scan_photos_with_exif` 改 async + `spawn_blocking`（Tauri 主线程不再冻结）+ 每 50 张 emit 进度；前端 rAF 合并进度事件 + `startTransition` 批量 setState + fallback EXIF 8 worker 并发；`toolProps`/工具卡片 `useMemo` 稳定引用（扫描期间只刷新 ProgressBar，四工具不重渲染）

---

## 2026-08-07 · 日志压缩归档（08-07 阶段，17 条日志摘要）

> 本段为 2026-08-07 当日 17 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。当日主题：照片整理（EXIF/去重）功能大版本。

- **照片去重新增 pHash 感知哈希层 + 优先删除无拍摄日期**：SHA256 字节级匹配无法识别"内容一样但字节不同"（微信重编码/EXIF 剥离）的照片；新增 [perceptual-hash.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/perceptual-hash.ts)（32×32 灰度 DCT → 8×8 低频 → 64 位哈希），[hash.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/hash.ts) 升级四级引擎（Phase4 汉明距离 ≤5 + 并查集传递性分组，标记 `similarity:'visual'`）；保留评分：有日期 +5/无日期 -3/整理目录 +3 等
- **照片整理扫描 EXIF 读取链三次演进**：① 扫描时完全不读 EXIF 导致 dateTaken 全空 → exifr 读前 64KB + `readExifDateWithFallback`（EXIF 5 级优先 + 文件名解析 fallback）；② JS 方案每文件 4 次 IPC 慢 → 重构为 Rust 批量扫描 `scan_photos_with_exif`（kamadak-exif + walkdir，1000 张 5-15s → 0.5-1s、1 次 IPC）；③ 美图秀秀等非标准 EXIF（损坏 IFD 链）严格解析器整体失败 → Rust 三级解析链（kamadak-exif → `read_exif_date_manual` 手动字节流查 DateTimeOriginal tag → 前端 exifr 容错 fallback，`needs_js_fallback` 三态）。Rust 端 ext 统一带点小写（`.jpg`）修复 ExifTool 显示 0 张可处理
- **EXIF 单文件修改 + 扫描进度实时显示**：[ExifTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/ExifTool.tsx) 新增 `NoDatePhotoRow`（文件名自动识别日期预填 + 单文件写入）/`NoGpsPhotoRow`（地名查询 + 经纬度校验单改 GPS）；Rust `app.emit('organize://scan-progress')` 推送扫描进度，ProgressBar 支持 total=0 不定态动画；有日期/GPS 的照片默认不勾选（防覆盖）
- **文件名日期识别增强**：[filename-time.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/photo-tools/filename-time.ts) `parseFilenameDate` 9 → 16 种模式 7 级优先（新增斜杠/点号分隔、单数字、无秒、DSC/QQ图片/美图/轻颜相机等前缀），35 测试用例通过
- **去重手动调整 + 大图预览**：[DedupeTool.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/DedupeTool.tsx) 算法建议（★）+ 用户覆盖层（✓保留/✗删除，每组至少留 1 张）+ 卡片网格 UI；新建 [PhotoQuickView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/organize/PhotoQuickView.tsx) 轻量大图预览（键盘/缩放/拖拽，不依赖 editor store）；Tauri 缩略图按需 `readPhotoData` 加载；删除改 `trash_files` 进回收站（原为物理删除）
- **PhotoQuickView 体验优化**：UI 对齐编辑模式 PhotoPreview（绝对定位浮层 + 毛玻璃按钮 + 旋转 + 胶囊工具栏）；blob URL 缓存 + 相邻预加载实现秒切（原每次切换 IPC 重读）
- **去重千张性能优化**：8 worker 并发读文件、Phase3→4 fullDataCache 复用、LSH 鸽巢分桶（threshold+1 子串，零假阴性，O(M²)→近 O(M·k)）、可分离 DCT O(N⁴)→O(2N³)、每 50 hash 进度回调
- **多标签切换隔离与持久化**：四工具 `key={tool-tabId}` 隔离状态；`busyTools: Set` 按工具名汇总 busy（原单值 last-writer-wins 被空闲工具覆盖），执行中禁用切标签；去重 result/overrides 提升到面板级 `dedupeStates: Map<tabId>` 跨标签持久化（DedupeTool 改受控组件）
- **整理面板 UI 一揽子优化**：格式统计（`countByExt`）合并到总张数同一行；路径换行（break-all）+ 复制/打开文件夹（Rust `open_folder`）；路径标签栏 10→20 + 弹性收缩自适应；四工具卡片位置上移；列表统一 custom-scrollbar + 高度限制（去重 480/归类 520 自适应）；格式转换显示原大小 + `estimateJpgSize` 预估；去重卡片文件名去重/日期角标右下/状态徽章白描边；大图点击灰色区域关闭（居中层 pointer-events-none 穿透）

---

## 2026-08-06 · 日志压缩归档（08-06 阶段，18 条日志摘要）

> 本段为 2026-08-06 当日 18 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。当日主题：macOS 适配系列修复 + 贴纸交互优化。

- **macOS 平台支持系列修复（延续 08-05）**：① macOS 26 (Tahoe) 启动崩溃——`setTrafficLightPosition:` 非公开 API 被移除，加 `respondsToSelector:` 防御检查；② 红绿灯位置多轮对齐（window_height-31→-35→-37→-30→-28，最终统一 Header 56px 后对齐 28px 中心）；③ 最大化/全屏内容左移——仅 `isFullscreen` 时移除 72px 让位 padding（最大化保留，参考 VSCode/Figma），paddingLeft 加 0.3s 过渡；④ 标题栏隐藏"MemBook"文字（`set_title("")`）；⑤ macOS 拖拽紫色高亮——`html.is-dragging` 全局类 + `::selection transparent !important`（body 行内样式不继承）；⑥ objc `sel` 宏导入、tauri.conf resources 格式、`on_window_event` 替代 on_resized、NSPoint 字面量等编译错误修复
- **Windows 打印修复**：CI 安装包报"无法连接打印机"——tauri.conf.json `bundle.resources` 未打包 `sm.exe`，显式声明 `"resources": {"resources/sm.exe": "resources/sm.exe"}`；PrintDialog catch 改透传实际错误信息
- **PNG 透明底显示黑底修复**：压缩管线全转 JPEG 丢 alpha；[compress.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/storage/compress.worker.ts) 新增 `checkBitmapTransparency`（64×64 采样），含透明像素输出 PNG 否则 JPEG，主线程 fallback 与 SmartLayoutView 同步
- **贴纸交互五项修复**：① 列表拖出预览瞬间放大——预览 div 挂载时从 ref 计算 inline width/height/transform（首次绘制即定尺寸）；② 点击闪烁——4px 拖拽阈值区分点击与拖拽；③ 画布拖拽闪烁——`onDragMove` 不调 onUpdate（避免 Konva/React 位置双写冲突），`onDragEnd` 一次性同步；④ 拖到非工作区仍被添加——`endStickerDrag` 传 mouseup 实际坐标 + drop 检测先过 `containerRef`（可见工作区）边界再算 Konva 坐标；⑤ 贴纸多选/框选——marquee 收集 slot+text+sticky+sticker（中心点转左上角），多选隐藏单独手柄
- **多选照片拖拽到页面**：多选模式下点击已选照片可启动拖拽（拖全部选中），drop 后捕获阶段 stopClick 防选择切换
- **CI Test 失败修复**：`releasePhotoUrl` 10s 延迟回收与测试断言不同步——测试改 `vi.useFakeTimers()` + `advanceTimersByTime(10_000)`
- **弹窗 Enter 快捷键统一**：[Modal.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/Modal.tsx) 新增 `onConfirm`/`confirmDisabled`；新增 [useDialogHotkeys.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/useDialogHotkeys.ts) 覆盖自实现弹窗；textarea/contenteditable 智能过滤；删除类弹窗不加 Enter 防误删
- **照片列表选中态靠边裁剪修复**：外层 border + 内层 `inset shadow` 纯内描边（永不超出边界被 overflow 裁剪），ITEM_GAP 6→8
- **导入照片自动展开日期分组**：[PhotoPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PhotoPanel.tsx) `prevPhotoIdsRef` diff 新增照片，按其日期算 monthKey/dayKey 自动展开（首次挂载不展开、删除不触发）
- **统一 Header 高度**：`--layout-toolbar-height` 48→56px 与主页一致，红绿灯/内容中心统一 28px

---

## 2026-08-05 · 日志压缩归档（08-05 阶段，4 条日志摘要）

> 本段为 2026-08-05 当日 4 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。当日主题：macOS 平台支持 + 内容感知裁切修复。

- **新增 macOS 平台支持**：Tauri 不支持交叉编译，完成代码层适配（构建需 macOS 环境）。[Cargo.toml](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/Cargo.toml) Windows crate 移到 `cfg(windows)` + 新增 `trash="5"`；[tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json) targets "all" + `titleBarStyle:"Overlay"` + macOS ≥10.15；[lib.rs](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/src/lib.rs) 6 个命令补 macOS 分支（HEIC 用 sips、打印机 lpstat、PDF 打印 lpr、机器指纹 ioreg、试用锚点 defaults/plist、回收站 trash crate）；新增 [platform.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/platform.ts)；WindowControls macOS 隐藏、AppHeader 左侧 72px 让位
- **修复内容感知裁切失效（所有照片居中，6 重缺陷）**：① face-api 模型文件缺失 404 → 下载 tiny_face_detector 模型到 public/models/face-detection/；② 分析完成后不触发重渲染 → `ensurePhotoAnalyzed` 改返回 Promise + `contentVersion` state；③ 失败静默 → 补 logger；④ SmartLayoutView 与全局缓存不通 → 预计算结果同步写 `photoContentCache`；⑤ Worker 渲染路径拿不到主线程缓存 → `contentInfoMap` 参数穿透 thumbnail.worker；⑥ 导出不预热 → exportEngine `preheatContentAnalysis` 并发 6 路
- **修复竖图全身人像放横图槽位人脸被裁**：① TinyFaceDetector inputSize 224→416（小脸低于 32px 检测下限）+ 全身竖图上半 40% 分区二次检测；② 新增 Sobel 梯度 8×8 网格质心主体估算 `computeSubjectFocusByEnergy`（覆盖漏检/无人脸场景，~5-12ms）；③ 失败结果与有效结果分缓存（`failedAnalysisCache` 30s 退避 3 次），渲染条件放宽为纯焦点偏离
- **修复拖拽放置照片后偏移抖动**：入场动画 offset=(imgW/2) 与智能定位 imgX 变化不同步导致两次抖动；`pendingAnalysis` Set→Map（多调用者共享同一 Promise），动画启动前 await 分析完成（缓存未命中先 opacity=0 等 onReady，200ms 超时兜底），startScale 0.6→0.7

---

## 2026-08-04 · 日志压缩归档（08-04 阶段，11 条日志摘要）

- **照片拖拽入场动效 + 编辑后偏移修复**：新增 Konva.Tween 入场动效（opacity 淡入 + 0.6→1 BackEaseOut Q弹）；动画期间中心缩放临时 `offset=imgW/2`，`onFinish`/cleanup 必须 `resetToProps()` 恢复 react-konva prop 缓存同步，否则编辑 pan 更新后照片偏移 `imgW/2`；动画 effect 用 `useLayoutEffect` 防先 paint 后置 opacity(0) 爆闪。涉及 [CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)、[useDragDrop.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useDragDrop.ts)（`markPhotoJustPlaced`，仅拖拽 drop 触发）
- **Tauri unlisten 报错**：unlisten 在组件卸载后调用报 `Cannot read properties of undefined (reading 'handlerId')`；新增 `safeUnlisten`（[tauri.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/tauri.ts)）+ disposed flag，App/AppHeader/WindowControls/FullscreenView/EditorView 五处统一使用
- **CSP 违规根治**：`fetch(dataURL)` 违反 connect-src（控制台 8958+ 报错）；统一原则：**data: URL 用 `Image.src`/`atob` 解码，绝不用 fetch()**；blob:/asset:/http(s): 仍走 fetch。修复 imageBitmapLoader / gridThumbnailRenderer（两处）/ exportEngine（`dataURLtoBlob`）/ CanvasPhotoRenderer
- **贴纸 LRU revoke 导致加载失败**：[useStickerSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/useStickerSrc.ts) 缓存 blob URL 会被 LRU 淘汰 revoke → 改缓存 **dataURL**（字符串不可撤销，淘汰无需释放）；主页贴纸库新增多选删除（[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)）
- **贴纸列表懒加载 + page 模式裁剪恢复**：[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx) IntersectionObserver（rootMargin 200px）离开视口卸载 `<img>` 释放解码位图；恢复 Canvas 的 `pageDisplayMode` clipFunc 裁剪（此前误删订阅导致全显/页面按钮失效）
- **贴纸拖放三连修复**（[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)）：①坐标计算必须用 `stageRef` 容器 rect（containerRef 含居中偏移算错）；②drop 判断用 notify 传递的 state（不依赖 ref）；③接受范围扩大到整个工作区、落点不 clamp（灰色区域可放置可见）
- **贴纸拖拽卡顿根治**：根因 `stickerDragPreview` 进 effect 依赖，每 mousemove 拆销/重建订阅（60+ 次/秒 Canvas 重渲染）→ 改 `stickerDragStateRef` + `stickerPreviewRef` DOM 直操 transform + 仅 active boolean 触发 2 次重渲染
- **贴纸缩放原点**：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx) `x/y` 是**中心坐标**，缩放必须「锚点页面坐标反算新中心」：`anchorPage = center + R(θ)·anchorLocal`，缩放后 `newCenter = anchorPage − R(θ)·newAnchorLocal`（角点对角固定等比、边点对边固定单维度）
- **blob URL/ImageBitmap 资源三项修复**：①`releasePhotoUrl` 改 **10s 延迟回收**（in-flight fetch 不再 ERR_FILE_NOT_FOUND，宽限期内 acquire 可复活）；②ImageBitmap 缓存 16→40、evict 不再 `close()`（防 detached）+ 渲染前 `isImageReady` 校验（[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)）；③`swapPagePhotoPlacements` 移除仅 Google Photos 页限制（[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)）

---

## 2026-08-03 · 日志压缩归档（08-03 阶段，13 条日志摘要）

- **模板贴边根治（强制贴边+尺寸填满）**：移除归一化方案（会缩小跨行大图尺寸），改为基于原始 bbox 边缘的强制贴边——贴两对边时尺寸填满安全区、贴单边时仅平移，解决 slotGap≥10 时 L 型六图左大图底部不贴边。涉及文件：[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)
- **模板间距 bbox 膨胀根治（归一化到原始 bbox）**：根因为 slotGap 调整后 bbox 膨胀导致缩放因子变小、照片位被压缩；新增归一化逻辑将调整后坐标按比例映射回原始 bbox 范围，使 bbox 始终等于原始值。涉及文件：[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)
- **EXE 图标模糊修复 + 大间距贴边**：根因为任务栏优先使用窗口图标（generate_context! 宏嵌入 32x32）；修复三步——bundle.icon 数组 ICO 在前、ICO 尺寸从大到小排列、setup 中手动 set_icon 128x128；同时增大 isPageSpread snapPx 到 max(20, CW*0.06)。涉及文件：[tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json)、[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs)、[lib.rs](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/src/lib.rs)、[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)
- **大间距贴边 + icon 两阶段缩小**：小尺寸（≤48px）图标改为两阶段缩小（先到 4x 中间尺寸再到目标），ICO 添加 20/40 中间尺寸覆盖 125%/150% DPI。涉及文件：[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs)、[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)
- **模板安全区填满 + icon 彻底优化**：isPageSpread 改用原始 bbox（slotGap 调整前）判断缩放策略；图标改用 BMP-in-ICO + normalise/sharpen + cargo clean 清缓存。涉及文件：[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)、[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs)
- **模板间距修复 + icon 清晰度优化**：isStagger 判断从标签改为实际重叠检测（新增 slotsHaveOverlap），让无重叠平铺模板正确应用 slotGap；prepareSource 检测 logo 占比≥95% 时跳过 padding，小尺寸加 sharpen。涉及文件：[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)、[make-icons.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/make-icons.cjs)
- **贴纸浮动工具栏位置优化**：原实现跟随贴纸本地"上方"旋转致 180° 时工具栏跑到下方遮挡图案；改用旋转后 AABB 包围盒顶部计算，始终位于屏幕视觉上方 48px。涉及文件：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)
- **贴纸旋转中心修复 + 工具栏自适应**：StickerNode Group 存在双重偏移（中心坐标+半宽=右边）致旋转枢轴落在右下角；修复为 Group 无 offset 直接用中心坐标，exportEngine/thumbnailCore 改两段 translate。涉及文件：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)、[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)、[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)、[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)
- **激活验证修复 + 相册数量限制**：应用内置公钥与用户密钥不匹配致验签失败，更新 LICENSE_PUBLIC_KEY.n；canCreateProject 原逻辑试用期外直接 return false（0 个相册也不让建），修复为未激活时最多 3 个。涉及文件：[publicKey.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/license/publicKey.ts)、[licenseService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/license/licenseService.ts)
- **贴纸旋转中心真正根因修复（Konva offset 变换顺序）**：Konva getAbsoluteTransform 返回变换包含 offset（M=T(x,y)*R(θ)*T(-offsetX,-offsetY)），本地原点(0,0)变换后偏到左上角；修复为传 (pw/2, ph/2) 给 point()。涉及文件：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)
- **贴纸旋转中心根因修复 + 喜欢按钮改主题色**：getAbsolutePosition() 追加被 rotation 旋转的 offset 平移致返回偏移点；改用 getAbsoluteTransform(stage).point({x:0,y:0}) 不用缓存；喜欢按钮改淡蓝紫主题色。涉及文件：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)、[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)、[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)
- **贴纸缩放/旋转/工具栏三大交互修复**：新增 screenDeltaToLocal 将屏幕 delta 反向旋转到本地空间解决旋转后缩放方向错乱；旋转中心改为从父 Group x()/y()/scaleX() 手动计算（避免 getAbsolutePosition 缓存过期）；工具栏移除关闭按钮。涉及文件：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)、[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)
- **贴纸"收藏"改"喜欢"**：按钮尺寸统一（主页 32px/编辑器 24px），已喜欢改仅 hover 显示，图标改爱心红色；i18n 文案收藏→喜欢、Favorites→Liked（key 不变只改 value）。涉及文件：[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)、[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)、[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)、[en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json)

---

## 2026-08-02 · 日志压缩归档（08-02 阶段，1 条日志摘要）

- **贴纸功能全面优化（删除修复 + 收藏功能 + 重命名 + 旋转中心修复）**：删除按钮 `onMouseDown` 改 `onPointerDown` + z-index 升 `z-20` 修复删除失效；DB v6 迁移新增 `favorite` 字段及 `updateSticker`/`renameSticker`/`toggleStickerFavorite` 方法；主页/编辑器贴纸库新增收藏筛选与左上角收藏按钮、卡片重命名（Enter/Escape/blur）；旋转中心根因为 `e.target.getParent()` 返回旋转手柄子 Group 而非贴纸主 Group，新增 `mainGroupRef` 修复。涉及文件：[StickerPanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/StickerPanel.tsx)、[db/index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/db/index.ts)、[stickerService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/stickerService.ts)、[StickerGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/StickerGallery.tsx)、[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)

---

## 2026-08-01 · 日志压缩归档（08-01 阶段，1 条日志摘要）

- **修复首次创建相册添加页面时 React Error #310 崩溃**：根因为 Canvas.tsx 早返回之后调用 `globalLayerElements` 的 `useMemo`，空相册与有页面状态 Hook 数量不一致（N vs N+1）；修复为将 `useMemo` 改为 IIFE（不参与 Hook 计数）；HomeView 新建相册时显式重置 `currentPageIndex` 避免越界索引。涉及文件：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)、[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)

---

## 2026-07-31 · 日志压缩归档（07-31 阶段，8 条日志摘要）

> 本段为 2026-07-31 当日 8 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。

### 1. 多选线框实时跟随 + 多选阴影工具 + 一键成册主体感知分析
- 多选缩放时 slot-box 线框实时跟随：将 slot-box 的 x/y/width/height 改为 sx/sy/sw/sh（预览几何）
- 多选悬浮工具栏新增阴影切换按钮：新增 batchSetShadow 批量设置方法，统一一次 pushSnapshot
- 一键成册主体感知已确认完整支持（布局阶段评分加分 + 渲染阶段 computeSmartObjectPosition + 预览阶段对齐）
- 涉及：[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)、[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)

### 2. 自定义模板分组筛选 + 统一多选 + 框选缩放漏白修复 + 弹窗位置调整
- 新增运行时自定义模板注册机制：registerCustomTemplates / getCustomTemplateRegistry / findTemplateById
- 编辑模式 TemplatePanel 支持自定义模板"自定义"分组筛选
- 消除 multiSelectedSlots 双轨状态，框选 hook 产出 multiSelectedElements 与 Ctrl+click 统一
- 修复框选多选缩放照片漏白：commitPreview 中对每个尺寸变化的槽位用 computePanForResizedSlot 重算照片 pan 值
- "重排照片"/"微调排版"对话框 top 从 72px → 112px
- 涉及：[useMarqueeGroupSelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useMarqueeGroupSelect.tsx)、[useMultiElementGroupSelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useMultiElementGroupSelect.tsx)、[TemplatePanel.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/TemplatePanel.tsx)

### 3. 间距自适应 + 叠加布局模板 + 渲染层级一致性
- 平铺模板 isTiled 判定 + gapThreshold=Infinity，所有相邻槽位间距严格按 slotGap 调整
- 新增 20 个模板（12 个叠加 overlay+white-space + 8 个常规）
- isStagger 检查新增 tags.includes('overlay')：跳过 slotGap 调整 + 等比缩放，渲染层级一致性
- validate-templates.cjs 支持 overlay 标签
- 涉及：[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)、[sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)、[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)

### 4. 缩略图灰色根因修复（Worker 崩溃 + isSpread 不一致）
- 根因1：sharedRender.ts 顶层 import useEditorStore 导致 Worker 导入链崩溃
- 修复1：移除 sharedRender 对 useEditorStore 依赖，getSlotRect 不再从 store 回退读取 margin
- 根因2：getSlotRect isSpread 与 helpers.calcMarginOverrides 不一致
- 修复2：isSpread 增加 !isStagger 检查
- Worker 超时保护 + 崩溃监听 + RENDER_VERSION 5→6
- 涉及：[sharedRender.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/sharedRender.ts)、[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)

### 5. 缩略图灰色修复 + 主页筛选器统一 + 多行并列模板 + 主图居中修复
- 缩略图灰色根因：Web Worker 无法读取主线程 pageMargin，ThumbnailWorkerRequest 新增 margin 字段，RENDER_VERSION 4→5
- 主页 CountFilter 类型扩展为 'all'|1..8|'9+'，与编辑器一致
- 新增多行并列布局模板（3图三行/4图四行/5图五行/6图多行共12个）
- 主图居中根因：calcMarginOverrides 对 magazine 模板执行 slotGap 调整破坏主图位置；isStagger 扩展为同时检查 tags.includes('magazine') 和 subCategory === 'magazine'
- isPageSpread 增加 !isStagger 条件，使用等比缩放保持模板原始比例
- 涉及：[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)、[TemplateGallery.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/TemplateGallery.tsx)、[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)

### 6. 模板边距自适应 + 问题模板重设计 + 全局重叠验证
- 页面铺开型检测（bbox 宽高均 ≥85%）使用独立 X/Y 轴缩放填满安全区
- 四向吸附（8px≈4mm 容差），瀑布流跳过对齐后处理
- 过窄细条模板重设计为 2-3 行网格（上英雄7-10/L型7-10/上大下6-9/杂志英雄7-10）
- 中心环绕标准化：36×36 中心 + 36×26/26×36 四边 + 26×26 四角，3% 间距
- 新增 validate-templates.cjs 全局验证脚本（重叠/越界/间距检查）
- 涉及：[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)、[generated-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/generated-templates.ts)、[validate-templates.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/validate-templates.cjs)

### 7. 生成模板深度清理 + 模板内置照片位支持删除
- 模板库从 155 精简至 64：剔除散落重叠无呼吸感 + 阶梯式重叠 + 过窄细条
- AlbumPage 新增 hiddenTemplateSlotIds?: string[]，resolveTemplate 过滤隐藏槽位
- 移除一键成册页面"至少保留一张照片"限制，支持删至空白
- 涉及：[generated-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/generated-templates.ts)、[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)、[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts)

### 8. 模板库清理 + 阴影改为用户可配置 + 移除方向匹配
- 剔除 6 套不合理模板（six-top-big-bot5/seven-zigzag/eight-top-big-bot7 等）
- 移除方向匹配自动旋转：删除 slotOrientation() 和 pickAutoRotation()
- 阴影改为 per-placement 用户可配置：PhotoPlacement 新增 shadow? 字段 + toggleShadow action
- 悬浮工具栏新增阴影按钮（置底和清除之间），开启时品牌色高亮
- 涉及：[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)、[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)

---

## 2026-07-30 · 日志压缩归档（07-30 阶段，6 条日志摘要）

> 本段为 2026-07-30 当日 6 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。

### 1. 渲染引擎四阶段优化（模板库+筛选+等比缩放+主体感知）
- 阶段1：完善基础模板库，Template 类型扩展 subCategory 字段，新增 9-12 图基础平铺模板
- 阶段2：扩展筛选器 CountFilter 类型 'all'|1..8|'9+'，新增 matchCountFilter 函数
- 阶段3：修复模板应用等比缩放问题，scale=min(sx,sy) 等比缩放+居中偏移保持模板原始比例
- 阶段4：渲染引擎优化——主体感知缓存层（photoContentCache + ensurePhotoAnalyzed + computeSmartObjectPosition）+ 方向匹配自动旋转 + 槽位阴影
- 涉及：[index.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/index.ts)、[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)、[content-aware.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/content-aware.ts)、[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)

### 2. 版式库去重优化 + Mixbook 风格叠加布局补充
- 修复 seven-2x3-plus1 与 seven-3x2-plus1 槽位完全重复
- 生成器三重去重机制：5% 网格精确签名 + 10% 网格视觉签名 + 结构模式签名
- 新增 2 种 Mixbook 风格创意叠加生成器：genStaircaseStack 阶梯叠层 + genShiftedCollage 错位拼贴
- 去重效果：218→155 个生成模板
- 涉及：[gen-templates.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/gen-templates.cjs)、[generated-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/generated-templates.ts)

### 3. 版式库扩展至 357 种 + 网格视图缩放快捷键修正
- 网格视图缩放快捷键 Alt+滚轮 → Ctrl+滚轮
- 版式库扩展至 357 种：实现 11 种参数化布局生成器
- Template 类型新增 tags?: string[] 承载细粒度风格标签
- 新模板 ID 统一使用 gen-{数量}p-{序号} 格式，与现有 ID 无冲突
- 涉及：[gen-templates.cjs](file:///f:/N-编程/MenBook开发项目/MemBook/scripts/gen-templates.cjs)、[generated-templates.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/types/generated-templates.ts)、[GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx)

### 4. 贴纸旋转中心修复 + 网格Ctrl+滚轮缩放 + 全屏滚轮缩放修复
- 贴纸旋转中心修复：根因是 getClientRect() 返回旋转后 AABB 包围盒，flip 时中心偏移；改用 groupNode.getAbsolutePosition()
- 贴纸浮动工具栏偏移 36→48px
- 网格视图 Ctrl+滚轮缩放：新增全局 window 级 Ctrl+wheel preventDefault 拦截器
- 全屏视图滚轮缩放修复：useWheel deps 改为 [open]，open 变 true 时重新附加监听器
- 涉及：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)、[GridView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/GridView.tsx)、[FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx)

### 5. 贴纸控制点优化 + 智能编排改名一键成册 + 删除最后一张照片崩溃修复
- 贴纸控制点全面优化：角点圆形（Circle radius=6px），边点长方块（Rect 3:1），与照片槽一致；角点等比缩放（对角点为原点），边点单维度缩放
- 旋转手柄改为白色圆底 + ↻ 图标，单击旋转 90°，拖拽 15° 吸附
- "智能编排"统一改为"一键成册"（zh-CN/en-US/Readme/tauri.conf.json）
- 删除最后一张照片 React error #300 修复：增加守卫检查 + lastPhotoCannotDelete i18n
- 涉及：[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)、[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)、[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts)

### 6. 编辑器第一页偶现空白彻底修复
- BUG 1：blobUrlCache 容量过小（30），相册超过 30 张照片时 LRU 淘汰第一页照片 → BLOB_URL_CACHE_CAPACITY 30→200
- BUG 2：handleOpenProject/EditorView 用 readPhotoFromDB（refCount=0）而非 acquirePhotoUrl（refCount=1）→ 改用 acquirePhotoUrl pin 住 URL
- 涉及：[import-store.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/storage/import-store.ts)、[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)、[EditorView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/EditorView.tsx)
---

## 2026-07-29 · 日志压缩归档（07-29 阶段，15 条日志摘要）

> 本段为 2026-07-29 当日 15 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。

### 1. 照片显示与 Blob URL 生命周期修复
- 全屏黑屏修复：FullscreenView loading 初始值 false→true + 渲染 effect 加 getCachedThumbnailUrl 快速路径
- 主页第一页封面偶现空白：PageThumbnail 丢失 albumSize prop 回退全局 store 不匹配；CanvasPageThumbnail 新增 800ms 重试机制（最多 2 次）；RenderOptions 增加 albumSize 字段优先于全局 store
- 重进编辑器照片不显示：usePhotoSrc 统一走 acquirePhotoUrl（refCount+1 pin 住 URL），消除直接使用 photo.src（refCount=0 被 LRU 回收后不恢复）的路径
- 工作区/缩略图偶现不显示：CanvasPhotoRenderer.assetUrlToPath 去除 Windows 前导斜杠（与 ensureCanvasSafeUrl 一致）；usePhotoSrc direct 模式 preview/full 档优先 acquirePhotoUrl；IDB 缓存 key 加入 RENDER_VERSION 使旧版残缺缓存失效
- 全屏空格切换失效：usePanZoom 在 fullscreen-open 类标记时跳过 Space 拦截，让 FullscreenView window 级监听器收到事件
- 涉及：[FullscreenView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/FullscreenView.tsx)、[usePhotoSrc.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/hooks/usePhotoSrc.ts)、[CanvasPhotoRenderer.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/CanvasPhotoRenderer.tsx)、[PageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/PageThumbnail.tsx)、[CanvasPageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CanvasPageThumbnail.tsx)、[usePanZoom.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/usePanZoom.ts)

### 2. 缩略图渲染管线与缓存一致性修复
- Worker 完整性检查：drawnPhotoCount === undefined 旁路把残缺渲染当完整；修复为 !== undefined && >= expected；RENDER_VERSION 递增使旧版残缺 IDB 缓存失效
- Worker 路径残缺 dataURL 不写 LRU：与主线程一致，不完整时跳过 thumbnailCache.set + saveThumbnail
- LRU cache key 加入 content hash：原 key 不含 hash 导致贴纸添加后命中旧缓存；三处 key 构造全部修复（getCachedThumbnailUrl/renderPageThumbnail/renderPageThumbnailInWorker）
- invalidatePageThumbnail 改为前缀匹配删除：覆盖所有内容版本的缓存条目
- drawTemplateSlot 缺少 ctx.beginPath()：多个槽位路径叠加变单色；cornerRadius<=0 时不绘制 → 添加 else 分支用 fillRect/strokeRect；SLOT_PALETTE_VERSION 5→6 强制清除旧版 IDB 缓存
- 缩略图统一为 Canvas 2D：新增 CanvasPageThumbnail 通用组件（基于 renderPageThumbnailInWorker 渲染所有元素类型，内置 LRU+IDB 二级缓存）；BottomNav 和 Home PageThumbnail 从 CSS 渲染改为 Canvas 2D
- 缩略图渲染一致性深度修复：thumbnailCore.drawPageToCanvas 重构为与编辑器 globalLayerElements 一致的统一图层排序（5 类元素合并按 zIndex+typeOrder 排序）；新增 drawBrushStroke/drawStickyNote/drawSticker 绘制函数；页面边界 ctx.clip 裁剪；gridThumbnailRenderer 新增 preloadStickers/releaseStickerImages 贴纸预加载；Worker 协议扩展 stickerBitmaps 字段；computeContentHash 补充 szi/bs/sn/st/te.zIndex 五个字段
- 涉及：[gridThumbnailRenderer.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/gridThumbnailRenderer.ts)、[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)、[thumbnail.worker.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnail.worker.ts)、[CanvasPageThumbnail.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/common/CanvasPageThumbnail.tsx)、[BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)、[PageCard.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageCard.tsx)、[templatePalette.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/constants/templatePalette.ts)

### 3. 渲染一致性（编辑器 ↔ 导出 ↔ 缩略图）
- 贴纸/便利贴三段 translate 完整复刻 Konva Group transform：T(x,y) * R(θ) * T(-offsetX,-offsetY)，修复位置偏移和旋转中心错误
- 固定像素值按 mmToPx/MM_TO_PX 比例缩放（cornerRadius/shadowBlur/shadowOffsetY/padding/tape 尺寸）
- Bug44 渲染差异修复：竖排文字（rotation===-90 逐字竖排从右到左）、荧光笔 globalCompositeOperation='multiply'、便利贴内边距 8→6px、文字换行宽度 tw→tw-8、缩略图背景支持渐变/纹理
- 导出引擎 drawPage 页面边界裁剪：ctx.save/rect/clip 包裹所有元素绘制，装饰元素超出页面边界被裁剪
- 涉及：[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)、[thumbnailCore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/thumbnailCore.ts)、[printEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/printEngine.ts)

### 4. CSP 安全策略修复
- CSP connect-src 添加 asset: / http://asset.localhost / https://asset.localhost：解决 fetch(asset://) 被 CSP 阻止导致缩略图/工作区照片不显示
- CSP connect-src 添加 blob:：解决 GP 模式删除后重新排版触发 fetch(blob:url) 被拦截 + React error #300 崩溃
- 涉及：[tauri.conf.json](file:///f:/N-编程/MenBook开发项目/MemBook/src-tauri/tauri.conf.json)

### 5. 智能编排与页面布局修复
- tierPatterns 崩溃：generateMultipleLayouts 第一版 score>=95 提前 break 导致 versions 数组长度 1，SmartLayoutView 默认 selectedPreviewIndex=1 越界；修复为 versions.length>=topK && score>=95 才 break + UI index clamp + effect guard
- Bug41/45 GP 模式 extraSlots 删除：removeSlotFromPage 将 extraSlot 检查移出 GP 条件分支，统一从 extraSlots/placements/slotOrder/slotOverrides/slotZIndices 清除
- 涉及：[google-photos-layout.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/engine/google-photos-layout.ts)、[SmartLayoutView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/SmartLayoutView.tsx)、[pageLayoutService.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/services/pageLayoutService.ts)

### 6. 编辑器多选系统与交互优化
- 跨类型统一多选系统：selectionSlice 新增 SelectedElement 类型 + multiSelectedElements 数组 + toggleMultiSelect/clearMultiSelect/setMultiSelectedElements；Canvas 所有元素 onClick 加 Ctrl+click 分支；多选浮动工具栏（同类型≥2 时显示置顶/置底/删除）；useCanvasKeyboard Escape/Delete 多选支持；StickerNode onSelect 修复未传事件
- Bug42 多选包围盒：新增 useMultiElementGroupSelect hook（统一收集 slot/text/sticky/sticker 几何 + 8 控制点 + 移动/缩放预览松手提交）；单选跨类型互斥（setSelectedSlot 等清空所有其他类型 + 多选状态）
- Bug43 照片位层级顺序：useDragDrop 候选槽位排序加 slotOrder 索引降序作为 tiebreaker（zIndex 相同时选渲染在后的）
- Ctrl+C/V 完整槽位复制：BottomNav ClipboardData 新增 slotOverride + slotLayout；粘贴时无空槽位自动创建 extraSlots（偏移+顶层 zIndex）
- 添加照片位按钮：PageToolbar 新增按钮 + pageSlice.addPhotoSlot（页面中央 35%/35% 30%×30% + getGlobalMaxZ+1）
- 页外照片拖拽：useDragDrop 移除 overPage 前置检查 + 重叠槽位按 zIndex 降序选顶层
- 照片位删除修复：removeSlotFromPage 区分 extraSlots（完整删除）与模板槽位（仅清照片+toast 提示）
- 页面显示模式切换：uiStore 新增 pageDisplayMode（full/page）+ Canvas 用 Konva Group clipFunc 裁剪 + PageDisplayModeToggle 组件
- 涉及：[selectionSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/selectionSlice.ts)、[Canvas.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/Canvas.tsx)、[useMultiElementGroupSelect.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useMultiElementGroupSelect.tsx)、[useDragDrop.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useDragDrop.ts)、[useCanvasKeyboard.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/useCanvasKeyboard.ts)、[BottomNav.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/BottomNav.tsx)、[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)、[PageToolbar.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageToolbar.tsx)、[PageDisplayModeToggle.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageDisplayModeToggle.tsx)、[uiStore.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/uiStore.ts)、[StickerNode.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/canvas/StickerNode.tsx)

### 7. 页面设置/预设 UI 优化
- Bug47 页面设置弹窗：PageSettings 尺寸选项从 ALBUM_SIZES(5个) 切换为 SIZE_PRESETS(14个)；新增 showSizeOptions 折叠态（默认只显示当前尺寸+"修改页面"按钮）；弹窗宽度 440→560px
- Bug46 创建新相册快捷预设：CreateDialog 快捷预设默认值改为 STANDARD_PRESET（margin=15, gap=5, cornerRadius=8）；HomeView handleCreateAlbum 新增 setApplyMarginToAll(true)
- 涉及：[PageSettings.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/editor/PageSettings.tsx)、[CreateDialog.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/home/CreateDialog.tsx)、[HomeView.tsx](file:///f:/N-编程/MenBook开发项目/MemBook/src/components/views/HomeView.tsx)

### 8. 构建、代码审查与文档矫正
- selectionSlice 未使用 SelectedElement 导入导致 TS6196 错误修复
- EXE 安装包构建成功（多次构建，13.06~13.86 MB）
- 全项目代码审查（8 维度）：P1 问题 6 项（store 操作 DOM、engine 副作用、跨 store 编排、跨 slice import、pushSnapshot 缺失、as any 类型缺失）；P2 问题 13 项；P3 问题 5 项；详细清单写入代码开发规范附录
- P1-5 修复：pageSlice.setCurrentPage 懒计算分支补 pushSnapshot
- P1-4 修复：getGlobalMaxZ/getGlobalMinZ/collectAllElements 从 decorationsSlice 迁移至 helpers.ts
- Readme.md 矫正：新增功能描述（多选/页面显示模式/添加照片位）、删除不存在的 WiX/MSI、路径修正、缓存容量更新、CSP blob 硬约束
- 代码开发规范.md 矫正：技术栈版本 React 18→19 + Konva 10/Zustand 5/Tailwind 4/Dexie 4、新增跨类型多选协议 4.4 节、Canvas 统一 zIndex 系统和页面显示模式、缓存容量更新、文件大小更新、CSP blob 硬约束、新增技术债务附录
- 涉及：[selectionSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/selectionSlice.ts)、[pageSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/pageSlice.ts)、[helpers.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/helpers.ts)、[decorationsSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/decorationsSlice.ts)、[placementSlice.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/store/editorStore/placementSlice.ts)

---

## 2026-07-28 · 日志压缩归档（07-28 阶段，18 条日志摘要）

> 本段为 2026-07-28 当日 18 条开发日志的压缩归档，保留主题与核心结论，详细过程已省略。

### 1. 导出引擎修复（贴纸渲染 + 便利贴样式 + 统一图层排序）
重构 `drawPage` 为 async，统一图层排序（槽位+笔触+文字+便利贴+贴纸 5 类合并 RenderItem[]，按 zIndex+typeOrder 排序）。新增贴纸渲染 `drawSticker`，修复便利贴 style 字段（square/rounded/tape/shadow）分支渲染。`renderPage` 改 async，所有调用方加 await。
- 涉及：[exportEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/exportEngine.ts)、[printEngine.ts](file:///f:/N-编程/MenBook开发项目/MemBook/src/utils/printEngine.ts)

### 2. EXE 安装包构建（修复 4 个 TS 错误 + 成功打包）
修复 StickyNoteNode 漏导入 memo、Canvas.tsx 回调参数隐式 any。构建成功生成 MemBook_1.0.0_x64-setup.exe（12.65 MB）。配置 TAURI_BUNDLER_TOOLS_GITHUB_MIRROR 防超时、TAURI_BUNDLER_TIMEOUT=360、bundle targets=["nsis"]。

### 3. 代码开发规范 + P0/P1/P2/P3 修复 + 6 项体验优化
新增代码开发规范文档（12 章节）。3 个 Canvas 节点组件包裹 React.memo。6 项优化：编辑器移除语言切换、EN 切换残留中文修复（新增 200+ i18n key）、照片槽位与装饰元素统一层级系统（slotZIndices + globalLayerElements 单一渲染队列）、主页面板顺序调整为相册/布局/贴纸/整理+项目→相册改名、顶部导航层级修复（--z-toolbar=60）、文字/便利贴工具面板合并。

### 4. i18n 全项目国际化（12 条日志合并）
完成全项目 i18n 改造，覆盖：编辑器核心组件（GridView/PhotoPreview/LayoutAdjust 等 15 文件）、对话框组件（watermark/pageSettings/print/createTemplate）、整理工具（organize/*）、SmartLayoutView、hooks/services/license（6 文件）、编辑器与通用组件（8 组件）、全局化修复（OrganizePanel + TemplateGallery）、全局生效修复+残留 P0、BottomNav/Canvas、PageToolbar/ToolsPanel、StickerPanel/Gallery、LeftPanel/EditorView、需求审查 P0/P1。确保所有组件用 useTranslation() 订阅语言状态。
- 涉及：[zh-CN.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/zh-CN.json)、[en-US.json](file:///f:/N-编程/MenBook开发项目/MemBook/src/i18n/locales/en-US.json) 及全项目组件

### 5. 任务栏图标模糊修复 + favicon 视图框修复
修复任务栏 icon 模糊问题，修复 favicon SVG 视图框。

---

